{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp"
      ],
      "description": "Playwright 浏览器自动化 — 截图、测试、页面交互"
    },
    "gitee": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-gitee"
      ],
      "description": "Gitee Issues/PRs 操作（官方 mcp-gitee）— 需系统环境变量 GITEE_ACCESS_TOKEN（setx·子进程直接继承）",
      "disabled": false
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-github"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PAT}"
      },
      "description": "GitHub Issues/PRs 操作 — 已停用（origin 迁 Gitee·2026-08-15·仅留配置备查）",
      "disabled": true
    },
    "vision-bridge": {
      "command": "python",
      "args": [
        ".claude/mcp_servers/vision_bridge_server.py"
      ],
      "description": "视觉桥接 — 调用火山引擎 Ark Vision API 识图，让不支持图片的模型也能看图"
    }
  }
}