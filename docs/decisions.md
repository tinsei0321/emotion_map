# 技术决策记录 (Architecture Decision Records)

> 记录项目中的关键技术决策、背景、选项对比及最终选择。  
> 格式：ADR (Architecture Decision Record)

---

## 📋 决策模板

```markdown
### ADR-00X | YYYY-MM-DD | 决策标题

**状态**：提议 / ✅ 已采纳 / ❌ 已废弃 / 🔄 已替代

**背景**：
为什么需要做这个决策？

**选项**：
| 选项 | 优点 | 缺点 |
|------|------|------|
| A    | ...  | ...  |
| B    | ...  | ...  |

**决策**：
选择 X，因为...

**后果**：
- 正面影响：...
- 负面影响：...
- 风险：...
```

---

## 决策列表

### ADR-001 | 2026-05-30 | 选择 SnowNLP 作为 MVP 情绪分析引擎

**状态**：✅ 已采纳

**背景**：
需要在 MVP 阶段快速实现文本情绪分析。备选方案包括 SnowNLP、Senta（百度）、BERT-wwm-ext。

**选项**：

| 选项 | 优点 | 缺点 |
|------|------|------|
| SnowNLP | 轻量(~10MB)、离线、pip 安装、中文友好 | 精度一般、仅正面/负面分数、无情绪分类 |
| Senta (百度) | 精度高、支持细粒度 | 需 PaddlePaddle 框架、模型较大、部署复杂 |
| BERT-wwm-ext | 精度最高 | 需 GPU、模型巨大(~400MB)、零基础难上手 |

**决策**：
选择 SnowNLP。MVP 阶段追求"快速可用"而非"精度最优"。通过 `AnalyzerBase` 抽象接口预留了未来升级路径。

**后果**：
- ✅ 分钟级完成环境搭建和首批数据分析
- ✅ SnowNLP 的不准确性可转化为语料库本地化研究的切入点
- ⚠️ 精度限制可能导致部分分析结果偏差，需要在展示层加以说明
- 🔜 未来通过 `LLMAnalyzer` 接口平滑升级到溯佰科 LLM

---

### ADR-002 | 2026-05-31 | 三段式工作框架

**状态**：✅ 已采纳

**背景**：
项目发起人是零基础规划师，需要在"学 Python"和"推进课题"之间平衡。

**决策**：
采用三段式递进框架：
1. **理论 + 技术概念**（理解 NLP 能做什么、技术栈是什么）
2. **软件环境 + 数据采集**（搭环境、跑通数据管道）
3. **数据治理 + 分析 + 可视化**（核心产出）

每段目标明确，"学以致用"——只学当前段需要的 Python，不追求精通。

**后果**：
- ✅ 降低了学习曲线，避免"学完再干"的拖延
- ✅ 每段都有可视化产出，保持动力
- ⚠️ 可能在某些底层原理上理解不够深入

---

### ADR-003 | 2026-06-09 | 重构为模块化架构（core/ + apps/ + SCRIPT/）

**状态**：✅ 已采纳

**背景**：
项目初期代码分散在多个测试脚本中（`test_scripts.py`、`streamlit_app_v2.py`、`test_scripts_heatmap.py`），存在大量重复逻辑。

**决策**：
采用三层模块化架构：
- `core/` — 可复用基础设施（config、data_loader、map_engine、ui_components、export）
- `apps/` — Streamlit 应用入口
- `SCRIPT/` — 分析引擎 + 测试脚本

**后果**：
- ✅ 消除了 80%+ 的重复代码
- ✅ 新增功能只需修改一个模块
- ✅ 测试脚本可以独立运行，不依赖 Streamlit
- ⚠️ 增加了模块间的导入依赖管理

---

### ADR-004 | 2026-06-09 | 使用天地图替代 OpenStreetMap 作为底图

**状态**：✅ 已采纳

**背景**：
Folium 默认使用 OpenStreetMap 瓦片，国内加载缓慢甚至失败。

**决策**：
使用天地图 WMTS 服务（影像 + 中文注记），通过 `config.py` 统一管理 Key 和 URL 模板。

**后果**：
- ✅ 国内访问稳定快速
- ✅ 中文注记对城市规划场景更友好
- ⚠️ 需申请天地图 Key（开发 Key 有调用限制）
- ⚠️ 部署到公网需申请企业级 Key

---

### ADR-005 | 2026-06-09 | Streamlit Dialog 弹窗 vs 侧边栏

**状态**：✅ 已采纳

**背景**：
全屏地图应用需要数据选择、概览等辅助面板。Streamlit 提供两种方案：侧边栏和 Dialog 弹窗。

**决策**：
采用 Dialog 弹窗（`@st.dialog`）。全屏地图模式下侧边栏会挤占地图空间，弹窗按需出现、用完即走。

**后果**：
- ✅ 地图保持 100% 全屏显示
- ✅ 弹窗交互直觉友好（弹出 → 操作 → 关闭）
- ⚠️ Dialog 是 Streamlit 1.35+ 的新特性，对旧版本不兼容
- ⚠️ 弹窗状态管理需通过 `st.session_state` 协调

---

### ADR-006 | 2026-06-11 | 建立项目文档体系

**状态**：✅ 已采纳

**背景**：
项目从原型阶段进入持续开发阶段，需要系统化的文档来记录思路、架构、决策和任务。

**决策**：
在 `docs/` 下建立四类文档：
- `dev-notes.md` — 开发笔记（过程记录）
- `architecture.md` — 架构设计（系统蓝图）
- `decisions.md` — 技术决策（ADR 记录）
- `todo.md` — 待办事项（任务追踪）

**后果**：
- ✅ 开发思路不再依赖记忆
- ✅ 新成员（或未来的自己）可快速理解项目
- ✅ 技术决策有据可查，减少重复讨论
- ⚠️ 需要保持文档更新，否则会变成"死文档"

---

### ADR-007 | 2026-06-12 | 系统架构层级优化：新增数据采集层 + 空间分析引擎重定义

**状态**：✅ 已采纳

**背景**：
项目架构最初为五层（应用层 / UI组件层 / 地图引擎层 / 分析引擎层 / 基础设施层 / 数据层）。随着对项目理解的深入，发现以下不足：
1. 缺少数据采集层——数据爬取是独立职责，不应混入其他层
2. "地图引擎层"概念过窄——未来需要缓冲区分析、空间聚合等 QGIS 级别功能
3. "分析引擎层"命名不够精确——该层实际承担 raw→L1(治理)→L2/L3/L4(分析) 的完整数据加工职责
4. 溯佰科定位错误——在代码文档中被描述为"LLM大模型"，实为城市规划时空大模型平台

**决策**：
1. **新增"数据采集层"（SCRAPER/）**，置于数据层之上，负责多源数据爬取（大众点评/美团/小红书/微博/12345），基于 Scrapy 框架，备用方案为购买数据
2. **"地图引擎层"→"空间分析引擎层"**，扩展职责为：底图渲染 + 空间可视化 + 空间分析（热点分析/缓冲区分析/行政单元聚合），MVP 阶段空间分析工具选型 geopandas + shapely 自研
3. **"分析引擎层"→"数据分析引擎层"**，明确其 L1(数据治理)→L2(SnowNLP)→L3(LLM)→L4(多维归因) 四级数据加工管道
4. **纠正溯佰科定位**：从"LLM大模型"改为"城市规划时空大模型平台（数据底座+GIS工具+NL工作台）"，情绪地图未来以 Agent 方式嵌入
5. **数据层**增加 L0~L4 五级数据分级概念，导出命名规范对应

**后果**：
- ✅ 架构层级从 6 层扩展为 7 层，职责更清晰
- ✅ 空间分析引擎层有了明确的功能路线图和工具选型建议
- ✅ 溯佰科定位纠正，避免未来开发方向偏差
- ✅ 数据分级(L0~L4)与导出命名规范一致，溯源清晰
- ⚠️ 数据采集层技术方案尚未最终确定（Scrapy vs 购买），需近期明确
- ⚠️ 空间分析核心功能（缓冲区分析/行政单元聚合）需编码实现

---

### ADR-008 | 2026-06-12 | 选择 Scrapy 作为数据采集框架

**状态**：✅ 已采纳

**背景**：
需要从大众点评、美团、小红书、微博、12345 等多个中文平台持续采集城市情绪相关文本数据。面临三个选项：自研脚本、Scrapy 框架、购买数据。

**选项**：

| 选项 | 优点 | 缺点 |
|------|------|------|
| 自研脚本 (requests+bs4) | 上手快、几十行能跑 | 每个网站单独开发、反爬/去重/断点续抓需手写 |
| Scrapy 框架 | 内置去重/限速/Pipeline、多源管理统一、社区成熟 | 学习曲线（需理解 Spider/Engine/Pipeline 概念） |
| 购买数据 | 零技术负担、合规 | 成本高、依赖供应商、数据定制性差 |

**决策**：
选择 Scrapy 框架。理由：
1. 5 个数据源需要持续采集，不是一次性任务
2. 内置的去重、断点续抓、自动限速等功能省去大量手写代码
3. Pipeline 架构与项目现有的 Pipe-and-Filter 设计一脉相承
4. 备用方案：若爬取严重受阻，购买数据作为兜底

**后果**：
- ✅ 项目搭建完成：9 个文件、标准 Scrapy 结构、首个 Spider（小红书）
- ✅ 测试验证：小红书搜索页 HTTP 200，SSR 数据可无登录提取
- ⚠️ 各平台反爬策略不同，需逐个适配
- ⚠️ 购买数据仍为备用方案，不排除未来切换

---

## 🔖 ADR 索引

| 编号 | 日期 | 标题 | 状态 |
|------|------|------|------|
| ADR-001 | 2026-05-30 | SnowNLP 作为 MVP 分析引擎 | ✅ |
| ADR-002 | 2026-05-31 | 三段式工作框架 | ✅ |
| ADR-003 | 2026-06-09 | 模块化架构重构 | ✅ |
| ADR-004 | 2026-06-09 | 天地图替代 OSM 底图 | ✅ |
| ADR-005 | 2026-06-09 | Dialog 弹窗 vs 侧边栏 | ✅ |
| ADR-006 | 2026-06-11 | 建立文档体系 | ✅ |
| ADR-007 | 2026-06-12 | 架构层级优化 + 空间分析引擎重定义 | ✅ |
| ADR-008 | 2026-06-12 | Scrapy 作为数据采集框架 | ✅ |
