# 双环境合流同步报告（2026-08-15 · home 端执行 · Claude）

> 背景：office 无 GitHub 外网，08-14 home 侧与 office 侧在 `7cd5b424` 后各自分叉。本报告记录首次通过 **DEV-SYNC-HUB 移动硬盘同步专区**完成的双侧合流，及后续 Gitee 迁移决策。
> 结论先行：**合流完成，零内容丢失**。本地 = 盘仓 = GitHub 三方一致于 `87c31bdd`。

## 一、DEV-SYNC-HUB 机制解读（office 端 08-15 部署）

- 位置：`E:\DEV-SYNC-HUB`（盘符无关）；裸仓 = `repos\emotion_map.git`（唯一真相源，`receive.denyNonFastForwards=true` 拒绝改写历史）
- 四个双击入口：`Leave.bat`（离盘：WIP 兜底提交 → push 全分支 → refs/backups 快照留30 → 增量 bundle 留10 → fsck → 账本）、`Arrive.bat`（到岗：fetch → 当前分支**只做快进**，分叉只报警不动手）、`Status.bat`（分支矩阵体检）、`Rescue.bat`（四层灾难恢复）
- home 端职责：Leave 后顺手 `git push origin`（GitHub 备份由 home 统一完成）
- 本次 home 接入已完成：`registry.json` 登记 `home = LAPTOP-HB0DA58R` + 项目路径 `d:\Github\emotion_map`；`safe.directory '*'`（exFAT）已设

## 二、合流前两侧状态（共同祖先 `7cd5b424`）

| 端 | 领先 | 内容 |
|---|---|---|
| office（盘仓） | 25 提交（至 `ec9c70e`） | CB36 体检点数据管线审计 + CB37 page7 社区问题一览图 + page7 三表/三类社区点面数据 + zonal membership 修复 + HUB 部署与 sim 验证 |
| home（GitHub） | 1 提交（`d324b501`） | 08-14 晚 checkpoint：CB32-35 评估文档 + page7 预览脚本 + .gitignore 防回流（sync 工具链随后在 `89ce0f2b` 删除） |

**重叠文件**（双侧各自 add、字节级相同，git 自动消解）：CB32-35 评估文档 7 篇、page7小结 脚本/CSV/PNG、`SCRIPT/_tmp_audit_cb33_v1-4.py`——office 在 `d1cfbde` 独立入库了同一批文件，哈希比对全部 SAME。

## 三、异常发现与处置：`ec9c70e` 悬空

- 账本 `13:28:15` 记录 leave 时 office 本地头 = `ec9c70e`，但盘仓分支头停在 `c9caab0c`（11:54 SYNC-WIP）——13:26-13:28 的多次 leave/arrive 为 HUB 增量打包**验证测试**，最后一次 push 未落盘头，`ec9c70e`（sim 验证提交）成为盘上悬空对象
- **处置**：在盘仓打临时 ref `refs/heads/_rescue_ec9c70e` 使其可 fetch → 合并目标取 `ec9c70e`（含 office 全部工作，保证明日 office Arrive 干净快进）→ 合流后删除临时 ref
- 遗留提示：office 明日到岗若本地仍在 `ec9c70e`，Arrive 对 `87c31bdd` 是纯快进（`ec9c70e` 已是合流历史的祖先），无分叉风险

## 四、执行序列（全部成功）

1. `89ce0f2b` chore: remove home-side sync toolkit —— 昨日自建 sync_guard/bats/规则文档/SessionStart hook/_tmp 补丁全部移除（被 HUB + Gitee 方案取代）；**保留** .gitignore 防回流规则（docs 根散图/PDF、tests/browser/out/，与同步无关）
2. `3be6398c` merge office `ec9c70e` —— **零冲突**（todo.md/revision-log.md 双侧改动在不同区域，自动合并；add/add 同内容自动消解）
3. `87c31bdd` chore: untrack basemap 测试截图（随 office 回流的再生性产物，.gitignore 已覆盖）
4. `git push origin` → GitHub `codex/dsh-onboarding = 87c31bdd`
5. `Leave.bat`（home 首跑）→ 盘仓全分支更新 + 增量 bundle `20260815-210949_inc.bundle` + 账本记录 + GitHub 镜像复核
6. 删除 `_rescue_ec9c70e` 临时 ref；`main` 维持 `4f6fac71` 未动（双侧一致，归位决策另议）

## 五、勘误（08-14 todo 记录）

08-14 todo 段所记「分组入库 + ff 归位 main + 双分支 push」实际未完整发生——当晚模型服务故障，家机实际只形成单个 checkpoint `d324b501`（bat 兜底）+ GitHub push；main 未归位。以本报告为准。

## 六、后续

- **Gitee 迁移已完成**（本日第二阶段）：`origin` = `https://gitee.com/tinsei0321/emotion_map.git`（全量 2 分支 + 1 标签校验一致）；GitHub 改名 `github` 降级为 home 侧备份镜像；项目 `.mcp.json` 换绑官方 `mcp-gitee`
- **office 明日到岗**：插盘 → `Arrive.bat`（快进到最新，`ff1f7e26` 或更新）→ `git remote set-url origin https://gitee.com/tinsei0321/emotion_map.git` → 首推弹窗用私人令牌当密码 → 之后每日常规 Leave/Arrive + push origin
- 双机日常纪律不变：离开双击 Leave、到岗双击 Arrive、home 离盘前多推一次远端（origin=Gitee、github 镜像可选）
