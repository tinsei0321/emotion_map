# CB-16 CB-15 P1 实施后检查（Codex 第三方）

> **评估方**：Codex（GPT-5 · 第三方独立评估组）  
> **时间**：2026-08-04 | **分支**：`fix/emc-buglog` @ `c3e1c9d`（P1 commit `61567d6`）  
> **方法**：commit diff 逐行核验 + 单测实跑 + 端点/契约直测 + 失败项归因（validate drift 是否为本次引入）  
> **范围**：只读评估 · 不改代码 · 不 commit

---

## 结论摘要（先行）

**CB-15 P1 通过。** A（buffer 中文 POI fallback）/ C（lookup_place 六处同步）/ D（`+` 多字段合成 + 文案修复）全部落地且正确；单测实测 **34 passed + 1 failed（既有 drift·确认非本次引入·不阻塞）**。  
**P2 三项（不阻塞）**：① 合成 source 列出全部字段（含 null 提取失败者）② tool_contracts `required_slots=['q']` 与 SKILL_DEFS `[]` 不一致（lng/lat 直查合法·契约应对齐）③ buffer fallback 在"本地命中≥1 但 <3 且高德不可达"时有 ~7s 重试延迟（buffer 只需坐标·可本地即用）。

---

## 一、4 环节核验

| 环节 | 落地核验 | 判定 |
|---|---|---|
| ① buffer fallback | ✅ `resolve_boundary` 失败 → str-only → `search_place(limit=1)` top-1 → 无命中诚实 400 → WGS84 点缓冲；preset 优先（resolve_boundary 先试·fallback 仅在失败后）·非 str 原样 re-raise | 正确 |
| ② `+` 多字段合成 + 文案 | ✅ 逐字段取（`split('+')` → 去 qualifier/`（`/`/`）→ 逐个 `_extract_emc_value` → 非空 join）；单字段路径无回归；`:179` 陈旧文案已改"P0 双源融合·place_name_source 标置信度" | 正确（2 个 P2 微调） |
| ③ lookup_place 六处同步 | ✅ TOOL_CONTRACTS 条目（触发 地点/在哪/叫什么/附近/坐标·避开周边）+ paradigm GEO_TOOL_CATALOG + tools.js TOOLS.lookup_place（q→/place/search→reverse 近邻·lng/lat 直查·无命中诚实）·SKILL_DEFS · emc-patterns GEO_VERB_KW · candidate_selector F_013 | 正确（required_slots 契约对齐 P2） |
| ④ 测试 | ✅ test_geo_routes +3（中文名 200/无命中 mock 400/契约）+ test_outlet_schema +2（组合合成/poi 字段暴露+文案断言）·34 passed 实测 | 覆盖主线 |

---

## 二、七问逐答

### 1. buffer fallback 边界正确 ✓

- preset 优先（resolve_boundary 先试·preset_id 语义不被绕过）✓；只对 str center（GeoJSON dict 走原路径·失败原样 raise）✓；top-1（search_place limit=1·forward 按分数排序）✓；无命中诚实 400（"无法解析…请改用已加载范围或上传"·禁编造）✓；WGS84（search_place 保证）→ 4546 米制缓冲 ✓。

### 2. `+` 多字段合成正确 ✓（2 个 P2 微调）

- 逐字段取·非空 join·空值跳过 ✓；单字段路径（无 `+`）与旧行为一致（无回归·outlet 既有断言通过）✓；qualifier 后缀（降序/占比/排序/负数/正数/TOPn）仍逐字段剥离 ✓。
- **P2**：source 列出全部 `_field_parts`（含提取为 null 的字段·如 `place_name + 网格/POI` 的 source 含"网格（确定性）"但 value 无网格）——建议 source 只列实际提取字段。

### 3. lookup_place 契约同步完整 ✓；required_slots 微调（P2）

- 六处同步确认（契约/paradigm 派生/tools.js/SKILL_DEFS/GEO_VERB_KW/track F_013）✓；**触发词避开"周边"**（GEO_VERB_KW 补 地点/在哪/坐标/附近·无"周边"·buffer 保留"周边"语义）✓ 冲突规避成立；FC 可见（契约 when/voice/triggers_str 进 schema + paradigm 目录）✓。
- **P2**：tool_contracts `required_slots=['q']` vs SKILL_DEFS `required_slots=[]`——lng/lat 直查是合法替代（工具支持 q 或坐标）·契约应对齐为 `[]`（或注明二选一）。

### 4. `:179` 文案修正确 ✓（1 处微调）

- 陈旧"CB-15 后升级 POI 双源"已移除 → "P0 双源融合·place_name_source 标置信度" ✓（测试断言覆盖）。
- **P2 微调**："格内最近 POI"为网格语义；polygon 模式 place_name=边界名（poi_top_places 增强）——文案可加"网格=最近 POI / 面域=单元名"更准（不误导但可更精确）。

### 5. 测试覆盖够 ✓（3 边界补注）

- 已覆盖：buffer 中文名 200 / 无命中 mock 400 / lookup 契约 / 组合合成 / poi 字段暴露 + 文案断言（34 passed 实测）。
- 边界核验：多命中 top-1（search limit=1 天然·forward 排序）·坐标直查（tools.js lng/lat → reverse 路径代码验证）·search 无命中（mock 400 测试）·`+` 合成极端（空值跳过·source 微调见 P2）。

### 6. 承重零触碰 ✓

- diagnose/harness/ChatRequest 不动；resolve_boundary 不改（fallback 在 buffer 端点层 catch）；place_layer/geocode 只增不改；新工具契约三处同步 + 连续 track ID（F_013）✓。

### 7. test_validate_skill_params drift —— 确认既有·不阻塞（但漂移面比"density 一项"更广）

- 失败 `test_geo_catalog_equivalent` 的 diff 列表：**density/buffer/clip/overlay/zonal_stats/extract_feature/merge 七个既有工具**的 paradigm `when` 文本滞后于 tool_contracts——全部是 P1 未触碰的工具；**lookup_place 等价通过**（不在失败列表）→ 确认非本次引入 ✓。
- **建议**：backlog 记全量（7 工具 paradigm when 同步），而非仅 density 一项。

---

## 三、端到端验证

| 项 | 结果 |
|---|---|
| `pytest test_geo_routes + test_outlet_schema + validate_skill_params` | ✅ **34 passed + 1 failed（既有 drift·非本次引入·不阻塞）** |
| buffer 中文名 200 | ✅ 测试通过（万达广场 fallback 出缓冲）；手动直测受沙箱网络 amap 重试挂起影响（生产可达快） |
| lookup_place 契约 | ✅ 实测 `'lookup_place' in TOOL_CONTRACTS` + triggers 含"在哪" |
| all_pois | 现 4342（Wave 2 补修去重连锁店后·非 4310·正常演进） |
| 浏览器「奥体中心周边」/「滨江公园有什么」 | 待 claude组/用户环境（本机 py 占位符 + 网络限制） |

---

## 四、判定

- **CB-15 P1 通过**：A/C/D 全落地正确·测试全绿（除既有 drift）·承重零触碰。
- **P2（3 项）**：① 合成 source 只列实际提取字段 ② tool_contracts required_slots 对齐 `[]`（q 或坐标二选一）③ buffer fallback 本地命中即用（减 amap 不可达时的重试延迟）+ 文案微调（网格/面域语义区分）。
- **既有 drift**：validate_skill_params 7 工具 paradigm when 同步（记 backlog 全量·不阻塞 P1）。

---

*本报告为 Codex 组独立评估；单测实跑 + commit diff 逐行核验 + 端点/契约直测 + drift 失败项归因（lookup_place 等价通过·7 工具为既有滞后），未参考其他组报告。*
