# CB-16 Wave 0 剩余实施预检（glm组 · ZCode + GLM 5.2）

> **预检方**：glm组（ZCode + GLM 5.2）·第三方独立评估  
> **日期**：2026-08-03 | **对象**：f84b3ae 三 bug 修复确认 + Wave 0 剩余两项实施预告预检  
> **方法**：f84b3ae diff 核验 + emc-patterns.js / ai_qa.css / design/tokens.css 代码审查 + validate_field_dict_sync.py 参考模式 + pytest 12 passed

---

## 预检结论：通过（2 项实施预告全部 OK·1 个同步守卫细节建议）

**f84b3ae 三 bug 修复正确落地（glm组 P0 domain + Codex qualifier/UI 语境词·12 测试全过）。Wave 0 剩余两项实施预告预检通过——触发词表镜像 emc-patterns.js 末尾合适 + 同步守卫对路 + 前端卡片渲染仿 .cpd-guide-card 纯模板函数符合分层。**

---

## 一、f84b3ae 三 bug 修复确认

| Bug | 修复 | 验证 |
|-----|------|:---:|
| P0 domain 不匹配（glm组 发现） | `urban_checkup_outlets.py:13` DOMAIN `'urban_checkup'`→`'urban_governance'` | ✅ `test_resolve_outlet_id_checkup` PASSED |
| qualifier 后缀解析失败（Codex 发现） | `build_outlet_schema.py:130` 加 `re.split(r'\s*(?:降序\|占比\|排序\|...)'` 去尾部限定词 | ✅ `test_qualifier_field_parsed` PASSED |
| "更新"过宽（Codex 发现） | `build_outlet_schema.py:21-22` 加 `_UI_CONTEXT_WORDS` 排除"更新图层/时间/样式/刷新" | ✅ `test_update_layer_no_false_trigger` PASSED |

**12 测试全过**（7 原 + 3 新 bug 修复 + 2 field_dict_sync）。

---

## 二、Wave 0 剩余两项预检

### ① 条件触发词表镜像（emc-patterns.js）

| 预检点 | 判定 | 理由 |
|--------|:---:|------|
| 放 emc-patterns.js 末尾合适？ | **✅** | 末尾是 `REGION_KW`（宜昌地名）——词表集中地的自然延续。OUTLET_TRIGGER_KW 加在 REGION_KW 后·注释标"与后端同步" |
| 同步守卫（validate_outlet_trigger_sync.py）对路？ | **✅** | 仿 `validate_field_dict_sync.py`（刚跑过·2 passed）——后端 `TRIGGER_WORDS` vs 前端 `OUTLET_TRIGGER_KW` 集合一致·不一致即报错。模式正确 |
| "仅 UI 提示不改控制流"符合出口驱动？ | **✅** | 出口驱动 = 先定义出口 → 反推开发。触发词表是"前端感知出口意图"（显示提示·让用户知道"这个分析有行业对接价值"）——不改路由/控制流·纯增强 |

**细节建议**：
- `_UI_CONTEXT_WORDS` 也应镜像到前端（或前端检查时也排除 UI 语境词）——否则前端提示和后端触发不一致（前端提示"即将生成行业卡片"但后端因 UI 语境词排除而不出卡 → 用户困惑）
- 同步守卫应检查**两个集合**：TRIGGER_WORDS + _UI_CONTEXT_WORDS 都同步

### ② 前端 outlet-card 渲染

| 预检点 | 判定 | 理由 |
|--------|:---:|------|
| 纯模板函数符合分层？ | **✅** | 数据全来自后端 JSON（build_outlet_schema 产出）·前端只做 HTML 拼装（与 `_renderGuideCard` :264 同模式）——正确分层 |
| 缺失灰色 + 引用块符合设计语言？ | **✅** | `.cpd-guide-card` 用 `var(--geojson-color-text-secondary, #999)` 灰色 + `var(--geojson-color-gray-100)` 背景——outlet-card 用同类 token 自然一致。引用块 `> ` 与 CB-12 降级标注格式一致 |
| {{show:}} 复用对路？ | **✅** | outlet_card 的"地理定位"要素含图层引用——复用 `renderAnswer` 的 `{{show:图层名}}` ref 解析（已有成熟路径）正确。不需另造图层引用机制 |

**设计语言确认**：
- `.cpd-guide-card`（ai_qa.css:744-771）用 `var(--geojson-color-gray-100)` 背景 + `var(--emc-accent)` 顶条 + `border-radius: 12px`——outlet-card 用同类变量即保持一致
- `var(--effect-opacity-disabled: 0.4)` 可用于"暂无数据"灰色文本
- 不需新 token——全部既有变量覆盖

**实现建议**：
- outlet-card CSS 类加在 `ai_qa.css` `.cpd-guide-card` 之后（同级·非 cpd-guide-card 子类——出口卡片是独立组件·非引导卡子集）
- 字段列表用 `.cpd-guide-opt` 同模式（key-value 行 · 13px 标签 + 12.5px 值）——但去掉 hover/cursor（非可点击）
- 局限标注用 `<blockquote>` 或 `> ` markdown·灰暗色（`var(--geojson-color-text-tertiary)`）

---

## 三、验证清单

| # | 预检项 | 结果 |
|:---:|------|:---:|
| 1 | f84b3ae 三 bug 修复（domain + qualifier + UI 语境） | ✅ 12 测试全过 |
| 2 | 触发词镜像放 emc-patterns.js 末尾 | ✅ |
| 3 | 同步守卫仿 field_dict_sync 模式 | ✅ |
| 4 | "仅 UI 提示不改控制流" | ✅ |
| 5 | _UI_CONTEXT_WORDS 镜像同步 | ⚠️ 建议（防前后端不一致） |
| 6 | outlet-card 纯模板函数分层 | ✅ |
| 7 | 缺失灰色 + 引用块设计语言 | ✅ |
| 8 | {{show:}} 复用 renderAnswer ref | ✅ |
| 9 | 既有 token 覆盖（不需新造） | ✅ |

---

## 四、一句话结论

**f84b3ae 三 bug 修复正确（glm组 P0 domain + Codex qualifier/UI 语境·12 测试全过）。Wave 0 剩余两项预检通过——触发词表镜像 emc-patterns.js 末尾合适 + 同步守卫仿 field_dict_sync 对路 + 前端 outlet-card 纯模板函数用既有 token 不新造。1 个细节建议：同步守卫应同时检查 TRIGGER_WORDS + _UI_CONTEXT_WORDS 两个集合（防前端提示与后端触发不一致）。**

---

*glm组（ZCode + GLM 5.2）· CB-16 Wave 0 剩余实施预检 · 2026-08-03*  
*预检基于：f84b3ae diff + emc-patterns.js 末尾结构 + ai_qa.css :744-771 cpd-guide-card 样式 + design/tokens.css 既有变量 + validate_field_dict_sync.py 参考模式 + pytest 12 passed。*
