# CB-16 Wave 3 余留 2b + P2 实施后检查（glm组 · ZCode + GLM 5.2）

> **验证方**：glm组（ZCode + GLM 5.2）  
> **日期**：2026-08-04 | **对象**：Wave 3 余留 2b（B 类条件等式计算器）+ P2（checkup_satisfaction prose→真实字段）  
> **方法**：build_outlet_schema.py 逐行审查（_parse_emc_expr + compute_perceptible_metrics 2a/2b 分支）+ urban_checkup_outlets.py P2 diff + panel.js 渲染 + 端到端运行时验证（2 场景）+ pytest 29+274 passed

---

## 核验结论：通过

**2b B 类条件等式 + P2 满意度真实字段——全部正确落地。7 个 B 类指标覆盖完整·_parse_emc_expr 多值 `/` 拆列表正确·2a/2b 分支逻辑对路（生态宜居明示留 2a·可量化不参与 2b）·P2 满意度卡从 prose→真实字段（polarity_index/element_top/domain_top）防空卡。端到端多卡 + B 类感知验证成功。pytest 274 passed 零回归。** 2 个低优先级观察（CSS 缺 .outlet-metrics 样式 / perceptible_metrics 全卡无条件挂载）不阻塞。

---

## 一、逐项核验

### ① _parse_emc_expr — **OK**

| 核验点 | 结果 | 证据 |
|--------|:---:|------|
| 含 polarity_index → `{'polarity': True}`（走 2a）| ✅ | `:172-173` |
| 条件等式正则 `([a-z_]...)=([^+]+)` 提取 condition_field + condition_values | ✅ | `:175-179` |
| 多值条件 `/` 拆列表（`element_top=设施/环境` → `['设施','环境']`）| ✅ | `:179` `.split('/')` |
| 值字段提取（`+` 后第一个 ASCII 标识符）| ✅ | `:181-183` |
| 关键词提取（`（kw1/kw2）` 拆 `/`）| ✅ | `:184-185` |
| 无条件无极性 → None（跳过）| ✅ | `:176-177` |

### ② compute_perceptible_metrics 2a/2b 分支 — **OK**

| 核验点 | 结果 | 证据 |
|--------|:---:|------|
| 2a 极性类（polarity=True）：polarity_index 取值 + _kw_hit 标注 | ✅ | `:293-304` |
| 2b 仅可感知 industry 进入（`'可感知' not in _industry → continue`）| ✅ | `:306-307` 生态宜居等可量化不参与 2b |
| 2b element_top 缺失 → '暂无数据' | ✅ | `:308-312` |
| 2b 条件不匹配 → 跳过（不占卡面）| ✅ | `:313-315` |
| 2b 关键词未命中 → 跳过（防跨类误标）| ✅ | `:329-330` |
| 2b 命中 → 值 + source 对齐 2a 格式 | ✅ | `:331-333` `f'{value_field}（确定性）·条件：{condition_field}={matched}·{kw_hit}'` |
| _kw_hit 共用（2a 关键词标注）| ✅ | `:337-348` |

**端到端验证（Test 2 B 类）**：
```
element_top=环境, topic_top=公园散步
→ 公园绿地步行可达性感知: value=公园散步, source="topic_top（确定性）·条件：element_top=环境·命中：公园"
```
✅ 条件匹配（环境）+ 关键词命中（公园）+ source 格式统一。

### ③ checkup_satisfaction P2 — **OK**

| 核验点 | 结果 | 证据 |
|--------|:---:|------|
| 满意度（4 尺度）→ polarity_index | ✅ | `urban_checkup_outlets.py:27` |
| 8 领域情绪值 → element_top/domain_top + polarity_index | ✅ | `:28` `/` 取首（element_top 优先·Wave 1 一致）|
| 不满意项定位 → issue_label + place_name（不动）| ✅ | `:29` |
| CI 兼容（validate_outlet_fields 全过）| ✅ | 29 passed |

**端到端验证（Test 1 多卡）**：
```
checkup_satisfaction:
  满意度（4 尺度）: -0.32  ← polarity_index 真实取值 ✅
  8 领域情绪值: 设施、-0.32  ← element_top + polarity_index ✅
  不满意项定位: 停车难、大南门  ✅
```
✅ P2 从 prose → 真实字段·防空卡。

### ④ panel.js perceptible_metrics 渲染 — **OK（附 1 CSS 观察P3）**

| 核验点 | 结果 | 证据 |
|--------|:---:|------|
| 可感知指标小节渲染（metricsHtml + metricsBlock）| ✅ | `panel.js:303-311` |
| 暂无数据灰色（outlet-muted）| ✅ | `:305` |
| source title（hover 显示来源）| ✅ | `:307` `title="${esc(mt.source)}"` |
| 有指标才渲染小节（metricsBlock 空则不插入）| ✅ | `:309-311` |
| 不破坏既有卡片布局（metricsBlock 在 fields 之后 task 之前）| ✅ | `:320` 顺序合理 |

**⚠️ P3 观察**：`.outlet-metrics` / `.outlet-metrics-title` CSS 类在 ai_qa.css 中**不存在**——渲染时 div 会无样式（浏览器默认布局·不崩但无视觉区分）。建议补 CSS（仿 .outlet-field 样式）。

### ⑤ 测试覆盖 — **OK（+7 新测试）**

| 测试 | 覆盖 | 判定 |
|------|------|:---:|
| test_wave3_2b_conditional_match | B 类命中 | ✅ |
| test_wave3_2b_conditional_mismatch_skipped | 条件不匹配→跳过 | ✅ |
| test_wave3_2b_conditional_missing_elem | element_top 缺失→暂无数据 | ✅ |
| test_wave3_2b_multi_value_condition | 多值条件（设施/环境）| ✅ |
| test_wave3_2b_keyword_miss_skipped | 关键词未命中→跳过 | ✅ |
| test_wave3_2b_ekon_shifted | 生态宜居留 2a（非 2b）| ✅ |
| test_wave3_p2_satisfaction_real_fields | P2 满意度卡字段非空 | ✅ |

**覆盖完整**——7 项覆盖 B 类全部路径（命中/不匹配/缺失/多值/关键词未命中/生态宜居 2a/P2）。

---

## 二、承重 + 回归

| 核验点 | 结果 |
|--------|:---:|
| diagnose/harness/ChatRequest 不动 | ✅ 只改 outlet_kb + panel.js + test |
| 2a 逻辑不回归（生态宜居仍走 2a）| ✅ `:293-304` 2a 分支不变 |
| 确定性组装不变（不调 LLM）| ✅ |
| pytest 274 passed | ✅ 零回归（242→274·+7 新测 +25 其他 Wave 测试）|

---

## 三、观察（P3·不阻塞）

### 观察 1：.outlet-metrics CSS 缺失

`panel.js:310` 渲染 `<div class="outlet-metrics">` 和 `<div class="outlet-metrics-title">`——但 ai_qa.css 中无对应样式定义。渲染不崩（浏览器默认布局）但可感知指标小节无视觉区分（与上方 fields 无分隔）。

**建议**（P3）：补 CSS·仿 `.outlet-field` 加 `.outlet-metrics { margin-top: 6px; }` + `.outlet-metrics-title { font-size: 12px; font-weight: 600; color: var(--emc-accent); margin-bottom: 4px; }`。

### 观察 2：perceptible_metrics 全卡无条件挂载（已知·非本次引入）

`_build_card:267` `card['perceptible_metrics'] = compute_perceptible_metrics(result)` —— 所有卡（含 renewal_demand 等更新类）都带可感知体检指标。这在语义上不太准确（更新需求卡挂体检指标）。

**已知**：claude组 标注"domain 门控暂缓 backlog 未做"——确认不阻塞。当前行为可接受（更新卡带体检指标 = 跨领域信息补充·用户可选看）。

---

## 四、验证清单总结

| # | 验证项 | 方法 | 结果 |
|:---:|------|------|:---:|
| 1 | _parse_emc_expr 多值 `/` 拆列表 | 代码审查 | ✅ |
| 2 | _parse_emc_expr 含 polarity → 2a | 代码审查 | ✅ |
| 3 | 2a/2b 分支（生态宜居明示留 2a）| 代码审查 + 测试 | ✅ |
| 4 | 2b 条件不匹配→跳过 | 测试 PASSED | ✅ |
| 5 | 2b 关键词未命中→跳过 | 测试 PASSED | ✅ |
| 6 | 2b source 对齐 2a 格式 | 端到端验证 | ✅ |
| 7 | P2 满意度 prose→真实字段 | 端到端验证 | ✅ |
| 8 | P2 CI 兼容 | validate_outlet_fields PASSED | ✅ |
| 9 | panel.js perceptible_metrics 渲染 | 代码审查 | ✅（CSS 缺 P3）|
| 10 | 测试 +7 覆盖 | pytest 29 passed | ✅ |
| 11 | 全量回归 | pytest 274 passed | ✅ |
| 12 | 承重零触碰 | diff 范围 | ✅ |
| 13 | .outlet-metrics CSS | grep | ⚠️ P3 缺失 |
| 14 | perceptible 全卡挂载（已知） | 代码审查 | ⚠️ 已知 backlog |

---

## 五、一句话结论

**Wave 3 余留 2b + P2 全部正确落地——_parse_emc_expr 多值 `/` 拆列表正确（glm组 预检注意①采纳）·2a/2b 分支对路（生态宜居留 2a·可量化不参与 2b·关键词未命中跳过防跨类误标）·P2 满意度卡 prose→真实字段防空卡·端到端多卡 + B 类感知验证成功（公园绿地=公园散步·条件 element_top=环境·命中公园）·pytest 274 passed 零回归。2 个 P3 观察（.outlet-metrics CSS 缺失 / perceptible 全卡挂载已知 backlog）不阻塞。**

---

*glm组（ZCode + GLM 5.2）· CB-16 Wave 3 余留 2b + P2 检查 · 2026-08-04*  
*验证基于：build_outlet_schema.py :155-334 逐行审查（_parse_emc_expr + compute 2a/2b + _kw_hit）+ urban_checkup_outlets.py P2 diff + panel.js :300-324 渲染 + 端到端 2 场景运行时验证 + pytest 29+274 passed。*
