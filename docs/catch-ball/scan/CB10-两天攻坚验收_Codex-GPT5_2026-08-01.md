# CB-10 两天攻坚代码审查验收报告（Codex 第三方）

> **验收方**：Codex（GPT-5，第三方独立评估）  
> **验收时间**：2026-08-01 | **CB 轮次**：CB-10 ③（两天攻坚 4 commit 验收）  
> **审查对象**：`fix/emc-buglog` @ `b2949e1`（`898998b` + `7735cb8` + `392ecc1` + `b2949e1`）  
> **依据**：CB-10 SCAN + 反评价二轮审核（7 修正清单）+ 真码核对 + pytest 实测 + B0 飞轮报告  
> **方法**：逐条 verify——7 修正是否真实落地、无回归、不撞红线  
> **验收结论**：**有条件通过**（7 修正全部真实落地；4 项收尾条件见第六部分）

---

## 一、逐条核验（对应 7 修正）

### 1. B005 扩成功路径 `_autoExpandOverlays`（非模式 D）—— agree（落地属实）

| 声称 | 核实 |
|---|---|
| `_LANDUSE` 去「用地」泛词 | `emc-patterns.js:9` `LANDUSE_KW` 无「用地」；`harness.js` 两处引用改为 `LANDUSE_KW`。防「商业用地」误匹配：现在 `商业用地 → ['商业']` 1 词（原 ['商业','用地'] 2 词会误走多用地分支 + 「用地」匹配所有用地层 → 3 overlay）——修复成立 |
| `_autoExpandOverlays` 扩单用地 | `harness.js:1286-1288`：`_mentioned.length < 1 → return null`；`< 2 && !wildcard && !/(范围内\|内的\|裁剪\|裁剪出\|筛选出\|提取)/ → return null`——单用地 + 裁剪语义进入扩展 |
| 保留模式 D 且只在 FC 失败路径 | 模式 D（`harness.js:1209-1235`）位于 `_deterministicRecover` 内，仅 `harness.js:810-819`（`!diagnose \|\| diagnose.degraded`）触发；与成功路径的 `_autoExpandOverlays` 不冲突，两者最终都汇入 `_allToolCalls → runAllToolCalls` |

**边界验证**：「筛选出西陵区内的情绪点」（clip 语义）→ `_mentioned.length < 1` → 不触发 ✓；「裁剪出西陵区范围内的商业+居住+公园广场用地」（B002）→ 3 词正常扩展 ✓；「将西陵区+伍家岗区范围内商业用地筛选出来」（B005）→ 1 词 + 「范围内」→ 扩展 ✓。

### 2. B007 类型校验随 P0-1 同包—— agree（落地属实，契约强化留 P1）

`tools.js:710-724` `_checkGeomType(value, want, toolName)`：

- clip 需点层（`tools.js:1039-1041`）；extract_feature 需面层（`tools.js:1061-1063`）；overlay layer_a/layer_b 需面层（`tools.js:1168-1170`）——三工具全覆盖。
- 传错类型返回 `[ERR] ... 图层类型不匹配：目标「X」是点层/面层·但 tool 需...`——非静默错配。
- GeoJSON 对象参数跳过检查（`_checkGeomType` 仅查字符串 id/name）——auto-expand 传 `layer_a` 为 boundary GeoJSON 时不受误伤 ✓；幻觉层名（查不到）也跳过，由既有诚实观测兜底。
- `tool_contracts.py` 在 4 个 commit 中未改动——契约文本强化确实留 P1，与声称一致。

### 3. 完成度守卫代码层确定性追加—— agree（落地属实）

`harness.js:1350-1356`（runAllToolCalls）：`failedSteps.length` 时在**最终结论**确定性追加 `> ⚠️ 仅完成 X/Y 步（第N步未产出图层·未生成）`——结论层、不依赖 LLM 措辞；与 R4（`applyQualityDefense` 状态矛盾拦截）互补不重复（R4 拦"成功说失败/失败说成功"，本行补"部分失败显式声明"）。

**观察项（非红线）**：部分失败时 `exit` 仍为 `'result'`（`harness.js:1358`），四态出口语义上归 result 而非 partial——机制未破坏，但可考虑与 while-loop 路径的 `EXIT_PARTIAL` 对齐。

### 4. FC prompt 守卫—— agree（落地属实）+ 关键分歧意见（见第三部分）

- `router.py:25-44` `build_fc_sys_prompt(context)` 抽函数 ✓；恢复 31e2a00 极性纪律原文（含"默认全部三个极性·严禁自行缩窄"完整两句）✓。
- `test_emc_template.py:88-99` `test_fc_sys_prompt_keeps_polarity_discipline` 断言 4 个锚点（极性范围纪律 / 严禁自行缩窄 / 全部三个极性 / clip 仅用于点数据）✓，实测通过。
- 只恢复极性纪律、不恢复 plans/domain_lens/多要素提取段——**判定成立**，详见第三部分。

### 5. P0-4 静态/动态分解瘦身—— agree（落地属实）+ 1 项 docstring 口径未统一

- `prompts.py:31-41` 人民城市理念抽 `_PEOPLE_CITY_LINE` + `_is_concept_question` 条件注入；`FINAL_TEMPLATE` 语言风格 7 条并 3 条（`prompts.py:143-145`），禁工具名/中文化/专业词/禁箭头核心约束全部保留。
- 实测 `build_final_prompt('','')` = **2641B < 3000** ✓；`test_final_prompt_stays_lean` 转绿 ✓。
- **口径未统一**：测试 docstring 写"final prompt 须 <2KB"，断言 `<3000`，实际 2641B > 2KB——docstring 已失真，建议改"<3KB"。

### 6. 分歧2 词表集中 + 边界—— agree（主体落地）+ 1 处残留

- `emc-patterns.js` 新建（36 行，零 import → 无循环依赖），收纳 `LANDUSE_KW / DOMAIN_KW / POLARITY_KW / CONCEPT_KW / INVENTORY_KW / GREETING_KW / GEO_VERB_KW / REGION_KW`；`harness.js`/`stages.js` 全部改为引用（`harness.js:9` import、`stages.js:7` import DOMAIN_KW）✓。
- 残留：`harness.js:27-31` `_POL_MAP` 的 `overall` 行（`['综合','总体','overall','全部']`）未入 emc-patterns（POLARITY_KW 只含负/正/中三组）——1 行内联残留，非阻塞。
- `tools.js:35 _DOMAIN_CN2EN`、`tools.js:523 _LANDUSE_NAME_RE` 为翻译/层名分类映射，非检测词表，不算泄漏。

### 7. CPD-RESERVED 空骨架记 KNOWLEDGE—— agree（落地属实）

- `harness.js:554-557`（_plansToCapsules 注释）与 `harness.js:827-828`（ctx.plans 注释）标 CPD-RESERVED + 数据生产链已停供说明 ✓。
- `KNOWLEDGE.md` 新增 2 条 learning：FC prompt 无守卫删四段（←CB-10）、CPD-RESERVED 是空骨架（←CB-10）✓。
- `executePlans` 死代码已删（`harness.js` diff -46 行）✓。

---

## 二、验收标准核查

### 红线检查

| 红线 | 结果 | 证据 |
|---|---|---|
| diagnose prompt（build_diagnose_prompt）永不动 | [OK] 未触碰 | `git diff a274362 b2949e1 -- ai_qa/prompts.py` 仅涉及 FINAL_TEMPLATE / build_final_prompt / _PEOPLE_CITY_LINE，无 DIAGNOSE 改动 |
| 决策追踪编号连续 | [OK] 未触碰 | `git diff a274362 b2949e1 -- core/tracker.py` 为空 |
| 四态出口契约 | [OK] 未破坏 | runAllToolCalls 出口语义未改（P0-3 只追加文本）；观察项：部分失败仍归 result |
| FC prompt 非 diagnose eval prompt | [OK] | 改动在 `router.py` fc_diagnose 分支（`build_fc_sys_prompt`），非 `build_diagnose_prompt_dispatch` |

### 回归检查

| 项 | 结果 | 证据 |
|---|---|---|
| pytest | 220 = 198 通过 + 19 沙箱隔离失败 + 3 环境 error（均在本沙箱可复现，单独跑沙箱全过 28/28）| 本环境实测 `198 passed / 19 failed / 5 skipped / 3 errors`；与声称"220 passed"在健康环境口径一致（220 = 198+19+3）；无新增 EMC 域失败 |
| B0 飞轮 36/45 | 可信 | `report-2026-08-01-01-no-llm`：fail 9 = CPD-03~08/10（7）+ UI-09 + PRED-09，全部位于**未触碰子系统**（cpd-guide.js/cpd-state.js/panel.js 均不在 4 commit diff 中）→ 与"fail 9 = 既有 CPD/UI"一致 |
| emc-patterns.js import 链 | [OK] | 新文件零 import（无循环）；harness/stages 引用正常 |
| 新守卫 | [OK] | `test_fc_sys_prompt_keeps_polarity_discipline` + `test_final_prompt_stays_lean` 实测通过 |

### 诚实性检查

- commit message 与代码一一对应（B003/B005/B006/P0-4/P1-1/右半段/B007/词表集中均找到落点）——无"声称修复但实际未生效"。
- B005 9.7s / B003 12.7s / B006 三极性 浏览器验证记录在案（test_p0_repro.py 定向，非全量 LLM 回归）——我未独立复验（需 API key + serve），标记为"记录在案·待独立复验"。
- B002 半成品 answer 体验（overlay 执行不稳 + 先渲染半成品再后台跑）明确记待修——诚实。
- **一处不实**：`emc-fix-progress.md` 更新行写"分支 @ a274362"、总计行仍写"v3.1（657c2e3）·pytest 221 passed"——b2949e1 只同步了头部与当前状态节，总计行残留旧口径（新旧混杂）。
- **一处 CI 假绿风险**：`_gen_index.py --check` 当前实测**失败**（见下）。

---

## 三、关键分歧：只恢复极性纪律、不恢复 plans/domain_lens/多要素提取

**判定：成立（agree），其中 domain_lens A 部是"可接受但有损失"，不应声称"无损失"。**

| 段 | 是否应恢复 | 论据 |
|---|---|---|
| plans 产出指令 | **不恢复（正确）** | plans[] 无消费方（executePlans 已删、胶囊禁用、CPD-RESERVED），恢复 = 让 LLM 输出无人消费的 token。KNOWLEDGE 已记"CPD 复活时须同步恢复"——处置正确 |
| 多要素提取段（CB-09 M2 few-shot） | **不恢复（基本正确）** | 指引已由契约承担：`tool_contracts.py:182` extract_feature where hint"op∈eq\|in·多值用 in/值1,值2 如 MC/in/西陵区,伍家岗区"，而契约 when = FC tool description（KNOWLEDGE §2 自己的判据，比 sys prompt 更上游）；`_norm_where` 拆逗号兜底；模式 D / _autoExpandOverlays 覆盖典型场景。残余风险：few-shot 完整示例比契约 hint 更具体，LLM 主动产 `where=in` 倾向可能略降——B001 已收口、回归清单在，可接受 |
| domain_lens 输出指令 | **不恢复（可接受，但记为损失）** | A 部（parse `[domain_lens:xxx]`，`stages.js:390-393`）因无指令恒空，只剩 B 部关键词兜底（`stages.js:395-405`，首个命中取 1 域）。对非关键词措辞（如"这个片区怎么提升"）返 `[]` → finalStep 无领域知识注入。恢复需 +~600B prompt 体积 + 承担 L01 行为回归风险，B 部覆盖主场景 → 可不恢复，但**应记入待修表而非静默当"无损失"** |

**关于"恢复会回退旧诊断卡行为"**：成立——0073990 删除的旧 prompt 含"输出所有需要的工具（tool_calls 数组）+ plans JSON + domain_lens 标签"的任务指令，正是 Flash 预选工具/格式漂移的诱因；只恢复**约束性纪律段**（极性纪律）而非**输出格式指令**，是安全的。判定与实施一致。

---

## 四、发现的问题（按严重度）

| # | 严重度 | 问题 | 证据 | 建议 |
|---|---|---|---|---|
| 1 | MED | `_gen_index.py --check` 实测失败，且**时间戳分钟级脆弱**：`render_summary` 的"最后更新"时间戳每次重渲染都会变，--check 逐字节比对 → 过了同分钟必红，CI 守护实质失效 | `_gen_index.py:548-552` 全文比对；实测仅第 3 行时间戳差异（12:06 vs 13:10），其余内容同步 | --check 比较时忽略"最后更新"行，或时间戳仅生成不参与比对 |
| 2 | LOW | `emc-fix-progress.md` 新旧混杂：更新行 `@ a274362`、总计行 `v3.1 / 221 passed`，与当前状态节 `v3.3 / 220` 矛盾 | 文件头部 3 段 | 统一为 b2949e1 / v3.3 / 220 passed |
| 3 | LOW | `_cb-index.md` 分支 hash 仍 `@ a274362`（应 `b2949e1`） | `_cb-index.md:14` | 同步 |
| 4 | LOW | `test_final_prompt_stays_lean` docstring 写"<2KB"、断言 `<3000`、实际 2641B——口径三处不一致 | `test_emc_template.py:80-86` | docstring 改"<3KB" |
| 5 | INFO | `_POL_MAP` overall 行残留内联（未入 emc-patterns） | `harness.js:27-31` | 顺手并入 POLARITY_KW |
| 6 | INFO | runAllToolCalls 部分失败 exit 仍 'result'（未归 partial） | `harness.js:1358` | 与 while-loop EXIT_PARTIAL 对齐（非红线） |

---

## 五、独立复验说明

- 浏览器用例（B002/B005/B003/B006 定向验证，9.7s/12.7s 等）未在本环境复跑——需 DEEPSEEK_API_KEY + serve + Playwright，非只读审查可覆盖。代码路径核实 + 记录在案，标记"待独立复验"。
- 全量 pytest 与 `_gen_index --check` 已在只读方式下实测。

---

## 六、验收结论：**有条件通过**

7 修正全部真实落地、无回归、不撞红线；关键分歧（只恢复极性纪律）判定成立。以下 4 项收尾后转为"通过"：

1. **修 `_gen_index.py --check` 时间戳脆弱**（否则 CI 守卫恒红，P1-1 闭环不完整）；
2. **文档口径统一**：`emc-fix-progress.md` 总计行/更新行 → b2949e1 / v3.3 / 220；`_cb-index.md` 分支 hash 同步；
3. **domain_lens A 部损失记入待修表**（不静默当"无损失"）；
4. **B3 全量 LLM 回归跑完更新报告**（当前仅定向验证，未覆盖全链路）。

---

*本报告为第三方独立验收；条件项均为文档/工具链收尾，无核心功能阻塞。*
