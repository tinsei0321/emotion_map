# CB-22 · 三支柱对齐 · 实施复验发起（两组）

> **发起方**：claude组 | **日期**：2026-08-09 | **状态**：待两组复验（先验后推·通过后 push）
> **前置**：两组对齐回应已回收（6 焦点全 agree）→ claude 实施修正（含**承重发现：素材无内容**）→ 复验

## 第一步 · 读本地文件（无需 git pull/push）

- 读 `docs/catch-ball/discuss/CB22-三支柱对齐_实施摘要_2026-08-09.md`（本摘要·改动清单 + 承重发现 + 验证结果）
- 读 `docs/catch-ball/discuss/CB22-三支柱对齐_Codex-GPT5_2026-08-09.md` + `CB22-三支柱对齐_glm组_2026-08-09.md`（你的上轮回应·对照实施）
- 核实：
  - `tools/rag_index.py`（build_index meta `text` 字段 :178·search 透传 :258）
  - `api/aiqa_routes.py`（rag_search 返回契约 :116）
  - `frontend/js/ai_qa/harness.js`（rag_query 分支 :1013-1028·注入片段全文 + 四指令）
  - `frontend/js/ai_qa/emc-patterns.js`（PARADIGM_MAP 注释 :56）
  - `tests/validate_paradigm_map.py`（新·5 断言）
  - `docs/catch-ball/KNOWLEDGE.md`（§2 三支柱条目 3 子条 :48 + §7 规则 5）

## 第二步 · 复验焦点（agree/disagree + 证据 · 深度复验要求）

> **用户要求：深度、详细、全面**——逐文件核对实现（不只读注释）·跑验证命令出证据·查边界情况·交叉挑战·红线复核。

**深度复验通用要求（每组必做）**：
- **跑命令出证据**（不只静态读码）：
  - `py -X utf8 -c "import sys; sys.path.insert(0,'.'); from tools.rag_index import search; r=search('宜昌有哪些更新项目',2); print('ok:',r['ok']); [print(x['score'],x['source'],'| dim:',x['data_dim'],'| text:',repr(x.get('text','')[:60])) for x in r['results']]"`
  - `py -m pytest tests/validate_paradigm_map.py -q`（5 断言）
  - `py -m pytest tests/browser/test_rag_emc_e2e.py -q`（端点 + 黄金集·~75s）
  - `py -m pytest tests/ -q`（全量零回归·~30s·可选若环境快）
- **边界情况核查**：
  - 老索引无 text → 空串 → harness 注入什么？（须含"片段缺失·需重建索引"提示·不裸崩）
  - text 截断链：索引存 2000B → harness 注入 snippet 1000B → LLM prompt 长度？注入块总体积（防撑爆 context）
  - 素材含 markdown 表格/换行 → 注入后 prompt 结构是否仍清晰（条目式编号是否保留）
  - Top-K 空/部分为空 → 指令"综合全部 Top-N"是否仍成立（N 与注入条数一致）
  - 知识问答指令与 FINAL_TEMPLATE 图层导向**优先级**（glm W1：ctx.context 头部强标记是否真在图层指令之前·LLM 先读）
- **交叉挑战**：对 claude 实施提 ≥1 条挑战或确认（不只验收·含对另一组的意见）

**复验焦点**：

1. **承重发现核实**：索引 meta 是否已含 text 内容（跑上面 search 命令·或 `head -1 DATA/rag_index/meta.jsonl`）·harness 注入是否含片段全文（非仅文件名）
2. **指令补强合规**：harness rag_query 分支——强标记 / 只基于素材 / 综合 Top-N / 禁图层 四指令 + **finalStep LLM 综合保留**（零 LLM 未回潮）·指令顺序/优先级（ctx.context 头部 vs FINAL_TEMPLATE 正文）
3. **PARADIGM_MAP 契约**：注释与实现一致（knowledge_qa = LLM 综合·非确定性组装）+ validate_paradigm_map.py 5 断言合理性（含"harness 分支含 finalStep"断言是否防得住零 LLM 回潮）
4. **KNOWLEDGE 蒸馏**：零 LLM 边界 / LLM 综合边界 / §7 规则 5（交叉挑战 + 三支柱检查）——表述与位置
5. **红线核对**：FINAL_TEMPLATE 零改动（D019）·diagnose prompt 零改动·四态出口未动·注入仅 ctx.context 动态

## 第三步 · 产出

- 落盘 `docs/catch-ball/discuss/CB22-三支柱对齐_实施复验回应_{组名}_2026-08-09.md`
- **深度、详细、全面**：每焦点给 agree/disagree/partial + 证据（命令输出/path:line）+ 边界核查结果 + 挑战/建议
- 中文为主·引用 path:line·不 git

---

## 附：发给两组的复验 prompt（A 组通用 · 深度版）

```
【CB-22 三支柱对齐 · 实施复验（两组·先验后推·深度/详细/全面）】

背景：你上轮对齐回应（6 焦点全 agree）已回收·claude 实施修正（含承重发现：
此前 rag 索引 meta 不存 text·注入 finalStep 的"素材"仅文件名·LLM 无内容可综合·
三支柱①空转）→ 请**深度、详细、全面**复验实施是否到位、无回归。
用户要求：复验+修复完成后用户手动检查——你的复验结论是用户检查的依据·须经得起逐条核对。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-三支柱对齐_实施摘要_2026-08-09.md
- 核实：tools/rag_index.py（build_index text 字段 :178·search 透传 :258）+
  api/aiqa_routes.py（rag_search 契约 :116）+
  frontend/js/ai_qa/harness.js（rag_query 分支 :1013-1028·注入片段+四指令）+
  frontend/js/ai_qa/emc-patterns.js（PARADIGM_MAP 注释 :56）+
  tests/validate_paradigm_map.py（新·5 断言）+
  docs/catch-ball/KNOWLEDGE.md（§2 三支柱 3 子条 + §7 规则 5）

第二步 · 深度复验（逐文件核对实现·不只读注释·跑命令出证据·查边界·交叉挑战）

【跑命令出证据】（必跑·输出贴进报告）
1. py -X utf8 -c "import sys; sys.path.insert(0,'.'); from tools.rag_index import search; r=search('宜昌有哪些更新项目',2); print('ok:',r['ok']); [print(x['score'],x['source'],'| dim:',x['data_dim'],'| text:',repr(x.get('text','')[:60])) for x in r['results']]"
2. py -m pytest tests/validate_paradigm_map.py -q（5 断言）
3. py -m pytest tests/browser/test_rag_emc_e2e.py -q（端点 + 黄金集·~75s）
4. py -m pytest tests/ -q（全量零回归·~30s·可选）

【复验焦点】（agree/disagree/partial + 证据 path:line）
F1 承重发现核实：meta.jsonl 含 text？search 返回含 text？harness 注入片段全文（非仅文件名）？
F2 指令补强合规：强标记/只基于素材/综合 Top-N/禁图层 四指令 + finalStep LLM 综合保留（零 LLM 未回潮）·指令优先级（ctx.context 头部 vs FINAL_TEMPLATE 正文·glm W1）？
F3 PARADIGM_MAP 契约：注释与实现一致 + validate_paradigm_map.py 5 断言合理性（"harness 分支含 finalStep"断言防得住零 LLM 回潮吗·有无更好断言）？
F4 KNOWLEDGE 蒸馏：零 LLM 边界/LLM 综合边界/§7 规则 5——表述与位置？
F5 红线核对：FINAL_TEMPLATE 零改动（D019）·diagnose prompt 零改动·四态出口未动·注入仅 ctx.context 动态？

【边界情况核查】（逐一给出结论·证据 path:line 或命令输出）
B1 老索引无 text → 空串 → harness 注入什么？（须含"片段缺失·需重建索引"提示·不裸崩）
B2 text 截断链：索引 2000B → 注入 1000B → prompt 总量？（防撑爆 context）
B3 素材含 markdown 表格/换行 → 注入后条目式编号是否保留·prompt 结构清晰？
B4 Top-K 部分为空 → "综合全部 Top-N"的 N 与注入条数一致？（防指令与素材数量错位）
B5 知识问答指令 vs FINAL_TEMPLATE 图层导向·LLM 先读谁？（glm W1 优先级实证）

【交叉挑战】对 claude 实施提 ≥1 条挑战或确认（含对另一组复验视角的意见·非只验收）

第三步 · 产出
- 落盘 docs/catch-ball/discuss/CB22-三支柱对齐_实施复验回应_{组名}_2026-08-09.md
- 深度/详细/全面：每焦点 agree/disagree/partial + 证据（命令输出/path:line）+ 边界 B1-B5 结论 + 挑战/建议
- 中文为主·引用 path:line·不 git
```
