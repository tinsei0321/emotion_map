# CB 环境自检 Prompt（三组并行测试 · 2026-08-09）

> **用途**：测试任务三组并行分布式执行（claude组 拆解分配）→ 各组在各自平台 Harness 下自检 CB 机制 + 工作环境，保证测试环节顺畅运行。
> **背景新规**（用户 2026-08-09 定）：测试任务越来越重、单靠人眼测难以为继 → 测试任务由 **claude组 合理拆解、针对性分配给三组同时进行**；项目推进中 claude组 持续提出优化 CB 机制/流程意见（工作坊式先进性/流畅性/科学性）。
> **本卡 = 三组通用环境自检 prompt**：① 读本地文件（无需 git）② 逐项自检 Harness 环境 ③ 报告异常。codex组 / glm组 各自对号入座平台差异。
> **登记**：docs/context-map.md（记忆共享通则）。

---

```
【CB 环境自检 · 三组并行测试就绪 · 2026-08-09】

背景：测试任务越来越重，已改为三组（claude组 / codex组 / glm组）并行分布式执行，由 claude组 拆解分配。你是【CODEX组 / GLM组】（用你所在平台）——请自检你所在平台 Harness 下的 CB 机制 + 工作环境，确保能顺畅接手测试任务。

第一步 · 读本地文件（无需 git）
- 不要 git pull/push——本地工作区已由 claude组 同步到最新（网络动荡·以本地为准）。
- 按序读：docs/catch-ball/_cb-index.md → _handoff/CB恢复记忆卡_2026-08-03.md → RULES.md → KNOWLEDGE.md → _handoff/HOME.md + OFFICE.md → cb-journal.md 最新四轮（CB-18/18b/19/20）。
- 确认你掌握：CB 流程（先验后推·落文档+push 由 claude组 执行·并集采纳·trace 取证纪律·承重红线·七轴评分）与当前状态（CB-17~20 全闭环·发版候选通过）。

第二步 · 逐项环境自检（每项给结论 OK / WARN / FAIL + 一行证据；异常即报告，不阻塞）

1. Python 环境：`py --version` / `python --version` 可跑？pytest 可执行（`py -m pytest tests/ -q` 冒烟 1-2 例即可·不必全量）？
2. Playwright 浏览器：tests/browser/ 下脚本可跑吗（`py -m playwright --version`·浏览器已装？）——B3 飞轮等浏览器用例需要。
3. API Key：.env 里 DEEPSEEK_API_KEY 等是否就绪（跑测试必需）？缺失会 WARN 报告。
4. trace 工具：`py tools/trace_query.py --stats` 可跑吗（根因分析第一动作·B3 带 EMOTION_TRACE_SESSION 的前提）？
5. 端口隔离：三组并发跑 B3 等长任务时，是否知道用 `--port/--backend-port` 隔离 + `sys.executable`（避免三组同端口冲突）？serve 是否已/能起（start.bat·自测前必须重启 serve 否则跑旧代码）？
6. 平台 Harness 机制（按你所在平台对号入座）：
   - 【codex组·Codex + deepseek-v4-flash】：.codex/ hooks 是否生效（SessionStart 打印 [HOOK] 环境检查）？MCP 配置（config.toml 的 playwright / vision-bridge）可用吗？
   - 【glm组·ZCode + GLM 5.2】：ZCode 侧 CB 文件可读吗（docs/catch-ball/ 全目录）？GLM 5.2 能跑 trace 取证（.trace/trace.log 直读）吗？
7. CB 机制核对：确认你本轮身份 = 第三方评估（只读本地·不 git·产 SCAN/响应落盘 docs/catch-ball/discuss/ 由 claude组 push）；无歧义。

第三步 · 结论汇报（简洁·表格式）
- 每项一行：{1. Python}|{2. Playwright}|{3. API Key}|{4. trace}|{5. 端口隔离}|{6. 平台 Harness}|{7. CB 机制} → 结论
- 汇总一句：**可以接手测试任务** 或 **有 N 项异常需 claude组 协调**（列出异常项）。
- 若环境异常（如缺浏览器/缺 Key），标注你组能承接的测试类型（如纯 Python/静态核验/e2e-seam 直测），让 claude组 分配时规避盲区。

第四步 · 待命
- 自检完进入待命，等 claude组 发具体测试任务分配（含任务拆解 + 你组负责部分 + session 标签）。
- 有疑问先自查 KNOWLEDGE/RULES，再在 CB 框架内提出；git 操作一律留给 claude组。
```

---

**claude组 分配测试任务的默认考虑**（自检结果收集后，按各组能力盲区做针对性分配）：
- **codex组（Codex + deepseek-v4-flash）**：适合代码级静态核验 + 机制推演 + e2e-seam 直测（无浏览器依赖）+ 修复验证；浏览器用例需确认 Playwright 就绪。
- **glm组（ZCode + GLM 5.2）**：适合 trace.log 取证 + 逐 case 分析 + 长链路复现（trace 直读派）；同需确认浏览器。
- **claude组（Claude Code + DeepSeek/GLM）**：开发 + 全量回归主责；B3 并发时起 `--port/--backend-port` 隔离实例。
- 每组跑 B3/长任务统一带 `EMOTION_TRACE_SESSION=<组>-<批>`，报告附 `trace_query --stats --session <批号>`。

*本卡由 claude组 编写（2026-08-09）·codex组 与 glm组 通用（平台差异各自对号入座）。*
