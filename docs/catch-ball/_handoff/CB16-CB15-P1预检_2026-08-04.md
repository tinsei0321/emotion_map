# CB-16 CB-15 P1 实施预检（Codex / glm组 · 2026-08-04）

> **用途**：CB-15 P1（评论↔POI 关联 + lookup_place + buffer 中文 POI + 归因落点模板）落地前，claude组 发起两组预检（用户定：走 CB 流程·先讨论再实施）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③s。

---

【CB-16 CB-15 P1 实施预检（Codex / glm组）】

背景：CB-15 P0 已闭环（place_name 双源融合 + poi_names + /grid/pois + 3220 POI all_pois=4310）。P1 = 让地点真正进 EMC 问答管线。claude组 探索确认四子项现状与缺口（下述）。请预检下方实施草案。

第一步 · 同步
- git pull（fix/emc-buglog·最新 ad18f59）
- 读探索依据：`frontend/js/ai_qa/tools.js:1206-1220`（buffer 工具）· `api/geo_routes.py:532-560`（buffer 端点）· `core/geo_registry.py:151-186`（resolve_boundary 只认 preset/GeoJSON）· `core/geocode.py:187-235`（search_place·中文名→POI 坐标·已暴露 /place/search）· `core/place_layer.py:506-531`（reverse 逐点 O(n·4310)）· `api/geo_routes.py:613`（/geo/nearest sjoin_nearest 范本）· `core/spatial_analysis.py:625-687`（_attach_poi_attrs POI sjoin）· `ai_qa/tool_contracts.py:20-294`（TOOL_CONTRACTS 契约单一源）· `ai_qa/outlet_kb/build_outlet_schema.py:140-160,179`（field_mapping 只读 place_name·陈旧文案）

第二步 · 实施草案（4 子项·请预检）

**A. buffer 中文 POI 修复**
- 根因：中文 center（"奥体中心"）透传 tools.js:1211 → buffer-tool.js:217 → resolve_boundary（只认 preset/GeoJSON·无名字解析）→ 400
- 草案：后端 /geo/buffer `resolve_boundary` 失败时 fallback `search_place`（中文名→POI 坐标→点缓冲·复用 geocode.py search_place）·或前端 tools.js buffer 先 search 转坐标
- 倾向后端 fallback（AI/前端共用一处·不依赖前端预解析）

**B. 评论↔POI 关联（批量最近 POI 反查）**
- L1/L2 已带 area_seed/spatial_hotspot（模拟期标注）·reverse 逐点 O(n·4310) 不可批量
- 草案：新 core 函数 `attach_nearest_poi(points_gdf)`（layer 点 × all_pois `sjoin_nearest`·参照 /geo/nearest + _attach_poi_attrs）→ 批量落 nearest_poi_name/id 到 L1/L2 行

**C. lookup_place AI 工具（治 LLM 不可见地点）**
- search/reverse 端点已存在（/place/search·/reverse-geocode）·无 AI 工具包装
- 草案：TOOL_CONTRACTS 新条目（查地点·triggers=有什么/周边/地点）+ tools.js TOOLS 方法（纯前端·调两端点）+ stages.js SKILL_DEFS + emc-patterns GEO_VERB_KW + register_track_id（契约三处同步）

**D. 归因落点模板（出口卡片 place_name 增强）**
- field_mapping 只读 place_name 单字段
- 草案：build_outlet_schema 落点组合模板（place_name + poi_names + issue_label + 4×5 归因合成表单项）+ 暴露 poi_names/place_name_source + 修 :179 陈旧文案（P0 已双源）

请预检：
1. **A buffer 中文 POI**：后端 fallback search_place（resolve_boundary 失败→中文名→坐标）方案对路？还是前端先 search 转坐标？有无边界（多命中取 top？search 无命中诚实报错？坐标系统一 WGS84？）
2. **B 评论↔POI**：sjoin_nearest 批量反查方案对路？性能（4310 POI × N 点·sjoin_nearest 复杂度）？落列到 L1/L2 会不会污染既有字段（加新列名还是覆盖 area_seed）？
3. **C lookup_place**：纯前端工具（调 /place/search + /reverse-geocode）vs 后端 TOOL_CONTRACTS 条目（get_place_layer 直调）——哪个对路？契约三处同步 + track ID 守则？触发词（有什么/周边/地点）够不够？
4. **D 归因落点模板**：落点组合模板（place_name + poi_names + issue_label）合成表单项方案对路？会不会破坏既有出口卡用例（test_outlet_schema）？
5. **范围边界**：P1 四子项是否都必要·有无可后置（如 B 评论↔POI 批量是否 P1 核心）？P2/P3 后置清晰？
6. **测试方案**：buffer 中文名 / lookup_place / 评论↔POI / 落点模板单测 + 端点直测够？
7. **承重零触碰**：不碰 diagnose/harness/ChatRequest·buffer/geocode/place_layer 只增不改·新工具契约三处同步？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-CB15-P1-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-15 P1 实施预检。*
