# CB-16 无法回答措辞修复 + 发版遗留问题 综合实施预检（Codex / glm组 · 2026-08-04）

> **用途**：用户实测「EMC 无法回答时结论统一'无法生成图层'·但问题可能与图层无关」+ 发版快照发现的遗留问题（eval 76% / RST-L06 / PRM 残留）·**一并打包进 CB 定 plan**。草案落地前请两组预检。
> **登记**：docs/context-map.md · cb-journal CB-16 ③w4。

---

【CB-16 无法回答措辞修复 + 发版遗留问题 综合实施预检（Codex / glm组）】

背景：① 用户实测「EMC 遇到无法回答的问题时，结论是'无法生成图层'，但我的问题可能跟图层没有任何关系」② 发版快照（B3 84.6% + eval 76% NO-GO）暴露遗留问题。用户定**一并打包进 CB 定 plan**。claude组 定位根因 + 出综合草案，先请两组预检。

第一步 · 读本地文件（同一工作区·**无需 git pull/push**·直接读）
- 探索依据：
  - 措辞：`frontend/js/ai_qa/harness.js:1310`（gap 出口 `newLayerCount === 0 && !hasRows`）· `:213-236`（composeGapCard·:227 默认措辞）· `:897` failedObs（:753/755/1261 push）· `:906` quickIntent general · `:895/896` answered/narratedAnswer
  - eval：`tests/eval_template_flash.py`（37 条·28/37=76% NO-GO·MISS 2 = `西陵区的商业用地`→overlay·`西陵区范围内密度分析`→density·歧义用例）+ `tests/reports/report-2026-08-04-01-llm.md`（快照报告）
  - RST-L06：`frontend/js/ai_qa/harness.js:1104-1125`（chain pre-check·:1113 deriveAvailable 依赖 getLayers）· `frontend/js/test-cases.js:121`（RST-L06 用例）
  - PRM：`tests/reports/report-2026-08-04-01-llm.md`（PRM-03/04 radius[ERR]·PRM-07 zonal 边界）· `frontend/js/ai_qa/tools.js`（buffer params 填充）

第二步 · 修复草案（请预检）

**核心：gap 措辞与问题性质匹配（用 failedObs 判据）**

改 `harness.js:1310`（零产出 gap 出口）：
```js
if (newLayerCount === 0 && !hasRows) {
  const _triedTools = failedObs.length > 0;
  const _honestText = _triedTools
    ? /* 现有图层措辞（确实在试图层）*/ composeGapCard(...) + '本轮未产出新图层...'
    : /* 新：零工具尝试 → 问题可能非图层类 */ '当前无法直接回答...';
  ...
}
```

**新措辞**（零工具尝试时）：
```
## 当前无法直接回答该问题

我理解你问的是「...」，但这超出了当前情绪地图可直接分析的范围（现有数据/工具无法支撑）。

**下一步建议**：
- 换一种问法（把问题落到具体区域/用地/时点，情绪地图擅长定位空间情绪）
- 或确认你想了解的是否属于情绪地图能力范围（城市情绪空间分布、设施/环境/服务/文化/事件归因）
```

**改 composeGapCard 默认分支（:227）**：failedObs 为空时 head 不用「我没能生成可用的图层」（假话——没试过）→ 用「无法直接回答」。

请预检：
1. **failedObs 判据**：`failedObs.length > 0` = 在试图层（图层措辞合理）·`=== 0` = 零工具尝试（非图层叙事）——客观可靠？有无漏判（如工具被静默跳过但实际失败）？
2. **新措辞**：文案是否得体？「当前无法直接回答」是否比「未生成图层」准确？
3. **gap 出口结构**：exit:'gap' 保留只改措辞·不动三态出口结构/quickIntent/answered——对吗？
4. **测试方案**：e2e-seam 单测（仿 test_r7_truncation·直测 JS 防线·failedObs 空/非空两分支）够？
5. **范围边界**：只改措辞是否够？还是应同时改善「为什么答不上」的诊断（如 failedObs 为空时记录原因）？
6. **承重零触碰**：不碰 diagnose prompt / 三态出口结构 / ChatRequest？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-WordFix预检-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）
- 独立判断·不要互相参考对方报告

**②b 发版遗留问题草案（用户定一并打包·请预检）**

**1. eval 76% NO-GO 修复**（发版阻塞·最优先）
- 现状：`tests/eval_template_flash.py` 37 条 28/37=76%·<80% gate
- MISS 2 = 歧义用例：`西陵区的商业用地`（期望 multi·实得 overlay）·`西陵区范围内密度分析`（期望 multi·实得 density）
- 草案：治歧义路由——`西陵区的商业用地` =「区内+用地类型」跨层多步 → 路由 multi（链 overlay）；`西陵区范围内密度分析` = 范围内密度 → 路由 multi（clip+density 链）。改 `select_template`/FC 路由规则（`frontend/js/ai_qa/stages.js`）+ eval 断言
- 目标：回 80%+ gate

**2. RST-L06 回归修复**（独立 backlog·③w3b 已定根因）
- 根因：harness.js:1113 `deriveAvailable` 依赖已加载层·B3 测试环境缺行政区边界层→boundary null→clip range='' 失败
- 草案：chain pre-check boundary derive 硬化——`deriveAvailable` 无匹配时·从 `presets/行政区.geojson`（preset 注册表）fallback 取区要素·而非 null（治测试/演示环境缺层场景）

**3. PRM-03/04 buffer radius[ERR]**（既有 backlog·CB-12 已知）
- 现状：`大南门周边 30m` / `大南门周边 1` → buffer radius[ERR]
- 草案：buffer 参数填充（`tools.js` buffer 执行）——radius 从问句「周边 N 米/公里」解析（复用 buffer 现有尺度表·250/500/1000）·N 字面量直读

**4. PRM-07 zonal 边界**（既有 backlog·法定功能区认知）
- 现状：`小溪塔范围内按面聚合情绪统计` → EMC 硬识别法定功能区·应 request_upload
- 草案：zonal 路由——「小溪塔」非 preset 行政区名→ fallback 搜索（复用 lookup_place/search_place）或 request_upload 措辞更明确

请预检：
8. **综合范围**：4 项（措辞 + eval + RST-L06 + PRM）打包是否合理？优先级？（eval 阻塞发版·最优先）
9. **eval 歧义路由**：`西陵区的商业用地`→multi（链 overlay）·`西陵区范围内密度分析`→multi（clip+density）——路由规则对？会误伤其他问句吗？
10. **RST-L06 硬化**：deriveAvailable fallback 到 presets/行政区 治本？还是有更稳方案？
11. **PRM buffer/zonal**：radius 解析 + 非 preset fallback——是否本次应做（还是纯发版前）？
12. **测试**：eval 用例断言 + e2e-seam（措辞/RST-L06）+ 全量回归——够？

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-16 措辞修复 + 发版遗留问题 综合实施预检。*
