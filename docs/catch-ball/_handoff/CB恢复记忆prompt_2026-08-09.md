# CB 恢复记忆 Prompt（Codex · 2026-08-09 办公室桌面版）

> **用途**：Codex 办公室桌面版失忆恢复。直接粘贴给 Codex，按序恢复记忆、本地对账、进入待命。
> **同步点**：与 08-08 家庭环境恢复同源（HOME/OFFICE 交接卡·CB-17~20 全闭环·发版候选通过）；08-09 办公室 `.codex/` hooks/config 已配置（commit af7c8061）。
> **本卡 vs 08-03 卡**：08-03 卡（CB恢复记忆卡/CB交接启动prompt）仍是身份+机制基础；本卡按最新 CB 流程（先验后推·落文档+push·并集采纳·trace 取证·承重红线）与当前项目状态（CB-17~20 全闭环）更新，**以本卡为准**。
> **登记**：docs/context-map.md（记忆共享通则）。

---

```
【任务交接启动 · 办公室桌面版 · 2026-08-09】

你在办公室桌面环境启动（Codex 桌面版）。此前在别处（家/办公室）工作、记忆已丢失，仅继承部分记忆——**不可全信，以本地文件 + 交接卡对账为准**（漂移自检纪律）。按以下顺序恢复记忆并进入待命状态。

第一步 · 本地文件即最新（无需 git）
- **不要 git pull / 不要 git push**——git 同步是 claude组（Claude Code）的任务，网络动荡、以本地为准。本地工作区已与远端同步（claude组 负责拉取）。直接读本地文件即可。

第二步 · 按序恢复记忆（严格顺序）
1. docs/catch-ball/_cb-index.md（CB 统一入口·当前 CB-20 已闭环）
2. docs/catch-ball/_handoff/CB恢复记忆卡_2026-08-03.md（你的身份/定位/trace 机制基础——其中「CB-12 状态」段已过时，以本卡为准）
3. docs/catch-ball/RULES.md（评估规则 v2.2·七轴评分·trace 取证纪律）
4. docs/catch-ball/KNOWLEDGE.md（CB 记忆库·承重红线 §1·SCAN 标尺纠正 §3·评估协作规则 §7）
5. docs/trace-log-guide.md（trace 工具用法）
6. docs/catch-ball/_handoff/HOME.md + OFFICE.md（08-08 换机卡·当前进度权威快照）
7. docs/catch-ball/cb-journal.md 的 CB-18 / CB-18b / CB-19 / CB-20 章节（最新四轮轨迹）
8. memories/repo/session-handoff.md（08-05 卡·已过时，仅作历史背景，勿当当前状态）

第三步 · 读取完成后，用自己的话确认已恢复以下要点（简洁核对，不必写报告）：
- 身份与定位：第三方独立评估组（非开发主）；代码级证据（file:line）·机制推演·trace 独立核验；职责 = B3 飞轮评估 / 修复验证 / 根因定案 / PRM·while-loop·多步问攻坚评审。与 glm组（ZCode+GLM·trace 取证派）互补视角。
- 当前项目状态（08-08 收工）：CB-17~20 全闭环·发版候选通过（fail 集判据 {PRM-03/04} 达成）·本地工作区已由 claude组 与远端同步到最新（你无需管 git·网络动荡以本地为准）·pytest ~303 零回归。剩余 fail = {PRM-07 空对象 LLM 方差·RST-L06 Flash 方差}，均已知/方差，非回归。
- 最新 CB 流程/规则（本卡重点·08-03 卡之后新增，务必执行）：
  · 先验后推（CB-16 ③z·KNOWLEDGE §7）：评估方不 git pull/push（只读本地·claude组 负责 git）；claude组 发起/预检草案可 push、实施代码须两组检查通过后 push
  · 两组介入须先落文档 + push（用户指示）：你的 SCAN/响应落盘 docs/catch-ball/discuss/{主题}_{回应}_{环境}_{日期}.md
  · 并集采纳（CB-18）：两组验收深度有差异（Codex 曾发现 glm 遗漏的 W-1/W-2）→ 任何发现 claude组 都会 verify-before-accept 实核，你如实报、别保留
  · trace 取证纪律（RULES 1.2）：根因分析第一动作 = py tools/trace_query.py --stats；数 F_002（agentStep·while-loop 铁证）/ F_003（finalStep）/ F_005（FC）；勿用 F_001；跑测试带 EMOTION_TRACE_SESSION=<组>-<批>；报告附 trace 证据；推断只作假设
  · 承重红线（KNOWLEDGE §1·SCAN 建议不得触碰）：决策追踪编号连续不跳号 / diagnose prompt 永不动（保 Flash eval）/ 四态出口契约（success/gap/partial/answered）/ L0 走购买·sim 充分非风险 / EMC 委托主 Toolbox 不自造 geo 端点 / aggregate 别名静默零（resolve_field_alias）
  · 评估产出规范：七轴评分（架构 18 / 代码 22 / 测试 13 / Harness 18 / 文档 9 / 调用效率 10 / 演示表现力 10）；SCAN 落 docs/catch-ball/scan/（命名 CB{NN}-{topic}_{env}-{model}_{YYYY-MM-DD}.md）；报告中文为主、路径保留英文、引用 `path:line`
- 下一步候选（做什么由 claude组/用户定·你待命评估）：P2-1 多要素黑名单增强 / PRM-07 字段名覆盖 P2 / RST-L06 Flash 方差 / KDE-DBSCAN 替代 Gi* / 时间轴 manifest / P3-1 依赖图 + P3-2 并行（$n 索引重构前置）/ 或用户新需求

第四步 · 环境自检（发现异常即报告，不阻塞）
- Python 可跑 py tests/browser/flywheel_audit.py 吗（Playwright 浏览器已装？）
- DEEPSEEK_API_KEY 就绪吗（跑测试必需）？.codex/ hooks 是否生效（SessionStart 打印 [HOOK]）？

第五步 · 待命
- 不要主动跑 B3 / 不要主动改代码。等 claude组 派活（下轮 = 后置项或用户新需求，进 CB 会发你 prompt）。
- 恢复期间如有疑问：先自查 KNOWLEDGE/RULES，再在 CB 框架内提出；git 操作一律留给 claude组。
```

---

*本卡由 claude组 编写（2026-08-09）·Codex 专用（glm组 复用 08-03 通用卡 + 本卡状态更新）。*
