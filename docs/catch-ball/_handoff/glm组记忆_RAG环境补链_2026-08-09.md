# glm组 记忆 · RAG 环境补链 learning（同步 claude组 KNOWLEDGE §2）

> **持有方**：glm组（ZCode + GLM-5.2 · 第三方评估）
> **日期**：2026-08-09 | **性质**：CB 记忆同步轮（承接 claude组「三组记忆完善 prompt」）
> **权威源**：[KNOWLEDGE.md §2](../KNOWLEDGE.md)（claude组 沉淀）+ 本文 = glm组 侧镜像笔记
> **登记**：`docs/context-map.md`（不孤岛·与 CB KNOWLEDGE 双链）

---

## 一、已同步的 learning（glm组 读 KNOWLEDGE §2 + 三组记忆完善 prompt 后确认）

### 1. RAG 链 = 纯 git 内数据源（不依赖 OneDrive/G 盘）

`tools/rag_index.py` 的 **4 个来源全部在 repo 内**：
- `docs/urban-renewal-plan/` 提炼笔记（note·185 条）
- `ai_qa/outlet_kb/` 三 py 文件（fact 36 + case 5 + concept 9）

**原始资料（OneDrive/G 盘 875+ docx/pptx/GIS）从不进索引**（CB-21b 共识：原文不复制进 repo，只存提炼笔记 + `{URENEWAL_ROOT}` 占位引用）。

→ **换环境 RAG 索引可独立重建，与 OneDrive 路径无关**。`{URENEWAL_ROOT}` 只影响 L0 原文溯源（`_PATHS.md` 双环境配置表）。

### 2. RAG 补链三步（踩坑）

| 步 | 动作 | 坑 |
|---|---|---|
| ① 依赖 | `pip install -r requirements-rag.txt` | **torch `2.13.0+cpu` PyPI 默认源无 `+cpu` 标签**（报 No matching distribution）→ 用阿里云 `mirrors.aliyun.com/pytorch-wheels/cpu/torch-2.13.0+cpu-cp314-cp314-win_amd64.whl`（官方 download.pytorch.org 国内慢/15min 无响应） |
| ② 模型 | 首次 build 自动下载 BGE `bge-small-zh-v1.5` | 脚本内置 `HF_ENDPOINT=https://hf-mirror.com` |
| ③ 索引 | `py tools/rag_index.py --build` | 索引不入 git·**每环境必建**·家建成 235 条 |

### 3. AMAP_KEY 历史会话可找回

高德 key 家环境 .env 重建时丢 → `~/.claude/projects/<proj>/*.jsonl` grep `AMAP_KEY=` 找回（已实测 `status=1` 有效）。**换环境补 .env 先查历史会话，别重复申请**。

### 4. 双环境路径（家庭已填·glm组 本机核对一致）

`docs/urban-renewal-plan/_PATHS.md`：办公室 `G:\` / 家庭 `C:\Users\Hi\OneDrive\2026\15_城市更新专项规划研究`。

**glm组 本机**：家庭路径 `C:\Users\Hi\` —— **与本机环境一致，确认正确**。

---

## 二、glm组 本机环境核对（2026-08-09 自检）

| 项 | 状态 | 证据 |
|---|---|---|
| git 同步 | ✅ | `fix/emc-buglog` @ `c18b106`（claude组 已 commit·本地工作区即最新·glm组 只读不 git） |
| Python | ✅ | `py -3.14` → 3.14.6（torch/st 装在此版）；**注意：`python`/`py` 默认可能解析到 3.13.2（无 torch）→ 跑 RAG 须显式 `py -3.14`** |
| RAG 依赖 | ✅ | torch `2.13.0+cpu` + sentence-transformers 5.7.0（`py -3.14 -c "import torch"` 通过） |
| BGE 模型 | ✅ | `~/.cache/huggingface/hub/models--BAAI--bge-small-zh-v1.5` 已下载 |
| 向量索引 | ✅ | `data/rag_index/` 235 条（`--stats`：fact 36 + note 185 + case 5 + concept 9·维度 512）·本环境已建 |
| 检索冒烟 | ✅ | `--query "葛洲坝有哪些更新项目"` → Top-5（fact URP-P02/P01 @ 0.752/0.704 + note）·**fact 已命中**·与 claude组 记录一致 |
| .env | ✅ | `DEEPSEEK_API_KEY` + `AMAP_KEY` 双 key 在位（glm组 本机 .env 完整） |

### ⚠️ glm组 本机特别注意

**Python 解析陷阱**：当前 shell `python` → 3.13.2（无 torch）；`py -3.14` → 3.14.6（有 torch）。
→ **跑 `tools/rag_index.py` 或任何 RAG 相关命令，必须显式用 `py -3.14`**，否则报 `ModuleNotFoundError: No module named 'torch'`。
（claude组 家环境记录 `python --version` 直接是 3.14.6，glm组 本机 shell 绑定不同，需显式指定。）

---

## 三、进入待命

glm组 环境就绪，等待 claude组 发 RAG 遗留任务分配（见 OFFICE 卡「明天待做」）：
B 路径（CB-22b） / 混合检索（P1） / 全仓 `[中文]+类` 扫描 / Recall@5 / P0-6 复审 / L2 出向。

**纪律**：评估方只读本地·不 git·产 SCAN 落 `docs/catch-ball/{scan,discuss}/` 由 claude组 push。

---

*glm组 · RAG 环境补链记忆 · 2026-08-09*
*权威源 = KNOWLEDGE.md §2 · 本文 = glm组 侧镜像*
