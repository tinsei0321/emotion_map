# CB-16 Wave 1（macro 出口）实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组预检反评价实施 Wave 1 macro 出口（commit 97cf232·分支 `fix/emc-buglog`·**未推·用户定"先验后推"**）。请两组**核验落地效果**（实施后检查·通过后 claude组 推）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③l。

---

【CB-16 Wave 1（macro 出口）实施后检查（Codex / glm组）】

背景：Wave 0 出口卡片完整链路已闭环。Wave 1 = macro 尺度出口（renewal_object_identify 更新对象识别 + checkup_dimension 体检四维度）。结构性缺口 = macro 分析（zonal/rank）权威产物是 rows 数组·而出口链路只收图层 fc + 不识别 {rows} → 三层断裂（不出卡/出空卡）。两组预检反评价全采纳（Codex P1 scale 限定 + claude组 P1×3 统一收/城市体检长词/data_base 语义 + rows 可达性缓存）。请核验落地效果。

第一步 · 同步
- git pull（fix/emc-buglog·最新 9ea1290）

第二步 · 核验已实施内容（对照两组预检 P1/P2 落地）

① `build_outlet_schema.py`（ai_qa/outlet_kb/）
- `_extract_emc_value` 统一收 rows/features（claude组 ①）：rows[0] 当 feature.properties（Top-1·与 features 同构）→ macro rows 产物取值非"暂无数据"
- field_mapping 循环加 `[scale=xxx]` 槽位限定（Codex P1）：仅填匹配 diagnose.scale 的维度·其余"需对应尺度分析"（治四维度×单尺度语义错位）
- `data_base` rows 分支（claude组 ⑤）：N=区域单元数（非评论数）·note 区分·total_points=sum(point_count) 总评论数

② `harness.js`（frontend/js/ai_qa/）
- `_lastToolRows` 模块级缓存×3（3 处工具调用点捕获 rows·治 rows 可达性：_maybeBuildOutletCard finalStep 后调·局部 r 出作用域）
- `_maybeBuildOutletCard` 产物收集优先 rows（macro 权威）·门放宽「rows 或 newLayerCount>0」（治"不出卡"）·无 rows 退图层 fc 保底

③ `emc-patterns.js`
- `DOMAIN_KW.urban_governance` 补「城市体检」长词（claude组 ③·防健康体检/体检中心误触）·TRIGGER_WORDS 保留「体检」（触发/domain 分离逻辑）

④ `urban_checkup_outlets.py`
- checkup_dimension 四维度 prose→真实字段（住房=issue_label+place_name[scale=micro]·小区=domain_top/element_top+polarity_index[scale=meso]·街区=issue_label+polarity_index[scale=meso]·城区=polarity_index+domain_top[scale=macro]）·字段∈_EMC_FIELDS 白名单

⑤ `e2e-seam.js` + 测试
- 钩子 `setOutletRows`/`buildOutletCardForTest`（seam 直测·不赌博 LLM 路由）
- test_outlet_schema.py 4 新增（rows 取值 / checkup_dimension scale 限定 meso+macro / 空 rows）
- test_outlet_macro.py E2E（两场景：rows 门放宽出卡 + scale 限定）

请检查：
1. `_extract_emc_value` 统一收 rows/features 是否正确（rows[0] 取值·features 兜底·顶层 dict 兜底顺序）？有无破坏既有 features 用例？
2. `_lastToolRows` 缓存×3 是否覆盖全部工具调用路径（runTemplatePath :555 / runChainPath :751 / runAllToolCalls :1847）？有无遗漏入口导致 macro rows 取不到？
3. 门放宽「rows 或 newLayerCount>0」有无副作用（无产物也出卡？空 rows 守卫？）
4. `[scale=xxx]` 槽位限定是否正确（macro 问句只填城区维度·其余需对应尺度·meso 问句填小区/街区）？
5. `data_base` rows 分支语义（N=单元数·total_points 总评论数·note 区分）是否不误导？
6. DOMAIN_KW「城市体检」长词触发正确性（健康体检/体检中心不误触·城市体检评估触发）？
7. 测试覆盖够（单测 4 新增 + E2E 两场景）？有无遗漏边界？

第三步 · 端到端验证（可复现）
```
py -m pytest tests/test_outlet_schema.py tests/test_outlet_kb.py tests/validate_outlet_trigger_sync.py -q   # 应 15+6+2 passed
py tests/browser/test_outlet_macro.py                                                                       # 应两场景全过（rows 门放宽出卡 + scale 限定）
```
另：浏览器问「各行政区更新优先级排序」（macro）→ 应出 renewal_object_identify 卡（更新对象 ← rows issue_label·非"暂无数据"）

请核验：
1. 单测/E2E 是否全过？
2. 浏览器端到端是否出卡（需起 serve）？
3. 有无边界遗漏（空 rows / 多工具链 rows 覆盖 / scale 限定边界）？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave1完成-*_2026-08-04.md
- 判定：Wave 1 是否通过·有无需修项（P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 1 macro 出口实施后检查（先验后推）。*
