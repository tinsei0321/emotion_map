# CB-16 Wave 0 剩余实施预告（Codex / glm组 · 2026-08-03）

> **用途**：claude组 将实施 Wave 0 剩余两项（条件触发词表镜像到 emc-patterns.js + 前端 outlet-card 渲染）。实施前发预告，请两组预检方案（按 CB 机制"每轮工作进 CB"）。三 bug 已修复（f84b3ae）。
> **登记**：docs/context-map.md · cb-journal CB-16。

---

【CB-16 Wave 0 剩余实施预告（Codex / glm组）】

背景：Wave 0 三 bug 已修复（qualifier/更新词/体检 domain·commit f84b3ae·已 push）。
本轮实施 Wave 0 剩余两项，实施前请预检方案。

第一步 · 同步
- git pull（fix/emc-buglog·最新 4369df2）

第二步 · 本轮实施方案（请预检）

① 条件触发词表镜像（emc-patterns.js）
- 在 `frontend/js/ai_qa/emc-patterns.js` 末尾加 `OUTLET_TRIGGER_KW`（镜像
  `ai_qa/outlet_kb/build_outlet_schema.py` TRIGGER_WORDS·8 词）
- 注释标注"与 build_outlet_schema.py TRIGGER_WORDS 同步·单一权威源在后端"
- 用法：仅 UI 提示（如 panel 显示"即将生成行业出口卡片"）·不改路由/控制流
- **同步守卫**：新增 `tests/validate_outlet_trigger_sync.py`（仿 validate_field_dict_sync.py·
  校验前后端词表一致·防漂移）

请预检：
1. OUTLET_TRIGGER_KW 放 emc-patterns.js 末尾是否合适（与其他 _KW 同层）？
2. 同步守卫方式（validate_outlet_trigger_sync.py 仿 field_dict_sync）是否对路？
3. "仅 UI 提示不改控制流"是否符合出口驱动开发（触发判定收敛在后端·前端只提示）？

② 前端 outlet-card 渲染（仿 .cpd-guide-card·既有 token）
- 新增 `outlet-card` 渲染（7 要素 → markdown/HTML 卡片·数据全来自 build_outlet_schema JSON·
  前端不计算不补字段）
- 用 `design/tokens.css` 既有变量·不新造样式·局限标注用 `> ` 引用块（与 CB-12 降级格式一致）
- 字段缺失显示"暂无数据"灰色文本（与后端降级一致）
- {{show:图层}} 联动复用 renderAnswer 的 ref 解析（不另造联动机制）

请预检：
1. outlet-card 渲染为纯模板函数（后端 JSON → 前端摆）是否符合分层？
2. 字段缺失灰色文本 + 局限引用块是否符合设计语言？
3. {{show:}} 复用 renderAnswer ref 解析是否对路？

第三步 · 产出
- 简短 SCAN（预检方案 + 提示风险）落 docs/catch-ball/scan/CB16-Wave0剩余实施-*_2026-08-03.md
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-03）·Wave 0 剩余实施预告（触发词表镜像 + 前端渲染）。*
