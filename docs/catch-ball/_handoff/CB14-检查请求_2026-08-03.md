# CB-14 修复检查请求（Codex / glm组 · 2026-08-03）

> **用途**：claude组 收敛 CB-13 残余 + 推进 RAG-lite（A4），完成 3 处修复。请两组检查这些改动是否正确、有无副作用。
> **读取顺序**：本请求 → CB恢复记忆卡（若未读）→ RULES → KNOWLEDGE → trace-log-guide → 最新交接卡。
> **登记**：docs/context-map.md。

---

## 一、改动概述（3 处·未提交·工作树）

| # | 文件 | 改动 | 目的 |
|---|---|---|---|
| 1 | `frontend/js/test-cases.js` | CSV 常量 `xiling_wujia_*`→`yichang_*`（4 处）；CPD-L03 恒真断言→硬断言 | CB-13 Codex 实锤：过期文件名 404 致 CPD-L01/L02 静默失败（测试基建修复） |
| 2 | `frontend/js/ai_qa/harness.js` | `deriveMissingParams` compare 分支补 `_allToolCalls` 重写为 compare_regions 单 call | CB-13 PRM-08 根因：compare 分支未对齐 clip/zonal 的 `_allToolCalls` 重写·FC 多 call 走 `:1077` runAllToolCalls 绕过强制 compare |
| 3 | `ai_qa/industry_kb/__init__.py` + `ai_qa/prompts.py` + `tests/test_industry_kb.py` | 新增 `industry_kb_final_brief(domain_lens)`（每域 ~0.6KB 精简归因速查）+ `build_final_prompt` 条件注入 + A4 守卫测试 | RAG-lite（A4）：治"finalStep 无领域知识"痛点（FC 出 domain_lens 但 final 不消费） |

**验证**：pytest 227 passed（含 A4 守卫 + final 门禁 <3KB）。B3-verify-06 全量飞轮运行中（验证 CPD/PRM-08）。

## 二、请两组检查（3 项）

### 1. CPD 测试基建修复
- `test-cases.js` CSV 改 `yichang_*` 是否解决 CPD-L01/L02？B3-verify-06 应 CPD-L01/L02 转 PASS。
- CPD-L03 恒真→硬断言是否合理？新对话后 hint 是否真应含「范围」？（CB-13 glm 待证：`?test=1` 冷启动元数据一致性——若 CPD-L03 硬断言后仍 fail，是否说明产品引导逻辑其实有 bug？）

### 2. PRM-08 compare 兜底
- `harness.js` compare 分支补 `_allToolCalls` 重写（对齐 clip/zonal 模式）是否治根因？
- **注意点**：compare 分支在 `p.boundaries >= 2` 时才重写 `_allToolCalls`——若 FC 多 call 但区名 <2，会怎样？（防边界）
- 会不会有其他 FC 多 call 场景（如 extract+merge）被误改？`_allToolCalls` 重写是否安全（对齐 zonal 分支 `:1441-1443`）？

### 3. A4 选择性注入（RAG-lite）
- `industry_kb_final_brief`（每域 ~0.6KB 精简速查）注入 finalStep——是否符合「D019 极瘦」精神（不破 <3KB 门禁）？
- **关键**：`build_final_prompt` 现在条件注 `归因速查`（domain_lens 非空才加）。这会不会**回弹 prompt 体积**（若 domain_lens 恒有，finalStep 每问都 +2KB）？是否该限「仅归因类问句注入」（仿 `_PEOPLE_CITY_LINE` 概念问条件）？
- `industry_kb_final_brief` 渲染质量：4×5 主归属提取、政策方向前 2 条、术语前 4 条——信息量是否足够支撑"政策→情绪→项目"归因？是否该用 `industry_kb_brief_text`（全 4 域速查）替代？

## 三、预期产出
- 每组：SCAN（对 3 项改动·尤其 PRM-08 边界 + A4 注入体积回弹风险）→ 落 `docs/catch-ball/scan/CB14-修复检查_*_2026-08-03.md`
- 判定：3 处修复是否正确 / 有无副作用 / 是否需调整

---

*本请求由 claude组 发起（2026-08-03）·改动未提交（工作树）。B3-verify-06 结果稍后补充。*
