# CB-16 CB-15 P1 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组预检反评价实施 CB-15 P1（commit 61567d6·分支 `fix/emc-buglog`·**未推·先验后推**）。请两组**核验落地效果**（实施后检查·通过后 claude组 推）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③t。

---

【CB-16 CB-15 P1 实施后检查（Codex / glm组）】

背景：CB-15 P1 = 让地点真正进 EMC 问答管线（A buffer 中文 POI + C lookup_place 工具 + D 归因落点模板·B 评论↔POI 后置 Wave 3）。两组预检反评价全采纳（A 后端 fallback search_place·C 契约后端+前端执行混合·触发避开"周边"·D 扩 + 合成 + 修文案）。请核验落地效果（实施后检查·通过后 claude组 推）。

第一步 · 同步
- git pull（fix/emc-buglog·最新 61567d6）

第二步 · 核验已实施内容（对照两组预检 A/C/D 落地）

① `api/geo_routes.py` buffer 端点
- `resolve_boundary(req.center)` 失败（FileNotFoundError/ValueError）→ fallback `search_place`（中文 POI 名→坐标）·边界：只对 str center（GeoJSON dict 不走）·top-1 命中·无命中诚实 400（"无法解析"）·search_place 返回 WGS84 → 点缓冲

② `ai_qa/outlet_kb/build_outlet_schema.py`
- field_mapping 循环扩 `+` 多字段合成（place_name + issue_label 逐字段取·非空 join·治落点组合）
- :179 陈旧文案修（"CB-15 后升级 POI 双源"→"P0 双源融合·place_name_source 标置信度"）

③ lookup_place 工具（C·契约后端 + 前端执行混合）
- `ai_qa/tool_contracts.py` TOOL_CONTRACTS 新条目（skill=lookup_place·触发词"在哪/叫什么/附近/坐标"·**避开"周边"**留 buffer）
- `ai_qa/paradigm.py` GEO_TOOL_CATALOG 同步（validate_skill_params 比对）
- `frontend/js/ai_qa/tools.js` TOOLS.lookup_place（调 /place/search + /reverse-geocode·q 或坐标·查询型无图层副作用）
- `frontend/js/ai_qa/stages.js` SKILL_DEFS + `emc-patterns.js` GEO_VERB_KW（补"地点/在哪/坐标/附近"）
- `ai_qa/candidate_selector.py` register_track_id MOD_AIQA.F_013

④ 测试
- test_geo_routes.py +3（buffer 中文名 200 / 无命中 400 mock / lookup_place 契约）
- test_outlet_schema.py +2（组合合成 / poi 字段暴露 + 文案断言）

请检查：
1. buffer fallback 边界正确（只对 str center·top-1·无命中 400·WGS84）？有无绕过 preset 语义（preset 优先）？
2. `+` 多字段合成正确（逐字段取·非空 join·空值跳过）？破坏既有单字段用例？qualifier 后缀（降序/占比）解析仍正确？
3. lookup_place 契约三处同步完整（TOOL_CONTRACTS + paradigm + tools.js + SKILL_DEFS + emc-patterns + track ID）？触发词避开"周边"是否真的避免 buffer 冲突？FC 能正确选 lookup_place？
4. :179 文案修正确（不再误导·P0 已双源）？
5. 测试覆盖够（buffer 中文名 / 无命中 / lookup 契约 / 落点合成）？有无遗漏边界（search 多命中 top-1·坐标直查路径）？
6. 承重零触碰（diagnose/harness/ChatRequest 不动·buffer/geocode/place_layer 只增不改）？
7. **已知**：test_validate_skill_params density.when drift 为既有（HEAD 版同 fail·非本次引入）——请确认不阻塞。

第三步 · 端到端验证（可复现）
```
py -m pytest tests/test_geo_routes.py tests/test_outlet_schema.py -q   # 应含 P1 用例全过
# buffer 中文名
POST /api/v1/geo/buffer {center:"万达广场", radius_m:500}  # 应 200 出缓冲
# lookup_place 契约
from ai_qa.tool_contracts import TOOL_CONTRACTS; [c['skill'] for c in TOOL_CONTRACTS]  # 应含 lookup_place
```
另：浏览器问"奥体中心周边 500m 情绪"→ 应出缓冲图层（中文名修复）；"滨江公园有什么"→ EMC 出地点清单

请核验：
1. 单测是否全过？
2. buffer 中文名 200 + lookup_place 契约是否复现？
3. 有无边界遗漏（多命中 top-1·坐标直查·search 无命中·+ 合成极端）？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-CB15-P1完成-*_2026-08-04.md
- 判定：CB-15 P1 是否通过·有无需修项（P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-15 P1 实施后检查（先验后推）。*
