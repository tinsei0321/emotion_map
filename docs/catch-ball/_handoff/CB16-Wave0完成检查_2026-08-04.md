# CB-16 Wave 0 完整链路实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组预检实施 Wave 0 完整链路（commit 38d3a0c + bd3ccce·已 push）。请两组**核验落地效果**（实施后检查）。
> **登记**：docs/context-map.md · cb-journal CB-16。

---

【CB-16 Wave 0 完整链路实施后检查（Codex / glm组）】

背景：Wave 0 出口卡片完整链路已实施（按两组预检：Codex 关键缺口"无前端接线" + glm 排除表镜像建议·全部采纳）。请核验落地效果。

第一步 · 同步
- git pull（fix/emc-buglog·最新 bd3ccce）

第二步 · 核验已实施内容（5 环节）

① 后端端点 `/api/v1/aiqa/outlet_card`（api/aiqa_routes.py）
- 接收 {question, diagnose, result} → build_outlet_schema → {card: {...}} 或 {card: null}
- 确定性组装·不调 LLM·字段缺失降级·诚实标注

② 前端接线（harness.js）
- `_maybeBuildOutletCard`：result 态后条件调（OUTLET_TRIGGER_KW 触发·UI 语境排除·产物图层收集·失败静默）
- onFinalDone 后调用·`hooks.onOutletCard(card)` 派发

③ 前端渲染（panel.js + ai_qa.css）
- `renderOutletCard` 纯模板（仿 .cpd-guide-card·既有 token·7 要素·字段缺失灰·局限引用块·{{show:}} 复用 renderAnswer）
- `.outlet-card` CSS（独立组件·accent 顶条·同 cpd-guide-card 语言）

④ 触发词表镜像（emc-patterns.js）
- OUTLET_TRIGGER_KW（8 词）+ OUTLET_UI_EXCLUDE_KW（5 词·含排除表·Codex/glm 建议）

⑤ 同步守卫（tests/validate_outlet_trigger_sync.py）
- 双份校验（8 词 + 5 排除词）·2 passed

请检查：
1. 后端端点契约是否合理（入参/出参/错误处理）？有无安全/边界问题？
2. 前端接线是否符合"纯增量不碰承重路径"（result 态后·不撞 harness 主循环/ChatRequest）？
3. renderOutletCard 纯模板是否符合分层（数据全来自后端·前端不计算）？{{show:}} 复用是否对路？
4. 触发词表镜像 + 排除表是否完整（前端提示与后端触发一致）？
5. 同步守卫是否覆盖够（8 词 + 5 排除词双份）？

第三步 · 端到端验证（可复现）
```
POST /api/v1/aiqa/outlet_card
{question: "西陵区老旧小区更新需求分析", diagnose: {scale: meso, domain_lens: [urban_renewal], outlet: 建议清单},
 result: {polarity_index: -0.32, point_count: 1247, features: [{properties: {place_name: 夷陵广场, issue_label: 停车难}}]}}
→ {card: {interface: 片区策划·问题清单/需求分析, fields: {问题类型: 停车难, 需求强度: -0.32, 需求位置: 夷陵广场}, limitations: [...]}}
```
另：浏览器问"西陵区老旧小区更新需求分析"→ 应出 outlet-card（7 要素·缺失灰·局限引用块）

请核验：
1. 端点返回是否符合预期（7 要素齐全·诚实标注）？
2. 浏览器端到端是否出卡（需起 serve·重启后端）？
3. 有无边界遗漏（问句无接口词/UI 语境/字段缺失/多契约）？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave0完成-*_2026-08-04.md
- 判定：Wave 0 完整链路是否通过·有无需修项
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 0 完整链路实施后检查。*
