# CB-16 发版 backlog 收尾 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按 ③w6b 反评价实施（4 项·23efe74·**未推·先验后推**）。请两组**核验落地效果**。
> **登记**：docs/context-map.md · cb-journal CB-16 ③w7。

---

【CB-16 发版 backlog 收尾 实施后检查（Codex / glm组）】

背景：③w6b 两组预检通过（Codex P1 fallback MC bug + 破坏性依赖·glm fixture 治本·用户拍板「清理 + 同步 district-stats」）。claude组 实施。请核验。

第一步 · 读本地文件（同一工作区·**无需 git pull/push**·直接读）
- `frontend/js/ai_qa/harness.js`（项1 footer 条件化 :240·项4 RST-L06 fallback MC :1136）
- `DATA/boundaries/presets/行政区.geojson`（项3 清 4 要素·备份 .bak9）+ `frontend/js/district-stats.js`（_TUAN_MAP 8→4 组团）
- `tests/eval_template_flash.py`（项5 注释精确化）

第二步 · 核验已实施内容

**项 1 footer 条件化**（Codex/glm P2）
- composeGapCard footer：`_footerLayer = (failedObs && failedObs.length > 0) ? '或未生成图层' : ''`——failedObs>0 才「未生成图层」·零工具尝试改「未完成分析」·与 request_upload 分支不冲突（glm 建议实现）

**项 3 preset fixture 清理 + district-stats 同步**（用户拍板·治本）
- `行政区.geojson` 9 → **4 要素**（西陵/伍家岗/猇亭/点军·FIXED_ADMIN_DISTRICTS）·备份 `.bak9`（保 BOM）
- `district-stats.js _TUAN_MAP` 8 → **4 组团**（西陵/伍家岗/猇亭/点军·法定功能区不再预置）
- 影响：PRM-07 根治（法定功能区不在 preset·FC 无法直供）·L1 数据总览 4 组团

**项 4 RST-L06 fallback MC 修复**（Codex P1 死代码）
- harness.js chain pre-check fallback：匹配字段补 `MC`（行政区 preset 仅 MC·manifest nameField=MC·原 name/NAME/name_field 恒缺→永不命中）
- **复跑待前台 serve 环境**（serve 后台不稳·RST-L06 浏览器验证留待前台·backlog）

**项 5 eval 注释精确化**（Codex P2）
- 注释改「select_template 对多数单工具问返单工具·复合问（…并排序）可能返 multi·标尺对齐多数情况」

请检查：
1. footer 条件化逻辑正确？零工具尝试 footer 无「图层」·与 request_upload 分支不冲突？
2. fixture 清理（9→4）+ district-stats（8→4）正确？备份 .bak9 合理？L1 数据总览 4 组团行为正常？
3. RST-L06 fallback MC 修复对？死代码消除？（复跑待前台 serve·确认不阻塞）
4. eval 注释精确化合理？
5. 影响面：preset 清 4 会不会破坏其他依赖 9 要素的功能？（manifest nameField=MC 不变·其他引用方核查）
6. 承重零触碰（diagnose prompt / 三态出口结构 / ChatRequest 不动）？
7. 测试够（pytest 277 + ESM-OK）？需补 fixture 清理相关断言吗？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Backlog收尾检查-*_2026-08-04.md
- 判定：实施是否通过·有无需修项（P0/P1/P2）·独立判断·不要互相参考

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-16 发版 backlog 收尾 实施后检查（先验后推）。*
