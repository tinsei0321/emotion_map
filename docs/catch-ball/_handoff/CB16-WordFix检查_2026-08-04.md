# CB-16 措辞修复 + 发版遗留问题 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按 ③w4b 反评价实施（②a 措辞 + ②b eval/RST-L06/PRM·c53aa99·**未推·先验后推**）。请两组**核验落地效果**。
> **登记**：docs/context-map.md · cb-journal CB-16 ③w5。

---

【CB-16 措辞修复 + 发版遗留问题 实施后检查（Codex / glm组）】

背景：③w4b 两组预检通过（glm 标尺纠错 + Codex P1×5 修正·全采纳）。claude组 实施。请核验落地效果。

第一步 · 读本地文件（同一工作区·**无需 git pull/push**·直接读）
- `frontend/js/ai_qa/harness.js`（②a 措辞 :1310 + composeGapCard :227 分支·②b-2 RST-L06 :1113 fallback·②b-3 buffer :1572 门控）
- `tests/eval_template_flash.py`（②b-1 标尺 :88/89 multi→clip/density）
- `tests/reports/report-2026-08-04-01-llm.md`（B3 快照基线）

第二步 · 核验已实施内容

**②a 措辞修复**（failedObs 判据）
- `harness.js:1310` gap 出口：`failedObs.length > 0`（在试图层）→ 现有「未产出新图层」措辞；`=== 0`（零工具尝试）→ composeGapCard 内分支（非图层叙事）
- `composeGapCard :227` 默认分支：failedObs 空时区分两子情况——`diagnose.degraded`（无法理解）vs 非 degraded（暂无法回答）·不再说「没能生成可用的图层」
- exit:'gap' 结构不动·quickIntent/answered/narratedAnswer 不动

**②b-1 eval 标尺**（glm 标尺纠错）
- `eval_template_flash.py :88/89`：multi → 实际单工具（`西陵区的商业用地`→clip·`西陵区范围内密度分析`→density）·注释明示 select_template 单工具不返 multi·改标尺非改路由
- **重跑验证：34/37 = 92%**（76%→92%·超 80% gate·GO）·MISS 3 为既有歧义（rank/zonal·clip/overlay·hotspot/density）

**②b-2 RST-L06 硬化**
- `harness.js:1113` chain pre-check：deriveAvailable 无匹配 + 问句含区名（`/(.+?)(?:区|市|县)/`）→ fallback 到「行政区」preset layer·**取单要素**（仿 :1453-1460）·无区名不猜

**②b-3 buffer stale-tool 门控**（Codex P1①）
- `harness.js:1572` radius 门控改判 `diagnose.template === 'buffer'`（G5 reroute 后 template 更新·局部 tool 变量旧值）·cell_size 门控同改（:1568）

**②b-4 PRM-07**：白名单门控（tools.js FIXED_ADMIN_DISTRICTS）已在 deriveAvailable 层实现·preset geojson 含法定功能区是数据 fixture 问题 → **留 backlog**（改数据需用户确认）

请检查：
1. 措辞两处分支（gap 出口 + composeGapCard）逻辑正确？failedObs 判据 + degraded 区分可靠？「图层」字眼不再出现在零工具尝试场景？
2. eval 标尺改（multi→clip/density）合理？92% 验证可信？MISS 3（既有歧义）确认非标尺引入？
3. RST-L06 fallback（preset feature 级 + 区名条件）对？无区名不猜正确？
4. buffer stale-tool 门控改判 diagnose.template 正确？cell_size 同改无副作用？
5. PRM-07 留 backlog（数据 fixture）合理？
6. 承重零触碰（diagnose prompt / 三态出口结构 / ChatRequest 不动）？
7. 测试够（eval 92% + pytest 277 + ESM-OK）？需补 e2e-seam（措辞分支断言）吗？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-WordFix检查-*_2026-08-04.md
- 判定：实施是否通过·有无需修项（P0/P1/P2）·独立判断·不要互相参考

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-16 措辞 + 发版遗留 实施后检查（先验后推）。*
