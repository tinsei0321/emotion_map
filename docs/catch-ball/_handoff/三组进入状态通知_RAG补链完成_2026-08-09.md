# 三组进入状态通知 — 家环境 RAG 补链完成 · RAG 遗留工作启动前

> **发起方**：claude组（Claude Code·家环境）| **日期**：2026-08-09 | **性质**：CB 同步轮（三组就绪·待命）
> **承接**：公司 08-09 全链（CB-21b + CB-22·`56cd67b`）→ 家环境 pull 同步（`3abe31e`/`a1a5205`）→ 环境检查发现 RAG 缺项 → **claude组 已补链**
> **目的**：① 通知 Codex/glm组 claude组 家环境 RAG 链已就绪·三组环境一致 ② 两组各自 pull + 核对自家 RAG 链（按 OFFICE 换环境提醒）③ 进入待命·等 claude组 发明天 RAG 遗留任务分配

---

## 〇、一句话结论

**claude组 家环境 RAG 链已补齐（依赖 + BGE 模型 + 索引重建）·核心环境三组一致就绪。请两组 git pull 最新 → 按 OFFICE 卡核对自家 RAG 链（缺则补）→ 进入待命，等 claude组 发 RAG 遗留任务分配。**

---

## 一、claude组 家环境状态（已补齐）

| 项 | 状态 | 证据 |
|---|---|---|
| git 同步 | ✅ | `fix/emc-buglog` @ `a1a5205`（含公司 08-09 全链 + 同步 commit） |
| Python | ✅ | 3.14.6 |
| RAG 依赖 | ✅ | torch `2.13.0+cpu`（**PyPI 默认源无 `+cpu` 标签版·须从 PyTorch CPU index 或国内镜像拉**——阿里云 `mirrors.aliyun.com/pytorch-wheels/cpu` 可用）+ sentence-transformers 5.7.0 |
| BGE 模型 | ✅ | `BAAI/bge-small-zh-v1.5`（脚本内置 `HF_ENDPOINT=https://hf-mirror.com`·已下载到 `~/.cache/huggingface`） |
| 向量索引 | ✅ | `data/rag_index/` 重建 **235 条**（事实 36 + 笔记 185 + case 5 + 概念卡 9·维度 512·原子写）·不入 git·本环境已建 |
| 检索冒烟 | ✅ | `--query "葛洲坝有哪些更新项目"` → Top-5（含 fact URP-P02/P01 0.752/0.704 + note·**fact 已可命中**·对比 OFFICE 卡记录「Top-5 全 note」） |
| Playwright / trace / pytest | ✅ | 1.61 / 可跑 / 41 冒烟 |
| .env | ⚠️ | 仅 `DEEPSEEK_API_KEY`·**缺 `AMAP_KEY`**（B3 高德类用例需补） |

## 二、两组动作（收到本通知后）

1. **git pull** 拉取最新（本通知 + 同步 commit）
2. **核对自家 RAG 链**（OFFICE 卡换环境提醒）：
   - `pip install -r requirements-rag.txt`——若报 `torch==2.13.0+cpu` 找不到，改用 `pip install "https://mirrors.aliyun.com/pytorch-wheels/cpu/torch-2.13.0+cpu-cp314-cp314-win_amd64.whl"`（或等价国内镜像）
   - `py tools/rag_index.py --build` 重建索引（索引不入 git·**每环境必建**）
   - 首次跑 build 自动经 HF 镜像下载 BGE 模型
3. **进入待命**：等 claude组 发明天 RAG 遗留任务分配（见下）

## 三、待做任务预告（RAG 遗留·OFFICE 卡）

| # | 任务 | 说明 |
|---|---|---|
| 1 | **B 路径**（CB-22b·query_knowledge_base 确定性查询） | RAG_QUERY_KW 临时结构化词待迁移·`knowledge_query` 范式已预留 |
| 2 | **混合检索**（P1） | fact 加权或 Top-5 保底 ≥1 fact·降 note 占比 |
| 3 | **全仓 `[中文]+类` 扫描** | 黑名单机制 + 人工审新词·防误拦官方词 |
| 4 | **Recall@5 素材质量机制** | Codex V5·黄金集 Recall≥80% 持续跟踪 |
| 5 | **P0-6 分通道 tier 复审** | 暂缓·路径跑顺后勿忘 |
| 6 | **L2 出向任务** | outlet_kb 接入运行时·进 CB 讨论 |

## 附 A：通知 prompt（发给两组用）

```text
【CB-22 收工后 · 三组进入状态】情绪地图 — claude组 → Codex/glm组（2026-08-09）

claude组 家环境 RAG 链已补齐（torch 2.13.0+cpu + sentence-transformers 5.7.0 + BGE 模型 + 索引重建·阿里云镜像可用）。三组核心环境一致就绪。

请两组：
1. git pull 拉取最新（本通知 + 同步 commit @ a1a5205）
2. 核对自家 RAG 链（OFFICE 卡换环境提醒）：
   - 依赖：pip install -r requirements-rag.txt（若 torch==2.13.0+cpu 找不到·改用国内 PyTorch 镜像）
   - 索引：py tools/rag_index.py --build（索引不入 git·每环境必建）
   - 模型：首次 build 自动经 HF 镜像下载 BGE
3. 进入待命·等 claude组 发明天 RAG 遗留任务分配（B 路径 / 混合检索 / 全仓 [中文]+类 扫描 / Recall@5 / P0-6 复审 / L2 出向）

有疑问先自查 KNOWLEDGE/RULES·git 操作留给 claude组。回复：确认就绪 / 异常清单。
```

---

*claude组 · 三组进入状态通知 · 2026-08-09*
*Codex/glm组 收到后回复就绪状态·claude组 收集后发任务分配*
