# CB-16 Wave 3 实施预检（Codex / glm组 · 2026-08-04）

> **用途**：Wave 3（可感知计算器 + validate_outlet_fields CI + 多卡支持 + B 评论↔POI 批量）落地前，claude组 发起两组预检（用户定：走 CB 流程·先讨论再实施）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③w。

---

【CB-16 Wave 3 实施预检（Codex / glm组）】

背景：CB-15 P0/P1 已闭环（place_name 双源 + POI 接入 + lookup_place + buffer 中文修复）。Wave 3 = 出口抽象层深化最后一块：可感知 10 项计算器（compute_perceptible_metrics）+ validate_outlet_fields CI（防死字段·Codex CI 守卫）+ 多卡支持 + B 评论↔POI 批量（P1 后置）。claude组 探索确认现状。请预检下方实施草案。

第一步 · 同步
- git pull（fix/emc-buglog·最新 04a6487）
- 读探索依据：`ai_qa/outlet_kb/urban_checkup_outlets.py:58-117`（可感知 10 项 METRIC_MAPPINGS·emc_field 表达式）· `ai_qa/outlet_kb/build_outlet_schema.py`（resolve_outlet_id 只返最高分单契约·field_mapping 消费）· `tests/validate_outlet_trigger_sync.py`（AST 解析同步守卫·validate_outlet_fields 模式参考）· `api/geo_routes.py:613`（sjoin_nearest 范本）· `core/spatial_analysis.py _attach_poi_attrs`（POI sjoin 模式）

第二步 · 实施草案（4 子项·请预检）

**1. B 评论↔POI 批量**
- `core/spatial_analysis.py` 新函数 `attach_nearest_poi(points_gdf, max_dist_m=1000)`：点 × all_pois `sjoin_nearest`（4546 米制·参照 /geo/nearest）→ 落新列 `nearest_poi_name`/`nearest_poi_dist_m`（禁覆盖 area_seed/spatial_hotspot·运行时附加不改 CSV）

**2. 可感知 10 项计算器（compute_perceptible_metrics）**
- 输入：聚合产物 result（rows/features 字段）·对 METRIC_MAPPINGS 可感知 10 项：解析 emc_field 表达式（"字段A + 字段B（说明）"）→ 从 result 取字段 → 计算指标值（极性/主题命中）
- 输出：`[{metric, value, source, industry}]`（确定性·不调 LLM）·落点 /outlet_card 扩展（card.perceptible_metrics）

**3. validate_outlet_fields CI**
- 仿 validate_outlet_trigger_sync（AST 解析）：提取 CONTRACTS field_mapping + METRIC_MAPPINGS emc_field → 被消费字段集合 → 对比聚合产物实际字段 → 未消费标"死字段"·消费但产物缺标"缺消费"

**4. 多卡支持**
- resolve_outlet_id 返回最高分单契约 → 改支持多契约命中（不同 domain 各一张）·build_outlet_schema 返回 cards 列表·/outlet_card 返回 cards·前端 renderOutletCard 多卡渲染

请预检：
1. **B 评论↔POI**：attach_nearest_poi sjoin_nearest 方案对路？性能（4310 POI × N 点）？落新列策略（nearest_poi_name/dist·禁覆盖 area_seed）？max_dist_m 阈值？
2. **可感知计算器**：emc_field 表达式解析方案对路（复用 build_outlet_schema + 合成逻辑？）？10 项指标计算语义（极性值/主题命中·怎么算）？落点 card.perceptible_metrics 合理？
3. **validate_outlet_fields CI**：AST 解析提取消费字段→对比产物字段·"死字段/缺消费"判定合理？放 tests/ 还是运行时？
4. **多卡支持**：多契约命中（不同 domain）出多卡·对 resolve_outlet_id/build_outlet_schema/前端兼容性影响？会不会破坏既有单卡用例？
5. **范围边界**：四子项都必要？优先级？（B 评论↔POI 已后置一次·是否 Wave 3 核心？）P2/P3 后置？
6. **测试方案**：compute_perceptible / validate_outlet_fields / 多卡 / attach_nearest_poi 单测 + 端点直测够？
7. **承重零触碰**：不碰 diagnose/harness/ChatRequest·确定性组装不变·多卡兼容既有？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave3-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 3 实施预检。*
