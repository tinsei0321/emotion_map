# CB-16 Wave 2 / CB-15 数据认知实施预检（Codex / glm组 · 2026-08-04）

> **用途**：Wave 2（CB-15 数据认知）落地前，claude组 发起两组预检（用户定：Wave 2 保持 CB 机制·先讨论再实施）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③o。

---

【CB-16 Wave 2 / CB-15 数据认知实施预检（Codex / glm组）】

背景：CB-15 讨论稿已收敛（五点共识 + glm 致命发现已核实）+ 出口抽象层 Wave 0/1 已闭环。Wave 2 = CB-15 **P0 落地**：下钻链最小闭环——格↔POI sjoin（聚合输出 poi_names）+ 修正 place_name 双源融合（sjoin POI 优先·fallback spatial_hotspot·再 area_seed）+ `/grid/pois` 端点（选中格子按需）+ 3220 POI 接入（DATA/POI/yichang_pois_wgs84.geojson 孤岛入运行时）。

现状（claude组 探索）：
- `core/spatial_analysis.py:585-597` `_attach_4x5_attrs` 的 `place_name` 只读点侧 `spatial_hotspot`/`area_seed`（sim 期标注·真实数据恒空·glm 致命发现已核实）
- `core/place_layer.py` `PlaceLayer`（get_place_layer·_read_pois·reverse）已存在——3220 POI 载入 `_load()`·`reverse(lng,lat)` 给最近 POI + 近邻 top-5/计数
- `core/geocode.py:263` `reverse_geocode` 已接本地 reverse 主 + 高德 regeo 兜底
- DATA/POI/yichang_pois_wgs84.geojson（3220·孤岛无消费方）·place_layer 的 _load 已读它（来源=amap）

草案（请预检）：
① **place_name 双源融合**（`_attach_4x5_attrs`）：聚合时对每格 sjoin 格×POI（poi 点 ∈ 格 polygon）→ 格内 POI 清单（名称/类别/domain/element）→ `place_name` = 格内最高频 POI 名称（sjoin 优先）·fallback `spatial_hotspot` 众数·再 `area_seed` 众数（保旧 sim 数据兼容）
② **聚合输出 poi_names**：`aggregate_by_polygons`/`create_*_grid` 产物加 `poi_names` 列（格内 POI 清单·逗号分隔 top-N·防全量清单配额爆）
③ **`/grid/pois` 端点**：选中格子（zonal index / 质心坐标）→ 返回格内 POI 清单（名称/类别/domain/element/坐标）·按需重查（对齐悬停试探/点击锁定·双层触发范式）
④ **3220 接入**：复用 place_layer（已读 yichang_pois）·不新建孤岛·评论↔POI 最近反查（复用 reverse_geocode）
⑤ **承重零触碰**：不碰 diagnose/harness/ChatRequest·place_layer/geocode 只增不改·新工具走契约三处同步 + 连续追踪 ID

请预检：
1. **place_name 双源融合**（sjoin POI 优先 → fallback 标注）方案是否对路？sjoin 性能（3220 POI × N 格·每聚合一次全量 sjoin 开销？）有无更优实现（如先建格索引再空间索引查询）？
2. **poi_names 输出**：格内 POI 清单 top-N（N=?）·逗号分隔还是结构化（JSON 数组）？防配额爆（全量清单）是否够？
3. **`/grid/pois` 端点**：请求参数（格子 id/质心坐标？）·返回结构（POI 名称/类别/domain/element/坐标）？与既有 zonal/rank rows 结构兼容？LLM 问答如何用（P1 lookup_place 之前·前端点击先用）？
4. **3220 接入**：place_layer 已读·是否够（来源判定/去重 3220 vs 1270）？还是需独立 POI 消费层（问答管线可见）？
5. **承重零触碰**：place_name 改动在 `_attach_4x5_attrs`（聚合层）——会影响既有 zonal/grid 产物吗（回归风险）？旧 sim 数据 fallback 兼容？
6. **测试方案**：单测（place_name 双源融合·poi_names·grid/pois 端点）+ 端到端（大南门 ermawu 数据·POI sjoin 出地点清单）够？
7. **边界**：本波只做 P0（下钻链最小闭环）·P1（lookup_place/归因落点模板）P2（配额节流/拓扑端点）后置——有无越界风险？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave2-CB15-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 2 / CB-15 数据认知实施预检。*
