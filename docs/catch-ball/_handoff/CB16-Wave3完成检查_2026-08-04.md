# CB-16 Wave 3 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组预检反评价实施 Wave 3（commit 1daffc6·分支 `fix/emc-buglog`·**未推·先验后推**）。请两组**核验落地效果**（实施后检查·通过后 claude组 推）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③x。

---

【CB-16 Wave 3 实施后检查（Codex / glm组）】

背景：Wave 3 = 出口抽象层深化最后一块（多卡支持 + validate_outlet_fields CI + 可感知计算器 2a·B 评论↔POI 再后置）。两组预检反评价全采纳（glm 计算器分步 2a/2b·多卡同 domain 去重·Codex 表达式共享）。请核验落地效果（实施后检查·通过后 claude组 推）。

第一步 · 同步
- git pull（fix/emc-buglog·最新 1daffc6）

第二步 · 核验已实施内容（对照两组预检落地）

① `ai_qa/outlet_kb/build_outlet_schema.py`（多卡 + 计算器）
- `resolve_outlet_ids`：多契约命中 → 按 score 降序 oid 列表·**同 domain 只取最高分**（防冗余）·保留 resolve_outlet_id 兼容
- `build_outlet_schema` 返 cards 列表·`build_outlet_schema_single` 返首卡兼容旧调用
- `compute_perceptible_metrics`（2a 极性类）：emc_field 含 polarity_index 的可感知指标取 polarity_index + 关键词命中标注（（关键词）提示→topic_top/issue_label 匹配）+ 缺失诚实'暂无数据'·B 类条件等式 6 项后置 2b

② `api/aiqa_routes.py` /outlet_card
- 返 {cards, card: cards[0]}（cards 多卡·card 兼容旧前端·未命中 []/None）

③ 前端（多卡渲染）
- `harness.js` _maybeBuildOutletCard：cards 优先（数组）·回退 card（兼容 Wave 0-2 旧端点）
- `panel.js` onOutletCard：数组循环 renderOutletCard（单卡兼容）

④ `tests/validate_outlet_fields.py`（CI·Codex 采纳）
- 正则提取 CONTRACTS field_mapping + METRIC_MAPPINGS emc_field 消费字段（ASCII 标识符·剥 [scale=]·跳过中文说明段）→ 对比产物白名单 → 死字段 fail / 缺消费 warn（手动跑打印）

⑤ 测试
- test_outlet_schema +4（多卡跨 domain / 多卡兼容单卡 / 计算器极性类 + 缺失）

请检查：
1. resolve_outlet_ids 多契约正确（跨 domain 多卡·同 domain 最高分·score 排序）？保留 resolve_outlet_id 兼容？
2. 多卡兼容（build_outlet_schema_single 返首卡·/outlet_card {cards, card}·前端 cards 优先/单卡回退）？破坏既有单卡用例？
3. compute_perceptible_metrics 2a 正确（极性类·关键词命中标注·缺失诚实）？B 类条件等式后置 2b 边界清晰？
4. validate_outlet_fields CI 正确（正则提取·死字段 fail/缺消费 warn）？有无误报（中文说明段被当字段）？
5. 测试覆盖够（多卡/计算器/validate）？有无遗漏边界（同 domain 多契约·关键词未命中·空 result）？
6. 承重零触碰（diagnose/harness/ChatRequest 不动·确定性组装不变）？
7. **已知**：test_validate_skill_params density.when drift 为既有（HEAD 版同 fail·非本次引入·backlog 7 工具）——请确认不阻塞。

第三步 · 端到端验证（可复现）
```
py -m pytest tests/test_outlet_schema.py tests/test_outlet_kb.py tests/validate_outlet_fields.py tests/validate_outlet_trigger_sync.py -q
# 端点多卡
POST /api/v1/aiqa/outlet_card {question:"大南门片区更新需求与体检满意度", diagnose:{domain_lens:[urban_renewal,urban_governance],scale:meso}, result:{rows:[...]}}
# 应 {cards: 2, card: renewal_demand}
```
另：浏览器问"大南门片区更新需求 + 体检满意度"→ 应出 2 张出口卡

请核验：
1. 单测是否全过？
2. 端点多卡（2 卡·card[0] 兼容）是否复现？
3. 有无边界遗漏（同 domain 多契约·关键词未命中·空 result·多卡前端渲染）？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave3完成-*_2026-08-04.md
- 判定：Wave 3 是否通过·有无需修项（P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 3 实施后检查（先验后推）。*
