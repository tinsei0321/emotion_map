# CB-16 续·Wave 0 剩余实施检查（Codex / glm组 · 2026-08-03）

> **用途**：claude组 已完成 Wave 0 核心（build_outlet_schema 确定性组装器·commit 9a98785·已 push·测试 13 passed·端到端 S2 卡组装验证成功）。本轮继续 Wave 0 剩余两项：① 条件触发词表（emc-patterns.js 镜像·用户定案方案 A）② 前端 markdown 卡片渲染（仿 .cpd-guide-card·用既有 design token·不新造样式）。请两组核验已完成的 Wave 0 核心 + 预告将实施的剩余项。
> **登记**：docs/context-map.md · cb-journal CB-16。

---

【CB-16 续·Wave 0 剩余实施检查（Codex / glm组）】

背景：claude组 已完成 Wave 0 核心（build_outlet_schema 确定性组装器·commit 9a98785·
已 push·测试 13 passed·端到端 S2 卡组装验证成功）。本轮继续 Wave 0 剩余两项：
① 条件触发词表（emc-patterns.js 镜像·用户定案方案 A）② 前端 markdown 卡片渲染
（仿 .cpd-guide-card·用既有 design token·不新造样式）。

第一步 · 同步
- git pull（fix/emc-buglog·最新 9a98785）

第二步 · 核验已完成的 Wave 0 核心
- ai_qa/outlet_kb/build_outlet_schema.py：resolve_outlet_id（outlet+domain+scale+接口词→契约）+
  build_outlet_schema（7 要素确定性组装·不调 LLM·字段缺失降级·尺度分派·诚实标注）
- tests/test_outlet_schema.py（13 passed·含 domain 修复/尺度分派/字段降级/端到端 S2 卡）
- 端到端：问"西陵区老旧小区更新需求分析"→ 需求分析卡（接口标识/问题类型/需求强度/
  需求位置/需求类型/数据基础/对接任务/局限）

请检查：
1. build_outlet_schema 的确定性组装是否符合 CB-16 共识（不新增 LLM 阶段/不编造/诚实标注）？
2. resolve_outlet_id 路由是否合理（domain+scale 必要条件 + 接口词/outlet 提示触发）？
3. 测试覆盖是否够（触发/尺度/降级/端到端）？有无遗漏边界？

第三步 · 本轮实施预告（供两组实施后检查）
本轮将实施：
- ① 条件触发词表：接口词（更新/体检/需求/满意度/排序/识别/时序/改造）镜像到 emc-patterns.js
  （与现有用地/区域词表一致·用户定案方案 A）——请确认"触发词表放 EMC 侧"符合出口驱动开发逻辑？
- ② 前端 markdown 卡片渲染：仿 .cpd-guide-card·用 design/tokens.json 既有 token
  （保持设计语言和交互逻辑统一·不新造样式）——请确认"用既有 token 不新造"符合设计语言铁律？

第四步 · 产出
- 简短 SCAN（核验 Wave 0 核心 + 本轮实施预告检查）落 docs/catch-ball/scan/CB16-Wave0剩余-*_2026-08-03.md
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-03）·Wave 0 剩余实施检查（触发词表 + 前端渲染）。*
