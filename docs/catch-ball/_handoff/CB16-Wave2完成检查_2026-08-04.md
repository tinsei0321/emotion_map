# CB-16 Wave 2 / CB-15 数据认知 P0 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组预检反评价实施 Wave 2 / CB-15 P0（commit 623e293·分支 `fix/emc-buglog`·**未推·先验后推**）。请两组**核验落地效果**（实施后检查·通过后 claude组 推）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③p。

---

【CB-16 Wave 2 / CB-15 数据认知 P0 实施后检查（Codex / glm组）】

背景：CB-15 数据认知 P0 = 下钻链最小闭环（place_name 双源融合 + poi_names + /grid/pois + 3220 接入 + 去重）。两组预检 P0 实锤 place_layer 未读 3220（1270≠3220·FC 字段错配）+ P1 语义分层（polygon 保留边界名·grid POI 优先·最近质心 POI）+ place_name_source 可追溯。请核验落地效果（实施后检查·通过后 claude组 推）。

第一步 · 同步
- git pull（fix/emc-buglog·最新 623e293）

第二步 · 核验已实施内容（对照两组预检 P0/P1/P2 落地）

① `place_layer.py`（core/）
- `_read_pois_geojson`（GeoJSON FC 适配层：geometry→lng/lat·category→baidu_level1·keyword→baidu_level2·district→area）
- `_load` 合并 3220（yichang_pois = _read_pois_geojson(_YICHANG_POIS_GEOJSON)·all_pois = amap + landmark + yichang）
- `_dedup_pois`（name 归一化 + coord 容差 <30m·保先序 1270 优先）
- 实测 all_pois=4310（1270+7+3220·去重 187）

② `spatial_analysis.py`（core/）
- `_attach_poi_attrs`（grid/polygon 双模式）：polygon 保留边界名·POI 作 top_places（poi_names/poi_count）·grid 覆盖 place_name=最近质心 POI + place_name_source=poi_sjoin
- sjoin 列名冲突修复（poi/poly 双 name → poly_ 前缀·防 name_left/right 冲突取不到）
- `_attach_4x5_attrs` 加 place_name_source 兜底（spatial_hotspot/area_seed/empty·可追溯）

③ `api/geo_routes.py`
- `/geo/grid_pois` 端点（cell_id 或质心坐标 + cell_size 双收·确定性·格内 POI 清单 名称/类别/domain/element/坐标）
- 实测 centroid 200 count=32（CBD）·cell_id 路径 200 同 count（确定性一致）

④ 测试
- `tests/test_spatial_analysis.py` 5 新增（3220 接入 / grid_pois centroid / grid_pois cell_id / grid_pois 缺参 400 / polygon poi 属性）
- `tests/test_geocode.py` limit 200→1000（3220 扩容 1277→4310·200 上限触顶·保 relaxed>default 区分）

请检查：
1. `_read_pois_geojson` 适配层正确（geometry→lng/lat·category→baidu_level1·缺失字段空）？3220 字段映射有无遗漏（domain/element 空·limitations 标注？）
2. `_dedup_pois` 去重逻辑正确（name 归一化 + coord 容差·保先序 1270 优先·连锁店名同址异不误删）？
3. `_attach_poi_attrs` 双模式正确（polygon 保留边界名·grid POI 优先·最近质心）？sjoin 列名冲突修复是否完整？
4. `place_name_source` 兜底链正确（poi_sjoin / poi_top_places / spatial_hotspot / area_seed / empty）？
5. `/geo/grid_pois` 端点契约合理（cell_id 确定性·质心兜底·返回结构·与 zonal rows 兼容）？LLM 问答如何用（P1 lookup_place 之前·前端点击先用）？
6. 测试覆盖够（3220 接入 / 端点双路径 / polygon 属性 / 缺参）？有无遗漏边界（空 POI 格 / grid place_name 覆盖 / 去重极端）？
7. 承重零触碰（place_layer/geocode 只增不改·聚合层改动回归）？

第三步 · 端到端验证（可复现）
```
py -m pytest tests/test_spatial_analysis.py tests/test_geocode.py -q   # 应 16+37 passed
# 3220 接入验证
python -c "from core.place_layer import get_place_layer; pl=get_place_layer(); print(len(pl.all_pois))"  # 应 4310
# grid_pois 端点
POST /api/v1/geo/grid_pois {cell_lng:111.286, cell_lat:30.708, cell_size:200}  # 应 200 count=32 CBD
```
另：浏览器问"大南门·二马路片区有哪些地点"→ 聚合产物 poi_names 有地点清单

请核验：
1. 单测是否全过？
2. 3220 接入 count=4310 + grid_pois 端点 200 是否复现？
3. 有无边界遗漏（空 POI 格 / grid place_name 覆盖 / 去重极端 / 聚合回归）？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave2完成-*_2026-08-04.md
- 判定：Wave 2 P0 是否通过·有无需修项（P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 2 / CB-15 数据认知 P0 实施后检查（先验后推）。*
