# CB-16 Wave 3 余留 2b + P2 实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按两组补充预检反评价（③z2·Codex P1×3 + glm 注意项×2 全采纳）实施 2b + P2（分支 `fix/emc-buglog`·**未推·先验后推**）。请两组**核验落地效果**（通过后 claude组 推）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③z3。

---

【CB-16 Wave 3 余留 2b + P2 实施后检查（Codex / glm组）】

背景：Wave 3 余留两项（可感知计算器 2b B 类条件等式 + checkup_satisfaction P2 prose→真实字段）已按补充预检反评价实施。请核验落地效果。

第一步 · 读本地文件（同一工作区·**无需 git pull/push**·直接读）
- 探索依据：
  - `ai_qa/outlet_kb/build_outlet_schema.py`（`_parse_emc_expr` 新增 + `compute_perceptible_metrics` 2a/2b 分支 + `_kw_hit` 共用）
  - `ai_qa/outlet_kb/urban_checkup_outlets.py`（checkup_satisfaction field_mapping prose→真实字段）
  - `frontend/js/ai_qa/panel.js` renderOutletCard（perceptible_metrics 渲染新增）
  - `tests/test_outlet_schema.py`（+7 测试）

第二步 · 核验已实施内容（对照补充预检反评价落地）

① **2b 可感知计算器 B 类条件等式**（Codex/glm 反评价采纳）
- `_parse_emc_expr`：拆 `+` + 条件/值识别·**多值条件 `/` 拆列表**（`element_top=设施/环境` → ['设施','环境']·或语义·glm 注意①）·关键词 `/` 拆多词·含 polarity → {'polarity': True}（走 2a）
- `compute_perceptible_metrics` 分 2a/2b：
  - 2a 极性类（含 polarity_index·**生态宜居明示留 2a·条件为提示不参与判定**·Codex P1①）
  - 2b 条件等式（**仅可感知 industry 进**·生态宜居可量化不参与 2b）：element_top 缺失→暂无数据·**条件不匹配→跳过**·匹配→取值·**关键词未命中→跳过**（防跨类误标·Codex P1②）·source 对齐 2a `{value_field}（确定性）·条件：element_top=X·命中：kw`
  - 生态宜居（element_top=环境 + polarity_index·可量化）→ 走 2a（出极性值·条件不判定）
- `_kw_hit` 抽共用（2a 关键词标注）

② **checkup_satisfaction P2**（prose→真实字段）
- `'满意度（4 尺度）': 'polarity_index'`（极性=满意度代理·非问卷率）
- `'8 领域情绪值': 'element_top/domain_top + polarity_index'`（**element_top 优先**·Codex P1③·`/` 取首）
- `'不满意项定位': 'issue_label + place_name'`（不动）

③ **panel.js 渲染 perceptible_metrics**（Codex P2 并入）
- renderOutletCard 新增 metricsBlock（可感知体检指标小节·暂无数据灰·source title）

④ **测试**（test_outlet_schema.py +7）
- 2b 命中/不匹配/缺失/多值条件/关键词未命中→跳过/生态宜居 2a·P2 满意度字段非空

请检查：
1. `_parse_emc_expr` 解析正确（多值 `/` 拆列表·含 polarity → 2a）？`compute_perceptible_metrics` 2a/2b 分支逻辑对路？生态宜居明示留 2a 合理？
2. 2b「条件不匹配→跳过」+「关键词未命中→跳过」语义正确（不占卡面·防跨类误标）？会不会漏掉真实适用的指标？
3. P2 契约字段（polarity_index / element_top/domain_top / issue_label+place_name）语义对路？`element_top` 优先 + `/` 取首是否符合「8 领域情绪值」语义？
4. panel.js 渲染 perceptible_metrics 不破坏既有卡片布局？
5. 测试覆盖够（B 类 7 项·多值·关键词未命中·生态宜居·P2）？有无遗漏边界？
6. 承重零触碰（diagnose/harness/ChatRequest 不动·2a 逻辑不回归·确定性组装不变）？
7. **已知（非本次引入）**：renewal_demand 等卡也带 perceptible_metrics（`_build_card:230` 无条件调用·Codex P2 建议 domain 门控·**暂缓 backlog 未做**）——确认不阻塞。

第三步 · 端到端验证（可复现·服务已启 :8000）
```
py -m pytest tests/test_outlet_schema.py tests/validate_outlet_fields.py -q   # 29 passed
py -m pytest tests/ -q                                                        # 274 passed（+7）
# 端点多卡（含 B 类）
POST /api/v1/aiqa/outlet_card
  {question:"大南门片区更新需求与体检满意度", diagnose:{scale:meso, domain_lens:[urban_renewal,urban_governance], outlet:"报告结论"},
   result:{rows:[{place_name:"大南门",issue_label:"停车难",polarity_index:-0.32,domain_top:"urban_renewal",element_top:"设施",topic_top:"停车难",point_count:900}]}}
  → cards: [renewal_demand, checkup_satisfaction]·checkup_satisfaction fields 出真实值（满意度←polarity_index·8领域←element_top·不满意定位←issue_label+place_name）
# 2b B 类（element_top=环境）
POST ... {question:"城市体检满意度调查", diagnose:{scale:meso, domain_lens:[urban_governance], outlet:"报告结论"},
   result:{rows:[{place_name:"滨江公园",issue_label:"绿地不足",polarity_index:0.15,domain_top:"urban_governance",element_top:"环境",topic_top:"公园散步",point_count:500}]}}
  → 公园绿地步行可达性感知 = 公园散步·source "topic_top（确定性）·条件：element_top=环境·命中：公园"
```

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave3完成2b-P2-*_2026-08-04.md
- 判定：2b + P2 是否通过·有无需修项（P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 3 余留 2b + P2 实施后检查（先验后推）。*
