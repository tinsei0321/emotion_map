# CB-16 发版 backlog 收尾 实施预检（Codex / glm组 · 2026-08-04）

> **用途**：③w5 措辞修复 + 发版遗留已闭环。③w5b 遗留 5 项发版 backlog 打包收尾·落地前请两组预检。
> **登记**：docs/context-map.md · cb-journal CB-16 ③w6。

---

【CB-16 发版 backlog 收尾 实施预检（Codex / glm组）】

背景：③w5（措辞 + eval 标尺 + RST-L06 + buffer 门控）已闭环。③w5b 遗留发版 backlog 5 项打包收尾。用户定「发版 backlog 收尾」。请预检下方草案。

第一步 · 读本地文件（同一工作区·**无需 git pull/push**·直接读）
- 探索依据：`frontend/js/ai_qa/harness.js`（composeGapCard footer :231-232·FC boundary 白名单校验点）· `frontend/js/ai_qa/tools.js`（deriveAvailable 白名单 :622·FIXED_ADMIN_DISTRICTS :8）· `DATA/boundaries/presets/行政区.geojson`（9 features 含法定功能区·PRM-07 fixture）· `tests/eval_template_flash.py`（注释 :88/89 + 单次采样）· `frontend/js/test-cases.js`（RST-L06 用例）

第二步 · 实施草案（5 小项·请预检）

**1. footer「未生成图层」措辞条件化**（Codex P2·③w5b backlog）
- `composeGapCard` footer（:231-232「在没有可靠数据或未生成图层前…」）恒含「图层」字眼——零工具尝试分支也出现·与「非图层叙事」不完全一致
- 草案：footer 按 failedObs 条件化——failedObs=0（零工具尝试）→ 改「在没有可靠数据或未完成分析前…」·failedObs>0 → 保留「未生成图层」

**2. FC 直供 boundary 白名单校验**（Codex P2·backlog）
- 08-04 PRM-07 `GeoJSON{1}#小溪塔` = FC 直供单要素·白名单在 derive 层（tools.js）·FC 直供绕过
- 草案：`deriveMissingParams` boundary 校验——FC 给的 boundary 若 source=preset + 行政区层·要素名不在 FIXED_ADMIN_DISTRICTS → 拒绝（清 boundary·走 request_upload 措辞）

**3. PRM-07 preset fixture 清理**（Codex/glm P2·backlog·需用户确认数据改动）
- preset 行政区 geojson 9 features 含法定功能区（小溪塔/龙泉/生物产业园）·模型硬识别有数据来源
- 草案：preset 行政区 geojson 清理为只含真实行政区划（西陵/伍家岗/猇亭/点军·FIXED_ADMIN_DISTRICTS）·**改数据文件需用户确认**（红线）

**4. RST-L06 复跑验证**（Codex P2·根因实证）
- ③w3b 定根因 = clip range derive（缺行政区层）·hardening 已做（preset fallback）·但 08-04 FAIL 是否由此修复未实证
- 草案：复跑 RST-L06（浏览器 e2e 或 B3 子集）验证 hardening 生效

**5. eval 注释精确化 + 发版前复采**（Codex P2）
- eval :88/89 注释「select_template 不返回 multi」不严谨（复合问「…并排序」仍返 multi）
- 草案：注释精确化 + 发版前复采 eval（记录日期/模型·防单次采样方差）

请预检：
1. **footer 条件化**：failedObs=0 改「未完成分析」对吗？会不会与 request_upload 分支冲突？
2. **FC boundary 白名单校验**：deriveMissingParams 拒绝 FC 直供法定功能区·清 boundary 走 request_upload——逻辑对？误伤 FC 正常 boundary（用户上传层不受限）？
3. **preset fixture 清理**：改数据文件（preset 行政区 geojson）算数据红线吗？清理 9→4 要素影响面？
4. **RST-L06 复跑**：浏览器 e2e 单例还是 B3 子集？验证 hardening 的充分方法？
5. **eval 注释 + 复采**：注释精确化表述 + 复采时机（发版前）？
6. **范围边界**：5 项都必要？优先级？（footer 措辞 + FC boundary 白名单是代码·fixture 需用户确认·RST-L06 复跑是验证·eval 复采是记录）
7. **承重零触碰**：不碰 diagnose prompt / 三态出口结构 / ChatRequest？

第三步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Backlog收尾预检-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）·独立判断·不要互相参考

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·CB-16 发版 backlog 收尾 实施预检。*
