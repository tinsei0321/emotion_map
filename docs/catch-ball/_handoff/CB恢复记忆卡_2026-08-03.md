# CB 恢复记忆卡（Codex / glm组 · 2026-08-03）

> **用途**：Codex / glm组 换环境（公司）后记忆丢失——本卡恢复你们的**身份、定位、CB 机制、trace 工具**等新加入内容。
> **读取顺序**：本卡 → RULES → KNOWLEDGE → trace-log-guide → 最新交接卡。

---

## 一、你们的身份与定位

### Codex（GPT-5 · 第三方评估组）
- **定位**：第三方独立评估·非开发主——SCAN/验证/审查/定案·与 claude组 开发互补
- **风格**：代码级证据（file:line）·机制推演·trace 独立核验
- **职责**：B3 飞轮评估 · 修复验证 · 根因定案 · PRM/while-loop/多步问攻坚评审

### glm组（ZCode + GLM 5.2 · 第三方评估组）
- **定位**：第三方独立评估·非开发主——评估/SCAN/讨论/反评价审核·与 Codex 互补视角
- **风格**：trace.log 取证派（5M 行直读）·Node.js 复现·逐 case 分析
- **职责**：同上 + **trace 取证功臣**（B3 根因定案靠 glm 读 trace.log 推翻误判）

### 双阵营结构
```
claude组（开发主）←→ Codex + glm组（评估·独立产出 SCAN/验证/审查）
CB 闭环：SCAN → 反评价 → 行动 → 验证 → 下一轮
```

---

## 二、CB 机制（新加入内容）

### 1. trace 取证纪律（CB-12 新增·RULES 1.2 步骤 0）
- **根因分析第一动作 = 拉 trace.log 计数**：`py tools/trace_query.py --stats`
- **数 F_002（agentStep·while-loop 铁证）/ F_003（finalStep）/ F_005（FC）·勿用 F_001**（LLM 公共出口·不区分 agentStep/finalStep）
- **跑测试带 session**：`EMOTION_TRACE_SESSION=<组>-<批号> py tests/...`
- **报告附 trace 证据**：`trace_query --stats --session <批号>`
- **推断只作假设·trace 数据先行**（B3 教训：claude/Codex 凭推断错两次·glm 读 trace 定案）

### 2. trace.log 工具（CB-12 新增）
- 路径：`.trace/trace.log`（本地·gitignore·轮转 200MB→.1/.2/.3）
- 查询：`py tools/trace_query.py --stats/--id/--time/--level/--session/--case/--tail`
- 文档：`docs/trace-log-guide.md`（ID 速查：F_002 while-loop / F_003 final / F_005 FC）

### 3. CB 流程（既有·更新）
- RULES（评估规则）/ KNOWLEDGE（记忆库·跨轮蒸馏）/ cb-journal（按轮轨迹）
- 报告落盘：`docs/catch-ball/scan/`（Codex/glm）+ `discuss/`（讨论）
- 反评价：claude组 对 SCAN 逐条 agree/disagree/partial + 行动

---

## 三、当前 CB-12 状态（截至 08-03 凌晨）

### 成果
- **B3 88% 历史最佳**（4%→88%·22 倍）·PRM 9/10·p95 46s·9.8min
- **while-loop 根治**（recover 扩展·glm gate 连锁被 localStorage 铁证推翻·Codex recover 缺口正确）
- **pro 停用**（flash 足够·三层守卫）·**trace 工具**（session/轮转/trace_query）

### 待验证（上午 B3 全量）
- PRM-08 测量修复（3abb503）+ slice 200（4ea8b6d）→ 应 PRM 10/10
- RST-L06 多步问（4ea8b6d+0407f78+6d2e609 链修复）→ 分母 25→26·pass 预期 85-88%
- 多步问单例不稳定（FC 方差·已加 recover 链前置治·6d2e609）——若 B3 仍 fail·按调试方向取证（`[seq-chain]` 日志）

### 演进全景
```
B3: 4% → 80% → 88%（搜索旁路/while-loop/pro 根治·trace 取证）
PRM: 0/10 → 9/10（derive/路由/recover/测量修复）
```

---

## 四、恢复后建议动作

1. 重读 `docs/catch-ball/RULES.md` + `KNOWLEDGE.md`（CB 规范/记忆）
2. 读 `docs/trace-log-guide.md`（trace 工具用法·新）
3. 读 `memories/repo/session-handoff.md`（上午详细计划 + 待续）
4. 待 claude组 发 B3 重测结果（session B3-verify-05 + trace 报告）→ 评估

---

*本卡由 claude组 编写（2026-08-03）·供 Codex/glm组 换环境恢复记忆。登记：docs/context-map.md。*
