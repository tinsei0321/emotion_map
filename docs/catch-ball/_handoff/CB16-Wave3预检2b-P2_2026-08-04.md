# CB-16 Wave 3 补充预检（Codex / glm组 · 2026-08-04）

> **用途**：Wave 3 已实施（commit 1daffc6·实施后检查已发 0862f09·待两组）。本请求是 **Wave 3 余留的两个待续项**落地前预检——① 可感知计算器 **2b**（B 类条件等式 6 项）② checkup_satisfaction 契约 **P2**（field_mapping prose→真实字段）。落地前走 CB 流程（用户定：先讨论再实施）。
> **登记**：docs/context-map.md · cb-journal CB-16 ③z。

---

【CB-16 Wave 3 补充预检（Codex / glm组）】

背景：Wave 3 实施已发检查（`0862f09`），其中两组预检一致定的「可感知计算器 2b（B 类条件等式·P2）」与 Wave 2 余留「checkup_satisfaction P2（field_mapping prose→真实字段）」为余留待续项。claude组 本次要落地这两项，先请两组预检下方实施草案。

第一步 · 同步
- git pull（fix/emc-buglog·最新 1500e5b）
- 读探索依据：
  - `ai_qa/outlet_kb/build_outlet_schema.py:235-272`（compute_perceptible_metrics·现只算 A/C 极性类·`if 'polarity_index' not in expr: continue`）
  - `ai_qa/outlet_kb/urban_checkup_outlets.py:24-28`（CONTRACTS['checkup_satisfaction']['field_mapping']·现为 prose）· `:58-117`（METRIC_MAPPINGS·B 类条件等式 6 项）
  - `ai_qa/outlet_kb/build_outlet_schema.py:126-152`（_extract_emc_value·统一取值入口）· `:191-207`（_build_card `+` 拆分 + qualifier 剥除·表达式解析参照）
  - `tests/validate_outlet_fields.py:29`（_extract_contract_fields·正则收 ASCII 消费字段·P2 改后须仍能提取）

第二步 · 实施草案（2 子项·请预检）

**1. 可感知计算器 2b（B 类条件等式 6 项）**
- 扩展 `compute_perceptible_metrics`：现 2a 只处理含 `polarity_index` 的表达式（A 类 2 + C 类 2）。2b 补 B 类条件等式——`emc_field` 形如 `element_top=环境 + topic_top（公园/绿地/散步）`（条件 + 值表达式）且 `industry` 含「可感知」的 ~6-7 项。
- 新增表达式解析 `_parse_emc_expr(expr) -> dict`（确定性·纯函数·不调 LLM）：按 `+` 拆分 → 识别条件部分（`element_top=X/Y`）与值部分（`field（kw1/kw2）`）→ 输出 `{condition_field, condition_values, value_field, keywords}`。
- 2b 逻辑：取 result 的 `element_top`（经 `_extract_emc_value`）→ 缺失 → `{'value':'暂无数据','source':'缺失·不编造'}`；不匹配条件值 → **跳过**（该指标对当前要素不适用·不占卡面）；匹配 → 取 value_field → 缺失 `暂无数据` / 命中则值 + 关键词标注（复用现有（关键词）命中逻辑）。
- 其余表达式（`（客观…）`/`满意度情绪值` 等）保持跳过。2a 逻辑不动。

**2. checkup_satisfaction P2（field_mapping prose→真实字段）**
- `CONTRACTS['checkup_satisfaction']['field_mapping']` 前两项 prose → 真实 EMC 字段：
  - `'满意度（4 尺度）': 'polarity_index'`（评论情绪 → 自动满意率·极性直取）
  - `'8 领域情绪值': 'domain_top/element_top + polarity_index'`（4×5 归因 → 领域情绪）
  - `'不满意项定位'` 已是 `'issue_label + place_name'`·不动
- 均 ∈ `_EMC_FIELDS` 白名单（`tests/validate_outlet_fields.py` 不红）·对齐 Wave 1 已修的 `checkup_dimension` 真实字段模式。

第三步 · 请预检
1. **2b 表达式解析**：`_parse_emc_expr` 拆 `+` + 条件/值识别方案对路？B 类 6 项覆盖全（公园绿地/养老托育/15分钟生活圈/小区环境品质/物业管理/老旧街区改造/商圈活力）？「不匹配条件值→跳过」语义正确？
2. **2b 计算语义**：element_top 缺失→暂无数据·不匹配→跳过·匹配→取值+关键词标注，合理？与 2a 极性类如何统一 source 标注？
3. **P2 契约**：满意度 3 项映射到真实字段（polarity_index / domain_top/element_top+polarity_index）语义对路？会不会让 checkup_satisfaction 卡面字段与 checkup_dimension 信息重复？`domain_top/element_top` 双字段 + 连接符取值 `_extract_emc_value` 支持吗？
4. **CI 兼容**：P2 改后 `tests/validate_outlet_fields.py` 能否仍提取消费字段（prose→真实字段·正则收 ASCII）？有无误报？
5. **范围边界**：这两项是否都是 Wave 3 余留必要项？优先级？（2b 是计算器闭环·P2 是满意度卡防空卡）有没有应一并处理的关联项？
6. **测试方案**：compute_perceptible 2b（命中/不匹配/缺失/双值条件）+ 满意度卡字段非空 + validate_outlet_fields 回归，够？
7. **承重零触碰**：不碰 diagnose/harness/ChatRequest·确定性组装不变·2a 逻辑不动？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-Wave3补充预检-*_2026-08-04.md
- 判定：草案是否可行·有无需改项（优先级 P0/P1/P2）
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·Wave 3 余留 2b + P2 实施预检。*
