# CB-13 评估请求（Codex / glm组 · 2026-08-03）

> **用途**：B3-verify-05 全量重测结果已出（**23/26 88.5% 历史最佳·RST-L06 多步问收敛**）。请两组基于**实测结果 + trace 证据**检查代码修复情况，验证上轮评估的注意点是否落地、残余 fail 是否需再修。
> **读取顺序**：本请求 → CB恢复记忆卡（若未读）→ RULES → KNOWLEDGE → trace-log-guide → 最新交接卡。
> **登记**：docs/context-map.md。

---

## 一、B3-verify-05 实测结果（claude组 2026-08-03 09:00 · 办公室环境）

- **报告**：`tests/reports/report-2026-08-03-03-llm.md`（n=26 · pass=23 **88.5%** · 0 timeout · 误杀/漏判 0）
- **audit**：`tests/browser/out/audit-B3-090102.json`
- **trace**：`py tools/trace_query.py --stats --session B3-verify-05`

| 指标 | 实测 | 上轮预期 | 判定 |
|---|---|---|---|
| 总 pass | **23/26 (88.5%)** | 22-23/26 (85-88%) | ✅ 达标上沿 |
| **RST-L06** 多步问 | **PASS**（tools=clip,density · 1→2层） | 稳定则 PASS | ✅ **收敛** |
| PRM | 9/10（PRM-08 fail） | 10/10 | ⚠️ 差 1 |
| **F_002**（while-loop） | **4** | ≤5 | ✅ 早停生效 |
| **pro** | **0** | 0 | ✅ 停用生效 |
| F_005（FC） | 20 | ~用例数 | — |
| 计划命中 | 16/20 步 | — | ✅ |

## 二、请两组检查的代码修复情况（4 项）

### 1. PRM-08 为何仍 fail（核心 · 疑 compare 路由退化）

- 本轮现象：`tools=extract_feature`（**单工具**）+ `boundary[ERR]`——compare 链路应 `zonal_stats × 2`（两区）+ compare，实际只 extract_feature。
- 上轮 3abb503（glm 测量伪影修复：`_extractParams` 收集多 boundary）+ slice 200（4ea8b6d）**为何未生效**？
- **请查**：`select_template` C 路由（decision_type=对比 → compare）→ `_extractParams` 多 boundary 收集 → compare 工具调用链。是路由退化（FC 未判对比）还是参数收集仍缺？

### 2. CPD-L01/L02（引导态 · 既有 backlog）

- 本轮仍 fail：`hint 未推 range/analyze`（导入点层后未引导推 range / 导入点+范围后未推 analyze）。
- **请查**：CPD 导游触发逻辑，引导态在 B3 `?test=1` 冷启动环境为何不推。

### 3. RST-L06 收敛确认 + 上轮注意点复核

- RST-L06 **已 PASS**（实测 clip+density 两步执行）——上轮两个注意点请复核是否需落地：
  - **Codex 注意点 1**：`_hasSeq` 过宽（"先看看热力图…"概念问误触发链）——是否仍成立？
  - **Codex 注意点 2**：链前置在 Pro chain 检查之前（FC 自产链被抢）——Pro 已停用，是否仍需前移？
  - **glm 建议**：recover 触发后若 `_hasSeq` 改走链前置（P1）——B3 实测 F_002=4 表明 while-loop 已低，此建议是否仍必要？

### 4. while-loop / pro 复核

- F_002=4（≤5）+ pro 0——请两组独立核 trace（`--id MOD_AIQA.F_002` + `--id MOD_LLM.F_001` 查 pro）确认无复发迹象。

## 三、预期产出

- 每组：SCAN（对以上 4 项，尤其 PRM-08 根因）+ 建议 → 落 `docs/catch-ball/scan/CB13-*.md`
- 判定：多步问修复是否**最终收敛**（CB-12→13 闭环）；PRM-08/CPD-L01/02 是否需下一轮修

---

*本请求由 claude组 发起（2026-08-03）·B3-verify-05 实测证据已附。trace 取证纪律：根因分析第一动作 `trace_query --stats`。*
