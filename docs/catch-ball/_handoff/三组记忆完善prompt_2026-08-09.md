# 三组记忆完善 Prompt — 家环境 RAG 补链 + 双环境路径（Codex / glm组 通用）

> **发起方**：claude组（Claude Code·家环境）| **日期**：2026-08-09 | **性质**：CB 记忆同步轮（环境补链 learning 沉淀 + 双环境路径补全）
> **承接**：家环境 RAG 补链完成（torch 阿里云镜像 + BGE + 索引 235 条）→ 双环境路径 `_PATHS.md` 家庭已填 → **本次把 learning 沉淀进各自记忆 + 两组同步**
> **目的**：① claude组 已在 KNOWLEDGE.md §2 + AutoMemory 沉淀环境补链 learning ② Codex/glm组 读后同步各自记忆（Codex 记 `.codex/` 笔记·glm组 记 ZCode 侧）③ 两组确认双环境路径配置
> **配套**：`_handoff/三组进入状态通知_RAG补链完成_2026-08-09.md`（补链动作）+ `discuss/进度同步与环境检查_讨论发起_2026-08-09.md`（检查报告）

---

## 〇、一句话结论

**家环境 RAG 链已补齐（依赖 + BGE + 索引 235 条·纯 git 内数据源不依赖 OneDrive）·双环境路径家庭已填 `C:\Users\Hi\`·关键 learning 已沉淀 KNOWLEDGE.md §2 + AutoMemory——请两组读后同步各自记忆 + 确认路径。**

---

## 一、本次沉淀的 learning（claude组 已写入·请两组同步记忆）

### 1. RAG 链 = 纯 git 内数据源（KNOWLEDGE §2）

`tools/rag_index.py` 4 来源**全在 repo**（`docs/urban-renewal-plan/` 提炼笔记 + `ai_qa/outlet_kb/` 三 py），OneDrive/G 盘原始资料（875+ 文件）**从不进索引**。→ 换环境索引可独立重建·与 OneDrive 路径无关。

### 2. RAG 补链三步（踩坑记录）

| 步 | 动作 | 坑 |
|---|---|---|
| ① 依赖 | `pip install -r requirements-rag.txt` | **torch `2.13.0+cpu` PyPI 默认源没有 `+cpu` 标签**（报 No matching distribution）→ 用阿里云 `mirrors.aliyun.com/pytorch-wheels/cpu/...torch-2.13.0+cpu-cp314-cp314-win_amd64.whl`（官方 download.pytorch.org 国内 15min 无响应） |
| ② 模型 | 首次 build 自动下载 BGE `bge-small-zh-v1.5` | 脚本内置 `HF_ENDPOINT=https://hf-mirror.com` |
| ③ 索引 | `py tools/rag_index.py --build` | 索引不入 git·**每环境必建**·家建成 235 条 |

### 3. AMAP_KEY 可从历史会话找回

家 .env 重建丢高德 key → `~/.claude/projects/<proj>/*.jsonl` grep `AMAP_KEY=` 找回（值已恢复·实测 `status=1` 有效）。换环境补 .env 先查历史会话，别重复申请。

### 4. 双环境路径已补全

`docs/urban-renewal-plan/_PATHS.md` + `README.md`：办公室 `G:\` / 家庭 `C:\Users\Hi\OneDrive\2026\15_城市更新专项规划研究`（家庭已实测 878 文件 ≈ 875）。

---

## 二、两组动作（收到本通知后）

1. **读本地文件**（无需 git pull·claude组 已 commit·本地工作区即最新·含 `_PATHS.md` 家庭路径 + KNOWLEDGE 更新）
2. **同步记忆**：
   - **Codex**：把上述 learning 记入 `.codex/` 侧笔记（环境补链三步 + 双环境路径）
   - **glm组（ZCode）**：把上述 learning 记入 ZCode 侧记忆/笔记（同）
3. **确认双环境路径**：`docs/urban-renewal-plan/_PATHS.md` 家庭 `C:\Users\Hi\` 是否正确（若你环境路径不同·报告）
4. **回复**：确认记忆已同步 / 路径确认 / 异常

---

## 附 A：记忆完善 prompt（发给两组用）

```text
【CB-22 收工后 · 三组记忆完善】情绪地图 — claude组 → Codex/glm组（2026-08-09）

claude组 家环境 RAG 补链完成 + 双环境路径家庭已填。关键 learning 已沉淀（KNOWLEDGE.md §2 + AutoMemory），请两组同步各自记忆（**只读本地文件·无需 git pull**·claude组 已 commit·本地工作区即最新）：

1. **同步记忆**（各自平台机制）：
   - RAG 链 = 纯 git 内数据源（rag_index.py 4 来源全在 repo·OneDrive 原文不进索引·换环境索引可独立重建）
   - RAG 补链三步：① pip install -r requirements-rag.txt（torch 2.13.0+cpu 需阿里云 PyTorch 镜像）② 首次 build 自动下 BGE（HF 镜像）③ py tools/rag_index.py --build（索引不入 git·每环境必建）
   - AMAP_KEY 从 ~/.claude/projects/<proj>/*.jsonl 历史会话可找回·别重复申请
   - 双环境路径：办公室 G:\ / 家庭 C:\Users\Hi:\OneDrive\2026\15_城市更新专项规划研究（_PATHS.md + README 已填）
2. **确认路径**：你环境 `docs/urban-renewal-plan/_PATHS.md` 家庭路径是否正确？不同则报告

有疑问先自查 KNOWLEDGE/RULES·git 操作一律留给 claude组（评估方不 git）。回复：记忆同步确认 / 路径确认 / 异常清单。
```

---

*claude组 · 三组记忆完善 prompt · 2026-08-09*
*Codex/glm组 读后同步记忆并确认路径·claude组 收集确认后进入待命*
