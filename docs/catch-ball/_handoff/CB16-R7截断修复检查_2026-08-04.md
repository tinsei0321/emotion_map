# CB-16 R7 结论截断修复实施后检查（Codex / glm组 · 2026-08-04）

> **用途**：claude组 已按用户实测 + 用户定阈值策略修复 R7 结论截断（commit 0aff59e + 49e4122·分支 `fix/emc-buglog`·待 push）。请两组**核验修复效果**（实施后检查）。
> **登记**：docs/context-map.md · cb-journal CB-16。

---

【CB-16 R7 结论截断修复实施后检查（Codex / glm组）】

背景：用户浏览器实测「大南门·二马路片区更新需求分析」发现结论结尾出现「**4.**\n\n…（结论已截断·详见上方图层）」——问"未完成"。根因 = harness.js applyQualityDefense 的 R7 防线 `>800 字 → slice(0,800)` 字符级硬切（阈值过低 + 切点不感知 markdown + R2 按钮被切连带 + 文案误导）。用户定策略：**保留防线但阈值 800→1500**（多要素结论超 800 正常·实测结论 p95≈1000）+ 结构回切。请核验落地效果。

第一步 · 同步
- git pull（fix/emc-buglog·最新 49e4122）

第二步 · 核验已实施内容（4 环节·`frontend/js/ai_qa/harness.js` applyQualityDefense）

① R7 阈值 800 → 1500
- `if (final.length > 1500)`（:358 附近·注释标明 CB-16 用户定）
- 数据支撑：audit 结论 p95≈1000 → 1500 留 50% 余量·正常多要素结论完整通过·只拦真失控长文

② 切点结构回切（治「**4.**」空标题）
- `const _cut = Math.max(lastIndexOf('。',1500), lastIndexOf('；',1500), lastIndexOf('\n',1500), lastIndexOf('.',1500))`
- `_cut > 750 ? slice(0, _cut+1) : slice(0, 1500)`（下限 750 兜底·防无断句长段死循环）

③ R2 `{{show:}}` 图层按钮移 R7 后（连带修复）
- R2 块从 R1 后移到 R7 截断后（R10/R11 前）·防长结论时按钮被 R7 切掉（图层主出口失效）

④ 文案场景化
- 有图层（realLayers.length > 0）→「结论已截断·详见上方图层与数据」
- 无图层 →「结论较长已精简·详见上方分析」（不误导）

请检查：
1. 阈值 1500 是否合理（会不会仍截掉正常结论 / 或拦不住失控长文）？是否该按结论类型（多要素 vs 单句）动态调？
2. 结构回切的 lastIndexOf 边界选择（。；换行 .）是否足够？有无遗漏断句符（！？等）？下限 750 是否合理？
3. R2 移到 R7 后有无副作用（依赖顺序/realLayers 计算位置/其他 R 规则对 {{show:}} 的依赖）？
4. 文案场景化是否对路（无图层时"已精简"是否准确）？
5. **关键**：截断后 `{{show:}}` 按钮是否真的保留（R2 条件 `!/\{\{show:/.test(final)` 在截断后命中）？有没有边界漏网（如 final 已含 {{show:}} 但被 R7 切掉中间部分导致残句）？

第三步 · 端到端验证（可复现）
- 浏览器（需起 serve·重启后端）问「大南门·二马路片区更新需求分析」→ 结论应完整出（不再出现「**4.**」空标题·分析正常结束）
- 代码级可复现：构造 >1500 字长结论调 applyQualityDefense → 应截断在句末 + {{show:}} 保留

请核验：
1. 用户场景（1100 字左右多要素结论）是否完整通过（不触发 R7）？
2. >1500 字失控长文是否截断在完整句末（不切标题后）？
3. 产图层 + 长结论场景按钮是否保留？

第四步 · 产出简短 SCAN 落 docs/catch-ball/scan/CB16-R7截断修复-*_2026-08-04.md
- 判定：R7 修复是否通过·有无需修项
- 独立判断·不要互相参考对方报告

规则：只读评估·禁改代码/禁 commit·结论先行。

---

*本请求由 claude组 发起（2026-08-04）·R7 结论截断修复实施后检查。*
