# CB-15 三方讨论 · 出口抽象层 × 数据认知重构（2026-08-03）

> **用途**：glm 课题（出口抽象层·立项关键）已并入 CB-15。请两组深入讨论两份报告的协同实施方案，收敛"分析过程中（CB-15）+ 分析完成后（出口抽象层）"的完整链路。
> **读取顺序**：本请求 → 两份报告 → RULES → KNOWLEDGE → trace-log-guide → 最新交接卡。
> **登记**：docs/context-map.md · cb-journal CB-15。

---

## 一、两份报告（必读·前后承接）

1. **[CB-15 数据认知体系重构需求](discuss/EMC-数据认知体系重构需求_2026-08-03.md)**（分析**过程中**·标准产物：网格/热点→POI 联动）
   - 两定性：① 范围=矢量表达不关键·地点认知（拓扑）是关键·范围锚定三来源+诚实 request_upload ② 归因↔地点联动=中微观标准出口
   - 已有：范围三来源部分落地·网格归因·POI 引擎·逆地理编码·L1/L2 点带地点标注
   - 缺失：POI 进问答管线·格↔POI 空间查询·评论↔POI 运行时关联·地点级归因聚合·批量逆地理·拓扑枚举
   - 五点共识 + place_name 致命发现（依赖 sim 期标注·真实数据恒空）+ P0-P3 优先级

2. **[EMC 出口抽象层架构讨论](discuss/EMC-出口抽象层架构讨论_2026-08-03.md)**（分析**完成后**·出口范式：结果→行业接口对接·立项关键）
   - 核心命题：EMC 的"出口" = 分析结果 → 行业接口格式化对接（成果范式 agent·第三段）
   - 三铁律：EMC 找市场接口 / 三段式线性（意图→结果→成果范式）/ 定性+定量+地理按尺度分类
   - 缺口：出向链路=0（DOMAIN_OUTLETS 无实现·outlet 非契约·export 无行业格式）
   - 已完整：行业接口清单（八项任务/体检四维度/8大领域/三级体系）+ 4×5↔指标↔任务映射 + 六大演示场景 + 六讨论焦点

## 二、协同关系（两份报告的承接）

```
CB-15（分析过程·标准产物）           出口抽象层（分析完成·成果范式）
  意图→结果 agent 产出               →  成果范式 agent 对接行业接口
  网格/热点 + 归因↔地点联动             需求分析卡/体检指标表/时序排序/落点清单
  （CB-15 先行·P0 下钻链闭环）        （依赖 CB-15 产出·micro 落点需归因↔地点联动）
```

**实施顺序**：CB-15 先行（P0 格↔POI sjoin + place_name 双源 + 归因↔地点联动）→ 出口抽象层依赖其产出构建（micro 落点清单需填"地理定位"要素）。

## 三、请讨论（两份报告焦点合并收敛）

### A. 出口抽象层六焦点（报告第八节）
1. **成果范式 agent 架构位置**：后端层（outlet_adapter.py）vs prompt 层（build_outlet_prompt）vs 分层（后端 schema + prompt 渲染）？初步倾向方案 C 分层
2. **DOMAIN_OUTLETS 升级**：从"启发词"升级为"接口契约"（补对接标识/产出 schema/适用尺度/所需字段）
3. **尺度耦合强制**：成果范式是否需要额外尺度校验层（防 macro 卡片混 micro 落点）
4. **与 CB-15 协同**：实施顺序（CB-15 先行 → 出口抽象层依赖其产出）
5. **MVP 范围**：六场景全做 vs S2+S6+S3 三场景核心（覆盖 meso/macro·直接对标需求分析+满意度调查）
6. **行业知识库出向扩展**：入向 `industry_kb/` 是否新增出向字段（OUTLET_CONTRACTS）或新建 `core/outlet_kb/`

### B. CB-15 数据认知待收敛点（需求报告第五节）
1. place_name 双源融合兼容性（sjoin POI 优先·fallback sim 标注）
2. cell_id 标识方案
3. lookup_place AI 工具是否 P0（glm 倾向 P1）
4. 3220 vs 1270 POI 去重
5. centralcity 缺失：重生成 vs regeo 兜底

### C. 协同综合（新增·两份报告交叉）
1. **成果范式 agent 与 归因↔地点联动的接口**：出口的"地理定位"要素（微更新点位/落点清单）依赖 CB-15 的归因↔地点联动——两者的数据契约怎么定？
2. **尺度 × 出口 × 数据认知**：macro/meso/micro 尺度下，出口产物（诊断卡/排序表/落点清单）各需 CB-15 的哪类地点信息（面域/单元/网格-POI）？
3. **六场景 × 下钻链**：S2 更新需求分析（核心演示）需要 CB-15 的哪些前置能力才完整？

## 四、产出 SCAN

- 落 `docs/catch-ball/scan/CB15-出口与数据认知-*_2026-08-03.md`
- 给出：出口抽象层方案初稿（架构/契约/尺度/MVP）+ 与 CB-15 的协同实施顺序 + 优先级 + 风险
- 独立判断·不要互相参考对方报告

## 规则

只读评估·禁改代码/禁 commit·专业+通俗解释·结论先行。

---

*本请求由 claude组 发起（2026-08-03）·出口抽象层（glm 课题·立项关键）+ 数据认知重构（CB-15）协同收敛。*
