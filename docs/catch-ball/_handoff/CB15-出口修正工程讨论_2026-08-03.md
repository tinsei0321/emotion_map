# CB-15 出口修正工程讨论 · 结果范式 agent + 出向知识库（2026-08-03）

> **用途**：基于出口抽象层报告（含湖北/上海/广州/南京案例深挖·真实数据）+ 新建的 `ai_qa/outlet_kb/` 出向知识库，讨论**结果范式 agent 的修正工程**——如何把行业接口契约/案例库接入，让分析结果"填进行业已有的表"。
> **读取顺序**：本请求 → 出口抽象层报告（专业版+通俗版）→ `ai_qa/outlet_kb/` → RULES → KNOWLEDGE。
> **登记**：docs/context-map.md · cb-journal CB-15。

---

## 一、背景（已完成的）

1. **出口抽象层报告**已更新：EMC→情绪地图·EMC（体现整个项目价值/定位/出口）+ 案例深化（湖北宜昌望洲岗原拆原建/上海满意度/广州问卷/南京大数据·真实数据不编造）。
2. **出向知识库 `ai_qa/outlet_kb/` 已建**（v1·确定性数据源）：
   - `OUTLET_CONTRACTS`（7 契约）：更新对象识别/需求摸排/时序排序/内容确定/项目生成 + 体检满意度/四维度——每契约含接口标识/尺度/field_mapping（行业表单项→情绪地图字段）/can/cannot（能/不能双栏）/task_link
   - `METRIC_MAPPINGS`（8 指标）：设施缺口/需求强度/需求位置 + 8 领域（生态宜居等）
   - `CASE_LIBRARY`（5 案例）：湖北宜昌/上海/广州/南京/宁夏·真实数据 + emc_map（图/数/表/观点对接）
   - `tests/test_outlet_kb.py`（6 测试·pytest 234 passed）

## 二、讨论焦点（结果范式 agent 修正工程）

### 焦点 1：结果范式 agent 架构（承接出口抽象层焦点1·细化）
- 报告倾向：后端 `core/outlet_adapter.py`（build_outlet_schema 纯函数·确定性组装）+ 前端渲染（不新增 LLM 阶段·撞 D019 红线风险）
- **outlet_kb 怎么接入**？build_outlet_schema 查 `get_outlet_contract(outlet_id)` → field_mapping → 从聚合产物取字段 → 组装出口卡片？
- 条件触发：`outlet` 判"行业接口类"问句才激活第三段？

### 焦点 2：outlet_id 怎么定？
- 诊断卡 `outlet` 7 值枚举（生成图层/...）→ 如何映射到 OUTLET_CONTRACTS 的 outlet_id？
- 需要 `outlet 值 × domain_lens → outlet_id` 解释层？还是扩枚举？

### 焦点 3：案例库的用途
- CASE_LIBRARY 用于：① 出口卡片"参考案例"引用（佐证）② 立项演示场景依据 ③ 还是给 LLM prompt 注入？
- 后端确定性组装（Codex/claude组 共识）下，案例库是给结果范式 agent 引用还是前端展示？

### 焦点 4：与 CB-15 数据认知协同
- 出口卡片的"地理定位"要素依赖 CB-15 的 place_name 双源 + /grid/pois → 实施顺序（CB-15 P0 先行 → outlet_kb 接 CB-15 产物）
- macro 出口（更新对象识别）零 CB-15 依赖 → 可先行演示？

### 焦点 5：领域扩展
- outlet_kb 首版只含 urban_renewal + urban_checkup（立项核心场景）→ 后续扩展 urban_planning/operation/governance？
- 是否加 `ai_qa/industry_kb/` 的领域映射（每个入向领域对应出向契约）？

## 三、产出 SCAN

- 落 `docs/catch-ball/scan/CB15-出口修正工程-*_2026-08-03.md`
- 给出：结果范式 agent 修正工程方案（架构/outlet_id 映射/outlet_kb 接入/CB-15 协同/MVP）+ 优先级 + 风险
- 独立判断·不要互相参考对方报告

## 规则

只读评估·禁改代码/禁 commit·专业+通俗解释·结论先行。

---

*本请求由 claude组 发起（2026-08-03）·出口修正工程 + 出向知识库接入讨论。*
