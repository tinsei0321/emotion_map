# 用户复测滚动清单（跨批累计·阶段收口统一安排）

> **规则（用户令·2026-08-21 深夜）**：人工测试在有阶段性成果时**一并进行**——过程中各执行方只记录要测什么，**每过一个大阶段，统一安排测试内容一起测**。不逐件打断用户。
> 维护：zcode 主手（各执行组回收时上报待测项→并入本清单）。
> **状态注记（用户令 08-22 上午）**：T1-T5 统一测试**暂缓**（用户先跳过）——清单保留待测·测试窗口由用户定。
> 用法：阶段收口（如 PT-CB11 P1 全合入+审计通过）时，zcode 从本清单取「待测」段整理成一次测试指引给用户；测完移入「已测」段。

---

## 待测（按阶段排队）

### 阶段 1 · PT-CB11 P1 全合入后（**已就绪·08-22 凌晨全件合入·上午统一安排**）

| # | 来源 | 测试内容 | 预期 | 状态 |
|---|---|---|---|---|
| T1 | B-3 治本（zcode 后端 B3-1/B3-2 + Kimi 前端 B3-3~B3-6） | 3080 问「12345 诉求最密集的 5 个社区，显示在地图上」 | ①正常填色（非灰框·色带梯度可辨）②左侧标准色带图例 ③悬停显社区名+数值 ④参数面板无误导拾色器（提示换色走重投递） | ✅ 就绪（Kimi playwright 自测过·待用户真测） |
| T2 | Codex 三件工具（F_033-F_035·已合入） | 3080 连问三题：「按 800 米方格统计情绪点分布」「西陵区和伍家岗区哪个情绪更差，差多少」「情绪有没有显著聚集的区域」 | 各自 1-2 次工具调用内出结论；方格结果不冒充社区结论（口径标签在）；对比给差异方向；聚集结论带置信度表述 | ✅ 就绪 |
| T3 | Kimi area_stats（F_037·已合入） | 3080 问「各行政区面积占比」或对某面层问面积结构 | 面积+占比表；不把面积结论说成情绪结论 | ✅ 就绪 |
| T4 | B3-2 错配自纠（观察项·随 T1 顺带） | 若 dsh 用错 value_field，观察其是否按错误提示自纠改对字段再渲染 | 一次拒绝→自动改对（可用字段清单起作用） | ✅ 就绪（随 T1 顺带） |
| T6 | EMC 对话框（轻量引擎） | 8080 `?engine=light` 问 2-3 个知识问题 | 流式输出+追问建议条 | ✅ 就绪 |
| T7 | EMC 对话框（dsh 引擎·BA 首测） | 8080 `?engine=dsh` 问真实分析问题（如「西陵区最差社区」） | 「正在思考…」等待→一次性完整答案+地图联动 | ✅ 就绪 |
| T5 | 样式面板完善（色带编辑器·用户实测触发新增） | ①3080 问「按 800 米方格统计」→点方格图层参数面板→换色带/反向/零值显隐即时生效 ②点左侧图例色带条也能打开同一选择器 ③极性类层默认红绿发散带 | 全部即时生效·图例同步跟随·无重投递 | ✅ 就绪（C3 已合入·主手浏览器验证过） |

### 观察项（不单独安排·随上面顺带看）

- 重启 MCP 后 3080 页面须手动刷新（已知守则·操作提醒而非测试）
- 注入图层与 Toolbox 图层的悬停 tip 一致性（B3-4 效果顺带看）

## 已测（历史归档）

（暂无——首个统一测试窗口=PT-CB11 P1 收口）

---

> 建档：zcode · 2026-08-21 深夜 · 用户令「阶段性成果一并进行人工测试·过程只记录·大阶段统一安排」
