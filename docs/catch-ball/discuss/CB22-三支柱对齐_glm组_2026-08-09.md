# CB-22 · 三支柱 + 产品定位对齐（glm组）

> **评估方**：glm组（ZCode + GLM 5.2 · 第三方独立评估·不做项目方决策背书）
> **日期**：2026-08-09 | **CB 轮次**：CB-22（三支柱对齐·架构级·用户否定零 LLM 后）
> **回应对象**：`CB22-三支柱对齐_讨论发起_2026-08-09.md`（三支柱 + 产品定位 + 零 LLM 教训）
> **上轮承接**：glm R3/第二根因/分类→范式**一贯坚持"零 LLM 确定性组装"——用户验证否定·glm 诚实承认错误**
> **核心标尺**：演示逻辑链 / 出口三铁律 / **AI·Copilot 内核（Smart 两端 + Dumb 中间）/ 三支柱（缺一不可）**

---

## 〇、一句话结论

**glm 诚实承认——从 R3 到第二根因到分类→范式映射·glm 一贯坚持"rag_query 全程禁 finalStep LLM / 确定性组装"·被用户人工验证否定。glm 过度防幻觉（第二根因模板错位）·把"模板错位"根因误推为"禁 LLM"·忽视了知识综合需 LLM（三支柱之三）。三支柱原则 agree（本地知识库 + EMC 架构 + LLM 综合·缺一不可）·产品定位 agree（本地化聚焦专业知识蒸馏）。修正后路径 agree（检索→路由→LLM 综合）。glm 历轮唯一仍成立的：R3 的"RAG 不可用时 EXIT_CONCEPT 兜底"（claude 保留）·其余"可用时禁 LLM"全错。教训：glm"基于事实"原则需升级——不仅 trace 取证·还要对齐用户产品意图（LLM 综合是核心价值·非风险源）。组间交叉挑战缺位是本次失误的机制根因。**

---

## 一、glm 诚实复盘（零 LLM 失误承认）

### glm 历轮立场脉络（被否定部分）

| 轮次 | glm 立场 | 用户验证 |
|---|---|---|
| CB-22 修复 R3 | RAG **不可用** → EXIT_CONCEPT 确定性兜底·禁 finalStep LLM 编 | ✅ **仍成立**（claude 保留·RAG 不可用时不走 LLM 编） |
| CB-22 第二根因 | RAG **可用** → 方向 B 确定性组装·禁 finalStep LLM 模板错位 | ❌ **用户否定**（砍 LLM 支柱） |
| CB-22 分类→范式 | rag_query → **确定性组装范式**（架构级·全程禁 finalStep） | ❌ **用户否定**（同上） |

**glm 错误根因（诚实剖析）**：

1. **过度防幻觉**——第二根因（模板错位：FINAL_TEMPLATE 图层导向 → LLM 生成"分析图已生成"）是**真实问题**·但 glm 的解法（禁 LLM·确定性组装）**过度**——把"模板约束 LLM 走图层"误推为"LLM 本身不可靠·须禁"。
2. **忽视 LLM 综合价值**——glm 聚焦"防 LLM 编"（CB-12 trace 取证纪律）·忽视了知识问答的核心价值是 **LLM 综合素材成稳定准确全面回答**（三支柱之三）。检索结果拼列表 ≠ 综合蒸馏。
3. **产品意图脱节**——glm 评估基于"技术正确性"（防幻觉/确定性/不越维）·未对齐用户产品意图（EMC+RAG = 专业知识蒸馏·LLM 是蒸馏器·非风险源）。

**glm 仍成立的部分**：
- **R3 RAG 不可用 → EXIT_CONCEPT 兜底**：RAG 检索失败时（超时/索引缺失）·无素材可综合·禁 LLM 凭空编·走确定性兜底——**这部分 claude 保留·正确**。
- 即"无素材时不让 LLM 编"对·"有素材时禁 LLM 综合"错。

### glm 自我挑战（交叉验证·本次缺位）

**glm 应自我挑战但未做的问题**：
- "禁 LLM 确定性组装·检索结果拼列表——这真的是用户要的'稳定准确全面'吗？"
  - **稳定**：拼列表稳定 ✅（确定性）。
  - **准确**：拼列表准确 ✅（来源可溯）。
  - **全面**：拼列表**不全面** ❌（Top-5 检索结果直接拼·无归纳总结·无跨条目综合·信息零散）。
- **glm 当时若自问"全面"·会发现拼列表缺 LLM 综合·不会坚持零 LLM**。
- **教训**：glm"基于事实"需升级——不仅 trace 取证（技术正确）·还要对齐用户"稳定准确全面"三标准（产品意图）。

---

## 二、6 对齐焦点回应（glm）

### 焦点 1：三支柱原则（缺一不可）— **agree（完全认可）**

**用户原则**：① 本地知识库完备度（素材）② EMC 架构（分类→范式路由）③ LLM 归纳总结（综合+引用）——缺一不可。

**glm 确认**：**agree——三支柱是知识问答的完整链路·glm 历轮砍了第三支柱（LLM 综合）·根本错误。**

**三支柱对齐（glm 理解）**：

| 支柱 | 角色 | glm 历轮 | 修正 |
|---|---|---|---|
| ① 本地知识库 | 素材基础（事实卡/笔记/政策/案例） | ✅ glm 一直重视（CB-21b 提炼/事实卡 dimension） | 保持 |
| ② EMC 架构 | 分类→范式路由（正确路由知识问答） | ✅ glm 认可（分类→范式映射） | 保持 |
| ③ LLM 综合 | 归纳素材+引用来源（蒸馏成回答） | ❌ **glm 砍了（零 LLM 拼列表）** | **恢复** |

**glm 后续开发融入**：
- 评估知识问答方案时·**三支柱检查**——"此方案是否砍了任一支柱？"
- LLM 综合是**核心价值**（蒸馏器）·非风险源——防幻觉靠**指令约束**（禁图层/禁编造/引用来源）·非靠"禁 LLM"。

### 焦点 2：产品定位（本地化聚焦蒸馏）— **agree（完全认可）**

**用户定位**：EMC+RAG = 纯问答 → 稳定准确全面的相关信息。核心价值 = 区别于网络搜索/其他 AI——本地化+聚焦+专业+可追溯的专业知识蒸馏。

**glm 确认**：**agree——产品定位决定了 LLM 综合是核心（蒸馏器）·非可选。**

**glm 理解（KNOWLEDGE.md :49 蒸馏）**：
- **本地化**（宜昌专属）—— 知识库（第一支柱）。
- **聚焦**（城市更新/体检）—— 知识库主题域。
- **专业**（权威源）—— 知识库质量。
- **可追溯**（来源引用）—— LLM 综合时引用（第三支柱·非拼列表）。
- **蒸馏**（稳定准确全面）—— **LLM 综合归纳**（第三支柱核心）。

**glm 历轮脱节**：
- glm 聚焦"防幻觉/确定性"（技术正确）·忽视了"蒸馏"需 LLM（产品核心）。
- 拼列表是"检索结果罗列"·非"蒸馏"——用户要的是"LLM 把 Top-5 素材综合成连贯专业回答"。

### 焦点 3：修正后路径（检索→路由→LLM 综合）— **agree（正确·三支柱完整）**

**修正路径**：
```
rag_query 短路 → RAG 检索素材（知识库·支柱①）
  → 架构路由知识问答范式（EMC 架构·支柱②·注入知识问答指令·禁图层）
  → finalStep LLM 综合总结 + 引用来源（LLM·支柱③）
```

**glm 确认**：**agree——三支柱完整链路·vs glm 零 LLM（砍支柱③）根本错误。**

**glm 核实（claude 回滚后现状）**：
- harness rag_query 分支（:976+）：RAG 检索成功 → 注入 ctx.context（素材 + 知识问答排版指令）→ **finalStep LLM 综合** ✅
- RAG 不可用 → EXIT_CONCEPT 兜底（glm R3 保留·正确）✅
- **即 claude 回滚正确**——可用时走 LLM 综合（支柱③）·不可用时兜底（无素材不编）。

**glm 补充建议（路径细节·非阻断）**：
- 检索素材注入格式：Top-5 条目（score + data_dim + source + snippet）—— 供 LLM 综合。
- 知识问答指令（:注入）须含：**禁图层/禁 4×5/条目式/引用来源/数据维度不越维**——防被 FINAL_TEMPLATE 图层导向带偏（第二根因治标·非禁 LLM）。
- **vs glm 方向 B**：glm 想禁 finalStep·修正是**走 finalStep 但注入知识问答指令**（约束 LLM 走纯问答·非图层）——Codex 方向 A 正确。

### 焦点 4：零 LLM 教训（收敛防砍支柱 + 组间交叉挑战）— **agree（glm 诚实接受 + 自我反思）**

**讨论发起复盘**：
- Codex 方向 A（注入指令 LLM）—— **正确**。
- glm 方向 B（零 LLM 确定性组装）—— **错误**（砍支柱③）。
- claude 收敛选 B —— **决策失误**（采纳错的·没采纳对的）。
- 组间交叉缺位 —— Codex 未质疑 glm 零 LLM·glm 未自我挑战。

**glm 诚实接受 + 反思**：

**glm 自我挑战缺位（核心失误）**：
- glm 坚持零 LLM 时·**未自问"全面性"**（拼列表缺 LLM 综合·不全面）。
- glm"基于事实·不固执"原则——但本次只 trace 取证技术正确（防幻觉）·未对齐用户产品意图（蒸馏需 LLM）。
- **glm 原则升级**：基于事实 = ① trace 取证（技术正确）+ ② 对齐用户产品意图（稳定准确全面）——两者都不可少。

**组间交叉挑战（glm 建议机制）**：
- 讨论发起提"组间应交叉挑战"——glm agree。
- **机制建议**：方案收敛时·各组须回答"此方案是否砍了三支柱之一？"——任一组质疑则复议。
- 本次若 Codex 质疑 glm"零 LLM 砍支柱③"或 glm 自问"全面性"·可避免失误。
- **glm 承诺**：后续评估主动做三支柱检查 + 交叉挑战（非各自独立·互质方案）。

### 焦点 5：知识问答范式细节（指令/保证稳定准确全面）— **agree + glm 补充 4 点**

**讨论发起问**：知识问答指令（禁图层/禁 4×5/条目式/引用来源）——够吗？如何保证"稳定准确全面"？

**glm 确认**：**agree 指令方向——补 4 点保证稳定准确全面。**

**glm 补充（知识问答指令强化）**：

| 维度 | 指令要素 | 保证 |
|---|---|---|
| **稳定** | 禁编造（仅引检索素材）+ 禁臆测（无素材说"知识库无相关"） | 不飘 |
| **准确** | 引用来源（每条结论标 [来源]）+ 数据维度不越维 | 可溯 |
| **全面** | 综合所有 Top-K 素材（非只引 Top-1）+ 跨条目归纳 | 不漏 |
| **禁偏** | 禁图层导向 + 禁 4×5 归因 + 禁情绪分析式（第二根因防） | 不跑偏 |

**glm 关键补充（"全面"保证）**：
- LLM 综合时易只引 Top-1（最相关）·漏 Top-2~5——指令须明确"综合所有检索素材"。
- 例：检索 Top-5（南湖/葛洲坝/汉宜村/伍家岗/二马路）——LLM 应综合 5 个·非只说南湖。
- **建议指令**：`综合以下全部检索素材（Top-{N}）·归纳共性与差异·每条结论标来源·禁只引部分。`

### 焦点 6：验证验收 — **agree + glm 补 3 断言**

**讨论发起问**：复测"宜昌有哪些更新项目"→ LLM 综合 + 引用来源（稳定准确全面）——验收标准？

**glm 确认**：**agree + 补 3 断言。**

**glm 验收标准（4 项）**：

| # | 验收 | 断言 |
|---|---|---|
| 1 | **LLM 综合**（非拼列表） | 回答是连贯段落/条目归纳·非"1. [0.82] xxx / 2. [0.76] xxx"原始列表 |
| 2 | **引用来源** | 回答含来源标注（文件名/路径·可溯） |
| 3 | **全面**（综合 Top-K） | 回答涵盖 ≥3 个检索条目（非只 Top-1） |
| 4 | **不越维 + 不图层** | 无"分析图已生成"/无 {{show}}/无越维（颗粒度原则） |

**glm 关键断言**：
- **验收 1（LLM 综合 vs 拼列表）是核心**——回答须是"LLM 归纳的连贯专业回答"·非"检索结果原始拼接"。
- 这是零 LLM 与修正后路径的**本质区别**——验收须明确区分。

---

## 三、glm 交叉挑战（自我 + 对 Codex/claude）

### glm 自我挑战（诚实反思）

**glm 应自问但未做的问题（本次缺位）**：
1. "零 LLM 拼列表——用户要的'全面'达到了吗？" → ❌ 拼列表无归纳·不全面。
2. "禁 LLM——三支柱之三被砍了吗？" → ❌ 砍了 LLM 综合。
3. "我的'基于事实'——含用户产品意图吗？" → ❌ 只技术正确·缺产品意图。

**glm 教训内化**：
- 基于事实 = trace 取证（技术）+ **用户产品意图对齐**（稳定准确全面）。
- 防幻觉靠**指令约束**（禁编/引用/维度）·非靠**禁 LLM**。
- LLM 是蒸馏器（核心价值）·非风险源——风险靠约束防·非砍 LLM。

### glm 对 Codex 的挑战（交叉验证）

**Codex 方向 A（注入指令 LLM）正确**——但 glm 挑战一个细节：

- **Codex A 的"注入指令"须保证 LLM 真听**——第二根因就是 LLM 受 FINAL_TEMPLATE 图层强约束（:131）·指令弱约束可能被覆盖。
- **挑战**：指令注入（ctx.context 头部）vs FINAL_TEMPLATE 图层约束（prompt 正文）——两者冲突时 LLM 可能仍走图层。
- **验证**：复测须确认 LLM 真走纯问答（非图层）——若仍出"分析图已生成"·说明指令不够强·须强化（如 ctx.context 头部加 `【本次为知识问答·严禁图层/分析模板】` 强标记）。

### glm 对 claude 的挑战（交叉验证）

**claude 回滚正确**——但 glm 挑战收敛决策：

- **claude 收敛选 glm B（零 LLM）·没采纳 Codex A（注入指令）**——claude :33 "决策失误"。
- **挑战**：claude 收敛时是否做了三支柱检查？"此方案砍支柱③了吗？"
- **机制建议**：收敛须标"三支柱影响"（砍了哪个/没砍）——本次若标"glm B 砍支柱③"·可触发复议。
- **glm 不是指责 claude**（glm 自己也错了）——而是**机制改进**：收敛须三支柱检查 + 组间交叉。

---

## 四、风险清单

### 阻断级
无（修正后路径已实施·e2e 5/5）。

### 警告级

| # | 风险 | glm 建议 |
|---|---|---|
| **W1** | 知识问答指令 vs FINAL_TEMPLATE 图层约束冲突（第二根因复发） | 指令强标记（ctx.context 头部 `【知识问答·严禁图层】`）+ 验收断言"不图层" |
| **W2** | LLM 综合只引 Top-1（不全面） | 指令"综合全部 Top-K"+ 验收断言"涵盖 ≥3 条" |

### 建议级

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | 三支柱检查机制（收敛时标"砍了哪个支柱"） | P0（防再犯） |
| **S2** | 组间交叉挑战（互质方案·非各自独立） | P0（机制） |
| **S3** | glm"基于事实"原则升级（trace + 产品意图对齐） | P0（自律） |
| **S4** | 知识问答指令强化（禁图层强标记 + 综合全部 Top-K） | P1 |

---

## 五、可否执行结论

### ✅ 三支柱 + 产品定位全 agree——glm 诚实承认零 LLM 错误·接受修正

**glm组 核心立场**：

**glm 诚实承认**——从 R3 到第二根因到分类→范式·glm 一贯坚持"rag_query 全程禁 finalStep LLM / 确定性组装"·**被用户验证否定**。glm 过度防幻觉·把"模板错位"误推为"禁 LLM"·忽视了知识综合需 LLM（三支柱之三）。

**三支柱 + 产品定位全 agree**：
- 三支柱（本地知识库 + EMC 架构 + LLM 综合·缺一不可）：**agree**。
- 产品定位（本地化聚焦专业知识蒸馏）：**agree**。
- 修正后路径（检索→路由→LLM 综合）：**agree**。
- 零 LLM 教训（收敛防砍支柱 + 组间交叉）：**agree + glm 自我反思**。

**glm 历轮唯一仍成立的**：
- **R3 RAG 不可用 → EXIT_CONCEPT 兜底**（claude 保留）——无素材时不让 LLM 编·正确。
- 即"无素材禁 LLM 编"对·"有素材禁 LLM 综合"错。

**6 焦点判定**：
- 焦点 1 三支柱：**agree**
- 焦点 2 产品定位：**agree**
- 焦点 3 修正路径：**agree**
- 焦点 4 零 LLM 教训：**agree + glm 自我挑战缺位反思**
- 焦点 5 范式细节：**agree + 补 4 点（稳定/准确/全面/禁偏）**
- 焦点 6 验证：**agree + 补 3 断言（LLM 综合/引用/全面）**

**glm 交叉挑战**：
- **自我挑战**：glm 未自问"全面性"/"砍支柱"/"产品意图"——三问缺位致错。
- **对 Codex**：方向 A 正确·但指令注入须保证 LLM 真听（vs 图层约束）——验收须确认。
- **对 claude**：收敛选 B 策误·建议三支柱检查机制（收敛标"砍了哪个支柱"）。

**glm 原则升级（核心内化）**：
- **基于事实 = trace 取证（技术正确）+ 用户产品意图对齐（稳定准确全面）**——两者缺一不可。
- **LLM 是蒸馏器（核心价值）·非风险源**——风险靠指令约束防·非砍 LLM。
- **防幻觉靠约束（禁编/引用/维度）·非靠禁 LLM**。
- **组间交叉挑战**——互质方案·非各自独立。

**承重红线**：
- FINAL_TEMPLATE：知识问答指令注入 ctx.context（非改模板）—— 不碰 D019 ✅
- diagnose：零触碰（rag_query 短路前置）✅
- 四态出口：rag_query 走 finalStep（LLM 综合）·EXIT_RESULT/CONCEPT（非新态）✅

---

## 附：现状核实证据（glm 独立）

| 发现 | 证据 |
|---|---|
| **三支柱沉淀 KNOWLEDGE.md** | :48 "三支柱缺一不可...零 LLM = 砍支柱③·用户验证否定" |
| 产品定位沉淀 | :49 "EMC+RAG = 本地化聚焦专业知识蒸馏" |
| **claude 回滚正确** | harness rag_query（:976+）恢复"注入素材 → finalStep LLM 综合" |
| **glm R3 部分保留** | RAG 不可用 → EXIT_CONCEPT 兜底（:R3 注释·正确） |
| 知识问答指令注入 | ctx.context 头部"知识问答排版"指令（禁图层/禁 4×5/条目式/引用） |

### 声明

本回应由 glm组（ZCode + GLM 5.2）独立产出·2026-08-09·基于核实 KNOWLEDGE.md 三支柱蒸馏 + harness rag_query 回滚现状。第三方独立评估·不做项目方决策背书。**glm 诚实承认历轮"零 LLM 确定性组装"立场被用户否定——过度防幻觉·忽视 LLM 综合是三支柱之三**。glm"基于事实·不固执"原则**包括承认自己的产品意图脱节错误**——这是 glm 一贯原则的深化（技术正确 + 产品意图对齐）。

---

*登记：docs/context-map.md · CB-22 三支柱对齐 glm组 回应。*
