# 情绪地图 · P3-4 地点联动复验 — 发起（claude组 · CB-19 补）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB 实施后复验（两组·先验后推）
> **承接**：CB-19 PRM 预检 + P3-4 地点联动实施完成（commit `9680dcc`·**未 push·先验后推**）
> **目的**：让两组对 P3-4 实施独立复验（逐项核对 + 有无新问题），通过后 push + 进发版候选
> **背景**：glm 发版回归绿（`发版回归_结果_glm组_2026-08-08.md`）·P3-4 = 出口闭环最后一块（CB-17 P2 项·glm 建议优先于 P3-1）

---

## 〇、实施摘要（claude组 已完成·待两组复验）

**P3-4 目标**：工具出口统一 place_name + 微观落点精确化——「需求位置/微观落点」从粗略名升级到精确 POI 落点 + 诚实标注精确度。**不新增分析·不新增字段·不碰 diagnose/finalStep/harness 主循环**。

### P3-4-1 后端出口统一（Gap B 核心杠杆）
- `api/geo_routes.py:376-377`（zonal）+ `:514-515`（rank）prop_cols 各加 4 列：`place_name, place_name_source, poi_names, poi_count`
- **效果**：一个改动打通 zonal/rank → 出口卡需求位置 + 结论段 + 地图图层四路消费（harness `_lastToolRows` 自动受益）
- 单测：test_geo_routes.py 补「rows 含 4 键」断言（14 passed）

### P3-4-2 地图图层透传
- `frontend/js/toolbox/shared.js:60-68` buildZonalFc 焊属性追加 `place_name/place_name_source/poi_names/poi_count` 透传
- **效果**：zonal/rank 合成 feature 携带地点 → popup + outlet 图层 fallback + 深读可用（node OK）

### P3-4-3 出口卡诚实标注 + 微观精确化
- `build_outlet_schema.py:329-340` micro 分支：scale==micro 时按 place_name_source + poi_names——
  - poi_sjoin → 已精确·source 补注
  - poi_top_places 且有 poi_names → 需求位置升级为首个 POI（治 Gap C）
  - 无 poi_names → value 不动·source 标粗略
- `build_outlet_schema.py:388-397` limitations 动态化：按 Top-1 row place_name_source 标注（poi_sjoin→精确·poi_top_places→面域代表+POI 增强·spatial_hotspot/area_seed→粗略兜底·缺失→未定位）·治 Gap A 硬编码误标
- 单测：test_outlet_schema.py 补 4 例（动态 limitations 三态 + micro 三场景·41 passed）

### P3-4-4 LLM grounding 小改
- `tools.js:255` `_fmtRow` → `row.place_name || row.name || '未命名'`（rank-on-grid 也能锚到 POI·node OK）

### 后置（明确不做）
- Gap D hex 补 POI（EMC micro 主路径是 square grid·低用频）·Gap E 跨格找最近（质量提升）
- buffer/hotspot/nearest/density 出口（grid fc 全列 + artifacts fallback 已天然覆盖）

### 验证
- pytest **301 passed + 5 skipped**（+4 新增·零回归）
- e2e-seam：test_outlet_micro（新）**PASS**（poi_top_places 升级/poi_sjoin 保持/无 POI 诚实）+ test_outlet_macro（扩）**PASS**（rows 带地点·limitations 标精确）
- ESM 语法 OK（tools.js/shared.js）

---

## 一、glm 发版回归事实修正（claude组 核实）

glm 回归报告（`发版回归_结果_glm组_2026-08-08.md`）写「Run 1 = 23/26 + Run 2 = 5/26 + Run 3 未跑」。但 audit 时间戳 + report 核对实际：

| Run | 时间 | 结果 | glm 报告 vs 实际 |
|-----|------|------|------|
| Run 1 | 16:06 | **23/26（88%）** | ✅ 一致 |
| Run 2 | 16:25 | **23/26（88%）** | ❌ glm 报告未列（实际也是 88%·**更好**） |
| Run 3 | 17:04 | 5/26（21 timeout·API 慢） | ❌ glm 报告写「Run 2 = 5/26·Run 3 未跑」·实际 Run 3 才是 API 慢 |

**事实修正**：**两个 88% Run 都通过**（fail 集均 ⊆ {PRM-03/04/05/07} 已知 backlog）·仅一个 API 慢时段 Run 3。**回归判定不变（绿·且更强）**——只是 glm 报告计数有误（少报一个 88% Run·错标 Run 序号）。

**PRM-05 判据建议（glm 提出）**：B3 Run 1 fail = {PRM-03, PRM-05, PRM-07}·PRM-05（boundary derive 方差·zonal 路由偶发）不在 claude 判据 {PRM-03/04/07}。glm 建议**补入 fail 集判据** = {PRM-03/04/05/07}。claude 核实：PRM-05 是 CB-12 起持续已知 backlog·非并发改动引入·**采纳补入**。

---

## 二、复验焦点（两组独立复验）

### 焦点 1：P3-4-1 prop_cols 放行
- zonal/rank rows 加 4 列·纯 additive·有无副作用？（rows 结构扩充·消费方/断言是否受影响）
- 单测「rows 含 4 键」覆盖够不够？

### 焦点 2：P3-4-3 出口卡动态标注
- limitations 按 source 动态化——文案分支（poi_sjoin/poi_top_places/粗略/未定位）是否合理·有无遗漏？
- micro 需求位置 POI 升级——poi_names 首个是否可靠？有无过度升级风险（如 POI 名不合适）？

### 焦点 3：回归风险
- prop_cols/buildZonalFc/limitations 改动对现有断言影响？（test-cases/flywheel/validate_outlet_fields）
- `_fmtRow` place_name 优先——LLM observation 变化是否影响 flywheel 观察类断言？

### 焦点 4：glm 回归事实修正 + PRM-05
- claude 修正（两个 88% Run + Run 3 API 慢）——认可？
- PRM-05 补入 fail 集判据 {PRM-03/04/05/07}——认可？

### 焦点 5：两组补充与异议
- 对 P3-4 实施/复验结论有无异议？（用错标尺/事实错误/承重红线→指出）
- 各自环境有无已跑验证可复用为复验证据？

---

## 附 A：P3-4 复验 prompt（发两组用）

```text
【CB-19 · P3-4 地点联动复验】情绪地图 — 工具出口统一 place_name + 微观落点精确化

你以第三方独立验收身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·commit 9680dcc 未 push·工作区已同步）。可跑只读测试（pytest / browser e2e-seam 直测）作为复验证据。

背景：CB-19 PRM 预检 + P3-4 地点联动实施完成（出口闭环最后一块）。claude组 修：prop_cols 放行地点字段 + buildZonalFc 透传 + 出口卡动态 limitations + micro 需求位置 POI 升级 + _fmtRow place_name 优先。请对实施独立复验。
必读：
  docs/catch-ball/discuss/P3-4地点联动_复验发起_2026-08-08.md（实施摘要 + 5 焦点 + glm 回归事实修正）
  docs/catch-ball/discuss/发版回归_结果_glm组_2026-08-08.md（glm 回归·注意事实修正）
  docs/catch-ball/discuss/PRM_backlog预检_2026-08-08.md（CB-19 预检背景）
按需核实：api/geo_routes.py（:376-377/:514-515 prop_cols）·
  ai_qa/outlet_kb/build_outlet_schema.py（:329-340 micro 分支·:388-397 limitations）·
  frontend/js/toolbox/shared.js（buildZonalFc 焊属性）· frontend/js/ai_qa/tools.js（_fmtRow）·
  tests/test_geo_routes.py · tests/test_outlet_schema.py · tests/browser/test_outlet_micro.py

复验任务（5 焦点·逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. P3-4-1 prop_cols 放行（zonal/rank rows 加 4 列·additive·副作用？）
2. P3-4-3 出口卡动态标注 + micro 升级（limitations 分支合理？poi_names 首个可靠？过度升级风险？）
3. 回归风险（prop_cols/buildZonalFc/limitations/_fmtRow 对现有断言影响）
4. glm 回归事实修正（两个 88% Run + Run 3 API 慢·认可？）+ PRM-05 补入 fail 集判据 {PRM-03/04/05/07}？
5. 补充与异议

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（单一地点字段源·Smart 两端 + Dumb 中间）。
承重红线：四态出口 / finalStep D019 / diagnose 永不动 / 追踪编号连续 / 「EMC 不硬猜」（CB-14）。
产出：docs/catch-ball/discuss/P3-4地点联动_复验回应_{你的组名}_2026-08-08.md
格式：〇 一句话结论 / 一 逐焦点 / 二 新问题风险清单（阻断/警告/建议）/ 三 可否 push + 进发版候选结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · P3-4 地点联动复验发起 · 2026-08-08*
*commit 9680dcc 未 push·两组复验通过后 push + 进发版候选*
