# PT-CB11 · P1 三件工具执行记录（Codex · 2026-08-21）

> 派发单：`PT-CB11-P1三件工具派发单_Codex_zcode-2026-08-21.md`。分支 `EMC_harness_dsh`，基线 HEAD = **0fb5d604**（pull 后 Already up to date）。commit 前缀 `PT-CB11(C1):`。

## 一 真身核对（派发单前置条件·全部相符，零停手）

| 依赖 | 真身位置 | 结论 |
|---|---|---|
| `create_square_grid(gdf, cell_size, unit='m', target_crs='EPSG:4546', agg_cols=None)` | `core/spatial_analysis.py:806-812` | 签名符；显式 agg_cols 输出 `{col}_sum`/`{col}_mean`（:878-884），另有 `point_count`/`score_mean`/`polarity_index` |
| `aggregate_by_polygons(points, polygons, agg_cols=None, polygon_name_col=None)` | `core/spatial_analysis.py:237-242` | 签名符；`polygon_name_col='name'` 输出可读单元名 |
| `hot_spot_analysis(gdf, value_col='score', invert=True, threshold=1.96, soft_threshold=1.0)` | `core/spatial_analysis.py:32-38` | 签名符；返回列 `Gi_Z`/`Gi_P`/`hotspot`/`hotspot_tier`（五档同值 :117-122） |
| `list_boundaries()` 返回 `label` 字段 | `core/geo_registry.py:120-135` | 符（id/label/name_field/usage） |

## 二 实现摘要（严守同文件并行协调三处授权区）

改动仅 `tools/mcp_server_emc.py` + `tests/test_mcp_server_emc.py`：

1. **register 段**（F_032 行后）：F_033/F_034/F_035 三行注册；
2. **三个新函数**（rank 之后、`_dataset_meta` 之前）：
   - `grid_aggregate`（F_033）：resolve_points → 可选 G-2 守卫+gpd.clip 裁剪 → create_square_grid → `{value}_mean` 或 point_count 降序 → rows cap 20 + stats{total_cells, nonzero_cells, max_count} + layer_output 复用 `_layer_output_geojson`（200KB 硬顶）；value_col 缺列语义化拒绝（附可用列）；
   - `compare_regions`（F_034）：boundaries 规整（list 或 `a|b`/`a,b` 分隔串）→ <2 拒绝 / >5 截断标注 → 逐区 G-2 守卫 + dissolve 合一 + `list_boundaries()` label 命名 → pd.concat → aggregate_by_polygons → diff（point_count/polarity_index/score_mean/`{c}_mean`；polarity 按 **abs** 值语义对齐 zonal_stats；ratio = max/min，min≤0 → None 不除零）；
   - `hotspot_analysis`（F_035）：resolve_points → value_col 缺列拒绝（附可用数值列 + density 防呆提示）→ 真身五档 → counts → 显著优先排序（hot→cold→tend_hot→tend_cold→ns·组内 |Z| 降序）→ rows（place_name/Gi_Z/Gi_P/hotspot_tier）+ layer_output；
3. **build_server()**：`server.tool()` 注册三处（rank 后）。禁改清单（render_spec/render_file/_dataset_meta/_layer_output_geojson/_gdf_rows/zonal_stats/buffer/rank 函数体）全部未触碰；caliber 按派发单原文以函数内字面量提供（不动模块级 CALIBERS，避免与 zcode 并行段冲突）。

实现细节偏差（合理收敛，非签名不符）：hotspot 拒绝分支的「可用数值列」列表对非 DataFrame 点层（send-in dict 解析态）做了 try 容错降级为「列出全部非 geometry 列」——测试发现 `_FakePoints` 不可下标时崩溃，收敛为不阻塞语义化拒绝。

## 三 五判据逐工具一行答辩

| 工具 | 结构化 | 口径内建 | 脱敏自动 | 错误语义化 | 组合性 |
|---|---|---|---|---|---|
| grid_aggregate | rows 统一 dict + stats 三键 | caliber 四键（方格≠行政单元） | 只输出统计列·零 PII | 未知层→list_data hint；value_col 缺→附可用列；G-2 结论层拒绝 | layer_output geojson 直供 render_spec |
| compare_regions | regions 行 + diff 五键结构 | caliber 四键（2-5 区/同口径） | 聚合统计·零 PII | <2 区/类型错→hint；G-2 逐区拒绝；未知层→list_data | 结果可接 rank/zonal 交叉复核 |
| hotspot_analysis | counts 五档 + rows 统一 | caliber 四键（显著≠重要性/threshold 置信表） | 行仅 place_name/Z/P/档位 | 未知层/缺列（附 density 防呆）/真身异常捕获 | layer_output geojson + counts 供叙述层 |

## 四 测试与 DoD

- 新增 **12 用例**（`tests/test_mcp_server_emc.py`：注册断言 1 + grid 4 + compare 4 + hotspot 3），monkeypatch 范式沿 `_patch_geo`；
- `python -m pytest tests/test_mcp_server_emc.py -q` → **29 passed**（原 17 + 新 12）；
- `python -m pytest tests/ -q` → **collected 460**（基线 0fb5d604 实收 448 + 本批 12）：**448 passed / 7 failed / 5 skipped**。7 失败全部为环境缺依赖（`matplotlib`×3 于 test_sandbox、`sentence_transformers`×4 于 validate_rag_material），与本批改动文件零交集，属基线环境项——非本批引入，待主手裁决是否补装依赖（涉及 pip install，超出本批授权）。
- DoD：三函数 ✓；F_033-F_035 注册连续 ✓；测试绿（除基线环境项）✓；执行记录本件 ✓；显式路径 commit ✓（push 留主手统一）。

## 五 交付清单

- `tools/mcp_server_emc.py`（+约 210 行：三注册行 + 三函数 + build_server 三行）
- `tests/test_mcp_server_emc.py`（+12 用例）
- 本记录

> Codex · 2026-08-21 · PT-CB11 P1 三件完成。
