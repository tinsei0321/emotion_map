# 情绪地图 · 发版回归全面测试方案（三组协同 · CB-19 收尾）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：发版候选前最终回归（三组协同·全面严谨）
> **承接**：CB-19 修复全闭环（P3-4 `9680dcc` + PRM-03/04 `083b78d` + PRM-05/07 `469bf32`·已推送）→ Codex 指出「B3 三连跑在修复 commit 之前·回归绿未覆盖」→ **需补一轮覆盖修复 commit 的全面回归**
> **目的**：三组分工跑**全面测试矩阵**，覆盖所有修复 commit + 回归基线，fail 集对比判定 → 发版候选
> **用户指令**：三组一起跑·全面·严谨·claude 制定方案

---

## 一、测试范围（全面矩阵）

### 覆盖的修复 commit（本轮回归核心）

| commit | 修复 | 验证要点 |
|---|---|---|
| `9680dcc` | P3-4 地点联动（prop_cols 放行 / buildZonalFc 透传 / 出口卡动态标注 / micro 升级） | zonal/rank rows 带 place_name·出口卡需求位置精确·limitations 动态 |
| `083b78d` | PRM-03/04 根治（G5 周边→buffer 多工具重写） | 「周边 Nm 情绪」不再错路由 merge/lookup_place |
| `469bf32` | PRM-05/07 修复（zonal fallback + 测量端对齐 + 契约强化 + 法定功能区黑名单） | 西陵区聚合执行·小溪塔拒识 request_upload |

### 测试矩阵（9 项·按优先级）

| # | 测试项 | 命令 | 判据 | 归属 |
|---|---|---|---|---|
| T1 | **pytest 全量** | `py -m pytest tests/ -q` | **303 passed + 5 skipped** 基线 | claude（已跑·重跑确认） |
| T2 | **validate 系列** | `py -m pytest tests/test_emc_template.py tests/validate_*.py -q` | 34 passed | claude |
| T3 | **B3 快照 ×2**（覆盖修复 commit） | `py tests/browser/flywheel_audit.py --batch B3`（session 带批号） | fail 集 ⊆ **{PRM-03/04}**（修复后期望·center ask_user 正确行为不可消除）·任何新增 fail 即阻断 | claude ×1 + glm ×1 |
| T4 | **link_checkup 20/20** | `py tests/browser/test_link_checkup.py` | 20/20 PASS | glm |
| T5 | **eval 复采** | `py tests/eval_template_flash.py`（带 session） | ≥80% GO | glm |
| T6 | **PRM 修复专项**（浏览器×3） | `verify_prm03_fix.py` + `verify_prm57_fix.py` + 新增 `verify_prm04_fix.py` | 周边→buffer/ask_user 绝不 merge·小溪塔拒识·西陵区执行 | Codex |
| T7 | **三路径观点卡** | `py tests/browser/test_insight_cards_3path.py` | PASS（单/链/multi 出卡） | Codex |
| T8 | **出口卡 e2e** | `test_outlet_micro.py` + `test_outlet_macro.py` + `test_result_struct.py` | PASS（微观精确化·动态标注·结论带地点） | Codex |
| T9 | **ESM 语法** | node --check 改动 JS | 全 OK | claude |

### 修复后期望 fail 集（判据核心）

```
基线（B3 回归·修复前）：{PRM-03, PRM-04, PRM-05, PRM-07}
修复后期望：           {PRM-03, PRM-04}（center ask_user 正确行为·不可消除·判据已放行）
                       PRM-05 → 应 PASS（zonal fallback + 测量端对齐）
                       PRM-07 → 应 PASS（法定功能区黑名单拒识）
任何新增 fail（不在 {PRM-03/04}）→ 阻断发版候选
```

---

## 二、三组分工（并行·平衡工作量）

| 组 | 任务 | 产出 |
|---|---|---|
| **claude组（开发主）** | T1 pytest 全量 + T2 validate + T3-B3-快照#1 + T9 ESM | `发版回归全面测试_结果_claude组_2026-08-08.md` |
| **glm组（评估）** | T3-B3-快照#2 + T4 link_checkup + T5 eval | `发版回归全面测试_结果_glm组_2026-08-08.md` |
| **Codex（评估）** | T6 PRM 修复专项（浏览器×3 问）+ T7 三路径 + T8 出口卡 e2e | `发版回归全面测试_结果_Codex-GPT5_2026-08-08.md` |

**并行度**：三组可同时跑（B3 ~10-12min/次·link_checkup ~5min·eval ~5min·专项 ~10min）→ 总墙钟 ~15-20min（vs 单组串行 ~40min）。

---

## 三、严谨性要求（RULES §1.2 trace 取证）

1. **每项带 session**：`EMOTION_TRACE_SESSION=<组>-<批次>`（如 `claude-B3-final-1`）
2. **fail 集取证**：B3 报告附 `trace_query --stats --session <批号>` 证据（F_001/F_002/F_005 计数·防 while-loop 退化）
3. **结果落盘**：B3 audit json（`tests/browser/out/audit-B3-<ts>.json` 自动）+ report md（`tests/reports/`）
4. **判据统一**：fail 集 ⊆ {PRM-03/04}（修复后基线）·任何新增 fail 即阻断
5. **API 慢时段识别**：若遇 timeout 大潮（tpl=None 全 timeout）→ 按 CB-12 方法论识别为 API 慢时段（trace F_001 ~1/case + 对照健康 Run）·非代码回归·记录不阻断
6. **每项给「通过/失败/需排查」** + 证据（path:line / 报告链接）

---

## 四、产出物格式（三组统一）

```
# 发版回归全面测试_结果_{组名}_2026-08-08.md
〇 一句话结论
一 各项结果表（# / 结果 / 证据 / 备注）
二 fail 集 vs 基线对比（{PRM-03/04/05/07} → 期望 {PRM-03/04}）
三 新增 fail 检查（任何不在 {PRM-03/04} 的 fail → 标【阻断】）
四 可否进发版候选结论
```

claude 收到三份结果 → 汇总收敛 → 判定「发版候选就绪」→ 更新 todo/revision-log/cb-index。

---

## 五、时序

```
T1/T2/T9（claude·~2min） ──┐
T3#1（claude·~12min） ─────┤
T3#2 + T4 + T5（glm·~15min）├─→ 三组并行 ~15-20min → 汇总收敛 → 发版候选
T6 + T7 + T8（Codex·~15min）─┘
```

---

*claude组 · 发版回归全面测试方案 · 2026-08-08*
*三组协同·fail 集判据 {PRM-03/04}·任何新增 fail 即阻断·trace 取证·结果落盘*
