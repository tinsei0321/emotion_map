# PT-CB15 · DATA 目录与代码映射重构（全组通知）

> 性质：**下次任务派发时各组必读**。落盘人：Codex（2026-08-25）。
> 权威入口：`DATA/README.md` + 本通知。冲突时以 `DATA/README.md` 为准。

## 白话摘要

这次把数据仓库重新分了区：权威数据放 `AUTHORITY`，专题数据放 `THEME`，预设注册表放 `REGISTRY`，RAG 放 `RAG`，导出放 `Export`，退休和敏感数据放 `_Retired`。老的 `DATA/boundaries` 和顶层 `DATA/exports` 已退休/删除，各组以后不要再用旧路径；代码常量已经同步，测试全绿（597 passed / 2 skipped）。

## 一、新目录结构

```text
DATA/
├─ AUTHORITY/    权威边界/点/线（boundaries_ / points_ / lines_ 前缀）
├─ THEME/        专题数据（theme_城市体检 / theme_12345政务热线 / theme_大南门…）
├─ REGISTRY/     预设注册表（presets/manifest.json + 图层文件）
├─ RAG/          rag_index/ + ai_qa/（索引与自成长记录）
├─ RAW/          原始输入
├─ SIM/          模拟数据
├─ performance/  展演/演示池
├─ POI/          地点/POI（place/、baidu-heatpoints/、yichang_pois*）
├─ Export/       分析导出（analysis/、exchange/、processed/、exports/）
├─ others/       同文件名的副格式（.csv 等）
└─ _Retired/     退休/敏感/过时/非数据格式
```

## 二、映射（旧 → 新）

见 `DATA/README.md`「旧路径 → 新路径映射」。关键几条：

- 预设：`DATA/boundaries/presets/` → `DATA/REGISTRY/presets/`。
- 权威：`DATA/analysis/体检对象_*` → `DATA/AUTHORITY/`。
- 体检专题点：`DATA/analysis/77项量化/checkup_qty_*` → `DATA/THEME/theme_城市体检/themes_point_checkup_*`。
- 12345：城市体检派生 → `DATA/THEME/theme_城市体检/12345_政务热线_城市体检分析/`；情绪地图专题 → `DATA/THEME/theme_12345政务热线/`。
- RAG：`DATA/rag_index/`、`DATA/ai_qa/` → `DATA/RAG/`。
- POI：`DATA/place/`、`DATA/baidu-heatpoints/` → `DATA/POI/`。
- 导出：`DATA/analysis/`、`DATA/exchange/`、`DATA/processed/`、`DATA/exports/` → `DATA/Export/`。

## 三、代码与索引同步（已落地）

- `core/config.py`：`PROCESSED_DIR = DATA/Export/exports`。
- `core/range_selector.py`：`_BOUNDARIES_DIR = DATA/REGISTRY`、`_PRESETS_DIR = DATA/REGISTRY/presets`。
- `tools/mcp_server_emc.py`、`ai_qa/tool_contracts.py`、`tools/check_manifest.py`：manifest 指向 `DATA/REGISTRY/presets/manifest.json`。
- `tools/rag_index.py`：`RAG_DIR = DATA/RAG/rag_index`。
- `ai_qa/episode.py`：`_EPISODE_DIR = DATA/RAG/ai_qa`。
- `core/place_layer.py`：place 数据 = `DATA/POI/place`；水系/主城 = `DATA/AUTHORITY`。
- `core/geo_registry.py`：qty 点层指向 `DATA/THEME/theme_城市体检`；L1/L2/L3L4 改读 GeoJSON。
- `frontend/serve.py`：白名单新增 `/DATA/REGISTRY/`、`/DATA/AUTHORITY/`、`/DATA/THEME/`；`e2e-seam.js` 取数指向 `DATA/REGISTRY/presets/`。

## 四、各组行动项（下次任务派发时）

1. **勿再引用** `DATA/boundaries`、顶层 `DATA/exports`（已退休/删除）。
2. 预设/上传一律走 `DATA/REGISTRY/presets/`；权威数据读 `DATA/AUTHORITY/`；专题读 `DATA/THEME/`。
3. 用地数据、建成区数据：**禁止入库/RAG**，未来仅用户一次性上传，不落盘。
4. RAG 索引重建命令：`py tools/rag_index.py --build`（索引位置 `DATA/RAG/rag_index`）。
5. 同名多格式：`.csv` 副格式统一放 `DATA/others/`（保留相对路径），代码优先读 GeoJSON。
6. 前端若直接 fetch 预设文件，使用 `/DATA/REGISTRY/presets/<file>`。

## 五、验证

- 全量门禁：`597 passed / 2 skipped`。
- RAG stats：`364 向量 / 512 维`。
