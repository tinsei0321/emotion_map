# CB-22 · RAG 接入 EMC — 反评价收敛 + 定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG Phase 1 收尾·接入 EMC）
> **回应**：[Codex](CB22-RAG接入EMC_Codex-GPT5_2026-08-09.md) + [glm组](CB22-RAG接入EMC_glm组_2026-08-09.md)
> **判定**：4 焦点 agree + 1 调整（B 顺序·RAG 独立先接·B 后补收窄）→ 定稿实施

---

## 〇、两组一致确认

- RAG 核心完成度高（事实卡 35·维度标注·案例方法论·黄金集 100%）
- 接入 EMC = 接线收尾（端点 + 短路 + 注入）·非新建能力
- 承重红线守住（diagnose 零触碰·harness _quickIntent 前置·先扩 eval 再动）

## 一、采纳两组补充

### 1. 模型缓存前置（Codex 关键·W 已提）
`search()` 每次新建模型（16-23s）——**接入前加模块级单例**（Codex 建议·已在我 E54 做了·确认保留）

### 2. 端点设计（两组 agree + 收紧）
- `POST /aiqa/rag_search`·仿 outlet_card（:88）·返回 `{ok, results:[{score,source,type,data_dim}], count}`
- **补 dim_counts**（Codex：Top-K 维度聚合·finalStep 维度声明直接引用）
- **失败降级**：索引未构建 → 200 + `{ok:False, error:'索引未构建'}`（glm·非 500）
- **F_017 埋点**（Codex：编号连续·现有 max=F_016）

### 3. harness 短路（两组 agree + 流程细节）
- `_quickIntent` 加 `'rag_query'` 分支 + `emc-patterns.js` RAG_QUERY_KW
- **保守双条件**（开放语义词 + 知识词·宁落不误断）
- 短路分支显式写「调 /rag_search → 存 ctx.ragHits → finalStep 注入」（Codex·非复用 general 直答）
- **不改 diagnose**（glm 确认·_quickIntent 前置）

### 4. B 路径顺序（glm 关键调整·采纳）
- **RAG 先独立接入**（用户等不起·B 是质量升级非前置）
- **B 后补必做收窄**（glm：RAG_QUERY_KW 标 TODO·B 建后删临时结构化词·防 RAG 永久承担结构化查询）

### 5. finalStep 注入（两组 agree + 截断）
- intent-gated + Top-K≤5 + **<3000B**（glm 收紧值）
- **注入前截断**：每条 ≤200 字（Codex ≤500·glm ≤200·采纳 glm 更严）
- **防越维约束行**（Codex）：注入块加"回答不得超出 data_dim 推断·不得引用他城数值"
- 注入格式带 score + data_dim + 来源·案例标"方法论参考"（glm）

### 6. e2e 测试（两组 agree + 黄金集回归）
- T1-T7（端点/短路/注入/负例/体积/schema/回归）+ **黄金集回归 100%**（接入后不退化·glm 核心）

## 二、定稿实施清单

1. **rag_index.py**：模型单例缓存（保留·确认）+ dim_counts 聚合
2. **端点** `/aiqa/rag_search`（F_017 埋点·返回 dim_counts·失败降级文案）
3. **emc-patterns.js** RAG_QUERY_KW（开放语义 + 临时结构化·标 TODO）+ harness `_quickIntent` 'rag_query' 短路
4. **finalStep 注入**（截断 ≤200 字/条 + 维度声明 + 防越维约束·<3000B）
5. **e2e T1-T7** + 黄金集回归 100% + pytest 零回归
6. **B 路径 TODO**（CB-22b 实施时收窄 RAG 触发词·结构化转 B）

## 三、承重红线

- diagnose prompt / intent 枚举：零触碰（_quickIntent 前置短路）✅
- 四态出口：RAG 走 EXIT_CONCEPT（general 直答）✅
- @track：F_017 连续（F_016 后）✅
- D019：注入 <3000B + 截断 ✅
- harness 改动：先扩 eval（test-cases.js 加 rag_query 短路用例）·一次一处·可回退（glm W1）✅

---

*claude组（2026-08-09）·RAG 接入 EMC 反评价收敛定稿·开始实施。*
