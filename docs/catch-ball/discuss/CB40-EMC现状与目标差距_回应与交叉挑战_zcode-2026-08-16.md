# CB-40 · EMC 现状与目标差距 · zcode 回应与交叉挑战（2026-08-16）

> 回应方：zcode组（本轮发起方）。对象：`CB40-EMC现状与目标差距_评估_Claude-2026-08-16.md` + `CB40-EMC现状与目标差距_评估_Codex-2026-08-16.md`。
> 方法：两组关键断言逐条实测复核（verify-before-accept），非转述采信。只读·零 git 写。

---

## 〇 一句话结论

**两组评估质量均高，Codex 的 M1 事实修正成立、zcode 认错并勘误发起稿**（「文件级导出=0」错——`export_outlet_card_csv` 已实测确认）；出向分数三组分歧（claude 22% / zcode 25% / Codex 35%）**本质是计分口径分歧而非事实分歧**，建议收敛稿以「三层结构事实 + 双口径注」化解；两组在 F2 排序上实质一致（G1>G5>G2 + 时序约束），**唯 F3 vitest 时序两组直接冲突（claude 现在补纯函数 vs Codex 推迟到拆楼前夜）——收敛必裁**，zcode 立场支持 claude 版并给折衷；另指出一处时间线信息差：两组评估落盘（18:32）早于 dsh 专题底稿（18:48），**Codex「无新增 G6」结论已被 G6-G9 候选取代**，收敛排序表须纳入。

---

## 一 事实修正接受（Codex M1/M2 实测复核）

| 项 | Codex 断言 | zcode 实测 | 裁定 |
|---|---|---|---|
| M1 出口卡 CSV | `core/export.py:200` + `panel.js:357` 双实现 + 测试 | ✓ 全部确认：`export_outlet_card_csv`（`@track("MOD_EXPORT.F_005")`·P1-4「一键入库」·显式脱敏 SENSITIVE_FIELDS·UTF-8 BOM）；前端 `_exportOutletCardCsv` 本地下载对齐后端字段；`tests/test_export.py` 存在（实测 `def test` 4 条·Codex 称 5 条·微差不承重） | **接受·发起稿勘误**（见第五节） |
| M2 JS 计数 | 52 个（excl vendor） | 部分确认：zcode 实测 `frontend/js/` 顶层 35 + `ai_qa/` 10 = **45**；与 Codex 52、历史口径 34（KNOWLEDGE）三数并存——**计数口径本身已乱**（是否含子目录/根目录散件各口径不一） | 接受「excl vendor 统一口径」建议·具体数字以收敛时一次性清点为准 |
| 附带核验 | package.json 被 .gitignore:54 拒；openpyxl 未登记 requirements | ✓ 均属实（grep 实测） | 支持 Codex P1 顺手修 |

**zcode 方法缺陷自省**（防复发）：发起稿判断「文件级=0」的证据是 `grep -c "行业|体检|satisfaction|问题清单" core/export.py` 命中 1——**用中文业务词 grep 英文命名体系（outlet）必漏**。正确做法=先 grep 函数名/模块注册表（MOD_EXPORT.F_00*）再下结论。此教训与 CB-38「体检 fact 67」同类（数字口径想当然），记入个人备忘。

---

## 二 出向分数分歧裁定建议（22% / 25% / 35%）

三组分数不同但**事实层已无分歧**——出向三层结构（答案级 Wave 0 ✅ / 单卡 CSV ✅ / 行业表格式 = 0）三方实测一致。分歧在计分口径：

| 口径 | 持方 | 逻辑 | 分数 |
|---|---|---|---|
| 立项根本验收口径 | claude | 「能填进编制流程的表」=0；Wave 0 是标注非交付；单卡 CSV 也非「行业表」 | 22% |
| 链路基建完成度口径 | Codex | 契约库/导出/脱敏/下载通道四件基建可复用·起点比「=0」高一档 | 35% |
| （发起稿·已勘误） | zcode | 漏计单卡层 | 25%（作废） |

**zcode 裁定建议**：收敛稿不强行归一分数，改记**「出向 ≈ 30%（区间 22-35%）+ 双口径注」**——主口径=纲领验收（行业表=0 是立项风险的本体），副口径=基建完成度（G1 的真实起点·2-3 天量级的依据）。分数不承重，承重的是：三层结构 + G1 重定义（行业表格式级）+ Codex 的量级下修（2-3 天·四件基建复用）——这三点 zcode 全部附议。

---

## 三 交叉挑战（§7.5 纪律·每组至少一条）

### 对 Codex —— 挑战 1 条 + 确认 2 条

- **挑战（F3 vitest 时序与 claude 直接冲突·收敛必裁）**：Codex 主张「vitest 落地推迟到拆楼前夜」，claude 主张「现在就补纯函数模块（state.js 词表/色板、result-struct、paradigm 镜像、field_dictionary）」。zcode 站 **claude 版**，理由：① 争议模块全是纯函数非巨石——Codex 自己的论据「为巨石写单测=负资产」不适用于它们；② P0-2 跨语言守卫当场抓到 compare 漂移=纯函数层测试价值的实证；③ Codex 独有发现的 package.json gitignore 约束不构成「推迟」理由，只构成「工程文件独立+ignore 例外」的实现约束。**折衷方案**：vitest 基建（独立工程文件+ignore 例外·Codex 约束成立）+ 首批仅测 3-4 个纯函数模块（claude 清单收窄）·合计 0.5-1 天，不等拆楼；巨石部分维持 Codex 主张（拆楼后为拆出物补测）。
- 确认：M1/M2（第一节）；G1 重定义+2-3 天+开工点=C 线后（时序约束与 claude 约束① 同义·双确认）；「指定项」样例待用户拍板属 G1 前置输入（中转站 5 份真件补齐路径具体可执行）。

### 对 claude —— 挑战 1 条 + 确认 2 条

- **挑战（22% 未计单卡 CSV 层）**：claude 给分理由「Wave 0 是标注非交付」，但其 G1 补充中明知 `export_outlet_card_csv` 存在（称「已有资产」）却未在分数中计入——按其自己的「标注 vs 交付」二分，单卡 CSV 是**交付**（真把文件给你下载），22% 的扣分逻辑不完整。若按「交付=行业表」严格口径应更低、若计入单卡层应更高——建议随第二节双口径注修正，不单独调分。
- 确认：四维给分三项吻合（78/30/65）与独立依据；goal-status.md 落位独立文件 + 每轮收敛必更 DoD 化（与 Codex 同议·双确认）；G4 轻量化方向（与 Codex「并入 C5 SOP」兼容：策略维度并入 G3 轮·工程维度并入 C5 收尾——收敛稿合并表述即可）。

### 对两组共同 —— 时间线信息差 1 条

两组评估落盘于 18:32，**dsh 专题底稿 18:48 才落盘**（`EMC-harness架构借鉴dsh_专题底稿_zcode-2026-08-16.md`·已入 todo）——故 Codex「无新增 G6——检查过双环境协同/PII/演示物料等候选」在其时点为真，但**该检查未覆盖 dsh 借鉴维度**。dsh 底稿提出 G6（session log 可重放）/G7（守卫管线化）/G8（契约全自动派生）+ 条件性 G9（CPD delegated turn）四候选。**收敛排序表须以「G1-G5 + G6-G9（待 dsh 专题会话收敛后并入）」全景呈现**，或显式注明 G6-G9 归 dsh 专题轮裁定——避免两张缺口清单并行漂移。

---

## 四 收敛建议清单（供 claude组 收敛稿）

1. **发起稿勘误采纳**：M1/M2 按第五节文本改；出向三层结构进收敛稿主表。
2. **F1 出向分**：记 ≈30%（区间 22-35%）+ 双口径注（第二节）。
3. **F2 排序**：三组实质一致——`G1（行业表格式级·开工点=CB-39 C 线完成后·2-3 天）> G5（插空·用户 gate）> G2（分层）`，采纳 Codex 时序条件 + claude 约束②（终归用户拍板·演示时间表可改执行序不改价值序）。
4. **F3 vitest 冲突裁决**：建议采 zcode 折衷（基建+首批纯函数 3-4 模块 0.5-1 天不等拆楼；巨石等拆楼）——留 claude组 终裁。
5. **F4**：双组一致（零工程投入+限定词+G5 作诚实载体）·直接定稿。
6. **F5**：双组一致（CLAUDE.md 出向节三态修正措辞采 Codex 版·goal-status.md 立项·首轮内容以四维+修正分起步）·直接定稿。
7. **G4 合并**：策略维度并入 G3 轮、工程维度并入 C5 收尾 SOP（两 组建议的合并表述）。
8. **G6-G9 并轨**：dsh 专题底稿四候选入排序表全景或注明归专题轮（第三节信息差条）。
9. **P1 顺手修采纳**：requirements.txt 补 openpyxl；JS 计数口径统一（excl vendor·一次性清点）。

---

## 五 发起稿勘误（原文→修正）

| 位置 | 原文 | 修正为 |
|---|---|---|
| §2.3 | 「**文件级导出（=0·立项风险点）**：`core/export.py` 实测无任何行业格式」 | 「**行业表格式导出（=0·立项风险点）**：文件级分两层——单卡 CSV 已落地（`export_outlet_card_csv`·export.py:200·F_005·前后端双实现+测试·CB-18 起）；**行业表格式（多行·体检表/项目统计模板）=0**（G1 对象·Codex 修正采）」 |
| §2.4 / G2 | 「前端 34 个 JS 文件零单测」 | 「前端 JS（excl vendor·计数口径待统一·45-52 间）零单测·契约层已有跨语言守卫兜底（P0-2·非全盲）」 |

> 勘误执行：本轮零实施·不回改发起稿原文（历史快照纪律）——修正文本以上表为准进收敛稿；goal-status.md 建立时直接用修正后口径。

---

> zcode组 · 2026-08-16 · 本文件为 CB-40 交叉挑战回应，交 claude组 收敛。只读·未 git 写·零实施。
