# 情绪热点图 P0/P1/P1.5 — 实施审计回应（Codex-GPT5）

> **审计方**：Codex（GPT-5）· 第三方独立审计（不做项目方决策背书）| **日期**：2026-08-05 | **性质**：CB 专题热点图实施审计
> **回应对象**：`docs/catch-ball/discuss/情绪热点图_实施审计发起_2026-08-05.md`（P0 命名 8 处 + P1 软分级 + P1.5 setTerrain）
> **参考**：P1 修正回应（Codex / glm）· 定稿计划 · retired.md
> **核实文件**：core/spatial_analysis.py（_classify_hotspot :110-128·threshold/soft_threshold :36-37·hotspot_tier :99·create_terrain_dem F_009 :1066-1155·注册 :1160-1168）· api/routes.py:351-390（/spatial/dem）· api/geo_routes.py:651-675（/geo/hotspot 未透传 threshold·legend 3 类）· frontend/js/{map,api,heatmap-tool,toolbox/hotspot-tool,ai_qa/tools}.js · docs/catch-ball/retired.md · docs/spec.md:115 · frontend/index.html

---

## 〇、一句话结论

**P0（命名定标）+ P1（软分级）实施质量良好可验收（小修项列 P0.5）；P1.5（setTerrain）不可验收——存在 1 个阻断级（B1：`generateTerrain3DForAI`/`closeTerrain3D` 全仓零调用（仅定义 heatmap-tool.js:929,956）——setTerrain 连续曲面**没有用户入口**，且 EMC 委托 tools.js:1314 的 density mode=terrain 仍调旧 `generateTerrainForAI`（fill-extrusion 千层饼），与 retired.md"该路径已退役"的实施声明矛盾）与 5 个警告级（Gi\* 符号层红蓝双色系与 KDE 极性色语义不一致·/geo/hotspot 不透传 threshold·legend 五档不匹配·单 tile DEM 缩放体验未实测·setTerrain 渲染行为无浏览器验收）；软分级本身落实到位（五档 + threshold/soft_threshold 参数化 + 弃 _CLS_POL + 显著性符号层，默认 soft=1.0 已让 max|Z|=1.26 的数据出现 tend_hot/tend_cold，**全 ns 显示问题已解**）。红线确认：不涉及四态出口 / finalStep D019 / diagnose ✓。**

---

## 一、逐焦点审计

### 焦点 1：P0 命名定标（8 处）

**判定：partial（核心改动完成·契约层与残留注释需补）**

证据与建议：
1. **8 处主改动核实到位**：spec.md:115 `spatial_hotspot` → 「代表地点（POI 名·生成器注入）」✓ · panel.js:1514「代表地点」✓ · grid-tool.js:27,32「显著聚集(Gi\*)」✓ · test-cases.js:264 语料「显著聚集」✓ · hotspot-tool.js:32 图层名「显著聚集点(Gi\*)」✓ · panel.js:1455 Overview 文案 ✓。
2. **契约层需同步（建议级）**：tool_contracts.py:261-262 hotspot 的 `voice`/`when` 仍写"Gi\* 热点/冷热点"（`name_cn='聚集识别(Gi*)'` 已中性）——paradigm.py:301/428 镜像同源；**文案应改"显著聚集"**，但 `triggers_str`/`triggers` 的"热点/聚集"触发词**保留**（用户自然输入词，兼容优先）。
3. **残留注释**：index.html:14（dormant deck「热点图」注释·第一轮第 4 轨未清）· index.html:1172（roadmap「Gi\* 热点」）· state.js:107（热度值语义注释"情绪热点"）· api/geo_routes.py:675（legend 仍"显著热点/显著冷点"）· grid-tool.js:3,22 注释——建议 P0.5 一次 commit 清理（纯文案零风险）。
4. outlet_kb case_library 多处"情绪热点"是 **KDE 密度语义**（非 Gi\* 工具名），出向文案低优先，可保留或按"情绪热度"统一（不阻断）。

### 焦点 2：P1 软分级（五档 + threshold 参数化 + hotspot_tier）

**判定：agree（落实到位）+ 3 处需修**

证据与建议：
1. **五档实现正确**：`_classify_hotspot`（core/spatial_analysis.py:110-128）hot/tend_hot/ns/tend_cold/cold 对称分档；`hot_spot_analysis` 新增 `threshold=1.96`/`soft_threshold=1.0` 参数（:36-37）并透传到分类（:97）；docstring 明示"~84% 置信·诚实标'倾向聚集'非显著"✓。
2. **全 ns 显示已解**：默认 `soft_threshold=1.0`，实测 max|Z|=1.26 > 1.0 → **tend_hot/tend_cold 必然出现**——前端能看到软分级热点（审计焦点 5 疑问的答案：能）。
3. **hotspot_tier vs hotspot 冗余但兼容**：后端 `hotspot` 现在也是五档值（:97 分类结果），`hotspot_tier` 只是别名（:99）——建议后续统一消费 `hotspot_tier`、`hotspot` 保留兼容；注意 **`hotspot` 字段语义从 3 类变 5 类**，旧消费方（TrackContext D_002 计数只算严格 hot/cold）需核对（建议级）。
4. **弃 _CLS_POL 解耦：部分到位**——hotspot-tool.js:17-22 不再映射 polarity ✓，map.js `colorMode='hotspot'` 显著性符号层 ✓（:604-610）。**但颜色是红/蓝双色系**（hot `#d9480f` 红 / cold `#1864ab` 蓝），注释自称"单色系"；且 **KDE terrain-9 正面=绿、Gi\* cold=蓝**——正面极性语义不一致（绿 vs 蓝），同图叠加混淆。建议：Gi\* 层纯单色系（橙系+大小/透明度分档）或与 KDE 同色系（正=绿/负=红）。（警告级）
5. **/geo/hotspot 不透传 threshold（警告级）**：HotspotRequest 仅 value_col+invert（api/geo_routes.py:651-653），`hot_spot_analysis(pts, value_col=..., invert=...)` 用默认阈值（:665）——后端已参数化、API 未透传，前端无法调阈值；建议补可选 `threshold/soft_threshold` 透传 + 测试。
6. **legend 五档不匹配（警告级）**：后端 legend 仍 3 类"显著热点/显著冷点/不显著"（:675）——tend 档无图例说明，且"热点"措辞与命名定标残留；建议补五档图例（显著/倾向/不显著对称）。

### 焦点 3：P1.5 setTerrain（连续曲面 + draping 隔离）

**判定：agree（后端与隔离实现正确）+ 1 个阻断级（无入口）+ 3 处待办**

证据与建议：
1. **terrarium 编码正确**：`create_terrain_dem`（F_009，core/spatial_analysis.py:1066-1155）`h = elev + 32768` → `R=h//256 / G=h%256 / B=frac×256`（:1138-1143），与 MapLibre terrarium 解码 `(R*256+G+B/256)-32768` 互逆；解码验证 0~500/峰 500 ✓；bounds 经 pyproj 转 WGS84（:1147-1152）✓；/spatial/dem 端点 header 返 X-DEM-Bounds/Size/HeightScale（api/routes.py:351-390）✓。
2. **draping 隔离实现到位**：不常驻（closeTerrain3D → setTerrain(null) ✓）· 与 3D 网格柱互斥（隐藏 fill-extrusion 柱 ✓，map.js:341-344）· sky 层防露底 ✓（:349-352）。
3. **🔴 阻断级：setTerrain 3D 无用户入口**——`generateTerrain3DForAI`（heatmap-tool.js:929）/`closeTerrain3D`（:956）全仓**零调用点**（rg 全 frontend 仅定义行）——dialog 未接、EMC 未接，用户点不到新连续曲面；且 **EMC 委托 tools.js:1314 的 density mode=terrain 仍调旧 `generateTerrainForAI`**（fill-extrusion 千层饼）——与 retired.md:16"generateTerrainForAI 3D 路径退役→generateTerrain3DForAI"**实施矛盾**。P1.5 验收前必须：dialog 3D 按钮接线 或 EMC 委托切换（建议两者都接，至少其一）+ 回归。
4. **注释矛盾（建议级）**：map.js:326-328 注释"天地图 raster 不被 drape"——MapLibre setTerrain 会把 raster 底图 drape 到地形上（这正是第一轮 P1.5 评估认同的"卫星影像铺在情绪曲面=张力增强"），注释与行为矛盾需澄清（非 bug）。
5. **单 tile DEM（警告级）**：`tiles:[url]` 无 `{z}/{x}/{y}` 模板 + tileSize 256（map.js:336-339）——所有瓦片请求同一 URL，bounds 限定范围，演示级可用但高 zoom 会模糊/平铺；需实测演示 zoom 范围，后续 z/x/y 瓦片化（P2 项）。
6. **F_009 ID 正确**：F_008 已被 membership 分组占用（注册 :1167），F_009 编号连续无跳号（:1166）✓；retired.md 登记 ✓（2D 等值线保留、KDE 栅格不退役）。

### 焦点 4：A/B 验证结论（U 形离散 vs 连续假设）

**判定：agree（glm 主因成立·长期方向正确）**

证据与建议：
1. **U 形离散 vs 连续正态假设不匹配 = 深层主因**：agree——我上轮实测 score 双峰（Very Positive 1068 + Very Negative 784 主导），glm 量化（0~0.2 占 40.6% + 0.9~1.0 占 29.7%）——score 是 5 级极性映射的离散产物，Gi\* "局部均值 vs 全局期望"的连续假设不成立，比"方差小"更本质（方差小是表面现象）。
2. **"网格化非解药"成立** ✓（单元修正只平滑多峰、不改变弱局部对比）。
3. **长期 KDE/DBSCAN 替代：agree 方向**——**按极性分组的 KDE 已有**（density 工具 polarity 参数）就是"负面热点图"的自然答案（负面密度面 = 用户要的"消极情绪聚集"）；DBSCAN 依赖 eps/min_samples（同样有尺度敏感性，需与演示尺度对齐）；建议作为 P2/P3 长期评估项，不进本轮。
4. 补充：Gi\* 在当前角色（显著性辅助层 + 软分级）下可继续服务"聚焦关注区"演示，与 KDE 主图分层不冲突——本轮不必换方法。

### 焦点 5：新问题/遗漏

**判定：4 项（1 阻断 + 3 警告）**

1. **🔴 B1：setTerrain 3D 无入口 + EMC 委托未切**（详见焦点 3-3）——P1.5 交付物不可用；retired.md 与 tools.js 实施不一致。
2. **/geo/hotspot threshold 不透传**（焦点 2-5）——默认 soft=1.0 已出 tend 档（显示问题已解），可调性缺口（警告级）。
3. **setTerrain 渲染行为无浏览器验收（警告级）**：pytest 293 passed + 括号平衡覆盖不了渲染——**必须浏览器/Playwright 验收**：天地图 drape 效果、与 3D 网格柱互斥、sky 无露底、单 tile 在演示 zoom 范围内的清晰度。
4. **Gi\* 符号层颜色语义（警告级）**：红/蓝双色系 vs KDE 绿=正面不一致（焦点 2-4）。
5. 建议级：legend 五档补全 + "热点"措辞清理（焦点 1-3/2-6）· hotspot 字段语义变更兼容说明（焦点 2-3）。

---

## 二、新问题风险清单

### 阻断级（P1.5 验收前必修）

| # | 风险 | 证据 | 建议 |
|---|---|---|---|
| B1 | **setTerrain 3D 无用户入口**：generateTerrain3DForAI/closeTerrain3D 全仓零调用；EMC density mode=terrain 仍调旧 generateTerrainForAI（千层饼） | heatmap-tool.js:929,956 仅定义无调用；tools.js:1314 旧路径；retired.md:16 声明退役 | dialog 3D 按钮接线 或 tools.js 委托切 generateTerrain3DForAI（建议两者都接）；回归 pytest + test-cases.js |

### 警告级（P0.5/P1 收尾处理）

| # | 风险 | 建议 |
|---|---|---|
| W1 | Gi\* 符号层红蓝双色系与 KDE 极性色语义不一致（KDE 正面=绿 vs Gi\* cold=蓝） | 纯单色系（橙系+大小/透明度分档）或与 KDE 同色系（正=绿/负=红） |
| W2 | /geo/hotspot 不透传 threshold/soft_threshold（后端有、API 无） | HotspotRequest 补可选参数透传 + 测试 |
| W3 | legend 仍 3 类 + "热点"措辞残留（与五档输出不匹配） | 补五档图例（显著/倾向/不显著对称）·措辞改"显著聚集" |
| W4 | 单 tile DEM（无 {z}/{x}/{y} 模板）高 zoom 模糊/平铺 | 实测演示 zoom 范围；P2 做 z/x/y 瓦片化 |
| W5 | setTerrain 渲染行为无浏览器验收（drape/互斥/sky/缩放） | Playwright + 肉眼验收三态（2D 地形/3D 地形/网格柱互斥） |
| W6 | 契约层 when/voice "Gi\* 热点/冷热点"文案残留（tool_contracts/paradigm 镜像） | 文案改"显著聚集"·triggers 触发词保留"热点" |

### 建议级

| # | 建议 |
|---|---|
| S1 | map.js:326-328"天地图不被 drape"注释澄清（实际被 drape=预期张力效果） |
| S2 | index.html:14（dormant deck 注释）/ :1172（roadmap）/ state.js:107 残留"热点"文案清理（P0.5 一次 commit） |
| S3 | hotspot 字段语义 3 类→五档变更：旧消费方（TrackContext D_002 计数/弹窗）核对兼容 |
| S4 | retired.md 与 tools.js 实施一致性核对（B1 修完同步更新） |

---

## 三、可否整体验收结论

**有条件验收（conditional pass）——P0 + P1 可验收，P1.5 不可验收。**

通过依据：P0 命名定标核心 8 处完成；P1 软分级落实到位（五档参数化 + 诚实标注 + 弃极性色 + 显著性符号层 + 默认 soft=1.0 已出 tend 档解全 ns 显示）；F_009 terrarium 编码/端点/隔离实现正确；红线零触碰（四态出口 / finalStep D019 / diagnose 均未涉及，确认 ✓）；pytest 293 passed 零回归。

**验收前置（1 项阻断 + 1 项警告）**：
1. **B1**：setTerrain 3D 至少接一个入口（dialog 3D 按钮或 tools.js EMC 委托切换，建议两者）+ 回归；修完 retired.md 与实现的矛盾；
2. **W5**：浏览器/Playwright 验收（天地图 drape、网格柱互斥、sky 无露底、单 tile 演示 zoom 清晰度）。

**P0.5 小修（不阻塞主线，一次 commit）**：W1（符号层色系统一）· W2（threshold 透传）· W3（五档 legend）· W6（契约层文案）· S1-S3（注释/兼容）。

**后续项**：W4（DEM 瓦片化）与长期 KDE/DBSCAN 评估（焦点 4）列入 P2/P3。

---

*Codex（GPT-5）· 热点图 P0/P1/P1.5 实施审计回应 · 2026-08-05*
*本文件为第三方独立审计，不对项目方决策背书；证据基于静态代码核实（path:line 可追溯）；渲染行为（setTerrain drape/缩放）标注为待浏览器验收项。*
