# PT-CB4 · 数据治理批 B2+B3 · 开工与拆解（zcode·2026-08-19）

> 主手：zcode。承接：PT-CB2（B1 批）全完工送审。本轮=PT 总计划阶段一第二批。spec 权威=`discuss/CB39-实施计划_Codex-2026-08-16.md` §四 B2/B3。
> 编号说明：原拆解预留号 PT-CB3 已让位于学习线（08-19 用户 PT 命名令·本批顺延为 PT-CB4）。
> 分支纪律：一切在 `EMC_harness_dsh`（main 冻结）。commit 前缀 `PT-CB4(T{n}):`。开工令：待用户（B1 审计回收不阻塞本轮开工·用户定）。

---

## 〇 本轮目标（一句话）

**同名数据新旧版本对账定谳 + 口径注册表与复核工具通电**——阶段一治理收官（B4 红线复核另批），蒸馏前置门就位。

## 一 任务表

| # | 任务 | 执行 | 白名单（允许触碰） | 验收 |
|---|---|---|---|---|
| **T1** | B2 版本对账裁决：①performance vs analysis 同名三对（占比表 676 vs 690 行需逐行 diff·其余两对按预判核验）②旧层双版本 md（安全/民生的`分析整合`vs`分析汇总`）→ superseded 标注。**四证据法**逐表：git log 提交时间 / 汇总 md v6 引用链 / 行数口径推断 / 生成时间与脚本。产出《对账裁决表》并入 `DATA/analysis/_总账.md`；输家 git mv → `_retired/` + retired.md 登记 | **zcode（判裁类不派发）** | `DATA/performance/`、`DATA/analysis/`（mv 操作）、`_总账.md`、`retired.md` | 三处交叉一致（总账/retired/manifest）；**实事求是条款：证据冲突挂起待用户裁决，不强行拍板** |
| **T2** | B3 口径注册表起步：落位 `docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md`（下划线=不进 RAG·治理台账）。种子内容四件：① page2 §3.3 现行口径；② 双口径 test case（board 全量 vs 三层筛选·同名不同值并存带口径标签）；③ 社区四口径对照（193/174/130/1682）；④ 作废数字清单（5,615/87.9%/港务 1,153 等·检索命中作废声明）。**吸收项**：登记字段扩展=能力目录字段（样例行/安全级别/口径链接·PT-CB3 学习线 B1 条）+ `usage_alt` 精确指路字段设计（T2 遗留·manifest 侧落位随下批注册） | **zcode 主笔（判裁内容不派发）** | `_口径注册表.md`（新） | 四件种子齐；作废数字与 `_总账.md`/T1 裁决表交叉一致 |
| **T3** | check_caliber 复核工具：读注册表旧值 → 扫描待蒸馏素材 → 命中即 fail 输出清单（人工裁决改写或加注「历史口径·已废弃」）。**取号：MOD_AIQA.F_020 起由主手注册**（连续不跳号）；实现主体清单明确 | **dsh（实现）+ zcode（取号/契约/验收）** | `tools/check_caliber.py`（新）、`core/tracker.py`（仅注册行） | 种子作废数字对既有素材零误报；工具测试入 pytest（门禁上浮注明）；F_020 注册合规 |
| 审计 | T1-T3 完工后随 B1 批审计余波一并送审（或独立轮·用户定） | 审计组 | — | 落盘 `PT-CB4-审计_*.md` |

**时序**：T1 先行（对账结果喂 T2 的作废数字清单与口径种子）→ T2 → T3（依赖 T2 的注册表格式）。T2/T3 尾部可交叉。

## 二 派发判据备注

- T1/T2 = 判裁类（版本定谳/口径认定），主手亲自——守「判裁不派发」纪律。
- T3 = 清单明确的工具实现（读表→扫描→报告），适合 dsh；但**取号（F_020）与函数契约由主手先行定稿**，dsh 在白名单内实现。派发单按升级版任务卡（业务背景+数据源两栏·PT-CB3 学习线 A3 吸收项首次实用）。
- B4（红线复核·PII 全量化等）留下一批，不混入。

## 三 学习线吸收项对照（PT-CB3 两轮裁决·本轮落地清单）

| 吸收项 | 落点位 |
|---|---|
| B1 能力目录字段（样例行/安全级别/口径链接） | T2 注册表字段设计 |
| usage_alt 精确指路（T2 遗留） | T2 设计定稿 → manifest 落位随下批注册任务 |
| A6 副作用必带补偿 | T3 工具若有缓存/句柄·同文件带清理函数 |
| B5 Service 命名空间（`em.*`） | 暂记档——MCP v1 立项时用（本轮不实施） |

## 四 纪律重申

main 冻结；dsh 白名单外禁碰；门禁 387+3（上浮须注明）；追踪 ID 连续不跳号（F_020 起主手注册）；**实事求是将款**（T1 证据冲突挂起待用户·不硬拍）；AGENTS v2.4 学习新规生效（报告必讨论必落盘）。

---
> dsh 派发单（T3·升级版任务卡）随开工令交付用户。T1 回收→T2→T3→送审→收敛→阶段一收官评估（B4+阶段二 C 线排期）。
