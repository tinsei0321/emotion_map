# 情绪地图 · 深读修复协助 — 发起（claude组 · CB-19 综合）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB 综合深读（两组详细报告·协助修复）
> **承接**：glm 发版回归 + claude 数据核对 → PRM-03/04 已根治（083b78d）+ P3-4 已实施（9680dcc）·**待两组详读复验 + 深挖修复未决项**（PRM-05/07 + Codex 补验挂起）
> **目的**：让两组（Codex 回归·glm 持续）对**未决修复项**产出详细深读报告（现状/根因/修复方案/风险），协助 claude 修复；对已修项复验。**全部只读·不 git**

---

## 〇、待协助问题总览

| # | 问题 | 状态 | 需要的深读产出 |
|---|---|---|---|
| 1 | **PRM-03/04** buffer radius（多工具错路由 merge/lookup_place 绕过 G5） | ✅ 已修（`083b78d`·未 push） | **复验**：G5 加 `_allToolCalls` 重写 + 去合并排除——完备？副作用？ |
| 2 | **PRM-05** zonal boundary derive 方差（`deriveAvailable` 词边界 `_validAfter` 偶发失败·Flash 方差） | 🔴 **未修·待深挖** | **深读**：`_validAfter` 词边界正则（tools.js:610-618）为何偶发失败？`_sepRe/_blockRe` 对「西陵区范围内按面聚合情绪统计」的判定？修复方案（正则放宽 / derive 兜底 / 契约强化）？ |
| 3 | **PRM-07** 法定功能区 dict 直通（LLM 直传「小溪塔」GeoJSON 绕过白名单） | 🔴 **未修·方案待收敛** | **深读**：前端 `zonal-tool.js` 用 `resolveBoundaryGeo`/`collectBoundarySources`（:7-8）——LLM 直传 GeoJSON 如何绕过 `FIXED_ADMIN_DISTRICTS` 白名单？修复方案（A 后端 resolve_boundary dict 词表校验 / B 前端来源标记 _source / C 不动）哪个符合 CB-14 + 不破「用户上传层不受限」？ |
| 4 | **P3-4** 地点联动（prop_cols 放行 + buildZonalFc 透传 + 出口卡动态标注） | ✅ 已实施（`9680dcc`·未 push） | **复验**：4 个改动（geo_routes / shared.js / build_outlet_schema / tools.js _fmtRow）——正确？副作用？ |
| 5 | **Codex 补验挂起**（W-2 threshold 全链 / S-3 result-struct 边界 / 三路径 DOM 断言前置评估） | ⏸️ 挂起（Codex 缺席时发起·回归后） | **深读**：W-2 转发链（hotspot-tool.js:15-16 + tools.js:1293）是否全闭合？S-3 纯函数直测边界是否充分？三路径 DOM 断言是否需前置？ |
| 6 | **glm 回归报告数据修正**（Run1/2 双 88%·fail 集修正·Run3 API 慢） | ✅ 已修正（`95123fd`） | **复核**：claude 数据核对（audit json vs report）是否准确？ |

---

## 一、每项深读要点

### 1. PRM-03/04 复验（已修·083b78d）

**修复**：harness.js:1529-1535 G5 修正加 `_allToolCalls` 重写（多工具错路由 merge/lookup_place → 强制 buffer 单 call）+ 去 `/(合并)/` 排除。

**深读要点**：
- G5 重写完备吗？有无其他「周边 Nm 情绪」绕过路径（chain / recover / autoExpand）？
- 去 `/(合并)/` 排除——问句真含「合并」且要 buffer 的场景？副作用？
- center 缺失走 ask_user（tools.js:1209 + harness.js:494）是否始终正确？

### 2. PRM-05 深挖（未修·核心）

**现状**：B3 Run 2「西陵区范围内按面聚合情绪统计」zonal `boundary=西陵区？` fail（计划1→未生成）。Run 1 过 → Flash 方差。

**已定位**：`deriveAvailable`（tools.js:617-640）匹配「西陵区」时 `_validAfter`（tools.js:610-618）——`_sepRe = /^[\s，。；、的区内范围边界]$/` 判分隔符·`_blockRe = /^(路|山|街|公园|广场|大道|车站|码头|桥)/` 判块词。问句「西陵区**范围内**按面聚合情绪统计及」——`_validAfter('西陵区', q)` 后 2 字是「范围」·`_sepRe` 测「范」（非分隔符）+ `_blockRe` 测「范围」（非块词）→ 应匹配。**为何 Run 2 偶发失败？** 可能是 FC 输出的 method 不是 zonal 导致 `tool !== 'zonal_stats'` 分支没进，或 `_allToolCalls` 多工具干扰。

**深读产出**：① 复现/推演 PRM-05 完整链路（FC 输出 → derive → 执行）② 定位偶发失败的真因 ③ 修复方案（正则放宽 / zonal 强制前置检查强化 / 契约 few-shot 强化）。

### 3. PRM-07 深挖（未修·方案收敛）

**现状**：B3 Run 1/2「小溪塔范围内按面聚合情绪统计」zonal fail——「区=GeoJSON{1}#小溪塔·法定功能区·应 request_upload」但实际产了层。

**已定位**：LLM 直传「小溪塔」GeoJSON 作为 boundary → 前端 `resolveBoundaryGeo`（zonal-tool.js:7）对 GeoJSON 直通不校验 → 绕过 `FIXED_ADMIN_DISTRICTS` 白名单 → 后端 `resolve_boundary` dict 直通执行。

**深读产出**：① 前端 `resolveBoundaryGeo`/`collectBoundarySources` 完整链路（zonal-tool.js + shared.js）——LLM 直传 GeoJSON 从哪进来 ② 方案 A（后端 resolve_boundary dict 词表校验）/ B（前端来源标记）/ C（不动）深入评估——哪个符合 CB-14「EMC 不硬猜」+ 不破「用户上传层不受限」 ③ 改动面 + 回归风险。

### 4. P3-4 复验（已实施·9680dcc）

**实施**：① geo_routes.py prop_cols 加 place_name/place_name_source/poi_names/poi_count ② shared.js buildZonalFc 焊地点字段 ③ build_outlet_schema.py 动态 limitations + micro 需求位置 POI 升级 ④ tools.js _fmtRow place_name 优先。

**深读要点**：4 改动正确性 + 副作用（prop_cols 扩列对消费方影响 / buildZonalFc 加列 / limitations 文案子串兼容 / _fmtRow 观察文案）。

### 5. Codex 补验挂起（W-2/S-3/DOM）

**现状**：Codex 缺席时挂起。回归后 Codex 回归——需补验：
- **W-2 threshold 转发全链**：hotspot-tool.js:15-16 + tools.js:1293 → /geo/hotspot → 后端——是否全闭合？有无别处缺口？
- **S-3 result-struct 边界**：纯函数直测（test_result_struct.py）是否充分？P0-3 三路径 DOM 断言是否需前置？
- **三路径 DOM 断言前置评估**：test_insight_cards_3path.py 已落地（浏览器抽验·注入数据后 PASS）——是否足够？还是要更早的单元级 DOM 断言？

**深读产出**：① W-2 全链核对（含 tool_contracts 契约 vs 运行时）② S-3 边界评估 ③ 三路径 DOM 断言充分性。

### 6. glm 回归报告数据复核

claude 已按 audit json + report 修正 glm 报告（Run1/2 双 88%·Run3 API 慢·fail 集修正）。请复核：数据核对是否准确？PRM-05 补入 fail 集判据 {PRM-03/04/05/07} 是否合理？

---

## 附 A：深读修复协助 prompt（发两组用）

```text
【CB-19 · 深读修复协助】情绪地图 — 未决修复项详读 + 已修项复验

你以第三方独立评估身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·工作区已同步）。可跑只读测试（pytest / browser e2e-seam 直测）作为证据。

背景：glm 发版回归 + claude 数据核对 → PRM-03/04 已根治（083b78d）+ P3-4 已实施（9680dcc）·**待两组详读复验 + 深挖未决项**（PRM-05/07 + Codex 补验挂起）。请对下 6 项产出**详细深读报告**（不是一句话结论·是逐行代码级分析）。

必读：
  docs/catch-ball/discuss/深读修复协助_发起_2026-08-08.md（总览 + 每项要点）
  docs/catch-ball/discuss/PRM修复_复验发起_2026-08-08.md（PRM-03/04 修复背景）
  docs/catch-ball/discuss/P3-4地点联动_复验发起_2026-08-08.md（P3-4 实施背景）
  docs/catch-ball/discuss/PRM_backlog预检_2026-08-08.md（PRM 预检·真根因）
  docs/catch-ball/discuss/发版回归_结果_glm组_2026-08-08.md（glm 回归·含 claude 数据修正）
按需核实：
  PRM-05：frontend/js/ai_qa/tools.js（:610-640 deriveAvailable/_validAfter）· harness.js（:1508-1527 zonal derive）
  PRM-07：frontend/js/toolbox/zonal-tool.js（:7-8 resolveBoundaryGeo/collectBoundarySources）· shared.js · core/geo_registry.py（:151-186 resolve_boundary）
  PRM-03/04：harness.js（:1529-1535 G5 修正·:1126 runAllToolCalls）· verify_prm03_fix.py
  P3-4：api/geo_routes.py（:376-377/:514-515 prop_cols）· build_outlet_schema.py（:329-340/:388-397）· shared.js（buildZonalFc）· tools.js（_fmtRow）
  Codex 补验：hotspot-tool.js（:15-16）· tools.js（:1293）· test_result_struct.py · test_insight_cards_3path.py

深读任务（6 项·每项给「现状确认 + 根因/方案 + 修复建议 + 风险」）：
1. PRM-03/04 复验（G5 加 _allToolCalls 重写完备？去合并排除副作用？center 缺 ask_user 正确？）
2. PRM-05 深挖（_validAfter 词边界为何偶发失败？完整链路推演 + 真因 + 修复方案）
3. PRM-07 方案收敛（resolveBoundaryGeo 链路 + 方案 A/B/C 评估 + 改动面）
4. P3-4 复验（4 改动正确性 + 副作用）
5. Codex 补验（W-2 全链 / S-3 边界 / 三路径 DOM 充分性）
6. glm 回归数据复核（claude 修正是否准确？PRM-05 补入判据是否合理？）

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（编排器确定性·Smart 两端 + Dumb 中间）。
承重红线：四态出口 / finalStep D019 / diagnose 永不动 / 追踪编号连续 / 「EMC 不硬猜」（CB-14）。
产出：docs/catch-ball/discuss/深读修复协助_回应_{你的组名}_2026-08-08.md
格式：〇 一句话结论 / 一~六 逐项深读（现状确认·根因·修复方案·风险）/ 七 综合修复建议清单（按优先级）。
报告中文为主，代码/路径保留英文。详读报告要**深**（逐行代码级）·不要泛泛。
```

---

*claude组 · 深读修复协助发起 · 2026-08-08*
*两组详读复验已修项 + 深挖未决项（PRM-05/07 + Codex 补验）·全部只读不 git*
