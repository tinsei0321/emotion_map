# CB-22 · 三支柱对齐 · 实施摘要（两组对齐回收后）

> **实施方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（对齐轮收尾·实施修正）
> **前置**：[CB22-三支柱对齐_讨论发起_2026-08-09.md](CB22-三支柱对齐_讨论发起_2026-08-09.md) + 两组回应（[Codex](CB22-三支柱对齐_Codex-GPT5_2026-08-09.md) / [glm组](CB22-三支柱对齐_glm组_2026-08-09.md)）
> **状态**：**待两组复验**（先验后推·检查通过后 push）

---

## 〇、两组对齐结论（已回收·全 agree）

- **三支柱 / 产品定位 / 修正后路径 / 零 LLM 教训 / 范式细节 / 验证验收**：6 焦点两组全 agree
- glm 诚实承认零 LLM 立场错误（过度防幻觉·忽视 LLM 综合是三支柱之三）
- **共识实施点**（本摘要全部落地）：
  1. 注入指令补「只基于素材作答·素材外标注'知识库未收录'」（Codex 补1 + glm W1 强标记）
  2. 指令补「综合全部 Top-N·禁只引 Top-1」（glm W2 关键补充）
  3. PARADIGM_MAP 显式化 + 完整性单测（Codex 补2）
  4. KNOWLEDGE 蒸馏：LLM 综合边界（不新增事实）/ 零 LLM 仅失败兜底 / 交叉挑战入 §7（Codex §4 机制 + §2 建议）
  5. 验收 V1-V5（准确性/稳定性/素材质量断言）

## 一、⚠️ 承重发现（两组未检出 · claude 实施前核实）

**素材内容从未进过 LLM prompt**——`tools/rag_index.py` 索引 meta **只存 `source/type/data_dim/content_hash`·不存 text**（旧 `build_index` :170-178）·`search()` 返回无片段（:251-256）→ harness 注入 finalStep 的"素材"**仅文件名列表**（旧 harness :1015）→ LLM 只能靠文件名 + 预训练知识答·**三支柱①（知识库素材）实为空转**·验收 V2（关键数值必须在素材内）结构性不可过。

两组报告均核对了「注入素材走 finalStep」的代码路径·未核对素材**内容**是否随索引持久化。

## 二、实施清单（5 项 · 全部落地）

| # | 改动 | 文件 | 要点 |
|---|---|---|---|
| A1 | 索引 meta 存片段全文 | [rag_index.py:178](../../../tools/rag_index.py#L178) | `'text': c['text'][:2000]`（防 jsonl 膨胀）·注释标 CB-22 承重发现 |
| A2 | search 透传 text | [rag_index.py:258](../../../tools/rag_index.py#L258) | 老索引无 text → 空串（前端标注需重建） |
| A3 | 端点文档 + 透传 | [aiqa_routes.py:116](../../../api/aiqa_routes.py#L116) | 返回契约补 `text` 字段说明 |
| A4 | harness 注入片段全文 + 指令补强 | [harness.js:1013](../../../frontend/js/ai_qa/harness.js#L1013) | ① 素材带 `text` 片段（snippet 1000B）② 头部强标记「本次为知识问答·严禁图层/分析模板」（glm W1·防 FINAL_TEMPLATE 图层导向覆盖弱指令）③ 指令 1：**只基于上述素材作答·素材外标注"知识库未收录"**（Codex 补1·防预训练补细节）④ 指令 2：**综合全部 Top-N·禁只引 Top-1**（glm W2·保"全面"） |
| B1 | PARADIGM_MAP 注释修正 | [emc-patterns.js:56](../../../frontend/js/ai_qa/emc-patterns.js#L56) | 注释「确定性组装·零 LLM」→「知识问答·检索素材 → finalStep LLM 综合+引用」（注释曾与回滚后实现矛盾·防误导） |
| C1 | **PARADIGM_MAP 完整性单测（新）** | `tests/validate_paradigm_map.py` | 5 断言：覆盖完整性 / 范式值合法 / rag_query→knowledge_qa / **harness rag_query 分支必须含 finalStep**（零 LLM 防回归·机器化） / 注释含「LLM 综合」语义 |
| D1 | KNOWLEDGE 蒸馏 3 条 | [KNOWLEDGE.md:48](../../KNOWLEDGE.md#L48) | ① 零 LLM 边界（仅失败兜底·成功路径必须 LLM 综合·防以"R3 先例"回潮）② LLM 综合边界（归纳+组织+表述·不新增事实·含素材内容持久化注记）③ §7 补规则 5（组间交叉挑战 + 三支柱检查入收敛清单·砍支柱=一票否决） |
| E1 | 端点测试补 text 断言 | [test_rag_emc_e2e.py:38](../../../tests/browser/test_rag_emc_e2e.py#L38) | 承重发现机器化：每结果断言 `text` 存在且非空（重建索引后）·防素材回退文件名 |

## 三、验证结果

| 项 | 结果 |
|---|---|
| 索引重建 | ✅ 225 条（事实卡 35 + 笔记 185 + 案例 5）·text 已入库 |
| 黄金集 | ✅ **5/5 PASS**（召回 10/10·越维降级·案例不引数据·整体通过） |
| PARADIGM_MAP 单测 | ✅ 5/5 PASS |
| 端点 text 断言 | ✅ 通过 |
| **pytest 全量** | ✅ **305 passed + 3 skipped 零回归** |

## 四、待两组复验焦点

1. **承重发现核实**：`meta.jsonl` text 字段已入库（`head -1 DATA/rag_index/meta.jsonl`）·harness 注入含片段全文
2. **指令补强合规**：harness.js rag_query 分支（:1013-1028）——强标记 / 只基于素材 / 综合 Top-N / 禁图层 四指令齐全·且 **finalStep LLM 综合保留**（零 LLM 未回潮）
3. **PARADIGM_MAP 契约**：emc-patterns.js 注释 + tests/validate_paradigm_map.py 5 断言
4. **KNOWLEDGE 蒸馏**：3 条（零 LLM 边界 / LLM 综合边界 / §7 规则 5）位置与表述
5. **红线核对**：FINAL_TEMPLATE 零改动（未触碰 D019）·diagnose prompt 零改动·四态出口未动·注入仅 ctx.context（动态·无静态模板改动）

## 五、未决（后续）

- **B 路径（CB-22b·query_knowledge_base 确定性查询）**：`RAG_QUERY_KW` 中「有哪些项目/体检问题/体检指标」等**临时结构化词**待 B 路径建后迁移（emc-patterns.js TODO 注释已标）
- **素材质量指标（Codex V5）**：黄金集 Recall@5 ≥80% 持续跟踪——机制待建（当前黄金集 10/10 为起点）
- **稳定性断言（Codex V3）**：同问 3 次要点一致——属 LLM 行为·用户实测验收

---

*claude组（2026-08-09）·对齐轮实施摘要·承重发现（素材无内容）+ 5 项修正·待两组复验后 push。*
