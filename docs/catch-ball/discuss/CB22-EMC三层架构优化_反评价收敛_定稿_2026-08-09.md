# CB-22 · EMC 三层架构优化 · 反评价收敛定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（架构级·用户主导·意图判断归位）
> **前置**：两组讨论回收（[Codex](CB22-EMC三层架构优化_Codex-GPT5_2026-08-09.md) + [glm组](CB22-EMC三层架构优化_glm组_2026-08-09.md)）·两组高度收敛零分歧
> **状态**：定稿·待实施（含红线豁免显式化 + eval 复采前提）

---

## 〇、两组讨论回收评估（高度收敛·零实质分歧）

| 焦点 | Codex | glm | claude 判定 |
|---|---|---|---|
| 0 三通道路由 | agree + 挑战1（三段式内容通道化）+ 挑战2（弹药选择·data_plan 覆盖判定） | agree（知识问答接入三段式） | ✅ 采纳 |
| 1 三层对齐 | agree + 概念库 vs MANIFESTO 分层（概念卡=检索副本·禁重述·防口径漂移） | agree + type='concept' | ✅ 采纳 |
| 2 概念库 | 范围 3 类 + 两级链路 | partial·范围收窄 3 类 + 粒度 type='concept' | ✅ 采纳 |
| 3 Q&A 归位 | agree 意图层 + 挑战 B′（选型非意图判断·不取） | agree 倾向 claude（glm 修正 B′） | ✅ 采纳（diagnose 加类·B′ 弃） |
| 4 意图判断强化 | agree + 判据要点 + 边界判据段 + 多轮补分支 + 红线豁免 3 条件 | agree（全对齐）+ 边界判据写段 | ✅ 采纳 |
| 5 防回归 | 触发正负例语义更新 + 枚举断言 + e2e | 枚举断言 + 合流路由 | ✅ 采纳 |

## 一、定稿架构（三通道路由 + 意图判断归位）

```
意图判断 agent（diagnoseStep·LLM）→ 路由三通道：
  知识问答（intent=knowledge_qa）→ rag_search 检索 → 注入素材 → finalStep LLM 综合
  GIS 工具（intent=gis_operation）→ 14 工具执行 → 图层
  情绪分析（intent=emotion_analysis）→ 情绪分析 → 图层/报告
统一表达层三段式（观点 + 方法/分级 + 对标）·内容通道化（各通道按自身产物填三段）
```

**知识问答通道**（本次修复核心）：diagnose 加 `knowledge_qa` 枚举（LLM 判）·短路降级加速器（明显命中直通）·**两者合流**统一范式（rag_search→注入→finalStep）。

## 二、实施清单（分阶段）

### P0 · 意图判断归位（治本·本次失败根因）

| # | 改动 | 文件 | 要点 |
|---|---|---|---|
| P0-1 | **红线豁免显式记录** | KNOWLEDGE §1 + cb-journal | 用户拍板（NL 意图判断必须 LLM）= 用户级豁免·记录「2026-08-09 豁免：diagnose 加 knowledge_qa 枚举」·豁免条件 3 条（增量不改存量 / eval 复采通过 / 静态断言守现有文本）·**eval 复采不通过则回滚** |
| P0-2 | **diagnose 加 knowledge_qa**（增量） | `ai_qa/prompts.py` :200 | intent 枚举加 `knowledge_qa`·**不改不删现有三值判据文本**·新增 knowledge_qa 判定段（数据/事实/清单类问·含地名+领域词·非分析非概念）·**概念问 vs 知识问边界**（general=定义解释「什么是更新单元」·knowledge_qa=数据清单「宜昌有哪些更新单元」）·多轮续作补「穿插知识问例外」（分析中问知识→判 knowledge_qa·非续作） |
| P0-3 | **先扩 eval**（豁免前提） | `tests/` | diagnose eval 扩 knowledge_qa 断言（Flash 能判知识问答）·现有三值零回归断言 |
| P0-4 | **_quickIntent 降级加速器** | `frontend/js/ai_qa/harness.js` :60-82 | 只做明显命中直通（CONCEPT/INVENTORY/GREETING + RAG 精确命中）·**漏网（含词序变体）全落 diagnose 由 LLM 判**·非判断主体 |
| P0-5 | **harness 知识问答路由**（合流） | `frontend/js/ai_qa/harness.js` | diagnose.intent==='knowledge_qa' → 调 rag_search → 注入素材 → finalStep LLM 综合（复用 rag_query 短路分支·抽 `_assembleKnowledgeQA` 合流）·data_plan 覆盖「知识库覆盖判定」（素材够不够→request_upload/降级·Codex 挑战 2） |

### P1 · 概念库（数据层补全）

| # | 改动 | 文件 | 要点 |
|---|---|---|---|
| P1-1 | **概念类素材 3 类**（产品定位/方法论/边界认知·每条 ≤200 字·摘抄+引用·**禁 LLM 重述生成·防口径漂移**） | `ai_qa/outlet_kb/` 新 concepts 源 | type='concept'·与 fact/note 区分 |
| P1-2 | **重建索引** | `tools/rag_index.py` | 概念类入 RAG（225 → ~240 条） |
| P1-3 | **概念问答两级链路** | harness | 明显概念（general→concept 模板直答·保留快速路径）+ 复杂/边界概念问 → knowledge_qa（RAG 检索概念卡 + LLM 综合引用） |

### P2 · 防回归机器化

| # | 改动 | 要点 |
|---|---|---|
| P2-1 | 触发正负例**语义更新** | 正例（明显命中直通）·负例含「宜昌市城市更新的项目有哪些」（降级后**期望落 diagnose**·非短路失败·测试语义随架构变） |
| P2-2 | diagnose 枚举断言（新） | intent 含 4 值 + **现有三值判据文本未删**（增量断言·防重构删类） |
| P2-3 | 知识问答 e2e（e2e-seam） | 模拟 diagnose 卡 intent=knowledge_qa → harness 路由 → 注入 → finalStep 链路断言（不依赖真实 LLM） |

### P3 · 43/12 数字出处（独立小瑕疵·照做）

| # | 改动 | 要点 |
|---|---|---|
| P3-1 | URP-P01 detail 做厚 | 55 构成 + 43 完整社区正确语义（源自 00-02:30-37）·关键词补「完整社区 重点片区 城中村」 |
| P3-2 | 知识问答指令出处锚定 | harness 注入指令 1 补「逐数字锚定 [来源]·禁自创拆分」 |
| P3-3 | 素材语义断言 | 检索「43 个完整社区」命中 URP-P01 |

## 三、验证

1. **eval 复采**（P0 前提）：knowledge_qa 识别率 + 现有三值零回归 + 模板命中 gate 0.6 仍守
2. node 触发回归（加速器正例/负例·漏网落 diagnose）
3. `py -m pytest tests/ -q` 全量零回归 + validate 轨 + 新断言
4. 重建索引（概念类 + URP-P01）
5. **用户复测**：「宜昌市城市更新的项目有哪些？」→ 落 diagnose → LLM 判 knowledge_qa → RAG 检索 → LLM 综合带来源·不图层；「什么是更新单元」→ general concept 直答（负例不误判）

## 四、红线核对

| 红线 | 结论 |
|---|---|
| diagnose 永不动 | ⚠️ **用户豁免**（2026-08-09·意图判断归 LLM）·显式记录 + 增量不改存量 + eval 复采前提·不通过回滚 |
| FINAL_TEMPLATE / D019 | 不动（知识问答走 ctx.context 注入） |
| 四态出口 | knowledge_qa 走 EXIT_CONCEPT（LLM 综合·非新态） |
| 三支柱 | ① 概念库补数据 ② 意图判断归位强化架构 ③ LLM 综合不变 |

## 五、流程（下一步）

定稿 → 实施（P0 先行·含红线豁免记录 + 先扩 eval）→ 两组复验（先验后推）→ 用户复测 → push

---

*claude组（2026-08-09）·三层架构优化反评价收敛·两组零分歧·P0 意图判断归位 + P1 概念库 + P2 防回归 + P3 出处·待实施。*
