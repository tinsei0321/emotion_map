# 情绪热点图重做 · 定稿执行计划（待用户拍板）

> **制定方**：claude组（收敛两轮讨论）| **日期**：2026-08-05 | **状态**：待用户拍板 → 拍板后执行
> **三轮文档链**：
> ① 发起 `情绪热点图_热力vs热点_专题讨论_2026-08-05.md`（概念基座 + 4 焦点）
> ② 第一轮回应 `情绪热点图回应_{glm组|Codex-GPT5}_2026-08-05.md`
> ③ 反评价+焦点5 `情绪热点图_第二轮_claude组反评价与3D焦点_2026-08-05.md`
> ④ 第二轮回应 `情绪热点图_第二轮回应_{glm组|Codex-GPT5}_2026-08-05.md`

---

## 〇、Context

用户宣布「情绪热点图需要重做」——当前逻辑与效果都不对。三轮 CB 讨论（claude组 发起 + 两组独立评估）已收敛出定稿方向：**正名 KDE 热力面为主图 + Gi\* 改「显著聚集检验」并网格化修正 + 3D 形式定案 MapLibre setTerrain 连续三角网地形（弃 fill-extrusion 千层饼）**。用户定调：热点图（Gi\*）= 2D 即可；热力图 2D+3D 形式待本计划拍板；今天定稿。

---

## 一、定稿决策（三方共识 · 拍板即生效）

| # | 决策 | 内容 | 共识来源 |
|---|---|---|---|
| **D1** | 命名定标 | 「情绪热点图」用户可见词 = KDE 连续密度面（2D 热力 + 3D 地形统一命名族）；Gi\* 改名「显著聚集点(Gi\*)」，**切断"热点=热力"混淆**；`spatial_hotspot` 字段显示名 →「代表地点」；dormant deck.gl「热点图」清理/标废弃 | 两组一轮 agree + Codex 5 义扩围 |
| **D2** | KDE 主图 | KDE 主图不重写（`create_terrain_mesh` F_007 方向正确）；2D 热力保留（MapLibre 原生 heatmap·radius 屏幕尺度·快）；3D 作张力增强，独立验收闸门 | 两组共识 |
| **D3** | 3D 形式 = a | **MapLibre `setTerrain` + RGB DEM**（后端 Hs 栅格 → Mapbox RGB 编码 PNG + tilejson → raster-dem source + setTerrain 三角网真曲面）·exaggeration 可调；**弃 b（deck.gl TerrainLayer：本仓 GridLayer extruded 已踩坑不渲染·map.js:791-794）与 c（fill-extrusion 改造：逐 feature 固定高度=伪曲面，7 层环即"千层饼"根因·map.js:636）** | 两组二轮全选 a |
| **D4** | Gi\* 网格化 | 输入从逐点改**网格聚合**：复用 `create_square_grid`（F_006）→ `score_mean`（主）/`polarity_index`（备）作 value_col；**500m 默认** + `point_count>=3` 过滤（空格不入邻接矩阵非填 0）+ min cells≥30（不足自动降档 300m）；KNN k=8 保留（距离带 P2 实验）；输出补 **tier 90/95/99%** 置信度分档（保留 Gi_Z/P 原值）；legend 补"hot=负面显著聚集（score invert）" | 两轮收敛 + 二轮复核 |
| **D5** | Gi\* 视觉去极性色 | hot/cold 改**显著性视觉通道**（符号大小/描边/透明度按 tier 分档·单色系），不复用 5 色极性色带（`hotspot-tool.js:16 _CLS_POL` 弃）；KDE 层独占极性色（terrain-9） | 两组共识（焦点 3） |
| **D6** | 契约/测试护栏 | 不动 `tool_contracts.py`/`paradigm.py` 契约结构，只改用户可见命名 + 文案；**补 F_001/F_007/DEM 端点单元测试**（现状无护栏）；tools.js 过期注释清理（顺带核实 density 2D 路径确无后端依赖） | Codex 遗漏补充 |
| **D7** | 退役与保留 | fill-extrusion 等值线环地形渲染退役（retired.md 登记·替代 setTerrain）；**F_007 保留**（函数改实现：提环 → 暴露 Hs 栅格 + DEM 编码，track ID 不变·铁律 10）；KDE 栅格管线不退役（成 DEM 源头） | glm 二轮 1.5 + 3.4-3 |

---

## 二、执行范围（P0 / P1 / P2）

### P0 · 命名定标（今天·2-4h·低风险·纯文案/契约）

| 任务 | 文件 | 说明 |
|---|---|---|
| Gi\* 改名「显著聚集点(Gi\*)」 | `frontend/js/toolbox/hotspot-tool.js:32` · `frontend/js/grid-tool.js:27` | 图层名 + 占位卡 label（dev:true 保留） |
| KDE 命名族定标 | `frontend/index.html:144-149` 工具行 tip | "情绪热力图（2D）/ 情绪热力图·地形（3D）"，去掉"情绪地形"歧义 |
| `spatial_hotspot` → 「代表地点」 | `frontend/js/ai_qa/panel.js:1514` · `docs/spec.md:115` | 显示名 + 描述（全仓仅此两处，Codex 核实） |
| 契约文案同步 | `ai_qa/tool_contracts.py`（name_cn/when/examples 里"热点"表述）→ paradigm/prompts 自动派生 → `frontend/js/ai_qa/stages.js` SKILL_DEFS 镜像 | 跑 validate_skill_params 守护 |
| 测试语料同步 | `frontend/js/test-cases.js:264` 含"情绪热点聚集在哪" | 随改名同步，跑 B3/断言回归 |
| dormant deck.gl 清理 | `frontend/js/map.js:742-784` addHotpointLayer | 标废弃/删除（无调用方） |

### P0-前置 · Gi\* A/B 验证（今天·S·**P1 必须前置**，glm 二轮升格）

- 真实 L2 宜昌数据：**逐点 Gi\*（现状）vs 500m 网格 Gi\***，对比显著格数量/空间形态/z 分布。
- **判据**：网格版显著格与逐点版分布一致（仅边界规整）→ 单元语义修正被证伪，回查 value_col 选择；分布显著不同且更符业务直觉 → 单元修正成立，继续 P1。
- 用脚本跑（复用 `hot_spot_analysis` + `create_square_grid`），不出图，先出统计对比。

### P1 · Gi\* 网格化 + 显著性符号层（本周·1-2 天·中风险·统计口径改变）

| 任务 | 文件 | 说明 |
|---|---|---|
| 新 grid-Gi\* 函数 | `core/spatial_analysis.py`（新增 + `register_track_id` 连续编号）· `api/geo_routes.py:657` | 500m 默认+降档、point_count≥3、min cells≥30、score_mean、tier 90/95/99 |
| 前端显著性符号层 | `frontend/js/toolbox/hotspot-tool.js`（弃 `_CLS_POL`）· tip-popup/图例 | 符号大小/描边/透明度按 tier 分档；legend 明示 invert 语义 |
| 接入「多维归因分析」工具行 | `frontend/index.html:179-183` | Gi\* 归宿（非新建按钮）；grid-tool 占位卡去 dev 接线 |
| 统一三处默认粒度 | `core/spatial_analysis.py:728`(200) · `grid-tool.js:20`(400) · `tool_contracts.py:46`(600) | 收敛到尺度表：社区 250 / 区 500 / 主城 1000，默认 500 |
| 测试护栏 | `tests/test_spatial_analysis.py` 补 F_001/F_007/grid-Gi\* | 合成数据 + 断言 hot/cold 分布 |

### P1.5 · 3D 定案 a：MapLibre setTerrain + RGB DEM（独立验收闸门·2-3 天·中风险）

| 任务 | 文件 | 说明 |
|---|---|---|
| 后端 Hs 栅格暴露 + DEM 编码 | `core/spatial_analysis.py`（重构 F_007 输出栅格）· `api/routes.py` 新 `/spatial/dem` | Mapbox RGB 编码（numpy 位运算 + matplotlib imsave，≈20 行·**不引 rasterio/GDAL**）；PNG + tilejson（bounds 4326） |
| 依赖 | `requirements.txt` | matplotlib 已有；Pillow 或纯 Python PNG 编码器二选一（glm：matplotlib imsave 即可，可能零新增） |
| 前端 terrain 接线 | `frontend/js/api.js`（runDem）· `frontend/js/map.js`（raster-dem source + setTerrain + exaggeration 滑块 5-20）· `frontend/js/heatmap-tool.js`（3D 入口随本 P1.5 合并打开，**不做两次接线**——glm 4.2-1） | **draping 隔离（成败关键·glm 1.3）**：setTerrain 不常驻（显式 3D 模式才开）、3D 地形与 3D 网格柱互斥显隐、平面底图 + KDE DEM 唯一 terrain source（天地图 raster 不被 drape）、补 sky 层防露底（先例 revision-log 06-30） |
| compare 卷帘同步 | `frontend/js/map.js:120-164` | mapA/mapB 同步 setTerrain/removeTerrain |
| 退役登记 | `docs/catch-ball/retired.md` | fill-extrusion 等值线环地形（map.js:631-641 terrain 分支）· 原因"千层饼非连续曲面" |
| 测试 | `tests/`（DEM 端点 PNG 魔数/尺寸/tilejson）+ Playwright（setTerrain 加载·grid-3D 互斥·compare 一致·无露底） | Codex 补充项 |

### P2 · 后续（1-1.5 天·低风险）

- **距离带权重实验**（threshold=格对角线×1.5~2·transform='R'·岛屿 ns 兜底·A/B 对比 KNN）
- **2D/3D 共源**（glm 倾向统一到 Hs 栅格一条链；时间紧则仅文档化"两者参数不一致"——Codex 保留 2D 原生 heatmap 性能；**默认不统一，标注不一致**）
- tools.js:249-250,1302 过期注释清理（顺带核实 density 2D 无后端依赖）
- 2D/3D 参数差异文档化

---

## 三、执行顺序与闸门

```
P0 命名定标（今天·全量）
   ↓
P0-前置 Gi* A/B 验证（今天·闸门：分布一致性判据）
   ├─ 通过 → P1 Gi* 网格化
   └─ 证伪 → 回查 value_col / 权重，P1 调整后重验
   ↓
P1 Gi* 网格化 + 显著性符号层（本周）
   ↓
P1.5 setTerrain 3D（独立闸门：draping PoC 先行——
     天地图底图正常 / grid-3D 不漂浮穿模 / 2D 图层不扭曲，
     不过则回退 c fill-extrusion 细化兜底）
   ↓
P2 距离带实验 + 清理
```

**关键闸门**（不过则回退，不硬推）：
1. Gi\* A/B 前置（glm 升格必须项）
2. setTerrain draping PoC（glm 1.3：a 成败关键，不过回退 c 兜底）
3. 3D 独立验收：连续曲面 + 起伏张力 + 无露底 + grid-3D 互斥 + compare 一致——过闸前不合并主演示（Codex）

---

## 四、待用户拍板的分歧点

| # | 分歧 | 选项 | claude组 收敛建议 |
|---|---|---|---|
| **P-1** | **3D 形式确认** | a. setTerrain DEM（两组全选）/ c. fill-extrusion 细化兜底 | **选 a**（真连续曲面 + 官方原生 + 不引第二渲染栈），c 仅作 PoC 不过的兜底 |
| **P-2** | 2D/3D 是否共源 | glm：统一到 Hs 栅格一条链 / Codex：保留 2D 原生 heatmap | **默认不统一**（2D 保性能快、3D 先上 setTerrain；共源作 P2·文档标注不一致）——今天定稿优先稳 |
| **P-3** | 今天执行范围 | A. 仅 P0 命名定标 / B. P0 + A/B 验证 / C. P0 + A/B + P1 开工 | **B**（今天锁命名 + A/B 前置闸门；P1/P1.5 拍板后排期）——3D 需独立验收闸门，不宜今天硬塞 |
| **P-4** | 3D 入口时机 | glm：随 P1.5 setTerrain 合并开（免双重接线）/ 先开现状 3D | **随 P1.5 合并开**（P0 只开 2D 入口） |

---

## 五、验证方案（执行后）

1. `py -m pytest tests/ -q` 全过（新增 F_001/F_007/grid-Gi\*/DEM 端点用例纳入）
2. Gi\* A/B 统计对比表（逐点 vs 500m 网格·显著格数量/形态/z 分布）
3. 命名定标后跑 `validate_skill_params` + `test-cases.js` 回归（语料/契约同步无遗漏）
4. 3D 验收闸门（Playwright + 肉眼）：连续曲面、起伏张力、无露底、grid-3D 互斥、compare 一致
5. `docs/catch-ball/retired.md` 登记 fill-extrusion 地形退役
6. todo / revision-log §5 同步

---

*claude组 · 情绪热点图定稿执行计划 · 2026-08-05 · 基于三轮讨论（发起 + 两组各两轮回应）*
