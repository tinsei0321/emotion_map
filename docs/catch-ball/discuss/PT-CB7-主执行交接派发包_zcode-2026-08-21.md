# PT-CB7 · 主执行交接派发包（审计先行 → Qoder 主执行·zcode 主手签发·2026-08-21）

> 角色变更（用户令）：**Qoder = PT-CB7 本批主执行**（此前为审计方·本批获执行权）；**dsh 转协助/待命**（其未提交 WIP 已由主手收档·见下）；zcode = 主手（设计已完·负责回收验收与裁决）；Codex+claude = 终审。
> 用户意图：验证 Qoder 主执行效果（含执行风格 vs dsh 的对照观察——回收时主手将评估）。

---

## 〇 开工必读（按序）

1. `docs/catch-ball/discuss/PT-CB7-稳定与灵魂批_任务书_zcode-2026-08-21.md`——**你的执行规格**（T1-T6 函数级 + 增补 T7-T9/F1-F4）；
2. `docs/catch-ball/discuss/12345_800m方格_DSH任务双报告合并讨论纪要_zcode-2026-08-21.md`——设计总纲（「让不需要思维的任务不进思维循环」+双模预设三升级信号）+ 两份评估（zcode 总评估/你的 CB42）——T7 的依据；
3. `docs/catch-ball/discuss/EMC-dsh架构评议与外脑思路_评估讨论发起_Qoder-2026-08-21.md`——你自己 08-21 的评议（R1-R6 已在计划内吸收）；
4. git 状态知会：分支 `EMC_harness_dsh`（main 冻结）；最新提交 `1c2ce974`=主手刚收档的 **dsh 在途 WIP**（render_client 新增 boundary_fill_v1 scheme + debug-memory R14=better-sidebar 拦截 openPath 根因+直连 host RPC 解法）——**这两个改动未经任何人验收·你执行相关任务（T1/T2/T3）时先复核它们**（R14 直接是 T3 的已知障碍与解法记录·boundary_fill_v1 是否该保留/是否与 T1 冲突由你判并落审计意见）。

## 一 第一步：审计先行（零实施·先落盘再动手）

对你将执行的计划（任务书 T1-T9 全文）给出审计意见并落盘 `docs/catch-ball/discuss/PT-CB7-主执行前审计_Qoder-2026-08-21.md`，覆盖：
1. 任务切分/依赖/白名单有无漏洞或遗漏（尤其 T1 图层清理与 boundary_fill_v1 WIP 的关系、T3 内嵌与 R14 解法的一致性、T7 双模预设与 dsh 冻结期的兼容性）；
2. 验收锚是否可执行（浏览器实测清单/T7 的 ≤2 分钟目标）；
3. 任何你认为该修正的顺序或内容（**审计意见随执行生效**——修正项写进执行记录即可，无需等主手二次确认，但超白名单/超范围的修正须列「待主手裁决」）。
审计文档完成后**立即开始执行**，不必等主手回复（用户令「审计并主执行」=串行一体）。

## 二 主执行任务清单（T1-T9+F1-F4·规格与白名单全在任务书）

| T | 任务 | 环境 |
|---|---|---|
| T1 | 图层残留自清理（[dsh] 前缀层·新 spec 前清空） | EMC 仓 |
| T2 | better-sidebar terminal chunk 修复决策树（含 R14 复核） | dsh 环境 |
| T3 | EMC 入口内嵌 registerTab 为主+就绪 gate（含 R14 直连 host RPC 方案复核） | dsh 环境（dsh-emc-entry） |
| T4 | 思维过长计时取证（两 profile 分解报告·只取证不改） | dsh 环境 |
| T5 | 阶段二灵魂：EMC 身份卡+人设 system prompt+RAG 重建 | EMC 仓+dsh 环境 |
| T6 | MCP 八工具描述紧凑化（≤~200 字符/件·零红线·caliber 不变） | EMC 仓 |
| T7 | 双模预设：emc-analysis（默认·flash+low/medium+工具子集+step 预算≤15）/emc-research（max 保留） | dsh 环境 |
| T8 | 800m 脚本参数化（grid_size/source/region_caliber 三参数·输出口径对照段）+入库 | EMC 仓 |
| T9 | 交付模板「口径对照」段固化（并入 T5 同批纪律） | EMC 仓 |
| F1-F4 | effort 覆写支持/context 压缩机制等四问调研 | dsh 环境 |

## 三 执行纪律

1. commit 规范 `PT-CB7(T{n}):`·**显式路径提交·禁 add -A**（本仓两次误扫教训）；每任务一 commit+push origin `EMC_harness_dsh`；
2. 白名单=任务书逐任务列出的文件；超范围改动列「待主手裁决」勿自行扩大；
3. EMC 仓外（dsh 环境）改动只落执行记录（含 file:line/时间戳证据）；**dsh 上游冻结期内禁 merge**；
4. 编码铁律：禁 emoji/_safe_print/追踪 ID 连续（新增先查注册表最大号）/A9 禁宽 except 静默；
5. 执行记录落盘 `docs/catch-ball/discuss/PT-CB7-主执行记录_Qoder-2026-08-21.md`（每任务销号+证据+待裁决项）；
6. 全程只读不改：main、QA 主链路、diagnose prompt、`_口径注册表.md` 卡 ID 体系。

## 四 验收与回收

- 完成告知用户 → **zcode 主手回收**：逐任务验收+浏览器实测清单核（用户参与）+ 送终审（Codex+claude）+ **Qoder 执行效果评估**（对照 dsh 前几批执行的效率/纪律/白名单合规——即用户「看看效果怎么样」的答案）。
- 特别验收锚：T7 后 `dsh --profile emc-analysis` 跑一次简单聚合任务，wall-time ≤2 分钟（同"最密集 10 社区"类）。

---

> zcode 主手 · 2026-08-21 · 设计完成·审计+执行交 Qoder
