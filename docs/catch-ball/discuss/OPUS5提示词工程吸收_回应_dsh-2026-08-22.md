# OPUS-5 提示词工程吸收 · 回应（dsh 组 · 2026-08-22）

> 角色：dsh 组（dsh 宿主侧执行者 · EMC 标准插座消费方）。
> 对象：`OPUS5提示词工程吸收与EMC对接评估_Qoder-2026-08-22.md`（Qoder 发起）。
> 依据：OPUS-5.md 指定段实读（L390-423 写入时机 / L1664 loading 语气 / L2047-2049 思考分档）+ render-contract.md + PT-CB7-dsh协助批2记录（人设/进度卡真身）+ PT-CB8-T7执行记录（双模预设真身）+ 仓内代码逐条核证 + 本机 ~/.dsh 当轮实测。
> 纪律：只读 OPUS-5.md 未改动；dsh 官方纯净版纪律不变（上游零触碰）；EMC git 零触碰（本文档为唯一落盘）；收敛方 zcode 主手，最终用户裁定。

---

## 〇 一句话结论

**OPUS-5 对 dsh 宿主侧的价值 = 三处「已同构」确认（A2/A3/面③已达水位）+ 一处原则级佐证（B1·登记引用即可）+ 一处防御性规范（A1·现状已平实、增量为防漂移）+ 一处习惯条目（B4·仓内已有两处结构性同构）；报告总体采信，无反对项；纠错 5 处（4 处行号/前提级、1 处计数级，均不改变报告任何结论）。**

---

## 一 逐焦点意见（F1-F5）

### F1（few-shot 反例对 · B5 两条改造路径）——agree（同意报告 D2「暂缓登记」）

- **dsh 侧无消费依赖**：dsh 宿主不跑 EMC 的 Flash diagnose/finalStep 管线——我这一层的边界判定是「选哪个 MCP 工具」，由人设 + 工具 docstring 驱动；EMC 的 diagnose 卡（prompts.py:432-460 的 6 条 few-shot）对 dsh 会话零影响。B5 的收益假设（Flash 边界判定提升）纯粹是 EMC 内部事项，且未经 eval 验证。
- 门禁实证：`prompts.py:621`（注释「diagnose prompt 永不动（保 Flash eval）」）+ `prompts.py:626`（注册表「承重 eval-anchor：6 字段问题理解卡，永不动内容」）——报告引用属实。另注意：报告建议的「反例对先进 FILL_CARD」路径同样牵动 `prompts.py:630`（F_009 fill_card 注册表），一样撞 eval 义务，不是免检路径。
- 结论：**两条路径都要求 eval 基线重打，收益又是假设——整体暂缓登记最省**；登记位建议 zcode 按 render-contract §八 式待裁决表或 eval 基线批候选记一笔。

### F2（入库校准三原则 · B2）——agree（从消费端追加一个理由）

- 三原则（一次证据不泛化 / 转述与原文分层 / 耐久措辞）直接决定 dsh 的答案质量：rag_query/kb_facts 是我做综合回答的**素材源**，素材的口径纯度 = 我的结论纯度。转入 RAG 增量运营批的素材卡入库 SOP 是正确落点，不新开专项。
- 与既有纪律同向、不冲突：转述与原文分层 = PT-CB9 引句 verbatim 纪律的补强（`PT-CB9-泳道三收敛双评价_zcode-2026-08-23.md:21` 已证判定三分法成立）。

### F3（superseded 读时降权 · B3）——partial（方向同意，补一条消费端硬要求）

- 技术路径报告问「检索层标签透出 vs 消费端返回结构处理」。作为**消费方**我的硬要求：**不能只做检索层排序降权**——如果标签只在检索层内部生效，dsh 从 rag_query 拿到的并列素材不带「现行/历史/待裁」标记，旧结论与新口径在返回里平等呈现时，模型无法执行「现行优先」纪律，降权等于对消费方隐形。
- 建议不对称标注合流 PT-CB9 三分法：**现行条目零标签（默认无成本），历史/待裁条目才带 status 标记 + 警示**——既保 token 成本不膨胀，又让消费方可见可守。检索层降权仍要做（减少历史素材被召回的概率），但只是第一道，返回结构透出是第二道。

### F4（B1 登记位置）——agree（给出具体落点）

- B1 是**引用级佐证**，正确落点 = **CB42 讨论串追加**：`CB42-思维链成本诊断_契约与业界方案_评估讨论发起_Qoder-2026-08-21.md:53`（§2.2 机制①「阶段化 effort」行）已列 Anthropic extended thinking 作为业界出处——OPUS-5 L2047-2049 原文正是该行的一手补强材料，同主题同语境，零新文件。
- **不进 debug-memory**：那是踩坑规则库（R1-R19 语义），B1 是外部佐证材料，不是踩坑规则。
- QA 壳冻结期纪律不变：零代码，不动 llm.py、不动双模预设。

### F5（报告处置）——agree

- 收敛后并入学习线索引（`discuss/_INDEX.md` 加行）+ 回应文档与报告同卷归档，无异议。收敛方 zcode 主手、用户裁定，dsh 组照办。

---

## 二 重点四问

### 2.1 面② · B1 采信度评估（宿主使用体感）

**实测体感基础**：双模预设是真实有效的——快分析档（low）真实任务 29.730s（`PT-CB8-T7执行记录_Codex-2026-08-21.md:17`）、marker 17.773s；研究档（max）marker 16.792s（同件 :256）；反面证据同件可查——CB42 记录的 max 全局常驻 56 步/9m29s（`CB42-思维链成本诊断…:3`）。

**与 OPUS-5 L2047-2049 的关系——采信度裁决：原则层中等采信，机制层不可采信**：

| 层 | OPUS-5（L2047-2049） | dsh 双模预设 | 关系 |
|---|---|---|---|
| 原则层 | 思考深度随复杂度分档，不为显然问题付深思考税 | low/max 按任务分档 | **同构**——佐证成立 |
| 机制层 | 模型内置（训练+系统指令描述行为），同会话内自适应（模型自己判「潜伏复杂性」） | harness 参数档（reasoningEffort），跨会话静态选择（人/关键词 launcher 定档） | **不同构**——不可类比 |
| 基线方向 | 「默认先思考」，见复杂才加深 | 快分析档基线「low」 | 方向一致但默认值不同，不构成矛盾（消费端 chat 与受限工具链的复杂度分布不同） |

关键采信边界：**L2047-2049 恰恰不能引为「提示词可以控制思考深度」的证据**——该段证明思考档是模型内置行为，从反面支持 CB42 的结论「思考深度由参数控制，不由劝说控制」（`CB42-思维链成本诊断…:47`）。即 B1 对双模预设的**修正价值为零**（low/max 维持不动），唯一作用 = 给 CB42 §2.2 ① 补一条一手业界出处。

### 2.2 面④ · A1 语气校准实操增量（dsh 会话呈现体感）

**实测：现状已全部落在 T18 的「枯燥机制化」分支，纠正性增量为零**：

- 进度卡三态原文（我 08-21 实测记录，`PT-CB7-dsh协助批2记录_dsh-2026-08-21.md:101-107`）：`EMC 启动中：正在拉起后端服务（8080）…` / `前端已就绪，后端 RAG 预热中（约 20-30s）…` / `EMC 已就绪，地图页已在默认浏览器打开。`——三态全是机制描述，无任何戏剧化措辞。
- 结果卡确定性组装层：outlet_card 文案全是 value+source 平实对（`ai_qa/outlet_kb/build_outlet_schema.py:401-402`「复合规则（…·确定性）」）；report_assemble 的 suggestion 段为对输入结果的聚合，无自带建议时给诚实占位「（输入结果未携带建议字段——本工具不产生新结论…）」（`mcp_server_emc.py:1278-1280`）。
- 唯一可能出戏剧化措辞的层 = LLM 综合层（dsh 自己的最终回答），已有人设纪律兜底（不编造数字/口径对照段/宏观≠微观，`PT-CB7-dsh协助批2记录_dsh-2026-08-21.md:43-52`）。

**裁决**：A1 是**防御性规范而非纠正性修复**——写进文案规范 checklist 一行（「负面聚集/投诉密集类结果文案保持平实机制化」），防未来新增文案（report_assemble 扩段、出口卡新字段、进度卡新态）漂移。增量低但成本≈0，吸收。

### 2.3 B4「边对话边写」对 dsh 会话沉淀/跨机交接卡的适用性

**原则可迁移，机制不可照搬**：OPUS-5 的 T4（L390-423）是逐事实细粒度记忆文件（「如果对话现在结束，这条应已保存」）；EMC 仓内沉淀是**文档粒度**（讨论记录/交接卡），逐事实文件级写入不适用、也不需要。

**落地判断：仓内已有两处结构性同构，B4 不需要新机制，只需把时机纪律写明**：

1. **CB 落文件义务（RULES.md 5.1.1）**：意见必须当轮落盘 .md、禁只回聊天文字——这已经是「边对话边写」的仓内强制版（本回应文档本身即此纪律的产物）。
2. **双环境同步规则⑤**：仓外本机（~/.dsh）改动必须产复刻清单/配方进仓，否则判不可同步退回——把「写入时机」延伸到了跨机面。

**落地形态（习惯条目·零新机制·不碰 ~/.dsh）**：①决策/事实在产生当轮的文档中即时写入（不等任务收尾）；②交接卡在任务边界更新；③~/.dsh 改动按规则⑤产复刻清单进仓（PT-CB7/PT-CB8 记录即先例）。

**补一条当轮实测证据（本回应执行时对家机 ~/.dsh 的清单）**：家机 `C:\Users\Hi\.dsh` 当前 `profiles` 仅 `emc-test`+`web`，**无 `.agent-presets` 目录、无 emc-analysis/emc-research profile**，`settings.yaml` 仅全局 `agent-default-model`（reasoningEffort: max）无 persona 键。即：PT-CB7 A-2 落地的人设 preset 在当前家机环境不可见（原因不臆断），PT-CB8 双模预设位于 office 机（C:/Users/Administrator/.dsh）——**人设与双模预设的跨机一致性正是未验证状态**。这恰是 B4 与规则⑤要补的课：office 侧 ~/.dsh 改动的仓内复刻清单（PT-CB8 记录）存在，但「按差异注记在另一机无脑照做」从未实测。建议并入到岗体检脚本方向（EMC×dsh 路线第 5 条），不另开专项。

### 2.4 人设视角 · 定稿人设 vs OPUS-5 分层身份栈

**结论：无结构性缺口——分层身份栈在 EMC×dsh 架构下是跨进程分布的，不是缺失的**：

| OPUS-5 层 | EMC×dsh 架构中的真身 | 归属 |
|---|---|---|
| 宪法（行为约束） | manifesto.py 领域宪法 + 四态出口契约 + G-2 结论层禁令 | EMC 侧 |
| 产品知识 | kb_facts/list_data 按需检索（非静态注入） | EMC 侧 |
| 场景化工具行为 | 工具 docstring 内嵌规则（S4 已在 EMC 侧同构实现） | EMC 侧 |
| 工具 schema | MCP 协议自带 | 协议层 |

dsh 单段人设的正确内容 = 身份 + 消费纪律 + **到各层的路由指针**（先 list_data / 口径卡 ID / 禁自带色值 / 结论层禁回灌）——A-3 验证回答逐项命中（`PT-CB7-dsh协助批2记录_dsh-2026-08-21.md:34-52`）。若把宪法/产品知识复制进 dsh 段，反而违反「权威源不变、dsh 只调度能力」路线纪律，且制造双写漂移风险。

**有一个维护性弱点（非结构性缺口）**：单段人设把「身份+纪律+工具清单」混在一个文本块——工具面已从 PT-CB7 的 7 件演进到当前 18 件，人设里的静态清单必然过时。对照 S2「上层稳定下层易变」，建议下一个人设维护窗口做两件事（**暂缓，不进本批排期**）：①人设内**不静态枚举工具**，改写「先 list_data 取当前工具面」（指针化 = 消费方正确姿势，不违反权威源纪律）；②稳定段（身份/纪律/口径义务）与易变段（工具面/出图范式）分段维护。落地涉及 ~/.dsh，须走备份纪律 + 复刻清单进仓。

---

## 三 纠错义务（file:line · 5 处 · 均不改变报告结论）

1. **报告§二面④：「入口进度卡三态已验证（`PT-CB7-dsh协助批2记录_dsh-2026-08-21.md:92-95`）」——行号错**。92-95 行是「B-3 结论（T17 核验）」段（首调超时结论/建议回报 Qoder）；进度卡三态表实际在 **:101-107**（Task C · C-1 段）。
2. **报告§二面④：「（如 rank worst 的 suggestion 段）」——前提不成立**。rank 工具返回结构无 suggestion 字段（`tools/mcp_server_emc.py:613` `out = {'rows','row_count','caliber'}`；函数体 :565-616 全程无建议文案生成）。suggestion 汇总仅存在于 report_assemble（`mcp_server_emc.py:1278-1280`），且为对输入结果自带 suggestion 字段的聚合、缺省给诚实占位。A1 的落点表述应改为「LLM 综合层与未来新增文案」，而非「rank worst 的 suggestion 段」。
3. **报告§二面③：「工具面已扩至 19 件（F_021-F_040，mcp_server_emc.py:30-47）」——计数错**。`:30-47` 实际注册 **18 条**工具 ID（F_021-F_028 八件 + F_031-F_040 十件）；公开工具函数 def 表同为 18 个（list_data/rag_query/kb_facts/outlet_card/zonal_stats/buffer/rank/grid_aggregate/compare_regions/hotspot_analysis/area_stats/nearest_analysis/overlay_analysis/trend_analysis/report_assemble/render_spec/render_file/emc_status）。
4. **报告§二面②：「升级三信号=机械规则禁 AI 分类器（`PT-CB7-主执行记录_Qoder-2026-08-21.md:85`）」——行号错一**。该条实际在 **:86**（:85 是「step 预算 ≤15 软约束」条）。
5. **报告§一与§6.2：「共 2050 行/全文 2050 行」——差一行**。实测 OPUS-5.md 为 **2049 行**（read 工具报 total 2049 lines，末行 2049 为 `</thinking_behavior>`）。零影响。

（核证无误的引用：`prompts.py:621/626` eval-anchor、`prompts.py:432-460` few-shot 6 条、`render-contract.md:76-86` §七、`render-contract.md:113` 禁自带色值、`mcp_server_emc.py:371/441` A2 路由分工、`mcp_server_emc.py:628` 格结论禁说成社区结论、`PT-CB8-T7执行记录:14-18`、`PT-CB9-泳道三收敛双评价:21/:44`。）

---

## 四 四档裁决（A1-A3 / B1-B5 / C1-C6 全部吸收项）

| 项 | 裁决 | 理由与落点 |
|---|---|---|
| **A1** 语气校准 | **吸收**（轻） | 防御性规范一行，并入文案规范维护点；纠正性增量为零（进度卡三态/确定性组装层实测已平实，见 §2.2）。零成本防未来漂移。 |
| **A2** 不重复已忽略建议 | **吸收** | 零改动验证结论登记（followup_cues 确定性派生已天然满足）。 |
| **A3** 问前先查 | **吸收** | 零改动验证结论登记（人设「先 list_data」+ A2 路由分工已同构）。 |
| **B1** 思考分档佐证 | **并入排期** | 登记进 **CB42 讨论串**（§2.2 ① 补 OPUS-5 L2047-2049 一手出处），零代码；QA 壳冻结期纪律不变；对双模预设修正价值为零（见 §2.1 采信度）。 |
| **B2** 入库校准三原则 | **并入排期** | 并入 RAG 增量运营批素材卡入库 SOP（与 PT-CB9 verbatim 纪律同向）；消费端（dsh）收益明确（素材纯度=结论纯度）。 |
| **B3** superseded 读时降权 | **并入排期** | 同批合流 PT-CB9 三分法；附消费端硬要求：**标签必须透出到返回结构**（不对称标注：现行零标签、历史/待裁加 status），不能只做检索层降权（见 F3）。 |
| **B4** 边对话边写 | **吸收** | 习惯条目：并入沉淀纪律/交接卡习惯（当轮文档即时写入+任务边界更新交接卡）；零新机制、不碰 ~/.dsh；已有 RULES 5.1.1 + 双环境同步规则⑤ 两处结构性同构；家机/office 跨机一致性实测缺口（§2.3）建议并入到岗体检脚本方向。 |
| **B5** few-shot 反例对 | **暂缓** | eval-anchor 门禁（prompts.py:621/626）+ 收益假设未经 eval 验证；FILL_CARD 先行同样牵动 F_009 eval（prompts.py:630）；登记待裁决（render-contract §八 式或 eval 基线批候选），无批次不动。 |
| **C1** 隐私三表 | 同意不适用 | EMC 无个人用户画像场景；数据脱敏铁律已覆盖 EMC 自身需求。 |
| **C2** 记忆无归因 | 同意不适用（**特别赞成**） | 场景分歧导致的有意相反设计：EMC 结论**必须**带口径卡 ID（用户核验需求），消费端 chat 记忆无归因是流畅性优先——EMC 不应学这条。 |
| **C3** 版权硬限额 | 同意不适用 | EMC 不产公开转载内容；知识卡已有来源标注纪律。 |
| **C4** 拒绝哲学等 | 同意不适用 | 消费端通用 chat 专用；四态出口契约已定义 EMC 自己的「做不成/缺口」语义。 |
| **C5** MCP opt-in | 同意不适用 | 无第三方商业伙伴场景；「禁自带色值」已覆盖消费方约束。 |
| **C6** 平台机制件 | 同意不适用 | 无对应面。 |

**计数**：吸收 5（A1/A2/A3/B4 + 惯例确认）/ 并入排期 3（B1/B2/B3）/ 暂缓 1（B5）/ 反对 0 / 同意不适用 6。与报告「真正需要投入的只有 B2/B3/B5 三处」一致；dsh 侧增量 = B4 习惯条目 + 人设指针化建议（后者暂缓，见 §2.4）。

---

## 五 附 · dsh 侧增量登记建议（交 zcode 汇总时参考）

1. **B1 登记句**（CB42 讨论串追加·建议原文）：「OPUS-5 消费端系统提示 L2047-2049 为机制①补一手佐证：思考深度按复杂度分档是 Anthropic 官方内置行为（默认思考、见潜伏复杂性才开 extended thinking）——与『思考深度由参数控制，不由劝说控制』同向互证；注意其为模型内置非提示词劝说，不可反引为 prompt 控制档位的依据（dsh 组回应 §2.1）。」
2. **B3 消费端契约行**（RAG 增量运营批任务书候选）：「rag_query/kb_facts 返回结构对历史/待裁素材加 status 标记（现行条目零标签·不对称标注），保证 dsh 侧可执行『现行优先』纪律。」
3. **B4 习惯条目**（沉淀纪律附录候选）：「决策/事实在产生当轮的文档中即时写入，交接卡在任务边界更新；~/.dsh 改动按双环境同步规则⑤产复刻清单进仓——写入时机对齐 OPUS-5『如果对话现在结束，这条应已保存』（文档粒度降格适用，非逐事实文件级）。」

---

> dsh 组 · 2026-08-22 · 当轮回应落盘。待 zcode 主手收敛、用户裁定。
