# 2026-08-25 工作详细报告（claude · EMC 项目）

> 角色：claude（执行）| 分支 `EMC_Codex_Harness` | 本报告 = 今日（08-25）全部工作链路的详细落盘，供 codex 审计。
> 前置：08-24 home 已收工（分支更替 EMC_Codex_Harness·PT-CB15 转正批 14 件·基线 597+2 绿）。
> 今日工作三块：①PT-CB14 claude 包四件（本会话早段·随 home 收工提交）②数据治理任务（用户纠偏后确认无需治理）③office 到岗 + T8 首测（完整排查链）。

---

## 一 工作全景

| # | 工作块 | 性质 | 结果 | 落盘 |
|---|---|---|---|---|
| 1 | PT-CB14 claude 包四件（C1-C4） | 确定性清单修复 | ✅ 全过·门禁 581+2 | `PT-CB14-修复批claude执行记录_claude-2026-08-24.md` |
| 2 | 数据治理（小区面 SSSQ 合并） | 数据治理·用户纠偏 | ⚠️ 纠正字段语义→确认现状无需治理 | 本报告 §三 |
| 3 | office 到岗 + T8 首测（?engine=codex） | 环境重建 + 实测 | ✅ 全链通·597+2 零退化 | `PT-CB15-office到岗T8首测执行记录_claude-2026-08-25.md` |

---

## 二 工作块 1：PT-CB14 claude 包四件（已被 home 收工提交收录）

> 完整规格/验收见任务书 `PT-CB14-修复批任务书_zcode-2026-08-24.md`·执行细节见 `PT-CB14-修复批claude执行记录_claude-2026-08-24.md`。此处摘要。

### C1 · D-4 qty 体检点层 11 层注册（core/geo_registry.py）
- `_POINT_LAYERS` 追加 11 个 qty 层（`checkup_qty_*.geojson`·presets 目录·第 4 元素子目录 `DATA/boundaries/presets`·level=CHECKUP·GeoJSON 分支直读）。
- 验收：list_data 点层 12→23 ✓；zonal_stats(停车层) 跑通（5 rows·营盘路社区 23 点）✓。

### C2 · D-1 清单一致性（tools/mcp_server_emc.py + core/geo_registry.py）
- list_data presets 段 available 过滤（`os.path.isfile(预设目录/文件)`·与点层段同纪律）——admin_community 等 manifest 登记但未落盘者不再出现在清单（presets 63→60）。
- resolve_boundary FileNotFoundError 文案改「文件未落盘·需上传：{file}（manifest 已登记）」。
- 验收：admin_community 不出现在清单 ✓；报错含「未落盘」+ 文件名 ✓。

### C3 · D-6 测试件标记（tools/mcp_server_emc.py + docs/render-contract.md + render_client.js:113）
- render_spec `data_nature` 增 `'test'`（dataset 路径 test 不被 dataset meta 覆盖·real/demo 语义不破）；render-contract §五 补 test 值 + 清理纪律（测试 spec 用毕即删）。
- **白名单外单行**：render_client.js:113 徽标分支 `'[测试] '`（C3 验收「前端徽标 [测试]」必需·已在执行记录显式注明供主手复核）。
- 验收：完整链路实测图层名 `[dsh] [测试] PT-CB14测试投递` ✓；测试 spec 用毕已删。

### C4 · D-7 引擎徽标 + [dsh引擎] 前缀（frontend/js/ai_qa/panel.js + brain-adapter-dsh.js）
- `_initEngineBadge()` 常驻徽标（chat-head·light 灰/dsh 橙/mock 紫·getEngineMode 单一权威）；dsh 引擎报错加 `[dsh引擎]` 前缀（hint + _REASON_LABEL 两处）。
- 验收：浏览器三态实测 light/dsh/mock 跟随 ✓。

### 门禁
- 新增 `tests/test_ptcb14_claude_batch.py`（7 用例）+ 适配 `test_render_channel.py` 1 处（fake preset 补落盘——新过滤契约下未落盘本就不该进清单）。
- 全量 **581 passed + 2 skipped**（574 基线 + 7 新增·零退化）。

---

## 三 工作块 2：数据治理任务（用户纠偏·确认无需治理）

### 3.1 用户原始指令
「DATA/analysis 城市体检对象中小区维度：同小区名（SSSQ 字段）的不同面合并成一个，治理后替换旧数据（旧数据入"退休"文件夹），同步纳入 RAG 等相关数据库。」

### 3.2 我的执行与错误
1. 探查：`体检对象_小区_面.geojson` 1562 面·列 XQMC/SSSQ/SSJD/来源·SSSQ unique=187。
2. **错误**：未质疑「SSSQ=小区名」假设（探查已见矛盾证据：SSSQ 组内 XQMC 最多 40 个·XQMC unique=1562=面数），直接按 SSSQ dissolve 生成 1562→187 治理产物。
3. 用户纠正：**小区名=XQMC·社区名=SSSQ**——用户明确指出我「这么明显的错误为什么不指出来」。
4. 重新实证（四文件交叉验证 + strip/去空格归一 + git 历史）：
   - XQMC（小区名）**每面唯一（1562/1562·零重复）**——不存在「同小区名的不同面」→ 按 XQMC 合并无对象。
   - presets/checkup_配置_小区（1562 面）同构·楼栋质心/住宅点（26426 点）SSSQ=188 社区·SSJD=22 街道——全数据集字段语义一致。
5. 向用户摆事实 + 选项 → 用户选「确认现状·无需治理」→ 删除错误产物（_tmp 脚本+输出）·数据文件零改动。

### 3.3 教训（已入 memory：challenge-user-assumptions-with-evidence）
探查证据与用户假设冲突（字段层级/唯一性/值形态）必须当场指出再动数据；数据治理先实证合并键语义；任务无对象如实汇报不硬造。

---

## 四 工作块 3：office 到岗 + T8 首测（本日主链路）

### 4.1 到岗四步
1. 交接卡（session-handoff.md + `_handoff/OFFICE.md`）→ git fetch/switch（已在 EMC_Codex_Harness·旧分支已删）→ fsck（仅 dangling 正常态）。
2. 环境补装：**codex-cli 0.149.1**（npm i -g @openai/codex@0.149.1）·桥 `_resolve_codex_exe` 实测命中 APPDATA npm vendor（**非 WindowsApps 商店版**·冲突问题第一层实测化解）。
3. 基线：**597 passed + 2 skipped**（与 PT-CB15 DoD 597/2 一致·零退化）。
4. T1-T8 实测：T8=?engine=codex 首测完成（§4.3）·T1-T7 用户浏览器侧收尾。

### 4.2 服务栈重建（office 机缺三件·全部补齐）
| 缺口 | 现状 | 处置 |
|---|---|---|
| 后端 8000 跑旧分支旧代码（750a41b8·无 codex_bridge·version 实锤） | 必须重启 | serve.py 自起 uvicorn 标准栈重启（`py frontend/serve.py 8080`·commit 2742f733·EMC_Codex_Harness 验证） |
| **8600 EMC MCP 未起** | **cdh 的 app-server 启动强依赖**（config.toml `[mcp_servers.emc]` required=true·thread/start 握手·失败则整个 thread 创建失败） | `py tools/mcp_server_emc.py --http`（streamable-http·18 工具·握手 200） |
| 3080 dsh web 未起 | cdh 时代不需要 | 不起 |

### 4.3 T8 首测（?engine=codex·EMC 壳里完整 Harness 首测）
**壳体验全通**：绿色「引擎·codex」徽标 ✓ · 推理占位符 ✓ · 逐字流式 ✓ · 回答渲染 + 追问卡 ✓。

**内容链三次尝试→最终全通**：

| 尝试 | 配置 | 事件流 | 结果 |
|---|---|---|---|
| ① | deepseek-v4-flash（08-25 上午我误改的全名） | 不调 emc 工具（仅 list_mcp_resources/模板·read_mcp_resource 失败）·commandExecution 被 sandbox read-only 拒 | 回答「环境故障」·**链断** |
| ② | deepseek-chat + 无引导（对比实验·环境变量临时覆盖） | 仅 list_mcp_resources/模板 | 未调 emc 工具·**链断** |
| ③ | deepseek-chat + AGENTS.md 引导（回滚 + 补写引导） | list_data→zonal_stats→rag_query→kb_facts→**render_spec×2**→hotspot_analysis | **955 delta·68s·全链通** ✓ |

**根因链（三环·全修复）**：
1. **缺 8600 MCP** → app-server thread/start 失败（required emc 握手）→ 起 8600（§4.2）。
2. **缺 EMC 人设引导**（PT-CB15 C2 转正批未落地·`D:\Github\_codex_cwd` 空目录）→ 模型不知道 emc 工具用途 → **补写 `_codex_cwd/AGENTS.md`**（身份+先 list_data+唯一工具面=emc MCP+禁 webSearch+必出图·仓外文件·换机复制清单需登记）。
3. **模型档位混淆**（用户提问「chat 是 flash 还是 pro 价格」→ 查官方文档）：
   - **deepseek-chat = deepseek-v4-flash 兼容别名**（官方：映射 v4-flash 非思考模式·flash 档价格：高峰输入 3 元/输出 9 元每百万 token·闲时减半·pro 高峰 9/27 元）。
   - 用户令「cdh 恒 Flash」与桥现状（deepseek-chat）**天然一致·零改动**；全名 deepseek-v4-flash 实测断链（别名通·保持别名）。

### 4.4 模型配置变更（08-25 用户令）
| 面 | 变更 | 状态 |
|---|---|---|
| cdh（EMC 外置大脑） | `CODEX_MODEL` 默认保持 deepseek-chat（=Flash 档·符合令·环境变量逃生门保留）·注释记录定稿 | ✅ |
| codex 桌面版 | `[model_providers.deepseek] models = ["deepseek-v4-pro","deepseek-v4-flash"]`（Pro/Flash 可切换·改前备份 config.toml.bak-20260825） | ✅ |

### 4.5 冲突问题结论（codex 桌面客户端 vs cdh 共存·用户问）
1. **命令解析隔离**：桥命中 npm CLI（APPDATA vendor）·不碰 WindowsApps 商店版。
2. **配置隔离**：桥 `-c model_provider/model` 进程级覆盖·不读写桌面顶层 config.toml。
3. **运行隔离**：cwd 仓外 `_codex_cwd`·sandbox read-only·approvalPolicy never（不弹审批窗）。
4. **共享面**：~/.codex/auth.json 登录态（特性·EMC 复用登录）。
5. **唯一注意**：npm CLI 不能卸载/重装到别处（桥依赖 vendor 路径探测）。

---

## 五 今日关键决策与经验

1. **诚实认错两次**：SSSQ 假设未质疑（用户批评）·v4-flash 全名误改（用户提问触发查证）——均当场纠正并留痕。
2. **实证优先**：字段语义（三文件交叉验证）、模型档位（官方文档 + 双模型对比实测）、服务版本（version 端点实锤旧代码）。
3. **环境缺环三连**：8600 MCP / AGENTS.md 引导 / 模型档位——office 机环境复制遗漏导致 T8 首败，逐个补齐。
4. **白名单纪律**：PT-CB14 仅 render_client.js:113 一处白名单外单行（显式注明）；Qoder 包文件零接触。

---

## 六 审计请求（发往 codex）

> 本报告为审计对象。请求 codex 审计以下维度，输出审计意见：
> 1. **完整性**：今日三块工作是否有遗漏环节（环境/配置/文档/测试）？
> 2. **正确性**：§四排查链（8600 依赖/引导缺环/模型档位 alias）结论是否与官方文档/代码事实一致？桥默认值回滚是否合理？
> 3. **风险**：`_codex_cwd/AGENTS.md` 仓外引导文件的复制遗漏风险；webSearch 在 app-server 下 ok=false；config.toml 含 API key 明文（备份副本同含）的暴露面。
> 4. **纪律**：白名单外单行（render_client.js:113）与数据治理零改动是否符合纪律？
> 5. **建议**：T1-T7 收尾指引 / 换机复制清单补全 / 其他你发现的问题。
