# 情绪热点图 · P0/P1/P1.5 实施审计（发起）

> **发起方**：claude组 | **日期**：2026-08-05 | **性质**：CB 专题热点图实施审计（两组独立审计）
> **承接**：热点图定稿计划（`情绪热点图_定稿执行计划_2026-08-05.md`·P-1~P-4 已拍板）→ P0 命名定标 + P1 软分级 + P1.5 setTerrain 实施完成 → **请两组审计实施**
> **目的**：让两组对热点图 P0/P1/P1.5 实际改动独立审计（逐条核对 + 有无新问题），通过后整体验收

---

## 〇、实施摘要（claude组 已完成·待审计）

**用户拍板**：P-1 3D=a setTerrain / P-2 2D/3D 不统一 / P-3 B 范围 / P-4 随 P1.5 合并开·**强调工具间解耦**。

### P0 · 命名定标（8 处）
| 文件 | 改动 |
|---|---|
| `toolbox/hotspot-tool.js` | 图层名「显著聚集点(Gi*)」 |
| `grid-tool.js` | 占位卡/组卡「显著聚集(Gi*)」 |
| `panel.js` | `spatial_hotspot`→「代表地点」+ Overview 文案 |
| `test-cases.js` | hotspot 语料改「显著聚集」 |
| `index.html` | 注释 |
| `spec.md` | `spatial_hotspot` 字段 + 调试描述 |

### P0 前置 · Gi\* A/B 验证（真实 L2 数据·2500 点）
- **全 ns 实锤**：逐点/500m/200m 网格 Gi\* **全部无显著热点**（Gi_Z 全在 ±1.07~1.26）
- **根因加深**（glm 主因）：score 是 **U 形多峰离散分布**（5 级极性分类副产物）·与 Gi\* 连续正态假设不匹配
- 网格化平滑 Z 但未变全 ns → **输入粒度修正不足以让 Gi\* 出热点**（原 P1 核心扩围）

### P1 · 软分级（两组 P1 修正评估后）
- `_classify_hotspot` **五档**（hot/tend_hot/ns/tend_cold/cold·`threshold`/`soft_threshold` 参数化·默认 1.96/1.0）
- `hot_spot_analysis` 透传 threshold + 输出 `hotspot_tier` 字段
- 前端 hotspot-tool 弃 `_CLS_POL`（极性色）·map.js `colorMode='hotspot'` **显著性符号层**（五档单色系+大小分档·与 KDE 解耦）
- 单测 +3（五档/阈值参数化/hotspot_tier）

### P1.5 · setTerrain 3D（连续曲面·替代千层饼）
- 后端 `create_terrain_dem`（**F_009**·KDE→terrarium RGB·bounds 转 WGS84）+ `/spatial/dem` 端点（header 返 bounds/size/heightScale）
- 前端 `runDem`（api.js·解析 header）+ map.js `setTerrainDEM`（**draping 隔离**：不常驻+与 3D 网格柱互斥+sky 层防露底）+ heatmap-tool `generateTerrain3DForAI`/`closeTerrain3D`
- retired.md 渲染路径退役登记（fill-extrusion 3D→setTerrain·2D 等值线保留）

### 验证
- pytest **293 passed**（+3 零回归）
- DEM 解码验证（terrarium 高度 0~500·峰 500m·bounds WGS84 正确）
- 括号平衡（全部改动 JS）

---

## 一、审计焦点（两组独立审计）

### 焦点 1：P0 命名定标（8 处）
- 有无漏改（其他"热点"语义残留）？`tool_contracts.py`/`paradigm.py` 契约层是否需同步（Codex 第一轮 5 义）？

### 焦点 2：P1 软分级（五档 + threshold 参数化 + hotspot_tier）
- `_classify_hotspot` 五档边界 + `soft_threshold=1.0`（~84%）诚实标注够不够？
- `hotspot_tier` 字段 vs `hotspot` 冗余？前端消费哪个？
- 弃 `_CLS_POL` 极性色 + map.js 显著性符号层——与 KDE 解耦是否到位？

### 焦点 3：P1.5 setTerrain（连续曲面 + draping 隔离）
- `create_terrain_dem` terrarium 编码正确性（解码验证：0~500·峰 500m·bounds WGS84）？
- **draping 隔离**（setTerrainDEM 不常驻+与 3D 网格柱互斥+sky 层）——是否如 glm P1.5 强调？
- F_009 追踪 ID 分配（F_008 已被占用·用 F_009）对不对？

### 焦点 4：A/B 验证结论
- "score U 形离散分布 vs Gi\* 连续假设不匹配"根因判定 + "网格化非解药"结论——够不够指导"长期 KDE/DBSCAN 替代"？

### 焦点 5：新问题/遗漏（审计核心）
- 有无漏改/副作用？特别：`/geo/hotspot` 后端是否透传 threshold（前端 hotspot-tool 未传·默认 1.96 仍全 ns？软分级前端能看到 tend_hot 吗）？
- setTerrain 与现有图层的冲突实测（天地图 raster 被 drape？）
- 前端 EMC 委托链（tools.js）是否需接 generateTerrain3DForAI？

---

## 附 A：实施审计 prompt（发两组用）

```text
【CB 专题·热点图实施审计】情绪热点图 P0/P1/P1.5

你以第三方独立审计身份（不做项目方决策背书）。

背景：热点图定稿计划（P-1~P-4 已拍板·3D=a setTerrain）→ claude组 实施 P0 命名定标 +
P1 软分级 + P1.5 setTerrain 完成。现请你对实际改动独立审计。
必读：
  docs/catch-ball/discuss/情绪热点图_实施审计发起_2026-08-05.md（改动清单 + 5 审计焦点）
  docs/catch-ball/discuss/情绪热点图_P1修正回应_glm组_2026-08-05.md（你的上轮评估·查根因/方案）
  docs/catch-ball/discuss/情绪热点图_P1修正回应_Codex-GPT5_2026-08-05.md（另一组评估）
按需核实：core/spatial_analysis.py（_classify_hotspot/hotspot_tier/create_terrain_dem F_009）·
  api/routes.py（/spatial/dem）· frontend/js/{map,api,heatmap-tool,toolbox/hotspot-tool}.js ·
  docs/catch-ball/retired.md

审计任务（5 焦点，逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. P0 命名定标（8 处·有无漏改·契约层 tool_contracts/paradigm 是否需同步）
2. P1 软分级（五档边界/soft_threshold 诚实标注/hotspot_tier vs hotspot/弃 _CLS_POL 解耦）
3. P1.5 setTerrain（terrarium 编码/draping 隔离/F_009 ID）
4. A/B 验证结论（U 形离散 vs 连续假设·网格化非解药·长期 KDE/DBSCAN）
5. 新问题/遗漏（重点：/geo/hotspot 后端是否透传 threshold·前端能否看到 tend_hot？
   setTerrain 与天地图 raster/网格柱冲突实测·tools.js EMC 委托是否接 generateTerrain3DForAI）

核心标尺：演示逻辑链（张力图面→聚焦关注区）/ 出口三铁律 / AI·Copilot 内核。
承重红线：四态出口 / finalStep D019 / diagnose 永不动（本实施不涉及·请确认）。
产出：docs/catch-ball/discuss/情绪热点图_实施审计回应_{你的组名}_{今天日期}.md
格式：〇 一句话结论 / 一 逐焦点 / 二 新问题风险清单（阻断/警告/建议）/ 三 可否整体验收结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · 热点图实施审计发起 · 2026-08-05*
*P0 命名 8 处 + P1 软分级（五档/hotspot_tier/符号层）+ P1.5 setTerrain（DEM/draping 隔离）·pytest 293 passed*
