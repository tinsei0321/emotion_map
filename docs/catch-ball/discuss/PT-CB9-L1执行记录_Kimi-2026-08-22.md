# PT-CB9 · 泳道① 内容治理执行记录（Kimi·2026-08-22）

> 派发：`PT-CB9-泳道一内容治理派发单_Kimi_zcode-2026-08-22.md`。分支 EMC_harness_dsh·commit 前缀 `PT-CB9(L1):`·零 pull 零 push。
> 权威源：`docs/rag-loader-contract.md` + `docs/rag-baseline.md` §二.2（11/60 同源实证）。
> 铁则遵守：只改 loader 四函数与数据侧·**未碰 `search()`/`build_index()` 一行**（泳道②分区·其 BM25/RRF 三提交在途合入零冲突）。

---

## 一 结果速览

| 件 | 状态 | 数字 |
|---|---|---|
| 件① 语料地图 `docs/rag-corpus-map.md` | ✅ | 四源登记（实跑 notes 282/facts 68/concepts 9/cases 5=364）+ 新增来源五步流程节 |
| 件②a status 字段 | ✅ | **superseded 38 chunk / 15 文件**（3prime 数据批次）·active 326·枚举零越界 |
| 件②b lineage 字段 | ✅ | **67/68 事实卡标注**（格式 `src:<文件>#<节>`·只标注不删档）·唯 EMC-IDENTITY-01 豁免 |
| 件②c 测试 `tests/test_rag_loader.py` | ✅ | 新增 6 用例（全文纪律抽 20 段/枚举/批次/谱系格式与覆盖/契约序） |
| 门禁 | ✅ | `test_rag_gate`+`test_rag_loader` 9 绿（零 hit→miss 翻转——chunk text 零改动·索引未重建）·全量 **516 passed+2 skipped**（基线 508+4·上浮 +8 含本批 6） |

## 二 status 标注明细（grep 实证链）

1. 作废值定位：`87.9%`/`5,615`/`港务 1,153`/`双高各代`/`page7 密度版` 在语料（notes+facts+concepts+cases）中**仅见于** `_口径注册表.md`（`_` 前缀不被索引）与 `03-10`（新口径文档·active）——**语料内无散落作废值 chunk**；
2. 批次判定：`03-10 §一` 声明「3prime/ 系列（2026-08-12·77 项占比表、双高区等）均为西陵伍家旧口径/密度旧口径·两处数字不一致时一律以本笔记为准」→ 3prime 全 16 文件逐一读文件头核验：
   - **15 个数据/落位文件**（占比表×9、77 项×4、B1/B2/B3B4 落位×3）→ 全节 `superseded`（38 chunk）；
   - `分析计划与内容_总纲` → **待裁不改**（计划文档·不承载数据口径·见 §四）；
3. 实现：loader 侧规则表 `_SUPERSEDED_FILE_PREFIX/_SUPERSEDED_EXEMPT_PREFIX` + `_governance()`（`tools/rag_index.py`）——按文件级标注·规则透明可审。

## 三 lineage 标注明细（67 条·逐卡 token 验证）

**方法**：自写匹配器（`_tmp/l1_lineage_match.py`·distinctive 数字/专名加权评分）产候选 → 逐卡 grep 原文核验（verbatim 命中才锁）→ 解析器（`_tmp/l1_lineage_resolve.py`）验证 token 落节并计算 loader 位置序号 `#i` → 多节命中人工定主节（5 条 override 注释在案）。

**分布**：
- 事实卡→`00-宜昌专项/03-0x` 笔记：34 条（CHK 系 15 + URP-I09~I16/C05~C07/P09~P12/M04~M05 等·declared source_path 与 matcher 互证）；
- 事实卡→`_笔记/` 三组提炼笔记：21 条（codex_0819×5/codex_伍家岗十五五×6/codex_体检附件×4/codex_部委文件×1/glm_05×1/claude_GIS×2/00-01×1/00-02×1）；
- 事实卡→`城市更新专项规划资料集_总览报告`：3 条（URP-P11/L01/L04·verbatim 命中）；
- 事实卡→案例库：5 条（URP-K01~K05·declared 直挂）；
- 黄金集 11 条实证对全部覆盖（N02/N04/U03/U05/R02/R10~R14/C01 对应卡在表内）。

**与黄金集注释的两处分歧（待主手裁定·按 verbatim 证据锁定）**：
1. `URP-I07`（92:6:2 泊位结构）：黄金 U05 注释「系占比表蒸馏」·实测 verbatim 在 `03-08`（「停车位结构比例 92:6:2 不达标（应≥85:10-15:≤5）」逐字一致）→ 锁 03-08#5；
2. `URP-M02`（40 项指标）：黄金 R11 注释「系 codex_湖北 蒸馏」·实测 40 项/市政/韧性/住房 verbatim 在 `codex_伍家岗十五五` → 锁该笔记#2。

## 四 待裁清单（不确定不改·待主手裁决）

| 项 | 内容 | 建议 |
|---|---|---|
| 1 | `3prime/分析计划与内容_总纲`（active 保留） | 计划文档不承载数据口径→倾向维持 active；若主手判「旧框架整体压注」→ 一行豁免删除即生效 |
| 2 | EMC-IDENTITY-01（唯一无 lineage 事实卡） | 上游=PT-CB6/7 任务书（非语料）→ 无需标注 |
| 3 | 03-07/03-08/03-09 三份体检摘要笔记**彼此近重复**（同指标值多处命中·lineage 解析 WARN 24 次） | 笔记间互标 lineage 属泳道②去重决策面·本批未标（只标事实卡→笔记单向） |
| 4 | 案例 chunk（5 条）自身 lineage | 上海/南京/广州/宁夏/望洲岗案例与 `_笔记`/`04-案例资料` 未检出小节级 verbatim 锚点（ golden R02「上海分享↔考察报告」=笔记间关系非 case↔笔记）→ 未标 |
| 5 | 概念卡（9 张） | 上游=CLAUDE.md/KNOWLEDGE.md（非语料）→ 无 lineage |
| 6 | `docs/rag-corpus-map.md` §三 治理现状节 | 后续批次字段变更时需同步更新（防图漂移） |

## 五 给泳道②的接口说明（零冲突合流点）

- `load_chunks()` 已按契约 §二落位（facts+notes+cases+concepts 序与 build_index 内联序一致）——`build_index()` 换挂一行即得治理字段；
- 索引持久化缺口：`build_index()` 的 metas 尚未透传 `status/lineage` 进 `meta.jsonl`——`search()` 的 superseded 预置过滤（泳道②已写）需重建索引后才实际生效；**本批故意不重建**（重建属泳道②验收动作·本批 chunk text 零改动·门禁零翻转）。

## 六 蒸馏声明（新坑/规则）

- **无新坑**：全程按「grep 定位→读原文→逐卡验证」手术流·无 ≥2 轮返工·无症状迁移。
- 环境注记一件（非坑·已解）：泳道② BM25 依赖 `rank_bm25` 已在 requirements.txt:71 登记但本机未装 → 门禁 ModuleNotFoundError（与我方改动无关）·`py -m pip install rank_bm25` 后全绿。**双机同步注记：office 机到岗需同装（体检脚本可纳入该项）**。
- 可复用方法（已留 `_tmp/` 脚本·未提交）：「distinctive token 加权匹配 + 逐卡 grep 核验」做蒸馏谱系建档——后续 03-09↔03-08 等笔记间谱系（待裁 3）可复用同一管线。

## 七 纪律自查

- [x] 只改 loader 四函数+数据侧（`_governance`/`load_chunks` 新增·`search/build_index` 零触碰）
- [x] 零新追踪 ID·print 走 `_tag`（既有件·未新增 print）·禁 emoji·A9（无新 except）
- [x] 字段缺失=active 兼容（`_governance` 默认 active·泳道② `m.get('status','active')` 预置对齐）
- [x] chunk text 零改动 → 索引免重建 → 门禁零 hit→miss 翻转（9 绿实证）
- [x] 全量 516 passed+2 skipped（基线 508+4·上浮 +8 注明：本批 6 + 泳道②在途 2）
- [x] 显式路径 commit（rag_index.py/test_rag_loader.py/rag-corpus-map.md/本记录）·零 pull 零 push·`docs/progress.md` 他组在途改动未触碰

> Kimi · 2026-08-22 · PT-CB9 泳道①交付·待主手回收
