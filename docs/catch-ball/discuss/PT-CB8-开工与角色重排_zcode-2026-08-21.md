# PT-CB8 · office 续班批 开工与角色重排（zcode·2026-08-21）

> 签发：zcode（主手）。**角色重排（08-21 用户令）**：**执行 = zcode 主 + Codex 副**；**审计 = claude 组**；dsh = 测试环境兼知识线（不再承担执行批）。
> 分支：`EMC_harness_dsh`（main 冻结）。门禁基线：**442 passed + 2 skipped**。

---

## 一、本批任务（承接 PT-CB7 交接卡续点③ + 今日新令）

| # | 任务 | 执行 | 白名单 | 验收 |
|---|---|---|---|---|
| **T17** | MCP 服务端预热真修：冷启动根因=server 每次被拉起重复做 RAG 模型加载（>120s·timeout 缓解非根治）——**预热常驻/启动即热方案设计与实施** | **zcode（主·攻坚）** | `tools/mcp_server_emc.py`、`api/`（如需）、`SCRIPT/`（如需）+ 测试 | 冷启动首调 <10s 或预热后首调无超时；门禁 442+2 零退化 |
| **T7** | 双模预设落地 + ≤2min 验收（PT-CB7 批 3 遗留） | **Codex（副·按任务卡）** | 派发单随本档 §三 | 验收口径见派发单 |
| **E1** | rc.8 更新挂起项（今日新令受阻）——见 §二 留档 | 挂起（上游澄清后重启） | — | — |
| **E2** | debug-memory 双 R11 撞号合并（PT-CB7 遗留裁决） | zcode（顺手） | `docs/debug-memory.md` | 单一 R11·两案例合并 |
| 审计 | 本批完工后 claude 组按章程审 | claude 组 | — | 落盘 `PT-CB8-审计_claude-*.md` |

## 二、E1 留档：rc.8 更新受阻实录（office·08-21）

- **动作**：robocopy 镜像 zip→checkout（保 .git/备份分支 backup-rc.7-pre-upgrade）→定制重放（4 文件 3way 干净应用）→pnpm install。
- **三重阻断**：①镜像静默漏 `packages/attachment/attachment`（已手动补齐）；②**pnpm install 反复定向删除 `packages/api/gateway/`**（恢复后下一轮 install 再删·根因不明·嫌疑=rc.8 某安装期钩子/workspace 清理机制，attachment 手动补丁未被删而 api/gateway 被删·模式异常）；③npmmirror 网络抖动（UND_ERR_DESTROYED）。
- **止损**：checkout 已 reset 回 rc.7+定制（a636297374）·pnpm 依赖重装完成（tsx 在位·3080 web 可正常重启）。
- **遗留**：rc.8 zip 与解压目录保留（D:/Github/deepseek-harness-master.zip）；重启条件=上游澄清删除行为 or 家机（rc.8 装成）导出安装步骤对比。**影响**：office 人设件（rc.8 特性）顺延。
- 教训：跨机器环境重装先问**装成机器的安装步骤**（家机 rc.8 是怎么装的——直接问可省全部排障）。

## 三、T7 派发卡（Codex 副·首个执行批）

> 业务背景：PT-CB7 双模预设（spec/react 双推理模式·缩短自测轮次）家机已验思路，office 落地。
> 数据源/规格：见 `PT-CB7-主执行交接派发包_zcode-2026-08-21.md` §T7 + 家机批 2 记录；office 适配注意 rc.7（preset 集=code/cordis/minimal/standard·无 subagent 变体）。
> 白名单：`~/.dsh/`（dsh 环境）+ 执行记录落 EMC 仓 `PT-CB8-T7执行记录_Codex-2026-08-21.md`（唯一 EMC 仓写入物）。
> 验收：双模可切换 + 自测轮次 ≤2min + 零污染用户日常 web profile。

## 四、纪律

claude 组审计制首次主用（原 Codex 审计线转执行）；派发必附 prompt；EMC 仓 commit 由 zcode 统一执行；AGENTS v2.4 学习规则继续生效。
