# EMC × dsh 整体合体 · 讨论过程台账（living doc）

> **目的**：本任务多轮讨论的过程与内容全落盘，供双环境（家/办公室）context 管理。**换机或新会话接手本任务，第一步读本文件**（再按需读文件清单里的具体稿件）。
> **纪律（用户 2026-08-16 定·最高）**：① zcode 角色 = **回收各组评估 + 落盘**；② **零执行**——不实施、不出实施计划、不合并分支、不动生产代码，除非用户明示；③ 每轮讨论后更新本台账；④ 拍板权全程在用户。
> **分支**：`EMC_harness_dsh`（讨论专用；main 上 CB-39 B/C 线实施不受影响）。

---

## 当前状态快照（R2 后·2026-08-17）

| 项 | 状态 |
|---|---|
| 轮次 | **R15=PT-CB1 开工**（用户新结构令：zcode 主手/dsh 副手/Codex+claude 审计·新命名体系 PT-CB{n}·拍板清单 1/2/11 生效）——T1 落账✅·T2 G8a 开工·T3/T4 派发 |
| 回收状态 | claude组 ✅（自行提交 9704a5b6）/ Codex ✅ / dsh组 ✅（均 2026-08-16，zcode 代提交入分支） |
| 三组一致结论 | **D1 否决方案A / D2 方案B 立项+spike / D3 载体 MCP server / D4 dsh组 回归**（4/4 决策点三组全一致·罕见一轮收敛） |
| 下一步 | 用户拍板 D1-D4 → 拍板后按 CB 惯例出收敛定稿 + Codex 出实施计划（**本轮 zcode 零执行，等用户指令**） |
| Open questions | ① Codex 挑战③（「三份 harness 无原生 GIS」→ dsh 增量是否=dsh 原有 GIS 工具面）dsh组 未直接点名回答——dsh组 列的 dsh 独有点（Web GUI/MIT/自托管/DeepSeek 适配）不含 GIS 工具，隐含答案「无原生 GIS」待确认；② mcp 包未入 requirements.txt（spike 时 Ops 补登记——dsh组 提出）；③ run_analysis 降 v2 的口径（P1-3 落地前同步阻塞）是否接受 |

## 文件清单（本任务全部产物·按阅读顺序）

| 文件（同目录 `docs/catch-ball/discuss/`） | 轮次 | 性质 |
|---|---|---|
| `EMC-harness架构借鉴dsh_专题底稿_zcode-2026-08-16.md` | 前置 | dsh 四支柱研读 + EMC 对照 + G6-G9（体内借鉴·另一条线并行） |
| `EMC-dsh整体合体_寄生式嵌入_评估与讨论发起_zcode-2026-08-16.md` | R0 | zcode 独立评估（方案A 六维反对/方案B 工具级寄生）+ 六议题 + D1-D4 |
| `EMC-dsh整体合体_寄生式嵌入_回应_Claude-2026-08-16.md` | R1 | claude组：议题1 独立复裁 + 议题5 七条硬约束 |
| `EMC-dsh整体合体_寄生式嵌入_回应_Codex-2026-08-16.md` | R1 | Codex：议题2/3/6 主答（载体/清单/排期） |
| `EMC-dsh整体合体_寄生式嵌入_回应_dsh组-2026-08-16.md` | R1 | dsh组：议题4 主答（dsh 源码级实锤）+ 议题1/2 质证 |
| `EMC-聪明路径_入口端灵活性_讨论稿_zcode-2026-08-17.md` | R3 | **真痛点对症稿**（四层机理/入口端 reframe/四机制 M1-M4/决策点 E1-E3）——接手本任务必读 |
| 本文件 | 每轮 | 过程台账 |

---

## R0（2026-08-16 晚·家环境）发起

- 用户提出大胆设想：情绪地图**整个项目**（项目+数据+功能模块）作为拓展寄生 dsh 架构，EMC 融入 dsh 对话机制而非独立模块，直接用 dsh 开放性+人机交流+工作流+工具调用 harness。
- zcode 评估拆解为两层：**方案A 整体寄生**（字面·架构迁移）vs **方案B 能力融合**（精神·工具暴露）；结论「方向性反对 A·证据性支持 B（MCP server 优先·EMC 本体零改动）」，六维反对论证 + 关键洞察「用户已拥有三份通用 harness（zcode/claude code/codex），情绪地图独有资产是领域栈——正确寄生方向=协议级而非运行时级」。
- 产出：发起稿落盘（commit `1ce6cc64`）+ CB 索引/分支注记 + 三组联合讨论 prompt 交用户投递（dsh组 08-16 已退出 CB 索引·需回归）。

## R1（2026-08-16 深夜·家环境）三组回应回收纪要

### 1. 决策点立场对照（D1-D4）

| 决策点 | claude组 | Codex | dsh组 | 收敛度 |
|---|---|---|---|---|
| **D1 方案A 裁定** | **否决**（独立复裁五维成立一维微调·两维加重） | **否决**（附议+三条执行层实证+第七维） | **否决**（源码级实锤·补三刀） | **3/3 一致否决** |
| **D2 方案B 立项** | **立项+spike**（1-2d·可证伪） | **立项+spike**（G8 合流后 <1d） | **立项+spike**（1-2d·体外零耦合） | **3/3 一致**（量级修正见下） |
| **D3 载体预选** | **MCP server**（双轨不必要） | **MCP server**（in-process FastMCP·bash 侧门维持） | **MCP server 唯一解**（dsh 原生 MCP 客户端·反双轨） | **3/3 一致**（dsh 事实使 MCP 从预选升唯一解） |
| **D4 dsh组 回归** | **回归**（缺席则议题4=外组猜 dsh） | **回归·限单议题答辩**（不恢复常设席位） | **回归**（本回应即回归产出·持续供给质证） | **3/3 一致**（范围微差：常设 vs 限定） |

### 2. 各组独特贡献（不重复共识部分）

- **claude组**：① 「红线载体湮灭」加重——寄生后不是 prompt 被改而是 diagnoseStep/finalStep 架构消失；② 「演示逻辑链断两环」——张力图面+引导点击是地图 UI 专属，产品最高纲领不可实现（纲领级否决·比 zcode「半条工具面无家可归」更重）；③ 对「三份 harness」论断三次反证均失败（血缘亲缘/GIS 生态/评委演示——论断成立）；④ 「课题消亡场景」检索并否定（=放弃 EMC·与现行主线语境内不相容）；⑤ 议题5 七条工具面硬约束 **C1-C7**（脱敏输出过滤/sim 白名单+demo 参数/不暴露 gdb·中转站/三级审批·删除类不进面/路径沙箱白名单前缀/不透传 key/片区结论附 usage 元数据）。
- **Codex**：① 三条执行层实证（CB-39 P0-1 诚实度刚修好·A=把病再引进门 / B/C 线双运行时对冲 / 铁律 4 前端=主界面是宪法级·A 违宪·52 个 JS 前端成孤儿资产）；② 第七维「CB 流程冻结」（discuss/ 文档体系与 repo 绑定·迁 dsh 后 CB 工作流重造）；③ spike 成本细分修正：schema 适配 ≈0（G8 合流后剥壳 ~10 行）+ 宿主审批配置隐藏成本 0.3d/宿主 → **首宿主 ≈1d·G8 先行 <1d**；④ 工具面改版：`spatial_query` 按契约粒度拆（zonal_stats/buffer/rank·13 GIS 契约内 schema 直派）、`run_analysis` **降级 v2**（P1-3 前同步阻塞+file_path 攻击面）、增补 **`outlet_card`**（/aiqa/outlet_card 零新逻辑·与 G1 呼应：G1=文件级·此=对话级）→ **v1 七工具最小集**（rag_query/kb_facts/list_data/outlet_card/zonal_stats/buffer/rank·6 只读）；⑤ 新增风险三条（路径穿越→v1 一律 dataset_id 不收裸路径/Windows py 启动器钉死/长任务阻塞声明）；⑥ 排期主答：**编号 G10（体外工具面）**、G8+G10 组合批（同一条线：契约派生→多消费端·合流省 1d）、总表 G1>G5>G6>G8+G10>G7>G2>G9/G3/G4。
- **dsh组**（对本地 dsh checkout `D:\Github\dsh_test` master@cafd4e6132 v0.1.0-rc.5 现场核查·全部 file:line/哈希级证据）：① **dsh 原生支持 MCP**（一阶客户端 `@deepseek-ai/dsh-mcp-client`·stdio+streamable-http 双 transport·`mcp__server__tool` 命名与 Claude Code/Codex 同形·自动重连/再同步）——zcode「若 dsh 不支持 MCP」分支前提作废，**MCP 写完当天 dsh 零代码接入**；② **破坏性变更为定量实锤**：600 commit 内 16 个 `!:`、周级节奏（最近一批 07-29~08-09·08-06 单日 3 个·集中在 cli/config/profile=plugin 生态承重层）、30 天约 9,500 commits——「整体寄生=每周陪跑 API 迁移；MCP 级寄生完全绝缘」；③ plugin 真实成本：「骨架便宜（~20 行·无需 fork monorepo·npm/tarball 分发）长期贵（TS strict 全栈永久负担+API 陪跑+行为保障重焊+地图 UI=React client plugin 完整工程且单宿主专属）」；④ 编排器在 dsh 是「**重焊**不是重写」（pre-step/turn-stopping/concludeTurn/guard 可近似实现·但焊点=周级变动 API）；⑤ 哲学判断「dsh 欢迎消费者不欢迎住户」；⑥ **场景 S 识别**（用户想把 EMC+开放对话交给非开发者在浏览器用——四 harness 中 dsh 是唯一浏览器原生 Web GUI+MIT+自托管）但仍指向 B：场景 S 全量需求由 MCP+dsh 原生客户端覆盖；⑦ 议题5 补充：**纪律单一权威源必须在 MCP server 侧**（脱敏在 server 做绝·宿主审批只是第二道·否则四宿主×各自配置=纪律漂移）；outputSchema 用最小公共词汇四宿主全兼容。

### 3. 对 zcode 发起稿的修正汇总（R1 的增量事实）

| # | 修正 | 来源 | zcode 态度（台账记录） |
|---|---|---|---|
| 1 | dsh 原生 MCP 客户端——「若 dsh 不支持 MCP」前提作废·MCP 唯一解 | dsh组 | 接受（对结论有利·加强 B） |
| 2 | breaking 定性→定量（16/600·周级·单日 3 个） | dsh组 | 接受（加强 A 反对） |
| 3 | 契约维「失守」微调为「重建可保·守卫链重接」（:473 函数实存使单一源技术可保） | claude组 | 接受（裁决不变） |
| 4 | 「无地图 UI」表述修正（Web Client 有 ConversationNode 机制·地图 viewer 可行但单宿主贵工程） | dsh组 | 接受（论据更换·结论不变） |
| 5 | 「增量≈0」修正为「增量=dsh 原生 GIS 工具面（待证实）」——**open**：dsh组 未直接回答 dsh 有无原生 GIS 工具 | Codex 提出 | 待确认（见 Open questions ①） |
| 6 | spike 量级 1-2d → 首宿主 ≈1d / G8 合流后 <1d | Codex | 接受 |
| 7 | 工具面 6→7（+outlet_card·spatial 拆契约粒度·run_analysis 降 v2·v1 参数一律 dataset_id） | Codex | 接受（更具体更安全） |
| 8 | 第七维反对「CB 流程冻结」 | Codex | 接受（补强） |
| 9 | mcp 包未入 requirements.txt（Ops 补登记） | dsh组 | 接受（spike 前置项） |

### 4. 分歧点

**无对抗性分歧**——四决策点三组全一致。仅范围微差（D4 常设 vs 限定单议题）与量级口径（1-2d vs <1d·取决于 G8 是否先行），均可在收敛稿中统一。

### 5. 状态

**未收敛——拍板权在用户。** 三组建议已齐（D1 否决A / D2 立项B+spike / D3 MCP / D4 dsh组回归），等用户对 D1-D4 裁定；裁定后是否出收敛定稿稿、是否进 CB-40 总表（G10）、何时让 Codex 出实施计划，均听用户指令（本轮纪律：零执行）。

---

## R2（2026-08-17·用户↔zcode）拍板前澄清：方案B 对现状的提升与核心改变

**用户问题**：如果按方案B，对我目前有哪些提升，核心改变在哪？

**zcode 回答要点**（全文见当轮会话·此处为台账版）：

1. **三层提升**：
   - **工作流调用质量**（立即可感）：agent 调情绪地图能力从「读代码猜参数+bash 裸调+报错重试」变为「function calling+schema 前置校验+dataset_id 枚举」——CB 出图主线/数据治理任务往返减少；同时三类风险机制化（脱敏输出过滤/sim 白名单/路径沙箱）——注意现状里 agent bash 调用的 stdout 原样进 LLM 上下文，铁律 7 目前并不约束这条野生通道。
   - **真正的新能力**：rag_query（235 条索引首次进入任意 agent 会话·目前只服务 EMC 前端）/ outlet_card 对话级出口（与 G1 文件级呼应）/ kb_facts 权威事实源（CB-29 口径漂移类问题的部分解药）。
   - **生态战略**：四 harness 即插即用（dsh 零代码·场景 S 路铺好）/ G8+G10 同线（契约第三消费端）/ 竞赛「开放 agent 操作城市数据」演示不绑框架。
2. **核心改变一句话**：能力入口从两个（前端 GUI 给规划师 / bash 野生侧门给 agent）变三个，新增**协议级一等入口**——变的不是能力，是能力的**可达性与安全性**。
3. **诚实边界（四条·不附和）**：① EMC 产品本身零提升——四痛点（知识库薄/参数填充/假结论/多步链弱）一个不解决，那是 CB-39 B/C+G1-G5 的战场；方案B 提升的是用户工作流不是产品智商；② 标准操作受益、创新分析不受益（page7 强度评价类全新分析仍需现写代码）；③ 新增小维护面（server 随契约演进·G8 后趋零·mcp 依赖登记·宿主配置坑）；④ 收益与 CB 主线中数据/空间类任务占比成正比。
4. **可证伪验收建议**：spike 验收=同一真实 CB 任务跑一次对比（bash 考古 vs 工具调用）。

**状态**：仍待用户拍板 D1-D4；本轮为澄清轮，无新增组间分歧。

---

## R3（2026-08-17·用户↔zcode）讨论核心点纠偏：真痛点 = EMC 入口聪明度不足

**用户澄清**（讨论错位声明）：EMC 最大痛点 = 自然语言→分析方法+工具调用+结果产出的路径不够"聪明"——LLM 接入效果未充分发挥，靠提示词/字典驱动预制分析路径；稳定有了但 LLM 灵活性被大大弱化；核心=意图判断不够准/不够灵活→选不中正确方法+工具组合；困境持续很久，一直"拆东墙补西墙"修不完，没发挥 agent 优势。

**zcode 机理分析**（全文见当轮会话）：

1. **四层叠加机理**：① 开放问题空间 vs 封闭意图分类法（`paradigm.py` 尺度→方法映射 + `harness.js:57` 轻量预判词表 geo 动词/宜昌地名/宁落不误断）——结构性覆盖缺口，有限字典索引无限空间，修补数学上不收敛；② 单发诊断不可恢复——diagnose 一次 FC 定终身，失败全走降级（`harness.js:185` 起 degraded 出口串）不澄清不追问；③ 参数闭卷填空——CB-08 实测选型 12/12 全对、瓶颈在参数填充（buffer.center/overlay.layer_a,b），LLM 看不见数据资产清单；④ eval 失真（CB-01 C6 空 context）——补丁过当轮用例、长尾回归、下轮再补。
2. **精化用户诊断**：非"单次选型精度"问题（覆盖内 100%），是覆盖面+单发不可恢复+参数闭卷+eval 失真的叠加体感。
3. **核心 reframe**：痛不在内核错，在内核承诺的"Smart 入口"未兑现足额——入口只给 LLM 一发子弹（不能追问/不能看数据定参/不能产多步计划·CPD 空骨架）。**灵活性归还位置=入口端，非中间开放**（B001/B003 是中间变聪明的病）。三个机制：迭代诊断（1-2 轮澄清）/ 参数接地（数据图层清单实时喂诊断端）/ 计划生成（plans 契约·G9 delegated turn）+ G6 session log（eval 反 Whac-A-Mole）。
4. **诚实校准**：① "彻底解决"不存在——开放 loop 只是把路由错误换成过程漂移（B001 极端形态），dsh 也靠 guards 兜底；能收敛的是把结构缺失补成机制，不是换脑子；② 无需动架构骨架——四机制全长在内核允许位置。
5. **拍板语境更新**：用户真痛在 EMC 体内聪明度 → 方案A（换运行时）与方案B/G10（工作流侧门）都不治此痛（R2 边界①升为主矛盾）；**对症排序 = G6 → 参数接地 → 迭代诊断 → G9，G10 退居侧门**。
6. **建议（待用户指令）**：将「聪明路径」（入口端三机制+G6）作为新议题交三组讨论——比 A/B 裁决更接近真问题。

**状态**：讨论焦点已从"寄生方式"转向"入口聪明度"；D1-D4 拍板仍悬置（D1 结论更稳固，D2/G10 权重下降）；「聪明路径」议题开轮与否待用户指示。**R3 讨论稿已单独落盘**：`EMC-聪明路径_入口端灵活性_讨论稿_zcode-2026-08-17.md`（四机制 M1-M4+决策点 E1-E3·含红线兼容性分析——M2 走 schema 侧不走 prompt 侧）。

---



## R4（2026-08-17 晚·用户↔zcode）天然对照实验：通用 harness vs EMC 的空间操作能力

**用户问题**：用 claude/codex/zcode/dsh 喂线面数据做合并/剪裁等操作，不告诉剪裁逻辑、不做 GIS 工具调优，它们能迅速准确完成；EMC 调优多次仍常出错。同样是"用 GIS 工具"，差别在哪？为什么它们不用调优？

**zcode 分析**（全文见当轮会话）：

1. **核心答案：调优没有消失，而是发生了三次转移**——①**前移到预训练**：通用 agent 根本没用"GIS 工具"，用的是代码（通用底座）+ geopandas/shapely（公共库知识·训练分布内见过海量样本）；EMC 的 19 契约是**私有 API**，模型零先验，全靠 prompt/字典现场教——「你每调优一次 EMC 学到一条映射；通用 agent 的教材是整个 GitHub，这场词典竞赛 EMC 数学上赢不了」。②**内置到会话循环**：通用 agent 跑-看-改（读真实数据→写代码→执行→检查结果→修正），多次内部迭代，用户只看到最后一次成功——"不调优就完成"是错觉，**调优发生了，只是不可见**；EMC 单发闭卷，第一次尝试直接呈现给用户，试错责任外包给用户。③**外置给用户**：EMC 剩余不适配全落到用户身上（字典/映射/CB 轮）。
2. **三个结构差**：工具空间（无限=代码生成 vs 有限=19 契约分类选择）；反馈回路（执行后可自检自纠 vs 无执行反馈）；接地方式（先读真实数据列名/CRS/几何再操作 vs 闭卷填参）。
3. **公平性 caveat（诚实）**：用户测试任务（合并/剪裁）恰是通用范式最强的类型——明确、可客观验证、训练分布内；EMC 的叙事分析类任务（颗粒度/结论/行业对接）是私有域分布外。但 EMC 也必须服务空间操作类，故 caveat 不推翻结论。
4. **关键区分（对 B001/B003 的再认识）**：可验证操作（结果客观可查：面积/行数/几何有效性）vs 叙事分析（结论不可客观验证）。B001/B003 的病在叙事类的开放循环，不在可验证操作类。**"稳定 vs 灵活"的成熟形态不是一刀切 dumb 中间，而是按任务类路由**。
5. **对 R3 立场的修正（诚实声明）**：R3"灵活性归还入口端"不够——还需要第五机制候选 **M5 有界代码执行通道**：可验证空间操作类走「LLM 写 geopandas 片段→白名单数据集→沙箱执行→结果校验（几何有效性/面积/行数）→有界重试（2-3 次）→通过确定性渲染契约呈现」；叙事分析类仍走契约路径。这正是 G7 守卫管线+G6 日志的对口场景。风险与边界：沙箱安全/成本延迟/渲染确定性/迭代上限。
6. **与方案B 的对称性观察**：方案B（MCP）是把 EMC 能力导出到"能工作的范式"（通用循环）；M5 是把"能工作的范式"导入 EMC（限可验证操作类）——同一枚硬币的两面。

**状态**：R4=对 E1-E3 议程的重要输入——建议聪明路径机制清单从 M1-M4 扩为 M1-M5（下轮讨论稿更新时并入·本稿不改 R3 讨论稿以保持轮次清晰）；待用户拍板项新增：是否将 M5 纳入三组议题。

---



## R5（2026-08-17 晚·用户↔zcode）存在之问：dsh 比 EMC 做得好，那做 EMC 的意义是什么？

**用户问题**：用 dsh 喂数据做操作能实现且比 EMC 好——那做 EMC 的意义是什么？只是用来展示在地图上吗？

**zcode 分析**（全文见当轮会话）：

1. **四层拆解 EMC**：①NL 路由层（diagnose→19 契约→final）=**被通用范式支配的壳**（R4 实证·继续手工调优=输）；②数据与口径层（治理后 12345/体检落位/边界/总账/口径注册）=**不可替代的沉淀资产**（dsh 每会话从零理解原始数据；仓库一次治理终身复用·可辩护可复现）；③知识与范式层（industry_kb/RAG 363 chunks/出口卡/尺度-方法）=不可替代的私有认知；④产品形态层（地图+演示逻辑链+视野-数据-结论同步）=面向规划师的交付物语言（规划师不读终端输出）。
2. **"dsh 更好"的隐藏前提**：它服务的是会提问的用户（你自己）、一次性任务、无需口径辩护；EMC 服务的是不会开终端的规划师、要求跨会话一致、要求口径可辩护（评委/行业质询）。**这几天的 CB 工作流本身就是「通用 harness × 仓库领域栈」模式的实证——它工作良好，但服务的是你，不是你的客户**。
3. **会话智能 vs 沉淀资产**：dsh 的优秀是每会话的（用完蒸发）；EMC/仓库的价值是累积的（治理/口径/知识/出口）。"EMC 的意义"之问混淆了两者：路由壳是会话智能（输），平台是沉淀资产（赢）。
4. **三种终局形态**：形态1 现行自研路由全栈（R4 证明此层被支配）；形态2 壳改造（M1/M2/M5 入口聪明化·栈与产品不动）；形态3 平台化（EMC 退为领域平台：数据+口径+知识+渲染 API，NL 入口由通用 harness 经 MCP 承担，前端为共享呈现层）。证据指向：形态2 是渐进正路、形态3 是长期归趋（M5 与 MCP 是同一枚硬币两面·R4 第 6 点）——非互斥，可分阶段。
5. **已投入价值盘点**：沉没部分=路由层的词典式调优；复利部分=契约纪律（tool_contracts 已成 MCP schema 源）、守卫与诚实度机制（任何形态都需要）、harness 编排模式、eval 教训、产品与数据资产。
6. **待拍板新增**：EMC 终局形态选择（形态1/2/3）应并入 E1-E3 议程（E4 候选）。

**状态**：R5 完成；待拍板项=D1-D4 + E1-E3 + M5 纳入 + E4 终局形态。

---

## R6（2026-08-17 晚·用户裁定倾向）终局形态：用户倾向形态3（平台化）

**用户裁定倾向**（原话要点）：① 形态3 更接近想要的形态——地图+情绪分析图例样式+专业知识库+宜昌市基础信息+地图交互方式逻辑=可复用长期迭代积累的财产；② 深挖 EMC/意图判断/agent-loop 让 EMC 更聪明=**死路**——只能在输入输出口径和范式上做规则，「如何跑起来的路由不是个人开发者该做的事」；LLM/harness 都用通用版本，**在通用基础上搭架子**。

**zcode 意见**（全文见当轮会话·同意+五个实施含义）：

1. **同意，理由=比较优势**：路由/意图/loop 是巨头商品化竞赛（个人参战必输·R4 实证）；口径/范式/资产是私有复利。「只能做规则」不是退让——**规则工作（契约/schema/口径/脱敏/审批）就是平台本体**（R1 claude组 C1-C7=平台宪法）。
2. **五个实施含义**：a) 产品入口空窗——冻结现有 QA 作演示+中期薄入口（通用 FC over MCP 工具面）重建+dsh Web 场景S；b) **渲染 API=最大新工程**——样式/交互资产须接口化（style registry/render contract），否则资产锁死前端=重蹈 19 契约锁死路由的覆辙（一般化教训：**资产没有稳定对外接口就会被锁死在当时的入口形态里**）；c) 现行 CB 线大多直接转为平台资产（B3 口径注册表/C 线知识/G1 出口表格式=平台工作·无推翻只重排）；d) 治理逻辑转变——从「限制入口」到「把正道修得比野路好走」（MCP 工具体验须优于 bash 裸调）；e) M1/M3/M5(体内) 随裁定沉没；M2 存活但变身（schema 接地为 MCP 工具面服务）；G6 降权为平台 eval。
3. **最初直觉的正确实现**：用户的「合体」设想在形态3中以协议级方式实现——dsh 与所有 harness 成为平台一等消费者（当初反对的是运行时级寄生）。
4. **下一步建议（待指令）**：出「形态3 路线图讨论稿」（平台资产盘点→工具面+渲染 API 设计→迁移顺序→产品入口策略）交三组终审。

**状态**：E4 用户倾向形态3（待正式拍板+三组终审）；待拍板全集=D1-D4+M5(转体外)+E4+路线图开轮。

---

## R7（2026-08-17 深夜）讨论纪要落盘 + 三组再邀

- 用户指示：还需进一步讨论、**不急着落盘 plan**；将讨论原文全部落盘成**讨论纪要**（授权润色·尤其用户发言）；出 prompt 邀 claude组+Codex 加入；**dsh组 另给专项 prompt**（用户明确：dsh=纯 harness 平台·可做任何嵌入工程 plugin 形式·未来将用 dsh 与 EMC 合体）。
- 产出：`EMC-dsh整体合体_讨论纪要_zcode-2026-08-17.md`（R0-R6 完整叙事·三组背景必读·含待决清单七项）+ 双 prompt（claude/Codex 联合评审 + dsh 侧合体工程专项）交用户投递。

---

## R8（2026-08-17 深夜）形态3 评审轮回收 + 提交 Codex

- 回收：claude组 两份（回应紧凑版 `641af085` + 评审报告详细版 `4d162903`）；Codex 一份（**v2 覆盖旧稿**·含对 claude 四点质证·原未提交由 zcode 代提交）；**dsh组 专项回应缺盘**（全仓双搜索无果·五问悬置·处置二选一待用户）。
- 结论：**E4 两组一致「成立」**——claude 三前置（C11 范式迁移/演示用 QA 冻结壳/冻结≠放烂）·形态2 被吸收；Codex 全收并工程化（C11 三层可操作化/G8 拆 G8a/G8b/敏感分层 T0-T3/薄壳 2d 三前提/渲染三步 1.5-2.5d·交互不进 v1 契约）。七议题全部有结论·交叉质证全吸收·无对抗分歧。
- 提交件：`EMC-形态3平台化_回收汇总_提交Codex_zcode-2026-08-17.md`（对照表+质证链+缺盘处置+附录 A/B/C 三份原文全量）。Codex 任务=收敛预备（拍板包）·零实施不出 plan。

---

## R9（2026-08-18）外挂大脑链路轮（朋友思路）

- 输入：用户朋友实践「Codex 经 MCP 遥控 dsh」（dsh 自写 Host 插件暴露 start/send/status/stop·Codex 侧薄 MCP 转发·七条守卫纪律）→ Codex 评估落盘发起文档（定位=哑入口外挂 dsh 大脑·与形态3 正交·消费 G10·排 G10 后·第 0 步=确认插件成品）。
- zcode 回应已落盘 `EMC-dsh外挂大脑链路_回应_zcode-2026-08-18.md`：附议双定位+三增量——①「大脑端口」契约化（协议归 EMC·dsh 为第一驱动·breaking 隔离在适配器层）；②「A 是按钮不是路由」（防重建意图分类器·对话框默认唯一走 B）；③第四技术前提「审批语义透传」（headless 下 dsh ask 审批门三档裁定·禁全 allow 默认）+安全增补⑩⑪⑫。D1=双定位附两前提·D2=朋友成品评审优先否则 dsh组 合并 B 卷新写·D3=独立小轮。
- **回收完成（同日·Codex）**：三组回应齐（claude S1-S7 宪法 C12 / zcode 三增量 / dsh组 源码实测三前提全证实）——**零对抗分歧**；Codex 抽验 dsh 断言 3/3 属实并新增两发现（ACP 会话拒挂外部 MCP→G10 须走 profile 全局配置；ACP request_permission 机制承接审批语义）；**Codex 立场修正**：B 变体从「可排期」降「实验保留」（三组一致更保守·接受）。收敛落盘 `EMC-dsh外挂大脑链路_回应与收敛_Codex-2026-08-18.md`：D1=双定位·B 实验保留（转正五条件集）/ D2=剩用户一问（朋友成品是否存在→评审或 ACP 底新写 1-2d）/ D3=独立小轮。附带收获：B 卷① 事实底座随 dsh 回应交付。
- 其他在盘：`EMC-形态3平台化_拍板包_Codex-2026-08-18.md` + `EMC-dsh合体讨论_通俗收敛报告_2026-08-18.md`（应用户要求通俗版·已按四问结构收敛）。

---

## R11（2026-08-18 晚）zcode 接棒：收敛收尾三件套

- 输入：打包索引（Codex 移交·33 文件+8 必读）。产出三件：
  1. `EMC-dsh专题_全链一致性核查报告_zcode-2026-08-18.md`——12 项不一致（3 类 dsh 事实残留/审批语义两时点矛盾/25-22-35 分数出处待查/缺盘表述过时等），**全部注记级·零方向性矛盾**；修法=勘误注记随 CB-41 2-0 批执行。
  2. `EMC-dsh专题_统一拍板清单_zcode-2026-08-18.md`（**最重要产出·已请用户作答**）——六处旧清单合并为 11 项四组（方向排期 3/插座标准 5/收尾 3）+3 项自动执行知会+一句话作答模板；抽验五键与五项决策同物已合并。
  3. `CB41-EMC平台化转型_实施计划草案_zcode-2026-08-18.md`（v0.9）——三阶段全量口径（11-14 工作日·与拍板包表口径关系已注明）；新增 2-0 勘误归档批；精髓十条嵌入表零新增批；B 变体转正判据附录；转正条件=拍板清单 1/2/11 项。
- 状态：**等用户作答统一拍板清单**→CB-41 转正（首日=G8a+勘误批+B 线续推）。

---

## R12（2026-08-18 晚·用户↔zcode）dsh 接入后的可见性之问

- 用户问：dsh 接入 EMC 后在哪里体现？我怎么在情绪地图项目中看到 dsh 的存在？
- zcode 答（全文见当轮会话）：**几乎看不到，且是刻意设计**——合体发生在「插座+使用」层，不发生在「房子」层。三个可见场景：①你打开 dsh 用七件工具干活（主要场景·存在感在 dsh 界面里）；②简易接待室自由任务背后是 dsh（观察项·EMC 界面内唯一痕迹=结果上的 brain:dsh 溯源标签）；③dsh 网页版直接展示地图（场景 S·观察项）。EMC 仓库内痕迹仅：考试档案里 dsh 作消费者的实测数据+goal-status 观察条目——**零专属代码**（当初否决整体寄生即否决"dsh 住进 EMC 家门"）。类比：商家贴收款码≠商家装了微信；USB 不绑定任何设备。

---

## R13（2026-08-18 晚·用户↔zcode）可见性澄清补充：dsh 是可选项不是必经路

- 用户质疑 R12 场景①「打开 dsh 干活」：情绪地图项目中有 dsh 吗？是不是应该是 EMC？
- zcode 澄清：①项目里确实**零 dsh 代码**（R12 原话一致·无分歧）；②R12 把「你自己的工作方式（任选通用助手·dsh 四选一）」当主场景讲，易误读为「干活必须去 dsh」——修正表述：**产品=EMC 界面（永远无 dsh）；个人工作台=四家任选（dsh 可一次不用）**；③dsh 真实地位=引子+四个顾客之一，明天消失 EMC 一行不改（设计目标）；④活证据=本场对话（zcode 正在消费情绪地图仓库=平台消费场景实例）。

---

## R14（2026-08-18 晚·用户↔zcode）EMC 去向之问

- 用户问：那 EMC 用作干嘛？作废吗？
- zcode 答：**不作废——三段去向**：①现在~参赛=冻结演示壳照常演示（只修致命 bug）；②零件拆件上架（契约层→插座 schema 源/范式知识→转生知识库资产/知识问答组装与提示词→rag_query 复用/图层对象与样式函数→图纸化七成材料·服务对象从「自己的问答界面」扩为「所有助手」）；③参赛后=简易接待室（2 天版）接班当产品入口，老面板退役归档（资产已全部搬走）。唯一停的是「路由聪明化」投入（M1/M3/M5·本来也未做完）。类比=老店改造：店面营业到比赛，后厨升级中央厨房供给全城，赛后换新前台。

---

## R15（2026-08-18 晚）PT-CB1 正式开工（新协作体系）

- 用户令：正式开工·新结构五条（zcode 主手=设计/拆解/派发/攻坚；dsh 副手=融入类小任务；Codex+claude=审计协助；每轮回收→收敛→拆解→派发→执行；每轮日志同步+自动 commit+push）；**重启 CB 命名=任务代号+CB 轮次**。
- zcode 执行：PT-CB1 开工档（拍板生效记录/角色矩阵/首轮 T1-T4/纪律）+ 命名体系落地（旧 CB 封存·CB-41 撞号化解·CB41 草案更名转正为 PT 总计划 v1.0）+ 三日志落账（cb-index/cb-journal/goal-status 含产品入口维度首更与 G6-G9 改道）+ 分支并轨（EMC_harness_dsh→main 快进·实施回归主干）。
- 派发：T3a/b → dsh（勘误批 5 文件注记+干净环境设计稿·prompt 交用户）；T4 → Codex+claude（审计章程+B6 出处+拆解异议·prompt 交用户）。T2 G8a 由 zcode 紧接攻坚。
- **用户修订（同晚）**：dsh 任务定位从「聚焦 dsh 融入类」升格为**通用执行手**——一切非长思考、不过于复杂的任务（含编码类·其代码能力强）皆可派 dsh；主手派发判据=长思考/复杂度（融入类仍优先 dsh）。开工档角色矩阵与 cb-index 已同步修订；影响=第 2 轮起 B 线数据治理批等将有更多任务量可派 dsh。

---

## R16（2026-08-18 深夜）PT-CB1 T3/T4 回收收敛

- 回收：dsh T3（五文件勘误+干净环境设计·自 commit+push ✅·验收通过）；Codex T4 审计（零 git 写·文件由主手归档）。
- **B6 反转**：Codex 查实 25% 出自 CB-40 发起文档（zcode组著 L12/L54）——主手「未打 25 分」声明被推翻，检索范围遗漏平行轮，认账补注；方法教训=跨轮归因必须全仓检索。
- 采纳：三条加固（文件白名单下沉 prompt/任务外命令留痕/office 双云提示）入开工档纪律；**基线重录 372+3**（T2 验收参照更新）；指定项清单 owner=zcode 挂 goal-status；审计文件归档流程固化；T2 审计位章程（五动作+四打回触发）。
- claude组 休息缺席（单组过·T2 回归）。`.dsh-meow/` 入 .gitignore。
- 状态：T2 G8a=主手下一动作（白名单：tool_contracts.py+派生目标+CI 断言）；T2 完工回口 PT-CB1 → PT-CB2（B 线拆解）。

---

## R17（2026-08-19 早）分支策略纠正 + T2 完成

- **用户纠正**：实施应在分支上·不在 main——主手 08-18 晚 T1 的「快进并轨+实施回归主干」为越权误判（主手认账）。处置：EMC_harness_dsh 快进至 3dc5db18 后切回为实施分支·T2 未提交改动无损随行；main 停止直推（已被推至 3dc5db18·纯增量：讨论文档+08-17 数据工作+CB1 记录·是否回退 main 由用户裁定·建议不回退（回退需 force-push 共享分支·风险大于收益））；三文档分支表述更正（cb-index/开工档/PT 总计划）。
- **T2 G8a 静态契约派生完成（分支上）**：新增 `tools/gen_stages_mirror.py`（单一源→前端镜像生成器）+ `frontend/js/ai_qa/contract_mirror.generated.js`（SKILL_DEFS 18 技能+TOOL_ALIAS 15 工具·@generated 禁手改）；stages.js 手写 SKILL_DEFS/双层别名表永久退役（改 import+re-export·normalizeParams 全按工具派生别名——顺手根治旧通用层 `field_name→'field'` 无主映射漂移 bug·lookup_place.name→q 归位）；守卫升级：真身解析改指向生成文件+新增 mirror_freshness（派生 diff=0）/tool_alias_derived/guard_fields_report（paradigm 红线保护区差异报告·不 fail）三断言；validate_generate_point_layer 正则适配生成文件。**验收：375 passed+3 skipped（基线 372+3+新增 3·零退化）·零新增追踪 ID**。node --check 双 JS 通过。
- T2 待 Codex（+claude 如回归）按审计位章程复审（读 diff 对照白名单/验 freshness 断言/跑基线/核零新增 ID/落盘报告）。

---

## R18（2026-08-19 早）main 冻结裁定 + 全组切分支通知

- 用户裁定：**main 先不要动**（不回退·不推送·维持 3dc5db18 现状）；**通知各组切入分支 EMC_harness_dsh**——尤其 dsh（其 08-18 T3 两提交 `4396030f/33ef7614` 时落 main·已随快进无损收编进分支·需切分支并核对本地状态）。
- 通知 prompt 已交用户投递（含 dsh 专项核对 + 审计组「只读也在分支上读」要点）；T2 审计 prompt 一并交付（含分支指向+白名单+基线口径 375+3）。

---

## R19（2026-08-19）PT-CB1 回口 + PT-CB2 开工

- 回收：Codex T2 审计（零打回·五动作全绿+旧别名 26 条覆盖核验+两行为变更判修复）；dsh 切分支回报（口头·其 T3 提交已在分支核验）。
- 观察处置：①guard 字段差异冻结为 8 项清单断言（validate_skill_params·清单外新漂移或清单内消失均 fail——红线保护实体化·7 passed）；②freshness 幂等确认（无需处理）。
- **PT-CB1 回口**：T1-T4 全销号（journal 已标）。
- **PT-CB2 开工**：B1 批（spec=CB39 计划 §四 B1）——T1 manifest 注册四件套+usage 字段（dsh·prompt 交付用户）；T2 usage 白名单消费点（geo_routes+toolbox·zcode 攻坚·依赖 T1）；T3 验收包（zcode）。B2/B3 留 PT-CB3。开工档落盘。

---

## R20（2026-08-19 凌晨）PT-CB2 T1 回收 + 收工交接

- 回收：dsh T1 验收通过（manifest 5→8 组/38→57 层/19 新注册/usage 45+12 全覆盖/JSON 可解析/pytest 375+3 稳定·主手独立复核一致）；执行记录质量高（含待裁决四项与存量问题发现）。
- 主手四裁决（详见执行记录§五）：12345 聚合面维持 input（留 B3 口径复核）/三结论层补标 analysis_output 采纳/18村单注册采纳/qty_民生_公服设施 存量路径 bug 已修（PT-CB2(T1b)）。
- 用户裁定：**审计顺延至下轮开工**（凌晨收工）；上午 office 继续——**续点=PT-CB2 T2（usage 白名单消费点·geo_routes+toolbox·zcode 攻坚）**，T2 完工后 T1+裁决+T2 一并送审。
- 收工交接：HOME.md/session-handoff/todo.md/cb-index 更新；hub 盘在 office——到岗后 `git push hub --all` 补推+一键到达。

---

## R21（2026-08-19 晚·home）PT-CB4 T1 对账裁决完成

- 输入：dsh T1 证据包（5 对四证据）+ E 总账对账（qty 双头/孤儿 25/账内缺失 10）。
- 主手独立复核：组1/2 行集 diff（137⊇126·137⊆193·伪社区行）、组3 字节 diff（仅 BOM）、qty 11 对 md5（3 同 8 异）+公服 README 口径破案（1068=957 误计旧口径）。
- 裁决六组（表入 `_总账.md` 文末）：三份 perf 旧版→`_retired/`；两份《分析汇总》superseded 标注；qty 全覆盖 11 件=权威源登记。
- **挂起待用户①**：presets qty 物理同步批（8 类旧内容+公服误计口径在前端加载位·建议独立小批派 dsh·白名单 presets/）。挂起②：E 其余孤儿/账内缺失→总账补登记批（B4/R1）。
- 验证：活引用 grep 零（生成器模板已同步）·pytest **389+3 零退化**。
- 下一步：T2 口径注册表（吃 T1 裁决表+作废清单）→T3 check_caliber 派 dsh（F_020 主手先取号）。

---

## R22（2026-08-19 晚）用户重心纠偏：契约=资产·数据内容=耗材

- **用户纪律（长期生效）**：不要纠结具体数据内容（小区/社区/范围核对等）——数据多是一次性、分析完即弃；**真正的"数据资产"=格式/schema/字段/规则/范式/口径的标准卡片（"需求契约"）**——从数据工作中总结归纳契约、指导后续开发、承接 dsh 精髓接入 EMC。记录数据没问题但不是工作重点；勿在行级核对上花大量时间。
- **主手认账**：T1 对账裁决的重心偏了——行集 diff/伪社区/BOM 等内容核验占了主要精力，正确姿势=以最小核验定谳+**立即转译为契约条目**（T1 六组裁决已补转译为六条契约·见 R21 裁决表注记）。
- **队列调整**：降级=presets 同步批与孤儿登记（机械批·全权 dsh·主手零时间或延后）；升级=T2 改型为「需求契约卡体系·首批=口径契约卡」；**此后每批 DoD 增一条：本批产出哪些契约条目**（无契约产出的数据批视为低价值）。
- 关联：PT-CB3 终收敛 v2（EMC=契约集合体·四类+两补充）即该思想的体系化——用户此轮把执行重心拉回该主线。

---

## R23（2026-08-19 晚）PT-CB4 T2 需求契约卡首批

- 产出：`docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md`——元契约（卡片七必填字段）+K-01~K-04 现行口径卡+K-C1 社区六口径枚举卡+X-01 作废数字卡（87.9%/5,615/港务1,153/双高系/公服1,068/含村旧版）+G-1~G-6 数据治理规则卡。**契约产出 12 张**（R22 纪律首达标）；值锚点全部指向成文 md 出处·不内联数据。
- 种子对账：CB39 B3 四件种子全覆盖（page2 §3.3=K-01+X-01 双口径=K-02 社区对照=K-C1 作废清单=X-01）+08-17 增量吸收（K-03 两表/K-04 全覆盖/154 枚举）。
- 下一步：T3 check_caliber 派 dsh（读注册表 X-01/G 卡→扫蒸馏素材→命中 fail；**F_020 号已为主手预留**·派发单随本轮交付）。

---

## R24（2026-08-19 深夜）PT-CB4 T3 回收 + 三件齐送审

- T3 验收通过（主手独立复验）：工具实跑 6 命中+TRACE F_020 出站埋点工作；F_020 import+@track 双注册合规；白名单三文件零越界；**396+3**（+7 测试零退化）。
- 主手裁决：6 命中=**已知合规基线**（历史/演变记录内带作废声明提及·登记注册表§五；门禁真对象=蒸馏素材·C2 时执行）。
- PT-CB4 三件齐：T1 对账裁决六组+契约转译 / T2 契约卡 12 张 / T3 工具+测试。**送审 prompt 交付用户**（Codex 主审·claude 如回归附审）。

---

## R25（2026-08-19 深夜）PT-CB4 回口

- Codex 全轮审计：**通过·零打回**（裁决抽验属实/锚点 3/3/F_020 合规/基线成立/396+3/R22 执行评优）。
- 三观测登记下批：①test_manifest_gate.py GBK 编码自适配（0.1h·dsh）②注册表 K-C1/X-01 补显式值锚点③check_caliber v2 变体。
- 学习线：**Cordis 讲义 8/8 结课·检查题全过**（用户 home 完成·进度更新已入库）。
- 阶段一：B1 修复完成（**复审 prompt 仍待用户转发**·office 遗留）；B2/B3✅；B4 并 C 线。**下一轮 PT-CB5=C 线知识入库**（图层说明书卡/事实卡/门禁执行/重建索引五问——R22 重心主线批）。

---

## R26（2026-08-19 深夜）用户裁定二：架子先行（排序修订）

- 用户问：一定要先把城市体检数据治理完吗？——自答：数据多一次性·治理价值=RAG/归因支撑·非当前重点；**先搭整个架子·再丰富 RAG**。
- zcode 评估：成立且时机正好——阶段一的**前置性治理已恰好完成**（B1 usage 字段/T1 对账/T2 口径契约卡/T3 复核工具=全是契约侧）；剩余 C 线=RAG 充实（管子先通水后加压）；G8a 已完+G8b 依赖已备 → **插座批即刻可开**。排序修订入总计划头部。
- 新 PT-CB5=插座批：G8b 动态枚举收尾（0.5d）→G10 七工具+三标签（<1d·caliber 用 T2 卡/schema 用 G8/三标签吸收抽验 R1-R3）→插座考试（0.5d·真实任务 bash vs 插座）。C 线（知识入库/图层卡/五问）后移·挂 goal-status；B1 复审转发/presets 同步/GBK 顺修并行尾巴。

---

## R27（2026-08-19 深夜）PT-CB5 开工 + T2 G8b 完成

- 开工档落盘（T1-T5 任务表+审计位）；R26 架子先行裁定执行。
- **T2 G8b 动态开卷定参完成**：tool_contracts 增 derive_dynamic_enums（geo_registry 点层+manifest usage=input·双源兜底）+contracts_to_tools_schema(live_enums=True) 注入（手写枚举不覆盖·源缺失静态兜底·diagnose prompt 文本零接触）；5 测试（枚举非空/**铁律7 硬断言·analysis_output 零漏入**/注入/兜底/静态开关）；**401+3 零退化**（期间自查修复：编辑误删 _PARAM_RANGES 即时补回）。
- 契约产出：开卷参数契约（_SOURCE_ENUM_PARAM 映射+G-2 过滤规则机制化）。
- 待办：T3 G10 七件插座（zcode 攻坚）→T4 考试；T5 dsh 顺修包（GBK/锚点字段/v2 变体·派发单交付用户）。

---

## R28（2026-08-19 深夜）PT-CB5 T5 回收

- dsh 三件全过（独立复验）：T5a manifest gate GBK 自适应解码（subprocess 字节级 utf-8→gbk→replace 三级）；T5b K-C1/X-01 补显式值锚点 7 处；T5c check_caliber 全角数字/逗号/句点/百分号归一化 v2（+2 测试·实跑仍 6 真命中零新误报）。四 commit 各自白名单内零越界。
- **403+3**（+2 上浮已注明）。三条 Codex 审计观测全部销号。
- 待办：**T3 G10 七件插座**（backing 锚点已备：aiqa_routes:31/92/112+spatial_analysis:237/403+buffer_analysis:18+契约卡+G8b 枚举）→T4 考试→送审。

---

## R29（2026-08-19 深夜）T3 G10 改派：主手设计+副手执行

- 用户令：开工 G10·zcode 专注写详细计划·具体执行交 dsh。
- zcode 执行：核实五 backing 锚点真身签名（aiqa_routes post_outlet_card/rag_search·spatial_analysis.aggregate_by_polygons:237·buffer_analysis.create_buffer:18·geo_registry resolve 族）→ 派发单落盘（七工具逐一规格：输入 schema/三步 backing 链/caliber 对象逐工具映射 refs 指向 K 卡/体积纪律 rows≤20/依赖 mcp/测试 8-10 用例/五判据答辩/F_021-F_027 连续取号/白名单四文件/红线=纯只读+签名不符即停）。
- 设计决策（v1 诚实降级两处）：rag synthesize=True 不调 LLM 返 guidance+deferred_v2 标记；buffer fc 仅小要素返回否则 fc_omitted。

---

## R30（2026-08-19 深夜·用户↔zcode）MCP vs FC 之问 + 零自有 FC 设计倾向

- 用户问：dsh 经 MCP 接入后能否减少/禁用 FC？「MCP vs FC 选 MCP·更极致·FC 效果差（意图识别→选工具死循环）」。
- zcode 答：①**概念澄清**——MCP（供货协议）与 FC（宿主点菜机制）非对手·是上下游；dsh 调 MCP 工具内部仍走宿主 FC。用户的真实选择=「宿主循环 FC（多轮自纠开卷）」替代「EMC 单发闭卷 FC」——即形态3·已在执行。②**账目更正（不附和）**——B001/B003 死循环是开放 ReAct 的病（FC 太自由）；EMC 内部 FC 的病相反（单发不恢复）；勿误伤即将依靠的宿主 FC。③落点：内部 FC 已冻结停投·参赛后随壳退役（接班者上线日=退役日）；G8b 动态枚举恰治「FC 效果差」实测病根（参数闭卷·CB-08）；**新设计倾向记录：薄壳 v0 产品内零自有 FC**——A 变体只做按钮直连（无对话框零意图判断）·对话入口外包宿主·B 变体转正后再补——待薄壳批开工正式裁定。

---

## R31（2026-08-19 深夜）PT-CB5 T3 G10 回收通过

- dsh 按派发单执行（含两处纪律执行佳例：签名不符即停挂裁决标记/2.0 破坏性变更主动锁版本）；主手裁决②采纳 mcp>=1.0.0,<2.0.0。
- 独立复验：**414+3**；build_server 七工具注册齐（list_data/rag_query/kb_facts/outlet_card/zonal_stats/buffer/rank）；两 commit 白名单零越界；零 key 泄漏。
- **七件标准插座正式上线**（v1·只读·caliber 四键·体积纪律）——形态3 的物理载体落成。执行中 WIP 误扫事件已随终版提交自愈。
- 待办：**T4 插座考试**（zcode·真实任务 bash vs 插座对比·及格线五条）→ PT-CB5 送审。

---

## R32（2026-08-20 凌晨）PT-CB5 T4 插座考试完成

- 真实任务「行政区情绪最差3区+口径」双路实测：老路 6 轮（含 BOM 真实返工·口径标签 0）vs 插座路 2 轮（caliber 自动+TRACE 溯源·零错误）——**轮次 -67%·五条及格线全过·双路结果交叉一致**（伍家岗/猇亭/西陵同序同值）。「正道好走」首份基线证据入档（`PT-CB5-T4插座考试_zcode-2026-08-20.md`）。
- 考试发现：F1 list_data 点层漏报（P1·仅返1层而可解析9层·派dsh修）F2 首调冷启动 8-10s（预期已声明）。R22 契约产出=考试契约固化（五条+双路法+保守下界声明义务）。
- PT-CB5 全件完成（T1/T2/T3/T4/T5）——F1 修复后整轮送审。

---

## 附：用户指令记录（累计）

| 日期 | 指令 |
|---|---|
| 2026-08-16（晚） | 开分支 EMC_harness_dsh；对「整体寄生」设想做详细客观评估；生成讨论报告落盘；邀 dsh组+claude组+Codex 三组讨论 |
| 2026-08-16（深夜） | **回收评估，不要执行（除非明示）；任务将多轮讨论；讨论过程与内容全部落盘，供双环境 context 管理** |
| 2026-08-17（R2） | 拍板前先讨论：方案B 对我目前有哪些提升、核心改变在哪（澄清轮） |
| 2026-08-17（R3） | 讨论核心点纠偏：真痛点 = EMC 入口聪明度不足（意图→方法+工具组合路径不聪明·拆东墙补西墙） |
| 2026-08-17（R4） | 天然对照实验之问：通用 harness 不调优就能做空间操作，EMC 调优多次仍错——差别在哪？ |
| 2026-08-17（R5） | 存在之问：dsh 比 EMC 做得好，那做 EMC 的意义是什么？只是展示在地图上吗？ |
| 2026-08-17（R6） | 裁定倾向：形态3（平台化）——资产可复用；深挖路由=死路；通用 LLM/harness 上搭架子 |
| 2026-08-17（R7） | 不急落盘 plan；讨论原文落盘成纪要（可润色）；出 prompt 邀 claude/Codex；dsh 另给专项 prompt（未来用 dsh 与 EMC 合体） |
| 2026-08-17（R8） | 三组已完成；把讨论过程、原文与三组落盘情况全部落盘成详细文档，提交给 Codex |
| 2026-08-18（R9） | 外挂大脑链路三组讨论（朋友「Codex 经 MCP 遥控 dsh」思路）——zcode 以应答组身份参与 |
| 2026-08-19（深夜·R30） | MCP vs FC 之问：选 MCP 更极致·FC 效果差——答：上下游非对手+零自有FC倾向记录 |
| 2026-08-20（凌晨·R32） | 继续开考 T4——插座考试完成·五条全过 |
| 2026-08-19（深夜·R29） | G10 开工·zcode 写详细计划·执行交 dsh |
| 2026-08-19（深夜·R27） | 开工 PT-CB5 插座批 |
| 2026-08-19（深夜·R26） | 裁定二：架子先行——先搭架子再丰富 RAG；数据治理不必先行 |
| 2026-08-19（晚·R22） | 重心纠偏：数据内容≠资产·契约卡=资产；勿在行级核对耗时；每批须产出契约条目 |
| 2026-08-19（凌晨） | dsh 已完成 T1→回收记录·审计下轮；凌晨收工写交接卡更新进度·上午 office 继续·保持记录连续性·commit push |
| 2026-08-18（接棒） | 材料打包交 zcode 推进下一步：一致性核查/统一拍板清单/CB-41 预案三件套；完成任务②后即告知用户 |
| 2026-08-18（收工） | 用户反馈报告太技术看不懂·要求通俗语言收敛总结（四问结构）；三组已完成→回收+收敛；**收工落盘+commit push·明早 office 环境**（E7 换绑 origin=Gitee 待办） |

---

## R10（2026-08-18 晚）harness 精髓提炼 + 材料打包交 zcode

- 用户提供 dsh 源码仓（github.com/deepseek-ai/deepseek-harness·本地 `D:\Github\dsh`@015a8d74fc），指示「先尝试把 dsh 接入 EMC·重点是 harness 精髓如何在 EMC 中发挥作用」——纯讨论。
- Codex 实读 5 份架构文档+56 包清单，提炼**十条精髓**（模型可见即记录/能力接缝三元组/一切皆插件/工具五道闸流水线/轮次生命周期/上下文压力管理/子代理接口/Profile 组装/事件三域/防御模式七条），落盘 `EMC-dsh接入_harness精髓_讨论纪要_Codex-2026-08-18.md`——**三种落法**（本体吸收/平台资产化/语义参考）·与既有裁定零冲突·排期零新增。
- 同日用户要求先看详细计划：Codex 产出 `EMC平台化与主线治理_总开工计划_通俗版预览_Codex-2026-08-18.md`（三阶段 11-14 工作日·五项决策与计划关系）。
- **用户指示：全部材料打包交 zcode 推进下一步**——本索引+台账更新+commit push；zcode 任务=一致性核查/统一拍板清单/CB-41 预案（三件套）。

> 台账维护：zcode组·每轮更新（PT 期兼记 PT-CB 轮）。最后更新：2026-08-20 凌晨（R32=T4 考试完成·F1 修复后送审）。
