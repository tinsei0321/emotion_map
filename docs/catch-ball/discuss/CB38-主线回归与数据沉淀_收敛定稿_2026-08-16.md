# CB-38 · 主线回归与数据沉淀 · 收敛定稿（2026-08-16）

> 收敛方：claude组（反评价汇总）。参与：Codex（主开发）+ zcode组 + dsh组 三组评估齐（`discuss/CB38-*_评估_{组}-2026-08-16.md`）。
> 性质：**定稿=实施计划的唯一输入**。本轮零实施；Codex 据此出实施计划（CB-39 预检）后开工。
> 关键核实：三组关键指控已由收敛方逐条实测复核（9 份 md 字节级重复 / manifest 404+红线注释 / `街道`术语债 / range tooltip 只显图层名 / presets 项目层已注册——全部属实）。

---

## 〇 一句话结论

三组方向高度一致（数据沉淀框架成立、RAG 单库+domain 标签、geojson/xlsx/csv 禁入 RAG、时间轴真实时间点离散呈现）；收敛裁定四处分歧（page7 中间产物**归档不删**、T1~T3 **分轴并存**、B002/B004 升 **P0**、办件编号 RAG 层丢弃）；**E1 根因已由发起方实锤**（B012 格心归属·三组评估期间独立预判的嫌疑①获证）；新增统一编号 E8-E15 + N1-N7 补充发现全部入账。

---

## 一 事实修正（发起文档认错·三组实测采纳）

1. **「体检 fact 67」错**（Codex+zcode 双实测）：67 = fact **总量**；体检域 `CHK-*` 仅 **15 条**、更新域 `URP-*` 52 条，两域混装于 `urban_renewal_knowledge.py`。体检域底子比发起文档估计薄 4.5 倍——沉淀增量价值更大。
2. **「更新 183 项目在 DATA/ 其他位置」半错**（Codex 对 zcode 的表面矛盾作合并裁定）：**图层侧已就位**（presets 已注册 `checkup_project_point` 183 项目 185 点 + line 102）；repo 内**无**原始十五五 excel 清单（在中转站·zcode grep 实测）。沉淀缺口只在 RAG 侧（现仅 URP-P* 12 条 fact）。
3. **盘点补全**（zcode）：根目录 5 文件补入 1.1 表——`12345_安全/民生基础_西陵伍家点`（page4/6 活跃依赖·commit `747e0dba`）+ `12345_西陵+伍家岗`（社区级聚合面）→ 主观轨；2 xlsx（体检点类型来源对应/图数表汇总）→ 体检专题口径元数据。
4. **93 文件已全量入 git**（Codex）：删除/归档操作安全，git 历史可恢复。

---

## 二 F1-F5 定稿

### F1 留/删/再处理（逐族定稿）

| 数据族 | 定稿 | 关键细则 |
|---|---|---|
| 空间底座（根 12·补全后归图层层） | **留→图层体系** | 权威源=`DATA/boundaries/presets/manifest.json` 单点注册；根目录副本标 `snapshot` 防双头权威。**社区两口径并存须显式标注**：`体检对象_社区_面`=193 含村(SQMC) vs `checkup_cfg_community`=174 去村(社区)。zcode 补：底座移入 boundaries 新「城市体检底座」group（移前盘点 SCRIPT/md/xlsx 引用）；18村面/合并范围/聚合范围174 = 结论层标 `usage: analysis_output` 禁作输入（铁律7） |
| 体检专题 | **留→体检域** | **9 份占比表 md 已字节级存在于 `3prime/`（实测）——DATA/analysis 副本禁再入 RAG 防双倍相似度**；`汇总/` page1-7 md×7 + 盘点总表 = 真增量黄金素材；旧层双版本 md（安全/民生的分析整合/汇总）标 superseded 或不蒸馏；17 项宏观率值→fact；楼栋质心只进图层 |
| 主观轨（12345·含根 3 补入） | **留→体检域主观子轨** | 点层进图层（presets 待补注册）；分布表 csv×6 蒸馏口径 fact；素材保留「12345=情绪地图主观轨真实数据源」身份表述（dsh） |
| page7 产物 | **归档不删（裁定：采 zcode 弃 Codex/发起方「删」）** | **裁定理由**：答辩回溯窗口内不可逆删除风险 > 仓储成本；CB-26→35 口径演变链在中间版本里。落法：建 `DATA/analysis/page7小结/_retired/` 归档（v2/v3 xlsx、预览 csv×2、preview 脚本×2）+ `retired.md` 台账登记。**`render_page7_bivariate.py`+双变量主图 png 不转正、退役登记**（裁定：采 dsh 弃 Codex 转正——产物是合并双高 26 旧口径，转正必误导，CB-34/dsh 两轮报过）；最终版 xlsx 补「最终版」命名后缀（E9）。面版（presets page7_*）与点版（analysis）两口径都留、用途注明 |
| 成文分析（汇总/） | **留=RAG 黄金素材** | 拷贝 `docs/urban-renewal-plan/00-宜昌专项/`（`_load_notes` 自动扫·不以下划线开头）+ 头部口径块 + 蒸馏体检域 fact；**整篇 note 保口径链脉络 + fact 抽高频数字双形态**（dsh：切碎丢演进脉络） |
| 更新专题 | **留→更新域** | 图层已就位；RAG 侧补项目库存在性/字段口径 fact + 锚点项目（望洲岗）+ outlet_kb 出口卡映射标注；**明细不进 RAG**（易过期·dsh） |

### F2 RAG 数据层级（三组一致+存量债）

- **定稿：物理单索引 + meta `domain` 字段**（三组全 agree·枚举 `checkup`/`renewal`/`base` **三域封顶**·采 zcode 防域爆炸；Codex 的 methodology 不设域，方法论走 concept 类型）。域信号 fact 前缀已有（CHK-15/URP-52），加标签是规范化非造概念（术语纪律）。
- **存量回溯债（zcode 独立发现·采）**：现有 214 条 note 中 03-05~03-08 是体检域、00-01~00-06 是更新域——分域首先是一笔存量标注工作量，实施计划必列，否则分域只对新数据生效。
- 落地最小改动（Codex）：`_load_notes` 按目录推断 domain + 新建 `ai_qa/outlet_kb/urban_checkup_knowledge.py` 承接体检增量（不动存量 67 条·避免大迁移）+ `search(query, k, domain=None)` 可选过滤。
- 四类型（fact/note/case/concept）**够用不造第五类**（三组一致）。

### F3 大文件边界（三组一致+硬化）

- **定稿：geojson/xlsx/csv 原始/半原始数据一律禁入 RAG**（zcode 补 csv·统一准入规则=只收 md 成文与结构化素材卡）。
- **分工契约成文**（Codex 三表 + dsh 路由句）：图层体系=空间明细（在哪/有多少）· RAG=知识口径（是什么/为什么/口径多少）· EMC 按问题类型路由（CB-22 分类→范式映射延伸）。
- **图层卡片桥接**（dsh·采）：每个图层一张口径卡（名称/几何/字段/点数/口径/更新时点）进 RAG fact——RAG 与图层体系的唯一桥。
- **静态检查**（zcode·采）：RAG meta 的 source 出现 `.geojson/.xlsx/.csv` 路径即违规（机器防回潮）。

### F4 口径漂移防线（三层+两条纪律）

- **定稿防线**（Codex 三道 + zcode 注册表 + dsh 作废清单合并）：
  1. **口径注册表**（zcode）：指标→现行值/废弃值/生效日期/适用视图/依据文件；page2 §3.3 为种子；同时是蒸馏权威源与答辩武器。
  2. **沉淀前全量口径复核 pass**（前置门·复核不过不蒸馏）：grep 台账旧值命中即 fail 人工裁决。
  3. **素材头部口径块**：`caliber_status: current|superseded` + `caliber_date`，检索透传给 LLM prompt（被动兜底）。
- **作废数字纪律**（dsh·采）：5,615/87.9%/港务1,153 等作废值**不入素材正文**，集中进一条「作废数字清单」concept——检索命中作废声明而非沉默。
- **蒸馏源纪律**（dsh·采·承重）：**只认 DATA/analysis 最终产物与已成文 md，禁从 discuss/ 讨论稿取数**（过程态数字系统性污染源）。
- **test case**（zcode·采）：board 全量口径（安全 8,046/民生 49,192）vs 三层筛选口径（安全 4,800/民生 42,871）同名不同值并存——防线以此验证（「安全类诉求多少件」两个答案须带口径标签才不混算）。
- 社区四套口径（193/174/130/1682）对照必须进 base 域口径卡（Codex N4·E1 受害者）。

### F5 红线复核（三组实测+两条裁定）

- **PII**：实测干净（无姓名/电话·Codex/zcode 双核）。**裁定（收敛方）**：`办件编号` 不按 PII 对待（工单号非个人身份），但 **RAG 蒸馏时丢弃**（无用+防御性·图层层保留供 zonal）；**诉求原文文本禁入 RAG**（dsh）。
- **sim 禁入**：**source_path 白名单硬实现**（dsh E11·采）：仅 `DATA/analysis` + `docs/urban-renewal-plan` + `ai_qa/outlet_kb` 三处，白名单外拒收；RAG fact 的 source 禁指 `DATA/performance` 演示层。
- **铁律7**：结论层素材（page7 三组点层）可入 RAG 但**须标注结论性质+生成口径**（阈值 28.57/146.67 + TOP-N 截断·zcode）；结论不可逆推为输入口径。
- **新红线（dsh·采）**：「两板块=结论」——board 分类是本项目对标分类非官方分类，蒸馏注明，防 LLM 当官方口径输出。

---

## 三 EMC bug 清单定稿（统一编号·含根因宣布）

### E1 社区 tip 错误——**根因已实锤（评估期间由发起方完成）**

- **B012 主根因（已定位·buglog 已建）**：[tip-popup.js:433-435](frontend/js/tip-popup.js#L433) `fillCommunity(centroidOf(feat))`——**格心归属非指针位置**。用户五组样本全部相邻社区对、400m 网格跨界格归属翻转已模拟复现（`SCRIPT/_tmp_audit_b012_community_tip.py` 一键复现）。三组独立预判嫌疑①获证。
- **B013 新立（zcode 候选①·核实属实）**：[map.js:1004](frontend/js/map.js#L1004) `showRangeTooltip(lngLat, layer.name)`——**悬停社区面层显示的是图层名常量**而非命中要素社区名。与 B012 是两条不同显示路径，**都真都修**。修复方向：queryRenderedFeatures 取要素 nameField（manifest 已有先例）。
- Codex 嫌疑②（174 面 3 对重叠·金斗山×锦江 170,056m²=13%）→ 独立数据债 N5（zonal 重复计数隐患·修拓扑治本）；嫌疑③（口径错位）→ 并入 F4 口径卡。
- dsh「08-13 修过=复发」澄清：那次是 CB-23 **引入** tp-community 行（fillCommunity 首次落地），非修复复发——即该功能自诞生起就带格心归属缺陷。
- 修复排期：**方向 A（指针 lngLat 实时归属+节流/索引）**为收敛倾向，Codex 实施计划细化；B012/B013 同场修（一次浏览器会话验收）。

### E2 office 底图——纯色兜底升格演示默认

- 定稿：**自托管纯色底图（style.json 本地方案优先评估·zcode）为演示场景默认兜底**（参赛断网刚需·演示逻辑链地基）；GeoQ/SuperMap 验证 spike（WGS84 叠印比对·GCJ-02 一票否决）；Esri 中国镜像不采（无稳定承诺·Codex/dsh 一致）；瓦片缓存 P3 不优先（dsh：office 首访无效）；Codex 补：天地图 key Referer 白名单现场同查。

### E3 manifest 404——与任务③同批

- 实锤：`MANIFEST_URL='/DATA/performance/_time_manifest.json'` 不存在 + 旧 manifest 内 4 处 `/DATA/processed/` 死链（N3）。修复=manifest v2 再生成+路径统一。**红线（zcode·采）**：`time-source.js:21` 注释明示「再生成属数据红线（留用户）」——**manifest 生成须用户授权口径，Codex 不得自行生成**。

### E4 buglog 存量

- **B002/B004 finalStep 假结论复发：升 P0**（裁定·dsh 立场获采——CRIT 级伤演示可信度，排最前）。修复=复发用例锁定回归 + **结论白名单代码裁定**（图层名/数字必须存在于执行产物·编排器确定性铁律）。B008 P2 顺带；UI-09/PRED-09 与 E1 复现同场浏览器采集（一次收三条·zcode）。

### E5 登记债——**本轮已修 + DoD 化**

- dsh 立场获采：「顺手」=永远不顺手（已漂 15 轮·dsh 两轮报过）。**本轮收敛即补**（cb-journal CB-29~37 简记 + _cb-index 更新，见本次提交）；**定稿清单固定条目：每轮收敛必更新 _cb-index + cb-journal（DoD 化）**。

### E6-E15 统一编号表（三组补充合并·N 系并入）

| # | 内容 | 来源 | 级 | 处置 |
|---|---|---|---|---|
| E6 | tmp 脚本 9 个全清点（cb33 v1-v4 删·cb35 审计转正 tools/·cb36/37 待定·本轮 E1 诊断残留已清） | zcode 范围修正 | P3 | 清理批 |
| E7 | office Gitee 换绑（周一） | 发起 | P2 | 挂账 |
| E8 | geo_registry `_POINT_LAYERS` 硬编码 8 个 T1-T3 层——时间轴重构波及面扩至五处 | zcode | P1 | 任务③清单 |
| E9 | page7 四 xlsx 无版本标识 + 中间版归档命名 | dsh | P2 | F1 联动 |
| E10 | 旧口径可视化产物退役（双变量主图 png + render 脚本） | dsh | P2 | F1 联动 |
| E11 | `_infer_dim` 关键词推断质量债——dim 升显式标注、推断降兜底（CB-22 颗粒度纪律传导） | zcode | P2 | 沉淀同期 |
| E12 | 双环境索引可复现：--build 条数指纹 + pytest 断言（索引漂移显式红灯） | zcode | P2 | 沉淀同期 |
| E13 | rag_index `_DIM_KEYWORDS`「街道」术语债（街道≠街办）·已核实 [rag_index.py:49-50](tools/rag_index.py#L49) | dsh | P3 | 小修 |
| E14 | source_path 白名单缺失=F5 防线硬实现 | dsh | P1 | 实施计划必列 |
| E15 | 沉淀验收=抽 5 个体检域黄金问题跑通「检索→LLM 综合→引用来源」（CB-22 三支柱①机器化） | dsh | P1 | 实施计划必列 |
| N1 | exchange copy 段 5 份素材目录为空（来源闭环待确认） | Codex | P2 | 沉淀时补 |
| N2 | 9 份占比表 md 字节级重复（已核实 diff 一致）→ dedup 规则 | Codex | P1 | F1 已并入 |
| N3 | 旧 time manifest 4 处 /DATA/processed/ 死链 | Codex | P1 | 并入 E3 |
| N4 | 社区四套口径并存 + 注释 193 vs 实 174 | Codex | P1 | 并入 F4/E1 |
| N5 | 174 面 3 对重叠（最大 13%）→ zonal 重复计数隐患·数据侧修拓扑 | Codex | P2 | 排期 |
| N6 | time-source 拉取失败仅 console.error·UI 无降级提示 | Codex | P3 | E3 顺手 |
| N7 | 办件编号登记 | Codex | — | 已裁定（F5） |

---

## 四 任务③时间轴工程·边界定稿

- **定稿（zcode 表述最完备·获采）：数据替代、机制保留、分轴挂轴**——time-source 的 manifest 发现机制本就为自由时间点设计，T1~T3 只是当前 occupant；**真实时间轴（2024 体检/2025 体检/12345 分年度）与演示轴（T1~T3）分 manifest·严禁同轴混挂**（sim 禁入红线的时间轴版·一票否决）。
- manifest v1 schema 已支持真实时间点（period 枚举含 year/custom-range·Codex）——不需新 schema，重构=datasets 注册+切片生成+UI。
- UI：**离散时间点（胶囊/chip·三组一致）**，连续 slider 在 2-4 个稀疏点下是伪精度；T1-T3 的 lerp 仅演示轴保留。
- **波及面五处**（zcode E8）：前端三件 + geo_registry 点层注册 + DATA/performance 布局 + manifest 生成红线 + 飞轮测试基建（test-cases/test-assets 含 T1-T3 模式·粗测 12 文件）。**开工前 Codex 先出 T1~T3 全仓引用清单**（防漏改）。

---

## 五 下周排期（Codex 草案获采·按定稿修订）

| 日 | 内容 | 量级 |
|---|---|---|
| D1 | CB-39 实施计划预检；E7 换绑；E5 已于本轮清零 | 0.5d |
| D1-D2 | **B002/B004 复发用例锁定+白名单裁定（P0·前移）**；B012/B013 修复（E1 方向 A + range tooltip nameField）；UI-09/PRED-09 同场采集 | 1d |
| D2 | 沉淀批次1：F1 落位（_retired 归档+E9/E10 退役登记+presets 补注册+usage 标注）+ 口径注册表（F4.1 起步） | 1d |
| D2-D3 | RAG 批次：domain 标签（含存量 214 回溯）+ 体检域 fact 增量 + 白名单（E14）+ rebuild + E12 指纹断言 + E15 黄金问题验收 | 1.5d |
| D3-D4 | 时间轴工程（=E3·manifest v2 **经用户授权**）+ 离散 UI + E8 引用清单先行 | 1.5-2d |
| D5 | 全量回归（pytest 基线+e2e）+ E2 纯色底图落地+GeoQ spike + CB 审计 | 0.5-1d |

弹性：B008/N5/UI-09/PRED-09 滚动下周。

---

## 六 收敛方对三组的判定汇总

| 组 | 判定 | 要点 |
|---|---|---|
| Codex | **全采纳，2 处被推翻** | 推翻：page7 脚本转正（→退役·dsh 论点获采）；删除中间产物（→归档·zcode 论点获采）。7 条新发现 N1-N7 全部核实入账 |
| zcode | **全采纳** | 4 处独立发现（存量分域债/根 5 文件/口径注册表/board-vs-三层 test case）+ E8 波及面扩展全部获采；「归档不删」获裁定 |
| dsh | **全采纳** | 三条纪律（蒸馏源/作废数字/契约）获承重级采納；E5 DoD 化获采并即执行；E11/E15 进实施计划必列项 |

**CB-38: 三组评估全采纳（Codex 2 处推翻有据）/ 零 disagree / 分歧 4 处已裁定 / 零实施——待 Codex 出 CB-39 实施计划**
