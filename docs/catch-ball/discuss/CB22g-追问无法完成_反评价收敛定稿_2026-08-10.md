# CB-22g · 追问无法完成 · 反评价收敛定稿（claude组）

> **收敛方**：claude组（Claude Code·开发主）| **日期**：2026-08-10 | **性质**：CB 反评价收敛（glm + Codex 两组回应到 → claude 主线程核实事实 → 逐条 agree/disagree/partial → 定稿修复）
> **回应对象**：[CB22g-..._回应_glm组](CB22g-追问无法完成_根因讨论_回应_glm组_2026-08-10.md) + [CB22g-..._回应_Codex-GPT5](CB22g-追问无法完成_根因讨论_回应_Codex-GPT5_2026-08-10.md)
> **承重红线**：diagnose prompt / orchestrate 主循环 / finalStep D019 / @track() 签名 / 追踪编号连续 / 不造轮子
> **连带**：本轮纳入「宜昌 2025 体检资料整合进 RAG」作为数据层行动项（与代码根因正交并行）

---

## 〇、一句话结论

**Codex 戳穿了 claude 上轮 trace 取证的两个前提误判 —— sess-22008 是 pytest 会话（25 秒·含 test fixture）非用户实测、F_005 是 `build_diagnose_prompt` 非 FC。glm 在此误判前提上做的「FC 秒回失败」分析证据链失效。主线程独立核实：Codex 全部论断成立。真正可坐实的是 4 件事：① F_016 编号冲突 → 改 F_018；② FC 端点缺埋点 → 追问失败原因在 trace 里根本不可观测；③ knowledge_qa 伪工具零参 schema 是 FC 失败头号嫌疑（须 live 验证）；④ 旧 SSE diagnose 是死路径（diagnoseStep 零生产调用方）→ glm「补 knowledge」不做。收敛方向 = 采 Codex 主线（先补 FC 埋点 → 重采真实会话定位根因），glm 贡献 = F_018 一致 + stages.js:322 真实 bug。**

---

## 一、上轮 trace 取证误判澄清（诚实记录）

CB-22g 发起稿（claude组）的 trace 取证有两个决定性前提错误，主线程本轮独立核实坐实：

| 上轮断言 | 核实 | 判定 |
|---|---|---|
| sess-22008 是用户实测会话（15:58:26） | 时间跨度 15:58:26→15:58:51 **仅 25 秒**；含 `lon_gcj02`/`nonexistent`/`EmptyDataError` 等 tests/ 负路径 fixture 指纹 | **误判**——是 pytest 会话 |
| F_005 = FC 路由，0.2ms 秒回失败 ×10 | `prompts.py:247` `@track("MOD_AIQA.F_005")` + `:625` 注册「build_diagnose_prompt（承重 eval-anchor）」；FC 端点 `router.py:65-114` **无任何 @track** | **误判**——F_005 是字符串构造器，0.2ms 是正常耗时；FC 在 trace 中不可见 |

**后果**：基于「FC 秒回失败 ×10」推出的整条根因链（→降级旧 SSE → select_template 判 concept）在 sess-22008 上不成立——那是 pytest 跑 select_template 的记录，不是浏览器降级。glm 沿此链做的「前端 fetch 层」分析虽代码层面部分有效（后端 fc_diagnose 异常确全包成 SSE 帧），但「秒回」本身未被任何真实会话证实。

**教训**：trace 取证须先核会话身份（pytest vs 浏览器）+ 核 track ID 语义，不能凭计数推断。本轮验收硬加 `EMOTION_TRACE_SESSION` 固定 + FC 埋点，正是堵这个盲区。

---

## 二、逐焦点收敛（主线程核实事实为准）

| 焦点 | Codex | glm | 主线程核实 | 定稿 |
|---|---|---|---|---|
| **1 FC 秒回根因** | **disagree**：sess 是 pytest·F_005≠FC·FC 无埋点不可观测 | partial：前端 fetch 层（serve/后端没起/跨域），非后端路由被破坏 | Codex 全对（见上表）。后端 `router.py:107-112` 异常全包成 SSE 帧确不致 HTTP 500，glm 这点也对，但「秒回」未被真会话证 | **采 Codex**：FC 缺埋点是盲区，先补 F_019 → 重采真会话定位 |
| **2 旧 SSE 降级 concept** | **partial**：现网前端不调旧 SSE（diagnoseStep 无调用方·select_template 无生产调用方）·链不存在·不补 | agree：旧路径需补 knowledge_qa（方案1 harness 层 + 方案2 select_template 分支） | Codex 对：grep `diagnoseStep` in [frontend/js/ai_qa/](frontend/js/ai_qa/) **仅命中定义+注释·零调用方**；`select_template` 生产代码无直接调用（仅渲染成文本） | **采 Codex**：旧路径死路径·**不做 glm 的补 knowledge**（避免给死路径加无谓代码） |
| **3 F_016 编号冲突** | agree：改 F_018 + 防复发守卫测试（全仓含 outlet_kb/ 去重） | agree：改 F_018（glm 上轮已建议·实施未采纳） | 两组合一。F_014/015/016/017 已占·**F_018 空位** | **F_018 + 新增 `validate_track_ids.py` 守卫**（全仓含 outlet_kb/·堵本轮漏扫盲区） |
| **4 知识问答 F_015=0** | disagree：sess 是 pytest·端点链代码正确（quick-rag→F_017→F_015）·须重采真会话 | partial：须用户确认首问输出 | Codex 对：端点链 `_quickIntent→_assembleKnowledgeQA→post_rag_search(F_017)→search(F_015)` 代码正确，挂载正常 | **采 Codex**：重采 `EMOTION_TRACE_SESSION` 固定会话取证 |
| **5 验收口径** | agree + 3 修正：会话固定 + FC 埋点前置 + live FC 冒烟 + 断言集（F_017>0/F_019正常/F_005·F_001 不出现） | agree + 前置 FC console 定位 | Codex 更系统（会话固定直击本轮误判根因） | **采 Codex** 验收顺序 |

### glm 的真实独立贡献（不依赖被推翻的前提）

- **stages.js:322 `question` 未定义 bug**（方案 A content 标签兜底分支）：`_deriveScale(question...)` 的 `question` 在 fcDiagnoseStep 作用域内未定义，应为 `ctx.question`。**真实代码 bug，独立于会话争议，顺手修**（FC 修复后此分支一旦触发会 ReferenceError）。

### 两组共识 → 直接定稿

- F_016 → F_018（铁证）
- FC 不可观测 → 须补埋点
- 须重采真实用户会话（pytest 会话不能推断用户行为）

---

## 三、连带行动项：宜昌 2025 体检资料整合进 RAG（本次 plan 进 CB）

> 用户指示「连带本次一起修复」。此项是**数据层**操作，与 CB-22g 代码根因正交，可并行执行、互不阻塞。

**资料**：[宜昌市2025年城市体检全维度整合梳理.md](docs/refs/宜昌市2025年城市体检全维度整合梳理.md)（77 项四维度 + 葛洲坝 20 项 + 停车 13 项 + 空间落位 + 治理项目库）。

**定性**：它是现有简版体检 fact 的**完整母本**——[urban_renewal_knowledge.py](ai_qa/outlet_kb/urban_renewal_knowledge.py) 已有 36 条 fact（INDICATORS URP-I01~I08 + CHECKUP_ISSUES C01~C04 等），但 detail 不含空间落位。本资料补的正是 EMC 出口卡片（CLAUDE.md「出口抽象层」）最缺的**空间落位**（街办/社区/道路 → `place_name`）+ **治理项目库**。

**整合方式**（用户已定 = note 兜底 + fact 补强）：
1. md 移入 `docs/urban-renewal-plan/00-宜昌专项/03-07_...md`（与 2024 版 03-06 同列·进 note 扫描目录）。
2. 追加 16 条结构化 fact（聚焦空间落位 + 治理项目增量，与现有 36 条严格去重）：
   - INDICATORS +8（I09~I16：结构/围护隐患空间分布、学位错配、中学盲区、停车集聚、人口密度失衡、消防站、历史建筑空置对象）
   - CHECKUP_ISSUES +3（C05~C07：隐患集聚带、风险区域叠加、上年度整治成效）
   - PROJECTS +3（P09/P10/P12：四好对策+32 项目库、葛洲坝 19 治理项目、停车 83 项目）
   - METRICS_SYSTEM +2（M04/M05：葛洲坝片区概况、77 项指标体系）
3. `py tools/rag_index.py --build` 重建（note 自动扫到新 md + 16 fact 向量化）。
4. 术语纪律：detail 只罗列源文档事实，不造分类（过 `validate_rag_material.py` 黑名单校验）。

**与 CB-22g 的关联**：体检整合完成后，EMC 知识问答的素材更完整（出口卡片字段齐全）；但它**不修复** CB-22g 的追问失败（那是 FC 路由/埋点问题）。两者独立。

详细 fact 清单见 plan：[splendid-coalescing-music.md](../../../../C:/Users/Administrator/.claude/plans/splendid-coalescing-music.md)（已写）。

---

## 四、修复 plan（分层·P0 可立即做 / P1 需用户配合）

### P0 · 代码（立即可做）

| # | 行动 | 文件 | 说明 |
|---|---|---|---|
| 1 | F_016 → F_018 | [urban_renewal_knowledge.py:206](ai_qa/outlet_kb/urban_renewal_knowledge.py#L206) `@track` + `:14` 注释 | 一行改·消除编号冲突 |
| 2 | 新增守卫测试 | `tests/validate_track_ids.py`（新） | 全仓（**含 ai_qa/outlet_kb/**）扫 `register_track_id`/`@track` 的 `MOD_AIQA.F_*` 去重·重复即 fail·防复发（本轮 glm/claude 都漏扫 outlet_kb/） |
| 3 | FC 埋点 F_019 | [router.py:65-114](ai_qa/router.py#L65-L114) fc_diagnose 分支 | 入口/出口加 `@track('MOD_AIQA.F_019')`（含 tool_calls/tool_name/_fcError）→ FC 失败从此可 trace |
| 4 | stages.js:322 bug | [stages.js:322](frontend/js/ai_qa/stages.js#L322) | `question` → `ctx.question`（glm 发现·方案 A 兜底 ReferenceError） |

### P0 · 数据（立即可做·即体检整合）

| # | 行动 | 说明 |
|---|---|---|
| 5 | 体检资料整合 | md 移入 00-宜昌专项/ + 16 条 fact + `--build` 重建 + 检索命中验证（详见第三节） |

### P1 · 需用户配合（根因定位·真会话）

| # | 行动 | 说明 |
|---|---|---|
| 6 | knowledge_qa 零参 schema live 冒烟 | `curl/浏览器` 调 `/api/v1/chat` phase=fc_diagnose（纯问答句）→ 看 DeepSeek 是否拒零参 strict 工具（400）。**头号嫌疑**。若拒 → 伪工具加 dummy param 或退 content-tag 方案 A。须 live（我无法本地复现 DeepSeek FC） |
| 7 | 重采真实会话 | `EMOTION_TRACE_SESSION=CB22g-claude` 重启后端 → 浏览器复现「知识问答→追问」→ console Network tab 看 `/api/v1/chat` Status（直接证据）→ trace 断言（F_017>0 / F_019 正常 / F_005·F_001 不出现） |

### P2 · 独立后置（数据层·不阻断）

| # | 行动 | 说明 |
|---|---|---|
| 8 | F_009 DEM 报错 | 演示数据补 negative 点 / 或 polarity=negative 时诚实提示（非报错） |
| 9 | F_005 KeyError lon_gcj02 | 点层 schema 补列 / 或坐标转换容错 |

### 红线核对

| 红线 | 核实 |
|---|---|
| diagnose prompt | ✅ 不碰 prompts.py diagnose template（守卫测试只读·FC 埋点加在 router 非 prompt） |
| orchestrate 主循环 | ✅ 不碰 harness orchestrate（stages.js:322 是 fcDiagnoseStep 内 bug·非主循环） |
| finalStep D019 | ✅ 不碰 |
| @track() 签名 | ✅ F_016→F_018 改装饰器参数·非签名；F_019 新增 @track |
| 追踪编号连续 | ✅ F_018（query_knowledge_base）/ F_019（FC 埋点）连续不跳号 |
| 不造轮子 | ✅ 复用现有 register_track_id/@track/rag_index 机制 |

---

## 五、结论

- **CB-22g 双根因（FC 秒回 + F_016 冲突）→ 修正为单铁证（F_016）+ 双盲区（FC 不可观测 + 旧路径死路径）+ 一嫌疑（零参 schema）**。
- **claude 上轮 trace 取证误判，诚实记录，堵盲区（会话身份核 + FC 埋点）**。
- **Codex 主线采信（核实全部成立），glm 贡献 F_018 + stages bug，glm「旧路径补 knowledge」放弃（死路径）**。
- **体检整合作为连带数据层行动项纳入，与代码根因并行**。
- **P0（代码 4 + 数据 1）立即可做；P1 需用户 live 冒烟 + 重采真会话定位 FC 根因**。

---

*claude组 · CB-22g 追问无法完成 反评价收敛定稿 · 2026-08-10*
*登记 cb-journal + 更新 _cb-index · P0 立即执行 · P1 待用户 live 配合*
