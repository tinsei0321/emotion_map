# CB-22 · RAG 接入 EMC 实施计划（承重·先验后推）

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG Phase 1 收尾）
> **背景**：RAG 核心已完成（事实卡 35 条 + 索引 225 条 + 黄金集 100% 通过）·最后一步接入 EMC 运行链路。
> **承重**：触碰 harness/diagnose 周边（红线）·先验后推·两组确认后实施。

---

## 〇、已完成（RAG 核心·已验证）

| 项 | 状态 |
|---|---|
| case_library point 方法论（5 案例·无数据） | ✅ + test 断言 |
| L1.5 事实卡（35 条·7 类·含 dimension） | ✅ schema 校验 |
| rag_index（225 条·含事实卡） | ✅ 检索提升 |
| cannot 维度化（F_016） | ✅ 305 passed |
| 黄金集 3 类（召回 100%/越维 100%/案例 100%） | ✅ 通过 |

## 一、接入 EMC 步骤（承重·待确认）

### 1. 后端端点 `POST /aiqa/rag_search`

```python
# api/aiqa_routes.py 仿 outlet_card（:88）
@aiqa_router.post('/aiqa/rag_search')
def post_rag_search(body):
    """RAG 知识检索（开放语义·B 路径未命中降级）。"""
    from tools.rag_index import search
    r = search(body.query, body.k or 5)
    return r  # {ok, results:[{score, source, type, data_dim}], count}
```

- **触发条件**：开放语义/跨文档综合（B 路径 query_knowledge_base 未命中 → rag_search 兜底·B 先 RAG 后）
- 返回带 data_dim（颗粒度原则·回答不越维）

### 2. harness 短路（`_quickIntent` 扩展）

```javascript
// emc-patterns.js 加 RAG_QUERY_KW（开放语义问：哪些/如何/为什么+跨文档）
// harness.js _quickIntent 加 'rag_query' 分支 → 短路 → 调 /aiqa/rag_search → 注入 finalStep
```

- **红线**：不改 diagnose prompt·intent 枚举（走 harness 层短路·与 general 短路同模式）
- 保守：高置信才短路·宁落不误断

### 3. finalStep 注入

- rag 结果 intent-gated 注入（Top-K≤5 + 来源 + data_dim）·体积 <3000B
- 回答带维度声明（cannot 维度化已做）

### 4. B 先 RAG 后（CB-22b 定稿）

```
问句 → _quickIntent 分流：
  KNOWLEDGE_QUERY_KW（结构化：有哪些项目/体检问题）→ B 路径 query_knowledge_base
  RAG_QUERY_KW（开放语义：如何/为什么/哪些城市+跨文档）→ rag_search
  B 未命中 → 降级 rag_search（召回兜底）
```

## 二、待两组确认

1. **端点设计**：`/aiqa/rag_search`（仿 outlet_card）·返回 {ok, results, count}——认可？
2. **harness 短路**：`_quickIntent` 加 'rag_query'（B 先 RAG 后·不改 diagnose）——接入点认可？
3. **B 路径前置**：query_knowledge_base（CB-22b）尚未建——是否先建 B 路径（知识查询）再接 rag_search？还是 rag_search 先独立接入（B 后补）？
4. **finalStep 注入**：<3000B + 维度声明——认可？
5. **测试**：e2e（"宜昌哪些项目适合片区更新"→ rag_search Top-K）+ 回归——设计？

## 附：发给两组的确认 prompt（A 组通用）

```
【CB-22 RAG 接入 EMC 确认 · 承重】

背景：RAG 核心已完成（黄金集 100%）。最后一步接入 EMC 运行链路（承重·触碰 harness/diagnose 周边）。
先验后推·确认后实施。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-RAG接入EMC_实施计划_2026-08-09.md（本计划）
- 读 tools/rag_index.py + ai_qa/outlet_kb/urban_renewal_knowledge.py（已完成）
- 核实：api/aiqa_routes.py outlet_card 端点 + frontend/js/ai_qa/harness.js _quickIntent

第二步 · 确认（agree/调整 + 证据）
1. /aiqa/rag_search 端点设计
2. harness 短路（B 先 RAG 后·不改 diagnose）
3. B 路径前置 vs rag_search 先独立接入
4. finalStep 注入（<3000B + 维度声明）
5. e2e 测试设计

第三步 · 产出回应
- 落盘 docs/catch-ball/discuss/CB22-RAG接入EMC_{组名}_{YYYY-MM-DD}.md
- 中文为主·引用 path:line·不 git
```

---

*claude组（2026-08-09）·RAG 接入 EMC 实施计划·承重·待两组确认。*
