# 事件记录 · 本地 .git 对象库损坏与恢复指引（2026-08-23 15:55 许）

> **性质**：基础设施事件（非代码缺陷）·影响全部组·须用户在本机终端执行恢复（沙箱无凭据）。
> **写入时状态**：工作树完整无损 · reflog 完整 · 远端 gitee 为权威源 · 恢复动作 = 一次 fetch + 一次 update-ref。

## 一 事件经过与诊断

1. **15:55 前后一次 `git gc` 中断**（残留 `.git/gc.pid`=38676/BF-202608101011·进程已死——疑多窗口并发或被杀）。
2. gc 中断留下三处创伤：
   - `.git/refs/heads/` 目录被清空（HEAD 指向的 `refs/heads/EMC_harness_dsh` 失去载体 → 所有 git 命令报 unknown revision）；
   - **gc 的临时包 `tmp_pack_PxzNld`（274MB）截断**（index-pack 报 early EOF）——gc 策略是先把全部松散对象收进新包再删旧，包没写完就被杀，导致**约 08-22 晚以后的 commit 对象在本地整链缺失**（reflog 尾 20+ 条全部 could not get object info）；
   - 另发现一包 `pack-064a3518…pack`（6-21·131KB）缺 .idx（与本次事件无直接关系·已隔离至 `_tmp/pack_quarantine/`）。
3. **已完成的抢救**：`git unpack-objects -r` 从截断包救回 11,744 个松散对象（对象库现可读对象 14,554）；stale gc.pid 已清除；`tmp_pack_PxzNld` 与 `tmp_pack_recover.pack`（抢救副本）保留在原位未删——fetch 恢复后可删。
4. **关键事实：工作树零损失**——全部文件（含未提交的 S3/Qoder 工作）都在磁盘上；reflog（`.git/logs/HEAD`）完整记录到 5c0b593e。

## 二 恢复步骤（用户本机终端执行·需 gitee 凭据）

```bash
cd D:/Github/emotion_map
git fetch origin                                                  # 拉回远端全部对象（凭据在本机管理器）
git update-ref refs/heads/EMC_harness_dsh origin/EMC_harness_dsh  # 分支指回远端尖
git status                                                        # 应只见未提交改动（S3 panel.js/acp-channel.js 等）
```

- 若 `git log --oneline -3` 顶部不是 `5c0b593e`（S6）/ `be30774a`（S129）——说明这两件未 push，内容仍在工作树（未提交形态）或被覆盖差异中，reflog 有全部 hash 与 commit message，照 message 重新 commit 即可。
- 恢复后验证：`git log --oneline -5` 链完整 + `python -m pytest tests/ -q` 抽绿 + 可删 `.git/objects/pack/tmp_pack_*` 两个截断包。
- 备用路径：办公室机（双环境）同仓·若家机 fetch 异常可从办公室机 push 补齐。

## 三 预防建议（报主手/用户）

1. **多 Agent 并发的仓上禁跑交互窗口期 `git gc`**——gc 应单进程独占；建议把 `gc.auto` 调低触发频率（`git config gc.auto 256` 或禁自动 `git config gc.auto 0`·改为人工每周定时执行）。
2. 各组 commit 后**尽快 push**（当前纪律=主手统一回收·本事件中未 push 的本地 commit 就是暴露面）。
3. 换机交接件（_handoff）补一条「到岗先 `git fsck --no-progress | head` 体检」。

## 四 未提交工作备份（仓外·防恢复动作误伤）

`D:/Github/_emc_rescue_backup_20260823/`：
- `frontend_js_aiqa/`：S3 两件（panel.js 事件化版 + acp-channel.js 新模块）
- `discuss/`：S3 侦察设计文档 + 当日讨论件若干
- `tmp/`：S3 单测/E2E 脚本 · `memory/`：当日工作记忆 · `qoder/`：Qoder 在途件

> Kimi 记录 · 2026-08-23 · 待用户终端恢复后·S3 即行 commit（前缀 SHELL(S3):）
