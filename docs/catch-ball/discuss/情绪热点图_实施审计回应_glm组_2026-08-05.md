# 情绪热点图 P0/P1/P1.5 — 实施审计回应（glm组）

> **评估方**：glm组（ZCode + GLM 5.2 · 第三方独立审计·不做项目方决策背书）
> **日期**：2026-08-05 | **性质**：CB 专题热点图实施审计
> **回应对象**：`情绪热点图_实施审计发起_2026-08-05.md`（P0 命名 + P1 软分级 + P1.5 setTerrain + 5 焦点）
> **上轮承接**：glm组 P1 修正评估提"主因 = U 形离散 vs Gi* 连续假设不匹配" + "建议 A 软分级"——本轮核对实施
> **核心标尺**：演示逻辑链（张力图面→聚焦关注区）/ 出口三铁律 / AI·Copilot 内核

---

## 〇、一句话结论

**P0 命名定标 + P1 软分级 + P1.5 setTerrain 主体实施正确（五档分类/threshold 参数化/draping 隔离/terrarium 编码都对）·但发现 1 阻断级问题（/geo/hotspot 端点 + legend 未透传/暴露 soft_threshold 与 tend_hot/tend_cold 档位——软分级虽默认生效但 legend 仍标三档"hot/cold/ns"·用户看到 tend_hot 点却无 legend 说明）+ 1 警告级（paradigm.py 契约层命名未同步·多处"冷热点/Gi*"旧称）+ 1 建议级（tools.js hotspot 委托观察文案仍三档）。修复阻断项（端点加字段 + legend 五档）后可整体验收。**

---

## 一、逐焦点审计

### 焦点 1：P0 命名定标（8 处）— **partial（前端改了·契约层未同步）**

**核实**：
- hotspot-tool.js / grid-tool.js / panel.js / test-cases.js / index.html / spec.md——计划称 8 处改「显著聚集点(Gi\*)」/「显著聚集(Gi\*)」✅（前端层改了）
- **但 paradigm.py 契约层多处旧称未同步**：
  - :43 `'method': '50–100m 精细网格聚合 + 热点聚集（Gi*）'`
  - :48 `'method_templates': ['hotspot(Gi*冷热点)']`
  - :132 `'voice': '我用 Gi* 识别显著冷热点聚集'`
  - :301 `'when': 'Gi* 热点：负面/正面情绪在空间上显著聚集的冷热点'`
  - :303 `'yields': '每点 Gi* Z-score + hot/cold 分类'`——**仍是三档**·未同步五档

**glm组 判断：partial——前端命名定标到位·契约层 paradigm.py 未同步。**

**影响评估**：
- paradigm.py 是 FC 选型的 `GEO_TOOL_CATALOG` / `TEMPLATE_REGISTRY` 来源——LLM 看到的工具描述仍是"冷热点/hot/cold 三档"。
- **LLM 选型不受大影响**（hotspot 工具名没变·仍能选）·但 **LLM 产出结论时可能仍用"冷热点"旧称**（因 prompt 注入的是旧描述）·与前端"显著聚集"定标不一致。
- severity：🟡 警告级（非阻断·但命名一致性是定标目的·契约层漏改则定标不完整）。

**glm组 建议**：paradigm.py 的 hotspot 相关描述同步——
- "冷热点"→"显著聚集点"（命名定标）
- "hot/cold 分类"→"五档：hot/tend_hot/ns/tend_cold/cold"（软分级同步）
- tool_contracts.py 的 hotspot 条目（若有）同步。

### 焦点 2：P1 软分级（五档 + threshold 参数化 + hotspot_tier）— **agree（实现正确）+ 焦点 5 阻断关联**

**核实**：
- `_classify_hotspot`（spatial_analysis.py:110-128）：五档 `hot(>threshold) / tend_hot(>soft_threshold) / ns / tend_cold / cold` ✅ 边界正确
- `hot_spot_analysis`（:36-37）：`threshold=1.96·soft_threshold=1.0` 参数化 ✅
- `hotspot_tier`（:98-99）：`= hotspot`（五档同值·冗余但兼容）✅
- 诚实标注（:48 docstring）：`soft_threshold: 软分级阈值（默认 1.0·~84% 置信·P1 修正·诚实标"倾向聚集"）` ✅
- 弃 `_CLS_POL`（hotspot-tool.js:17）：不再映射极性色·保留 hotspot_tier 五档 ✅

**glm组 判断：agree——P1 软分级实现正确·对齐 glm 上轮建议（A 软分级 + 诚实标档）。**

**hotspot_tier vs hotspot 冗余**：
- :98-99 `gdf['hotspot_tier'] = gdf['hotspot']`——**同值两列·冗余**。
- 但语义是"hotspot 保留兼容（旧消费方）/ hotspot_tier 新名（五档语义明确）"——**过渡期可接受**·P2 可弃 hotspot 统一用 hotspot_tier。

**与 KDE 解耦**：
- hotspot-tool.js:17 弃 `_CLS_POL`（极性色）·map.js `colorMode='hotspot'` 显著性符号层——✅ 与 KDE 色板解耦。
- **但 tools.js:1296 观察文案仍三档**：`_CLS_CN = { hot: '显著热点(负面聚集)', cold: '显著冷点(正面聚集)', ns: '不显著' }`——**未含 tend_hot/tend_cold**·LLM 观察到的分类说明不完整（焦点 5 关联）。

### 焦点 3：P1.5 setTerrain（连续曲面 + draping 隔离）— **agree（实现正确）**

**核实**：
- `create_terrain_dem`（spatial_analysis.py:1066 F_009）：KDE→terrarium RGB ✅
- `/spatial/dem` 端点（routes.py:351）：header 返 bounds/size/heightScale ✅
- **draping 隔离**（map.js:323-364）：
  - :325 `不常驻——仅显式 3D 地形模式开启·退出 setTerrain(null)` ✅
  - :349 `与 3D 网格柱互斥（隐藏 fill-extrusion 柱层·防漂浮/穿模）` ✅
  - :357 `sky 层（防地平线露容器底）` ✅
  - :363 关闭时 `setTerrain(null) + removeLayer('emc-sky') + 恢复图层` ✅
- F_009 追踪 ID（:1066）——F_008 已占（:340 aggregate_by_boundary_id）·F_009 连续 ✅

**glm组 判断：agree——P1.5 setTerrain 实现正确·draping 隔离到位（glm P1.5 强调的三点都落实）。**

**terrarium 编码正确性**：docstring（:1079）`height = (R*256 + G + B/256) - 32768`——标准 Mapbox terrarium 公式 ✅。glm组 未独立解码验证（需 DEM PNG + RGB 解码·环境限制）·以发起文档"DEM 解码验证 0~500·峰 500m"为准。

### 焦点 4：A/B 验证结论 — **agree（glm 上轮根因已采纳）**

**核实**：
- 发起文档引用 glm 上轮根因："score 是 U 形多峰离散分布（5 级极性分类副产物）·与 Gi\* 连续正态假设不匹配" ✅
- "网格化非解药"结论 ✅（glm 上轮确认）
- 长期 KDE/DBSCAN 替代建议 ✅（glm 上轮提）

**glm组 判断：agree——A/B 结论准确反映了 glm 上轮根因判定。**

**补充**：A/B 验证是本专题最有价值的产出——它**证伪了"调参能救 Gi*"的假设**·把问题从"参数调整"上升到"方法适用性"。长期 KDE/DBSCAN 评估应作为热点图 P2/P3 的正式议题（非本轮范围）。

### 焦点 5：新问题/遗漏（审计核心）— **🔴 阻断级发现（端点 legend 未同步五档）**

**🔴 阻断级：/geo/hotspot 端点 + legend 未暴露 soft_threshold / tend_hot/tend_cold**

**完整链路核实（glm组 独立）**：

| 层 | threshold/soft_threshold? | tend_hot/tend_cold? | 判定 |
|---|---|---|---|
| `_classify_hotspot`（:110-128）| ✅ 参数化（threshold/soft_threshold）| ✅ 五档输出 | 正确 |
| `hot_spot_analysis`（:36-37）| ✅ 默认 1.96/1.0 | ✅ hotspot_tier 五档 | 正确 |
| **`/geo/hotspot` 端点（:652-680）**| ❌ **HotspotRequest 无 threshold/soft_threshold 字段**（:652-654 只有 value_col/invert）| ❌ **legend 只标三档**（:680 `'legend': {'hot': '显著热点', 'cold': '显著冷点', 'ns': '不显著'}`）| 🔴 **阻断** |
| hotspot-tool.js body（:11）| ❌ 不传（端点没字段）| — | 阻断关联 |
| tools.js 观察文案（:1296）| — | ❌ `_CLS_CN` 三档（无 tend_hot/tend_cold）| 🟡 警告 |

**关键判断**：
- **软分级实际上会生效**——因 `hot_spot_analysis` 默认 `soft_threshold=1.0`·端点不传时走默认·tend_hot/tend_cold 会产出 ✅
- **但 legend 三档**（:680）——用户在地图上看到 tend_hot/tend_cold 点·**legend 里没有说明**·用户不知道"倾向聚集"是什么·**演示困惑**。
- 且**参数不可调**——前端无法传 threshold/soft_threshold（端点无字段）·用户不能切换 95%/84% 置信档。

**glm组 建议（阻断修复·~10 行）**：
```python
# 1. HotspotRequest 加字段（geo_routes.py:652-654）
class HotspotRequest(_GeoBase):
    value_col: str = 'score'
    invert: bool = True
    threshold: float = 1.96          # P1 软分级透传
    soft_threshold: float = 1.0      # P1 软分级透传

# 2. 端点透传（:664）
res = hot_spot_analysis(pts, value_col=req.value_col, invert=req.invert,
                        threshold=req.threshold, soft_threshold=req.soft_threshold)

# 3. legend 五档（:680）
'legend': {'hot': '显著热点(95%)', 'tend_hot': '倾向热点(84%)', 'ns': '不显著',
           'tend_cold': '倾向冷点(84%)', 'cold': '显著冷点(95%)'}
```

**setTerrain 与天地图 raster/网格柱冲突**：
- map.js draping 隔离（:349 互斥 fill-extrusion）已处理网格柱冲突 ✅
- 天地图 raster 被全图 drape——setTerrain 是全局地形·**所有 2D 图层被 drape 到曲面**（含天地图底图）·这是 setTerrain 的正常行为（非 bug）·但用户可能视觉不适应。sky 层（:357）防露底已处理。
- glm组 未独立浏览器实测冲突——以 draping 隔离代码 + 发起文档为准。

**tools.js EMC 委托 generateTerrain3DForAI**：
- retired.md:16 `generateTerrainForAI 3D 路径 → generateTerrain3DForAI（DEM→setTerrain）`
- tools.js:17 `import { generateHeatmapForAI, generateTerrainForAI } from '../heatmap-tool.js'`——**仍 import generateTerrainForAI（旧）·未见 generateTerrain3DForAI**
- 需确认 heatmap-tool.js 是否新增了 generateTerrain3DForAI + tools.js 是否接——**glm组 grep 未命中 generateTerrain3DForAI**·可能遗漏或命名不同。

---

## 二、新问题风险清单

### 阻断级（验收前必修）

| # | 风险 | 证据 | glm组 建议 |
|---|---|---|---|
| **B1** | **/geo/hotspot legend 三档·未含 tend_hot/tend_cold** | geo_routes.py:680 `'legend': {'hot','cold','ns'}`·缺 tend_hot/tend_cold | legend 改五档 + HotspotRequest 加 threshold/soft_threshold 字段 + 端点透传（~10 行）|

### 警告级

| # | 风险 | glm组 建议 |
|---|---|---|
| **W1** | paradigm.py 契约层命名未同步（多处"冷热点/Gi*"旧称 + yields 三档）| 同步"显著聚集点"命名 + yields 五档 |
| **W2** | tools.js:1296 `_CLS_CN` 观察文案三档（无 tend_hot/tend_cold）| 补五档中文映射·让 LLM 观察含倾向档 |
| **W3** | tools.js import generateTerrainForAI（旧）·未见 generateTerrain3DForAI | 确认 heatmap-tool.js 是否新增 + tools.js 是否需接（或 generateTerrainForAI 内部已切 DEM）|

### 建议级

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | hotspot_tier vs hotspot 冗余——P2 弃 hotspot 统一 hotspot_tier | P2 |
| **S2** | 长期 KDE/DBSCAN 替代 Gi\* 评估（A/B 已证 Gi\* 不适合 U 形离散）| 热点图 P2/P3 |
| **S3** | 软分级诚实标注进 LIMITS（出口卡 limitations 标"84% 置信·非 95% 显著"）| P1 补 |

---

## 三、可否整体验收结论

### ⚠️ 有条件验收——先修 1 阻断项（B1 legend 五档 + 端点透传）·其余可后置

**主体实施正确**：
- ✅ P1 软分级五档实现正确（_classify_hotspot 边界 + threshold 参数化 + hotspot_tier + 弃 _CLS_POL 解耦）
- ✅ P1.5 setTerrain draping 隔离到位（不常驻 + 互斥 + sky 层 + terrarium 编码 + F_009）
- ✅ A/B 验证结论准确（glm 上轮根因已采纳）
- ✅ retired.md 登记完整
- ✅ pytest 293 passed 零回归

**阻断项（验收前必修）**：
- **B1**：/geo/hotspot legend 三档 → 改五档 + HotspotRequest 加 threshold/soft_threshold + 端点透传（~10 行）。
  - **不修则**：软分级虽默认生效（tend_hot/tend_cold 产出）·但 legend 无说明·用户困惑"倾向聚集"是什么·演示链断。

**警告项（可后置·建议同步修）**：
- W1 paradigm.py 命名同步（契约层一致性）
- W2 tools.js 观察文案五档（LLM 观察完整性）
- W3 generateTerrain3DForAI 接线确认

**承重红线确认**：
- **四态出口**：本实施不涉及（hot_spot_analysis 空间分析 + setTerrain 渲染·非出口裁定）✅
- **finalStep D019**：不涉及（不改 prompts.py）✅
- **diagnose 永不动**：不涉及 ✅

**glm组 最终立场**：P0/P1/P1.5 实施**质量良好**（软分级五档 + draping 隔离 + terrarium 编码都正确）·glm 上轮建议（A 软分级 + 诚实标档 + 长期重审方法）被采纳且落地。唯一阻断是**端点 legend 未同步五档**——软分级做了但"最后一步展示说明"没跟上·用户看不到 tend_hot 的 legend。修复 B1（~10 行）后可整体验收。

---

## 附：审计证据（glm组 独立核实）

| 发现 | 证据 |
|---|---|
| 五档 _classify_hotspot | spatial_analysis.py:110-128（threshold/soft_threshold 参数化 + hot/tend_hot/ns/tend_cold/cold）|
| hotspot_tier = hotspot 冗余 | :98-99 `gdf['hotspot_tier'] = gdf['hotspot']` |
| hot_spot_analysis 默认 soft_threshold=1.0 | :37 `soft_threshold: float = 1.0` |
| **/geo/hotspot 端点无 threshold 字段** | geo_routes.py:652-654 HotspotRequest 只有 value_col/invert |
| **端点不透传 threshold/soft_threshold** | :664 `hot_spot_analysis(pts, value_col=..., invert=...)` 只传两参 |
| **legend 三档（缺 tend_hot/tend_cold）** | :680 `'legend': {'hot': '显著热点', 'cold': '显著冷点', 'ns': '不显著'}` |
| hotspot-tool.js body 不传 threshold | :11 `body = { layer, value_col, invert }` |
| tools.js _CLS_CN 三档 | :1296 `{ hot, cold, ns }`（无 tend_hot/tend_cold）|
| setTerrain draping 隔离 | map.js:323-364（不常驻 + 互斥 fill-extrusion :349 + sky :357 + 关闭恢复 :363）|
| create_terrain_dem F_009 | spatial_analysis.py:1066 |
| retired.md 登记 | :11-25（fill-extrusion 3D→setTerrain · 2D 等值线保留）|
| paradigm.py 命名未同步 | :43/48/132/301-308 多处"冷热点/Gi*"旧称 |
| tools.js import generateTerrainForAI | :17（未见 generateTerrain3DForAI）|

### 声明

本审计由 glm组（ZCode + GLM 5.2）独立产出·2026-08-05·基于代码级核实 _classify_hotspot / /geo/hotspot 端点 / setTerrainDEM draping 隔离 / paradigm.py 命名 / retired.md。第三方独立审计·不做项目方决策背书。浏览器肉眼验收 + DEM 解码未独立复跑（环境限制）。

---

*登记：docs/context-map.md。*
