# CB-22 · L2 出向任务 — 反评价收敛 + 架构定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22
> **回应**：[Codex](CB22-L2出口任务_Codex-GPT5_2026-08-09.md) + [glm组](CB22-L2出口任务_glm组_2026-08-09.md)
> **用户架构定性**（本轮核心）：知识库/RAG 承接关系 → 三层成果消费链
> **判定**：采纳两组 + 用户定性 → 架构定稿 → 待落盘 plan

---

## 〇、用户架构定性（本轮最高指引）

> ① **知识库** = 《宜昌市中心城区城市更新专项规划》研究、编制的**参考资料**（方便查阅使用）
> ② **RAG** = 情绪地图项目（尤其 EMC outlets）的**底层数据库**·本次知识库是 RAG 的一部分（城市更新/城市体检专题）
> ③ **RAG 作用**：a) 支撑项目开发（大方向正确 + 颗粒度对齐）b) 让我直接使用（EMC 问答 → 承接知识库功能）
> **要求**：各任务分别产出什么/怎么架构·成果**互相消费**·RAG 后期**可维护**

## 一、三层成果消费链（架构定稿）

```
L0/L1 知识库（城市更新专项规划参考资料·docs/urban-renewal-plan/ + industry_kb/）
   │
   ├──→ ① 直接查阅（用户读总览报告/完成度清单/分类提炼）
   │
   ├──→ ② L2 出口（确定性·CB-22 A/B 路径）→ EMC 回答引用知识库
   │        A 路径：出口卡加 kb_reference（案例/项目引用·增强）
   │        B 路径：知识库查询（新意图·"宜昌有哪些更新项目"直答）
   │
   └──→ ③ RAG（向量化·后置 CB-22c）→ EMC 底层数据库
           本次知识库 = RAG 的城市更新/城市体检专题内容
           作用：支撑开发（方向/颗粒度）+ 直接使用（EMC 问答承接）
```

**成果互相消费**：
- 知识库（L0/L1）→ L2 出口（引用）→ RAG（向量化源）
- RAG 后期维护 = 更新知识库 → 重向量化（增量）

## 二、CB-22 采纳（verify-before-accept）

### 焦点 1 · 出口接入方式 — **采纳 glm 纠正 + Codex 三段式**

**glm 纠正属实（已核实）**：出口链路已通（harness.js:1426 `_maybeBuildOutletCard` → aiqa_routes.py:88 `/outlet_card` → build_outlet_schema → panel 渲染·CB-16 Wave 0/1）。「零消费」应修正为「出口只消费 EMC 分析产物·不引用知识库」。

**采纳 Codex 三段式**（glm 纠正上补充）：
1. **选型**：复用 `resolve_outlet_ids`（已实现）·无命中保持 0
2. **组装**：情绪数据 → `build_outlet_schema`（现成）；知识问题 → `build_knowledge_cards`（新增·确定性）
3. **注入**：出口卡含 kb_reference + 来源·finalStep 引用

### 焦点 2 · 知识库引用 — **采纳 glm 拆 A/B 两类任务（关键）**

**glm 关键纠正**：用户"宜昌有哪些更新项目"是**知识库查询（B）**·非出口契约任务（A）——**勿混**。

| 路径 | A. 出口卡引用知识库（增强） | B. 知识库查询（新意图） |
|---|---|---|
| 问句 | "大南门更新需求分析"（分析类） | "宜昌有哪些更新项目"（数据类·不产图层） |
| 意图 | emotion_analysis → 出口卡 | general → 直答 |
| 数据源 | EMC rows + case_library/L0 | L0 项目库 |
| 实现 | `_build_card` 加 `_attach_kb_reference` | 新工具 `query_knowledge_base`（city+topic 直查） |
| 优先级 | CB-22 当前 | **CB-22b 独立专题** |

**采纳 Codex L1.5 数据层**（补充 A）：`urban_renewal_knowledge.py` 事实卡（PROJECTS/INDICATORS/PANELS/CHECKUP_ISSUES·≤80 字 + source 指针）——L0→运行时的事实化映射·不重复搬 L1。

### 焦点 3 · 体检-更新衔接 — **agree（两组一致）+ 采纳 field_mapping**

- 放 `urban_renewal_outlets.py`（枚举已核实）✅
- 契约 `checkup_renewal_link`：体检问题→更新需求→项目库（field_mapping：issue_label/domain_top/place_name/polarity_index/task_link）
- **边界诚实**：情绪=感知·体检数值=客观·`cannot` 明确"不能替代结构鉴定"

### 焦点 4 · L2 vs RAG — **agree + 采纳 glm「B 路径天然需 RAG」**

| 问题类型 | 手段 |
|---|---|
| 结构已知查询 | 确定性（B 简单版·city+topic 直查） |
| 关键词过滤 | 轻量检索（L1.5 事实卡 WHERE·非向量） |
| 开放语义/跨文档 | **RAG（后置 CB-22c·向量化）** |

**禁把 RAG 塞进出口契约**（glm）——出口=确定性格式化·RAG=概率检索·混则降质。

### 焦点 5 · 测试 — **agree + 采纳两组用例**

- T1 "宜昌有哪些更新项目" → 知识出口卡（从 L1.5 数据层断言·非硬编码）
- T2 体检-更新衔接卡（真实数值）
- T3 无情绪负例（不编造·显示边界）
- T4 体积守卫（知识注入后 <8000B）
- T5/T6 触发词 + 字段 CI
- T7 数据一致性（L1.5 ↔ L0 抽查）

## 三、任务拆分（定稿）

| 任务 | 内容 | 承接 |
|---|---|---|
| **CB-22**（当前） | A 路径：`_attach_kb_reference` + L1.5 数据层 + 体检-更新契约 + 出口 e2e 测试 | 知识库→出口 |
| **CB-22b**（新专题） | B 路径：`query_knowledge_base`（city+topic 直查 L0）+ 独立测试 | 知识库→查询 |
| **CB-22c**（RAG·后置） | 向量化 L0/outlet_kb·EMC 底层数据库·承接 B 复杂语义 + 开发支撑 | RAG |

## 四、承重红线核对

- diagnose prompt / 四态出口 / @track：不触碰（_build_card 加字段·新工具注册走 tool_contracts）✅
- D019 final 极瘦：kb_reference 进出口卡 JSON·不进 final_brief·<8000B 测试守卫 ✅
- 出口链路复用（CB-16）·非新建 ✅

## 五、执行顺序

1. CB-22（A 路径 + 体检-更新契约 + L1.5 数据层 + e2e）——待用户确认后实施
2. CB-22b（B 路径·知识库查询）——A 完成后
3. CB-22c（RAG·向量化）——知识库 + L2 完成后（用户定承接）

---

*claude组（2026-08-09）·CB-22 反评价收敛 + 架构定稿·待落盘 plan。*
