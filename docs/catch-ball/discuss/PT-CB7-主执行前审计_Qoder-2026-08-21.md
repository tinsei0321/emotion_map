# PT-CB7 · 主执行前审计（Qoder 主执行自审 · 2026-08-21）

> 审计人=执行人：Qoder（用户令「审计先行+主执行」串行一体）。对象：任务书 T1-T9+F1-F4 全文。
> 审计纪律：审计意见随执行生效（修正项直接进执行记录）；超白名单/超范围修正列「待主手裁决」。
> 取证基础：1c2ce974 WIP diff 实读 / render_client.js 全文 / state.js·map.js 图层管理真身 / better-sidebar service.d.ts / FACTS schema 实读 / 八工具 docstring 长度实测 / test_render_channel.py·test_mcp_server_emc.py 结构核查。

---

## 〇 总判定

**任务书可执行，无结构性漏洞；4 项审计修正（随执行生效）、2 项设计风险备注、1 项待主手裁决。** WIP（boundary_fill_v1 + R14）复核结论：保留，且为 T3 的直接资产。

## 一 WIP 复核（1c2ce974·派发包点名）

| WIP 件 | 复核结论 |
|---|---|
| `render_client.js` boundary_fill_v1 scheme（+10 行） | **保留，与 T1 正交无冲突**。T1 的清理作用于「铺层前按 `[dsh]` 前缀移除既有层」，boundary_fill_v1 只新增一条 scheme 渲染分支（同样经 addToolboxLayer 铺层，前缀命名一致），天然被 T1 清理覆盖。代码走 `defaultPaint` 兜底同款样式、无数据编码，符合「范围/边界面」语义。 |
| `debug-memory.md` R14 | **属实且为 T3 关键资产**：better-sidebar ≥0.12 以 `interceptOpenPath:true` 默认 monkey-patch `workspaces.openPath`（.bat 被当文档开不执行 / URL 被拼 workspace-relative 致 ENOENT）。T3 的 start.bat 链必须沿用 R14 解法（直连 `POST /api/host.openPath`）；「在新窗口打开」兜底按钮沿用 `window.open` 手势段方案。 |
| 两脚本入库（gen_12345_grid_800m.py 235 行 / gen_task_report.py 389 行） | 已入库，T8 的「入库」半已完成，T8 实剩**参数化改造 + 口径对照段**——任务书 T8 描述与现状差半步，执行时按「改造」口径做。 |

## 二 逐任务审计

### T1 图层残留自清理 —— 可执行，2 项修正

- **真身核实**：图层管理 API 在 `frontend/js/state.js:766 removeLayer(id)` / `:795 getLayers()` 与 `frontend/js/map.js:683 removeLayerFromMap(id)`；`addToolboxLayer`（shared.js:188）已按 name/内容签名去重**同名同内容**层——T1 治的是**异名 spec 叠层**缺口，定位准确。
- **修正 M1（随执行生效）**：render_client.js 现只 import `{addToolboxLayer, defaultPaint}`，T1 需扩 import `getLayers/removeLayer/removeLayerFromMap`（shared.js 已转出口径·执行时核实转 export 链）。
- **修正 M2（随执行生效·测试形态）**：`tests/test_render_channel.py` 是**后端 Python 测试**，无法驱动 JS 运行时图层列表。任务书「构造两层→apply→断言剩一」的运行时断言在 pytest 面不可执行。**替代验收**：静态契约断言（render_client.js 的 `_apply` 体内、首个 `addToolboxLayer` 调用之前必须出现 `[dsh]` 前缀清理调用 + try/catch 包裹 + console.warn 不阻塞），运行时实测移交浏览器实测清单（用户参与项已含「同会话两次出图仅剩最新 [dsh] 层」）。此修正不超白名单。
- **设计风险备注 D-R1（不阻塞·记录在案）**：「移除所有 [dsh] 前缀层」= 语义裁定「一次只看最新一张 dsh 图」。若未来产品要求多张 dsh 图并存，需回到 spec 级开关。当前按任务书执行。
- **A9 落实**：移除失败 log 一行继续——用具体 try/catch 包清理段，console.warn 记录，不裸 except 吞错（JS 面同口径）。

### T2 better-sidebar terminal chunk —— 可执行，决策树合理

- 怀疑方向（旧 chunk × 新宿主运行时·与黑屏根因 B 同族）合理：rc.8 merge 后跑了 build:web 但 profile 内第三方插件 chunk 未重建。
- **注意**：修法 A 的 `pnpm install` 可能触发 lock 修剪（会顺带清掉 CB6 审计发现的 synapse lock 残留 M1/F4——**顺带收益，记执行记录**）。
- 与 T3 的联动已识别（openPath 拦截影响 start.bat 链）——执行顺序上 **T2 先于 T3**（任务书未显式排序·审计裁定顺序：T1→T6→T8→T5→T9 合批 → T2→T3→T7 → T4+F1-F4）。

### T3 EMC 入口内嵌 + 就绪 gate —— 可执行，1 项修正 + 1 项备注

- registerTab 真身已核：better-sidebar `ctx.betterSidebar.registerTab`（service.d.ts：registry + dedupeKey + createTab 机制），蓝本成立。
- **修正 M3（随执行生效·跨源限制）**：任务书「onload 后延时 1.5s 检测 DOM 根节点·缺失则 reload」——iframe 指向 8080、宿主在 3080，**跨源 iframe 的 contentDocument 不可访问**（同源策略），DOM 根节点检测不可行。替代：`onload` 事件 + 超时兜底 reload（首次 load 后设 1.5s 观察窗，无第二次 load 事件且 iframe 高度/内容不可判时 reload 一次）。效果等价（治半载），机制可行。
- **就绪 gate 与 no-cors 纪律衔接**：`/api/v1/health` + `/frontend/index.html` 双探测沿用 no-cors「送达即 resolve」口径（8080 不发 CORS 头·CB6 复盘问题 C 结论），不读 status。
- **设计风险备注 D-R2**：T3 用内嵌取代外部 Edge，是对 CB6 任务书需求 2 的**用户偏好覆盖**（任务书已明示裁定）——CB6 相关口径文档不回改，执行记录注明覆盖链即可。

### T4 计时取证 —— 可执行

- emc-test-headless 可跑 `dsh --profile` 命令行计时（CB6 指引已证 headless 路径可用）；web profile 需浏览器新会话（审计组无浏览器实操能力时降级：web 侧用既有 session log 计时分解·注明口径）。**只取证不改**纪律遵守。

### T5+T9 身份卡+人设+口径对照段 —— 可执行，1 项修正

- **修正 M4（随执行生效）**：FACTS 中**已存在简化版身份卡** `EMC-IDENTITY-01`（urban_renewal_knowledge.py:271-277·08-20 加入，仅欢迎卡文案）。任务书「追加一条 FACTS 身份卡」与现状重叠。**执行口径改为：原地扩写 EMC-IDENTITY-01 的 detail**（补「能做什么=七件工具+出图+口径引用义务 / 不能做什么=不编造·结论层禁作输入·宏观≠微观·不碰 QA 管线 / 口径纪律+T9 口径对照段纪律」），不新增重复卡、不动其他存量卡——仍在白名单文件内，语义更忠实于任务书目标。
- RAG 索引确认过期（现索引为 08-18 建·身份卡 08-20 入）——重建必须。
- dsh 侧人设 prompt 落 web+emc-test profile——属 dsh 环境改动，只落执行记录。

### T6 MCP 描述紧凑化 —— 可执行

- 实测 docstring 长度：list_data 245 / rag_query 272 / kb_facts 210 / outlet_card 138 / zonal_stats 434 / buffer 176 / rank 303 / render_spec 787。压缩重点=render_spec/zonal_stats/rank/rag_query 四件；outlet_card/buffer 已近达标（仅微调）。
- `tests/test_mcp_server_emc.py` **无 description 断言**（实测 grep 零命中）——无需同步测试，但执行后跑全量确认。
- 红线自查口径：只动 docstring 正文，签名/caliber/返回结构零变化；MCP 描述与 ai_qa/tool_contracts.py 分道（后者为 EMC 内部 FC 权威·不动）。

### T7 双模预设 —— 条件可执行（依赖 F1）

- **依赖修正（顺序）**：F1（per-session/per-step effort 覆写支持）是 T7 配置形态的前提——**F1 调研必须与 T7 同批先行**，若 dsh 无 effort 覆写能力，T7 降级为「分预设 + 工具子集 + step 预算（若支持）」，effort 档落「待 dsh 上游解冻后补」。
- step 预算 ≤15 是否为 dsh 原生能力：未知，随 F1 一并查证；无原生支持则以 profile 说明文档软约束 + 记录待裁决。
- 与 dsh 冻结期兼容性：新建 profile 目录/配置不动 dsh 仓代码——**兼容，无冲突**。

### T8 脚本参数化 —— 可执行，1 项备注

- 脚本全文已审：三参数（grid_size/source/region_caliber）改造面清晰；**遗留字段问题**（geojson/csv 混入 poi_names/place_name 等与本任务无关字段·Qoder 评估报告 R1）——**参数化改造时顺手清理输出 schema**（不改字段语义·删无关列），记执行报告。
- 口径对照段需引用全量基数（49,192）与注册表 K-01/K-02——注册表只读引用（禁改卡 ID 体系）。

### F1-F4 —— 可执行，并入 T2/T7 批次（见 T7 依赖）。

## 三 验收锚可执行性

| 锚 | 判定 |
|---|---|
| T1/T3/T5/T6 各自测试+commit | ✅ 可执行（T1 测试形态按 M2 调整） |
| T4 计时分解报告 | ✅ 可执行（web 侧口径见 T4 节） |
| T7 后 emc-analysis wall-time ≤2 分钟 | ⚠️ 部分依赖环境：需 8080/数据在位 + headless 可跑；**RAG 预热 20-30s + 工具冷启动 8-10s 是硬底**，2 分钟目标=模板直跑形态可达、自由探索形态不可达——验收时用「模板/工具直跑」类任务（最密集 10 社区=rank 一次调用），口径写进执行记录 |
| 浏览器实测四项（用户参与） | 属主手/用户侧，Qoder 提供可复现步骤清单 |

## 四 修正与待裁决汇总

| # | 类型 | 内容 | 处置 |
|---|---|---|---|
| M1 | 修正 | T1 扩 import 图层管理三函数 | 随执行生效 |
| M2 | 修正 | T1 测试改静态契约断言（运行时移交浏览器实测） | 随执行生效 |
| M3 | 修正 | T3 半载检测改 onload+超时兜底（跨源不可读 DOM） | 随执行生效 |
| M4 | 修正 | T5 原地扩写 EMC-IDENTITY-01（已存在简化卡） | 随执行生效 |
| Q1 | **待主手裁决** | 我此前 3 份未跟踪文档（CB42 诊断/架构评议发起/PT-CB6 审计报告）是否入库——建议随首个 PT-CB7 commit 一并提交（纯文档·CB 闭环需要），如主手不同意则保持未跟踪 | 先随提交·可回滚 |
| D-R1/D-R2 | 备注 | 「只留最新 [dsh] 层」语义裁定 / T3 内嵌覆盖 CB6 外部 Edge 口径 | 记录在案 |

---

> Qoder · 2026-08-21 · 审计完成，立即进入执行。
