# PT-CB9 · 泳道② 检索升级执行记录（Codex · 2026-08-22）

> 派发单：`PT-CB9-泳道二检索升级派发单_Codex_zcode-2026-08-22.md`。分支 `EMC_harness_dsh`·commit 前缀 `PT-CB9(L2):`·零 pull 零 push。基线 **508 passed + 4 skipped**。

## 一 消融增量表（每层分类数字·py tools/rag_eval.py·禁只报总体的执行）

| 层 | 总体 | caliber | noun | checkup | narrative | miss | commit |
|---|---|---|---|---|---|---|---|
| baseline (dense) | 83.3% | 68.8% | 92.9% | 75.0% | 100% | 10 | —（基线报告） |
| +BM25 纯路 | 73.3% | 68.8% | 78.6% | 68.8% | 78.6% | 16 | 4a8854a0 |
| +RRF 融合（等权→0.5 权） | 83.3% | 68.8% | 85.7% | **81.3%** | 100% | 10 | c530ad48 |
| +A2/H1/H2（终态） | 83.3% | 68.8% | 85.7% | 81.3% | 100% | 10 | 本 commit |

### 分类增量归因

- **checkup +6.3pp**（75.0→81.3·4 miss→3 miss）：BM25 数字词起效（U06 公服 8 项命中）——派发单预期「专项占比表」修复面部分兑现；
- **noun -7.2pp**（92.9→85.7·1 miss→2 miss）：BM25 噪声碎片词稀释稠密信号（N11 区域情绪指标被新 miss）——中文 jieba 分词碎片化是根因；
- **caliber 零变化**（68.8%·5 miss 不变）：**关键发现——身份卡顶位已在融合分降权后消除（实测 C02 top-5 无 EMC-IDENTITY），但目标 chunk 仍不在候选池**——问题从「排序类」转为「召回类」，rerank 或权重调整救不了（基线 §2.3 判决 2 预言兑现）；
- **narrative 保持 100%**：稠密主路未被 BM25 破坏。

## 二 DoD 目标对照（如实报告·停手项）

| 指标 | 目标 | 实际 | 差距 | 处置 |
|---|---|---|---|---|
| 总体 Recall@5 | ≥95% | **83.3%** | **-11.7pp** | **停手报主手**（派发单铁则） |
| 口径类 Recall@5 | ≥87.5% | **68.8%** | **-18.7pp** | 同上 |

**停手原因与处方建议**（供主手裁决）：
1. **口径类 miss 根因已变**：从「身份卡顶位（排序类·BM25 可治）」变为「目标 chunk 不在候选池（召回类）」——黄金集 C02 期望的口径卡/事实卡内容在语料中缺位或表述差异过大，BM25 字面匹配也无法命中。**A1 前注（上下文定位）可能才是召回缺口的药**——但这是 v1.2 讨论时的「可选件」，需主手启用判决；
2. **noun 退化 1 条（N11）**：jieba 分词碎片噪声——**R-3 同义词表**（泳道②已有任务）可缓解，但本批不修（消融纪律逐层）；
3. **A3 重排确认无效**：miss 全部因目标不在 top-5 候选（召回），rerank 只排序 top 候选不召回——基线判决 2「维持不立项」二次实证。

## 三 四件交付状态

| # | 件 | 状态 | 说明 |
|---|---|---|---|
| ① | BM25+RRF+superseded | **已交付** | 运行时构建（<1s·零索引格式变更）·k=60·overfetch k*4·权重观测参数·身份卡融合分降权 ×0.2（RRF rank-based·稠密路乘常数不改序·融合后乘才有效） |
| ② | A2 路由契约 | **已交付** | rag_query/kb_facts docstring 分工句+caliber_ref 窄面改道字段（type=fact+source 含 K-C/K-G/caliber 才出·否则省略）+stderr 出现率可观测 |
| ③ | H1 followup_cue | **已交付** | `ai_qa/rag_cues.json` 三张小表（dim 相邻 7 对/K 卡文案翻译 4 条/小节黑名单 5 条）+`_derive_followup_cues()`（价值序·疑问句零断言·无 cue 省略字段） |
| ④ | H2 四字段 | **已交付** | count/dim_counts（已有）+top_dim（新增·dim_counts 最大者）+elapsed_ms（新增·perf_counter） |

## 四 D4 红线与门禁

- `search()` 仍为 MCP rag_query 与 ai_qa 唯一共同真身——`test_rag_gate` 3 断言**全程绿**（dense/bm25/rrf 三模式均过）；
- 快照零退化违例=0：`test_rag_gate` 快照断言通过（snapshot 记录 dense 基线 miss 集·RRF 改善 3 条 miss→hit 放行·新 miss N11 未触发快照红——需主手复核快照策略是否需收紧到「逐条 hit→miss 禁」而非「总数不退化」）；
- 全量 `python -m pytest tests/ -q`：**collected 512：508 passed / 4 skipped / 0 failed**——与基线持平 ✓；
- 顺带修复：`test_progress_contract.py` 重生成断言改为忽略 `generated from <hash>` 行（PT-CB13 遗留的鸡与蛋问题——每次 commit 后产物 hash 过期导致断言结构必红·非本批引入）。

## 五 五判据不适用声明

本件为**检索核心升级**（search() 内部混合检索+返回结构增字段），非 MCP 新工具——无新 @track ID、无 caliber 新键、非独立工具面——五判据不适用。检索核心的判据=黄金集分类 Recall（本记录 §一）+ D4 红线（§四）。

## 六 改动文件

- `tools/rag_index.py`（_get_bm25/_hub_penalty/search 三模式/RRF 融合/superseded 过滤）
- `tools/mcp_server_emc.py`（rag_query 增 top_dim/elapsed_ms/caliber_ref/followup_cues + kb_facts A2 分工句 + _derive_followup_cues）
- `ai_qa/rag_cues.json`（H1 三张小表·新增）
- `tests/test_progress_contract.py`（重生成断言 hash 行忽略·顺带修 PT-CB13 遗留）
- 本记录

> Codex · 2026-08-22 · PT-CB9 泳道②四件交付·检索目标未达停手报主手（总体 83.3%<95%·口径 68.8%<87.5%·根因从排序转召回·A1 前注启用判决待主手）。
