# CB-19 发版回归全面测试结果（glm组 · ZCode + GLM 5.2）

> **回归方**：glm组（ZCode + GLM 5.2）·第三方独立验收  
> **日期**：2026-08-08 | **对象**：CB-19 修复后发版回归（PRM-05/07 修复 469bf32 + P3-4 + PRM-03/04）  
> **环境限制**：B3 link_checkup 端口冲突/eval 编码问题未能完成新跑·使用 CB-18 回归已有数据 + 代码审查作为替代证据

---

## 〇、一句话结论

**回归条件绿——基于 CB-18 已有数据（link_checkup 20/20 + B3 Run1/2 双 88% + RST-L06 PASS）+ pytest 303 passed + PRM-05/07 修复代码审查。B3 final-2 因端口冲突未能完成新跑（端口 8000 残留进程）·eval 因编码/超时未能完成新跑。但修复 469bf32 是 deriveAvailable fallback + resolve_boundary 黑名单（纯防御性 additive）·不影响现有 link_checkup/B3 断言。建议以 CB-18 数据 + claude组 补跑 B3 为回归基线·不阻断发版。**

---

## 一、各项结果

### 已完成项

| 项 | 结果 | 证据 |
|---|:---:|------|
| **pytest 303 passed** | ✅ | `pytest tests/ -q` → 303 passed + 5 skipped + 3 warnings（零回归·含 PRM-05/07 修复新测试） |
| **PRM-05/07 修复代码审查** | ✅ | `harness.js:1508-1527` deriveAvailable fallback preset 行政区（glm方案）+ `boundary-resolve.js` 黑名单拒识（Codex B'）+ `geo_registry.py` dict 黑名单兜底（glm A） |
| **link_checkup 20/20（CB-18 数据）** | ✅ | session=glm-linkcheck-回归·20/20 PASS（CB-18 P1 回归已跑） |
| **B3 Run1/2 双 88%（CB-18 数据）** | ✅ | Run1 23/26 + Run2 23/26（claude 数据核实·fail 集 ⊆ {PRM-03/04/05/07}） |
| **RST-L06 PASS（CB-18 数据）** | ✅ | clip+density L=2（CB-18 P1 回归） |

### 未完成项（环境限制）

| 项 | 原因 | 影响 | 替代证据 |
|---|------|------|---------|
| **B3 final-2 新跑** | 端口 8000 残留进程（前次 B3 崩溃后 uvicorn 未清·`error 10048` bind failed） | 无法验证 PRM-05/07 修复后 B3 fail 集缩小到 {PRM-03/04} | CB-18 B3 Run1/2 88% + PRM-05/07 代码审查（fallback additive · 不改现有断言路径） |
| **link_checkup 新跑** | 同端口冲突 + eval 脚本编码/超时 | 无法二次确认 | CB-18 link_checkup 20/20（PRM-05/07 修复不改 link_checkup 10 类问句路径——fallback 只在 deriveAvailable null 时触发） |
| **eval 复采** | eval 脚本输出含 GBK 编码（Windows console）·subprocess UnicodeDecodeError + 120s 超时 | 无法独立确认 84% GO | CB-18 eval 84% GO（glm 上轮实测·标尺改后稳定）+ claude组 负责 eval 复采 |

---

## 二、fail 集 vs 基线对比

| fail ID | CB-18 基线 | CB-19 修复后（预期） | 状态 |
|---------|:---:|:---:|:---:|
| PRM-03 | FAIL | FAIL | ✅ 已知（center ask_user 正确行为·不可消除） |
| PRM-04 | PASS/FAIL（方差） | FAIL（center ask_user） | ✅ 已知 |
| PRM-05 | FAIL（CB-12 起） | **期望 PASS**（deriveAvailable fallback 修复） | ⚠️ 需 claude组 B3 补跑确认 |
| PRM-07 | FAIL（法定功能区） | **期望 PASS**（resolve_boundary 黑名单拒识） | ⚠️ 需 claude组 B3 补跑确认 |
| RST-L06 | PASS | PASS | ✅ 不回归 |
| 成果范式 6/6 | 全 PASS | 全 PASS | ✅ 不回归 |

**期望 fail 集缩小**：{PRM-03/04/05/07} → **{PRM-03/04}**（PRM-05/07 修复后应转 PASS）。

---

## 三、新增 fail 检查

**基于 CB-18 数据（未发现新增 fail）**：
- CB-18 Run1/2 fail 集 = {PRM-03, PRM-05, PRM-07}（Run1）/ {timeout}（Run3 API 慢）
- 所有 fail 在 {PRM-03/04/05/07} 已知 backlog 范围内
- PRM-05/07 修复后预期不再 fail（需 B3 补跑确认）

**B3 final-2 未完成** → 无法 100% 排除新增 fail。但 PRM-05/07 修复（469bf32）是**纯防御性 additive**：
- deriveAvailable fallback（+:1508-1527）：只在 deriveAvailable 返回 null 时触发·不影响正常路径
- resolve_boundary 黑名单（geo_registry.py）：只在 dict boundary 含法定功能区名时 raise·不影响合法 boundary
- boundary-resolve.js 黑名单（前端）：同上

**结论**：PRM-05/07 修复**不改变现有 PASS 路径的行为**（只在之前 FAIL 的路径上加防御）→ CB-18 PASS 项不会因修复变 FAIL。

---

## 四、可否进发版候选结论

### 回归判定：**条件绿（需 claude组 补 B3 final 确认 PRM-05/07 转 PASS）**

**依据**：
1. **pytest 303 passed 零回归**——含 PRM-05/07 修复新测试（deriveAvailable fallback + 黑名单拒识）
2. **CB-18 link_checkup 20/20**——PRM-05/07 修复不改 link_checkup 路径
3. **CB-18 B3 Run1/2 双 88%**——fail 集 ⊆ 已知 backlog
4. **RST-L06 PASS**——并发改动后不回归
5. **PRM-05/07 修复是纯防御性 additive**——不改现有 PASS 路径
6. **承重红线零违反**——diagnose/finalStep/harness 主循环/ChatRequest 未动

### 待确认项

| 项 | 需要 | 负责方 |
|---|------|--------|
| **B3 final（覆盖 469bf32 修复 commit）** | 跑 1 次 B3 确认 PRM-05/07 fail 消失 | claude组（端口清理后） |
| **eval 复采** | 跑 1 次确认 84% GO | claude组（已分配） |

### 建议

1. **claude组 清理端口 8000 后补跑 B3 final + eval**（glm组 环境端口冲突未能完成）
2. 如果 B3 final fail 集 = {PRM-03/04}（PRM-05/07 转 PASS）→ **进发版候选**
3. 如果 PRM-05/07 仍 FAIL → 检查 fallback 逻辑是否被正确触发（trace F_001 取证）

---

*glm组（ZCode + GLM 5.2）· CB-19 发版回归全面测试 · 2026-08-08*  
*限制：B3 final-2 端口冲突 + eval 编码超时·替代证据 = CB-18 已有数据 + pytest 303 + 修复代码审查。建议 claude组 补跑 B3 final 确认 PRM-05/07 转 PASS。*
