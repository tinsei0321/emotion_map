# PT-CB6 · 外挂大脑批（B 变体主线）· 开工与拆解（zcode·2026-08-20）

> 主手：zcode。依据：用户裁定三（R36）——B 变体（dsh 当 EMC 外置大脑）升主线·尽可能优先。转正五条件转**计划式凑齐**；C12 十条**建设即生效**；R30 零自有 FC **正式落定**（B=对话转 dsh·EMC 只做壳+渲染）。
> 分支 `EMC_harness_dsh`（main 冻结）。commit 前缀 `PT-CB6(T{n}):`。基线 **416+3**。

## 目标图景（一句话）

EMC 前端一个聊天窗：用户自由提问 → 后端桥经**大脑端口**驱动 dsh（专用干净环境·headless）思考 → dsh 经全局注册的 EMC 七插座取数/分析 → 结果经**渲染契约**落地图 → 回答带 `brain: dsh@session` 溯源。dsh 断线时 A 通道按钮（直连插座）仍可用——降级显式。

## 📌 归类澄清（用户追问二·2026-08-20）

先行版（dsh 入口+8080 显示屏）**不是 B 变体**——入口方向相反（B=EMC 入口+dsh 脑·先行版=dsh 入口+EMC 屏）；B 变体本体=ACP 桥+薄壳对话窗（后移件）。渲染契约（三砖）**是平台共享资产**（先行版/B 变体/A 薄壳/场景 S/第三方渲染器共用·防资产绑单一消费者的形态3老错误）——它只是因 B 优先级而提前建设，先行版是其首次消费场景。

## ⚡ 先行件（用户 2026-08-20 追问定序·轻量先行版）：dsh 为入口 + 现有 8080 当显示屏

跳过 ACP 桥与薄壳，先通"你在 dsh 里问 → 结果铺上已打开的 8080 地图"（第二屏模式）。三砖：
- **P1 render_spec 第 8 插座**：dsh 调用→产出图层图纸（dataset_id+样式令牌+溯源标注 brain:宿主名）——复用 layer 对象/样式函数七成材料；
- **P2 投递通道**：后端 render 收件箱 + SSE 推送（复用 api.js 架子·v1 可轮询）→已打开的前端收图纸；
- **P3 前端 loader**：吃图纸→走现有 addLayer/renderLayer 铺层（令牌解析权威留 JS·双载防双源）。
验收：dsh 里说「把最差3区铺到地图上」→8080 出带样式图层。量级 1.5-2.5d（=原 T5+通道简化）。
**原 T3（ACP 桥）/T6（薄壳）后移为后续件**——先行版验证渲染契约后再建。

## 任务表（原排·调整后）

| # | 任务 | 执行 | 依赖 | 验收（R22 契约 DoD） |
|---|---|---|---|---|
| T1 | **大脑端口契约 v1**：四动词 start/send/status/stop+事件流 schema+状态对象+错误语义；轮次=责任窗口状态机（精髓 E5）；驱动接口与 ACP 适配分离（E7·换脑不换壳） | **zcode（攻坚）** | — | 契约文档+JSON schema 落盘；**契约产出=大脑端口契约（第 5 张平台契约）** |
| T2 | **dsh 侧实操确认**（B 卷欠账+ACP 细节）：ACP 五方法字段级样例/stdio JSON-RPC 握手/headless 启动命令/会话日志位置/profile 全局 MCP 注册（session/new 拒挂 MCP 的实测约束）/断线重连 | **dsh（调研·本机 checkout 可核）** | — | 确认报告落盘（file:line 级）——T3/T4 的地基 |
| T3 | **ACP 适配器桥**（EMC 后端 Python）：大脑端口四动词→ACP 映射；send 审计入 trace；审批三档自动应答（⑩·v1 只读+白名单写）；断线/超时→降级事件 | **zcode 设计+dsh 执行** | T1+T2 | 桥测试绿；断线降级可演示 |
| T4 | **专用干净环境**：dsh slim profile（base+EMC MCP 全局注册+最小权限+模式白名单）——拍板 8 硬条件+C12 落配置 | **dsh** | T2 | profile 配置+启动验证；权限矩阵对照 C12 十条 |
| T5 | **渲染契约 v1**=**先行件 P1+P3**（上表·提前为第一实施项）+P2 投递通道 | **zcode 设计+dsh 执行**（三步可拆） | —（与 T2-T4 并行） | spec 双载防双源；外部消费者可照图施工 |
| T6 | **薄壳前台**：chat 组件+后端薄代理——B 通道经大脑端口；A 通道快捷按钮直连七插座（同壳共存）；**零自有 FC** | **zcode 设计+dsh 执行** | T3+T5 | 双通道演示；dsh 断→A 兜底+显式提示 |
| T7 | **验收**：同任务三路对比（B 通道/手开 dsh/A 按钮）+降级测试+溯源标注检查 | zcode | T6 | 三路对比报告；R30 零自有 FC 实证 |
| 审计 | C12 十条合规重点审查 | Codex+claude | 完工后 | `PT-CB6-审计_*.md` |

**时序**：T1∥T2 起 →（T3、T4、T5 并行）→ T6 → T7。G1 行业表格顺后（指定项清单仍在挂账提取）。

## 纪律

C12 十条全程生效（产品链路）；范围外禁碰；dsh 执行窗口主手显式路径提交；**守卫禁宽 except 静默（A9 教训）**；报时查钟。
