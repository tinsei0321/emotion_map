# PT-CB9 · RAG 重建实施计划（分工版 v1.1 · 定稿候选）

> **性质**：实施计划 v1.1（综合 claude组 评估回应 `PT-CB9-RAG重建_回应_claude组-2026-08-21.md` 修订·**报用户确认后生效开工**）·2026-08-21
> **拟定**：zcode（glm组·主手）
> **上游链**：Qoder 审计（commit 8114d081）→ zcode 研判（1840e933）→ claude组 回应（692658b1·双机复现零翻案·三修+十补充+四盲点）→ **本件 v1.1**
> **拍板基线**：用户令转实施（6 问按推荐答案拍板·R53）；**v1.1 新增 2 问待用户裁决（Q-A/Q-B·见 §七·不阻塞无争议部分）**。

## v1 → v1.1 变更记录

| # | 变更 | 来源 |
|---|---|---|
| 修-1 | P0-3 门禁断言由「动态满分制」改为**基线相对制**（对 `docs/rag-baseline.md` 入档基线零退化+刚性判据 100%·满分断言留收口总验收）；skipif 扩为**索引缺失+依赖缺失两态** | claude 回应 §3.2 修-1 + §4.3-1 |
| 修-2 | 泳道②验收改**双层制**：开发期 `--index <快照>` 冻结语料测增量归因；合流后重建重测；验收数字必标语料时点 | §3.2 修-2 |
| 修-3 | 泳道③白名单补 `tools/rag_index.py`+`api/aiqa_routes.py`+`tools/mcp_server_emc.py` 三文件·注「②收口后独占」 | §3.2 修-3 |
| 补-A | 泳道②任务书吸收六项（成组原子/权重 spec/消融纪律/紧凑描述/同义词数据文件/验收措辞） | §3.3 |
| 补-B | 泳道③任务书吸收四项（出口卡链路字节级保 API/X2 阈值挂余弦/契约同步精确化/loader 全文纪律） | §3.4 |
| 补-C | 盲点四条入任务书：C-D 同源去重标注/口径优先级声明扩展/双机重建确认/E7=47KB | §4.4 |
| 补-D | P2 重启任务书预埋 4 护栏；P0 产出 loader 接口契约半页 md；泳道②验收含 browser e2e 手跑 | §1.2/§3.1/§4.2 |

一、目标与范围、二、阶段泳道总览同 v1（`PT-CB9-RAG重建实施计划_分工版_v1_zcode-2026-08-21.md` §一§二·不重复·仍有效）。

## 三、任务分解（v1.1 修订版·仅列变更与新增·未提及项沿 v1）

### P0 基线（zcode·先行独跑·0.5-1 天）

| 任务 | v1.1 内容 | 验收 |
|---|---|---|
| P0-1 黄金集 14→60 条 | 同 v1 + 新增条目附出处注记与「同源谱系」标注（为 C-D 去重铺路） | 60 条·口径类/精确名词类/体检域类各 ≥8 |
| P0-2 基线入档 | 同 v1（office/家机双机数字） | `docs/rag-baseline.md` 含 Recall@5/分类型命中率+测量语料时点 |
| **P0-3 门禁接线（修-1/修-补）** | 新建 `tests/test_rag_gate.py`：断言=**读 rag-baseline.md 对比零退化** + 越维/案例刚性判据动态计数 100%；**skipif 两态**（索引缺失 OR `sentence-transformers` 不可 import→importorskip）；模型加载 module-scope fixture 单次；新 pytest 标记先注册 `pytest.ini markers`（--strict-markers） | 标准门禁收集到·本机过·无索引/无依赖机器 skip 不红·**开门不红** |
| P0-4 旧断言改造 | 同 v1（browser 回归测试薄壳化复用同一逻辑） | 两处零漂移 |
| P0-5 依赖登记 | 同 v1（rank_bm25+jieba 锁版+Py3.14 注记） | Ops 在档 |
| **P0-6（新增）loader 接口契约** | 半页 md（或 docstring）：chunk dict schema（含 status/confidence/source 谱系字段）+ loader 函数签名——供泳道②对着编码、泳道③迁移复用 | 契约文件入 `docs/`·claude组 确认可编码 |

### 泳道① 内容（zcode 主·dsh 协力）

| 任务 | v1.1 增补 | 验收 |
|---|---|---|
| C-A/B/C/E | 同 v1；**新 loader 全文纪律**：片段全文存 meta（CB-22 承重教训·沿现有模式+防回归断言）；X-01 作废卡 retired 默认过滤 | 同 v1 + 全文断言过 |
| **C-D 成文 md 39 份入库（盲点-1/2）** | **同源去重标注**：39 份 md 为 282 段蒸馏笔记上游·入库时按 source 谱系/内容相似度去重或显式「同源」标注（防近重复段 RRF 双 boost 挤真答案）；**口径优先级声明扩展**：03-10「压旧口径」模式随入库扩到数据包 md（防「未作废但已过时」数字竞争回潮） | +60~150 段·同源标注 100%·黄金集刚性判据零退化 |

### 泳道② 检索（claude组·认领·与①并行）

| 任务 | v1.1 增补（补-A） | 验收（修-2 双层制） |
|---|---|---|
| R-1 BM25 路 | **成组原子**：vectors/meta/**bm25.pkl** 三件 `build_id` 成组·加载校验扩到三件（现状只盖两文件）；增量构建（content_hash 跳过） | 快照上增量归因可测 |
| R-2 RRF 融合 | **权重语义先定 spec**：fact×1.2 作用在融合前还是融合后·实施前写实现说明（研判实测在融合前·两种语义结果不同）；每类命中率落为输出字段 | **主验收=精确名词类全对**；整体 Recall@5 ≥ max(基线+10pp, 95%)·基线 ≥90% 时验零退化（**Q-B 封顶条款·待用户裁决**）；三类刚性零退化；**数字标注测量语料时点** |
| R-3 前缀+同义词表 | 同义词表**落数据文件**（非硬编码）+单测+术语纪律注记（=查询扩展映射·非新概念创造） | 前缀生效·表有单测 |
| R-4 元数据过滤 | MCP 加可选参数守 **T6 紧凑描述纪律**（每参数 ≤1 行） | 三入口零改动·**多入口逐入口验证+browser e2e 手跑一次**（防 E5 链重演·BM25 改变 finalStep 收片风格须肉眼过） |
| R-5 对账 | 同 v1 | in_n/out_n 日志 |
| 消融纪律（新增） | 前缀 / BM25+RRF / 权重调参**分 commit 各自重跑黄金集记增量** | 每层增量可归因·可单独回退 |

### 泳道③ 工程（claude组·认领·①②后）

| 任务 | v1.1 增补（补-B） | 验收 |
|---|---|---|
| E-1 knowledge/ 搬家 | **出口卡链路硬约束**：生成镜像字节级保 `all_facts`/`query_knowledge_base`/`build_outlet_schema` 三 API（双消费点 grep 实证：aiqa_routes:99 + mcp_server:327） | 出口卡链路零回归（/aiqa/outlet_card+MCP+test_outlet_schema 全绿） |
| E-2 industry_kb 收编 | **契约同步精确化**：行为兼容=契约零变→铁律 11 三处同步**实际不触发**（tool_contracts 列白名单仅备不时）；若实施发现契约必须变→**回头修 v1.1 白名单**·禁现场扩权 | kb_facts 回归零退化 |
| E-3 体验件 | **X2 阈值挂向量余弦 Top-1**（0~1 天然校准）·RRF/重排序分仅记录（名次函数不可跨配置比较）；X1 靠 search() 加字段自动透传（路由零改动·grep 实证） | X1/X2/X3/X5 件件有测·X4 不做 |

### 收口（zcode）

同 v1 + 两条：**泳道②验收数字复核语料时点标注**；满分断言（黄金集扩容后全绿）在收口总验收执行（修-1 分层）。

## 四、白名单（v1.1 修订）

沿 v1 §四，变更一处（修-3）：

| 批 | 文件 |
|---|---|
| 泳道③（claude组） | 新建 `knowledge/`、`ai_qa/outlet_kb/*`、`ai_qa/industry_kb/*`、`ai_qa/tool_contracts.py`（仅备不时·见 E-2）·**+ `tools/rag_index.py` + `api/aiqa_routes.py` + `tools/mcp_server_emc.py`（三文件 ②收口后独占·E-3 体验件落点）** |

其余批次白名单不变（P0/泳道①/泳道②/冻结区见 v1 §四）；`rag_index.py` ①②共同战场协调规则不变（zcode 打底+仲裁）。

## 五、执行纪律（两组通用）

同 v1 §五全部六条（commit 分权/trace session PTCB9-*/埋点连续占号/交付口径含重启+重建索引/离线纪律/DoD）+ 新增：
7. **消融纪律**：泳道②每层改动分 commit 重跑黄金集记增量（R 消融）；
8. **双机重建**：①②改 build 代码后 office/家机两机都必须重建索引再跑门禁（盲点-3）。

## 六、P2 重排序重启任务书预埋（4 护栏·claude §1.2）

触发条件不变（内容+BM25 后 Recall@5<95% 或精确名词类未全对）。重启时任务书必含：① 纯打分不进控制流（管线恒为召回→融合→精排→Top-K）；② 零 LLM API·零 token 生成（仅本地 CrossEncoder 复用 sentence_transformers）；③ `rerank=False` 可关路径同受黄金集回归覆盖；④ 模型名+版本写 build meta（沿 embedding_model 先例）·换模型=破坏性变更全量重建+回归。

## 七、待用户裁决（2 问·不阻塞无争议部分开工）

- **Q-A 门禁快慢两档**：推荐=快档进 pytest（刚性判据+抽样 recall·约 10s）+全量 60 条手动跑带 trace session；备选=全量进门禁（标准门禁 +60~90s）。**zcode 附议推荐**（门禁的价值在「每次都跑」·10s 档才能活下来）。
- **Q-B 泳道②验收封顶条款**：推荐=「精确名词类全对为主验收+整体 ≥max(基线+10pp, 95%)·基线 ≥90% 时验零退化」；备选=严格 +10pp（90% 基线下=要求 100%·有不可达风险）。**zcode 附议推荐**（封顶条款防不可达验收·主验收钉在实测痛点上）。

## 八、开工流程

用户确认 v1.1（+Q-A/Q-B 裁决）→ **P0 即刻开工（zcode）**→ P0-2/3/6 完成即解锁泳道①②并行（①zcode+dsh / ②claude组）→ ③（claude组）→ 收口（zcode）。PT-CB8 在途件收口为泳道开工前置（用户令如调整以令为准）。

---

> 维护：zcode。v1.1 = 定稿候选；用户确认后即为开工版本。变更记录见文首；v1 原文留档不删。
