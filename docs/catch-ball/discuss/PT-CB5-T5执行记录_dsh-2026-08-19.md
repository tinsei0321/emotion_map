# PT-CB5 · T5 执行记录（dsh·2026-08-19）

> 执行：dsh（副手）· 2026-08-19 · 分支：`EMC_harness_dsh`（main 冻结勿碰）。
> 任务：三条审计观测顺修包（T5a/T5b/T5c 各自独立 commit+push）。

---

## 一 三件销号

### T5a · manifest gate 测试 Windows GBK 自适配 — ✅

- 文件：`tests/test_manifest_gate.py`
- 修法：subprocess 不再固定 `encoding='utf-8'` 解码子进程输出，改为 `capture_output=True` 取原始字节后**先严格 UTF-8、再 GBK、最后 UTF-8 replace** 的自适应解码（`_decode_output`），Windows 默认编码下 `py -m pytest tests/` 不再依赖 `PYTHONIOENCODING=utf-8`。
- commit：`37dfbc7b PT-CB5(T5a): manifest gate 测试 Windows GBK 输出自适配`

### T5b · 口径注册表 K-C1 / X-01 补显式值锚点 — ✅

- 文件：`docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md`
- 修法：两卡均采用**卡尾补行**方式补 `值锚点` 字段（表格列与语义保持不变）：
  - K-C1：193/174/137/130/154/1682 六枚举各指向成文/物理出处（geojson/csv/manifest 条目）；
  - X-01：87.9% / 5,615 / 港务 1,153 / 双高 16/32/3/26 / 公服设施 1,068 / 194 行矩阵与 675 行占比表，各指向成文出处。
- commit：`5d3505e6 PT-CB5(T5b): 口径注册表 K-C1/X-01 补值锚点字段`

### T5c · check_caliber v2 全角变体增强 — ✅

- 文件：`tools/check_caliber.py`、`tests/test_check_caliber.py`
- 修法：
  - 新增 `_FULLWIDTH_MAP`（全角数字 ０-９、全角逗号 `，`、全角句点 `．`、全角百分号 `％` → 半角）；
  - 作废值生成变体前先归一化；扫描文件每行也归一化后再匹配——`８７．９％`、`５，６１５` 与半角写法双向同义；
  - 新增 2 个测试：素材全角写法命中、注册表全角写法命中共计 9 个用例。
- 实测：`py tools/check_caliber.py` 仍为既有 6 处真实命中（87.9%×4 / 双高3 / 双高 16），全角改动未引入新误报。
- commit：`98a55c6c PT-CB5(T5c): check_caliber v2 全角数字/全角逗号变体 + 测试`

---

## 二 验收

- `py -m pytest tests/test_manifest_gate.py -q`：1 passed。
- `py -m pytest tests/test_check_caliber.py -q`：9 passed（T5c 新增 2）。
- `py -m pytest tests/ -q`：**403 passed + 3 skipped**（基线 401+3，上浮 +2 = T5c 全角用例，已注明）。

---

## 三 提交清单

| commit | 内容 |
|---|---|
| `37dfbc7b` | T5a manifest gate GBK 自适配 |
| `5d3505e6` | T5b K-C1/X-01 值锚点 |
| `98a55c6c` | T5c check_caliber v2 全角变体 |
| 本记录 | PT-CB5 T5 执行记录 |
