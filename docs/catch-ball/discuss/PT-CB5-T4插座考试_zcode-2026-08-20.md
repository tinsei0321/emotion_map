# PT-CB5 · T4 插座考试报告（zcode·2026-08-20 凌晨）

> 考官：zcode（主手）。考题=真实任务：**「各行政区情绪排名，给出最差 3 个区，并说明口径」**。
> 方法：双路实测——老路（bash 考古·按新会话 agent 发现路径真实执行每一步探索）vs 插座路（直接调用 MCP 工具函数=工具面等价·传输层已由 stdio 冒烟另证）。
> 诚实声明：主手执行存在"已知内情"优势，故老路按**保守下界**计（探索命令全部真实执行、未跳步；真实新 agent 预期更差——走错目录/猜错列名的概率未计入）。

---

## 一 老路（bash 考古）实录

| 轮 | 动作 | 结果 |
|---|---|---|
| R1 | `ls DATA/` + `ls DATA/analysis/` 盘点目录 | 找到数据区 |
| R2 | `head -2` L2 CSV 认列 | 40 列中定位 lon/lat/score/polarity |
| R3 | `ls presets \| grep 行政` 找边界 | 行政区.geojson 落位 |
| R4 | `json.load` 认边界字段 | **❌ 真实返工：UTF-8 BOM 报错** |
| R4fix | 改 `utf-8-sig` 重试 | props=[MC] ✓ |
| R5 | 盲写 sjoin 脚本运行 | 出结果 ✓ |

**计 6 轮、1 次参数错误（BOM）、口径标签 0 条**（"mean score 排序"的口径与边界全靠 agent 自拼·无 usage/社区口径/结论性质提示）、溯源弱（靠脚本路径自证）、敏感泄漏 0。

## 二 插座路实录

| 轮 | 动作 | 结果 |
|---|---|---|
| R1 | `list_data()` 开卷 | presets 命中 admin_district；caliber refs=[G-2,K-C1] 自动附 |
| R2 | `zonal_stats(boundary='admin_district', layer='yichang_l2_t1', agg_cols=['score'], top_n=3)` | 一次成型 ✓ |

**计 2 轮、参数错误 0、caliber 四键自动**（每行自带 polarity_index/domain_top/issue_label/attribution/建议）+ TRACE 溯源（F_021/F_025 出站记录）。首调延迟 8.4s（geopandas 冷启动+POI 富化·工具描述已声明·属预期）。

**双路结果交叉验证**：一致——最差三区同为 伍家岗区(0.4217)/猇亭区(0.4331)/西陵区(0.4419)（老路 mean score 与插座 score_mean 同源同值）。

## 三 及格线五条判定（拍板清单第 5 项）

| 及格线 | 实测 | 判 |
|---|---|---|
| 来回尝试省 ≥30% | (6-2)/6 = **67%** | ✅ |
| 人工纠正 ≤50% | **0**（插座路零纠正） | ✅ |
| 零敏感信息 | 0（聚合值·无样例原文/身份字段） | ✅ |
| 每个结论可溯源 | 工具名+追踪号+输入参数全留痕（TRACE 出站） | ✅ |
| 口径清楚 | caliber 四键+attribution+refs 指注册表卡 | ✅ |

**结论：五条全过——插座考试通过。** 「正道好走」首份基线证据：**6 轮→2 轮（-67%）·1 错→0 错·口径 0→自动**。

## 四 考试发现（两件·T3 跟进）

| # | 发现 | 定级 | 处置 |
|---|---|---|---|
| F1 | **list_data 点层漏报**：point_layers 仅返 checkup_12345_2024 一层，而 zonal_stats 实际可解析 yichang_l2_t1 等 9 层（清单与可解析集不一致——疑 available/usage 过滤口径错） | P1（开卷面失真·误导宿主） | 派 dsh 修复+补测试（清单=resolve 可解析集） |
| F2 | zonal/rank/buffer 首调 8-10s（冷启动） | 预期（描述已声明） | 无需处置；v2 可选常驻预热 |

## 五 R22 契约产出

本批（T4）契约条目：**考试契约固化**——及格线五条+双路对照法（含"已知内情按保守下界"声明义务）成为「正道好走」的标准量尺（后续工具上线复用本考式）。
