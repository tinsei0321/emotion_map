# PT-CB9R-D 执行记录 · B3 治理标注返回结构透出（dsh · 2026-08-23）

> 派发：用户直接令（2026-08-23·「执行 PT-CB9R-B3…commit 前缀 PT-CB9R(D):」）——任务重排件《平台化愿景登记与任务重排》RAG 执行②行「PT-CB9R-B3：撤销件按新方向复活·规格到行·执行=dsh」的落地。
> 规格锚点：`PT-CB9R-增量运营批任务书` 件①（B3 不对称标注+suppressed_count）+ 用户令确定性规格。任务书页脚「批状态：暂停」已被后续用户令取代（HEAD d8062ca4「任务重排三线…dsh B3复活」+ 本令）——本件按最新用户令执行。
> 分支 `EMC_harness_dsh`·commit 前缀 `PT-CB9R(D):`·零 pull 零 push。

---

## 〇 一句话结论

**B3 完成·门禁全绿**：rag_query/kb_facts 返回结构不对称治理标注落地（现行零标签·X-01 关联历史叙述类带 `caliber_note:'历史口径·现行见<替代指针>'`·确定性零 LLM）+ 顶层 `suppressed_count`（search active_idx 过滤处观测·纯计数零过滤改动）；检索排序/过滤零触碰；96.7% 逐类全同零退化。**执行中发现与 Kimi A2 同仓并行（共享两文件）——按 L1 泳道分区先例做不相交增量，A2 先 commit（f09a5620）把本件生产 hunk 一并席卷入库；本线 commit 只含测试+记录（hunk 级归属见 §四）。**

---

## 一 规格落地对照

| 规格（用户令/任务书件①） | 落地 | 证据 |
|---|---|---|
| ① rag_query/kb_facts 返回条目不对称标注·现行零标签 | `tools/rag_index.py` `_history_note()`（直接命中/谱系链式两级·零 LLM）；search 三模式分支条目级标注；kb_facts 谱系判定 | 冒烟实测：「双高 16 格」k=10 → B3B4#0/总纲#3/B3B4#3 三条带注·非登记条目零 `caliber_note` 键 |
| ① 历史叙述类带 `caliber_note:'历史口径·现行见<替代指针>'`·判定数据=_SUPERSEDED_SOURCES/_LINEAGE_MAP+L3 登记表 | `_X01_HISTORICAL_NARRATIVE`（6 chunk·L3 A 行/裁定 active 行逐条映射）+ `_X01_SUPERSEDED_POINTERS`（2·谱系反查） | §二逐条映射表 |
| ② 返回顶层 `suppressed_count`（search() active_idx 过滤处可观测） | search 三模式返回均带 `suppressed_count = len(metas)-len(active_idx)`；rag_query 透传 | 实测=2（与 `_SUPERSEDED_SOURCES` 二条一致） |
| ③ 不动检索排序/过滤 | 零改动：active_idx/fused/order/score 逻辑一行未动·仅新增观测与字段 | diff 审阅+96.7% 逐类全同 |
| ④ D4 双入口断言扩展 | `test_dual_entry_single_core_d4` 扩为三重一致性（sources/caliber_note/suppressed_count） | tests/test_rag_gate.py |
| ④ +2 测试 | `test_x01_history_annotation_existence`（登记↔语料一致性+派生确定性+检索面可见性）/ `test_current_entries_zero_label`（全语料 meta 级零标签） | 同上·5/5 绿 |
| ④ 全量 pytest 不降 + rag_eval 96.7% 零退化 | 见 §五 门禁 | — |

---

## 二 登记表逐条映射（R24：chunk 级·原文引句核验在案）

`_X01_HISTORICAL_NARRATIVE`（历史叙述类·登记但 active）→ 替代指针（逐条来自 L3 执行记录 §三）：

| 登记 chunk | L3 登记表行 | 判定/理由 | 替代指针 |
|---|---|---|---|
| `00-宜昌专项/_口径注册表.md#3` | 行 1/2/3/13/14 | 作废数字卡自身（X-01 卡·87.9%/5,615/港务 1,153/194 行/675 行/page7 密度版） | X-01 卡替代列（K-01/K-02/K-03/K-04 等） |
| `3prime/B3B4_归纳与落图_交付_2026-08-12.md#0` | 行 5 | 数据源/管线记述（「双轨密度管线（16 格·可复现）」） | K-03 两表 |
| `3prime/B3B4_归纳与落图_交付_2026-08-12.md#3` | 行 6 | 交付清单事实登记（「双高区…16 格·5KB」） | K-03 两表 |
| `3prime/分析计划与内容_总纲_2026-08-12.md#3` | 行 8+主手裁定 | 计划状态记述（「双高区 ✅ 16 格…」引句实证在 #3） | K-03 两表 |
| `3prime/分析计划与内容_总纲_2026-08-12.md#4` | 行 8+主手裁定 | 平台操作指引（Range tab「双高区」图层名） | K-03 两表 |
| `3prime/分析计划与内容_总纲_2026-08-12.md#5` | Qoder F-03 补入+主手裁定 | 进度记述（「✅ 双高 16 格/聚拢 57-81%」） | K-03 两表 |

`_X01_SUPERSEDED_POINTERS`（谱系链式反查目标端·chunk 已被检索层滤除）：

| 源 | 替代指针 |
|---|---|
| `3prime/占比表_民生_公服设施_社区_2026-08-12.md#0` | K-04 全覆盖版 165 点 |
| `3prime/B3B4_归纳与落图_交付_2026-08-12.md#2` | K-03 两表 |

---

## 三 待裁清单（语义判定不确定·报主手）

1. **L3 登记表行号勘误**：L3 执行记录 §三 行 1/2/3/13/14 引 chunk `_口径注册表.md#4`——实测作废数字卡在 loader 第 **3** 节（`#4` 为「四 数据治理规则卡」G 卡）。本件按 **#3** 实现。建议 L3 记录随归档批注（不回改原文）。
2. **素材表#0（L3 行 7/12）不入标注面**：实测该 chunk 因 loader 2000 字截断，检索可见文本内无 dual_high/1068 行（作废值行在截断区外）——消费方看不到作废值即无需警示。若未来素材表改行级切分，需补登记。
3. **03-10（L3 行 10）零标签**：现行口径文档·自带取消声明（「3prime/ 系列均为旧口径…一律以本笔记为准」）——非历史叙述类，标注反会误导。
4. **口径注册表#6（挂起清单·含「5,615/港务 1,153 语义详注」）未登记**：L3 登记表未列该 chunk·保守不扩登记面。是否入标注面待主手裁（加一行即生效）。
5. **suppressed_count 承载面**：kb_facts 为确定性关键词查询·无 superseded 过滤面 → 该字段仅 rag_query/search 承载；kb_facts 零命中由 A2 的 `no_hit` 信号覆盖（不重复造轮子）。
6. **「lineage 非空且关联 X-01」谱系链式机制**：已实现（`_history_note` 二级判定）但当前语料零命中（67 张事实卡 lineage 目标均非 X-01 关联集·grep 实证）——机制为未来登记兼容·`test_current_entries_zero_label` 全语料兜底。若主手判该机制多余可整段删（仅删函数一个分支·测试同步）。
7. **并行合入先例确认**：A2（Kimi）与 B3（dsh）hunk 同文件合入 f09a5620——hunk 不相交·零冲突。供主手确认本仓「并行线共享文件·先 commit 者席卷后到 hunk」的合流惯例是否要成文。

---

## 四 改动清单与 hunk 归属（并行合入说明）

**已随 f09a5620（PT-CB9R(A2) commit·Kimi 14:48:56）入库的本件 hunk**：

- `tools/rag_index.py`：`_X01_HISTORICAL_NARRATIVE`/`_X01_SUPERSEDED_POINTERS`/`_history_note()`（:153-194）；search() docstring B3 行 + `_suppressed` 观测 + 三模式分支 `_entry`/`caliber_note` 标注 + 返回 `suppressed_count`。
- `tools/mcp_server_emc.py`：rag_query docstring B3 行 + `'suppressed_count': r.get('suppressed_count', 0)` 透传；kb_facts docstring B3 行 + 治理标注块（`_history_note`/`_LINEAGE_MAP`·失败降级 `_safe_print` 留痕非阻塞）。

**本线 commit（PT-CB9R(D):·显式路径·零 pull 零 push）**：

- `tests/test_rag_gate.py`：D4 扩展（三重一致性·k≤10 夹取注记）+ 2 测试。
- 本记录。

> 合入说明：执行中发现 Kimi A2 同仓在途（rag_index.py/mcp_server_emc.py 同文件·mtime 14:35-14:38 持续写入）。按 L1 泳道分区先例（「未碰 search() 一行」反面案例的合规版）做不相交增量编辑；A2 先 commit 将两共享文件全量入暂存——本件生产 hunk 随之入库（HEAD 已核：`_X01_HISTORICAL_NARRATIVE`/`_history_note`/`suppressed_count` 均在 f09a5620）。本线 commit 只含测试+记录层，hunk 级归属如上。

---

## 五 门禁（红线三连）

| 项 | 结果 |
|---|---|
| 全量 pytest | **522 passed + 2 skipped·0 failed**（基线 517+2 dsh 本机实测 → A2 +3（Kimi 报告 520）→ B3 +2 = 522·**不降**） |
| rag_eval | **96.7% 逐类全同零退化**：caliber 93.8（16）/ checkup 93.8（16）/ narrative 100（14）/ noun 100（14）·刚性 2 例过 |
| 索引重建 | **未执行·无需执行**——本件为查询期字段与观测改动·chunk text/metas 零改动（重建反而无谓）·构建摘要不适用 |

验证口径：rag_gate 5/5 绿（含 D4 扩展+2 新测试）·test_mcp_server_emc rag 三用例绿（fake-search 兼容性验证·`suppressed_count` 缺省回 0 不破既有契约）·冒烟实测（rag_query「双高 16 格」k=10 三标注/suppressed=2·kb_facts 零标注·「作废 口径」top1=口径注册表#3 带 X-01 替代列指针）。

---

## 六 纪律自查

- [x] 零 pull 零 push·本地仓即最新·显式路径 commit（测试+记录·前缀 PT-CB9R(D):）
- [x] 检索排序/过滤零触碰（active_idx/fused/order 逻辑 diff 零行改动·仅新增观测与条目字段）
- [x] 零新追踪 ID（复用 F_015/F_022/F_023 既有面）·零 emoji·ASCII 标记·`_safe_print` 走既有件
- [x] 确定性零 LLM（登记表查表·无任何模型调用）
- [x] 他组在途未触碰：`docs/progress.md`（M 未提交·非本件）、`.codebuddy/`/`.qoder/`/`.workbuddy/`、Qoder 未提交讨论件、`_tmp/` 脚本——均未纳入 commit
- [x] dsh 官方纯净版纪律无关本件（EMC 仓内件）

---

> dsh · 2026-08-23 · PT-CB9R-B3 交付 · 待主手 zcode 回收（待裁清单 §三 7 项随件上报）
