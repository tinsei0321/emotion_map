# PT-CB15 · 出图口径双 Bug 三报告交叉评审（Qoder·2026-08-25）

> 评审对象：claude《出图与口径Bug根因分析》· zcode《根因复核与修复计划书》· Kimi《新Harness出图双bug根因分析与修复计划书》（均 2026-08-25）
> 评审方法：**不采信任何一份报告的结论**，对三份报告的矛盾点逐一实测定谳（坐标级比对 / resolve 实测 / 源码 / mtime / 索引全文检索），全部结论附实测数字。验证脚本留存 `_tmp/verify_three_reports.py` 可复跑。

## 白话摘要

三份报告把两个 bug 查了个七七八八，但**对第二个 bug（7 个社区边界粗糙）的定性，三份报告给出了三种说法，只有一家的说法经得起我逐坐标核验**：claude 和 zcode 都说图是 AI「自己随手画的」，Kimi 说图是 AI「照着真图描的、只是描的时候只抄了零头」。我把图纸上全部 80 个坐标点和真实社区边界的坐标逐个比对——**80 个点全部是真图的点，一个不差**。随手画不可能画得和原图分毫不差，所以 Kimi 对、另外两家错。这个差别直接决定修法：如果是「乱画」，改说明书让 AI 别乱画就行；如果是「照着描但抄不全」，就必须改通道——让大图不过 AI 的脑子，像快递贴单号一样直接取件。另外我查出 Kimi 报告也有一处硬伤：它说 193 个社区那层「已注册可用」，实测那层**调不出来**（登记表里的文件地址是搬家前的旧地址），用户已拍板「以后默认用 193 层」，若不先修这个旧地址，拍板落不了地。三家能力一句话：Kimi 推理最深（定性一锤定音）、zcode 复核最规范（三条新证据全成立）、claude 取证最广（但两处定性翻车）。

## 一、独立验证结果（三报告矛盾点定谳）

| # | 矛盾点 | claude | zcode | Kimi | **我的实测定谳** |
|---|---|---|---|---|---|
| 1 | Bug2 几何来源 | 凭位置记忆**手绘** | 退化到 LLM **自画** | **转录抽稀**（几何真实经过上下文） | **Kimi 对**。spec 80 个坐标点 vs 130 层/193 层源坐标：**80/80 = 100% 精确命中**（6 位小数级）。幻觉/手绘不可能 100% 命中源顶点 |
| 2 | spec 顶点数 | 「仅 18 个/面」（单值） | 18/6/5/12/11/20/7 | 17/5/4/10/13/19/12 | 实测外环顶点 = 18/6/5/12/11/20/7。**zcode 精确**；Kimi 用的是去重后点数（闭环首尾重合少 1）·数字自洽但口径未标注；claude 取了朝阳路单值以偏概全 |
| 3 | layer_output 简化条件 | >200KB 才简化（对） | **>100 顶点即简化（错）** | >200KB 才简化（对）＋发现 docstring 与实现不符 | 源码 `_layer_output_geojson` L350：简化仅在序列化 >200KB 触发；docstring L327「顶点>100 simplify」确与实现不符。**zcode 读了 docstring 没读实现**；Kimi 的「文档漂移」发现成立 |
| 4 | 193 层（base_community_area）消费面 | （未实测·说 MANIFEST 常量未同步） | **resolve FAIL**（file 旧路径断裂） | **「已注册可用」**·模型「没有用」 | 实测 `resolve_boundary('base_community_area')` = **FileNotFoundError（`../../analysis/体检对象_社区_面.geojson` 未落盘）**；130 层/174 层 resolve OK。**zcode 对，Kimi 错**。且 list_data 有 available 过滤——193 层**根本不在模型可见清单里**（模型不是「没用」，是「看不见」） |
| 5 | MANIFEST 常量是否迁移同步 | **未同步·迁移中断**（误报） | 常量已同步·断的是 manifest 内部 file 字段 | 未提 | 实测 `mcp_server_emc.py` L50：`MANIFEST = DATA/REGISTRY/presets/manifest.json`（已同步·文件在）。**claude 误报，zcode 纠正正确** |
| 6 | 「两个方面」叙事来源 | 旧口径文档/知识卡 | 口径注册表 L15 | **manifest page7 label**＋索引 0 命中 | 索引 meta.jsonl 实测「两个方面/两方面」**0 命中**；manifest 实测 page7 三层 label 含「**两方面合计**」。**Kimi 定位最准**——模型的「两个方面」来自 list_data 可见的 label，不是 RAG 索引 |
| 7 | RAG 索引 08-20 / episodes 08-25 14:17 | ✓ | ✓ | ✓ | 实测一致：rag_index 08-20 17:16·episodes 08-25 14:17——三报告共识成立 |

**Bug2 完整真相（综合定谳）**：模型确实调了 `zonal_stats/rank(boundary=checkup_cfg_community_xlwj(130层), top_n=7, layer_output=True)` 拿到了 7 社区**真实精确几何**（合计约 2300 顶点·约 60-100KB < 200KB 不触发简化）→ 模型向 render_spec 转录时只能抄出 80 个点（抽稀率 1.2%~7.6%）→ 边界粗糙但顶点全真。**断点不在工具、不在引导缺失，在「内联档要求几何穿过模型上下文」的契约设计**。

## 二、评价①：报告质量与修复计划可行性

### claude《出图与口径Bug根因分析》

| 维度 | 评 | 依据 |
|---|---|---|
| 证据质量 | **高** | spec 产物逐条核验、mtime 链、rag_query 实测、历史同模式 spec（1787634299060）——取证面三份最广 |
| 根因定性 | **Bug1 准 / Bug2 错** | Bug1 三层根因链（索引未重建/完善落点脱节/旧素材仍在）全部实测成立；Bug2「凭位置记忆手绘」被坐标比对**证伪**；MANIFEST 常量误报 |
| 修复计划可行性 | **中** | 「前提修正」章节（推翻自己「工具面缺能力」预设）体现自纠品格；P1-1 AGENTS.md 方法论补全方向有效（但边际价值低于其声称——见第三节）；P1-3 `select_boundary_subset` 治的是「按名子集」场景，**非本次 TOP7 聚合链路**·对本案无效；P1-2 沙箱放宽需安全评估·标注诚实 |
| 关键误判代价 | 中 | 「手绘」定性若被采纳，修复会偏向「引导禁手绘」——治不了转录抽稀（模型本来就没画，是在抄） |

### zcode《根因复核与修复计划书》

| 维度 | 评 | 依据 |
|---|---|---|
| 证据质量 | **最高** | 交叉验证方法论（对 claude 报告逐条确认/补充/差异三栏）＋三条新证据**全部实测成立**（消费面不对称 / manifest file 断裂 / 顶点量化精确）；纠错 claude 的 MANIFEST 误报正确 |
| 根因定性 | **Bug1 准且最深 / Bug2 错** | 「两个 bug 同源：193 没进消费面」的统一判断被实测支持（193 resolve FAIL→模型只能见 130）；但「LLM 自画」定性同被证伪——**做了顶点量化却没做坐标比对**，量化数字反而强化了错误结论 |
| 修复计划可行性 | **较高** | P1-4（manifest file 修复→193 进消费面）是**三份计划中唯一覆盖该断点的**·本案关键前置；P1-1 select_boundary_subset 同 claude（场景错配·作能力补齐可以）；P2-3（layer_output 简化与精确出图解耦）基于其错误的简化条件认知·前提不成立 |
| 关键误判代价 | 低-中 | 修复面覆盖最全（file 修复在），定性错误主要影响「手绘 vs 转录」的叙事而非修复主干 |

### Kimi《新Harness出图双bug根因分析与修复计划书》

| 维度 | 评 | 依据 |
|---|---|---|
| 证据质量 | **高** | 坐标级比对（**全场唯一一锤定音的验证**·方法论可沉淀）；排除法推理（先排除服务端简化器：7 社区 <200KB 触发线·spec 仅 5.4KB）；「文档漂移」发现独有 |
| 根因定性 | **最高** | Bug2「大宗几何经 LLM 上下文转录的结构性缺陷」被 100% 命中验证；Bug1「单一口径真相多载体分裂」（RAG 源/索引/manifest 叙事/点层注册四载体）抽象最系统·且「两个方面」来源定位最准（manifest label·索引 0 命中实测复证） |
| 修复计划可行性 | **高（一处硬漏）** | R1-1（layer_output 服务端落盘+dataset_id·几何不过脑子）**直击本案实际链路**·已被用户拍板并入治本批；R1-3 契约改写（内联档收窄）是配套关键；R2-5 口径四载体同步清单治本防回潮；接口变更注记/门禁同步体现工程纪律。**硬漏：R2-3「默认边界=193 层」依赖的前置（193 resolve FAIL·file 路径断裂）在其计划中无对应修复项**——且其对 193 层「已注册可用」的断言本身错误。落地顺序若不先补 zcode 的 P1-4，拍板项会指向一个调不出来的层 |
| 关键误判代价 | 中 | 该误判直接影响已拍板项 R2-3 的可执行性——**需主手在派发批中显式补位**（见第三节建议 1） |

### 修复计划横向比较（对本案两条链路的覆盖度）

| 链路 | claude | zcode | Kimi |
|---|---|---|---|
| Bug1·索引重建 | ✓ P1-1 | ✓ P1-1 | ✓ R2-1（拍板） |
| Bug1·口径叙事清洗 | 部分（P1-2 落 docs） | ✓ P1-2（注册表标注） | ✓ R2-2/R2-4（拍板·含 label 清洗） |
| Bug1·193 进消费面（file 修复） | ✗（误判为常量问题） | **✓ P1-4（唯一覆盖）** | **✗（遗漏·且误判可用）** |
| Bug2·几何不过脑子（本案主链路） | ✗（引导面·间接） | ✗（新工具·场景错配） | **✓ R1-1（直接·已拍板）** |
| Bug2·按名子集能力（新场景） | ✓ P1-3 | ✓ P1-1 | ✗ |
| Bug2·内联质量守卫 | ✓ P2-1 | ✓ P2-1 | ✓ R1-2（软校验·更克制） |

**结论**：单份最优 = Kimi（主链路直击·定性唯一正确）；完整修复集 = **Kimi 主干 + zcode P1-4 前置 + 三家共有的守卫项**。claude/zcode 的 select_boundary_subset 降 P2（能力补齐·非本案链路）。

## 三、评价②：三组分析能力

**claude——取证广度第一，定性跳步**。证据采集的习惯极好（产物/时间戳/实测三件套），且有「前提修正」的自纠机制（用户质疑后实测推翻自己预设）——这在三份中是唯一显式记录自我纠错的。但两处翻车模式相同：**从强证据直接跳到定性，漏做最后一步排除验证**——① 18 顶点/面 → 直接「手绘」（没想到做坐标比对验证「顶点是否为源子集」）；② manifest.json 不在旧路径 → 直接「常量未同步·迁移中断」（没实测常量当前值·也没感知 Codex 并行迁移已完成）。能力画像：**开工快、覆盖广、适合首轮侦察**；定性结论需第二双眼睛。

**zcode——复核方法论标杆，独立定性欠一档**。「确认/补充/差异」三栏交叉验证是 CB 协作的教科书执行（对 claude 报告 6 确认 3 补充 0 误纠——纠错全部成立）；resolve_boundary 消费面实测是全场最有洞察力的单点证据（直接解释「为什么模型说 130」）。但两处错误同样有共同模式：**引用二手信息未回源**——① layer_output 简化条件引 docstring 未读实现（讽刺的是它据此「纠正」了 claude 恰好正确的说法）；② Bug2 定性沿用 claude 的「手绘」框架只做量化强化、没做独立验证。能力画像：**验证/复核角色最佳实践·交叉审读器**；独立扛首轮根因分析时证据回源纪律需加强。

**Kimi——推理链最深，断言纪律欠一处**。三份中唯一执行「假设排除 → 坐标级实证 → 定位环节」完整推理链的：先算 7 社区序列化体积排除服务端简化器，再做顶点子集比对排除幻觉，最后定位「模型上下文转录」——这是三份中**唯一靠推理设计出「能证伪自己」的实验**的（另两家从未想过「手绘」可以被验证/证伪）。「四载体分裂」把个案上升为机制、R2-5 把机制转化为防回潮制度，抽象与工程化能力均最强；拍板记录/接口变更注记/门禁同步说明治理流程成熟。短板恰好是 zcode 的长板：**「193 已注册可用」是未实测的断言**（只查了登记没查 resolve）——若沿 zcode 的实测习惯走一步就能避免。能力画像：**根因推理与方案设计主力**；关键事实断言需复核者兜底。

**组合评价**：本次三报告 + 本评审恰好构成四层验证网络（claude 首轮取证 → zcode 交叉复核 → Kimi 定性深化 → Qoder 矛盾定谳），每层都纠正了上一层的至少一处错误——**这个模式本身就是 PT-CB15 的最佳实践沉淀**，建议写入 CB 方法论：根因类任务「取证、复核、定性、定谳」四角色分离，且定谳人必须对三份报告的矛盾点逐一实测。

## 四、最终建议（交主手收敛）

以 Kimi v1.1（用户已拍板四项）为派发主干，按以下顺序补位：

1. **【必补前置·最高优先】manifest file 路径修复**（采 zcode P1-4）：`base_community_area` file 字段改指 `DATA/AUTHORITY/boundaries_社区村_193.geojson`（08-25 11:43 用户上传·实测在位）·subj_12345 点层同步改指迁移后路径——**此项不修，拍板项 R2-3「默认边界=193」不可执行**（契约会指向 resolve 失败的层·引入新 bug）。
2. **R1-1 落地时同步 R1-3 契约改写**（Kimi 计划内）：layer_output 返回结构变更（geojson→dataset_id）必须与 render-contract §三内联档收窄同步，否则旧契约文本仍在教模型走内联转录。
3. **select_boundary_subset 降 P2**（claude P1-3 / zcode P1-1）：按名子集是真实缺口但非本案链路（本案是 TOP7 聚合），不占治本批带宽。
4. **口径注册表 130/193/174 三口径标注**（zcode P1-2 + Kimi R2-3 合并）：193=现行权威（含村·SQMC）·130=历史调研范围·174=去村统计口径·「两方面合计」叙事退役标注。
5. **验收统一口径**（合并三家）：同题重问 → ①口径说明=193 社区·无「两个方面」；②图层 7 要素·**顶点数与源一致**（万达=66·朝阳路=369·可用 `_tmp/verify_three_reports.py` 复验思路）；③spec 几何来源=dataset_id 引用（非内联）；④RAG 索引 mtime ≥ 08-25。

> 评审声明：本评审全部定谳基于本人当日实测（坐标比对 80/80·resolve 三层实测·源码行号·mtime·索引全文检索），验证脚本 `_tmp/verify_three_reports.py` 留存可复跑。对三份报告的评价基于证据不基于身份。
