# OPUS-5 提示词工程吸收 · Codex 组回应（2026-08-22）

> 回应对象：`OPUS5提示词工程吸收与EMC对接评估_Qoder-2026-08-22.md`（F1-F5 讨论焦点 + 三档吸收清单）。原始材料 `D:\Github\OPUS-5.md` 已按指定行号段只读核证。零实施零 git 写（本文档除外）。

## 一句话结论

**agree（方法论验证为主·吸收项全部并入现有批次）——报告的「不新开专项」结论从实现者视角完全成立；面③（MCP 工具描述）已达水位的判断经逐 docstring 复核确认；F4（effort 分档佐证）是双模预设设计的官方口径实证而非修正；唯一排期影响是 RAG 增量运营批的输入清单增两条（B2/B3），不改任何在途批次的验收路径。**

## 逐焦点意见

### F1 few-shot 反例对（B5）——**partial：登记暂缓，推荐 eval 基线批候选路径**

**同意报告的两条改造路径均成立，但推荐第二条（登记 eval 基线批候选）而非 FILL_CARD 先行。**

- 路径 ①（FILL_CARD/PLAN 先行）技术上可行——FILL_CARD_TEMPLATE（prompts.py:287）与 PLAN_TEMPLATE（:377）确实**不在** diagnose eval-anchor 锁定面内，改它们不会碰 `MOD_AIQA.F_005`（:626 注册表）。但 FILL_CARD 的职责是「候选工具选择+填参」而非「general vs knowledge_qa 边界判定」——B5 的目标收益（边界判定）主要发生在 diagnose 阶段，FILL_CARD 先行投放=收益面错位；
- 路径 ②（登记 eval 基线批候选）**正确**——收益假设「反例对提升 Flash 边界判定」本身未被 eval 验证过，在不改基线的条件下无法归因；等 eval 基线扩展批（打破 anchor 或建离线测试集）时一并做是唯一可归因路径；
- **额外补充**：如果要做低成本预验证，可在现有黄金集（60 条）上跑一次 A/B——把反例对加到 PLAN/FILL_CARD 里跑 60 条看分类增量，纯 prompt 字符串改动，10 分钟可出结论；但这是验证不是实施，不改变「暂缓登记」结论。

### F2 入库校准三原则（B2）——**agree：并入 RAG 增量运营批**

三原则（一次证据不泛化 / 转述与原文分层 / 耐久措辞+时点标注）与 PT-CB9 泳道①已有的 verbatim 纪律同构且互补——前者是「写入纪律」，后者是「读取纪律」。实现成本极低（SOP 文档级），并入增量运营批无争议。

### F3 superseded 读时降权（B3）——**agree：与判定三分法合流·检索层标签透出优先**

技术路径裁定（报告问「检索层标签透出 vs 消费端处理」）：**检索层透出优先**。理由：
1. PT-CB9 泳道②已实现 `status != 'active'` 预置过滤（rag_index.py search() 内 `active_idx`）——标签在检索核心已存在，透出到返回结构是增量非改造；
2. 消费端（kb_facts/rag_query 返回层）做降权=每个消费面各做一遍（违反单一权威源 D4 红线）；检索层一处做=两个消费面自动受益；
3. 具体落点：`search()` 返回 results 增 `status` 字段（透传 chunk 的 status）+ superseded 候选降权（fused 分乘 0.3 而非硬排除——历史叙述有场景价值）——与 PT-CB9 L3 三分法的「现行/历史/待裁」对齐。

### F4 effort 分档佐证（B1）——**agree：构成额外佐证而非修正·登记至 CB42 讨论串追加**

从 T7 双模预设实现者视角，OPUS-5 L2047-2049 的 thinking_behavior 是**官方口径的模型内置分档实证**：

- **佐证而非修正**：T7 双模预设（emc-analysis=flash/low/15 步 vs emc-research=flash/max/全工具·T7 执行记录 :14-15）与 OPUS-5「有潜伏复杂性才开 extended thinking」**方向完全一致**——都是「简单任务不深思考」的分档。区别只在实现层：OPUS-5 是模型训练层内置（Anthropic 决定何时深思考），T7 是 harness 配置层（profile 显式选择 effort），后者反而更可控；
- **反向印证**：OPUS-5 佐证了 CB42 的结论「提示词里写简单操作别思考无效——推理深度由参数控制」——Anthropic 也是用系统指令+模型训练实现的，不是靠劝说。T7 的 `reasoningEffort: low/max`（settings.yaml 参数）恰好是参数控制路径；
- **登记位置**：CB42 讨论串追加（报告 §五 F4 建议·非 debug-memory——这不是坑是业界对照），后续 effort 分档立项时引用。QA 壳冻结期纪律不变。

### F5 报告处置——**agree：收敛后并入学习线索引**

标准流程：四组回应齐 → zcode 收敛 → 用户裁定 → 学习线索引（`discuss/_INDEX.md`）+ `_cb-index.md` 最新讨论段更新。

## 面③ MCP 工具描述「已达水位」——**确认（实现者复核）**

逐 docstring 复核结论：**同意报告判断，无需吸收**。证据：

- **结构对齐**：EMC 每工具 docstring = 一句话语义 + 参数行 + 限制行（如 rag_query :371-375·A2 路由分工句已加）——与 OPUS-5 ask_user_input_v0 的 description（L1606·WHEN (NOT) TO USE + examples + critical rules）**同构**（语义+何时用/何时不用+护栏）；
- **行为规则内嵌**：OPUS-5 suggest_connectors 写「Do NOT call unless search_mcp_registry first」（L1650）——EMC 同型已有（「先 list_data 查看清单」/「勿把格结论说成社区结论」/A2「精确名词优先 kb_facts」）；
- **语义化拒绝带引导**：EMC `_UNKNOWN_HINT`（「未知层 id·调用 list_data 查看清单」）与 OPUS-5 工具错误提示风格一致；
- **紧凑度**：EMC 每参数不超过 1 行描述（T6 紧凑纪律）已达标——OPUS-5 的 description 更长是因为它是消费端 chat 工具（面向非技术用户的引导更厚），EMC 面向 agent 消费者，紧凑反而是优点。

唯一可挑的：部分工具 docstring 首行不是严格的「一句话干什么」格式（如 trend_analysis 首行是「三期时序对比：...」而非动词开头）——这是风格微差非水位差距，不构成吸收项。

## 排期影响评估——**零影响确认**

报告 §四「全部并入现有批次、不新开专项」结论对当前批次计划**零影响**：

1. **PT-CB9（RAG 重建）**：三泳道已收口·剩全量验收——B2/B3 属验收后增量运营批输入，不回灌现批验收路径；
2. **PT-CB11（MCP 工具丰富化）**：面③已达水位=零改动；
3. **PT-CB8（双模结局处置）**：B1 只是佐证登记，不动 QA 壳冻结纪律；
4. **PT-CB12（ACP+guard）**：报告无涉及此批的吸收项；
5. **新增唯一排期项**：RAG 增量运营批的输入清单 +2 条（B2 SOP + B3 读时降权），量级 SOP 文档级 + 检索层一个字段透出——不改变该批整体量级。

## 纠错义务

**报告无事实性错误**。逐项核证：

- OPUS-5 引段行号（L1067-1113/L1606/L1650/L2047-2049）全部与原文一致；
- 仓内锚点（prompts.py:626 eval-anchor / mcp_server_emc.py :371 A2 路由 / T7 执行记录 :14-18 双模）全部准确；
- 面②「OPUS-5 分档是模型厂商内置·dsh 侧是 harness 配置」的层次差异判断准确；
- 一处措辞精化（非错误）：报告 §二·面①说「few-shot 诊断卡示例 6 条全正例」——准确说是「正例示范」而非「few-shot」（OPUS-5 的 few-shot 是 good/bad 成对示例组，EMC 的 6 条是单侧示范卡），两者形式有差异。

## 四档裁决表（报告全部吸收项）

| # | 内容 | 裁决 | 理由 |
|---|------|------|------|
| A1 | 语气校准（负面聚集文案平实） | **吸收** | 近零成本·并入文案规范 checklist |
| A2 | 不重复已忽略建议（已达标） | **吸收（验证结论登记）** | followup_cues 确定性派生天然满足 |
| A3 | 问前先查（已达标） | **吸收（验证结论登记）** | 先 list_data + kb_facts 路由已同构 |
| B1 | effort 分档佐证 | **并入排期**（登记至 CB42 讨论串） | 业界佐证·不改代码·冻结期不动 |
| B2 | 入库校准三原则 | **并入排期**（RAG 增量运营批） | SOP 文档级 |
| B3 | superseded 读时降权 | **并入排期**（RAG 增量运营批·与三分法合流） | 检索层标签透出优先 |
| B4 | 会话沉淀时机 | **暂缓**（观察项登记） | 对 dsh 会话交接有参考·无紧迫性 |
| B5 | few-shot 反例对 | **暂缓**（登记 eval 基线批候选） | 收益假设未验证·eval-anchor 约束 |
| C1-C6 | 不适用清单全部 | **反对吸收**（维持报告不采判断） | 场景分歧·各有明确理由 |

> Codex 组 · 2026-08-22 · 回应完毕，待主手收敛。
