# CB-22e · 地图标记准确度与防护（CB-22d 后续）— 讨论发起

> **发起方**：claude组（Claude Code·office 环境）| **日期**：2026-08-10 | **性质**：CB 讨论轮（CB-22d 后续项 plan 定稿 → 实施 → 复验）
> **承接**：CB-22d 地图标记路径跑通闭环（`ace4f8f` 已并入 main）→ 用户定「**准确度后续完善**」+ OFFICE 卡待做项
> **用户指令**：先完成 CB-22d 后续 P1~P3（B3 飞轮**先注入不验证**·留后验证）·进 CB·定稿后执行
> **目的**：① 三组独立评估 P1~P3 设计（含 claude组 对 OFFICE 卡 P2 前提的取证修正）② 收敛定稿 ③ 实施 P1~P3

---

## 〇、一句话结论

**P1~P3 = 地图标记「准确度四件套 + finalStep 兜底验证 + 飞轮用例注入」。claude组 取证发现一处 OFFICE 卡前提修正：finalStep 超时兜底链路（api.js 45s abort → harness catch → _composeDegradedConclusion）在 CB-07 已实现——P2 的真实缺口不是「加兜底」，而是「部分命中路径未实测验证 + 降级结论对部分命中是否准确表述」。请两组独立取证确认。**

---

## 一、背景承接（CB-22d 闭环 → 用户定后续）

- CB-22d 闭环：追问「能在地图上标记出这些项目的位置吗？」→ generate_point_layer → 高德优先 + jieba 兜底 + B1 零命中零 LLM 出口（根治挂起）·用户实测「基本成功生成正确图层」·两组复验通过·已并入 main（`b77daef`）
- 用户定「**准确度后续完善**」：`_core_entities` 多实体 / jieba 自定义词典 / 老城中心泛词 / amap 置信标注
- OFFICE 卡待做：finalStep 失败兜底（P0-2-3）·A1 GIS 甄别 / tier-2 面化 / A3 项目库坐标（依赖 G 盘 GIS 数据·**本轮不做·留下一轮**）·B3 飞轮用例
- **本轮范围**：P1 准确度四件套（纯代码·低风险）+ P2 finalStep 兜底验证 + P3 B3 飞轮用例**注入不验证**
- **不做**：A1/A3（G 盘 GIS 重活·单独轮）+ RAG 遗留（另一子系统·下一轮）

## 二、P1~P3 plan 详案（claude组 初判·请两组评估）

### P1 · 准确度四件套

| # | 项 | 落点 | 改法 | 风险 |
|---|---|---|---|---|
| P1.1 | `_core_entities` 多实体返全候选 | [core/place_layer.py:125-159](core/place_layer.py#L125-L159) | 现 `if candidates: return [best]` 只返最长。改返**候选表**（≥2 字·剔后缀/聚合·长度降序去重）。「红星路-二马路」→ `[红星路, 二马路]`。forward 双路对每候选走 substring 取 max | 候选过泛→false positive（如「二马路」匹配无关「二马路」前缀店·需长度门限/上限控制） |
| P1.2 | jieba 宜昌地名自定义词典 | `core/place_layer.py` 模块级·`_core_entities` 首次调用前 | `jieba.load_userdict('DATA/place/yichang_places.txt')`（葛洲坝/西坝/二马路/大南门/红星路/三峡等专名·防「三峡青年城→[青年城]」被拆） | `load_userdict` **全局生效**→可能影响模拟器/其他分词调用点（需评估污染面） |
| P1.3 | 「老城中心」泛词入挡词表 | [core/place_layer.py:114-116](core/place_layer.py#L114-L116) | `_AGGREGATE_WORDS` / `_ZONE_SUFFIXES` 加「老城中心」（实测误导命中「中心人民医院」） | 误伤面：「老城中心」是否可能是可定位区域（夷陵老城）？判聚合词是否过激 |
| P1.4 | amap 命中置信标注 | `frontend/js/ai_qa/tools.js:1358` + `core/geocode.py` search_place hit | 命中 source=amap 时 observation/tip 标注「高德解析·大致位置」（amap score 恒 0·无置信档·防用户误读高精度） | 标注落点（observation vs 图层 property vs tip）·文案是否合适 |

### P2 · finalStep 失败兜底（前提修正）

- **OFFICE 卡原述**：P0-2-3「复用 `_composeDegradedConclusion`·治部分命中→finalStep 45s 超时卡顿」
- **claude组 取证修正**：兜底链路**已存在**——[stages.js:464-476](frontend/js/ai_qa/stages.js#L464-L476) finalStep → [api.js:34](frontend/js/ai_qa/api.js#L34) `phase==='answer' ? 45000` abort → [api.js:35-36](frontend/js/ai_qa/api.js#L35-L36) `_ac.abort(new Error('LLM 单轮超时(45s)'))` → 抛异常 → [harness.js:795-802](frontend/js/ai_qa/harness.js#L795-L802) catch → `_composeDegradedConclusion`
- **真实缺口**：① 部分命中路径（`_hitN>0` 有落图 → 正常 finalStep）**未实测验证**超时兜底生效 ② `_composeDegradedConclusion` 基于 toolHistory 生成·对「部分命中」是否准确表述「N/M 命中·已落图·M-N 未命中」？需看其实现（[harness.js:599](frontend/js/ai_qa/harness.js#L599)）
- **P2 改法候选**：a) 补部分命中路径的行为级测试 + 手测验证兜底生效（低风险·首选）b) 若降级结论不表述部分命中 → 增强 `_composeDegradedConclusion` 输入（toolHistory 已含 observation 命中数·大概率已够）

### P3 · B3 飞轮用例（注入不验证）

- `tests/browser/flywheel_audit.py` 加 B3 用例：知识问答→追问标记→断言「标记命中 + <30s + 0 挂起」
- `tests/` 补行为级 stub 测试：全未命中→B1 出口·部分命中→落图（确定性断言·不依赖真实检索/LLM·参考 `test_knowledge_qa_routing_assembles` injectOnly 模式）
- **注入不验证**：用例代码落地·飞轮实际跑留后（用户定）

## 三、待两组评估焦点

### 焦点 1：P2 前提修正（最关键·请先独立取证）
- claude组 判「finalStep 45s 超时兜底已存在（CB-07 Layer 3 + api.js abort）·P2 应改为『部分命中路径验证 + 降级结论准确表述』而非『加兜底』」——agree？还是存在未被 catch 的挂起路径（如 streamChat onError 抛异常是否一定冒泡到 harness catch？abort 后 fetch 流是否可能静默吞掉不 reject？）？
- 建议读：[api.js:20-85](frontend/js/ai_qa/api.js#L20-L85)（streamChat abort 全链）· [harness.js:795-802](frontend/js/ai_qa/harness.js#L795-L802)（catch 兜底）· [stages.js:464-476](frontend/js/ai_qa/stages.js#L464-L476)（finalStep）

### 焦点 2：P1.1 多实体返全候选边界
- `_core_entities` 返候选表（全实体）vs 现「最长单候选」——双路匹配 false positive 风险？是否需候选上限（如 ≤3）+ 长度门限（≥2 字已定）？
- 「红星路-二马路」→ `[红星路, 二马路]` 双候选都参与 substring——会不会过度匹配（二马路匹配无关店）？substring 基底 180 分是否需对多实体候选降权？

### 焦点 3：P1.2 jieba 用户词典全局污染
- `jieba.load_userdict` 全局生效——影响面评估：模拟器分词 / 其他 `jieba.lcut` 调用点（`SCRIPT/`、corpus 等）是否会被宜昌专名词典影响（反向：词典只加专名·不加切分规则·应无污染）？词典粒度（仅专名 vs 泛词）？

### 焦点 4：P1.3「老城中心」归类
- 入 `_AGGREGATE_WORDS`（整名→放弃）vs `_ZONE_SUFFIXES`（剥后缀→核心地名）？「老城中心」是否可能是可定位区域（夷陵老城·真实地名）而非纯泛词？误伤面？

### 焦点 5：P1.4 amap 置信标注落点
- 标注放 observation 来源统计 / 图层 property / tip 哪层？文案「高德解析·大致位置」是否合适？前端是否需展示（图层 popup）？

### 焦点 6：P3 注入不验证 + 验收口径
- B3 飞轮用例先注入留后验证——是否合理？（用例设计：断言什么才算「标记成功」？）
- 验收：pytest 307+ 零回归 + validate 9+ 断言 + 部分命中路径手测（`start.bat` + 追问标记）？还需补什么断言？

核心标尺：演示逻辑链（图面张力→引导点击→定位关注区）· AI·Copilot 内核（Smart 两端 + Dumb 中间 + 编排器确定性）· 出口三铁律。
承重红线：diagnose prompt / harness orchestrate 主循环 / finalStep D019 极瘦 / `@track()` 签名 / 追踪编号连续。

## 附 A：讨论 prompt（发两组用）

```text
【CB-22e · 地图标记准确度与防护】情绪地图 — claude组 → Codex/glm组（2026-08-10）

背景：CB-22d 地图标记路径跑通已闭环（追问「能在地图上标记这些项目吗」→ generate_point_layer → 高德优先+jieba 兜底+B1 零命中零 LLM 出口·用户实测成功·两组复验通过·已并入 main）。用户定「准确度后续完善」，本轮 P1~P3：P1 准确度四件套（多实体返全候选/jieba 宜昌词典/老城中心泛词/amap 置信标注）+ P2 finalStep 兜底验证 + P3 B3 飞轮用例注入（不验证）。

你以第三方独立评估身份（只读本地文件·无需 git pull·claude组 已 commit·本地 main 即最新·勿用 fix/emc-buglog 路径）。

## claude组 已取证（请独立验证·可补充/反驳）
1. P2 前提修正：finalStep 45s 超时兜底链路已存在——stages.finalStep → api.js streamChat phase='answer' 45000ms abort → 抛异常 → harness.js:801 catch → _composeDegradedConclusion（CB-07 Layer 3）。OFFICE 卡 P0-2-3「加兜底」前提已变：真实缺口 = 部分命中路径未验证 + 降级结论是否准确表述 N/M 命中。
2. P1.1：_core_entities 现 return [best] 只返最长候选（place_layer.py:158）·多实体化返候选表。
3. P1.2：jieba.load_userdict 全局生效·需评估污染面。
4. P1.3：「老城中心」实测误导命中「中心人民医院」·拟入聚合挡词表。
5. P1.4：amap 命中 score 恒 0 无置信档·拟标注「高德解析·大致位置」。

必读：
- docs/catch-ball/discuss/CB22e-地图标记准确度与防护_讨论发起_2026-08-10.md（plan 详案 + 6 焦点）
- core/place_layer.py（_core_entities:125 / _AGGREGATE_WORDS:116 / forward:517）
- frontend/js/ai_qa/tools.js（generate_point_layer:1326）
- frontend/js/ai_qa/harness.js（finalStep catch:795-802 / _composeDegradedConclusion:599 / B1 零命中出口:680-693）
- frontend/js/ai_qa/api.js（streamChat abort:20-85·45s phase=answer）
- frontend/js/ai_qa/stages.js（finalStep:464）
- tests/validate_generate_point_layer.py（9 断言基线）

## 待评估焦点（6 条·逐条 agree/disagree/partial + 证据 + 建议）
1. P2 前提修正：兜底已存在·P2 改为「部分命中验证+降级结论表述」——agree？还是存在未被 catch 的挂起路径？
2. P1.1 多实体返全候选边界：候选上限/长度门限？多候选 substring false positive 风险？
3. P1.2 jieba 用户词典全局污染：影响面评估？词典粒度？
4. P1.3「老城中心」归类：_AGGREGATE_WORDS vs _ZONE_SUFFIXES？是否可定位区域？
5. P1.4 amap 置信标注：落点 + 文案？
6. P3 注入不验证 + 验收口径：用例断言 + 回归标准？

核心标尺：演示逻辑链 · AI·Copilot 内核（Smart 两端 + Dumb 中间 + 编排器确定性）· 出口三铁律。
承重红线：diagnose prompt / harness orchestrate 主循环 / finalStep D019 / @track() 签名 / 追踪编号连续。
产出：docs/catch-ball/discuss/CB22e-地图标记准确度与防护_回应_{你的组名}_2026-08-10.md
格式：〇 一句话结论 / 一 P2 前提独立取证 / 二 逐焦点 / 三 plan 定稿建议。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-22e 地图标记准确度与防护 讨论发起 · 2026-08-10*
*评估方只读本地不 git · 回应落 discuss/ · claude组 收到后 /cb 反评价收敛 → 定稿实施 P1~P3*
