# CB-39 · 双线实施计划 · 反评价（zcode组）

> 评估方：zcode组（ZCode + GLM 5.3）· 2026-08-16。对象：`discuss/CB39-实施计划_Codex-2026-08-16.md`（v3 双线·24 批次）。
> 输入核对：CB-38 收敛定稿（数据线）+ EMC 审计 claude 反评价（工程线 D1-D9）均已通读。
> 方法：只读本地实测复核——计划附A/附C 全部锚点 + 15 项关键断言独立验证（非转述采信）。零实施、零 git 写。

---

## 〇 一句话结论

**计划 v3 整体成立、可开工**——双线结构与三交叉点时序正确、批次依赖链自洽、锚点实测经 zcode 独立复核 **15+ 项断言零误差**（含 F_017 编号实况、同名三对行数 676/690·194/138·7/7、enforceMutualExclusion 死代码、4 未注册 ID、KNOWLEDGE 密钥条款确未落、SCRAPER/experimental 确不存在、_tmp 9 件清单、B013 确无条目、esri-imagery commit 实存）；但存在 **2 处 P0 级修正**（C3/C4 source 规则互斥——两批规则现状会互相拒收、P1-3 漏列前端 api.js 适配）与若干 P1-P3 澄清项，建议修正后进入实施，不阻塞整体框架。

---

## 一 总评

这是 CB 历史上工程密度最高的一份实施计划，三个突出优点：

1. **锚点实测文化**：附A/附C 的断言不是文档推断而是代码级实测（行号+字节数+行数），zcode 逐项独立复核**全部吻合**——包括容易出错的细节（如「B013 未建」「F 最大号 19」——F_017 实际注册于 `api/aiqa_routes.py:23` 而非 router.py，计划写「实测 ai_qa/router.py」定位文件略偏但编号结论正确；「_tmp 9 件」清单与我 CB-38 评估时的 9 件完全对应·其中 scan_community_names 已清、b012 新增，计划是最新的）。
2. **交叉点时序设计正确**：E16=A2 同批（数据池归一与迁移同批完成，PROCESSED_DIR 收敛不二次动）、R1=C5 后（沉淀分流完再归档残骸）、E6 先行+R1 迁移——三处都是「防二次迁移/防漏项」的正确次序，与 claude组 D2/D3 裁定逐条对齐无漂移。
3. **红线内化**：manifest 授权 gate（仅剩 E15 一处用户 gate·D3 冻结）、蒸馏源纪律、git 不捞回、追踪取号 F_020 起全仓复核——承重红线全部进入批次条款而非停留在原则层。

zcode 的保留意见集中在**批间规则一致性**（C3/C4 互斥、C1 映射覆盖面）与**改动面完整性**（P1-3 前端适配、B1 usage 白名单消费点）——都是实施前一行字到一天量级的修正，不动框架。

---

## 二 逐项表态（按任务书 5 点）

### 1. 双线结构与交叉点 —— **agree**（附一条排期意见）

- **三交叉点时序**：agree。二次迁移与漏项均防住——E16 并入 A2 使「performance 演示池语义恢复 + 真实数据迁出 + 引用同步」原子完成；R1 后置使沉淀路径（presets/RAG/注册表）先定型再归档；E6 先行清理与 R1 迁移分层正确。
- **两线并行互踩分析**（zcode 独立核）：文件集几乎不相交——数据线动 DATA/ + SCRIPT 路径引用 + rag_index + outlet_kb + tests/validate_rag_material；工程线动 serve/pytest.ini/emotion_analysis/state/stages/api routes + 前端 api.js。唯一交叠面 = tests/：P0-2 通电 pytest.ini 在首两日、C5 新测试在其后，新测试写入即被收集，顺序天然安全。**不踩**。
- **一条排期意见**（详见第 5 点）：P1-5（3.14 切换）会变动 pytest 基线数，若「插空」插在数据线中间批次，前后批次的「320+3 基线门」语义断裂——建议位置固定到数据线全部完成后（R1 前）。

### 2. 数据线八点

| 点 | 表态 | 理由与补充 |
|---|---|---|
| 批次依赖链 | **agree** | B2←A2（对账对象就位）、C←B3（复核门前置）、P1-2←P0-2（守卫即闸门）依赖方向全部正确；D3 冻结后「performance 只剩 sim」里程碑保留的表述成立（sim 禁入治理的独立价值，不依赖时间轴开工）。 |
| 决1（checkup_12345_2024 → 12345主观/） | **agree** | 三条理由成立，特别是「govern_12345_points.py 的 OUT 本就指向该目录·数据血缘同处」——我复核 `govern_12345_points.py:17` OUT 确实是 `DATA/analysis/12345主观`，血缘同处是实锤。 |
| 决2（2 xlsx 族内归位） | **agree** | 避混合目录、族内聚防回潮，理由充分；A3 已列两处脚本路径同步代价，对等。 |
| 决3（_总账 → DATA/analysis/_总账.md） | **agree（理由表述小修）** | 落位正确。但「下划线与 RAG `_load_notes` 跳过规则同防」表述不准：`_load_notes` 只扫 `docs/urban-renewal-plan/`，DATA/analysis 的 md **根本不在扫描范围**（这正是 C2 需要拷贝汇总 md 进 00-宜昌专项 的原因）——真正的防=加载器路径隔离，下划线只是第二道冗余。不影响结论，建议措辞修正防后人误解「DATA 下 md 会被 RAG 扫到」。 |
| A2 迁移与引用同步 | **agree** | 三处引用实测全部核实（geo_registry.py:43、dual_track_density.py:78、govern_12345_points.py:13 均指 performance/checkup_12345_2024.csv）；同名三对「暂留待 B2 裁决」比抢跑迁移正确（三对全 DIFF 实测吻合）；57,265 行数验收锚点明确。 |
| B1 usage 标注 | **partial** | `usage=input/analysis_output` 的铁律7 机械化方向 agree。**两个补充**：① **消费点未列明**——「EMC 空间操作输入白名单只认 usage=input」落在哪个文件（前端 tools.js deriveAvailable / 后端 geo_routes 过滤）须写进批次条款，否则字段标了没人消费=装饰；② **既有用例回归验收缺失**——18村面/合并范围/聚合范围174 标 analysis_output 后，若现有 EMC 用例（zonal/buffer 以这些层为输入）行为变化，B1 验收应加「既有 EMC 用例不因白名单回归」一条（计划验收只有 schema 校验+前端冒烟）。 |
| B3 口径注册表+复核门 | **agree** | 种子四项（page2 §3.3 / 双口径 test case / 社区四口径 / 作废数字清单）与 CB-38 定稿逐条对齐；check_caliber 取号 F_020 正确（我核实 F_017 已在 aiqa_routes.py:23 注册、F_019 为现最大号）；「三证据冲突挂起待用户裁决」的实事求是条款好。 |
| C 批 RAG 落地 | **partial（含 1 处 P0 修正）** | domain 三域封顶/白名单/指纹/黄金问题骨架全部 agree（E11/E12/E13/E14/E15 全入批次）。三个问题：① **C3/C4 source 规则互斥（P0）**——C3 说图层卡片 fact「source 指向 presets manifest 与 _总账.md」，但 C4 白名单只允许 `DATA/analysis` + `docs/urban-renewal-plan` + `ai_qa/outlet_kb` 三前缀——`DATA/boundaries/presets/manifest.json` **不在白名单内会被拒收**。须澄清：图层卡 fact 的 `source` 字段=fact 所在的 `ai_qa/outlet_kb/*.py#id`，manifest/总账以内容性指针字段（如 `layer_ref`）引用而非占用 `source`——否则 C4 上线第一批就拒收 C3 全部产物。② **C1 映射规则覆盖不全**——规则只覆盖 00-宜昌专项(00-0*/03-0*)/3prime/04-案例资料/_笔记，顶层 `01-政策文件/02-法律法规/03-指标体系-标准-指南-方法/05-杭州专题/06-新闻报道/07-研究综述/08-本地已有文件` 七个目录全部落「未分类清单」人工裁决——214 note 中这七个目录占比可能过半，人工裁决量级须在批次内显式预估（或先补目录级粗规则：02/03 顶层→base、05/07→方法论域语义待定）；**note 无「无域」选项的设计缺口**——「方法论走 concept 不设域」只对新素材成立，存量方法论 note 的 type 固定为 note，domain 三枚举强制选一会让方法论 note 在域过滤时不可见——建议允许 note `domain=''`（无域·仅全域检索可见）或全标 base 并在文档声明语义。③ **C2 双形态的维护窗口**——R1 前 DATA/analysis 原件与 00-宜昌专项 拷贝件并存，窗口内口径更新须改两处；窗口短可接受，但建议 C2 批次注一句「拷贝后 00-宜昌专项 版本为权威·analysis 原件只读」防误改错侧。 |
| 风险回滚 | **agree** | 十行风险表覆盖了主要路径（geo_registry 向后兼容/迁移保历史/索引漂移/对账挂起/key 断流/3.14 不兼容），回滚全部单批 revert 粒度——与「每批一 commit 先验后推」自洽。 |

### 3. 工程线专项

- **P0-1 诚实度验收口径（可机器化）——agree 并补一条**：「导出层级标签=真实计算层级」可机器化——修法上 `phase=engine.phase if not full_pipeline else 'L4'`（:1129 实测吻合）改为反映实际执行链后，单测可 mock 各阶段跳过条件（key 空/语料缺）断言导出 phase=最深实际执行阶段。**补充**：验收须同时覆盖「key 空显式报错」路径的测试用例（D1 修正后的机制=报错/明示降级而非补字段——claude组 核正已锁定，测试应对齐这个行为语义）。
- **P0-2 存量红处理——agree + 红线具体化**：「修复 or xfail+登记·不许静默豁免」方向正确。红线建议具体化：**xfail 项必须登记进 `tests/buglog/_deferred.md` 同款机制（有主·有期限·月度复核）**——防「红转 xfail 后永久沉睡」（现有 deferred 表已有「3 个月无进展升级」约定，直接复用）；xfail 无登记视为违反「不许静默豁免」。
- **P1-2 四 ID 注册 + gen_agents_table 边界——agree 注册 / partial 自动化**：4 个未注册 ID 实测全部确认（llm.py F_007 用 1 处·D_004 用 2 处、urban_renewal_knowledge F_018、spatial_analysis D_004 于 :1174·注册数 0）。**gen_agents_table.py 的边界要点**：AGENTS.md 模块表的状态列（✅/⬜/🔧/❌ retired）中 retired/🔧 是人工判断，代码只能机械判定「有无 register_track_id」——若生成器全表重写会抹掉人工状态，若 CI 校验「零偏差」则含人工列必红。**建议**：表分「机械区」（模块-文件-埋点状态·生成器管）与「人工区」（图例/规划意图·注释分隔保留），CI 只校验机械区零偏差——这个分区设计须在实施前定，否则 gen_agents_table 第一版就会陷入「要么抹人工信息要么 CI 恒红」的两难。
- **P1-3 Threading + BackgroundTasks 兼容——partial（含 1 处 P0 修正）**：单线程实锤（`frontend/serve.py:450` ReuseTCPServer(TCPServer) 实测吻合）、根 serve.py 双入口实锤（实存）。兼容分析：① ThreadingHTTPServer 改造一行级成立（Python ThreadingHTTPServer 默认 daemon_threads=True·Ctrl+C 行为不坏；反代各连接独立 wfile；SSE 分支并发写安全）；② BackgroundTasks 若 /analyze 为 sync def 则跑 threadpool 不阻塞事件循环 ✓——但 **task_id 轮询需要任务状态存储设计（内存 dict+锁+历史 task 清理防累积）**，计划未提，实施时须补（小设计缺口）；③ **P0 修正：前端适配遗漏**——`frontend/js/api.js:23/:33` 现为 `await fetch(/analyze)` `/governance` 同步等待合同，改 BackgroundTasks+task_id 轮询后**前端调用方必须同步改**（api.js 两处 + 调用方 loading/轮询 UI），计划 P1-3「动文件」只列了后端——**不补这条，实施当天前端分析入口直接断**；④ 后端 uvicorn 子进程管理不受 Threading/BackgroundTasks 影响 ✓。
- **P1-5 环境对齐基线口径——agree（附排期约束）**：「基线数可能变动·变动即记录新基线」可接受——3.14 是 CI 既定（ci.yml:25 实测 `"3.14"`）、本机 3.13 是漂移，对齐正确。两个约束：① **排期位置固定**——P1-5 若插在数据线中间，前后批次基线门语义断裂（见第 5 点）；② 新基线确立时应输出一份**差异说明**（哪些用例因版本变化 passed/skipped 变动），防版本差异掩盖中间批次引入的回归。

### 4. D 线

- **D1 finalStep 白名单——agree 方向 / partial 范围**：「结论引用的图层名/数字必须存在于执行产物结构化清单·违者降级诚实缺口」的确定性铁律方向正确（编排器不信 LLM 自觉）。**范围建议收窄 v1**：数字白名单的最大坑是 LLM 表述变体（千分位/百分比/四舍五入/「约 N 个」）——过紧则大量误杀合法表述、降级文案泛滥反而毁演示可用性。**v1 只做两层**：① 图层名白名单（精确字符串·无变体问题）；② observation 行格式校验（复用 CB-22e `_composeDegradedConclusion` 的 `/命中\s*\d+\/\d+/` 先例·「已生成/产出 N」声明须与 observation 计数一致）。**数字白名单作 v2 渐进**（先收集 finalStep 实际数字表述样本再定匹配规则），避免 v1 上来就误杀。诊断 prompt 不动 ✓ 红线已守。
- **D2 B012+B013 同场修——agree**：B012 方向 A（指针 lngLat 实时归属+节流+bbox 预筛）与收敛定稿一致；**B013 先建条目必要**（实测 open 目录仅 B002/B004/B008/B012·B013 零书面——口头债两次入坑）；复现脚本转正验收链 + UI-09/PRED-09 同场顺收（一次浏览器会话收三条）全部 agree。
- **D3 冻结后 A2 里程碑表述——成立（agree）**：「performance 只剩 sim」在 D3 冻结下仍有独立价值（sim 禁入红线的治理自证 + 为专题轮备好物理前提），表述准确不越界——时间轴内容本体确实全部移出（引用清单/manifest v2/离散 UI 均不在本轮），符合用户「暂不动工」指示。

### 5. 排期合理性 —— **agree + 一条约束**

P0-0 当日（用户 key 轮换·KNOWLEDGE 条款我核实**确未落**·是真实剩余项）→ P0-1/P0-2 首两日 → 数据线主体 → R1 收尾，与收敛定稿第五节排期和 D1-D9 裁定优先级全部对齐（B002/B004 P0 前移 ✓）。**一条约束：P1-5（3.14）位置从「插空」改为「数据线完成后·R1 前」固定**——理由见上（基线门语义完整性）。D4 的 GeoQ spike 与白名单核查含 office 现场项（周一），与 E7 换绑同日顺办即可。

---

## 三 批次级风险补充（zcode 新增）

| # | 批次 | 风险 | 建议 |
|---|---|---|---|
| Z1 | C3×C4 | source 规则互斥（详见二.2 C 批①）——C4 上线即拒收 C3 图层卡 | 实施前一行澄清：fact source=所在 py#id·manifest/总账走内容指针字段 |
| Z2 | P1-3 | 前端 api.js:23/:33 同步等待合同·改轮询后无适配即断（详见二.3） | 动文件清单补 api.js + 调用方 UI；task 状态存储（dict+锁+清理）入批次设计 |
| Z3 | C1 | 映射规则仅覆盖 4 个目录·7 个顶层目录全落未分类；note 无「无域」选项 | 补目录级粗规则或预估人工裁决量级；定义 `domain=''` 语义 |
| Z4 | B1 | usage 白名单无消费点列明·既有用例回归缺验收项 | 消费点（tools.js/geo_routes）入条款；验收补既有用例回归 |
| Z5 | P1-5 | 基线数变动若发生在数据线中间批次·门语义断裂 | 位置固定 R1 前；新基线附差异说明 |
| Z6 | P1-2 | gen_agents_table 全表重写抹人工状态 / CI 恒红两难 | 机械区/人工区分区设计先行 |
| Z7 | A4 | 「_retired 9 件」与文字可数清单 8 件差 1（v2/v3 xlsx+预览×2+preview×2+render+png=8）——v1 xlsx 去向未明（若归档 v1 则恰 9） | 清单逐件列名消歧 |
| Z8 | C2 | 双份并存窗口内口径更新改错侧 | 批次注一句「拷贝后 00-宜昌专项 为权威·analysis 原件只读」 |

---

## 四 P0-P3 建议

### P0（实施前必须修正·均为小改）

1. **Z1**：C3/C4 source 规则澄清（一行条款·防两批互斥）。
2. **Z2**：P1-3 动文件清单补前端 api.js 适配 + task 状态存储设计（防实施当天断前端入口）。

### P1（批次条款内落实）

3. **Z3**：C1 映射补全或人工量级预估 + note 无域语义定义。
4. **Z4**：B1 usage 白名单消费点列明 + 既有 EMC 用例回归验收项。
5. **D1 范围**：数字白名单降 v2·v1=图层名白名单+observation 格式校验（防误杀毁演示可用性）。
6. **P0-2 红线具体化**：xfail 必登记 deferred（复用月度复核机制）。

### P2

7. **Z5/Z6**：P1-5 排期固定 R1 前+基线差异说明；gen_agents_table 分区设计。
8. **B2 补充**：占比表 676/690 逐行 diff 时建议同时记录**两版生成时间与生成脚本**（git log + 脚本头），三证据变四证据，裁决表更防答辩。

### P3

9. **Z7**：A4 _retired 清单逐件列名（v1 xlsx 去向一行定）。
10. **决3 措辞**：「与 _load_notes 跳过规则同防」改为「加载器不扫 DATA/analysis·下划线为冗余第二道」。

---

> zcode组 · 2026-08-16 · 本文件为 CB-39 反评价产出，交 claude组 汇总收敛；收敛通过后 Codex 按批次实施。只读本地·未 git 写·零实施。
