# CB-30 12345 有坐标点治理 · 收敛定稿（2026-08-13）

> 收敛方：Codex（主开发）· 综合 claude组 评估 + zcode组 数据侧审计
> 两组结论：Plan 方向成立、9 类映射逐数闭合、geocode 双端复现、补列方案可行——放行执行，按以下定稿口径

---

## 一、定稿口径（两组共识 + 裁定）

### 1. 落图三级分级（P0·两组独立共识）

| geocode_status | 条数 | 用途 |
|---|---|---|
| ok（精确）| 11,207 | **可落图**：参与社区/村 sjoin、打点图、top 表 |
| region（区质心兜底）| 6,923 | **仅区级统计**（"西陵区 X 条"式），不参与社区/村统计（防错配到质心社区）|
| miss（无坐标）| 39,135 | 仅全量口径 |

### 2. 事件三大类（直接用诉求类型_归·不引入 polarity 重判）

| 事件类 | 定义 | 件数 |
|---|---|---|
| 投诉 | 诉求类型_归 = 投诉 | 46,870 |
| 求助 | 求助 + 咨询 | 8,131（1,275+6,856）|
| 建议 | 建议 | 2,206 |
| 其他 | 表扬 + 其它 | 58（不计入三大类·口径注单列）|

### 3. 9 类问题（落图版·board 直切·闭合验证通过）

**安全韧性（4 类）**：管网安全 4,249 / 出行安全 2,895 / 消防安全 687 / 环境安全 215 = 8,046
**民生基础（5 类）**：噪声 14,797 / 停车 7,359 / 住宅 11,452 / 出行 6,981 / 物业 8,603 = 49,192

> 命名对齐用户 page6 五类定稿（噪声/停车/住宅/出行/物业）+ page4 四类（管网/出行/消防/环境安全）。

### 4. 命名隔离（防撞车）

- **「9 类问题（落图版）」** = 本轮 board×project_type 归并（字段名 `类9落图`）
- **「9 归并类（事件-领域版）」** = CB-29 三层定稿旧体系（page2 表D 在用）
- 两套禁止都叫"9 类"。

### 5. 口径隔离（防混算）

- 83.3%（47,693）口径链建立在**原诉求类型_归**上，不动。
- 新三大类（咨询并入求助）**只用于落图统计层**，与 83.3% 口径链隔离，禁止混算。

### 6. 社区/村统计规则

- sjoin 只用 **ok 11,207 点**；
- 重叠（村面∩社区面）：**城市社区优先去重**；
- 县市点（非 174 社区+18 村范围）：**区级统计**，不静默丢弃。

---

## 二、执行清单

### Codex（主开发·DATA 侧）

1. 治理脚本入库 `SCRIPT/govern_12345_points.py`（可复现）
2. 生成 `12345_有坐标点.geojson`（ok 11,207 + region 6,923·标记 geocode_status/事件/9类/方面/社区村归属）
3. 生成社区×9类 + 村×9类 矩阵（ok 点 sjoin·城市优先去重）
4. 更新 page4/page6 md + 正式 Excel（全口径有坐标口径·社区村分别·三套口径衔接注）

### zcode（数据侧·中转站）

1. `12345_情绪地图中转版.csv` 补 `诉求类型_归` + `类9落图` 两列（办件编号 join）
2. `12345_分类映射.json` 升 v3（nine_class_mapping + event3_redef）
3. `manifest.json` v1.6.0 + `字段说明与坐标系说明.md` 补字段 + `交付说明.md` 修订

---

## 三、三套口径并存（衔接注·防 CB-29 再犯）

| 口径 | 数字 | 用途 |
|---|---|---|
| 全量（57,265）| 安全 4,800/民生 42,871（三层定稿）·83.3% 47,693 | page2 线二总览 |
| 有坐标（18,130）| ok 11,207 + region 6,923 | 本轮落图统计层 |
| 命中（精确点位）| 旧 page4 1,376 / page6 7,340 | 已作废·新落图口径替代 |

---

*收敛方：Codex · 2026-08-13 · 三组共识定稿 · 按此执行治理*
