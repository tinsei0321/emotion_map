# PT-CB5 · T3 执行记录（dsh·2026-08-19）

> 执行：dsh（副手）· 2026-08-19 · 分支：`EMC_harness_dsh`（main 冻结勿碰）。
> 任务：G10 七件标准插座（MCP server）——按派发单零判断执行；**与实际签名不符处停手记待主手裁决**。

---

## 一 交付与销号

| 项 | 状态 | 说明 |
|---|---|---|
| `tools/mcp_server_emc.py` | ✅ 新建 | 六个工具已实现；FastMCP 惰性装配 |
| `tests/test_mcp_server_emc.py` | ✅ 新建 | 10 用例 |
| `requirements.txt` | ✅ 追加 `mcp>=1.0.0,<2.0.0` | 见待主手裁决② |
| 本执行记录 | ✅ 落盘 | — |

## 二 工具实现状态（七件套）

| 工具 | 追踪号 | 状态 |
|---|---|---|
| list_data | F_021 | ✅ 实现（不含样例值·demo 默认不列·usage 标记透出） |
| rag_query | F_022 | ✅ 实现（k 夹取·synthesize=deferred_v2·索引缺失 ok=False） |
| kb_facts | F_023 | ✅ 实现（按主手裁决直映真身签名 query/keyword/topic/limit） |
| outlet_card | F_024 | ✅ 实现（确定性组装·空入参 cards=[]） |
| zonal_stats | F_025 | ✅ 实现（rows 硬顶 20·row_count 全量·caliber） |
| buffer | F_026 | ✅ 实现（面积+圈内点计数·大几何 fc_omitted） |
| rank | F_027 | ✅ 实现（worst/best·rows 硬顶 20） |

- 七工具纯只读、零 LLM、重依赖全部函数体内惰性导入；返回均带 caliber 四键（scale/semantics/limits/refs）。

## 三 验收

- `py -m pytest tests/test_mcp_server_emc.py -q`：**11 passed**。
- `py -m pytest tests/ -q`：**414 passed + 3 skipped**（基线 403+3，上浮 +11）。
- stdio 冒烟：`py -u tools/mcp_server_emc.py` 输出 `[OK] EMC MCP server stdio 启动（Ctrl+C 退出）` 后正常退出（本机 stdin EOF·无客户端连接；无异常）；`build_server().list_tools()` = `['list_data','rag_query','kb_facts','outlet_card','zonal_stats','buffer','rank']`。
- 零 key 泄漏自查：对两个新文件 grep `API_KEY|SECRET|TOKEN|PASSWORD|AMAP|DEEPSEEK|ARK` **零命中**。

## 四 五判据逐工具一行答辩

| 工具 | 为什么不用 bash 裸调 |
|---|---|
| list_data | 结构化 JSON 枚举 + usage/name_field 标注，bash 要读 manifest 手工拼且无口径标签 |
| rag_query | 返回 score/source/type/data_dim 结构化素材，bash 无 schema 且丢失维度标签 |
| kb_facts | 确定性事实卡查询（关键词精确 WHERE），bash 要手写过滤且无 source/year 口径 |
| outlet_card | 确定性组装出行业卡片 JSON，bash 需重写整套契约映射逻辑 |
| zonal_stats | 输入 schema 校验 + 返回值自带 caliber 口径边界，bash 解析 stdout 且纪律靠自觉 |
| buffer | 面积/圈内点计数/几何省略规则内建，bash 无错误语义与体积纪律 |
| rank | 排序规则（worst/best）+ rows 硬顶内建，bash 无组合性与截断保证 |

## 五 待主手裁决

1. ~~kb_facts 签名适配~~ **已按主手裁决解决**：`kb_facts(query, keyword, topic, limit)` 直映真身 `query_knowledge_base(query, city, topic, keyword, limit)`，domain 参数作废；F_023 描述已更新。
2. **mcp 版本锁定**：最新 `mcp==2.0.0` 已移除 `mcp.server.fastmcp`（派发单指定用法），故 requirements 锁定 `mcp>=1.0.0,<2.0.0`（实测安装 1.29.0 可跑）；如主手另有指定版本请覆盖。
