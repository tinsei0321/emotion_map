# PT-CB11 · 全批终审（claude 组·2026-08-22）

> 终审方：claude 组（第三方独立·零实施零 git 写·本报告为唯一产出）。
> 范围：上轮独立审计（P1 审计 2026-08-21）之后的全部增量——Codex P2 两阶段 + 主手协同修 4 处 + Kimi 三件 + Codex C3（全批 8 线·commit 链 `bf8c2ab3`→`9d5ec0bb`）。
> 方法：四份回收链深读 + 关键代码独立实证（旁路探测/守卫覆盖地图/基向推演/F 号跨文件核）+ 全量测试实跑。

---

## ① 全批总裁决

**通过（放行收官）**——两条过审条件实证闭环、守卫安全效果零缺口、口径诚实性达标、前端基向一致、F_021-F_040 连续、全量测试 **502 passed + 2 skipped 全绿**（实测优于状态表记载——7 件环境失败已清零）。附 P2 级新发现 1 件（guard 架构宣称落差·不阻塞）+ P3 级 2 件记账。

---

## ② 逐线增量复核表

| 线 | commit | claude 独立复核锚点 | 结论 |
|---|---|---|---|
| zcode B3-1/B3-2 | 0fb5d604 | 上轮已审 A 通过；本轮闭环项见 ③ | ✓ |
| Codex C1 三件 | 3f9e55a4 | 上轮有条件；条件闭环见 ③ | ✓ 转通过 |
| 主手协同修 4 处 | 99b2be93 / 246df7bc / 0ae56c62 | nearest representative_point 适配+_PAIR_BUDGET 5e7（17418×27717 静默 7.7GB 矩阵治本·真问题）；A9 尾 except 改 WARN；前缀收紧见 ③ | ✓ |
| Kimi K1 前端四件 | d9685861 | B3-3~B3-6（图例判定/tip 绑定/面板防误导/全零可观测）——上轮审计关注的 B-3 根因链前端侧收口；主手回收考核优 | ✓ |
| Kimi K2 area_stats | 99521515 | F_037 + 体内守卫（:747）+ 判空（:1103 同款语义化） | ✓ |
| Codex P2 Phase1/2 | bf8c2ab3 + 94bdf11c | 空集补丁 4 处（:577/:650/:948/:1103）；trend/report 见 ④a；guard 见 ④b | ✓（附 P2-1 落差） |
| Kimi A-4 徽章 | 6cdf443a + 4b2e001d | 主手 /version 现网实测 + 证据 4 件入库 | ✓ |
| Codex C3 样式面板 | 10732920 | 见 ④c | ✓ |

---

## ③ 两条过审条件闭环确认（上轮审计裁决的执行验证）

**P2-1 空集补丁 → 双落 + 超额闭环 ✓**

- grid：[mcp_server_emc.py:577-579](tools/mcp_server_emc.py#L577) `row_count == 0 or 'point_count' not in merged.columns` → 语义化 hint「点层为空或 boundary 裁剪后零点」——**上轮指出的两条坏路径（int(nan) 模糊错误 / KeyError 误报未知层）双条件全防**；
- compare：[:650-652](tools/mcp_server_emc.py#L650) 同款；
- **超额**：overlay（:948）与 trend（:1103）两个新工具也内置同款判空——上轮建议「空集边界列入标准用例模式」被直接采纳为实施惯例。

**P2-2 前缀收紧 → 枚举化旁路真堵死 ✓（实弹探测）**

- [render_policy.py](core/render_policy.py)：`DATASET_PROP_PREFIXES` 通配**整体退役**（grep startswith 双文件零残留）；聚合衍生列 `score_sum/score_std` + 体检轨 `polarity_score_5` 显式入枚举（commit 自述=实测唯一前缀依赖）；
- **本审计实弹探测**：`poi_private_note`/`place_secret`/`domain_xxx` → **False 拒绝**；合法衍生列 → True；`诉求总量` 无 preset 声明 → False（默认拒绝语义保持）。旁路确认堵死。

---

## ④ 重点核验详录

**a. trend / report 口径诚实性 ✓**

trend caliber limits 原文：「三期=采集批次非等间隔日历期——**勿算速率/环比**；趋势为聚合口径非个体追踪；单期深挖用 zonal/hotspot」——如实约束解释边界，禁过度解读的两类最可能误用（速率/个体追踪）均显式拦截。

**b. guard 迁 server 侧完备性 → 安全零缺口 + 架构宣称落差（本终审唯一 P2）**

- **安全效果面（完备）**：全部带面输入的 9 工具体内守卫覆盖地图实证——zonal:410 / buffer:463 / rank:508 / grid:568 / compare 逐区:638 / area_stats:747 / nearest:824 / overlay 双参:926-929 / trend:1085——**无漏网工具**。
- **架构面（落差·P2-1 新发现）**：`_GUARD_SPECS`（:977-985）声明 7 件，但 `_guard_check` **仅 trend 1 处接线**（:1085）——其余 6 件守卫仍在各自函数体内。「guard 迁 server 侧」的实际形态=**集中机制建立 + 单点试点**，迁移未完成。风险不在当下（体内守卫全部有效）而在未来：**表无 magic**——新工具挂了表若不主动调用 `_guard_check` 即无守卫；`_audit_input_surfaces` 启动核对的是参数名差集（spec vs 签名），**抓不到漏接线**。建议（不阻塞收官·下批顺手）：要么在工具注册分发层统一接线，要么 `_GUARD_SPECS` 表头注释明示「本表=核对清单·接线仍需体内调用 _guard_check」。

**c. C3 前端换色带一致性 ✓（代码级与主手浏览器验证互证）**

- 本地重算链：[settings.js:271-286](frontend/js/settings.js#L271) `normStops(k)` 重算 → `applyPaint({gridStops, rampKey})` 零服务端往返；
- **count 基向一致**：编辑器侧 normStops 输出 **[0,1] 归一域**（[grid-tool.js:70-75](frontend/js/grid-tool.js#L70) `(d-dMin)/span`）；投递链侧 [render_client.js:165](frontend/js/render_client.js#L165) `gridField:'_count_norm'`（countNorm 归一）——**两侧同域**，换色带只换 stops 不动 field，表达式基向无缝。与主手「图例双入口→popover→换 blues→地图实时变蓝」浏览器级验证互证闭环。
- 契约 §七-7 升级与代码行为一致（主手 C3 回收已对照·抽验无反例）。

**d. F_021-F_040 连续性 ✓（跨文件核）**

mcp_server_emc.py 内 021-028+031-040，**F_029/F_030 跨文件闭环**（api/render_routes.py:39-40 注册 + :217 @track 使用）；F_029 高频扫描免 @track 有**明文豁免登记**（注册串自述 + AGENTS 豁免条）。缺号零·连续 ✓。

---

## ⑤ 新发现清单

| # | 级 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| N1 | **P2** | guard「迁移」实为试点形态：`_GUARD_SPECS` 7 件仅 trend 1 件接线 `_guard_check`·其余体内；表无 magic·_audit 抓不到漏接线——**安全零缺口·架构宣称落差** | mcp_server_emc.py:977-985（表）/:1085（唯一接线）/:410-929（体内守卫地图） | 下批统一接线或表头注释明示；新工具 SOP 加「接线 _guard_check」一步 |
| N2 | P3 | emc_status（F_032）注册但 `def emc_status`（:1498）**无 @track**——「临时测试件」豁免未成文（对比 F_029 明文豁免） | :39 / :1493 / :1498 | 补 @track 或补成文豁免·二选一 |
| N3 | P3 | 环境依赖已补装（实测全量 502 绿零环境失败）但 Ops 挂账/全批状态表未同步——记载仍写「7 存量环境失败挂账」 | 本报告实测 vs C3 回收 §二 | 挂账销项 + 状态表更正（防下轮又当存量跳过） |

遗留确认（不阻塞）：untracked `tools/gen_yichang_l2_800m_full.py`（游离 800m 脚本）仍在等用户裁决——与 246df7bc 回收记录一致。

---

## ⑥ 放行意见

**放行 PT-CB11 收官**。理由汇总：

1. 两条上轮过审条件实证闭环（其一超额推广为实施惯例）；
2. 守卫安全效果零缺口（9 工具覆盖地图实证）·唯一 P2 为架构宣称落差、不影响任何现网安全行为；
3. 口径诚实性（trend 禁速率/环比）、前端基向一致性（代码级+浏览器级双证）、F 号连续性（跨文件核）全过；
4. 全量测试 **502 passed + 2 skipped**（实测优于状态表·环境项清零）；
5. 全批 DoD 对照：工具面 10→18 ✓ / B-3 后端根因收口 ✓（用户复测四口径待 T5·状态表已就绪）/ 契约条目（§七-7 + trend 契约）✓ / 纯仓内双环境 ✓ / 回收落盘全链 ✓。

**随收官带走三件**：N1 guard 统一接线（下批第一顺位·防新工具漏守卫）、N2 F_032 埋点补齐或成文豁免、N3 挂账销项同步。游离 800m 脚本维持留用户裁决。

—— claude 组 · 2026-08-22 · 全批终审·零 git 写（本报告除外）
