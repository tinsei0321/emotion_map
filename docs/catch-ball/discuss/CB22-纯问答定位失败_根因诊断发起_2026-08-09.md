# CB-22 · 纯问答定位失败 · 根因诊断发起（「宜昌市城市更新的项目有哪些？」出图层）

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（知识问答·用户实测再失败·进 CB 查根因）
> **背景**：用户手动检查复测「宜昌市城市更新的项目有哪些？」——**回答又偏到「出图层」**·纯问答定位失败。要求进 CB 查根因。

---

## 〇、trace 取证（第一动作·铁律）

**会话 sess-8072-1786271653（18:50-18:52·用户实测）**：

```
18:50:54 MOD_LLM.F_005 chat_with_tools_stream tools=14 msgs=2   ← diagnose 全工具 FC（14 工具）
18:51:03 MOD_AIQA.F_002 + MOD_LLM.F_001 msgs=2                  ← agentStep 内 LLM（工具执行）
18:51:15 MOD_AIQA.F_002 + MOD_LLM.F_001 msgs=2                  ← 又一次
18:52:31 MOD_LLM.F_005 chat_with_tools_stream tools=14 msgs=4   ← 再次 diagnose（多轮）
18:52:41 MOD_AIQA.F_002 + MOD_LLM.F_001 msgs=4
```

**无 `F_015`（rag_search）**——对比成功链路（18:34 同会话早前 `F_015` 走通）·本次**知识问答短路未触发**·落 diagnose FC 全工具链路。

## 一、根因链条（代码证据）

| 层 | 代码 | 证据 |
|---|---|---|
| **① 触发缺口** | `frontend/js/ai_qa/emc-patterns.js` `RAG_QUERY_KW` | 词表含「有哪些项目」·问句是「**项目有哪些**」（词序反）→ 命中 `[]` |
| **② 短路不触发 → 落点** | `harness.js:71-78` `_quickIntent` | :71 RAG 双条件不中 → :75 `REGION_KW`「宜昌」命中 → `SEARCH_EVIDENCE_RE` 不中（「更新」非实据词）→ :77 `return null` → **落 diagnose** |
| **③ diagnose 无问答通道** | `ai_qa/prompts.py` MANIFESTO/diagnose prompt | diagnose 是**分析/图层导向**（三句骨架「图层是主出口」）·**没有「知识问答/纯问答」分类**——知识问答完全靠短路拦截·短路漏网 = 无处可去 |
| **④ finalStep 图层导向** | `prompts.py:134` FINAL_TEMPLATE | 三句骨架「严禁只给文字·图层是主出口」→ 回答必然出图层 |

**根因定案**：**纯问答定位完全依赖 `_quickIntent` 词表短路命中；词表漏了（词序变体）= 知识问答通道消失 → 问句落 diagnose（分析/图层导向·无问答分类）→ 必然出图层**。这是**路由层根因**（短路词表脆弱 + diagnose 无问答兜底），**非** B5 指令层（本次连短路都没进·B5 仍未被实证）。

**同类词序缺口**（未验但可疑）：「宜昌有哪些城市更新项目」（「城市更新」插在中间）·「宜昌城市更新项目有哪些」等——凡 RAG_QUERY_KW 连续子串不命中的变体。

## 二、待两组诊断焦点

1. **根因定案**：上述 4 层链条 agree/disagree？有无遗漏根因层？（如 diagnose 分类是否真无问答类·查 diagnose prompt 分类枚举）
2. **「纯问答定位」架构**：知识问答靠**短路词表**定位——**架构是否脆弱**？该给知识问答一个**不依赖词表的兜底通道**吗？（如 diagnose 分类加「知识问答」类·或 RAG 触发放宽）
3. **修复方案收敛**（选一或组合·给证据）：
   - **A 触发放宽**：`RAG_OPEN_RE` 开放问词（有哪些/哪些/什么/如何/怎么）+ 知识词 gate——覆盖全部词序变体（claude 已 node 模拟验证：失败问句 ✅ 正例保持 ✅ 负例不误触 ✅）
   - **B diagnose 加问答通道**：diagnose 分类枚举加「知识问答」→ 短路漏网也能路由到问答范式（PARADIGM_MAP 已有 knowledge_qa）——治「短路=唯一通道」架构脆弱
   - **C 其他**（请补充）
4. **B5 关系澄清**：本次失败是**路由层**（短路没进）非指令层——B5（指令优先级）仍须短路进了才实证（用户后续复测「宜昌有哪些更新项目」若仍出图层才坐实 B5）
5. **防回归**：触发词表/开放问词的正负例回归测试（机器化·node 模拟或 e2e-seam）——如何设计？

## 附：发给两组的诊断 prompt（A 组通用）

```
【CB-22 纯问答定位失败 · 根因诊断（「宜昌市城市更新的项目有哪些？」出图层）】

背景：用户复测「宜昌市城市更新的项目有哪些？」→ 回答偏到「出图层」·纯问答定位失败·
进 CB 查根因。claude 已 trace 取证（sess-8072：F_005 tools=14 无 F_015·短路未触发）。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-纯问答定位失败_根因诊断发起_2026-08-09.md（根因链条 + 焦点）
- 核实：frontend/js/ai_qa/harness.js（_quickIntent :60-82·RAG 短路 :71 在 REGION :75 之前）+
  frontend/js/ai_qa/emc-patterns.js（RAG_QUERY_KW 词表·RAG_KNOWLEDGE_RE）+
  ai_qa/prompts.py（diagnose prompt 分类枚举·MANIFESTO·FINAL_TEMPLATE 三句骨架 :134）+
  .trace/trace.log（sess-8072-1786271653·18:50-18:52 段）

第二步 · 诊断焦点（agree/disagree + 证据·可交叉挑战）
1. 根因定案：4 层链条（触发缺口→落 diagnose→无问答通道→图层导向）·有无遗漏？
2. 「纯问答定位」架构：依赖短路词表是否脆弱·该有兜底通道吗？
3. 修复方案收敛：A 触发放宽（开放问词+知识词 gate）/ B diagnose 加问答通道 / C 其他
4. B5 关系：本次是路由层非指令层·B5 仍须短路进了才实证？
5. 防回归：触发正负例回归测试如何机器化？

第三步 · 产出
- 落盘 docs/catch-ball/discuss/CB22-纯问答定位失败_根因诊断_{组名}_2026-08-09.md
- 中文为主·引用 path:line/trace 行·不 git
```

---

*claude组（2026-08-09）·纯问答定位失败根因诊断发起·trace 取证 + 4 层根因链·待两组诊断收敛。*
