# PT-CB16 · C1/C3 执行记录（Kimi·2026-08-25）

> **任务书**：`PT-CB16-短板修复计划书_Kimi-2026-08-25.md`（C1 E2E 骨架 + C3 SSE 多页面串台；C2 先行产出 D1 讨论稿）。
> **门禁**：**607 passed / 1 skipped**（基线 602+1 → 净 +5：e2e×3 + C3×2；零失败零退化）。
> **git**：未 commit（主手/用户代提交·建议前缀 `PT-CB16(C1C3)`）。

## 白话摘要

两件事做完了。第一件「机器质检员」上岗了：现在有一套自动检查，会模拟真人走一遍「分析→出图→取数」全流程，还用真实数据核对「TOP7 的边界顶点是不是 100% 来自原图」——以后再有「边界变糙」或「旧图复活」这类病，机器比人先发现。第二件「双屏不串台」：地图页开两个时，可以指定某张图只上其中一块屏；不指定就照旧全上（老习惯不变）。另外给「会接话/会请示/会看事情大小」三条规矩写好了讨论稿，等主手组织评审，定了就开工。

## 一、C1 · E2E 自动化测试骨架

| 层 | 内容 | 状态 |
|---|---|---|
| A 级 HTTP 链路（`tests/e2e/test_render_chain_e2e.py`） | zonal(layer_output)→render_spec(dataset_id)→scan_inbox 消费→**TestClient 真实路由** dataset 端点取数（renderFields 透传断言） | ✅ 2 条过 |
| B 级黄金问题（integration 标记） | 真实 193 层+真实点层跑「诉求最多 7 社区」→ 7 要素 + **顶点 100% 源顶点子集 + 面均顶点 ≥30 双防线** | ✅ 1 条过（真实数据实测） |
| 浏览器级（`tests/browser/ptcb16_render_e2e.py`） | A 图层出现/B 硬刷新无复活（hello 协议）——按 s3 先例（隔离栈手跑） | ⚠️ 脚本入库·**栈上验证未完成**（office 8000 在役·未起隔离栈实跑·已在脚本头诚实登记） |

**踩坑记录**：①mock 泄漏——分析步的 resolve_boundary mock 罩住了端点步（R8 同构教训·改为端点步还原真实 resolve）；②真实 manifest 复制到 tmp 后相对路径断裂（各条目 file 改绝对路径解决）。

## 二、C3 · SSE 多页面串台严谨化

| 件 | 实现 | 状态 |
|---|---|---|
| 订阅身份 | 前端 sessionStorage 生成 `sub-*` id（每标签页独立）→ `?sub=` 随 SSE 连接登记 | ✅ |
| 定向投递 | spec 可带 `target`（render_spec 新增可选参数·缺省不写字段=广播）；服务端 `_publish` 仅投同名订阅；前端再过滤一层（防御纵深） | ✅ |
| 宽松规则 | 无 target 一律广播（dsh/脚本既有生产者零影响·T21 零退化） | ✅ 测试覆盖 |
| 顺手修 | render_stream docstring 漂移（仍写「先推 backlog」→ 改属实描述） | ✅ |

**接口变更**：render_spec 增可选参数 `target`（向后兼容·签名只增不改）；`/render/stream` 增 `?sub=` 查询参数（可选）。

## 三、C2 · D1 讨论稿（规格评审件·零实施）

`PT-CB16-D1-交互三机制讨论稿_Kimi-2026-08-25.md` 已落盘：追问（followup_cues 结构化+续接路由）/选择（三触发+单轮 ≤1 硬选择）/规模（意图卡 scale 枚举+出口核对软提示），附三个裁决点与实施切分（C2-1~C2-3）。**未定稿不实施**。

## 四、改动文件

- 新增：`tests/e2e/test_render_chain_e2e.py`、`tests/browser/ptcb16_render_e2e.py`、`docs/catch-ball/discuss/PT-CB16-短板修复计划书_Kimi-2026-08-25.md`、`PT-CB16-D1-交互三机制讨论稿_Kimi-2026-08-25.md`、本记录
- 修改：`api/render_routes.py`（C3 订阅+定向）、`tools/mcp_server_emc.py`（render_spec target）、`frontend/js/render_client.js`（订阅身份+过滤）、`tests/test_render_channel.py`（C3 两测）

## 五、验收指引（R7 三态：重启 8000 + 硬刷新 8080）

1. `pytest tests/e2e/ -q` 全过（含 integration 真实数据）；
2. 双开两个地图标签页 → 投递 `render_spec(..., target='<页A订阅id>')` → 仅页 A 上屏；不带 target 两页同上（页 A 订阅 id 见控制台「订阅 sub-xxx」日志）；
3. 浏览器级脚本：起隔离栈 8090/8009 后跑 `py tests/browser/ptcb16_render_e2e.py`（首次栈验结果补记脚本头）。

## 六、遗留

- C2 等 D1 裁决（三个裁决点见讨论稿 §一~三）；
- 浏览器级脚本栈上验证（首次到岗起隔离栈顺手跑·约 2 分钟）；
- commit+push（规则七）。

---

> Kimi · 2026-08-25 · C1/C3 完成·C2 讨论稿待裁决。
