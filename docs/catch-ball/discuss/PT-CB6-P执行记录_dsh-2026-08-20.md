# PT-CB6 · P 先行件三砖执行记录（dsh·2026-08-20）

> 执行：dsh（副手）· 2026-08-20 · 分支：`EMC_harness_dsh`（main 冻结）。
> 任务书：`PT-CB6-P先行件任务书_zcode-2026-08-20.md`（函数级规格·零判断执行）。

---

## 一 三砖销号

### P1 · render_spec 第 8 插座 + P1b layer_output — ✅

- 文件：`tools/mcp_server_emc.py`
- `register_track_id('MOD_AIQA.F_028', 'MCP render_spec（图层图纸：dataset/inline→spec 落收件箱）')` + `@track`。
- `render_spec(kind, name, dataset_id='', geojson=None, value_field='polarity_index', ramp_hint='', zoom_to=True, producer='dsh', source_tool='manual')`：
  - kind/name/data 校验全部语义化；dataset_id 与 geojson 同时给 → dataset_id 优先并记 `fixes`；
  - dataset_id 可解析（manifest preset 任意 usage·点层 available 集）；usage 填 `caliber_lite`；
  - inline FeatureCollection 要素 ≤60；choropleth 须 value_field；
  - spec 写 `DATA/exports/render_inbox/<spec_id>.json`（ensure_dir·UTF-8 无 BOM）。
- `zonal_stats` / `rank` 增 `layer_output: bool = False`：
  - True 时返回 `geojson`（仅 top_n 行多边形·`__geo_interface__`→`to_json`·WGS84·≤20），properties 含统计值与 `value`；
  - False 无 `geojson` 键（默认零变化）。

### P2 · 投递通道 — ✅

- 文件：`api/render_routes.py`（新）、`api/main.py`（唯二行：import + include_router）。
- `register_track_id`：F_029（watcher+SSE）、F_030（dataset 端点）；F_030 挂 `@track`（watcher 高频扫描不挂 @track 防 trace 刷屏）。
- watcher：模块导入即起 daemon 线程，扫 `DATA/exports/render_inbox/*.json`（spec_id 排序·last_seen）→ `queue.Queue`；坏 JSON/spec_version 不符 log 一行跳过（A9）。
- `GET /api/v1/render/stream`：SSE，先补 backlog（最近 20 条）再持续推新；事件 `event: spec\ndata: <json>\n\n`；每 15s 心跳注释。
- `GET /api/v1/render/dataset/{dataset_id}`：preset→`resolve_boundary`；点层→`resolve_points`；>2000 要素降级；未知 id 语义化 hint。

### P3 · 前端 loader — ✅

- 文件：`frontend/js/render_client.js`（新）、`frontend/index.html`（唯一行 module script）。
- `EventSource('/api/v1/render/stream')`；`spec` 事件 → `_apply`。
- 取数：dataset_id → fetch dataset 端点；inline geojson 直用。
- 令牌解析权威在前端：point → `addToolboxLayer`（`{radius:7, color:'#ff9000', opacity:0.9}` + `colorMode:'needsAnalysis'`）；choropleth → `piToNorm` 写 `_grid_norm` + `defaultPaint('zonal','polygon')`（`ramp_hint='worst_first'` 时反转向色带）。
- 图层名 `[dsh] ` 前缀；`zoom_to` 由 addToolboxLayer `fit` 参数实现；layer 对象附 `origin`/`usage`；analysis_output 控制台提示一行。

---

## 二 grep 所得真身签名（前端复用依据）

- `addToolboxLayer({name, kind, fc, paint, colorMode, parentId, fit})`：`frontend/js/toolbox/shared.js:188`；内部 addLayer/renderLayer/去重/fitBoundsTo。
- `defaultPaint('zonal','polygon')`：`shared.js:138` → `_ui.tool='zonal'` + `gridField:'_grid_norm'` + `gridStops:polarityStops('overall')`。
- `piToNorm(pi)`：`frontend/js/grid-tool.js:168`（pi -2~2 → 0~1）。
- `polarityStops(pol, palette, reverse)`：`grid-tool.js:58`。
- 点层 circle 形态照抄 `generate_point_layer` 参数形态（`ai_qa/tools.js:1409` 附近；实际 map.js 消费 `radius/color/opacity`）。
- `fitBoundsTo(bbox, padding, maxZoom)`：`map.js:1145`（addToolboxLayer 内部已用）。

---

## 三 测试与验收

- `tests/test_render_channel.py`：8 用例（spec 落盘契约/inline 上限与双缺/dataset 校验/layer_output/watcher 顺序+坏文件跳过/dataset 端点/未知 hint）。
- `py -m pytest tests/test_mcp_server_emc.py tests/test_render_channel.py -q`：**21 passed**。
- `py -m pytest tests/ -q`：**425 passed + 2 skipped**（任务书基线 416+3；本批新增 render 8 用例；pii_guard 1 skip 转 pass 的环境漂移已如实记录）。
- `node --check frontend/js/render_client.js` 与 `node --check frontend/js/toolbox/shared.js`：通过。

## 四 §七 演示三步实测（命令回执）

1. `py frontend/serve.py 8080` 后台起服务 → `Uvicorn running on http://127.0.0.1:8000`，8080 代理正常。
2. 本机模拟 dsh 调用：
   - `rank(boundary='admin_district', layer_output=True, top_n=3)` → `rows=3 / geojson features=3`；
   - `render_spec(kind='choropleth', name='情绪最差三区', geojson=rank.geojson, value_field='polarity_index', source_tool='rank')` → `ok:True`，落盘 `DATA/exports/render_inbox/1787155459868-8428.json`。
3. SSE 连通：连接 `/api/v1/render/stream` 读到 `event: spec`，`spec_id=1787155459868-8428`、`kind=choropleth`、`name=情绪最差三区`。
4. 直渲一路：`render_spec(dataset_id='admin_street', kind='choropleth', name='街办范围直渲')` → ok；`GET /api/v1/render/dataset/admin_street` → `200 / count=113 / features=113`。
5. 浏览器亮层为**用户视觉验收**步骤（8080 页面 + [dsh] 前缀层）；本机无浏览器截图层，交由主手回收后用户实测。

---

## 五 待主手裁决

1. 前端 token 解析权威在 JS 已遵守；choropleth 对 `value_field` 的通用分层仅支持数值型（v1 未做 category 分支）——若需 category choropleth，请裁决 v2。
2. watcher 扫描函数未挂 `@track`（高频 1s 扫描会刷 trace.log；F_029 仅注册）。如主手要求扫描也埋点，请指示（建议保留现状）。
3. `node --check` 验证的是 render_client.js 与其复用的 shared.js；未跑浏览器端亮层视觉验收（用户步）。
