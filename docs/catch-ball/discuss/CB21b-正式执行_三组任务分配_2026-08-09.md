# CB-21b · 正式执行 — 三组任务分配

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-21b（方案已定稿·正式执行）
> **目的**：G 盘 875 文件全量读取 + 提炼笔记 + 回填 EMC 知识库。三组并行，目录锁定不交叉。
> **前置已就绪**：L0 骨架已建（`docs/urban-renewal-plan/`：README/_INDEX/_PATHS/_提炼模板 + 九目录）

---

## 一、三组分派（目录锁定·不交叉）

| 组 | 平台 | 锁定目录 | 产出 |
|---|---|---|---|
| **glm组** | ZCode+GLM 5.2 | 0/会议文件 156 JPG 识图（P0 核心优先）· 北京/杭州 OCR · 体检图纸 JPG · PPT 图表 | JPG 识图笔记 + OCR 笔记 |
| **codex组** | Codex+deepseek | 0/部委+政策 docx · 湖北 4 PDF · 体检 6 附件 · 3案例 PDF · PPTX 文本层 | 文本提炼笔记 |
| **claude组** | Claude Code | 1/3_gis 三维度（GDB+shp）· 2/超大 PPTX 转 PDF · CAD dwg · .doc legacy · 葛洲坝 125MB · 根目录修编 · 汇总+回填 | GIS 图层表 + 提炼 + L1 回填 |

**三组共同优先**：体检 6 附件（指标/问题/项目库——决定 L1 回填粒度）。交叉抽验 10%。

## 二、统一模板（13 字段·见 `_提炼模板.md`）

完整名称/文号/机构/城市/版本状态/适用时段/采集状态/来源/位置(`{URENEWAL_ROOT}`)/参考位置/指标数据/归纳/EMC 对应。JPG 加识图质量/原始出处；GIS 加图层对应。

## 三、产出落盘

各组笔记落 `docs/urban-renewal-plan/_笔记/{组名}_{主题}_{日期}.md`（CB 权限内可写）；_INDEX 对应行填登记（单写者·每行一人填）；**claude组 push**。

## 四、执行顺序

1. 三组并行读盘（各组按分派目录）
2. 交叉抽验 10%（每组抽查另一组 1-2 份核心）
3. claude组 汇总 → L1 回填 urban_renewal.py（单写者·守 schema+体积）→ L2 outlet
4. 双环境 `_PATHS.md` 配置 + README 打包

---

## 附：发给各组的正式执行 prompt（各自对号入座）

### → glm组（JPG 识图 + OCR）

```
【CB-21b 正式执行 · glm组 · JPG 识图 + OCR】

背景：城市更新专项规划资料集 v2 方案已定稿（CB-21b 反评价收敛），正式执行全量读取。
你是 glm组（ZCode+GLM 5.2·多模态强）。锁定目录：

① 0/会议文件 156 JPG 三层筛选识图：
   - P0 核心图（~47 张·每子目录封面/章节首页/关键指标图表）→ 全量识图笔记
   - P1 支撑图（~62 张）→ 摘要笔记（标题+关键数）
   - P2 边缘图（~47 张）→ 只登记文件名+一句话
   - 子目录：01意见解读(37)/02财政部(4)/03发改委(15)/04国开行(18)/05编制导则(28★)/06城中村危旧改(31)/08愚园路(23)
   - ★05 编制导则（建办科〔2025〕46号）优先：它是 L1→L2 直接源头
② 北京 PDF 乱码 + 杭州征求意见稿扫描件 → OCR
③ 体检图纸 JPG（01-15·含数值图例如覆盖率 0%）→ 识图数值（与附件3对照）
④ PPT 图表识图（小 PPT·超大 claude 转 PDF 后给你）

模板：docs/urban-renewal-plan/_提炼模板.md（13字段 + 识图质量/原始出处）
落盘：docs/urban-renewal-plan/_笔记/glm_{主题}_{日期}.md
纪律：只读 G 盘（{URENEWAL_ROOT}）·不 git（claude push）·单写者（_INDEX 每行你填你锁的行）
```

### → codex组（文本提炼）

```
【CB-21b 正式执行 · codex组 · 文本提炼】

背景：城市更新专项规划资料集 v2 方案已定稿（CB-21b 反评价收敛），正式执行全量读取。
你是 codex组（Codex+deepseek-v4-flash·文本强）。锁定目录：

① 0/部委文件 8 docx + 政策文件 9 docx（部分已提炼·补全 + 复核）
② 湖北 4 PDF：指引已提炼(92页)·补 工作导则(22.5MB)+十五五初稿(13.5MB) 标题级+关键章节
③ 体检 6 附件（指标/问题/项目库·三组共同优先）——已提炼数值·补全 + 复核
④ 3案例 PDF：杭州已提炼·补 广州低效用地/成都/上海行动方案
⑤ PPTX 文本层（python-pptx 提取文字·超大 387-666MB 若超时标注给 claude）

模板：docs/urban-renewal-plan/_提炼模板.md（13字段）
落盘：docs/urban-renewal-plan/_笔记/codex_{主题}_{日期}.md
纪律：只读 G 盘·不 git·单写者·遇读不了（乱码/扫描件）标注给 glm/claude
```

### → claude组（GIS + 超大件 + 汇总回填）

```
【CB-21b 正式执行 · claude组 · GIS + 超大件 + 汇总】

背景：城市更新专项规划资料集 v2 正式执行。你（claude组·Claude Code）锁定：

① GIS：1/3_gis 三维度（住房/小区/街区）——GDB 2 个(31 图层) + shp 26
   - pyogrio 读 GDB（已验证 OpenFileGDB）·提取图层名/字段/属性/统计值
   - 产「GIS 图层对照表」（_INDEX §三）：图层↔维度↔指标↔附件数值↔EMC 4×5
   - 住房安全耐久数值与附件1一一对应（42栋=结构隐患等）·反查表
② CAD：2_cad 4 dwg——装 ezdxf 提取图层/数量（50年/2001-2005/250栋危旧/现状一张图）
③ 超大 PPTX：八大片区(387MB)/住新局(271MB)/夷陵(698MB)/专项规划(0819/1201/260713)
   - python-pptx 文本层 → 超大转 PDF 分段 → 图表给 glm
④ .doc legacy：上海考察报告 6 份——Word COM/antiword 转换
⑤ 葛洲坝片区体检 125MB：分片按章节读（指标/问题清单）
⑥ 根目录修编 0809 docx：第一条提炼
⑦ 汇总三组笔记 → L1 回填 urban_renewal.py（单写者·守 schema+体积）→ L2 outlet
```

---

*claude组（2026-08-09）·CB-21b 正式执行任务分配·三组并行开工。*
