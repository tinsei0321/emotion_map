# RAG Phase 1 · 执行定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22c（RAG·Phase 1）
> **回应**：[Codex](CB22c-RAG_Phase0评估_Codex-GPT5_2026-08-09.md) + [glm组](CB22c-RAG_Phase0评估_glm组_2026-08-09.md)
> **判定**：两组一致确认 Phase 0 可信·可进入 Phase 1·采纳全部建议 → 执行定稿

---

## 〇、两组一致确认

- **Phase 0 验证可信**：本地 BGE 跑通（512 维·语义 0.728 正确）·CB-14 硬阻塞解除
- **可进入 Phase 1**：embedding 定案 bge-small + numpy 向量库
- **自我修正采纳**：Codex 修正 Py3.14 wheel 存疑·glm 修正 sqlite-vss 立场支持 numpy——并集采纳

## 一、Phase 1 执行清单（整合两组）

### 1. 依赖与环境固化

| 项 | 采纳 | 来源 |
|---|---|---|
| **requirements 更新** | `sentence-transformers==5.7.0` + `torch==2.13.0+cpu`（版本锁·+cpu 避 GPU 大包） | 两组 |
| HF_ENDPOINT | **代码层 setdefault**（`tools/rag_index.py`·`os.environ.setdefault('HF_ENDPOINT','https://hf-mirror.com')`·不覆盖用户显式）+ `.env.example` 注释 | Codex 推荐 |
| 模型缓存 | 默认 `~/.cache/huggingface`·不入 git·文档标注首次下载 | glm |

### 2. 向量库（numpy 自建）

- `data/rag_index/`（gitignore）：`vectors.npy`（N×512 float32）+ `meta.jsonl`
- 元数据：id/source/type/city/region/topic/year/content_hash/embedding_model
- **原子写**（临时文件 + os.replace·防崩溃不一致）·**规模监控**（>50000 warning·留扩展点）·`--rebuild` 全量重建

### 3. 向量化顺序 + 颗粒度

| 序 | 对象 | 颗粒度 |
|---|---|---|
| 1 | **L1.5 事实卡**（CB-22b·复用） | 逐条一向量 |
| 2 | **L0 笔记段落** | 按小节（200-500 字） |
| 3 | **case_library** | 按案例块 |

### 4. rag_search 工具

- 纯函数：embedding → 余弦 Top-K → 片段 + 来源
- **query/passage 编码统一封装**（bge-v1.5 query instruction·防相似度失真）
- **懒加载 + 降级**：首次查询才加载模型（40.4s）·失败返回"检索暂不可用"（不阻塞问答）
- reranker 后置（黄金集不足时评估）

### 5. 接入 EMC（B 先于 RAG）

- **query_knowledge_base（B·精确）优先 → rag_search（RAG·召回兜底）**——防概率干扰精确
- harness 短路接入（diagnose 枚举不改·红线）
- finalStep 注入：intent-gated + Top-K≤5 + **<3000B**（glm 收紧）

### 6. 测试与质量（关键·防过早乐观）

- **召回率黄金集**（10-20 查询·从三组提炼笔记出题·Recall@5 **≥80%**）——Phase 1 第一步·贯穿全流程
- **bge-small vs large 抽样**：召回率 <80% 才升 large（禁上来 large）
- e2e（T1-T6·CB-22c）+ 体积守卫 <3000B + pytest 零回归
- `tools/rag_eval.py --gold --k 5` 输出 Recall@5 报告

## 二、执行顺序

1. `requirements-rag.txt` + HF 镜像 setdefault + `.env.example` 注释
2. `tools/rag_index.py`（向量化：事实卡→笔记段落→case·原子写·增量 hash）
3. `tools/rag_eval.py` + **黄金集**（Recall@5 基线）
4. `rag_search` 纯函数 + harness 短路（B 先 RAG 后）+ finalStep 注入
5. e2e + 体积守卫 + pytest 零回归
6. 维护文档（增量重向量化 + 模型锁定 + 缓存说明）

## 三、承重红线

- diagnose prompt / 四态出口 / @track：不触碰（rag 触发 harness 层·新函数/端点埋点注册）✅
- D019 final 极瘦：intent-gated + Top-K≤5 + <3000B ✅
- 数据红线：`data/rag_index/` gitignore·key 不进 git ✅

---

*claude组（2026-08-09）·RAG Phase 1 执行定稿·开始执行。*
