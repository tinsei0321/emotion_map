# CB-22 · 三层架构优化 · 执行定稿（两组验证通过 · 含全部细化）

> **执行方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（执行前验证通过 → 执行定稿）
> **前置**：两轮讨论（6 焦点 + 0b/0c）→ 定稿 → 执行前验证（Codex + glm·8 焦点全 agree·零阻断）→ 本定稿并入全部细化
> **状态**：**执行定稿·开始实施**

---

## 〇、执行序（并入两组全部细化）

```
P0-1 红线豁免记录 → P0-3 先扩 eval（gate·含模板命中 gate 0.6 检查）→ P0-2 diagnose 加类（对比句判据·续作例外优先）
  → P0-4 加速器（词表最小化）+ P0-5 合流（_assembleKnowledgeQA·三态阈值 0.5）→ P0-7 分通道增强（ctx.context 条件注入）
  → P1 概念库（3 类·来源清单列明）→ P2 防回归（正例两类 + 枚举断言细化）→ P3 出处（双来源标注）
  → P4 双轨回归 → P5 边界标注
```

## 一、P0 · 意图判断归位（治本·本次失败根因）

| # | 改动 | 文件 | 要点（含两组细化） |
|---|---|---|---|
| P0-1 | 红线豁免显式记录 | KNOWLEDGE §1 + cb-journal | 用户豁免「diagnose 加 knowledge_qa」·3 条件（增量/eval 复采/静态断言）·不通过回滚 |
| P0-3 | 先扩 eval（P0-2 前提） | `tests/` | knowledge_qa 判定断言 + 现有三值零回归 + **模板命中 gate 0.6 检查**（Codex V8 风险 2·新增类可能改 Flash 模板分布）·eval 是 gate 非并行 |
| P0-2 | diagnose 加 knowledge_qa（增量） | `ai_qa/prompts.py` :200 | intent 加 knowledge_qa·不改不删现有三值·判定段（数据/事实/清单类）+ **对比句判据**（有哪些/多少=knowledge_qa·什么是/为什么=general·成对出现·防 Flash 混淆·Codex V1）+ **3-5 示例句**（正例含失败句·负例含「哪些片区情绪最差」）+ **多轮续作例外显式高于承接**（分析后问知识→knowledge_qa·非续作 emotion·Codex V1/glm F1） |
| P0-4 | _quickIntent 降级加速器 | `harness.js` :60-82 | **词表最小化**（宁漏不误·只留高置信 CONCEPT/INVENTORY/GREETING + RAG 精确·Codex V2）·漏网（词序变体/模糊）全落 diagnose LLM 判·**GEO_VERB 仍返 null**（操作保守落 diagnose） |
| P0-5 | 合流路由 | `harness.js` | `diagnose.intent==='knowledge_qa'` → `_assembleKnowledgeQA`（抽公共·复用 rag_query 分支 :977+·**双入口同注入同 finalStep·防行为分叉**·Codex V2）·**data_plan 三态**（glm 挑战 1）：ready=Top-K 非空+score≥0.5 / fallback_annotated=非空+<0.5（标注相关性低）/ request_upload=空（EXIT_GAP·Codex V3 两层：诊断卡判静态可用性·运行时判结果） |
| P0-7 | 分通道增强 | harness ctx.context | **= ctx.context 条件注入**（glm 挑战 2 确认）：知识问答/概念通道注入强标记（:1023·禁图层·只基于素材·综合 Top-N）·分析通道不注入→FINAL_TEMPLATE 图层导向正常——**不碰 D019/FINAL_TEMPLATE**·分析类确定性红线完全不动（Codex 补⑤）·防幻觉底线问答/概念都守 |

## 二、P1 · 概念库（数据层补全）

| # | 改动 | 要点 |
|---|---|---|
| P1-1 | 概念类素材 3 类 | 产品定位/方法论/边界认知·每条 ≤200 字·摘抄+引用·**禁 LLM 重述**·**来源清单实施时列明**（产品定位=KNOWLEDGE 蒸馏/CLAUDE.md·方法论=08-03 出口抽象层/范式文档·边界=四态/颗粒度/能/不能·Codex V5）·type='concept'·source 标注（MANIFESTO:14 / CLAUDE.md:§X） |
| P1-2 | 重建索引 | rag_index 加 `_load_concepts`·概念类入 RAG（225→~240） |
| P1-3 | 两级链路 | 明显概念 general→concept 模板直答（保留快速路径）+ 复杂/边界概念 → knowledge_qa（RAG 检索概念卡 + LLM 综合引用） |

## 三、P2 · 防回归机器化

| # | 改动 | 要点 |
|---|---|---|
| P2-1 | 触发正负例语义更新 | **正例集两类**（Codex V6）：① 加速器直通（「宜昌有哪些更新项目」→knowledge_qa）② 漏网落 diagnose（失败句→null·各测各的层）·负例含分析问（「哪些片区情绪最差」→null） |
| P2-2 | diagnose 枚举断言 | intent 含 4 值 + **现有三值判据文本未删**（增量断言·防重构删类·glm 示例代码） |
| P2-3 | 知识问答 e2e | 模拟 diagnose 卡 intent=knowledge_qa → harness 路由 → 注入 → finalStep 链路断言（e2e-seam·不依赖真实 LLM） |

## 四、P3 · 数字出处（独立小瑕疵）

| # | 改动 | 要点 |
|---|---|---|
| P3-1 | URP-P01 做厚 | **双来源标注**（Codex V7）：55 个=2025-2027 项目库（来源 00-03）·43 完整社区=2030 目标（来源 00-02）·**55 含 43·非 55+43**（glm 措辞）·跨来源数字显式区分 |
| P3-2 | 指令出处锚定 | harness 注入指令：「每个数值标注来源条目（如 [URP-P01]）·**禁止自行拆分/合并/编造数字**」 |
| P3-3 | 素材语义断言 | 检索「43 个完整社区」命中 URP-P01（依赖 P3-1·顺序正确） |

## 五、P4 · 通道质量基线 + P5 · 产品边界

| # | 改动 | 要点 |
|---|---|---|
| P4-1 | 知识问答黄金集 | Recall + 回答完整性（增强后问答通道守住） |
| P4-2 | 分析 B3 回归 | 分析类确定性未随「增强」放开·防回归演示逻辑链 |
| P5-1 | 产品边界标注 | KNOWLEDGE 蒸馏「EMC=聚焦专业蒸馏工具（非追平官方）·闭源 harness/微调/后处理 API 拿不到·①模型 tier ②限制型中介 ③上下文可改善·④闭源不可改」 |

## 六、P0-6 暂缓（用户拍板·保持 flash）

- 暂不开 pro·保持 flash（基本路径没跑顺·不是开 pro 问题）
- **glm 风险标注**：flash 综合能力弱一档·知识问答质量上限受限——**路径跑顺后尽快复审 P0-6（CB-12 分通道·勿忘）**

## 七、验证（实施后）

1. **eval 复采**（P0-3 gate）：knowledge_qa 识别率 + 现有三值零回归 + 模板命中 gate 0.6
2. node 触发回归（正例两类 + 负例）
3. `py -m pytest tests/ -q` 全量零回归 + validate 轨 + 新断言
4. 重建索引（概念类 + URP-P01）
5. **用户复测 5 验收点**（glm 提供）：
   ① 「宜昌市城市更新的项目有哪些？」→ 落 diagnose → LLM 判 knowledge_qa → RAG → 带来源不图层
   ② 「什么是更新单元」→ general concept 直答（负例不误判）
   ③ 「宜昌情绪分布」→ emotion_analysis 分析（分析类不被误抢）
   ④ 数字准确（55 含 43）+ 逐数字标来源
   ⑤ 回答综合 ≥3 条素材（全面性）

## 八、红线核对（最终）

| 红线 | 结论 |
|---|---|
| diagnose 永不动 | ⚠️ 用户豁免（2026-08-09）·P0-1 显式记录 + P0-3 eval gate·不通过回滚 |
| FINAL_TEMPLATE / D019 | 不动（分通道增强 = ctx.context 条件注入·非改模板） |
| 四态出口 | knowledge_qa 走 EXIT_CONCEPT（LLM 综合·非新态）·R3 兜底保留 |
| 分析类确定性 | 完全不动（图层主出口/四态/演示逻辑链防回归）·增强仅问答/概念通道 |
| 三支柱 | ① 概念库补数据 ② 意图归位强化架构 ③ LLM 综合不变 |

---

*claude组（2026-08-09）·执行定稿（两组执行前验证通过·8 焦点全 agree·全部细化并入）·开始实施 P0。*
