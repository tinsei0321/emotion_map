# PT-CB6 期 · Codex 审计任务书（zcode 主手签发·2026-08-20）

> 审计方：Codex（审计协助·零实施·只读）。签发：zcode（主手）。
> 对象：**2026-08-19 晚 ~ 08-20 凌晨 home 班全部工作**（commit 7a04aaf5..0278f083·共 34 提交·48 文件 +3817 行）。
> 产出：`docs/catch-ball/discuss/PT-CB6-审计_Codex-2026-08-20.md`（本任务书末尾附直接可用 prompt）。

---

## 一、审计范围全景（三线 + 两项挂账）

| 线 | 内容 | 已有审计 | 本次要求 |
|---|---|---|---|
| **PT-CB6 外置大脑实测**（主体·最大头） | P 先行件三砖（render_spec F_028 / 投递通道 F_029+F_030 / 前端 loader）+ P+ 增补（scheme 词表/徽标/demo 脚本）+ R41 浏览器实测 + 大脑实测（Q1-Q4）+ S6 用户复测与三坑修复 + S7 判读 | **未审** | **全量审计**（重点清单见 §三） |
| **PT-CB5 插座批**（补审） | G8b 动态开卷 + G10 七件插座 + T4 插座考试 + T5 顺修 + F1 + zcode 执审回口（G-2 穿透洞修复） | zcode 执审通过（**利益冲突声明在档·补审位保留**） | Codex 独立补审（重点 §四） |
| **PT-CB4 治理批**（已审抽验） | T1 六组对账裁决 / T2 契约卡 12 张 / T3 check_caliber | Codex 零打回评优（8c04b217） | 不重复全审；仅**抽验复核**（§五） |
| **B1 复审挂账**（并入） | 08-19 office 班七项修复（H1 随层标记/H2 hotspot 补挂/M1 全入口过滤/L1-L4） | 初审 FAIL 后修复·复审未做 | **本次一并复审**（§六·判定依据=已被三轮全量门禁持续回归覆盖） |

## 二、审计基准（先校准再开审）

1. **分支**：`EMC_harness_dsh`（main 冻结勿动·只读）。
2. **测试基线**：`py -m pytest tests/ -q` = **427 passed + 2 skipped**（zcode 08-20 上午实测）。审计全程以此为准，任何实测偏差须在报告注明。
3. **追踪 ID 连续区**：本次新增 F_020（check_caliber·MOD_AIQA）/ F_021-F_027（MCP 七工具）/ F_028（render_spec）/ F_029-F_030（投递通道）——**逐号核对注册表连续不跳号、模块归属正确、描述与实现一致**（含 D_ 系列）。
4. **红线铁律逐项**（审计清单必过）：usage 守卫全员生效（含 MCP 工具入口·G-2 穿透洞教训）；工具输出脱敏+带口径+可追溯；单一权威源（**不写第二份 schema**——MCP 侧 inputSchema 必须派生自 tool_contracts）；安全打印 `_safe_print`；ASCII 标记；禁宽 except 静默（A9 教训）；副作用必带补偿（A6）。
5. **R7 纪律**：凡实测服务行为，先 `tools/check_server_freshness.py` 核对进程早于最新提交（载旧码风险），再下结论。

## 三、PT-CB6 主体审计要点（逐条覆盖）

### 3.1 P 先行件三砖（代码审计核心）

- **render_spec 第 8 插座（F_028）**：契约与 `tool_contracts.py` 单一权威源关系（是否派生）；`usage=analysis_output` 层**显示放行**（不受 G-2 拒绝）的设计边界——显示≠分析输入的语义正确性；渲染契约 v1 字段（scheme/data_nature/community/producer）与 P+ 升级的一致性。
- **投递通道（F_029/F_030）**：watcher+SSE+dataset 端点——路径白名单（禁越界读）、SSE 实现（对照 R11 三坑：线程模型/重放去重/inbox 清理责任）、dataset 端点取数性能（内联 vs 引用的排队问题）。
- **前端 loader（render_client.js）**：令牌解析权威在 JS 的裁决依据；`_seenSpecIds` 去重实现；S6 三修复（ThreadingTCPServer/去重/inbox 清理）代码质量复核。
- **P+ 增补**：scheme 受管词表（sequential 计数着色修正·piToNorm 极性专用缺口——**与 CB-41 双 bug 教训的对照**：着色语义是否复用了 computeStyle 同源路径而非另起一套）；data_nature 徽标三处透传（sim|real）；demo_pioneer 脚本；community 口径字段（174/154/118 防混图）。
- **测试**：`tests/test_render_channel.py` 215 行——覆盖面/断言强度/是否验证与实现同构（R8 纪律）。

### 3.2 大脑实测链（文档与真实行为审计）

- 任务书（五层递进模型/Q1-Q4 考卷）与实测记录的一致性：L0-L4 判定依据是否充分；Q2 主验收（端到端成图）证据链完整；Q4 对抗样本缺位（大脑未按题面撞 G-2）——S6 建议"题面明写 base_174_aggregate_area"是否仍有效待办。
- D1-D5 缺陷清单定级合理性（D1 P1 排序支持/D2 layer_output 引导/D3 rag_query 差异化/D4 stdout→stderr 已裁决/D5 已销）+ D6-D8（S6 三坑·已修销号）验收结论复核。
- 大脑答卷质量评价（R43 判读）是否过度乐观——请独立评估 Q1-Q4 答卷中的口径引用/数据自洽性。

### 3.3 S6 用户复测与 S7 判读

- 三坑修复的**根因-修复-验证**闭环（对比 R11 已沉淀纪律）；`DATA/exports/render_inbox/_backup/` 15 个旧 spec 处置（未删只移——是否应删·给出建议）；`tests/_tmp_*` 临时文件残留（工作区卫生）。
- S7 初判读（R43）+ 增量判读（R44·zcode office 班）的结论链。

## 四、PT-CB5 补审要点

- G-2 穿透洞（三工具 usage 守卫补挂）修复质量——**独立复测**：MCP 工具入口传 analysis_output 层应拒；回归 416+3 基线（PT-CB5 当时）与现基线 427+2 的演进一致性。
- zcode 执审的利益冲突点是否全部如实披露（红线零接触/纯读/引用全真/钳制实证四项声明逐条核）。
- A9 教训（守卫禁宽 except 静默）是否已固化为纪律条款且无复发（审计 PT-CB6 代码时同查）。
- 七工具规格与 tool_contracts 真身签名一致性（backing 五锚点）。

## 五、PT-CB4 抽验复核（轻量）

- T1 六组裁决抽 2 组复核（四证据法证据是否可复现·裁决与证据是否一致）；qty 双头破案结论（全覆盖 11 件=权威源/presets 8 类过时）——**presets 同步批挂起待用户**的状态是否仍在挂账清单。
- T2 契约卡 12 张抽 3 张（元契约七字段齐全/值锚点指成文出处不内联数据）。
- check_caliber 6 命中基线（已知合规基线）判定记录。

## 六、B1 复审要点（并入本次）

- 七项修复（H1/H2/M1/L1-L4）逐一复核代码现状（对照 `PT-CB2-T2执行记录 §六` 与复审 prompt）；重点：H1 的 `l.presetUsage` 随层标记在 PT-CB6 前端改动后是否仍生效；M1 全入口过滤在 MCP 工具面新增后是否被绕过。
- 判定口径：若通过 → B1 批正式销号；若仍发现缺口 → 列明并给修复方向。

## 七、产出要求

`docs/catch-ball/discuss/PT-CB6-审计_Codex-2026-08-20.md`，结构：

1. 总裁定（PT-CB6：通过/需修复；PT-CB5 补审；PT-CB4 抽验；B1 复审——四线各自结论）；
2. 审计要点逐条结论（§三/四/五/六 编号对应·pass/fail/notes）；
3. 缺陷清单（若有新增：编号从 D9 起·含严重级/位置/修复建议）；
4. 红线铁律逐项核对表（§二.4）；
5. 基线复核实录（pytest 输出 + check_server_freshness 结果）；
6. 证据引用（文件:行号）。

纪律：零实施零修改；产出仅本报告一份；编号一律 PT；commit/push 由用户决定。

---

## 八、审计 prompt（直接转发 Codex）

```text
【审计任务 · PT-CB6 期 home 班全量审计】（zcode 主手签发·2026-08-20）

你是 Codex 审计组（零实施·只读）。审计对象：2026-08-19 晚~08-20 凌晨 home 班全部工作
（commit 7a04aaf5..0278f083·34 提交·48 文件）。

1. 环境：git checkout EMC_harness_dsh && git pull origin EMC_harness_dsh（main 冻结勿动）。
2. 先读任务书（唯一审计依据·基准/要点/格式全在）：
   docs/catch-ball/discuss/PT-CB6-审计任务书_zcode-2026-08-20.md
3. 四条线：
   - PT-CB6 主体全审：P 先行件三砖+F+增补代码（render_spec F_028/投递通道 F_029-F_030/前端 loader/
     scheme 词表/徽标/demo）+ 大脑实测链文档一致性 + S6 三坑修复闭环 + S7 判读结论链；
   - PT-CB5 补审：G-2 穿透洞独立复测（MCP 入口传 conclusion 层应拒）+ 利益冲突四项声明逐条核 +
     A9 教训复发检查 + 七工具真身签名一致性；
   - PT-CB4 抽验：T1 裁决抽 2 组复现 + T2 契约卡抽 3 张 + presets 同步批挂账状态；
   - B1 复审：七项修复逐一复核（H1 随层标记在 CB6 前端改动后仍生效？M1 在 MCP 面新增后未被绕过？）→ 销号或列缺口。
4. 红线铁律逐项核对：usage 守卫全员生效 / 脱敏带口径 / 单一权威源（MCP inputSchema 必须派生）/
   安全打印 / ASCII / 禁宽 except 静默 / 副作用必带补偿。
5. 追踪 ID F_020~F_030 逐号核对：连续不跳号/模块归属/描述与实现一致。
6. 基线：py -m pytest tests/ -q 应 427 passed + 2 skipped；实测服务前先跑
   tools/check_server_freshness.py 核对进程新旧（R7）。
7. 产出落盘：docs/catch-ball/discuss/PT-CB6-审计_Codex-2026-08-20.md
   （四线各自结论+要点逐条 pass/fail+新缺陷 D9 起编号+红线核对表+基线实录+文件:行号证据）。

纪律：零实施零修改；产出仅本报告一份；commit/push 由用户决定。
```
