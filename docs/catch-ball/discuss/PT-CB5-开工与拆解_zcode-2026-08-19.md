# PT-CB5 · 插座批 · 开工与拆解（zcode·2026-08-19 深夜·R26 架子先行裁定后首轮）

> 主手：zcode。定位：**平台架子核心批**——动态开卷定参（G8b）+ 七件标准插座（G10）+ 插座考试。C 线知识入库已后移为架子后充实批（R26）。
> 分支：`EMC_harness_dsh`（main 冻结）。commit 前缀 `PT-CB5(T{n}):`。门禁基线 **396+3**（上浮须注明）。

## 任务表

| # | 任务 | 执行 | 白名单 | 验收（含 R22 契约产出 DoD） |
|---|---|---|---|---|
| T1 | 开工拆解+落账 | zcode | 本档+三日志 | 本档落盘 |
| **T2** | **G8b 动态开卷定参**：`tool_contracts` 增 `derive_dynamic_enums()`（源=geo_registry 点层 id + presets manifest `usage=input` 项）→ `contracts_to_tools_schema(live_enums=True)` 给 source 型参数注入实时 enum+清单描述；源不可用静态兜底 | **zcode（攻坚）** | `ai_qa/tool_contracts.py`、`tests/test_dynamic_enums.py`（新） | enum 非空含已知 id；**analysis_output 零漏入（铁律7 断言）**；fallback 不崩；396+3+新增。**契约产出=开卷参数契约（param→数据类枚举映射+G-2 过滤规则机制化）** |
| **T3** | **G10 七件插座**：MCP server（rag_query/kb_facts/list_data/outlet_card/zonal_stats/buffer/rank）+三张说明标签（caliber 吃 T2 K 卡/list_data 开卷·样例过 C1 脱敏/rag synthesize 复用 finalStep 资产）+体积纪律（top_n/limit/摘要+引用）+`mcp` 依赖登记 | **zcode（攻坚·依赖 T2）** | `tools/mcp_server_emc.py`（新）、`requirements.txt`、测试 | 每工具过五判据一行答辩；outputSchema 最小公共词汇；stdio 启动实测。**契约产出=七工具对外 API 契约（含 caliber 对象 schema）** |
| T4 | 插座考试：真实任务 bash 考古 vs 插座对比·及格线五条·两组数字入档 | zcode | 考试报告 md | 及格线五条；摩擦差数字=「正道好走」首份基线证据 |
| T5 | 三观测顺修包：①`test_manifest_gate.py` GBK 编码自适配 ②注册表 K-C1/X-01 补显式值锚点字段 ③check_caliber v2 千分位/全角变体 | **dsh（派发单随本轮）** | `tests/test_manifest_gate.py`、`_口径注册表.md`、`tools/check_caliber.py`+其测试 | ①Windows 默认编码下全绿 ②两卡字段齐 ③变体用例过；396+3 或注明上浮 |
| 审计 | T2-T4 完工后送审（Codex+claude 如回归） | 审计组 | — | `PT-CB5-审计_*.md` |

**时序**：T2→T3→T4 串行（依赖链）；T5 并行。**红线**：diagnose prompt 文本零接触（schema enum 注入=拍板清单第 4 项明示批准的开卷机制·非 prompt 改动）；铁律7 枚举过滤硬断言。
