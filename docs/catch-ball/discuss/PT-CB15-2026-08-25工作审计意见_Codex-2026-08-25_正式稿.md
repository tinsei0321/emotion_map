# PT-CB15 · 2026-08-25 工作审计意见（Codex 正式稿）

> 审计对象：`docs/catch-ball/discuss/PT-CB15-2026-08-25工作详细报告_claude.md` 及两份附录。
> 审计方式：独立会话，逐行核对代码/配置/数据/文档；不可复现项显式标注【未核验】。
> 本稿为 CB 组员 Codex 正式意见，取代同日 cdh 链路产物中的非正式草稿。

## 审计结论：有条件通过

三块工作方向正确、核心链路已闭环，且我实测门禁 **597 passed / 2 skipped / 0 failed**，数据治理字段事实、PT-CB14 四件代码落地、office 环境均与报告一致。但存在 **1 个高优先风险（仓外人设引导未入复刻清单）、1 个模型别名遗留风险（deepseek-chat 已官方弃用且对比实验未隔离变量）、以及若干未闭环交付项（未提交/未推送、T1-T7 未收尾、webSearch 未定档、计数口径漂移）**。满足以下条件后可通过。

---

## 一、完整性（证据 + 判断 + 建议）

**已核验成立的部分**

- PT-CB14 四件代码落地均已在文件中找到：`core/geo_registry.py` 11 个 qty 层、`tools/mcp_server_emc.py` presets `available` 过滤与 `data_nature='test'`、`frontend/js/ai_qa/panel.js` `_initEngineBadge()`、`frontend/js/ai_qa/brain-adapter-dsh.js` `[dsh引擎]` 前缀、`frontend/js/render_client.js:113` `[测试]` 徽标、`docs/render-contract.md` §五 test 值 + 用毕即删纪律。
- 门禁实测通过：`python -m pytest tests/ -q` 实得 **597 passed, 2 skipped**（68.16s）；`tests/test_ptcb14_claude_batch.py` 单独跑 **7 passed**。
- 数据治理零改动：`git status` 无 DATA 改动；实测 `体检对象_小区_面.geojson` XQMC unique=1562、SSSQ unique=187，`体检对象_住宅_点.geojson` SSSQ unique=188，与报告一致。
- office 环境：`codex --version` = **codex-cli 0.149.1**，`_resolve_codex_exe()` 实跑命中 `AppData\Roaming\npm\node_modules\@openai\codex-*\vendor\...\codex.exe`（非 WindowsApps 商店版）。

**遗漏/未闭环项（需要补齐）**

1. **主手代提交尚未完成**：当前工作树仍有 `M core/codex_bridge.py`、`M docs/progress.md`，以及本批 discuss 文档未提交。按「未 push 不算交付」纪律，报告所述“主手回收：git 提交”目前只是待办，不是完成态。
2. **`_codex_cwd/AGENTS.md` 未进入复刻清单**：执行记录写的是“登记换机复制清单（docs/codex-harness-ops.md 建议同步）”，但我核读 `docs/codex-harness-ops.md` 与 `OFFICE.md` 均未登记该文件，换机后 T8 内容链会再次断。
3. **T1-T7 用户浏览器实测未收尾**：T8 已首测，但 T1-T7 只有“用户浏览器侧”一句话，无逐项验收回执。
4. **T8 复跑步骤未落盘**：955 delta/68s 是一次性结果，缺少可复跑的问题文本与观测方法。
5. **计数口径漂移**：报告 C2 写“presets 63→60”，但当前 `list_data()` 实得 **23 点层 + 61 presets = 84 total**；差 1 个 preset，疑似后续 `render_file` 又登记了临时项。建议报告补计数口径或把 60/61 对齐。
6. **webSearch ok=false 未在运维文档定档**：是“人设已禁用的设计行为”还是“环境限制”，报告只写在执行记录，未进 `docs/codex-harness-ops.md`。

**建议**：先提交并推送当前改动；把 `_codex_cwd/AGENTS.md` 与 8600 握手、T1-T7、T8 复跑并入 `docs/codex-harness-ops.md`；给 T1-T8 各附验收回执。

---

## 二、正确性（逐项核对）

### 2.1 8600 强依赖——判断成立（机制一致）

- `C:\Users\Administrator\.codex\config.toml` 有 `[mcp_servers.emc]`，且 `required = true`。
- `docs/codex-harness-ops.md` §一.2 与 PT-CB15 spike 执行记录 K-7/P7 均记载“required=true 只挂 emc，8600 未起快速失败”。
- 结论：报告“缺 8600 → app-server thread/start 失败”与配置事实一致；但具体失败点是 `initialize` 还是 `thread/start` 属于 Codex CLI 内部行为，桥代码本身没有显式检查，我未再起一个 app-server 验证【未核验】。建议在运维文档补一条诊断命令（`netstat -ano | findstr ":8600.*LISTENING"`）。

### 2.2 `_codex_cwd/AGENTS.md` 引导缺环——判断成立（机制一致）

- 桥代码 `core/codex_bridge.py` 以 `_CODEX_CWD = REPO.parent/_codex_cwd` 作为 app-server `cwd`，且 `_codex_cwd/AGENTS.md` 确实存在，内容含“先 list_data、唯一工具面=emc MCP、禁 webSearch、必出图”。
- 报告“无引导不调工具、有引导全链通”与机制一致；但该行为依赖 Codex app-server 自动加载 cwd 的 AGENTS.md，我未再启动一次 app-server 独立复现【未核验】。

### 2.3 deepseek-chat = v4-flash 兼容别名——官方依据存在，但结论有重要风险

- **官方依据成立一半**：DeepSeek 官方文档/变更日志确认 `deepseek-chat` 映射到 `deepseek-v4-flash` 的非思考模式，`deepseek-reasoner` 映射思考模式；报告的价格口径（flash 高峰 3/9、pro 高峰 9/27，闲时减半）与官方定价一致。
- **但官方同时声明这两个旧名将于 2026-07-24 弃用/退役**。今天已是 2026-08-25，继续把桥默认模型设为 `deepseek-chat` 是在依赖一个官方已声明退役的兼容别名，属于潜伏断链风险。
- **对比实验没有隔离变量**：尝试①是 `deepseek-v4-flash`（全名）+ 无引导；尝试②③是 `deepseek-chat` + 无/有引导。缺少 `deepseek-v4-flash` + 有引导 的对照。因此“全名断链、别名通、所以保持别名”不能由现有证据严格推出；更可信的根因是引导缺环，而非别名本身。
- 当前未提交的 `core/codex_bridge.py` 注释仍写“Flash 档=deepseek-chat 语义待用户终裁”，与报告 §4.4“已定稿”不一致。

**判断**：alias 映射本身是真的，但“保持 deepseek-chat 为默认”不是被充分验证的正确结论，且踩在官方弃用时间点之后。建议：以有引导状态重测 `deepseek-v4-flash`；若通过则把默认模型改为规范名 `deepseek-v4-flash`，否则显式记录“退役别名仍可用”的依据、截止风险与回退预案，并同步 `docs/codex-harness-ops.md`。

### 2.4 桥默认回滚 deepseek-chat——短期合理，长期不充分

- 在当前实测可跑通、且用户令“cdh 恒 flash”的前提下，回滚到 flash 档语义方向是对的。
- 但如 2.3 所述，回滚对象应优先是规范名 `deepseek-v4-flash`，而不是退役别名 `deepseek-chat`；当前“回滚合理”只能算**暂时可用**，不能算**定稿**。

---

## 三、风险

### 3.1 `_codex_cwd/AGENTS.md` 仓外遗漏——高风险

- 文件位于 `D:\Github\_codex_cwd\AGENTS.md`，在仓库之外；clone/换机不会携带，而它正是 T8 内容链能否调用 emc 工具的开关。
- 建议：把 EMC 人设的**权威模板入仓**（如 `docs/emc-codex-persona.md`），并在 `docs/codex-harness-ops.md` 增加“到岗/换机：复制/生成 `{REPO}/../_codex_cwd/AGENTS.md`”步骤；`_codex_cwd` 只作运行时产物。

### 3.2 webSearch ok=false——中低风险，但必须定档

- `core/codex_bridge.py` 对 `webSearch` 工具完成事件以 `item.status == 'completed'` 判 `ok`，逻辑本身没问题；实际 `ok=false` 是否普遍、是否因无 search provider、是否被沙箱限制，我未复现【未核验】。
- 因 `_codex_cwd/AGENTS.md` 已禁止 webSearch，它更像“设计上不应使用”的行为；但模型仍可能发起，产生失败噪声。建议在运维文档写明“webSearch 非本次工具面，禁止使用；若出现 ok=false 属预期噪声”，避免 T1-T7 时误判故障。

### 3.3 `~/.codex/config.toml` API key 明文——高风险，且备份放大暴露面

- `C:\Users\Administrator\.codex\config.toml` 含 deepseek、ZAI、web-reader、web-search-prime、zai 多组明文 key/token。
- 同目录 7 个 `config.toml.bak-*` 均含明文 key，等于同一份密钥扩散了 7 份副本。
- 仓库内 `.codex/config.toml` 无密钥（已核验），未入 git；风险在本地文件与备份。
- 建议：改环境变量注入/仅占位符，`config.toml` 与备份清理旧 key、必要时轮换；换机清单只带占位符不带真实 key；本机 ACL 收紧。

---

## 四、纪律

### 4.1 render_client.js:113 白名单外单行——有条件接受

- 核读该行：`const natureBadge = nature === 'demo' ? '[演示] ' : (nature === 'test' ? '[测试] ' : (nature === 'real' ? '[真实] ' : ''));`
- 单行、纯增量（只加 `[测试]` 分支）、配合 `docs/render-contract.md` §五“用毕即删”，且已在执行记录显式注明供复核。**接受**，条件：①该行加注释“CB15 临时·待折入受管徽标机制”；②不得再叠加第二条白名单外改动；③纳入后续 C4 徽标体系统一收敛。

### 4.2 数据治理“先错后纠 + 实证 + 零改动”——符合纪律

- 初始按 SSSQ dissolve 有据可依（用户原令括号写 SSSQ），但错误在“探查已见矛盾证据（SSSQ 组内 XQMC 多值、XQMC unique=1562=面数）却未先质疑再动数据”。
- 用户纠正后重新实证（XQMC 每面唯一、SSSQ=社区名）、删除错误产物、数据零改动、教训已入 `memories/auto/challenge-user-assumptions-with-evidence.md`，且 memory 已含“先实证合并键语义、任务无对象如实汇报不硬造”。
- 符合“用证据挑战用户假设”原则；建议把 memory 措辞再补一句“中文缩写字段语义歧义→先确认字段含义再执行批量修改”，确保未来同类缩写不再重复踩坑。

---

## 五、综合建议

### 5.1 T1-T7 收尾指引

- 对表任务书逐项验收：每项 = 证据落盘 + 门禁更新 + 可复跑方法；用户浏览器侧验收单独列，不得只写“T1-T7 用户侧”。
- 优先闭环顺序：①`_codex_cwd/AGENTS.md` 入复刻清单 → ②deepseek 默认模型名定稿并提交 → ③T8 复跑步骤 → ④webSearch 定档 → ⑤计数口径对齐。

### 5.2 换机复制清单（建议顺序）

1. EMC 人设权威模板（`docs/emc-codex-persona.md`）+ 生成 `{REPO}/../_codex_cwd/AGENTS.md` 的命令；
2. codex-cli 0.149.1（`npm i -g @openai/codex@0.149.1`）+ `_resolve_codex_exe()` 命中验证；
3. `~/.codex/config.toml`：仅占位符/env 注入 + `[mcp_servers.emc] required=true` + desktop models 列表 + 备份清理纪律；
4. 启动 8600 MCP + 8080 后端，并做握手验证；
5. RAG 索引重建命令；
6. 3 个缺失 preset 处置（`admin_community`、`renewal_unit` 需上传或从 manifest 移除；`tmp_render_1787246545` 属临时残留，应从 manifest 清理）；
7. 门禁基线 597+2 写入基线文件；
8. memory/教训条目确认可读。

### 5.3 其他发现

- **待提交/推送**：`core/codex_bridge.py`、`docs/progress.md` 及本批 discuss 文档需 commit+push，否则不满足“未 push 不算交付”。
- **deepseek 决策未真正定稿**：注释仍是“待用户终裁”，与报告“定稿”矛盾，需在提交前写死。
- **3 个缺失 preset 中有一个是临时残留**：`tmp_render_1787246545` 不应“待上传”，应清理 manifest 登记，避免把临时项当正式缺口。
- **报告数字复核**：当前 `list_data` 总数为 84（23+61），请补齐计数口径，避免后续基线核对歧义。
