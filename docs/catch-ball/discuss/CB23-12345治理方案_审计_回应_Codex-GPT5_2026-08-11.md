# CB-23 12345 数据治理方案 · 审计回应（Codex + GPT-5）

> **审计方**：Codex（GPT-5）| **日期**：2026-08-11 | **性质**：第三方独立审计（只读本地·不 git）| **对象**：`CB23-12345治理方案设计_2026-08-11.md` + `CB23-中转站层级评估_2026-08-11.md` + `CB23-12345热线提取_0819PDF_2026-08-11.md` + 原始 `2024年12345投诉数据_raw.xlsx`
> **实读**：原始 xlsx（57265 行·16 列·sheet「ioc正式库SQL」）· 诉求类型/区域/评价结果/主办部门分布统计 + EMC L2 `score` 语义核验（`emotion_analysis_v1.py`）
> **核心标尺**：真实数据红线 · EMC 枚举对齐（polarity/score 语义）· 不造轮子 · 诚实标注（地点推断/PII/样本偏差）· 口径可溯

---

## 〇、一句话结论

**方案方向正确（情绪 5 级对齐 L2 枚举不造期盼级 ✓·地点三级推断 ✓·两份 schema 基本完整 ✓·06 层级建议合理 ✓·与 zcode 互补 ✓），但有 3 条 P1 必改 + 3 条 P1 必补：P1-① `polarity_score 5→1` 与 EMC score 0~1 语义冲突（emotion_analysis_v1.py:114 score=0~1·`_score_to_polarity` 分档 0.2/0.4/0.6/0.8）——score 须按 EMC 分档归一否则 hotspot invert/density 权重全错位；P1-② 缺「大类/中类→domain/element 4×5 映射表」（4×5 归因链路断）；P1-③ 缺 `place_source/place_confidence` 字段（地点推断诚实标注·防误判不可追溯）；另 P1-④ 中心城区过滤口径未定（全域 9 县市区含夷陵/高新·0819 片区含小溪塔中心片）。实施前必补清单见文末。**

---

## 一、原始数据实读（证据基础）

| 维度 | 实测 | 对方案的启示 |
|---|---|---|
| 总行数 | 57265·16 列 | 与方案一致 ✓ |
| 时间列 | **已有 `事件上报时间` 完整时间戳**（如 2024-12-31 23:39:22） | 投诉时间直接用该列·**无需从办件编号前 8 位推导**（修正方案假设） |
| 诉求类型 | 投诉类 46870（81.8%）·咨询类 6856·建议类 2206·求助类 1275·表扬类 37·其它类 17·营商环境类 4 | **无「期盼类」**——期盼=用户概念·实际类型为「建议类」；表扬仅 0.06%→极性高度负偏是平台特性 |
| 区域 | 西陵 16908/伍家岗 14978/夷陵 8641/高新区 4580/当阳 3697/点军 2776/猇亭 2146/宜都 860/秭归 578…（**全域 9 县市区**）+ **100 条代码混存（420502）** | 区域清洗确需（代码→区名）·但全域/中心城区过滤口径必须先定（0819 片区含小溪塔中心片） |
| 主办部门 | 含街办/街办/镇 15643 行（**27.3%**·非方案所述 4205） | 三级推断线索源充足·但含县市镇级（夷陵黄花镇等）·街办级仅对中心城区 4 区有效 |
| 事发地 | 84.8% 空 | 地点主要靠内容词 + 主办部门·「78% 内容含地点词」需实测核验（方案未给抽样） |
| 评价结果 | 基本满意 30807 + 满意 25079 + 暂未评价 1111 + 不满意 268（**99.7% 满意系平台自评**） | 作情绪佐证价值有限·仅参考不参与极性 ✓（方案已对） |
| EMC score 语义 | `emotion_analysis_v1.py:114` score=0~1（0 极端负面·1 极端正面）·`_score_to_polarity` 分档 ≤0.20 Very Negative / ≤0.40 / ≤0.60 / ≤0.80 / >0.80 | **polarity_score 5→1 与 0~1 冲突·P1 修正** |

---

## 二、逐焦点评估

### 焦点 1 · 情绪 5 级映射：诉求类型→L2 五级 · 期盼语义用 emotion_aspect

**结论：partial（对齐枚举 ✓·映射规则基本合理·3 处修正）**

**一句话证据**：EMC L2 枚举 = `Very Negative/Negative/Neutral/Positive/Very Positive`（emotion_analysis_v1.py:165-166 映射 -2~2）·方案不造期盼级 ✓；但 `polarity_score 5→1` 与 `score` 0~1 语义冲突（:114）·且建议类 2206 条硬归 Positive 会拉高极性均值。

**定稿建议**：
1. **P1 · score 值域归一 0~1**：按 `_score_to_polarity` 分档赋值（Very Negative=0.1 / Negative=0.3 / Neutral=0.5 / Positive=0.7 / Very Positive=0.9·落在档内保证 score↔polarity 自洽）——`hotspot invert=True`（1-score）与 density 权重、zonal score_mean 全部对齐；保留 `polarity_score_5` 原始 5 级数值作参考列（如需要）。
2. **P2 · 建议类细分**：建议类 2206 条默认 Neutral，按标题/内容关键词升级 Positive（含「建议/期盼/希望改善」+正面语气）——避免投诉平台数据里 2206 条硬 Positive 扭曲 polarity_index；表扬类→Very Positive（37 条·占比 0.06%·不影响均值）✓。
3. **emotion_aspect 保留期盼语义 ✓**（`_followupCue`/`_keywordRank` 层消费·不撞 5 级）——补充：`emotion_aspect` 枚举建议 = `投诉/求助/咨询/建议/期盼/表扬`（期盼从建议类内容词细分出·供追问/观点卡）。
4. **样本偏差标注**：投诉 81.8% + 表扬 0.06% → 12345 极性 ≠ 市民整体情绪（平台自选择）·报告/出口卡标注（outlet `data_status` 诚实边界已先例）。

---

### 焦点 2 · 地点推断三级（内容地点词→街办→区域·细到小区/道路·坐标化·留空）

**结论：agree（方法对）+ 3 处补强**

**一句话证据**：内容地点词 + 主办部门街办（实测 27.3% 行·西陵区云集街办等）双线索成立·留空不强加 ✓；0819 PDF 页 33 已证片区级空间指向·原始数据可再升点位级（互补非冲突）。

**定稿建议**：
1. **P1 · 加 `place_source/place_confidence` 字段**：`place_source` = content（内容词）/ office（主办部门街办）/ region（区域兜底）/ empty；`place_confidence` = 高/中/低（内容词精确地址=高·街办级=中·区域兜底=低）——防高德 geocode 错位不可追溯·满足「地点推断错误的诚实标注」。
2. **P2 · 坐标化策略**：① 唯一地点词去重后 geocode（5.7 万行去重后 distinct 地点词估计 <5 千·防高德配额爆）② 主办部门街办→街办 preset 质心（待 A8 街办边界）③ 区域兜底→区质心（粗·标注）。**不做逐行 POI 级编码**（宏观诊断级）。
3. **P1 · PII 门牌降级**：中转版 `place_name` 禁门牌级（样本含「二马路55号居民」）——降级小区/道路/街办级·门牌仅治理清洗版（中转站内非 git）保留并标注。
4. **P2 · 78% 地点词覆盖率实测核验**：治理方法论落盘时给抽样（200 条人工核对）召回/精度——方案未给实测依据。

---

### 焦点 3 · 两份输出 schema

**结论：partial（基本完整·4 处修正）**

**一句话证据**：治理清洗版 8 条要求齐全·中转版字段含 polarity/score/intensity/domain/element/topic/place/lon/lat/region/source/emotion_aspect——消费链路字段齐；但 score 值域冲突（见 F1）·domain/element 无映射表·时间列假设可简化。

**定稿建议**：
1. **P1 · 投诉时间直接用 `事件上报时间` 列**（已有完整时间戳）·办件编号前 8 位仅作校验（20241231001755 ↔ 2024-12-31 一致 ✓）；`事件时间` 保留「从内容提取·未知=投诉时间」。
2. **P1 · 补「大类/中类→domain/element 4×5 映射表」**（12345 大类如噪声扰民/停车难/物业管理 → domain×element·含兜底：未映射→`urban_governance`+element=None 或 mapping 外标记）——否则 `_attach_4x5_attrs` 无输入·4×5 归因链路断；映射表落 `06_主观数据治理/12345_4x5映射.json`（与双轨索引同源可溯）。
3. **P2 · emotion_intensity 值域**：0~1 或 1~5 均可（`create_square_grid` KDE 已做 >1→/5 归一）——方案选 1~5 需与 score 区分语义（intensity=浓淡·score=正负）·文档注明。
4. **区域_清洗**：处理 100 条代码混存（420502→西陵区）+ 全域/中心城区口径列（`region_scope`：中心城区/县市——供 3' 两板块过滤）。

---

### 焦点 4 · EMC 消费链路：geo_registry 注册 checkup_12345_* → density/grid/zonal → 双轨对照

**结论：agree（复用不造轮子 ✓·score 修后链路通）+ 观察 2 条**

**一句话证据**：geo_registry 注册点层（checkup_12345_*）→ density/grid/zonal/hotspot 全复用现有函数（`create_square_grid`/`aggregate_by_polygons`/`hot_spot_analysis` 本轮已扩展 agg_cols/sum/sort_by）·双轨对照 = 新点位级数据升级 12 行双轨索引——零新端点。

**定稿建议**：
1. **P2 · 极性负偏的处理**：投诉 81.8% → polarity_index 全域负值·色带/热力全负面是数据特性——**观点/排序以强度（point_count·sum）为主·极性为辅**（与体检轨 A6 sum 口径一致）；报告标注「12345 平台样本偏差」。
2. **P2 · 双轨索引升级机会**：点位级数据可回算「类别×街办/片区」真实计数·补上轮缺口（环三峡大学无直接双轨行——大学路社区投诉可由点位级补）·3' 双轨对照数据源。
3. checkup 层 whitelist（G6）沿用·sim 层零引用。

---

### 焦点 5 · 中转站层级：06_主观数据治理

**结论：agree（04 职能过载实锤·06 建议合理·大改待 zcode 认可 ✓）**

**一句话证据**：04 现含 RAG 素材/双轨索引/**12345片区面/问卷片区指标/双轨对比表**——数据治理产物混入 EMC 桥接目录·职能过载成立；06_主观数据治理 归类清晰·05 片区统筹（问题-片区-项目结论）边界分明。

**定稿建议**：
1. **本次先建 06 放两份文件 ✓**（不动 04/05·等 zcode 拍板迁移）——增量安全。
2. **P2 · 迁移前引用同步**：04→06 迁移时须同步更新 `manifest.json`/`交付说明.md`/双轨索引引用路径（防断链）——迁移清单与 zcode 对齐时一并确认。
3. 建议 06 内子结构：`12345_治理清洗版.csv` + `12345_情绪地图中转版.csv` + `12345_治理方法论.md` + `12345_4x5映射.json`（F3 P1）。

---

### 焦点 6 · 与 zcode 已有产物不重复

**结论：agree（互补确认 ✓·且有升级机会）**

**一句话证据**：zcode = 0819 PDF 片区级映射（类别×片区·总量·无点位·页 33）·claude = 原始 5.7 万行点位级治理（地点推断·情绪 5 级·EMC 字段）——粒度不同·互补成立。

**定稿建议**：
1. **升级机会（建议写入分工）**：claude 点位级治理可回算「类别×片区」真实计数——解决 0819 页 33「34 处如何分配多片区需说明」的总量分配模糊（0819PDF 文档已标注此边界）·zcode 片区面 + claude 点位 = 更准片区映射（3' 数据源）。
2. 分工确认：zcode 管数据同步 + 片区映射（0819/片区面）·claude 管原始治理 + EMC 消费字段——不重复·产出互喂。

---

### 焦点 7 · 数据治理质量：5.7 万行清洗规则完备性

**结论：partial（规则方向对·4 处补强）**

**一句话证据**：时间（直接用事件上报时间列）/区域（代码混存 100 条需清洗）/标题简化（大类中类小类参考）/内容结构化（模板）规则齐；但中心城区过滤口径未定·78% 地点词未实测·地点推断缺 source/confidence·PII 门牌未明确降级。

**定稿建议**：
1. **P1 · 中心城区过滤口径**：与专项规划口径对齐（0819 片区含小溪塔中心片→夷陵区部分入中心城区？）——`region_scope` 列 + 过滤规则（西陵/伍家/点军/猇亭 + 夷陵小溪塔待定）·两板块报告用中心城区子集·方法论文档注明。
2. **P1 · 地点推断诚实标注**：`place_source/place_confidence`（见 F2）·高德 geocode 失败/低置信行标注 `place_confidence=low` 或留空·禁强填。
3. **P2 · PII 处置**：中转版去门牌/去原始诉求内容（仅结构化「时间+地点+事件+态度+诉求」·地点降级）·治理清洗版保留（中转站内非 git）·验收扫描加「中转版无门牌号」抽查。
4. **P2 · 满意度佐证边界**：99.7% 满意系平台自评·作参考列不参与极性（方案已对·方法论注明）。

---

## 三、总结论

**方案可实施·无方向性错误；实施前必须完成 4 条 P1 修正/补充**：
1. score 值域归一 0~1（按 EMC 分档·保 score↔polarity 自洽·hotspot/density 对齐）；
2. 大类/中类→domain/element 4×5 映射表（4×5 归因链路）；
3. place_source/place_confidence 字段（地点推断诚实标注）；
4. 中心城区过滤口径（含夷陵小溪塔判定·region_scope 列）。

**层级建议（06_主观数据治理）采纳·迁移待 zcode 拍板·与 zcode 片区映射互补成立·点位级可升级片区映射。**

---

## 四、实施前必补清单

| 级 | # | 必补 | 依据 |
|---|---|---|---|
| P1 | 1 | `polarity_score` → score 0~1（Very Negative=0.1…Very Positive=0.9·对齐 `_score_to_polarity` 分档） | emotion_analysis_v1.py:114/194-219·hotspot invert/density 权重 |
| P1 | 2 | 大类/中类→domain/element 4×5 映射表（含兜底·落 06/12345_4x5映射.json） | `_attach_4x5_attrs` 输入依赖 |
| P1 | 3 | `place_source/place_confidence` 字段（content/office/region/empty × 高/中/低） | 地点推断诚实标注·geocode 可追溯 |
| P1 | 4 | 中心城区过滤口径（西陵/伍家/点军/猇亭 + 夷陵小溪塔待定·`region_scope` 列） | 全域 9 县市区·0819 片区含小溪塔中心片 |
| P2 | 5 | 投诉时间直接用 `事件上报时间` 列（办件编号仅校验） | 原始列已含完整时间戳 |
| P2 | 6 | 78% 地点词覆盖率实测核验（抽样 200 条） | 方案未给实测依据 |
| P2 | 7 | 高德 geocode 唯一地点词去重（防 5.7 万次配额爆） | distinct 地点词估计 <5 千 |
| P2 | 8 | 建议类细分规则（默认 Neutral·正面语气升 Positive）·emotion_aspect 枚举定稿 | 建议类 2206 条·防极性均值扭曲 |
| P2 | 9 | PII 门牌降级（中转版 place_name 禁门牌）+ 验收抽查 | 样本含「二马路55号居民」 |
| P2 | 10 | 04→06 迁移引用同步（manifest/交付说明/双轨索引） | 防断链·与 zcode 对齐 |
| P2 | 11 | 12345 平台样本偏差标注（投诉 81.8%·表扬 0.06%·满意 99.7%）·极性负偏时观点以强度为主 | 平台自选择·报告口径 |

---

## 五、红线核对

| 红线 | 核对 | 状态 |
|---|---|---|
| EMC 枚举对齐 | 5 级枚举 ✓（不造期盼级）·score 0~1 修正后自洽 | ✅（P1-1 落地后） |
| 真实数据红线 | 原始 xlsx 治理·sim 禁入 | ✅ |
| 数据脱敏 | 中转版去门牌/去原始内容（P2-9） | ✅（落地后） |
| 口径可溯 | 全域/中心城区口径 + region_scope + source | ✅（P1-4 落地后） |
| 不造轮子 | 复用 density/grid/zonal/geo_registry/高德 geocode | ✅ |
| 诚实标注 | place_source/confidence·样本偏差·满意度平台自评 | ✅（P1-3 落地后） |

---

*Codex（GPT-5）· 第三方独立审计 · 2026-08-11 · 只读本地不 git · 回应落 discuss/ · claude 组回收后并入 12345 治理方案定稿 → 实施*
