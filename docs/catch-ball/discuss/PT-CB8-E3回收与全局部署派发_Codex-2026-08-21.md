# PT-CB8 · E3 回收与全局部署派发单（Codex → zcode）

> **用户令（2026-08-21）**：把 Codex 的避坑沉淀工作汇报给 zcode（主执行），由 zcode 做全局部署。
> **状态**：E3 Codex 侧已完成·**✅ zcode 已回收部署（2026-08-21·D1-D6 全执行）**——审计通过；E3 批 commit `8a82fc65` + 部署批 + report/ RAG 审计另立；T1-T6 裁决见 `PT-CB8-避坑沉淀报告_回应_zcode-2026-08-21.md`；家机补行裁定「补」（HOME 卡已加到岗补一件）。分支：`EMC_harness_dsh`（main 冻结）。

---

## 一、工作汇报（E3 产出·供 zcode 回收审计）

**主交付**：`discuss/PT-CB8-EMC-dsh避坑沉淀报告_Codex-2026-08-21.md`（学习报告·含全组回应义务 §七）。

| 产出 | 说明 |
|---|---|
| 坑盘点 | 08-18~21 EMC×dsh 期全部踩坑 → 三族 19 规则（R1-R19）全量对账 |
| 元模式 | 六条（服务端绿≠客户端绿 / 宿主隐式契约 / 静默失败 / 生命周期错配 / 投递幂等 / 症状迁移与语义层根治） |
| 业界对照 | Google SRE 无责复盘 / Amazon COE / 5 Whys / 清单革命 / 经验库+防腐 |
| 持续机制 | debug-memory 文末「维护协议」（蒸馏触发器·四件套门槛·编号纪律·载体分工·收口通知·除草） |
| 调优建议 | 报告 §六 T1-T6（任务书宿主契约自查段 / 验收=浏览器 DOM / 演示 runbook / 编号守护脚本 / 跨机复刻实录 / buglog INFRA 档） |

**改动文件（13·均已落盘未 commit）**：`docs/debug-memory.md`（撞号修正 R13-R16 顺号 + 新增 R17-R19 + 维护协议）、主报告（新）、项目 `AGENTS.md`、`docs/context-map.md`、`_cb-index.md`、EMC-dsh 台账（R50+指令行）、PT-CB8 开工档（E3 行）、todo.md、HOME.md / session-handoff.md / Qoder 审计档 / PT-CB6 执行记录（指针销号与重编号同步）。

**仓库外改动（1·不在 git）**：本机（office）用户级全局 `C:\Users\Administrator\.codex\AGENTS.md` 新增「Harness 工作方式」第 6 条「调试资产化」。

**自检已做**：① debug-memory 规则号 R1-R19 连续唯一（脚本数过 19 条）；② 旧编号引用五处（HOME/Qoder 审计/todo/session-handoff/PT-CB6 记录）已按新号同步；③ 全仓「撞号待合并」残留待办清零。

---

## 二、请 zcode 执行的全局部署清单

| # | 动作 | 要点 |
|---|---|---|
| D1 | **回收审计** | 过 13 文件 diff；重点核 debug-memory 重编号未破坏语义引用（已自查五处指针，建议抽验） |
| D2 | **commit + push** | `EMC_harness_dsh`；顺带处置 `docs/catch-ball/report/` 未跟踪项（RAG 审计报告·今日他组产物——属本批则一并收，否则单独 commit，zcode 判断） |
| D3 | **通知闭环** | cb-index 已登记「全组当轮回应待收」；zcode 部署后在下次各组开工读入口时自动触发回应义务（吸收/并入排期/暂缓/反对·无回应不销号） |
| D4 | **T1-T6 裁决** | zcode 以主手身份逐条四档裁决（或列提用户拍板项）；T4 编号守护脚本（~30 行·学 buglog `_gen_index.py` 先例）建议顺手排下批 |
| D5 | **跨机同步** | 家机 `git pull` 后项目级规范（AGENTS/context-map/debug-memory）自动生效；**用户级全局 AGENTS.md 家机需手动补同一行**（补丁文本见附录 A）——或裁定仅项目级生效 |
| D6 | **收口记账** | 部署完成后：台账 R51 标状态 + todo.md E3 段补「已部署」+ 本派发单标已回收 |

---

## 三、给 zcode 的接手 prompt（用户直接转发即可）

```text
zcode：用户令——回收 Codex 的 E3（EMC×dsh 避坑沉淀 + debug-memory 资产化）并做全局部署。
先读三件：① docs/catch-ball/discuss/PT-CB8-E3回收与全局部署派发_Codex-2026-08-21.md（本派发单·含部署清单 D1-D6）
② docs/catch-ball/discuss/PT-CB8-EMC-dsh避坑沉淀报告_Codex-2026-08-21.md（主报告·T1-T6 调优建议在 §六）
③ docs/debug-memory.md（重点看文末「维护协议」与 R13-R19）。
动作：按派发单 §二 D1-D6 执行——回收审计 13 文件 → commit+push（EMC_harness_dsh·注意 report/ 未跟踪项处置）
→ 确认全组回应义务生效 → 对 T1-T6 出四档裁决（需用户拍板项列出）→ 家机全局 AGENTS 补行（附录 A·或裁定仅项目级）
→ 台账 R51+todo 收口。白名单：本派发单 §一 所列 13 文件 + report/ 处置 + 记账三处。零代码改动（T4 只排期不实施）。
```

---

## 附录 A · 家机用户级全局 AGENTS.md 补丁文本

> 位置：`C:\Users\<用户>\.codex\AGENTS.md` → 「## Harness 工作方式（上下文连贯纪律 · 跨项目）」列表末尾（第 5 条后）追加：

```text
6. **调试资产化（debug distillation · 2026-08-21 用户令）**：修 ≥2 轮才好 / 修好又出新症状 / 同族 bug 复发 / 宿主与工具链隐式契约踩坑 → **当轮**复盘蒸馏为编号规则（规则/案例/违反后果/检查动作四件套）进项目 debug-memory 类载体；新会话排查前先读该库，交付前对照自查。debug log 只记不蒸馏 = 腐烂的流水账；蒸馏成可执行规则才是资产（落点样板：emotion_map `docs/debug-memory.md`·含维护协议）。
```

> 注：office 机已写入（Codex·08-21）；家机是否补行由 zcode 按 D5 裁定。
