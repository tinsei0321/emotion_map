# PT-CB15 · 2026-08-25 工作审计意见（Qoder 独立审计）

> 审计对象：`PT-CB15-2026-08-25工作详细报告_claude.md` + 附录 A（PT-CB14 修复批执行记录）+ 附录 B（office 到岗 T8 首测执行记录）。
> 审计方式：独立会话，不预设 Codex 审计结论；逐项实读代码/配置/数据/文档 + 本机只读复跑（pytest 全量与定向、MCP 工具活体调用、桥解析函数实跑、GeoJSON 字段统计、git diff）；官方依据经网络检索核对（DeepSeek 官方文档/变更日志/定价页）。不可复现项显式标注【未核验】。
> 审计环境：office 机（Windows·分支 `EMC_Codex_Harness`·HEAD=2742f733·8600/8080/8000 三服务在听）。

---

## 白话摘要

这次审计就像装修完请第三方验房师来验三间房：

- **第一间「工具箱补件」**（4 个小修复）：逐一验货，**全部合格**。4 个修复都在、配套的 17 道质检（7 道新增+全量 597 道）全部通过，我还当场按验收口径重跑了一遍示范操作，结果和报告一字不差。
- **第二间「数据整理」**：任务本来是「把同名小区合并」，工人一开始没多想就动手了，被业主叫停；重新查账后发现**全市 1562 个小区的名字本来就个个不重样**——根本没有可合并的对象。工人当场认错、把做坏的东西全删了、原始数据一个字节没动。这个「认错+查证+不动数据」的收尾是**对的**。
- **第三间「新机器开机」**：机器装好、测试跑通。但有一个大隐患：**让 AI 助手「认识自家工具」的那张说明书放在仓库外面**——换台电脑就丢，丢了 AI 就又不会用工具了（这次开机第一次失败就是这个原因）。另外 AI 用的「模型名字」是个**官方已经宣布停用的旧名字**，现在还能用属于宽限，随时可能真断。

与前一位验房师（Codex）大方向一致，但他有 **3 处小错**（备份数多算了 1 个、建议补的命令文档里本来就有、一个新增登记项的时间归因错了）和 **4 处没看出来**的问题（两份文档实验记录对不上、交接卡预告的测试基数和实测对不上、旧名字和新名字的「思考模式」默认档不同——这点很关键、任务书自身有个自相矛盾）。

**结论：有条件通过**——房子能住，但要先办 5 件事（见文末条件清单）。

---

## 审计结论：有条件通过

三块工作的核心事实链**全部实证成立**：PT-CB14 四件代码逐处落地且验收口径活体复现一致；数据治理的每个数字（1562/1562/187/188/22/26426/40）全部复核吻合、数据零改动属实；office 环境重建与 T8 首测的环境证据（codex 0.149.1、桥解析命中 npm vendor、required=true、人设文件在位、门禁 597+2）全部实读实跑验证。**但**存在 1 个高优先交付缺口（仓外人设引导未进任何复刻面）、1 个模型名决策未真正定稿（依赖官方已宣布退役的别名，且「保持别名」的论证存在变量未隔离的方法学缺陷）、以及若干未闭环项（未提交未推送/T1-T7 未收尾/webSearch 未定档/两处计数口径漂移未对账）。满足文末条件后可通过。

---

## 一、完整性

### 1.1 已核验闭环（证据在案）

| 项 | 核验证据 |
|---|---|
| C1 qty 点层 11 层注册 | `core/geo_registry.py:53-63` 逐层在案（id/子目录 `DATA/boundaries/presets`/level=CHECKUP 与验收口径一致）；活体复现 `zonal_stats(qty_民生_停车设施, checkup_cfg_community)` → 5 rows·首行营盘路社区 point_count=23 ✓ |
| C2 清单过滤+文案 | `tools/mcp_server_emc.py:413-417`（presets available 过滤）+ `core/geo_registry.py:209-222`（「文件未落盘·需上传：{file}（manifest 已登记）」文案）；manifest 实测 64 项/61 可用/缺文件 3 项（admin_community/renewal_unit/tmp_render_1787246545）→ admin_community 不进清单 ✓ |
| C3 test 标记+清理纪律 | `mcp_server_emc.py:1471-1482`（data_nature 增 'test'·dataset 路径不被 meta 覆盖）；`docs/render-contract.md` §五 含 `real / demo / test` + 用毕即删 ✓；`render_client.js:113` 徽标分支 ✓ |
| C4 引擎徽标+前缀 | `panel.js:2219-2240` `_initEngineBadge()`（现 4 态 light/dsh/codex/mock——codex 档为 PT-CB15 spike 后续扩展，属正常演进）；`panel.js:1642` + `brain-adapter-dsh.js:89` `[dsh引擎]` 前缀 ✓ |
| 门禁 | `tests/test_ptcb14_claude_batch.py` 7 用例单跑 0.37s 全过；全量 **597 passed + 2 skipped**（本机实测 94.23s·skip 为 test_pii_guard 空参数集 + test_range_selector_presets renewal_unit 未落盘，均数据依赖属正常两态）。PT-CB14 时点 581+2 为 home 机历史值，+16 来自 PT-CB15 转正批（Qoder 执行记录记载 581 基线+16 新增=597）——口径链自洽【581+2 本身无法在 office 机回溯复现·属合理跨机引用】 |
| 数据治理零改动 | `git status` 无 DATA 改动；`体检对象_小区_面.geojson` mtime=2026-08-13（早于工作日）；`_tmp` 无 SSSQ/dissolve/合并残留；DATA 无「退休」文件夹（未治理→未建，一致）✓ |
| office 环境 | `codex --version`=0.149.1 ✓；`_resolve_codex_exe()` 本机实跑命中 `AppData\Roaming\npm\...\vendor\...\codex.exe`（非 WindowsApps）✓；`~/.codex/config.toml` 实读：`[mcp_servers.emc] required=true` ✓、`[model_providers.deepseek] models=[v4-pro, v4-flash]` + 08-25 注释 ✓、备份 `config.toml.bak-20260825`（8:54）在位且现行文件 9:30 晚于备份（编辑顺序自洽）✓；`D:\Github\_codex_cwd\AGENTS.md` 在位且四要素齐全（先 list_data/唯一工具面=emc/禁 webSearch/必出图）✓；MCP 工具数活体实测 **18** ✓ |
| 服务栈 | netstat 实测 8600/8080/8000 三端口 LISTENING ✓ |

### 1.2 遗漏/未闭环项

1. **未提交未推送（判据：未 push 不算交付）**：工作区 `M core/codex_bridge.py`（仅注释）+ `M docs/progress.md`；未跟踪 5 份 discuss 文档——**包括被审计的工作报告本身、附录 B、Codex 正式审计稿**。报告所述「主手回收：git 提交」目前是待办非完成态。与 Codex 结论一致。
2. **`_codex_cwd/AGENTS.md` 未进任何复刻面——高优先**：`docs/codex-harness-ops.md` §一.4 只讲 cwd 隔离（防本仓 AGENTS.md 注入），**未提仓外人设文件必须在位**；`_handoff/OFFICE.md` 无此项；spike 执行记录 §八复刻表亦无（该表成文于 08-24，人设 08-25 才诞生——**事后新增的运行依赖未回写复刻面**，属典型缺口）。换机后 T8 内容链必再断。与 Codex 结论一致，且我方补齐了「为何三处都漏」的成因证据。
3. **T1-T7 未收尾**：只有「用户浏览器侧」一句，无逐项验收回执。与 Codex 一致。
4. **T8 复跑步骤未落盘**：955 delta/68s 为一次性结果，问题文本与观测方法未入任何文档（ops 文档 §二只是 4 步通用指引）。与 Codex 一致。
5. **webSearch ok=false 未定档**：仅执行记录一句冒烟观察，ops 文档无记载。与 Codex 一致。
6. **计数口径漂移两处**：
   - presets 60→61：我方定位到**确切成因**——新增项=`tmp_render_1787569867`（`checkup_qty_停车设施缺口_140区.geojson`·登记时间戳 2026-08-24 19:11:07），系 **PT-CB14 回收批「停车位题出图副作用实证」** 所登记（与 6d089391 提交信息互证），**早于 T8 一天**——Codex「疑似后续 render_file 又登记」的机制方向对、时间对象错（详见 §六）。
   - **门禁口径漂移（Codex 漏检）**：`OFFICE.md` 预告「应 595+4」，office 实测 597+2（同日 home 收工记录 HOME.md/todo.md 亦写 595+4，而 PT-CB15 PROMOTE 执行记录写 597/2——home 当日两轮自相矛盾）。总数均 599、skip 差 2，机差根因静态排查未能定位【未核验·疑似数据依赖参数化差异】。到岗记录只引 PROMOTE 数 declared「与 DoD 一致」，未与交接卡预告值对账——**到岗对账缺一环**。

---

## 二、正确性

### 2.1 8600 强依赖推断——成立（附一处表述差异待统一）

- `~/.codex/config.toml` `[mcp_servers.emc]` 实读 `required = true` ✓；桥 `ensure()` 流程（spawn→initialize→thread/start）与报告机制描述一致（`core/codex_bridge.py:151-187` 实读）。
- **补强证据（Codex 未引）**：spike 执行记录 P7 自述「8600 未起降级——**未触发**（8600 恒在）·required=true 快速失败语义保留」——即 spike 期从未实测触发该故障，**office 08-25 是该机制首次实测发生**，报告的「thread/start 失败」定性目前仅有 office 会话单一观察来源。精确失败点（initialize/thread/start/工具调用）我方亦未独立复现【未核验】——但配置+桥代码+spike 设计+office 现象四方互证，方向无误。
- **表述差异**：ops 文档 §一.2 写「8600 未起时**工具调用**快速失败」，报告写「**thread/start** 失败·整个 thread 创建失败」——两种口径应在运维文档统一（不同失败点对应不同诊断路径）。

### 2.2 `_codex_cwd/AGENTS.md` 引导缺环根因——成立（附一处文档不一致）

- 桥 `_CODEX_CWD = REPO.parent/_codex_cwd`（`codex_bridge.py:46-47` 实读）✓；人设文件在位、内容与报告描述四要素一致 ✓。
- 对照逻辑自洽：无引导（①v4-flash、②chat）均不调 emc 工具；有引导（③chat）全链通——引导是必要差异项。
- **文档不一致（Codex 漏检）**：主报告 §4.3 为**三行**尝试表（含 ② deepseek-chat+无引导 的对照实验），**附录 B 只有 2 行**且表头写「两次失败→一次全通」表体仅一次失败——**关键对照实验未进附录 B**。两文档粒度矛盾，无法从盘上痕迹独立复核 T8 会话细节【未核验】。附录 B 应补行对齐。
- app-server 自动加载 cwd AGENTS.md 的行为我方未独立复现【未核验】——但与 Codex 判定相同：机制与观测一致。

### 2.3 deepseek-chat = v4-flash 兼容别名——映射真实但表述过时，且有一个两方审计都漏掉的关键差异

- **官方依据（网络检索核对）**：DeepSeek 官方变更日志（2026-04-24·DeepSeek-V4 条目）原文：「旧有的 API 接口的两个模型名 `deepseek-chat` 与 `deepseek-reasoner` 将于三个月后（**2026-07-24**）停止使用。当前阶段内，这两个模型名分别指向 `deepseek-v4-flash` 的非思考模式与思考模式。」当前官方文档模型列表仅剩 `deepseek-v4-flash / deepseek-v4-pro / deepseek-v4-flash-vision-exp`，**已无 deepseek-chat**。
- **判定**：映射关系真实存在，但那是**过渡期语义**；今天（2026-08-25）已过官方停用日一个月。报告把过渡期映射当作现行官方事实表述——**过时**。实测两度全链通（home spike 08-24 + office T8 08-25）说明 API 侧仍在宽限接受该名，但这属**官方已宣布退役的宽限行为**，随时可能真断。与 Codex 判定一致。
- **关键新发现（Codex 亦未指出）**：官方模型明细页明载 `deepseek-v4-flash`「思考模式：支持非思考与思考模式（**默认思考**）」——即**规范名默认开思考档**；而 `deepseek-chat` 过渡期映射的是**非思考模式**。**两个名字不是纯别名：切换会翻转默认思考档**，直接影响延迟、输出风格与工具调用形态。这既部分解释了尝试①（v4-flash 全名）行为异常的可能来源，也意味着任何「改用规范名」的迁移**必须显式配置非思考模式**（详见 §六-6 对 Codex 建议的修正）。
- **价格口径全对**：官方定价页核对——flash 高峰输入 3 元/输出 9 元、pro 高峰 9/27 元、闲时一律减半 ✓。另外官方明载 v4-flash/pro 均支持 Responses API（与桥 `wire_api="responses"` 相容）✓。

### 2.4 对比实验变量隔离——Codex 批评成立，我方再加一层

- 三格矩阵：①(v4-flash·无引导)断 ②(chat·无引导)断 ③(chat·有引导)通——缺 **(v4-flash·有引导)** 格。「全名断链、别名通、所以保持别名」**不能**由现有证据推出；现有证据只支持「无引导则断、有引导则通（chat 已证）」。
- 报告根因链第三环定性为「模型档位混淆」有误导——①的失败主因是缺引导（②同模型无引导同样失败为证）；模型名差异（含 §2.3 思考档差异）从未被隔离证明。
- **结论**：桥默认回滚到 deepseek-chat 短期合理（实测可通+用户令恒 Flash+环境变量逃生门在），但「定稿」言过其实——未提交的 `codex_bridge.py` 注释原文即「Flash 档=deepseek-chat 语义**待用户终裁**」，比报告「✅ 定稿」诚实。HEAD 默认本就是 deepseek-chat，工作区 diff 仅两处注释（「回滚」无代码 diff——上午误改应为环境变量级临时改动，盘上已无痕迹，与报告自洽）。
- **真正定稿路径**：有引导状态下 A/B 实测 `deepseek-v4-flash`（**显式非思考配置**）——通过则切规范名；不通过则把「退役别名仍可用」的依据、截止风险与回退预案书面写进 ops 文档。当前状态只能算「暂时可用」，与 Codex 判定一致。

---

## 三、风险

### 3.1 `_codex_cwd/AGENTS.md` 仓外遗漏——高风险（确认，成因链补强）

见 §一-1.2-2。人设是 T8 内容链能否调 emc 工具的开关，位于仓库外，clone/换机不携带；且它是 08-25 office 首建、晚于全部三处复刻面成文时间（spike §八 / ops 文档 / OFFICE.md 均为 08-24），属「事后新增运行依赖未回写」。**建议**：人设权威模板入仓（如 `docs/emc-codex-persona.md`），ops 文档加「到岗：生成/复制 `{REPO}/../_codex_cwd/AGENTS.md`」步骤，spike §八复刻表补行；`_codex_cwd` 本体只作运行时产物。与 Codex 建议同向。

### 3.2 webSearch ok=false——中低风险，须定档

桥判定逻辑 `item.status == 'completed'`（`codex_bridge.py:280` 实读）本身正确；人设已禁 webSearch ✓；实际 ok=false 的普遍性/根因（无 search provider？沙箱限制？）我方未复现【未核验】。建议 ops 文档写明「webSearch 非本工具面·已禁用·出现 ok=false 属预期噪声，非故障」——防 T1-T7 用户侧误判。与 Codex 同向。

### 3.3 `~/.codex/config.toml` 明文密钥——高风险（确认，量化修正）

- 现行 config.toml 含 **6 处**密钥行（deepseek `experimental_bearer_token` + ZAI token + web-reader/web-search-prime/zai-mcp-server/zread 的 Authorization/Z_AI_API_KEY——**具体值本审计不复述**）。
- **备份 `.bak-*` 共 6 个**（2026-08-15/08-20/08-24/08-25/hubmerge×2），各含 1-6 处密钥行，合计 7 个文件 29 处密钥行——**Codex 称「7 个 .bak-*·同一份密钥扩散 7 份」计数错 1、定性不准**：各备份是不同时期的密钥配置快照（08-15 版仅 1 处），泄露的是当时的密钥集；若旧密钥未轮换则持续暴露。
- 仓库内 `.codex/config.toml` 无密钥未入 git（与 Codex 一致·风险面在本地文件）。
- **建议**：改环境变量注入/仅占位符；清理或脱敏备份；必要时轮换密钥（尤其已入备份的历史 key）；换机复刻清单只带占位符模板；本机 ACL 收紧。

### 3.4 补充风险（我方新增）

- 未提交注释（「待用户终裁」）与报告（「定稿」）口径不一——若主手回收时只看报告不看 diff，会把未定事项当已定收走。回收时必看 `git diff core/codex_bridge.py`。

---

## 四、纪律

### 4.1 render_client.js:113 白名单外单行——接受（我方立场较 Codex 更宽）

- git diff 实证（提交 6d089391）：该行改动**确为单行**（natureBadge 增 `'test' → '[测试] '` 分支），纯增量。
- **任务书自相矛盾在先**：PT-CB14 任务书 C3 验收要求「data_nature='test' 投递→**前端徽标 [测试]**」，但其白名单未列 `render_client.js`——验收标准要求改一个白名单外的文件。claude 的处置（最小单行+执行记录显式注明+待主手复核）是矛盾缺口下的正解；且主手已在回收提交 6d089391 明文「白名单外单行**追认**」——追认闭环。
- 对 Codex 三条件的修正：条件①「该行加注释」本身会构成**又一次包外改动**（讽刺性缺口）——注释应随下一次授权批次顺带补，不单独开工；条件②（不再叠加）③（纳入徽标体系统一收敛）同意。

### 4.2 数据治理「先错后纠+实证+零改动」——符合「用证据挑战用户假设」原则（纠偏版）

- 初次执行确有纪律瑕疵：探查已见矛盾证据（SSSQ 组内 XQMC 最多 40 个·XQMC unique=1562=面数）却未先质疑即动数据——报告自身承认且用户点名批评。
- 纠偏处置教科书级：四文件交叉验证 + strip 归一 + git 历史 → 摆事实给选项 → 用户裁决「确认现状」→ 删错误产物、数据零改动 → 教训入 `memories/auto/challenge-user-assumptions-with-evidence.md`（实存在盘）。
- **我方全数字复核一致**：小区面 1562 features/列 SSJD·SSSQ·XQMC·来源/XQMC unique=1562（原始与 strip 后均零重复）/SSSQ unique=187/组内 XQMC 最多 40；配置小区 preset 同构（1562/1562/187）；住宅点 26426/SSSQ=188/SSJD=22。结论「XQMC 每面唯一→无合并对象→无需治理」**实证成立**——用户前提错两层（SSSQ=社区名非小区名；且小区名本就无重复）。
- 极小瑕疵：用户原令后半句「同步纳入 RAG 等相关数据库」在「无需治理」结论后未显式回执（逻辑上无需治理→无需同步成立，但报告未一句话点破）。
- 同意 Codex 的补充建议：memory 措辞加一句「中文缩写字段语义歧义→先确认字段含义再批量动数据」。

---

## 五、建议

### 5.1 T1-T7 收尾指引

- 对表任务书逐项验收：每项 = 证据落盘 + 门禁更新 + 可复跑方法（问题文本+观测点+预期工具链）；用户浏览器侧验收单独列回执，不得只写「用户侧」。
- 建议闭环顺序（修订 Codex 5.1）：①人设模板入仓+复刻面回写 → ②deepseek 模型名 A/B 定稿（规范名+显式非思考）并对齐注释/报告口径 → ③T8 复跑步骤落盘 → ④webSearch 定档 → ⑤两处计数口径对齐（presets 60/61 + 门禁 595+4/597+2）→ ⑥T1-T7 逐项回执。

### 5.2 换机复制清单（修订 Codex 5.2）

1. **EMC 人设权威模板入仓** + 生成 `{REPO}/../_codex_cwd/AGENTS.md` 的命令（Codex 同·最高优先）；
2. codex-cli 0.149.1（npm i -g）+ `_resolve_codex_exe()` 命中验证（Codex 同）——**修正**：报告/Codex「npm CLI 不能卸载/重装到别处」表述过绝对——解析器有三候选（PATH 直 hit/APPDATA 默认/npm root -g），重装到任何 npm 全局可见位置均可，仅勿依赖 WindowsApps 商店版；
3. `~/.codex/config.toml`：占位符/env 注入 + `[mcp_servers.emc] required=true` + desktop models 列表 + 备份清理纪律（**备份计数以 6 个为准**）；
4. 启动 8600 + 8080/8000 并握手验证（netstat 诊断命令 ops 文档 §一.2 **已在位**，无需新增）；
5. RAG 索引重建命令（`py tools/rag_index.py --build`）；
6. 缺失 preset 处置：`admin_community`/`renewal_unit` 上传或从 manifest 移除；`tmp_render_1787246545` 从 manifest 清理（临时残留）——**补充**：manifest 另有 4 个**在盘** tmp_render 项（08-21×3 + 08-24×1·含停车位验证件），建议一并定档（保留=演示登记 or 清理），并给「render_file 登记 tmp 项」定清理纪律；
7. 门禁基线 597+2 写入基线文件——**补充**：同时修正 OFFICE.md 的 595+4 口径或注明 skip 机差来源；
8. memory/教训条目确认可读；
9. **（新增）deepseek 模型名定稿**：A/B 实测规范名+显式非思考配置；结果写入 ops 文档 §三环境变量表注释。

### 5.3 其他发现（我方新增）

- 附录 B 尝试表补中间对照实验一行，与主报告三行对齐；
- ops 文档统一 8600 失败点表述（「工具调用快速失败」vs「thread/start 失败」）；
- 主手回收时先看 `git diff core/codex_bridge.py`（注释「待终裁」vs 报告「定稿」）；
- 任务书层面的流程改进：验收标准涉及的前端文件必须进白名单（PT-CB14 C3 的矛盾勿再犯）。

---

## 六、对 Codex 审计的复核意见

Codex 审计总体质量高：核心发现（AGENTS.md 高风险/变量隔离批评/未提交未推送/明文密钥/有条件通过结论）**全部复核成立**。以下为错漏与分歧：

**事实错误（3 处）**

1. **备份计数错**：Codex 称「同目录 **7 个** config.toml.bak-* 均含明文 key，等于**同一份密钥扩散了 7 份副本**」——实测 `.bak-*` 为 **6 个**（含现行文件才是 7 个文件）；且各备份是不同时期快照（08-15 版仅 1 处密钥行），非同一密钥×7。风险实质相同，量化与定性应修正。
2. **冗余建议**：Codex §2.1「建议在运维文档补一条诊断命令（`netstat -ano | findstr ":8600.*LISTENING"`）」——该命令**原文已在** `docs/codex-harness-ops.md` §一.2（本审计实读）。此条暴露 Codex 对 ops 文档通读不完整。
3. **归因错误**：Codex §一.5 presets 漂移「疑似**后续** render_file 又登记了临时项（语境指向 T8 后）」——机制方向对，但时间与对象错：新增项=`tmp_render_1787569867`（停车设施缺口_140区·时间戳 **2026-08-24 19:11:07**），系 **PT-CB14 回收批「停车位题出图副作用实证」** 所登记（与提交 6d089391 信息互证），早于 T8 一天。

**漏检（4 处）**

4. **附录 B 与主报告尝试表不一致**：附录 B 2 行/表头「两次失败」表体一次失败 vs 主报告 3 行——关键对照实验（chat 无引导）未进附录 B。
5. **门禁口径漂移未抓**：Codex 抓了 presets 计数漂移，漏了 OFFICE.md 预告 595+4 vs 实测 597+2 的对账缺口（且 home 当日 PROMOTE 记录 597/2 与收工记录 595+4 自相矛盾无人解释）。
6. **v4-flash 默认思考档（最重要漏检）**：官方模型明细页明载 v4-flash「默认思考模式」，而 deepseek-chat 过渡期映射非思考模式——两名字行为不等价。Codex 建议「回滚对象应优先是规范名 deepseek-v4-flash」**缺关键前提**：不显式配非思考模式，切规范名会翻转思考档、改变桥行为（延迟/输出/工具调用形态）。照建议直接改名的后果可能比维持现状更糟。完整版建议应为「规范名 + 显式非思考配置 + 有引导 A/B 实测」。
7. **任务书 C3 自相矛盾未察**：C3 验收要求前端徽标但白名单漏列 render_client.js——此背景直接影响 §4.1 纪律裁定的合理性（我方因此较 Codex 更宽）。

**佐证补强（非错漏）**

8. Codex 将 8600 失败点标【未核验】是诚实的；我方补文档证据：spike P7 自述该故障未触发过——office 08-25 是首次实测发生，「thread/start」定性仅有单点观察来源。

**立场分歧（1 处）**

9. render_client.js:113：Codex「有条件接受（3 条件）」→ 我方「接受」——任务书矛盾在先+主手已追认，条件①（加注释）本身构成新的包外改动，应并入下次授权批次。

---

## 七、总体结论与通过条件

**结论：有条件通过。**

无条件通过部分：PT-CB14 四件（代码/测试/验收口径活体复现全一致）；数据治理零改动处置（数字全对、认错与纠偏纪律到位）；office 环境重建与 T8 首测的环境事实链（除对比实验归因瑕疵外全部成立）。

通过条件（满足后可通过）：

1. **commit + push** 全部未提交内容（`core/codex_bridge.py`、`docs/progress.md`、5 份 discuss 文档、本审计稿）——「未 push 不算交付」；
2. **人设模板入仓 + 三处复刻面回写**（ops 文档/OFFICE.md/spike §八）——治换机断链高优先风险；
3. **deepseek 模型名定稿闭环**：有引导 A/B 实测 `deepseek-v4-flash`（显式非思考配置）——通过则切规范名；不通过则书面记录退役别名依据/风险/回退预案进 ops 文档；**提交前对齐 codex_bridge.py 注释与报告口径**（要么都「待终裁」要么都「定稿」）；
4. **T1-T7 逐项回执 + T8 复跑步骤落盘**；
5. **密钥面处置**（占位符/env/轮换/备份清理）至少给出排期；顺带修正两处计数口径（presets 60/61·门禁 595+4/597+2）与附录 B 尝试表。

---

## 附：核验项清单

**已核验（本机实证）**：四件代码逐处（geo_registry.py / mcp_server_emc.py / render_client.js / panel.js / brain-adapter-dsh.js / render-contract.md）；7 用例+全量 597+2；C1 验收口径活体复现（5 rows·营盘路 23 点）；list_data 活体 23+61=84；manifest 64/61/3；数据治理全数字（1562/1562/187/188/22/26426/40）+零改动（git status/mtime/_tmp/退休夹）；codex 0.149.1；`_resolve_codex_exe()` 实跑；config.toml（required=true/models/密钥行 6 处/备份 6 个）；`_codex_cwd/AGENTS.md` 四要素；MCP 18 工具；netstat 三端口；tmp_render 时间戳换算；git log/diff（6d089391 单行追认·codex_bridge.py 注释 diff）；memory 教训条目在盘；DeepSeek 官方变更日志/定价页/模型明细（网络检索）。

**未核验（显式标注）**：8600 缺失的精确失败点（thread/start vs initialize）；app-server 自动加载 cwd AGENTS.md 行为；T8 三次尝试的会话细节（含 ② 对照实验是否如主报告所述执行——附录 B 与主报告不一致加剧存疑）；webSearch ok=false 的根因与普遍性；home 机 595+4 vs 597+2 的 skip 机差根因；PT-CB14 时点的 581+2（home 历史值·跨机引用）。
