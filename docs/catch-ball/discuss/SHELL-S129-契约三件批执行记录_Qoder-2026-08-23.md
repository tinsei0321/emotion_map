# SHELL(S129) · 壳阶段契约三件批执行记录（Qoder · 2026-08-23）

> **依据**：壳阶段联合任务书 v1.0（zcode+Qoder 两署名）§二 S1/S2/S9——原 zcode 自留件转派 Qoder 执行（zcode 审读收敛·不共笔）。
> **纪律**：中文·零新追踪 ID·测试随件·本地 commit 前缀 `SHELL(S129):`·零 pull 零 push。

## 〇 执行结论

三件全部完成，随件测试 2 passed（`tests/test_vertical_profile_boundary.py`·0.13s）。

| 件 | 产出 | 验收口径 |
|---|---|---|
| S2 ACP v1.1 增补 | `docs/acp-contract-v1.md` v1→v1.1（+47 行） | 四项增补+provenance+载荷结构模式全部落地·三处表述修正一并落（见 §二） |
| S9 垂域切换位预留 | `docs/vertical-profile.md`（69 行·六件套+挂载点+不变量+审计说明）+ `tests/test_vertical_profile_boundary.py`（150 行·可跑 grep 硬审计）+ ACP 契约 §六附录（共享不变量清单） | 审计脚本实跑通过·baseline 定档 30 文件 324 处（豁免测试资产后） |
| S1 BrainAdapter 契约 | `docs/brain-adapter.md`（44 行·半页） | 事件出/动词入签名+三形态（轻循环/dsh 降级/Codex 全量注记）+验收三条 |

## 一 S2 · ACP v1.1 增补明细（对照任务书 §一.4 定稿范围）

| 任务书要求 | 落地位置 |
|---|---|
| msg.delta 增 kind 子类型 reason\|content | §二事件表 + §五-1 |
| msg.delta 增 provenance: real\|synthesized（Qoder 评审增补项） | §五-1（降级形态诚实性标记·桩事件必带 synthesized） |
| tool.end 载荷四字段成文化 | §五-2——**按评审修正落为「载荷结构模式」**：工具特定摘要+必带 caliber 摘要·schema 按工具注册（沿 tool_contracts.py 单一权威源模式）；rag_query 四字段作示例载荷（zonal_stats/render_spec 各列其摘要字段）——写死字段集会让异构工具载荷无处安放 |
| followup_cue 渲染语义=过程通道 | §五-3——契约管到「tool.end 载荷字段存在与语义」；chips 点击回填属前端内容通道不管（过程-内容分层红线入契约头部） |
| approval.req 默认全放行+首个真实消费者=kb_inbox 写入预留注记 | §五-4（接线点登记·RAG 支柱二三级审批直接消费本事件族） |
| （评审三处表述修正） | ①载荷结构模式（§五-2）②followup_cue 过程/内容分层（§五-3+头部红线）③approval.req 接线点（§五-4）——全部落地 |

## 二 S9 · 垂域切换位预留明细

1. **六件套**（`docs/vertical-profile.md` §一）：数据包+知识库（**分两槽**：语料槽=向量语料·范式槽=paradigm.py/manifesto.py 结构化知识）+口径注册表+身份卡/人设+工具预设+**渲染预设**（受管色带词表垂域子集·样式权威仍在平台层·禁自带色值）。
2. **挂载点参数化**（§二）：`vertical_profile.json` 单一入口 + 三挂载点（grounding 上下文/身份卡查询/口径表引用）——**只设计不实现**（结构草案在档）。
3. **共享不变量清单**：入 ACP 契约 §六附录（五条：ACP 事件语义/MCP 工具签名 18 件/渲染契约七段/治理字段 schema/四态出口）——垂域②试点核心观察项（有一项要改=抽象划线错了）。
4. **grep 硬审计**（`tests/test_vertical_profile_boundary.py`）：
   - 词表：宜昌/yichang/西陵/伍家岗/点军/猇亭/夷陵（忽略大小写）；
   - 平台层范围：core/ tools/ api/ frontend/js/ serve.py（test-*.js 测试资产豁免）；
   - 断言：命中文件集与每文件计数双封顶（新文件/新增命中=失败·失败信息打印「文件:行号:内容」级违例清单）+总额防绕过断言；
   - BASELINE 定档：30 文件 324 处（全量扫描 412 处·豁免 test-assets.js 13 处与 test-cases.js 75 处）——**只减不增纪律**写进测试 docstring（上调须报批）；
   - 实跑：2 passed（含 baseline 诚实性检查——僵尸登记 warn 不 fail）。
5. **现状违例登记**（诚实账）：平台层 400+ 处城市名硬编码现状——清偿顺序建议（工具预设→身份卡 city→投影 CRS→grounding 地图初值）入 vertical-profile.md §二。

## 三 S1 · BrainAdapter 接口契约明细

- **事件出/动词入签名**：五族事件（含 v1.1 增补的 kind/provenance/载荷模式）+ 四动词（open/step/seal/close·携垂域 grounding）；
- **三形态**：轻循环引擎（首引擎·恒 real）／dsh 降级形态（headless+桩事件·必带 synthesized）／Codex 全量形态（app-server 注记·契约成熟后启用）；
- **红线**：只定义签名不定义实现（YAGNI·超一页即砍）；Adapter 是翻译层非编排层（壳不经 Adapter 调 MCP）；
- **验收三条**（未来怎么审）：S6 schema 校验器符合性／双轨对照口径（同题 10 题两引擎·架构证据归档）／降级形态 provenance 完备性（缺省即违规）。

## 四 自查与遗留

- **零新追踪 ID**：三件均为契约/文档/测试件·无新公开函数注册（测试文件的两个 test_ 函数属 pytest 收集面非 @track 面）——符合纪律。
- **eval-anchor 零接触**：未触碰 ai_qa/prompts.py 与 diagnose 内容。
- **过程-内容分层**：ACP v1.1 头部新增红线条款（契约级防线先行·S3/S4 实施时消费）。
- **遗留（非本批）**：①vertical_profile.json 实际文件未创建（S9=切换位预留·实现归垂域化批次）②S6 schema 校验器（dsh 件）将消费本批 ACP v1.1 契约③三形态的 Codex 映射样例 dump 待 Codex 试点期补（沿 ACP v1 既有注记）。

## 五 提交

- commit 前缀 `SHELL(S129):`·本地提交·零 push（遵批次指令）。
- 变更清单：`docs/acp-contract-v1.md`（改）+ `docs/vertical-profile.md`（新）+ `docs/brain-adapter.md`（新）+ `tests/test_vertical_profile_boundary.py`（新）+ 本执行记录（新）。

---

> SHELL(S129) · Qoder 执行 · 2026-08-23 · 三件完成测试全绿 · 待 zcode 审读收敛
