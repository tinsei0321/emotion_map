# CB-22 · L2 出向任务 — 知识库贯通 EMC「出口」（讨论发起）

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（承接 CB-21b 知识库构建完成后）
> **用户定**：① 同步进度·让新增知识库**贯通全局内容**（尤其 EMC"出口"）·后续开发都参考本知识库 ② L2 任务进 CB 讨论·定稿执行。
> **先验后推**：本方案草案进 CB → 两组评估 → 收敛 → 定稿执行。

---

## 〇、背景与现状（已核实）

- **CB-21b 知识库构建完成**：L0 资料库（`docs/urban-renewal-plan/`·875 文件提炼）+ L1 知识库（`urban_renewal.py` 做厚·305 passed）
- **关键缺口（已代码核实）**：`outlet_kb/`（出向知识库·含出口契约 + case_library 案例库）**运行时零 import**——只供人读的契约定义，EMC 回答时不会消费它
- **CLAUDE.md 已标注**：出向链路=0（结果→行业格式化未实现）·`DOMAIN_OUTLETS` 仅启发词无实现
- **用户需求**：让知识库（尤其项目库/案例/指标）通过 EMC"出口"真正贯通——回答"宜昌有哪些更新项目"等具体数据类问题能引用知识库

## 一、L2 任务范围（草案）

### 目标
让 L0/L1 知识库的内容**通过 EMC 出口契约被回答引用**——填补「出向链路=0」缺口。

### 任务分解（草案）

| # | 任务 | 说明 |
|---|---|---|
| 1 | **outlet_kb 接入运行时** | 让出口契约（renewal_object_identify/demand/sequence/content）真正被 EMC 调用——从"供人读"变"可执行" |
| 2 | **体检-更新衔接契约** | 补 `urban_renewal_outlets.py`「体检→更新」衔接（Codex CB-21 已建议·stages.js:419 枚举确认）——体检结果→更新需求 |
| 3 | **知识库引用注入** | L0 资料库（项目库/案例/GIS 图层对照）在出口卡/回答中被引用——标注来源·防幻觉 |
| 4 | **出口卡对接 L1** | 出口契约 field_mapping 引用 L1 做厚的字段（更新对象基数/片区分类/指标体系） |

### 关键设计原则（AI·Copilot 内核）

- **Dumb Tool**：出口卡组装 = 确定性（build_outlet_schema·不调 LLM 推理）
- **Smart**：意图识别（哪个出口契约匹配）+ 回答生成
- **编排器**：机械接线（diagnose 意图 → 选出口 → 组装卡 → 注入回答）
- **不碰承重**：diagnose prompt / 四态出口 / @track 不动·出口契约新增走 tool_contracts 注册 + 触发词同步

## 二、待两组评估焦点

1. **outlet_kb 接入方式**：怎么让出口契约被运行时消费（现有 build_outlet_schema 是确定性组装·但无触发入口）？走「diagnose 意图 → 触发出口」还是「finalStep 组装」？
2. **知识库引用边界**：L0 项目库/案例如何被出口卡引用（直接注入片段？标注来源？）——如何不撞 final_brief 体积守卫？
3. **体检-更新衔接契约**：放 `urban_renewal_outlets.py`（Codex 建议·已核实枚举）——同意吗？field_mapping 怎么设计？
4. **与 RAG 的关系**：L2 出口对接（确定性）vs RAG 检索（向量·后置）——边界在哪？L2 先做确定性出口，RAG 后补检索，是否合理？
5. **测试**：出口卡对接知识库的 e2e 测试（"宜昌有哪些更新项目"→ 出口卡 → 引用项目库）怎么设计？

## 三、执行节奏（草案）

1. CB-22 两组评估 → 收敛定稿
2. 实现：outlet_kb 接入 + 体检-更新契约 + 知识库引用注入
3. 测试：出口 e2e + pytest 零回归
4. 更新总览/完成度清单

---

## 附：发给两组的讨论 prompt（A 组通用）

```
【CB-22 讨论邀请 · L2 出向任务 · 知识库贯通 EMC 出口】

背景：CB-21b 知识库构建完成（L0 资料库 + L1 做厚·305 passed）。用户要求让知识库贯通 EMC"出口"——
尤其回答"宜昌有哪些更新项目"等具体数据类问题能引用知识库。outlet_kb 目前运行时零消费（出向链路=0）。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-L2出口任务_知识库贯通EMC出口_讨论发起_2026-08-09.md（本方案）
- 读 ai_qa/outlet_kb/（urban_renewal_outlets.py + urban_checkup_outlets.py + case_library.py）
- 读 ai_qa/industry_kb/urban_renewal.py（L1 做厚后）
- 读 docs/urban-renewal-plan/知识库构建完成度清单.md（L0 全貌）
- 核实：ai_qa/prompts.py 出口相关注入点

第二步 · 评估焦点（agree/disagree/partial + 证据）
1. outlet_kb 接入运行时的方式（diagnose 触发 vs finalStep 组装）
2. 知识库引用边界（如何引用 L0 项目库·不撞体积守卫）
3. 体检-更新衔接契约放 urban_renewal_outlets.py 是否合理·field_mapping 设计
4. L2 确定性出口 vs RAG 向量检索的边界
5. 出口 e2e 测试设计

第三步 · 产出回应
- 落盘 docs/catch-ball/discuss/CB22-L2出口任务_{组名}_{YYYY-MM-DD}.md
- 中文为主·引用 path:line·不 git·落盘由 claude组 push
```

---

*claude组（2026-08-09）·CB-22 L2 出向任务讨论发起·待两组评估。*
