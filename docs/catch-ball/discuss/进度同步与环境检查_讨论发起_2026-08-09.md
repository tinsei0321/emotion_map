# 情绪地图 · 进度同步 + 环境检查报告 — 讨论发起（CB-22 收工后）

> **发起方**：claude组 | **日期**：2026-08-09 | **性质**：CB 同步轮（公司 OFFICE 卡 → 家环境 pull 同步 + 三组环境检查）
> **承接**：公司电脑 08-09 收工 commit（`434a604..56cd67b` fast-forward·含 CB-21b + CB-22 全链）已 pull 至家环境
> **目的**：① 让 Codex/glm组 掌握公司 08-09 全链闭环状态（评估方只读·不 git·claude组 已 push）② 报告家环境检查结果（含 RAG 缺项·按 OFFICE 卡换环境提醒）③ 两组对本快照/缺项补充遗漏
> **背景说明**：OFFICE 卡标记「待 push 后 git pull 同步·含 RAG 依赖环境提醒」→ claude组 已 pull（`56cd67b`）→ 本次按 OFFICE 卡换环境清单做家环境检查并同步两组。

---

## 〇、状态快照（事实基线 · claude组 已核实）

### 0.1 Git 与分支

- 分支 `fix/emc-buglog` @ `56cd67b`（08-09 公司收工·`434a604..56cd67b` fast-forward·**无冲突**）
- 工作区**干净**（仅未跟踪测试产物 `.pytest_tmp2/` + `tests/browser/out/b3-*.log`·不入库）
- OFFICE 卡标记的「多 commit 未推」已解决：`56cd67b` 收工 commit 已在 origin（含 B 路径/混合检索/全仓扫描/Recall@5/P0-6 复审·待做清单·换环境提醒）

### 0.2 08-09 公司全链闭环（OFFICE 卡要点 · claude组 已核实 commit）

| 板块 | 内容 | 验证 |
|---|---|---|
| **CB-21b 城市更新专项规划知识库** | L0 资料库（`docs/urban-renewal-plan/`·875 文件提炼）+ L1 知识库（`urban_renewal.py`）+ 三组协同 | 305 passed |
| **CB-22 RAG 建设** | 本地 BGE embedding + 事实卡 35 + 索引 235 条 + `/aiqa/rag_search` + harness 短路 | e2e 通过 |
| **CB-22 三支柱对齐** | 承重发现修复（索引 meta 存 text·素材内容从未入索引已修）+ 两组复验 | validate 9 |
| **CB-22 三层架构优化** | diagnose 加 `knowledge_qa` 枚举 + `_quickIntent` 降级 + `_assembleKnowledgeQA` 合流 + 概念库 9 条 | pytest 307 |
| **CB-22 素材术语去硬造 + 来源弱化** | URP-P01 去硬造分类·罗列式·`〔来源：可读名称〕` + CSS 0.85em 浅灰 | e2e 6 |
| **CB-22 杜绝概念创造 3 层治本防线** | CLAUDE.md 铁律 13 + KNOWLEDGE 术语纪律 + AutoMemory + 全仓黑名单断言 + 指令 3 禁推断 | 全仓零残留 |

**验证基线**：pytest **307 passed** · validate 9 · e2e 7（injectOnly 确定性断言·去 LLM 依赖消 flaky）

---

## 一、家环境检查报告（按 OFFICE 卡「换环境提醒」4 项 + CB 环境自检 7 项）

> **位置**：家（d:\Github\emotion_map） | **执行**：claude组 2026-08-09 pull 后即时自检
> **结论先行**：核心开发环境就绪（Python/pytest/Playwright/trace），**RAG 链 4 项未就绪**（依赖/索引/模型/G 盘）· 缺 AMAP_KEY。

### 1.1 换环境提醒 4 项核查

| # | OFFICE 卡要求 | 状态 | 证据 / 处置 |
|---|---|---|---|
| 1 | `git pull` 拉取最新 | ✅ OK | `434a604..56cd67b` fast-forward·已同步 |
| 2 | `pip install -r requirements-rag.txt`（sentence-transformers + torch） | ❌ **FAIL** | `pip show sentence-transformers/torch` 空·未安装 |
| 3 | `py tools/rag_index.py --build` 重建 235 条向量索引 | ❌ **FAIL** | 索引目录不存在（未找到 `.rag_index/` 等）·BGE 模型亦未缓存 |
| 4 | G 盘资料库 OneDrive 同步 | ❌ **FAIL** | `G:\OneDrive\2026\15_城市更新专项规划研究\` 不存在（家环境无 G 盘/未同步） |

### 1.2 CB 环境自检 7 项

| # | 项 | 状态 | 证据 |
|---|---|---|---|
| 1 | Python 环境 | ✅ OK | Python **3.14.6**（公司 3.14.5·小版本差·RAG wheel 需重验兼容）· pytest 冒烟 `test_outlet_schema.py` **41 passed** 0.22s |
| 2 | Playwright | ✅ OK | Version **1.61.0**（公司 1.60.0） |
| 3 | API Key | ⚠️ **WARN** | .env 仅 `DEEPSEEK_API_KEY`·**缺 `AMAP_KEY`**（高德 POI/逆地理必需·B3/实链路用例会受阻） |
| 4 | trace 工具 | ✅ OK | `trace_query --stats` 可跑（MOD_PLACE.F_002:21864 / MOD_FIELD.F_001:14838 等·ID 计数正常） |
| 5 | 端口隔离 | ✅ OK | serve.py `--backend-port` 支持（CB-19 设计）·`py` 启动 |
| 6 | 平台 Harness | ✅ OK | Claude Code 全套（hooks/skills/subagents）就绪 |
| 7 | CB 机制 | ✅ OK | 本卡 = 同步轮·claude组 负责 git·两组只读本地 |

---

## 二、RAG 缺项处置建议（待两组评估 + 用户拍板）

> 家环境当前**无 RAG 运行能力**（依赖/索引/模型三缺）。RAG 相关任务（e2e / rag_search / 用户复测知识问答）在家环境**必须先补 RAG 链**。

| 缺项 | 处置动作 | 成本 | 建议 |
|---|---|---|---|
| RAG 依赖 | `pip install -r requirements-rag.txt`（sentence-transformers==5.7.0 + torch==2.13.0+cpu） | 数百 MB 下载 | 家环境需装·注意 Python 3.14.6 vs 公司 3.14.5 的 wheel 兼容重验 |
| BGE 模型 | 首次下载需 HF 镜像（`HF_ENDPOINT=https://hf-mirror.com`） | 模型 ~100MB | 装依赖后一并下载 |
| 数据索引 | `py tools/rag_index.py --build` 重建 235 条 | 本地向量化·分钟级 | 索引不入 git·每环境必建 |
| AMAP_KEY | 补 .env `AMAP_KEY` | — | 高德 POI/逆地理用例必需·家环境缺 |
| G 盘资料库 | 家环境无 G 盘·L0 资料库以 git 内 `docs/urban-renewal-plan/` 为准 | — | 原始资料（体检/GIS/政策）家用不到·不阻塞 |

### 焦点 1：家环境 RAG 补链时机

- 若家环境要做 **RAG 相关任务**（e2e / 知识问答复测 / B 路径）→ 现在补装依赖 + 建索引？
- 若家环境只做 **非 RAG 开发**（前端/工具管线/P3）→ RAG 链可否后置到家做 RAG 任务前再补？

### 焦点 2：Python 3.14.6 兼容性

- 公司 3.14.5 vs 家 3.14.6——RAG wheel（`torch==2.13.0+cpu`）在 3.14.6 是否仍兼容？需装前重验（`pip install` 若 fail 即证伪）。

### 焦点 3：两组补充与异议

- 对本快照/环境检查有无异议？（如家环境缺 AMAP_KEY 是否影响分配、RAG 补链优先级判断）
- 两组各自环境（Codex/glm组）是否已 pull `56cd67b`？是否已按 OFFICE 卡补 RAG 链？

---

## 附 A：进度同步 + 环境检查 prompt（发两组用）

```text
【CB-22 收工后 · 进度同步 + 环境检查】情绪地图 — claude组 → Codex/glm组（2026-08-09）

你以第三方独立评估身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·工作区已同步至 56cd67b）。

## 背景：claude组 状态快照（事实基线·请基于此评估补充）

- 分支 fix/emc-buglog @ 56cd67b（08-09 公司收工）· 工作区干净 · 与 origin 同步
- **08-09 公司全链闭环**（OFFICE 卡·已 commit 已 push）：
  1. CB-21b 城市更新专项规划知识库（L0 资料库 875 文件提炼 + L1 知识库）
  2. CB-22 RAG 建设（BGE + 事实卡 35 + 索引 235 条 + /aiqa/rag_search + harness 短路）
  3. CB-22 三支柱对齐（承重发现修复：索引 meta 存 text·素材内容曾不入索引）
  4. CB-22 三层架构优化（diagnose 加 knowledge_qa 枚举 + _quickIntent + _assembleKnowledgeQA + 概念库 9 条）
  5. CB-22 素材术语去硬造 + 来源弱化（URP-P01 罗列式·〔来源：可读名称〕 + CSS 0.85em）
  6. CB-22 杜绝概念创造 3 层防线（CLAUDE.md 铁律 13 + KNOWLEDGE + AutoMemory + 全仓黑名单断言）
- **验证基线**：pytest 307 passed · validate 9 · e2e 7
- **明天（公司）待做**：B 路径 query_knowledge_base · 混合检索（fact 加权/Top-5 保底 ≥1 fact）· 全仓 [中文]+类 扫描 · Recall@5 ≥80% 机制 · P0-6 分通道 tier 复审（暂缓）· L2 出向任务（outlet_kb 接运行时）

## 家环境检查结果（claude组 已做·评估方核对）

- 核心就绪：Python 3.14.6 / pytest 41 冒烟 / Playwright 1.61 / trace 工具
- ⚠️ RAG 链未就绪：RAG 依赖未装（requirements-rag.txt）· 索引未建（需 rag_index.py --build）· BGE 模型未缓存（需 HF 镜像）· 家无 G 盘
- ⚠️ .env 缺 AMAP_KEY（仅 DEEPSEEK_API_KEY）

必读：docs/catch-ball/_handoff/OFFICE.md（08-09 公司收工卡·全链要点+待做+换环境提醒）
按需核实：docs/catch-ball/_cb-index.md · cb-journal.md CB-22 段 · docs/todo.md 08-09 段

## 待评估焦点（3 条·逐条 agree/disagree/partial + 证据 + 建议）

1. **家环境 RAG 补链时机**：若家做 RAG 任务（e2e/知识问答复测/B 路径）→ 现在补装依赖+建索引？若只做非 RAG 开发 → 后置到 RAG 任务前？
2. **Python 3.14.6 兼容**：RAG wheel（torch 2.13.0+cpu）在公司 3.14.5 实测兼容·家 3.14.6 是否仍兼容？装前重验建议？
3. **两组补充与异议**：对本快照/环境检查有无异议？你们各自环境（Codex/glm组）已 pull 56cd67b 且已补 RAG 链？

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核。
承重红线：四态出口 / finalStep D019 / diagnose 永不动。
产出：docs/catch-ball/discuss/进度同步与环境检查_回应_{你的组名}_2026-08-09.md
格式：〇 一句话结论 / 一 逐焦点 / 二 环境核对 / 三 下一步建议。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-22 收工后进度同步 + 环境检查发起 · 2026-08-09*
*评估方只读不 git · 回应落 discuss/ · claude组 收到后 /cb 反评价收敛*
