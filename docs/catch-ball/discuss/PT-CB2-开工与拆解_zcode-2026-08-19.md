# PT-CB2 · 数据治理批 B1 · 开工与拆解（zcode·2026-08-19）

> 主手：zcode。承接：PT-CB1 已回口（T1-T4 全销号·T2 审计零打回通过）。本轮=PT 总计划**阶段一**首批（原 CB-39 计划 B1 批次·spec 权威=`discuss/CB39-实施计划_Codex-2026-08-16.md` §四 B1）。
> 分支纪律：一切在 `EMC_harness_dsh`（main 冻结）。commit 前缀 `PT-CB2(T{n}):`。

---

## 〇 本轮目标（一句话）

图层户口登记补全 + `usage` 字段落地（铁律7 机械化第一步：**结论层从此被机器拒绝作分析输入**）。

## 一 任务表

| # | 任务 | 执行 | 白名单（允许触碰文件） | 验收 |
|---|---|---|---|---|
| **T1** | manifest 注册补全四件套：①新增 group「城市体检底座」（根 12 底座·`snapshot: true` 防双头权威·社区三口径显式标注 193 含村/174 去村/130 西陵伍家）②12345 点层补注册（安全/民生×区级/社区/有坐标+西陵伍家×2+聚合面·A3 归位后路径）③page7 点版三组+18村面注册 ④`usage` 字段标注（清单照 CB39 计划：底座/配置/qty 点层=`input`；西陵伍家合并范围/聚合范围174/18村面/page7 三组面+点版=`analysis_output`） | **dsh**（四小件清单明确） | `DATA/boundaries/presets/manifest.json`（+若注册走脚本则该脚本） | JSON 可解析+schema 自检过+既有 pytest 全绿；执行记录落盘逐项销号 |
| **T2** | usage 白名单**消费点**落地：`api/geo_routes.py` 输入校验（`usage=analysis_output` 层拒绝作空间操作输入·报错语义化）+ 前端 toolbox 工具链接通同一校验（CB39 B1 zcode Z4 条款） | **zcode（攻坚）** | `api/geo_routes.py`、`frontend/js/ai_qa/tools.js`（或 toolbox 链对应文件·开工前白名单细化） | 拒绝路径有测试；既有 EMC 用例（zonal/双轨）回归零退化 |
| **T3** | B1 验收包：manifest schema 一次性校验脚本（tools 内）+ 前端范围选择冒烟（新按钮可加载）+ 全量 pytest 阶段基线复核 | **zcode**（随 T2） | `tools/`（新校验脚本） | 三验收项留档入执行记录 |
| 审计 | T1/T2 完工后 Codex（+claude 如回归）按章程审 | 审计组 | — | 落盘 `PT-CB2-审计_{组}-*.md` |

**时序**：T1（dsh）先行——T2 的校验逻辑依赖 usage 字段在 manifest 落地；T1 回收后 T2 开工。

## 二 派发判据备注

B1 四件=清单明确的数据登记（dsh）；usage 消费点=跨前后端校验链路（zcode 攻坚）。B2 版本对账（判裁类）与 B3 口径注册表+复核工具（涉追踪 ID F_020）留 PT-CB3——判裁与取号纪律不派发。

## 三 纪律重申

main 冻结；dsh 白名单外禁碰（含 `.dsh-meow/` 已 gitignore）；pytest 门禁=375+3（T2 若新增测试则基线上浮并在执行记录注明）；铁律全生效。

---
> dsh 派发 prompt 随本文件交付用户。T1 回收→T2 开工→审计→收敛→PT-CB3（B2+B3）。
