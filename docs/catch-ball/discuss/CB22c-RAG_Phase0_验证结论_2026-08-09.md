# RAG Phase 0 · embedding 验证结论（本地 BGE 已跑通）

> **验证方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22c（RAG·Phase 0）
> **用途**：向量化可行性客观验证结论——本地 BGE embedding 完整跑通·RAG 确定可实现。供两组评估。
> **结论先行**：**向量化确定能实现**——本地 BGE（bge-small-zh-v1.5·512 维）已实测跑通·中文语义检索有效·离线免费·不依赖外部 API 配额。

---

## 一、背景

- 用户问「宜昌有哪些更新项目」→ EMC 需检索知识库 → CB-22c 定 RAG 向量化（业界主流）
- CB-14（08-03）判「DeepSeek embedding 硬阻塞」·智谱/火山待验证
- **用户强调：按业界主流·保证效果·基于客观事实**

## 二、验证过程（客观·全部实测）

### 1. 候选 embedding 方案实测

| 方案 | 结果 | 证据 |
|---|---|---|
| **智谱 embedding-3** | ⚠️ **配额不可用** | 旧 key（Z_AI_API_KEY）报 429 code 1113 = 「无可用免费资源配额」·新 key（coding plan 套餐）未在 .env 配置·待实测 |
| **火山 multimodal embedding** | ❌ **概念混淆·不可用** | glm 核实 = **图片** embedding（2048 维图文匹配）·非文本·RAG 需文本 embedding |
| **本地 BGE**（sentence-transformers） | ✅ **完整跑通** | 见下方详细证据 |

### 2. 本地 BGE 完整验证（关键）

| 步骤 | 结果 |
|---|---|
| 安装 sentence-transformers | ✅ 成功（5.7.0 + torch 2.13.0·**Py3.14 有完整 wheel**·pip dry-run + 实际安装确认） |
| 模型加载 | ✅ 40.4s（`BAAI/bge-small-zh-v1.5`） |
| **HF 下载问题** | ✅ 解决——`huggingface.co` 超时·**`hf-mirror.com`（中国镜像）通**·设 `HF_ENDPOINT=https://hf-mirror.com` |
| 中文 embedding 编码 | ✅ **512 维向量**·3 条文本编码成功 |
| **语义检索质量** | ✅ 查询「葛洲坝更新项目」→ 葛洲坝 **0.728**·停车 0.374·北京历史街区 0.263——**语义相关度正确** |

### 3. 向量库（零依赖·Codex 建议）

- numpy 已装（2.4.6）·L0 数百文件 → 几千条向量·**numpy 暴力检索毫秒级**
- `data/rag_index/`（gitignore）：`vectors.npy` + `meta.jsonl`

## 三、结论

**向量化确定能实现**·技术路线定案：

```
embedding = 本地 BGE（bge-small-zh-v1.5·512 维·离线免费·无限流）
向量库    = numpy 自建（零新依赖·已装）
对象      = L0 知识库（875 文件提炼笔记 + L1.5 事实卡 + case_library）
检索      = rag_search（余弦相似度 Top-K + 来源）
```

- **不依赖外部 API 配额**（智谱不可用也不阻塞·本地 BGE 兜底）
- **HF 镜像** = 国内网络下载模型的标准解法
- **混合检索**（业界标准）：结构化走确定性（B 路径·精确）·开放语义走向量（C 路径·召回）

## 四、待两组评估焦点

1. **embedding 定案**：本地 BGE（bge-small-zh-v1.5·512 维）作主方案——同意吗？还是需验证更大模型（bge-large）？
2. **向量库**：numpy 自建（零依赖·Codex）vs sqlite-vss（glm）——Py3.14 兼容已排除障碍·确认 numpy？
3. **HF 镜像**：设 `HF_ENDPOINT=https://hf-mirror.com` 是否需固化（requirements/env）？还是运行时设？
4. **requirements 更新**：sentence-transformers 入 requirements——同意吗？（新增依赖）
5. **Phase 1 执行**：确认后进入 RAG Phase 1（向量化 L0 + rag_search + 接入 EMC）？

---

*claude组（2026-08-09）·RAG Phase 0 验证结论·本地 BGE 已跑通·待两组评估。*
