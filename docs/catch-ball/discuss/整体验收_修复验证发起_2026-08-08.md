# 情绪地图 · 整体验收修复验证 — 发起（claude组 · CB-18 补）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB-18 验收修复验证（两组复验·先验后推）
> **承接**：CB-18 整体验收**有条件通过** → claude组 修 W-1/W-2 + 补 S-1~4（commit `d5a5625`·**未 push·先验后推**）
> **目的**：让两组对修复/补证独立复验（逐项核对），通过后整体验收全通过 → push + 进发版就绪度回归

---

## 〇、修复/补证摘要（claude组 已完成·待两组复验）

### W-1 · tools.js 红/绿文案残留 → 纯橙系（回归前合入）
- `frontend/js/ai_qa/tools.js` hotspot docstring：「hot=负面聚集=红 / cold=正面聚集=绿 / ns=灰」→「hot/tend_hot/ns/tend_cold/cold 五档·纯橙系显著性符号层·弃极性色·与 KDE 解耦」
- `tools.js` hotspot observation：「（hot=红/cold=绿/ns=灰）」→「（纯橙系显著性五档·hot/tend_hot 深橙·ns 灰·tend_cold/cold 浅橙·弃红绿极性色）」
- 零逻辑影响·纯文案

### W-2 · threshold/soft_threshold 契约-运行时漂移 → 转发补齐（推荐方案）
- `frontend/js/toolbox/hotspot-tool.js` `_execute`：body 加 `threshold`/`soft_threshold` 转发（`!= null` 才带·默认不传走后端 1.96/1.0）
- `frontend/js/ai_qa/tools.js` hotspot handler：`generateHotspotForAI` 调用透传 `threshold: params.threshold, soft_threshold: params.soft_threshold`
- 补齐契约（tool_contracts.py "EMC-only·AI 执行"）承诺的参数化能力·LLM 指定阈值不再被静默丢弃

### S-1 · export_outlet_card_csv 单测（`tests/test_export.py` 新建·4 例）
- BOM 前缀 / 列（outlet_id/name/scale/interface/fields 平铺/data_base/task_link/limitations/geo_label）/ 显式脱敏（值命中 SENSITIVE_FIELDS 整列剔除）/ 空 card 不崩溃

### S-2 · create_terrain_dem terrarium 解码单测（`tests/test_spatial_analysis.py` +2）
- `test_create_terrain_dem_terrarium_decode`：PNG 尺寸=网格（PIL height,width 反序）+ terrarium 解码回高度 0~500m（非零）+ bounds WGS84 量级
- `test_create_terrain_dem_polarity_filter`：极性过滤后点不足 → ValueError（诚实报错）

### S-3 · result-struct e2e-seam 直测（`tests/browser/test_result_struct.py` 新建）
- `frontend/js/e2e-seam.js` 暴露 `buildResultStruct`（`window.__emcTest.buildResultStruct`·纯函数）
- 直测：观点提取（有标记）/ 无观点不显卡（无标记→null）/ 4 要点四段 / 结论带地点（rows place_name）/ scale 三档 / 无地名降级「关注区域」
- **P0-3 三路径出卡**：由 harness.js 三处 `_dispatchResultStruct` 接线（:705/:819/:1962）保证·共享纯函数直测确定性·**DOM 断言留给回归期浏览器抽验（S-5）**

### S-4 · geo_label micro 分支单测（`tests/test_outlet_schema.py` 补）
- micro → 「微观·落点」断言 + 未知 scale → `None`（诚实降级·不硬出卡）

### 验证
- pytest **297 passed + 5 skipped**（+6 新增·零回归）
- S-3 e2e-seam 直测 **PASS**（起 serve + Playwright·真实 JS 逻辑）
- 前端 JS ESM 语法 OK（.mjs 副本 node --check·tools.js/hotspot-tool.js/e2e-seam.js）

---

## 一、复验焦点（两组独立复验）

### 焦点 1：W-1/W-2 修复正确性
- W-1 文案是否已彻底去红/绿残留？（grep「红/cold=绿」等旧义全仓核对）
- W-2 转发链是否闭合？（tools.js hotspot → generateHotspotForAI → hotspot-tool.js _execute → /geo/hotspot·threshold/soft_threshold 全程透传·默认 1.96/1.0 不破坏）

### 焦点 2：S-1~4 补证充分性
- S-1 CSV 单测是否覆盖 BOM/列/脱敏/空卡？（有无遗漏·如 filename 规范化 `_safe_name`）
- S-2 terrarium 解码是否覆盖 0~500m/bounds/尺寸？（PIL height,width 反序处理对不对）
- S-3 直测是否覆盖观点提取/无观点/4 要点/结论带地点/scale 三档/无地名降级？（P0-3 DOM 断言留回归期是否可接受）
- S-4 micro/unknown 分支是否覆盖？

### 焦点 3：P0-3 三路径 DOM 断言
- S-3 用纯函数直测 + 三路径接线代码核验，**DOM 出卡断言留给回归期浏览器抽验**——此边界可否接受？还是本轮必须补 DOM 断言？

### 焦点 4：回归衔接
- 修复验证通过后 → push + 进发版就绪度回归（fail 集判据 {PRM-03/04/07}）——顺序确认？
- 回归期浏览器抽验（三路径观点卡 + 无 console 错 + setTerrain 互斥）范围确认？

### 焦点 5：两组补充与异议
- 对修复/补证有无异议？（用错标尺/事实错误/承重红线→指出）
- 各自环境有无已跑验证可复用为复验证据？

---

## 附 A：修复验证 prompt（发两组用）

```text
【CB-18 · 整体验收修复验证】情绪地图 — W-1/W-2 修复 + S-1~4 补证复验

你以第三方独立验收身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·commit d5a5625 未 push·工作区已同步）。可跑只读测试（pytest / browser e2e-seam 直测）作为复验证据。

背景：CB-18 整体验收有条件通过 → claude组 修 W-1/W-2 + 补 S-1~4（commit d5a5625）。请对修复/补证独立复验。
必读：
  docs/catch-ball/discuss/整体验收_修复验证发起_2026-08-08.md（修复摘要 + 5 焦点）
  docs/catch-ball/discuss/整体验收_反评价收敛_2026-08-08.md（验收结论·W-1/W-2 + S-1~4 由来）
  08-05 审计修正记录（W1/W2/W3 原始要求）
按需核实：frontend/js/ai_qa/tools.js（hotspot docstring/observation/转发）·
  frontend/js/toolbox/hotspot-tool.js（_execute body）· frontend/js/e2e-seam.js（buildResultStruct 暴露）·
  tests/test_export.py · tests/test_spatial_analysis.py（terrarium）· tests/browser/test_result_struct.py ·
  tests/test_outlet_schema.py（geo_label micro）

复验任务（5 焦点·逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. W-1/W-2 修复正确性（红/绿残留全仓核对·threshold 转发链闭合·默认值不破坏）
2. S-1~4 补证充分性（CSV/terrarium/result-struct/geo_label micro·有无遗漏）
3. P0-3 三路径 DOM 断言边界（纯函数直测 + 接线核验 + 回归期浏览器抽验——可否接受？）
4. 回归衔接（修复验证通过 → push → 发版回归 fail 集判据 {PRM-03/04/07}·顺序/范围）
5. 两组补充与异议

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（契约单一源·Smart 两端 + Dumb 中间）。
承重红线：四态出口 / finalStep D019 / diagnose 永不动——本修复不触承重·确认无违反。
产出：docs/catch-ball/discuss/整体验收_修复验证回应_{你的组名}_2026-08-08.md
格式：〇 一句话结论 / 一 逐焦点 / 二 新问题风险清单（阻断/警告/建议）/ 三 可否整体验收通过 + push 结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-18 修复验证发起 · 2026-08-08*
*commit d5a5625 未 push·两组复验通过后 push + 进发版就绪度回归*
