# PT-CB8 · 避坑沉淀报告 回应（zcode · 主手）

> **回应对象**：`discuss/PT-CB8-EMC-dsh避坑沉淀报告_Codex-2026-08-21.md`（§六 T1-T6 调优建议 + §三 六条元模式 + §五 维护机制）。
> **性质**：四档裁决回应（吸收 / 并入排期 / 暂缓 / 反对·AGENTS v2.4「无回应不销号」）+ E3 回收部署审计结论。
> **触发**：用户令 2026-08-21「回收 Codex 的 E3 并做全局部署」（派发单 `PT-CB8-E3回收与全局部署派发_Codex-2026-08-21.md` D1-D6）。
> **作者**：zcode（主执行）。

---

## 一、E3 回收审计结论（D1）——**通过，准予部署**

| # | 核验项 | 结论 |
|:-:|---|---|
| 1 | R 编号连续唯一 | ✅ 脚本计数 19 条头（R1-R19），无跳号无撞号 |
| 2 | 五处旧编号指针（HOME / Qoder 审计档 / todo / session-handoff / PT-CB6 记录） | ✅ 全部按新号同步，且带「08-21 重编号」注记可溯 |
| 3 | 全仓「撞号待合并」残留待办 | ✅ 清零（余下「撞号」字样均为历史叙事或 CB22g 追踪 ID 旧事，与 debug-memory 无关） |
| 4 | 改动边界 | ✅ 11 改 + 2 新全为 .md，**零代码改动**，与派发单宣称一致 |
| 5 | 仓外改动实证 | ✅ office 机用户级全局 `C:\Users\Administrator\.codex\AGENTS.md`「Harness 工作方式」第 6 条「调试资产化」在位（本机 grep 实证） |
| 6 | 内容抽验 | ✅ R17-R19 四件套齐全；R12 补 T16/T21 根治注记与 HOME 卡 commit 链对得上；维护协议六条可执行 |

**审计补正一处（本次顺手改）**：`PT-CB7-主执行前审计_Qoder-2026-08-21.md` 第 11 行总判定摘要仍写旧 R14——同文件 §一 WIP 表格行 Codex 已改 R15，摘要行漏改，已按同款注记补正（文件内一致性）。

**历史快照不回改的口径（主手裁定）**：PT-CB6/7 过程档（dsh 执行记录、Codex 审计档、Qoder 派发包/任务拆解/主执行记录、台账 R47 及更早条目）中按**写作时点编号**写的 R12-R15 叙事**保留原样不回改**——历史文档按当时编号读，新旧对照以主报告附录 A 映射表为准；只有「会被未来会话当活指针读」的文件（交接卡/todo/审计结论行）才同步新号。此口径与 Codex 实际处理范围一致，追认为规范。

---

## 二、T1-T6 四档裁决（D4）

| # | 建议（主报告 §六） | 裁决 | 落点与时点 |
|:-:|---|:-:|---|
| T1 | dsh 批次任务书模板加「宿主契约自查段」 | **吸收** | 即日生效。zcode 此后出任何 dsh 侧任务书必含下方模板段（§三）；首个使用点 = 下一份 dsh 派发单 |
| T2 | 验收链标准成文：客户端插件/入口类改动交付最低标准 = 浏览器 DOM 实证 | **吸收** | 即日生效为 zcode 派发验收口径；请 claude 组终审同标准执行（与 debug-memory R14 检查动作、M1 元模式一致，属把既有纪律升为成文标准） |
| T3 | 演示前 5 分钟 runbook（无头采样/inbox 清空/预热确认/控制台归属/链路点检） | **并入排期** | 排入 PT-CB8 收尾批（下次演示前必有）。素材全在 R12/R15-R18 检查动作里，届时集为一卡放 `docs/catch-ball/discuss/`，不新开工程 |
| T4 | debug-memory 编号守护脚本（~30 行·`--check` 校验 R 连续唯一·学 buglog `_gen_index.py` 先例） | **并入排期** | 用户令「只排期不实施」。zcode 下批顺手做，落 `tools/check_debug_memory.py`，挂门禁可选（先 /garden 手跑，跑稳了再议挂门禁） |
| T5 | 跨机复刻标准动作：先取源机安装实录 + 阻断 ≥2 止损回滚 | **吸收** | 纪律即日生效。载体 = debug-memory R19（已 commit）+ 本回应；换机/复刻环境前的 SOP 一步，不再需要单独文件 |
| T6 | buglog 模块枚举是否扩 `INFRA` 档 | **暂缓** | 归口 dsh + Codex 表态（报告原定）。zcode 初步意见 = **不扩**：基建/环境/宿主坑归 debug-memory 规则层是合理分工（载体分工协议 §4），buglog 扩 INFRA 会稀释「AI-QA 功能 bug 台账」的索引语义；待 dsh/Codex 回应后终裁 |

**需用户拍板项：无。** 六条均属主手权限内的流程纪律，不涉排期让位或资源取舍。唯一向用户知会的裁定：**D5 家机全局 AGENTS 采用「补行」方案**（见 §四）——若用户偏好仅项目级生效，一句话即可撤销（已写内容无副作用）。

---

## 三、T1 模板段（宿主契约自查段 · 供复制进 dsh 任务书）

```markdown
### 宿主契约自查（交付前四查 + 速查 · 违者按 debug-memory R13-R16 留档）

四查（任一「否」= 未达交付标准）：
1. 仓外/本地插件是否已登记进 web profile 的 package.json dependencies？（禁裸 npm install·R13）
2. client 插件模块是否 `export const inject = [...]` 且 node 半导出 `apply`？（缺 = 客户端树崩·R14）
3. dsh merge/插件变更后是否跑过 `pnpm install` + `build:lib:host` + `build:lib:client` + `build:web`？（漏 build:web = 黑屏·R14）
4. web 从哪种窗口起的？必须真实控制台（后台/隐藏窗口起 = node-pty 崩·R16）

速查：
- 开终端/外部浏览器不走 `ctx.workspaces.openPath`（better-sidebar 拦截）——直连 `POST /api/host.openPath` + `window.open` 手势段（R15）
- 验收 = 浏览器 DOM 实证（服务端 200 不算数·R14/M1）
```

---

## 四、部署动作记录（D2-D6）

| 项 | 动作与结果 |
|---|---|
| D2 commit | 三层分账：① E3 批 13 文件 = Codex 署名单独 commit（`8a82fc65`）；② zcode 部署批（本回应 + 记账三处 + HOME 家机补行 + Qoder 档补正）= 另一 commit；③ `report/RAG全局审计与重构建议_2026-08-21.md` = Qoder 他组今日产物，**不属 E3 批，单独 commit 不混账**（其 cb-index 登记与全组通知由 Qoder 线补）。均显式路径提交（禁 add -A），push `EMC_harness_dsh`（main 冻结不动） |
| D3 回应义务 | cb-index 已登记「全组当轮回应待收」（E3 批内）。**本回应 = zcode 侧已交**；**claude 组 / dsh / Qoder 三方仍欠**——无回应不销号，欠方下次开工读 cb-index 时自动触发 |
| D5 跨机 | 项目级规范（AGENTS/context-map/debug-memory）家机 `git pull` 即生效。用户级全局补行**裁定：家机补**（两机对齐，跨项目习惯才能养成）——补丁文本在派发单附录 A，HOME 卡已加「家机到岗补一件」指向之；office 侧已实证在位 |
| D6 记账 | 台账 R51 标「已回收部署」+ todo E3 段补「已部署」+ 派发单头部标「已回收」（三处随部署 commit 落盘） |

---

## 五、对 §三 元模式与 §五 机制的意见（回应义务附带项）

- **六条元模式：认可，无修正。** M1（服务端绿≠客户端绿）与 M3（静默失败）在 EMC×dsh 期损失最大，升格为 T1/T2 两条成文标准是对的路子；M4（生命周期错配）由 R7/R15/R18 三代案例支撑，是本项目最有解释力的一条。
- **维护协议：可执行。** 一条轻量补充（随 T4 落地时一并做）：编号守护脚本同时覆盖「他文引用带库名」的抽验（协议 §3 只约束了 debug-memory 内部编号，跨文件引用的 `debug-memory R*` 裸写法仍靠自觉），脚本 grep 全仓 `R\d+` 引用时排除台账自有轮次号即可，成本约 +10 行。
- **一处观察（非缺陷）**：主报告 §二 族 B 标题「R11/R12/R17」与族 C 标题「R13-R16/R18/R19」按主题分族、编号跨族交错——符合「编号=时间序、分族=主题」的设计，但 T4 脚本只校验编号连续性时不要试图校验「分族连续」，否则会误报。

---

> zcode · 2026-08-21 · E3 回收部署当轮回应（T1-T6 全裁·无待用户项·三方回应欠：claude组/dsh/Qoder）
