# CB-39 实施计划（v3）· 验收意见（Codex 审计线 · 2026-08-16）

> 验收方：Codex 审计/评估线（区别于产出本计划的主开发 Codex 线）。方法：全文深读 + 与 claude组裁定（`CB38-EMC全局系统审计_评估_Claude-2026-08-16.md` §二/§三/§四）逐条对照 + 关键锚点与测试基线**实测复核**。
> 验收对象：`discuss/CB39-实施计划_Codex-2026-08-16.md`（v3）。

## 〇 结论

**验收通过，可进入实施**——D1-D9 九项裁定全部正确落位、三处交叉点时序与裁定一致、红线/决策点/风险表/DoD 完备。附 **3 项微修**（均为一句话级，不阻塞开工，建议主开发并入 v3.1 后开工）。

## 一 裁定落位对照（D1-D9 逐条核验）

| 裁定 | 计划落位 | 核验 |
|---|---|---|
| D1 L4（短期 c + 中期 a'·Smart-Dumb） | P0-1（phase 错标 + 标注未接入 + **key 空显式报错·修正机制·不补字段**）+ P2-1（`_attach_4x5_attrs` 转正 + `/aiqa/deep_attribution` lazy·不新建 LLM 通道）+ L0 骨架移 experimental | ✓ 全采纳，含 claude组 两处修正（api_key 机制 / L4 规则底盲区） |
| D2+E16 数据池归一 | A2 同批：performance 9 件迁移（同名三对不抢跑→B2 三证据裁决）+ PROCESSED_DIR 收敛 + 大小写 + DATA/README（真实池=analysis·演示池=performance·红线） | ✓ 交叉点 1 落实 |
| D3 仓库结构（时序后置） | R1 = C5 完成后归档；P1-6 先行 _tmp×9/README/畸形目录 | ✓ 交叉点 2/3 落实 |
| D4 validate_* 方案 b | P0-2：pytest.ini 追加 `validate_*.py` + SKILL_DEFS 解析 `stages.js:45` 真身 | ✓ |
| D5 可见性 | P1-4：全显默认 + visibilityPolicy 单开关 + 删死代码（state.js:1073） | ✓ |
| D6 并发 | P1-3：删根 serve.py + ThreadingHTTPServer + BackgroundTasks/task_id（不上队列） | ✓ |
| D7 环境 | P1-5：3.14 venv + 补装 + CI browser job | ✓ |
| D8 AGENTS 单一事实源 | P1-2：4 未注册 ID 修复 + gen_agents_table.py + CI 零偏差 | ✓ |
| D9 密钥 | P0-0：serve 加固（已完成·claude组 §六 复核通过）+ 用户轮换 + KNOWLEDGE 密钥条款 | ✓ |

**P1 排序**与裁定 §四完全一致（数据池→追踪止血→双入口/并发→可见性→环境→其余）。

## 二 实测复核（verify-before-accept）

| 项 | 计划声称 | 实测 | 结果 |
|---|---|---|---|
| 测试基线 | `py -m pytest tests/ -q` = 320 passed + 3 skipped | **321 passed + 3 skipped**（54.1s·exit 0） | ✓ 可达（数字差 1·见微修①） |
| 双解释器 | 单一 venv 对齐 3.14 | `py` 默认=3.14（matplotlib 3.11.1 在）；裸 `python`=3.13.2 | ✓ 漂移实锤；**门禁命令必须钉死 `py` 前缀**（微修②） |
| MOD_AIQA.F 最大号 | 19（新号 F_020 起） | 附C 实测一致 | ✓ |
| PROCESSED_DIR 指向不存在 DATA/processed | A2 项 | 本会话审计+claude组 双线独立确认 | ✓ 交叉印证 |

## 三 微修 3 项（不阻塞·建议 v3.1 并入）

1. **基线数字更新**：320→**321** passed（或改「≥320 passed + 3 skipped」表述，防后续新增测试造成假红）。
2. **门禁命令加注**：批次验收门明确「必须 `py -m pytest`（3.14）；**禁用裸 `python`（3.13·缺 matplotlib·实测 315 passed/3 failed/6 skipped）**」——双解释器漂移正是 P1-5 要治的病，实施期先钉死命令防误判。
3. **P1-2 顺带修 MOD_AIQA.F_004 编号空洞**（F_003→F_005 跳号·审计 F12·「连续不跳号」红线存量违例）——P1-2 本就动注册表，补齐成本≈0。

## 四 角色与边界说明

- 本计划由主开发 Codex 线产出并自我修订至 v3；本验收意见为审计线独立复核，不改动计划本体。
- 微修是否并入 v3.1 由主开发决定；并入后 CB-39 即可开工（P0-0 剩余两条 = 用户 key 轮换 + KNOWLEDGE 密钥条款，均为当日动作）。
- 决议落地前审计线维持只读（本轮唯一写入 = 本验收文件）。

—— Codex 审计线 · 2026-08-16
