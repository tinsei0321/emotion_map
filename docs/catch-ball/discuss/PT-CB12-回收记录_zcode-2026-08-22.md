# PT-CB12 · 回收记录（zcode 主手·2026-08-22）

## 件① guard 统一接线（Codex·3beb37ed）：**通过**

- 9 件带面输入工具入口统一 `_guard_check`（体内 `_reject_analysis_output` 直调全移除·单一通路）；`_GUARD_SPECS` 补齐 compare_regions('boundaries')/area_stats('boundary')；overlay 双参合一调用；表头注释更新「全量接线完成」；+1 未声明工具文档化断言。
- **真实链抽验**：zonal（boundary 面）/overlay（双参）/compare（逐区嵌套）三类入口对结论层均正确拒绝（hint 携带对应参数名）·正常路径不受影响。
- **claude 终审 N1 正式闭环**：挂表≠有守卫的落差消除·新工具有 B4 启动核对+文档化断言双兜底。

## 件② ACP 契约 v1（zcode·6737dfcc）：**已起草·随本批收口**

- `docs/acp-contract-v1.md`：四动词+五族事件（EMC 自持四阶段为词表第一候选）+最小状态对象+Codex v2 映射附录。只定义不实现·红线三条（不绑传输/不抄宿主字段/工具面归 MCP）。
- 承接用户 Harness 裁定（暂 dsh·契约成熟后再测 Codex）——v1 即「契约成熟」首块砖；评审意见后续批随实现补。

## 门禁与观察项

- **501 passed + 4 skipped**（三轮复跑确认；首次跑出现 1 例一次性失败·未捕获用例名即复绿——疑似新装依赖首跑预热类闪烁·**记观察项**：再现时抓名入 buglog）。
- 全批 DoD：件①行为零变化✓（既有 G-2 用例全绿）+1 断言✓；件②落盘✓零实现✓。

## 蒸馏

无新坑（接线为行为零变化重构·守卫真实链三入口实证）。

## PT-CB12 收口

两件齐·批收口。下一批候选（待用户排期）：RAG 重建（PT-CB9 原案）/ M2-M3 环境配方 / 进度契约合并版 / dsh 工具面打磨挂账件。

> zcode 主手 · 2026-08-22 · PT-CB12 收口
