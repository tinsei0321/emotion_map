# CB-22d · 知识问答 → 地图标记 — 回应（Codex-GPT5 · 2026-08-10）

> **回应方**：Codex 组（第三方评估·GPT-5）| **日期**：2026-08-10 | **性质**：CB 讨论轮独立取证回应
> **依据**：讨论发起文档 + 代码路径逐链路复算（panel/harness/stages/tools/geocode/shared/prompts/paradigm/tool_contracts/map）+ trace 检索 + preset 盘点。未做任何 git 操作。

---

## 〇、一句话结论

**agree claude组 根因判定（意图路由落空 → C2 request_upload），并补一层：真正的根是「缺一个 names[]→点图层 工具」——C2 报上传只是该缺失在需点层工具上的出口表象；另有两个本地 trace 无法直接证实的诚实声明与一个数据面补充（面化回退依赖的面 preset 当前不完整）。解决 = P0 新工具 generate_point_layer（intent 仍 gis_operation·不动 knowledge_qa 成功路径）+ 最小路由使能 + diagnose 增量豁免记录；P1 项目库坐标抽取。**

---

## 一、根因独立判定（含 trace 取证）

### 1.1 代码路径复算（对用户句「能在地图上标记出这些项目的位置吗？」逐链路验证）

| # | 链路 | 代码证据 | 结果 |
|---|---|---|---|
| 1 | 续作识别 | `panel.js:1629` `_isResumeCue` = `/继续|接着|续做|补充|那个|上一个|把刚才/` → 句不含任何命中词 → `panel.js:1696` `ctx.resume = false` | ✅ claude组 判定成立 |
| 2 | _quickIntent 加速器 | `harness.js:60`：CONCEPT_KW 无 / INVENTORY_KW 无 / GEO_VERB_KW（核密度·热力·裁出·出图·生成图·地点·在哪·坐标…）无 / RAG_QUERY_KW（有哪些项目·更新项目…）**无**——句为「这些项目」非精确子串 / REGION_KW 无（未重复宜昌）→ 返回 null → 落 diagnose | ✅ 成立（词表见 `emc-patterns.js`） |
| 3 | 前置 L0 数据门 | `harness.js:1115`/`:1567` `_dataGate`：仅 用地/地块/土地 与 情绪点/点位 两类正则 → 句不含 → **不触发** | ⚠️ 补充：可排除该源头，用户所见 request_upload 不是来自此门 |
| 4 | C2 数据闸门 | `stages.js:362` `_NEEDS_POINT = density\|hotspot\|rank\|zonal_stats\|clip\|buffer\|nearest` + `layerMeta.has_point=false` → `strategy='request_upload'` → `harness.js:1221` 短路出「请求上传」结论 | ✅ 成立（上轮知识问答零图层→has_point=false 必然命中） |
| 5 | 工具层缺口 | `tools.js:823` TOOLS 共 19 工具（17 个 async 工具 + query_layers + query_zone_stats）全为「已有图层/单点/单区名」；`tools.js:1295` lookup_place 单点查询不产图层；`stages.js:45` SKILL_DEFS / `ai_qa/paradigm.py:367` TEMPLATE_REGISTRY 无「项目名→点位」技能 | ✅ 成立 |
| 6 | 数据层口径 | RAG fact 卡确为聚合文字（URP-P01「55 个·污水 16/葛洲坝 12/其他 12」·无坐标）；`core/geocode.py:188` search_place = 本地 POI（4310 条）+ 高德 place/text 兜底，片区名 fuzzy 大概率错位 | ✅ 成立 |

### 1.2 trace 取证 — 诚实声明

- 本地 `.trace/` 仅 `trace.log`（覆盖至 2026-08-09 23:58）+ `trace.log.1`（截至 08-02）。对「标记 / request_upload / gis_operation / knowledge_qa / 项目有哪些」全仓检索 **0 命中**——**用户复现会话不在本工作区 trace 中**，无法从 trace 直接证实「intent=gis_operation + request_upload」的实跑取值。
- 结论因此基于 **代码路径确定性**（上表每条链路均可独立复算）：只要 diagnose 判定 gis_operation 且模板落到任一 _NEEDS_POINT 工具，`request_upload` 是**必然出口**；若 LLM 选 lookup_place 则出口为「地点@坐标」单点查询（也不产图层）。两出口共同证明「决策路径缺失」，与用户判断（非地点精度问题）一致。
- 建议：claude组 若有该会话 trace 请补；或按「三组并行测试新规」复现一次带 `EMOTION_TRACE_SESSION` 取证（实施验证阶段自然覆盖）。

### 1.3 对 claude组 判定的补充/修正

1. **「C2 request_upload」是果不是因**：真正缺口 = 无「批量项目名→点图层」工具。即便没有 C2，缺工具也只会换一种失败形态（lookup_place 单点 / while-loop 乱探）。根因表述建议统一为「**缺意图→技能→工具的完整决策路径**」，C2 只是该缺失在数据门上的表现。
2. **数据面补充（面化可行性盘点）**：`DATA/boundaries/presets/` 现含 行政区/社区/大南门二马路滨江片区/用地×3；presets manifest 引用的 `街道.geojson` 与 `更新单元.geojson` **文件当前缺失**（available=false）。即「大南门·二马路滨江片区」（用户例中红星路-二马路街区）**可面化**；「葛洲坝片区」无街道面 → 只能退 行政区面（西陵区）+ 名称标注。面化方案须按此现实设计，勿假设街道/更新单元面可用。

---

## 二、逐焦点

### 焦点 1：根因判定 — **agree（核心成立）+ 补充 2 条**

- agree：意图层（续作未触发 + _quickIntent miss + 无技能）+ 数据闸门（C2 request_upload）双链成立，逐链路复算见 1.1。
- 补充 ①：工具层缺口是**上游根**（见 1.3-1）；补充 ②：trace 本地不可得，需诚实标注（见 1.2）。
- 修正 ③（小）：claude组 2.1 写「diagnose 判定偏 gis_operation」——本地无 trace 证实实判值；但**无论判 gis_operation 还是 knowledge_qa/unknown，当前都不存在成功出口**（knowledge_qa 禁图层·unknown 无技能），故不影响根因成立。

### 焦点 2：解决方向 — **B 为主 · A 为路由使能 · C 为 P1 数据增强 · 面化必须**

- **优先级：P0 = B（新工具）+ A 最小使能；P1 = C**。B 零数据依赖、纯装配（`/api/v1/place/search` 路由已就绪 `api/routes.py:440`·落图基建已就绪 `toolbox/shared.js` addToolboxLayer + `map.js:516/635` point circle 渲染），一次打通「知识问答→图层」闭环，直接服务演示逻辑链（图面张力→引导点击→定位关注区）。
- **A 单独不足**：意图通了但无工具可执行（现状即证明）；且「这些项目」指代解析必须由 LLM 从对话历史提取 names（Smart 两端）+ 工具确定性合成落图（Dumb 中间）+ 编排器确定性（零图层守卫/诚实 observation），正合 AI·Copilot 内核。
- **C 的价值边界**：55 个项目逐个地址→geocode 是「项目级精确点位」的唯一来源，但成本高且**非本轮缺口必需**（用户已接受模糊→大致区域/面）。C 与 B 叠加方式 = B 的解析顺序改为「项目点位源（若建）→ 通用 POI」。
- **面化必须（聚焦 2 子问题）**：三级回退，逐级诚实降级——① search_place 命中 → 点；② 未命中但为已知片区/行政区名 → 面/点位标注（大南门·二马路滨江片区 preset 可用；葛洲坝片区退西陵区面 + 名称标注「位于西陵区·葛洲坝街道范围」）；③ 聚合类无地点（如「污水厂网一体示范区 16 个」）→ 文字列「未匹配到坐标」+ 原因，**绝不 request_upload**。用户原话「模糊也应出大致区域/面而非拒答」= ② 是硬要求，③ 是诚实底线。

### 焦点 3：红线边界 — **独立新技能（gis_operation 下）· 不动 knowledge_qa 成功路径 · diagnose 增量豁免需走 3 条件**

- **否决「knowledge_qa 内部加图层分支」**：knowledge_qa 成功路径的「严禁图层」是已测试契约（`harness.js` `_assembleKnowledgeQA` 注入「本次为知识问答·严禁图层/分析模板」+ `tests/browser/test_rag_emc_e2e.py` 断言「不要生成分析图层」+ `tests/validate_paradigm_map.py` 三支柱契约）。内部加分支会模糊「知识问答=纯文字」边界，给 CB-22「图层带偏/零 LLM」教训回潮留口子。
- **不新增 intent 枚举**：新技能归 `gis_operation`（它本就是「操作→生成图层」语义）。好处：PARADIGM_MAP / `validate_paradigm_map._REQUIRED_KEYS` / 四态出口 / knowledge_qa 合流分支全部不动。
- **diagnose prompt 需要「增量」动（无法完全避免）**，共 3 处，全为追加不改旧：
  ① `ai_qa/paradigm.py:367` TEMPLATE_REGISTRY 新增 generate_point_layer 条目（自动进 `template_id_list_text` + 技能目录附录）；
  ② `ai_qa/prompts.py:267` 附近「选择要点·铁律」硬编码行 `intent=gis_operation→density/rank/buffer/clip/overlay/nearest/hotspot/area_stats/merge/extract_feature 之一` 追加新技能；
  ③ `ai_qa/paradigm.py:491` DIAGNOSE_CARD_FIELDS template 说明行追加。
  按 CB-22 knowledge_qa 增量豁免先例走 3 条件：**① 增量不改不删现有文本 ② eval 复采通过（含模板命中 gate 0.6 + Flash eval）③ 静态断言守现有判据文本（`tests/test_emc_template.py` 既有断言不删·新增新技能断言）**，并在 KNOWLEDGE §1 登记豁免。
- **FINAL_TEMPLATE 不必动**：新技能出口走 runTemplatePath→finalStep 既有管线（D019 不变）；胶囊技能清单不含新技能即可，若要支持 L1 追问胶囊属后续增量、非必须。
- **追踪编号**：新工具若新增后端批量端点须 `@track` + `register_track_id` 连续分配；纯前端工具至少补 console/事件埋点（tool:executed 已有派发模式可复用）。

### 焦点 4：验收口径

**成功标准（分层）**

| 层 | 标准 |
|---|---|
| P0 主场景 | 问「宜昌市城市更新项目有哪些？」→ 追问「能在地图上标记出这些项目的位置吗？」→ **产出点图层（≥1 命中）** + finalStep 诚实说明（命中 n/N·未命中名称文字列出·来源标注 search_place 本地/高德·不越维：片区级名称不冒充项目级坐标） |
| 模糊场景 | 片区名 → 点或面标注（大南门·二马路滨江片区可面化）；全未命中 → 不 request_upload，文字「知识库未收录该名称坐标」 |
| 红线回归 | knowledge_qa 文字通道不变（禁图层注入仍生效）；diagnose 既有判据文本不变；PARADIGM_MAP/四态出口/finalStep D019 不变；黄金集 recall/越维/案例 100% 不退化 |

**测试矩阵（确定性优先·仿 injectOnly 模式去 LLM 依赖）**

1. 工具单测/e2e-seam：stub `/api/v1/place/search`（全命中/部分命中/全未命中）→ 断言 fc 合成（点要素 + properties name/source）、落图（addToolboxLayer→addLayer）、observation 命中计数+未命中列表、全未命中零图层诚实出口。
2. 路由负例：确保「标记」句不误触发 rag_query/general（仍落 diagnose）；resume+knowledge_qa+「标记」句的上下文提示生效（若有）。
3. diagnose 静态断言：`tests/test_emc_template.py` 追加 generate_point_layer 断言（增量·旧断言不动）。
4. B3 浏览器回归：新增用例（先知识问答→追问标记→断言图层 + 答案含命中/未命中说明）；跑 `tests/browser/test_rag_emc_e2e.py` 全量 + `tests/validate_skill_params.py`（三处镜像一致）。
5. 用户验收：真实数据手动复测追问全链。

---

## 三、plan 建议

### P0（本轮闭环 · 按序实施）

- **P0-1 工具层（核心）**：`tools.js` 新增 `generate_point_layer({names, as})` —— 逐名 `GET /api/v1/place/search`（limit=1~3 首命中）→ 合成 point FeatureCollection（properties: name/address/source/category/score）→ `addToolboxLayer({kind:'point'·固定点样式})` → observation「命中 n/N + 未命中列表」；**全未命中 → 零图层 + 诚实文字（不 request_upload）**；同步 `stages.js` SKILL_DEFS 镜像（required_slots: ['names']）。
- **P0-2 路由使能（最小）**：`harness.js` orchestrate 增量：`ctx.resume && priorTurn.intent==='knowledge_qa' && /标记|标到地图|在地图上|点位/` → 上下文提示「选 generate_point_layer 并从上轮回答提取项目/片区名」（**上下文注入·不改 diagnose 文本**）；本句（无续作词）天然落 diagnose 由 LLM 判新技能；`_NEEDS_POINT` 不含新工具 → C2 天然豁免。
- **P0-3 契约+镜像+豁免**：`tool_contracts.py` 新增条目（when 含「标记/地图/项目名→点位」·failure_modes 含「names 缺失→提示从上一轮提取」）→ paradigm TEMPLATE_REGISTRY + GEO_TOOL_CATALOG 镜像 → prompts.py/DIAGNOSE_CARD_FIELDS 增量 → `validate_skill_params.py` 同步 → **KNOWLEDGE §1 登记 diagnose 增量豁免（3 条件）**。
- **P0-4 测试与验收**：焦点 4 测试矩阵 1-4 全绿 + 黄金集回归 + 用户验收（真实数据复测）。

### P1（数据增强 · 可叠加）

- 附件5-城市更新项目库.docx 抽取（项目名/地址/片区）→ geocode（AMAP·KEY 已配）→ 建「城市更新项目点位」源（place_layer 或独立注册表）→ generate_point_layer 优先命中该源 → 命中率/Recall 可测（仿黄金集机制）。

### 克制清单（勿做）

- 不改 knowledge_qa 成功路径/禁图层注入；不新增 intent 枚举；不动 PARADIGM_MAP/四态出口/finalStep D019。
- 不把「55 个」聚合口径冒充 55 个点位（越维）；输出按可命中粒度标注（片区级点/面 vs 项目级点）。
- 未命中不编造坐标（search_place 无命中即文字诚实出口）。

### 交叉挑战（KNOWLEDGE §7.5）

- **挑战 A（对 claude组 方向 A 的续作正则扩展）**：不建议简单把「标记/地图」加进 `_isResumeCue`——resume=true 时 harness 会跳过 knowledge_qa 合流分支（`harness.js:1204` 的 `!ctx.resume` 守卫），若 diagnose 仍判 knowledge_qa 会落入无出口路径；续作语义应放在「上下文提示 + 新技能选型」而非翻转 resume 布尔。
- **确认 B+C 叠加**：方向 B 与 C 不互斥，且 C 建成后 B 的解析顺序升级即可，无架构返工。
- **挑战 2（对「纯装配约 30 行」）**：同意主体，但需预留 3 个非装配点——点图层 paint（`defaultPaint` 无 point 分支·需显式 circle 样式）、N 个名字的并发/超时策略（55 名顺序请求过慢·建议 Promise.all + 限流或后端 batch 端点）、未命中名称的**来源可读标注**（防 LLM 自创地址）。

---

*Codex 组 · CB-22d 知识问答→地图标记 独立取证回应 · 2026-08-10*
*评估方只读本地不 git · 回应落 discuss/ · claude组 收到后 /cb 反评价收敛 → 定稿 plan*
