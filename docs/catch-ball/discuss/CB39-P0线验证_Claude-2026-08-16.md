# CB-39 · P0 线实施验证 · claude组 报告（2026-08-16）

> 验证方：claude组（只读·含一次受控试红并还原）。对象：`713e1ad9`（P0-0 收尾+P0-2）/ `7b2c3ead`（P0-1）/ `9ceff715`（归档）。
> 方法：基线复跑 + 逐项代码级核验 + 突变测试（mutation test）+ worktree 考古。

---

## 〇 一句话结论

**有条件通过**——P0 三批核心质量过硬（基线 367+2 复现、42+4 账目吻合、试红实证守卫有效、三断言非同义反复、15 ID 原号注册准确），**无 P0 打回项**；附 1 项 P1 修正（'街道'识别收窄）+ 2 项 P2 顺手项（F_004 注释缺·DATA/processed 空目录落地）——**修复后即可进 A 线**（P1 项不阻塞 A1-A5 开工，可与 A 线并行）。

---

## 一 逐批验证表

### 基础复跑

| 项 | 声称 | 实测 | 判定 |
|---|---|---|---|
| pytest 基线 | 367 passed + 2 skipped | **367+2 复现**（39.4s） | ✓ |
| 守卫收集 | 9 个 validate_* 进收集 | collect-only 含 9 个·单文件真跑测试数（track_ids 8/skill_params 9/outlet_fields 7）非空收集 | ✓ |
| 基线账目 | 321+3→363+3→367+2 | P0-2 +42（守卫42测）·P0-1 +4（新断言）吻合 | ✓ |
| xfail | 零 | 全 tests/ 零 xfail | ✓ 无静默豁免 |

### P0-2 守卫通电

| 验证点 | 结果 |
|---|---|
| 15 ID 原号补注册 | ✓ llm F_007(:468)/D_004(:527)、urban_renewal F_018(:26)、spatial D_004(:1226) **原号未改号**·描述准确；MOD_PERF 11 个改 @track 字面量装饰 + register 字面量双轨（:715 起·静态扫描两侧可见）闭环 |
| SKILL_DEFS 真身解析 | ✓ **突变测试实证**：改 stages.js `rank.top_n 5→7` → `test_skill_defs_mirror` 立即 FAILED；还原后 4 passed（试红后工作区 git checkout 还原干净·守卫复绿验证） |
| compare 漂移方向 | ✓ **正确**：contracts（权威）compare.agg_cols default=`['score','polarity_index']`·stages.js 补的值与权威一致——是 stages 对齐 contracts，非反向洗白 |
| `_FORM_FIELDS` 放行正当性 | ✓ **守卫语义修正非洗白**：对照 [urban_checkup_outlets.py:60-63](ai_qa/outlet_kb/urban_checkup_outlets.py#L60) 原文，field_mapping 值确为「形态字段（全角括号说明）」格式（`'统计表': 'stat_table（zonal 聚合·{col}_sum 总量口径）'`）——形态声明非情绪聚合字段引用，剥离括号注释+放行正确·注释理由如实 |
| 闸门 `test_used_track_ids_are_registered` | ✓ 逻辑成立（registered ⊇ used 断言）；**可绕形态确认存在**（见问题 P3-3） |
| MOD_PERF F_007 空洞注释 | ✓ 如实（:714「历史空洞·全仓零使用·ID 不重分配·同 MOD_AIQA.F_004 惯例」） |

### P0-1 诚实度修复

| 验证点 | 结果 |
|---|---|
| phase 机制 | ✓ `df.attrs['phase']` 锚点 L2→L3 就地升级（无 df 重建·attrs 存活）+ 导出侧 `or 'L2'` 兜底——当前管线形态下可靠；未来若管线重排引入 df 重建（reset_index/concat 等）attrs 会丢，P3 提示 |
| key 空显式报错 | ✓ `_eff_key` 四源合并（l3/l4/api/env）；空 → `success=False`（:1080 初始化保证）+ 三路径可操作文案 + 零产物（csv_path 空）——**完全符合 D9/claude组 修正裁定**（拒绝静默降级） |
| routes 打通 | ✓ `l3_api_key=req.api_key` + `l4_api_key=req.api_key`（schema 单 key 双驱·与 schemas 描述一致）+ AnalysisResponse.phase 向后兼容新增 |
| L4 显式跳过 | ✓ 不崩溃不假跑：safe_print [WARN]（:1425）+ `trace_warn('MOD_ANA.D_004', ...)`（:1426）·标签停留实际层级 |
| 三断言非同义反复 | ✓ 断言①真跑 key 检查分支（无核心 mock）；断言② mock 边界正确（mock 上游管道+下游导出·**被测的正是 attrs→phase_label→export 参数传递链**——改回硬编码 'L4' 必红）；局限=attrs 设置端被 mock（由代码审查覆盖） |
| L0 隔离 | ✓ 四件移 `SCRAPER/experimental/` + SPIDER_MODULES=[]·`spiders.` 活引用全仓 grep 零命中 |
| 第三个 skip 转绿 | **已定位**：`test_pii_guard.py:38`（模块级「DATA/processed 不存在」skip）——worktree(713e1ad9) 实测三 skip = pii:38 + pii:42 + range:25；**P0-1 新测试跑管道触发 `makedirs(PROCESSED_DIR)` 副作用，DATA/processed 空目录落地（13:44 实证）→ :38 条件通过只剩 :42 空参数**。归因=测试副作用建目录，非 skip 语义被改（见 P2-2） |

---

## 二 问题清单

| # | 级 | 问题 | 复现/证据 | 建议 |
|---|---|---|---|---|
| P1-1 | **P1** | **field_dictionary '街道'→'街办' 识别收窄**：boundary_name variants 删掉 '街道'——variants 是**数据层字段识别字典**（用户上传 shp/geojson 列名叫'街道'的真实常见），术语统一「街道→街办」是**表述层**纪律（铁律原文「数据层字段不改·仅表述层」+ AutoMemory risk-theme-naming 同界）；识别字典应超集，删词=外部数据 nameField 识别退化 | [field_dictionary.js:29](frontend/js/field_dictionary.js#L29) diff | **'街道'与'街办'并存**（识别宽进·表述统一不变）——一行修复，不阻塞 A 线，建议随 A 线首批顺手 |
| P2-1 | P2 | **MOD_AIQA.F_004 空洞未按 V10 处理**：v3.1 变更单 V10 要求「顺带修 F_004 空洞」——MOD_PERF.F_007 有注释（:714），F_004 本尊在 ai_qa/ **无等价注释**（grep 零命中），仅被他处引用为惯例 | grep ai_qa/ 'F_004' 零命中 | 补一行注释（「历史空洞·零使用·不重分配」）——P1-2 批顺手 |
| P2-2 | P2 | **DATA/processed 空目录落地**（P0-1 测试 makedirs 副作用·13:44）：审计 P1-1「数据池再裂」的种子已播——下次跑分析会往里写，与 A2 批「PROCESSED_DIR 单源收敛」冲突 | `ls DATA/processed` = 空目录实存 | ① A2 批将 PROCESSED_DIR 收敛时**删除该空目录**并写入处置记录；② test_pipeline_honesty 的导出路径改 `tmp_path` 隔离（消测试副作用） |
| P3-1 | P3 | 闸门绕过形态：`_first_str_arg` 只收字符串字面量——`track(f'MOD_X.F_{n}')`/`trace_log(prefix+code)` 动态拼接 ID 不被收集（usage 侧漏检） | [validate_track_ids.py:133-149](tests/validate_track_ids.py#L133) | 闸门加反向警告：track/trace_* 首参为非字面量且含 ID 形态 → 输出「疑似动态 ID 拼接」告警（不 fail） |
| P3-2 | P3 | df.attrs 传递脆弱性（pandas 实验性·df 重建即丢）：当前管线就地增强安全；未来管线重构需警觉 | 机制固有 | phase_label 兜底已有 `or 'L2'`·可在 B 线文档注记 |
| P3-3 | P3 | 验证过程瑕疵（claude组自记）：worktree 考古时 4 failed 未及留名即清理（评估=缺本机 188MB boundaries/presets 所致·真机零失败为准）——方法论教训：破坏性验证先抓证据再清理 | 本报告 §一 | — |

---

## 三 对 A 线开工的前置提醒

1. **A2 批新增两条**（并入原批次）：删除 DATA/processed 空目录（P2-2·与 PROCESSED_DIR 收敛同批天然合适）；field_dictionary '街道'/'街办' 并存修复（P1-1·一行）。
2. **A1 引用盘点注意**：SCRAPER 已动过（experimental 迁移）——A1 的活引用 grep 基线以 `7b2c3ead` 后状态为准。
3. **基线口径**：A 线各批验收基线 = **367+2**（当前阶段基线·V4 条款）；P1-5 切 3.14 venv 时再重录。
4. P2-1（F_004 注释）挂 P1-2 批；P3-1（动态 ID 告警）建议并入 gen_agents_table 同期。

---

**结论：有条件通过——1 P1 + 2 P2 + 3 P3，全部可随 A 线批次吸收，无打回项；Codex 可进 A1。**

—— claude组 · 2026-08-16 · 只读验证·未 git 写（除试红受控突变+还原·已验证还原干净）
