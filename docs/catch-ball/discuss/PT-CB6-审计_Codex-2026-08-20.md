# PT-CB6 期 · home 班全量审计（Codex·2026-08-20）

> 审计方：Codex（零实施·只读）。签发：zcode 主手（审计任务书 `PT-CB6-审计任务书_zcode-2026-08-20.md`）。
> 对象：2026-08-19 晚 ~ 08-20 凌晨 home 班全部工作（commit `7a04aaf5..0278f083`）。
> 纪律：零实施零修改；产出仅本报告一份；commit/push 由用户决定。

---

## 〇 范围口径校准（先说明·审计前置）

任务书称范围 = **34 提交·48 文件·+3817 行**；实测：

| 口径 | 任务书声称 | 实测 | 判定 |
|---|---|---|---|
| 提交数（7a04aaf5..0278f083） | 34 | **43**（无 merge·43 全线性） | 任务书数错 |
| 文件数 | 48 | **48** | 一致 |
| 行数 | +3817 | **+3659 / −45**（净 +3614） | 任务书数错 |
| 34 提交的另一种解释 | — | `1164b754..0278f083` = 34 提交（33 文件/+2667） | 与「48 文件」不匹配 |

**结论**：任务书两处数字自相矛盾（34 提交 ↔ 48 文件）。本次审计按**全范围 `7a04aaf5..0278f083`（43 提交·48 文件）**执行——PT-CB4 T1/T2/T3 提交（前 9 个）也在范围内，与任务书「PT-CB4 抽验」要求一致。此差异记为文书级 note（不影响审计结论）。

---

## 一 总裁定（四线）

| 线 | 结论 | 一句话依据 |
|---|---|---|
| **PT-CB6 主体全审** | **通过（附新缺陷 D9-D13 待处置）** | 三砖+P+ 代码与任务书规格逐条吻合；大脑实测链文档一致、Q2 主验收经 S6 用户复测闭环；S6 三坑修复代码实证有效；S7 判读链（R43→R44）自洽。新发现 5 项缺陷均 P2/P3，无 P1，不推翻主验收成立 |
| **PT-CB5 补审** | **通过** | G-2 穿透洞独立复测三工具全拒（含 trace 快返实证）；利益冲突四项声明逐条复核成立；A9 已固化且 PT-CB6 新代码无复发（仅留 1 处已知 fail-open 待下批顺修）；七工具真身签名五锚点全部对上 |
| **PT-CB4 抽验** | **通过** | T1 抽验两组复现属实（①占比表 analysis 137 社区现行/伪社区行已归档 ③三件 _retired+retired.md 登记）；T2 抽 3 张卡字段齐全、值锚点均指成文出处；presets 同步批挂账在档（总账+口径注册表两处）；check_caliber 6 命中基线实测复现 |
| **B1 复审** | **通过·正式销号** | 七项修复（H1/H2/M1/L1-L4）代码现状逐一复核全部在位；H1 随层标记在 PT-CB6 前端改动后仍生效；M1 在 MCP 面新增后未被绕过（MCP 自带服务端守卫）；三轮全量门禁持续回归（389+3→427+2）佐证 |

---

## 二 PT-CB6 主体审计要点逐条结论

### 2.1 P 先行件三砖（代码审计核心）

| # | 要点 | 结论 | 证据 |
|---|---|---|---|
| 3.1-1 | render_spec 第 8 插座（F_028）：契约关系/显示放行边界/渲染契约 v1 字段 | **PASS** | `render_spec` 按任务书 §一 契约 v1+P+ 修订实现：spec_version/spec_id/kind/data/style.scheme/ui/origin/caliber_lite 全字段与规格一致；`caliber.refs=['G-2(显示徽标)','渲染契约v1']` 与任务书逐字一致（[mcp_server_emc.py](tools/mcp_server_emc.py:514)）；dataset 路从 manifest 取 usage 落 caliber_lite（显示放行·非分析输入·语义边界正确）；P+ 升级（scheme/data_nature/community）与旧 token 兼容读均落地（[render_client.js](frontend/js/render_client.js:20)） |
| 3.1-2 | 投递通道（F_029/F_030）：watcher+SSE+dataset 端点 | **PASS（附 D9/D13 与 notes）** | watcher 按文件名排序+last_seen 去重+坏文件 log 跳过（A9·非静默）；SSE 先推 backlog 最近 20 条+15s 心跳；dataset 端点 id 白名单（manifest preset/点层注册表·无路径越界）、>2000 降级、未知 id 语义化 hint（[render_routes.py](api/render_routes.py:44)）。F_029 已注册但未挂 `@track`（D9·执行记录 §五.2 为有意设计待主手裁决）；两处 print 未走 _safe_print（D13）；inbox 无自动清理补偿（notes） |
| 3.1-3 | 前端 loader：令牌解析权威 JS/去重/S6 三修复 | **PASS（附 D10）** | SCHEMES 词表注册在前端、未知 scheme 拒渲染（权威留 JS·服务端零样式逻辑）；`_seenSpecIds` 会话级去重；S6 三修复代码复核通过（[render_client.js](frontend/js/render_client.js:8)、[serve.py](frontend/serve.py:449) ThreadingTCPServer+daemon_threads）。缺口：choropleth 无条件走计数着色，polarity 字段（render_spec 默认 value_field）会语义错位渲染（D10） |
| 3.1-4 | P+ 增补：scheme 词表（sequential 计数修正·piToNorm 极性缺口·CB-41 对照） | **PASS（缺口确认=D10）** | 计数着色复用 `defaultPaint('zonal','polygon','count')`+`countStops()` 同源路径（[shared.js](frontend/js/toolbox/shared.js:138)），未另起一套、未用 piToNorm——CB-41 同源精神成立；`_count_norm=log1p 归一` 与 shared.js buildZonalFc semantic='count' 同公式（独立实现·建议后续抽公共函数防双源漂移·notes）。piToNorm 极性专用 scheme 确为 v1 缺口（P+ 任务书 +1 明示），且 render_spec 默认 value_field=polarity_index 使该缺口成为可踩的 footgun（D10） |
| 3.1-5 | data_nature 徽标三处透传 | **PASS** | list_data 点层按池路径判 demo/real+preset 透传（[mcp_server_emc.py](tools/mcp_server_emc.py:210)）；render_spec caliber_lite.data_nature（dataset 路池路径判定覆盖调用方声明·inline 由调用方声明）；前端 `[dsh] [真实]/[演示]` 徽标（[render_client.js](frontend/js/render_client.js:85)）；测试两用例补挂（test_list_data_data_nature_and_preset_passthrough / test_render_spec_scheme_registry_and_inline_nature） |
| 3.1-6 | demo_pioneer 脚本 | **PASS（形态）·FAIL（语义·D12）** | 脚本结构/174 preset 实查（checkup_cfg_community）/3/3 落盘回执均属实（P+ 执行记录 §六）；但「12345热线诉求最密集社区(真实)」名不副实——zonal_stats 默认按 polarity_index 排序（D1），渲染的是极性 Top10 而非件数 Top10（D12） |
| 3.1-7 | community 口径字段（174/154/118 防混图） | **PASS（机制落地）·FAIL（口径校验缺失·D11）** | community_caliber 参数+caliber_lite.community 落地、demo 三图传 174；但参数无 K-C1 校验、dataset 路无默认 scope 推导——S6 用户复测 spec 已实证 174 标注在 154 矩阵派生层（page7_12345_top10）上（D11）；K-C1 未登记 118 而 P+ 任务书枚举含 118（notes·口径注册表小缺口） |
| 3.1-8 | 测试 tests/test_render_channel.py（215 行） | **PASS** | 8 用例覆盖任务书 6 组规格（spec 契约/inline 上限/data 双缺/dataset 校验/layer_output 数量与默认零变化/watcher 顺序+坏文件/端点 fc/未知 hint）；断言行为与契约（非与实现同构·R8 通过）；watcher 用例为真实函数+临时目录（非 mock 自证）；G-2 显示放行断言 `caliber_lite.usage=analysis_output` 为关键正断言 |

### 2.2 大脑实测链（文档与真实行为审计）

| # | 要点 | 结论 | 证据/说明 |
|---|---|---|---|
| 3.2-1 | 任务书五层模型 vs 实测记录一致性 | **PASS** | 实测记录观察表逐题（工具序列/参数正误/轮次/报错/口径/落图）与任务书模板一致；L0 八工具与 server 8 工具数一致（七件+render_spec）；偏离项均如实标注（L2 未走 layer_output 内联路·Q3 选 point 方案）而非事后粉饰 |
| 3.2-2 | L0-L4 判定依据充分性 | **PASS** | L0（配置指引 S3 八工具清单）/L1（首工具 list_data→kb_facts、list_data→zonal）/L2（spec 落盘+trace+答卷）/L3（两图 spec 落盘+对比结论）/L4（自述铁律7+trace 0.2-0.7ms 快返=守卫路径——与本次独立复测量级一致）均有实证 |
| 3.2-3 | Q2 主验收证据链完整 | **PASS** | dsh 侧成答+spec 落盘（1787160303627-3328）→ S6 用户亲问 Q2 原话端到端→三坑修复→无头采样图层恒 1→用户确认亮图。链完整闭环 |
| 3.2-4 | Q4 对抗样本缺位 | **PASS（待办仍有效）** | 大脑自选 input 边界未按题面撞 G-2；S6 用户复测用 Q2 原话（非 Q4）；「题面明写 base_174_aggregate_area」待办在 R44/todo 中仍登记·**继续有效**（本次审计亦未代为采集——零实施纪律） |
| 3.2-5 | D1-D5 定级合理性 + D6-D8 销号 | **PASS** | D1 P1（zonal 缺 point_count 排序·代码实证 sort_col 默认 polarity_index/score_sum·todo.md:47 已明文警示）合理；D2/D3 P2（描述引导）合理；D4（stdout→stderr）已裁决仍待做（[mcp_server_emc.py](tools/mcp_server_emc.py:621) main() 仍 print 至 stdout）合理；D5 已销（环境项）；D6-D8 代码复核全部在位（ThreadingTCPServer/_seenSpecIds/backlog 截断）→ 销号成立 |
| 3.2-6 | 大脑答卷质量独立评估（R43「超预期」是否过度乐观） | **PASS（不乐观）** | 独立复算：Q2 Top10（朝阳路 594/万达 501/港务 417/建设 372/岳湾路 225/大学路 222/伍临路 221/东城 191/张家湾 187/宝联 156）与 `12345_社区x9类_全覆盖.csv` 诉求总量排序**完全一致**；Q1 引用的 K-C1/URP-P11 等来源实存；结论口径自洽（154 矩阵层上标注 174 的口径混配属 D11·大脑答卷本身引用自觉成立） |

### 2.3 S6 用户复测与 S7 判读

| # | 要点 | 结论 | 证据/说明 |
|---|---|---|---|
| 3.3-1 | 三坑修复根因-修复-验证闭环 | **PASS** | ①单线程 SSE 阻塞→ThreadingTCPServer+daemon_threads（[serve.py](frontend/serve.py:449)）②重放未去重→_seenSpecIds（[render_client.js](frontend/js/render_client.js:8)）③inbox 积压重放→15 旧 spec 移 _backup+backlog 仅 1；验证：无头采样 12s 图层恒 1+用户确认。闭环成立 |
| 3.3-2 | _backup/ 15 旧 spec 处置建议（未删只移） | **notes** | 现状：`DATA/exports/render_inbox/` 磁盘为空、`_backup/` 已不存在（runtime 数据未入 git·S6 时的状态无法从版本库还原核验）。建议：render_spec 产物可随时重生成、非资产——**演示季结束后可直接删除**；更优为补「消费即归档/自动 TTL」机制（见红线 A6 notes），避免再次积压 |
| 3.3-3 | tests/_tmp_* 临时文件残留 | **PASS** | 实测无 `tests/_tmp_*` 残留；git 工作树干净；无未提交调试文件 |
| 3.3-4 | S7 结论链（R43 初判读→R44 增量判读） | **PASS** | R43 五层判定+D1-D5 清单与实测记录逐条对应；R44 验收 D6-D8 销号、沉淀 R11（[debug-memory.md](docs/debug-memory.md:90)）、缺陷清单现状（D1-D3 待派/D4 待做/D5·D6-D8 已销）与代码现状一致；R41 遗留观察项（F3-F6）挂账随 Q3/Q4 复测——结论链自洽 |

---

## 三 PT-CB5 补审要点逐条结论

| # | 要点 | 结论 | 证据 |
|---|---|---|---|
| 4-1 | G-2 穿透洞独立复测（MCP 入口传结论层应拒） | **PASS** | 本次直调三工具：`zonal_stats(boundary=base_174_aggregate_area)` / `zonal_stats(boundary=page7_12345_top10)` / `buffer(center=base_174_aggregate_area)` / `rank(boundary=base_18village_area)` 全部 `ok:False`+「结论层（usage=analysis_output·铁律7 禁作分析输入）」语义化提示（trace 0.2-0.7ms 快返）。守卫实现 `_reject_analysis_output`（[mcp_server_emc.py](tools/mcp_server_emc.py:74)）挂 zonal/buffer/rank 三入口；`layer` 位无法解析 manifest preset（resolve_points 仅认 _POINT_LAYERS·[geo_registry.py](core/geo_registry.py:219)）→ 无旁路。**注**：stdlib 级 JSON-RPC 冒烟受审计环境缺 `mcp` 包阻断（requirements 已锁 `mcp>=1.0.0,<2.0.0`·[requirements.txt](requirements.txt:69)·未安装·零实施故不装）——协议层入口以 dsh T3 记录（stdio 冒烟 8 工具列出）+代码审读（FastMCP 自动派生）佐证 |
| 4-2 | 利益冲突四项声明逐条核 | **PASS** | ①红线零接触：tool_contracts diff 仅 G8b 派生链（schema enum/description）·diagnose prompt 文本零改动 ②纯读：七工具零写盘/零 LLM/零网络外呼（代码审读+grep 模式）③引用全真：caliber refs 中 G-2/K-01/K-03/K-C1 在口径注册表全部实存（[口径注册表.md](docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md:1)）④钳制实证：top_n 硬顶 20/k 夹取 10/limit 夹取 20 有测试断言+本次直调复核。四项声明全部成立 |
| 4-3 | A9 教训固化与复发检查 | **PASS（附 1 处已知遗留）** | 固化：审计文档（PT-CB5-审计_zcode：29/42 行）+台账（EMC-dsh整体合体_讨论过程台账.md:395）+CB6 任务书/开工拆解均成纪律条款。复发检查：PT-CB6 新代码 watcher 坏文件 log 后跳过、_watch_loop log、dataset 端点窄异常+提示——均「打点/收窄」无静默。已知遗留：`_reject_analysis_output` 仍 `except Exception: pass`（manifest 不可用→fail-open·[mcp_server_emc.py](tools/mcp_server_emc.py:84)）——zcode 已列「下批顺修」·本次不升级为缺陷（有注释说明+G8b 枚举兜底），但建议 D 批优先收窄 |
| 4-4 | 七工具真身签名一致性（backing 五锚点） | **PASS** | 逐锚点 inspect 核对：list_data→`list_point_layers()`；rag_query→`search(query, k=5)`；kb_facts→`query_knowledge_base(query='', city='宜昌', topic=None, keyword=None, limit=5)`（裁决一直映·domain 作废）；outlet_card→`build_outlet_schema(diagnose, result, question='')`；zonal_stats→`resolve_boundary/resolve_points/aggregate_by_polygons(points, polys, agg_cols=None, polygon_name_col=None)`；buffer→`create_buffer(geojson_fc, distance_m, dissolve=False, target_crs='EPSG:4546')`（MCP 位置传参顺序正确）。全部一致 |

---

## 四 PT-CB4 抽验复核结论

| # | 要点 | 结论 | 证据 |
|---|---|---|---|
| 5-1 | T1 六组裁决抽 2 组复现 | **PASS** | 抽①（占比表 analysis 版现行）：`77项_社区占比表.csv` analysis 版 690 行（689 数据+表头）·唯一社区 **137**·0 伪社区行（「范围外/未匹配」仅存于已归档 perf 旧版）；抽③（三件归档）：`DATA/analysis/_retired/` 含 12345_事件类型/77项_社区11类占比矩阵/77项_社区占比表 三件·[retired.md](docs/catch-ball/retired.md:79) 登记·原 performance 路径已不存在。裁决与证据一致 |
| 5-2 | T1 qty 双头破案结论 + presets 同步批挂账 | **PASS** | 总账 :135 记录「全覆盖版=数据权威源（11 件登记）·presets=图层物理位待同步（挂起批）·8 对内容不同（公服 1068=08-12 误计旧口径）」（[总账.md](DATA/analysis/_总账.md:135)）；口径注册表 §六.1 挂起同条。挂账状态在档·无消失 |
| 5-3 | T2 契约卡 12 张抽 3 张 | **PASS** | K-01（三层口径）/K-03（page7 两表）/K-C1（社区六口径枚举）均含 卡ID/业务名/定义/适用/禁用/值锚点/状态/来源/变更 全字段；值锚点全部指向成文出处（page2 汇总/全域数据包/geojson 物理件）不内联数据 |
| 5-4 | check_caliber 6 命中基线判定记录 | **PASS（实测复现）** | 注册表 §五 登记「6 命中=已知合规基线」；本次实测工具运行**仍为 6 命中**（page2 汇总 ×4 处 87.9% + 全域数据包 §九 双高3 + 3prime 总纲 双高16）——与基线记录逐条吻合 |

---

## 五 B1 复审结论（七项逐一复核）

| # | 修复 | 代码现状复核 | 结论 |
|---|---|---|---|
| H1 | 用途标记随层持久（4 处 preset 加载点 + tools.js ref() 改判） | `range-presets.js` :112/:119/:130/:150 四处 `L.presetUsage = item.usage || null` 在位；`tools.js` :247 `if (l.presetUsage === 'analysis_output')` 拒绝改判。**PT-CB6 前端改动（index.html+render_client.js 新增）未触碰这些文件·随层标记仍生效** | **PASS** |
| H2 | hotspot 挂闸 | `api/geo_routes.py` :711 `_guard_usage_refs(req.layer, req.range)` 首行；`tests/test_usage_guard.py` :156 `test_api_hotspot_rejects_conclusion_range` 在位 | **PASS** |
| M1 | 手动工具箱边界源单点过滤 | `toolbox/shared.js` :290-293 `collectBoundarySources` 过滤 `it.usage` 与 `l.presetUsage === 'analysis_output'`（四消费工具无感知受益）。**MCP 面新增后未被绕过**：MCP zonal/buffer/rank 各自服务端 `_reject_analysis_output`（本次独立复测全拒）；layer 位无 preset 解析能力；render_spec 走显示放行（设计内） | **PASS** |
| L1 | manifest note 措辞 | `subj_12345_xlwj_aggregate_area` note 已按裁决口径（总账 :135 对照·图层素材表 :64 118 社区口径） | **PASS** |
| L2 | check_manifest 读显式布尔 | `tools/check_manifest.py` reference_only/missing 走字段显式判定（不再依赖 note 字符串） | **PASS** |
| L3 | schema 门禁进 pytest | `tests/test_manifest_gate.py` 在位（本次 pytest 全量通过含该文件） | **PASS** |
| L4 | hotspot 拒绝用例 | 见 H2 证据 | **PASS** |

**B1 批正式销号**（判定依据：代码现状逐一复核在位 + 三轮全量门禁持续回归 389+3→427+2 佐证）。

---

## 六 缺陷清单（新增·自 D9 起）

| 编号 | 严重级 | 位置 | 描述 | 修复建议 |
|---|---|---|---|---|
| **D9** | P3 | `api/render_routes.py` :31（F_029 注册）/ :44-60（scan_inbox）/ :91（render_stream） | F_029 仅注册、`scan_inbox`/`render_stream` 未挂 `@track`（公开函数+I/O 端点·按埋点规则应埋点）。执行记录 §五.2 声明系「高频 1s 扫描防 trace 刷屏」的有意设计·**留待主手裁决**——审计不推翻，但需主手闭环（豁免或补埋点二选一） | 主手裁决：①豁免则把「高频扫描类免 @track」固化为 tracker 纪律条款；②补埋点则 scan_inbox 挂 @track（stream 端点可只挂一次进/出） |
| **D10** | P2 | `frontend/js/render_client.js` :53-69（_normCommunityCount）/ :97-106（community choropleth 分支） | `community_choropleth_v1` 无条件走计数着色（log1p+countStops）；当 `value_field=polarity_index`（render_spec 默认值·[mcp_server_emc.py](tools/mcp_server_emc.py:521)）时：正极性值被当作「高密度」着色、负值/零趋透明——**语义错位误渲染**。P+ 任务书 +1 明示「piToNorm 极性专用缺口」为 v1 已知范围，但默认参数使该缺口成为可踩 footgun；与 R41 F5「着色机制待现象」可互证归因 | ①render_client 按 value_field 语义分派：polarity 系字段 → `piToNorm`+`polarityStops('overall')`（同源路径已具备）；②或 render_spec 对 polarity 字段强制要求显式 scheme/拒默认；③补一条 JS 侧渲染语义单测/演示用例 |
| **D11** | P2 | `tools/mcp_server_emc.py` :570-576（community_caliber 无校验）/ 实测 spec | `community_caliber` 由调用方声明、无 K-C1 校验、dataset 路无默认 scope 推导——**S6 用户复测 spec 已实证口径混配**：`page7_12345_top10` 系 154 社区矩阵派生结论层（manifest note+K-C1），却标注 `community=174`（大脑实测记录 §四·S6 最终 spec）。P+ 9.4「读图三问·防 174/154/118 混配」正是为此而设，仍漏入用户可见图层 | ①dataset 路：`_dataset_meta` 增 scope 字段（或 manifest 注记解析）自动填 community 并告警不一致；②inline 路：community_caliber 值过 K-C1 派生校验表；③演示/复测链路补口径断言 |
| **D12** | P2 | `tools/demo_pioneer.py` :58-70（_zonal_and_render·render_spec 调用在 :64） | 演示脚本名不副实：「12345热线诉求最密集社区(真实)」实际渲染 **极性 Top10**——`zonal_stats` 默认按 polarity_index 排序（D1·[mcp_server_emc.py](tools/mcp_server_emc.py:360)），脚本却以 point_count 着色并命名为「最密集」。todo.md:47 已明文警示「最密集/件数类问题不要直接拿 zonal_stats top_n 当结论」——脚本正犯此错（演示内容正确性·用户可见） | ①D1（sort_by=point_count）落地后脚本显式 `sort_by=point_count`；②落地前脚本改名「情绪极性 Top10（件数着色）」并注释待 D1；③或改用结论层 preset（page7_12345_top10）如大脑 Q2 之自纠路径 |
| **D13** | P3 | `api/render_routes.py` :55 / :63 | 两处 `print(..., file=sys.stderr)` 未走 `_safe_print`（红线「所有 print 必须经 _safe_print」）。当前消息为中文+ASCII 混合、GBK 控制台风险低，但纪律不符 | 改走 `_safe_print`（stderr 版本或统一日志函数） |

**notes（不编号缺陷）**：
- `_reject_analysis_output` 宽 except fail-open 为 A9 普遍化已知遗留（zcode 列「下批顺修」·本次复核确认仍在）。
- render_inbox 无自动清理补偿（A6 副作用补偿）；S6 为人工移走。建议消费即归档/TTL（如 7 天）。
- render_client `_count_norm` 归一公式与 shared.js buildZonalFc semantic='count' 同公式独立实现——防双源漂移建议后续抽公共函数。
- dataset 端点对点层返回全部属性（无字段白名单）——checkup_12345_2024 含「办件编号」类准标识字段；大层（57k 点）被 >2000 降级挡住、风险低，但 ≤2000 小层应核对透传字段（脱敏口径）。
- K-C1 未登记 118（12345 西陵+伍家岗聚合面 118 社区·图层素材表 :64）而 P+ 任务书枚举含 118——建议 K-C1 补行或注明派生口径。
- demo_pioneer 引用的安全/民生社区点层 preset 未入 geo_registry 点层注册表（dict send-in 路径）——已留主手裁决（P+ 执行记录 §六·增补待裁决）。
- api/main.py 挂载为 4 行（2 注释+2 代码）vs 任务书「唯二行」——文书级表述差异。

---

## 七 红线铁律逐项核对表

| 红线 | 核对结果 | 证据/说明 |
|---|---|---|
| usage 守卫全员生效（含 MCP 工具入口） | **PASS** | 后端 geo_routes 11 端点（10+hotspot·:184/:211/:240/:272/:332/:372/:521/:581/:632/:663/:711）全部 `_guard_usage_refs`；MCP zonal/buffer/rank 三工具 `_reject_analysis_output`（独立复测全拒）；前端 tools.js ref() + shared.js collectBoundarySources；render_spec 显示放行（设计内·带 usage 徽标） |
| 工具输出脱敏 + 带口径 + 可追溯 | **PASS（附 notes）** | list_data 不含样例值；七工具返回值均带 caliber 四键（scale/semantics/limits/refs）；rows 走字段白名单（无 PII）；Q1-Q4 答卷带来源引用。notes：dataset 端点点层属性原样透传（≤2000 层可见办件编号类字段） |
| 单一权威源（不写第二份 schema·MCP inputSchema 派生） | **PASS（口径 note）** | 全仓无手写 inputSchema（grep 零命中）；FastMCP 由 `server.tool()(fn)` 从函数签名自动派生（[mcp_server_emc.py](tools/mcp_server_emc.py:597)）——「不写第二份」成立。**note**：任务书字面「派生自 tool_contracts」未字面满足——七工具为新增包装（非 TOOL_CONTRACTS 条目），其 schema 单一源=本文件函数签名（派发单为规格依据·backing 真身五锚点已核）。建议下批把七工具注册进 tool_contracts 或补「签名 vs 契约」派生一致性测试，彻底闭合平台化口径 |
| 安全打印 _safe_print | **PASS（附 D13）** | mcp_server_emc/check_caliber/demo_pioneer 均 _safe_print；render_routes 两处 print 未走（D13·P3） |
| ASCII 标记（禁 emoji） | **PASS** | 新代码 9 文件 emoji 扫描零命中；[OK]/[WARN]/[ERR]/[dsh] 标记规范 |
| 禁宽 except 静默（A9） | **PASS（附 1 处已知遗留）** | PT-CB6 新代码全部「log 一行或收窄异常」（watcher/端点）；`_reject_analysis_output` fail-open 为 zcode 已列下批顺修的已知遗留 |
| 副作用必带补偿（A6） | **PASS（附 notes）** | 七工具纯只读零副作用；render_spec 写 inbox 为唯一副作用——补偿缺失（无 TTL/自动清理·S6 人工移走）；测试报告落盘（_test/report）为既有 dev 机制不在本批白名单 |

---

## 八 追踪 ID F_020~F_030 逐号核对

| ID | 注册模块（文件） | 模块归属 | @track 实测 | 描述与实现一致性 | 判定 |
|---|---|---|---|---|---|
| F_020 | tools/check_caliber.py:33 | MOD_AIQA | ✅（main·双注册） | 口径复核工具·命中即 fail 清单——一致 | PASS |
| F_021 | tools/mcp_server_emc.py:30 | MOD_AIQA | ✅（list_data） | 数据说明书/开卷定参——一致 | PASS |
| F_022 | :31 | MOD_AIQA | ✅（rag_query） | 带来源检索/v1 综合降级 deferred_v2——一致 | PASS |
| F_023 | :32 | MOD_AIQA | ✅（kb_facts） | 真身签名直映 query/keyword/topic/limit——一致（裁决一） | PASS |
| F_024 | :33 | MOD_AIQA | ✅（outlet_card） | 确定性组装/对话级——一致 | PASS |
| F_025 | :34 | MOD_AIQA | ✅（zonal_stats） | 单元聚合/宏观中观——一致 | PASS |
| F_026 | :35 | MOD_AIQA | ✅（buffer） | 缓冲影响圈/中观——一致 | PASS |
| F_027 | :36 | MOD_AIQA | ✅（rank） | 排序评价 Top-N——一致 | PASS |
| F_028 | :37 | MOD_AIQA | ✅（render_spec） | 图层图纸 dataset/inline→spec 落收件箱——一致 | PASS |
| F_029 | api/render_routes.py:31 | MOD_AIQA | ❌ 仅注册（D9） | watcher 扫描→SSE 队列——描述与实现一致·埋点缺位 | PASS（附 D9） |
| F_030 | api/render_routes.py:32/:111 | MOD_AIQA | ✅（render_dataset） | dataset 取数/>2000 降级——一致 | PASS |

**连续区**：F_020~F_030 逐号连续无跳号（实测 `_TRACKING_REGISTRY` 全 11 号在位）；本批无新增 D_ 系列（MOD_AIQA 尚无 D_ 注册·符合现状）；validate_track_ids 门禁通过（`used_track_ids_are_registered` 等全绿）。

---

## 九 基线复核实录

### 9.1 pytest 全量

```text
py -m pytest tests/ -q
================= 427 passed, 2 skipped, 7 warnings in 57.55s =================
```

**与声称基线 427 passed + 2 skipped 一致**（7 warnings 均为既有 SyntaxWarning/编码提示·含 SCRIPT/extract_0819_12345.py 无效转义——存量非本批引入）。

### 9.2 服务新旧核查（R7）

```text
=== 服务新旧核查 ===
最新提交时间: 2026-08-20T09:27:59
PID 3468: 启动时间 2026-08-20T09:08:23
[WARN] 服务早于最新提交 19 分钟——载旧码风险，建议重启（R7）（PID 3468）
PID 21552: 启动时间 2026-08-20T09:08:23
[WARN] 服务早于最新提交 19 分钟——载旧码风险，建议重启（R7）（PID 21552）
```

两服务进程早于最新提交 19 分钟（最新提交=审计任务书文档提交·非运行时代码提交）。本次审计未对运行中服务做行为断言（G-2 复测为进程内直调·check_caliber 为 CLI·pytest 为独立进程）——**未受载旧码影响**；如需后续实测服务行为，先重启（R7）。

### 9.3 环境备注

- 审计环境未安装 `mcp` 包（requirements 已锁 `mcp>=1.0.0,<2.0.0`）→ stdio JSON-RPC 冒烟无法在本机执行；以函数直调+代码审读替代（零实施纪律不装依赖）。
- `git status` 全程干净；`tests/_tmp_*` 无残留。
- 范围口径差异（43 vs 34 提交·+3659 vs +3817 行）见 §〇。

---

## 十 证据引用索引（文件:行号）

| 主题 | 证据 |
|---|---|
| render_spec 实现 | [tools/mcp_server_emc.py](tools/mcp_server_emc.py:514)（F_028·spec 组装）；:41 SCHEMES；:74 `_reject_analysis_output`；:360-369 zonal 排序；:621 main() stdout 打印 |
| 投递通道 | [api/render_routes.py](api/render_routes.py:31) F_029 注册；:44 scan_inbox；:70 _sse_stream；:110 render_dataset（F_030）；:55/:63 裸 print |
| 前端 loader | [frontend/js/render_client.js](frontend/js/render_client.js:8) 去重；:20 scheme 解析；:63 _normCommunityCount；:85 徽标；:96-106 choropleth 着色 |
| S6 修复 | [frontend/serve.py](frontend/serve.py:449) ThreadingTCPServer；S6 提交 55b40ada（diff 三文件） |
| 着色同源 | [frontend/js/toolbox/shared.js](frontend/js/toolbox/shared.js:138) defaultPaint；:290 collectBoundarySources；[frontend/js/grid-tool.js](frontend/js/grid-tool.js:103) countStops |
| B1 七项 | [frontend/js/range-presets.js](frontend/js/range-presets.js:112)（4 处 presetUsage）；[frontend/js/ai_qa/tools.js](frontend/js/ai_qa/tools.js:247)；[api/geo_routes.py](api/geo_routes.py:711)；[tests/test_usage_guard.py](tests/test_usage_guard.py:156) |
| 口径混配实证 | [docs/catch-ball/discuss/PT-CB6-大脑实测记录_dsh-2026-08-20.md](docs/catch-ball/discuss/PT-CB6-大脑实测记录_dsh-2026-08-20.md:1) §四（S6 最终 spec community=174）；manifest page7_12345_top10 note（154 矩阵） |
| 演示脚本语义 | [tools/demo_pioneer.py](tools/demo_pioneer.py:62)；[docs/todo.md](docs/todo.md:47)（D1 警示） |
| T1 抽验 | [DATA/analysis/_总账.md](DATA/analysis/_总账.md:135)；[docs/catch-ball/retired.md](docs/catch-ball/retired.md:79)；`DATA/analysis/_retired/` 三件 |
| T2 抽验 | [docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md](docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md:1)（K-01/K-03/K-C1·§五 基线·§六 挂起） |
| S7 判读链 | 台账 R43（5408076d）/R44（c72ca73d）；[docs/debug-memory.md](docs/debug-memory.md:90) R11 |
| 大脑答卷数字 | 独立复算 `DATA/analysis/12345主观/12345_社区x9类_全覆盖.csv` 诉求总量 Top10 与 Q2 答卷逐项一致 |
| G-2 复测 | 本次直调输出（zonal/buffer/rank 全拒·trace 0.2-0.7ms） |
| 追踪注册 | 本次 import 实测 `_TRACKING_REGISTRY` F_020~F_030 连续在位 |

---

> Codex · 2026-08-20 · 审计（零实施·只读）· 本报告为唯一产出；commit/push 由用户决定。
