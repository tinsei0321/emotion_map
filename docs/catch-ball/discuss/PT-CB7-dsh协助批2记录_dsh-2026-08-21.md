# PT-CB7 · dsh 协助批 2 记录（dsh 侧）

> 日期：2026-08-21（执行窗口 00:56 ~ 01:11 +08:00）
> 执行环境：dsh 官方 checkout `D:/Github/dsh` + 用户 profile `~/.dsh/profiles/{web,emc-test,emc-test-headless}`
> 纪律：EMC 仓仅落盘本文档与截图；dsh 上游未 merge、未 push；全部改动在 `~/.dsh`（dsh 环境）与 `D:/Github/dsh-emc-entry` 之外仅读引用。

---

## 任务 A（T5 dsh 半 · 人设 system prompt 落地）

### A-1 机制侦察结论

- dsh 人设机制 = **agent preset 文件**：`~/.dsh/.agent-presets/<preset>/agent.cordis.yml` 中的 `persona` 行（`@deepseek-ai/dsh-persona`，config-catalog.md:1463-1485），以及 preset 内 router 插件自带的 persona 常量。
- profile 侧无独立 persona 配置键（settings.yaml 只有 model/preset 等 namespace；`packages/settings` 无 systemPrompt/persona namespace）。
- **默认 preset 的 router 覆盖 persona**：web/emc-test 默认 preset = `router-standard-subagent`；其 `router-bootstrap-v1.mjs` 在 `system-prompt/assemble` 首轮把 sections 替换为 `RL_PERSONA`（standard 模式），会清掉其他 persona 段落（router-bootstrap-v1.mjs:92-97）。因此只改 agent.cordis.yml 的 persona 行不够，必须同时改 router 的 `RL_PERSONA`。

### A-2 落地改动（dsh 环境，全部带 .bak-ptcb7 备份）

| 文件 | 改动 |
|---|---|
| `~/.dsh/.agent-presets/router-standard-subagent/agent.cordis.yml` | persona 行 text 改为定稿人设（YAML 块标量） |
| `~/.dsh/.agent-presets/router-standard-subagent/router-bootstrap-v1.mjs` | `RL_PERSONA` 常量改为定稿人设（模板字符串） |

- 覆盖 profile：web（settings.yaml 默认 preset=router-standard-subagent）+ emc-test（无 settings.yaml，继承全局默认同 preset）——同一 preset 两 profile 共用，符合“挂到两 profile 默认 preset”。
- 定稿人设文本（照派发 §四）已完整写入，见改动文件；本文不再重复全量（以文件为准）。
- 验证需重启 dsh web（3080）——旧进程（00:34:54 起）早于改动，重启后（01:04:58，pid 50260）生效。

### A-3 验证：新会话问「你是谁」

- 会话：`session-048a67ce-0043-495a-b40e-f437a7c708db`（web profile 新会话）
- 模型思考明确引用人设：「按照我的系统提示：身份问题（你是谁/能做什么）先调 kb_facts(keyword=EMC) 按身份卡回答。我作为 EmotionMap Copilot…」
- **最终回答原文**（记录如下）：

> 我是 **EmotionMap Copilot（EMC 情绪地图助手）**——宜昌城市情绪空间分析平台的分析助手，身份信息来自行业知识卡 EMC-IDENTITY-01（PT-CB6 入口重定义 / PT-CB7 扩写）。
>
> 用一句话说：**用情绪地图看懂市民心声——问区域情绪、做空间分析、追原因与建议。**
>
> 【能做什么】
> - 基于多源市民诉求数据做城市情绪空间分析，七件标准工具：知识检索（rag_query）、行业事实卡（kb_facts）、数据清单（list_data）、行业出口卡（outlet_card）、单元聚合（zonal_stats）、缓冲影响圈（buffer）、排序评价（rank）
> - 另可经 render_spec 出图到情绪地图前端
> - 结论附口径引用义务（数据带 caliber + 口径注册表卡 ID）
>
> 【不能做什么】（边界，按身份卡）
> - 不编造数字——无数据则明说
> - usage=analysis_output 的结论层仅作展示、禁作分析输入
> - 宏观情绪倾向不等于微观诊断，不做单点归因
> - 不碰 EMC 前端 QA 管线
>
> 【口径纪律】
> - 引用数据必带 caliber 与口径注册表卡 ID（如 K-01/K-02）
> - 社区口径枚举按 K-C1（174/154/118/130 等不得混用）
> - 数据类交付必带口径对照段：口径卡 ID + 子集声明（本结果 ≠ 全量）+ 覆盖说明

- 截图：`docs/catch-ball/discuss/PT-CB7-dsh协助批2-人设验证.png`
- 结论：**人设落地生效**（A 路径成功，无需停摆回报）。

---

## 任务 B（T17 · MCP 首调超时）

### B-1 配置改动

- `~/.dsh/profiles/emc-test/cordis.patch.yml`：`toolCallTimeoutMs: 60000` → `120000`
- `~/.dsh/profiles/emc-test-headless/cordis.patch.yml`：同上

### B-2 headless 复跑对照（批 1 = 137.4s / 110.2s turn / 4 步 / 4 工具调用）

- 命令：`dsh --profile emc-test-headless "12345 热线诉求最密集的 5 个社区是哪些？"`
- 会话：`session-835e7625-b66c-4457-b5b2-f2ab1aacede4`
- 进程 wall：**494.914s**；turn active：**448.732s**（00:58:37.871 → 01:06:06.603）
- step 数：**22**；tool call 数：**31**

每步分解（节选关键）：

| step | start | end | dur | tools | 说明 |
|---|---:|---:|---:|---|---|
| 1 | 00:58:38.246 | 01:00:40.861 | 122.615s | mcp list_data ×1 | **仍超时**：`MCP error -32001: Request timed out`（120s 后返回） |
| 2 | 01:00:40.888 | 01:00:42.390 | 1.502s | mcp list_data ×1 | 重试成功 |
| 3 | 01:00:42.406 | 01:00:51.862 | 9.456s | mcp zonal_stats ×1 | 边界/图层参数错（admin_community 未知）→ 返回“未知层 id” |
| 6 | 01:01:14.151 | 01:01:38.195 | 24.044s | rank/zonal_stats | 继续试错 |
| 7 | 01:01:38.206 | 01:03:50.838 | 132.632s | kb_facts / rag_query | rag_query 首次**超时**（120s） |
| 8 | 01:03:50.856 | 01:03:59.346 | 8.490s | zonal_stats / rag_query | rag_query 重试成功（命中 12345 中心城区等） |
| 9-18 | 01:03:59.361 | 01:04:57.855 | ~58.5s | pwsh/read/grep/glob | 探查 emotion_map 数据目录/结论文件 |
| 19 | 01:04:57.866 | 01:05:18.998 | 21.132s | zonal_stats / pwsh | 改走结论层文件 |
| 21 | 01:05:41.534 | 01:06:00.540 | 19.006s | outlet_card ×1 | 出口卡未命中模板（不编造） |
| 22 | 01:06:00.557 | 01:06:06.603 | 6.046s | 无 | 最终归纳（朝阳路/万达/港务/建设/岳湾路口径说明） |

### B-3 结论（T17 核验）

- **首调 list_data 仍然超时**：即使 timeout 提到 120s，第一次调用仍在 120s 处超时（结果 error -32001）。根因 = MCP server 冷启动耗时 >120s，不是客户端 60s 阈值的问题；T17 只延长等待没有消除超时。
- 副作用显著：turn 从批 1 的 110s 恶化到 448.7s（22 步/31 调用），其中 agent 因 zonal_stats 参数试错 + 文件系统探查 + rag_query 二次超时大幅兜圈。
- **建议回报 Qoder**：T17 需改为服务端预热/启动门控（如 health 就绪后再放行首调），或加大到更大量级并配重试退避；仅调 timeout 无效。

---

## 任务 C（T3/T15 运行时验证）

### C-1 验证过程（Playwright headless，dsh web 3080）

- 前置：杀掉 8080/8000 使后端未就绪 → 硬刷新 3080 页面 → 点 EMC 按钮。
- 进度卡三态（`.dsh-emc-entry-progress` 实测文本）：

| 阶段 | 文本 |
|---|---|
| starting | `EMC 启动中：正在拉起后端服务（8080）…` |
| warming | `前端已就绪，后端 RAG 预热中（约 20-30s）…` |
| ready | `EMC 已就绪，地图页已在默认浏览器打开。`（含「打开测试页」按钮） |

- 三态时间点：starting≈0s → warming≈21s（iter 42×0.5s）→ ready≈21.5s（iter 43）。
- **无终端弹窗**：过程中未出现任何终端标签/弹窗（仅 popup 一个，为地图页）。
- **就绪自动开页**：popup 自动导航到 `http://localhost:8080/frontend/index.html`（默认浏览器打开，未走内嵌）。
- **无历史 [dsh] 残留层（T16 生效）**：8080 地图加载后 `window.__map.getStyle().layers` = `['tdt-img', 'basemap-veil']`，无任何 `[dsh]` / `12345` / `社区174` 层。
- 截图：
  - `docs/catch-ball/discuss/PT-CB7-dsh协助批2-进度卡三态.png`
  - `docs/catch-ball/discuss/PT-CB7-dsh协助批2-自动开图无残留层.png`
- 内嵌 tab「打开内嵌」入口未单独验证（批 2 可选；未做，如需可补）。

---

## 附：证据文件与时间戳

- 本文档：`docs/catch-ball/discuss/PT-CB7-dsh协助批2记录_dsh-2026-08-21.md`
- 人设改动文件（含备份）：`~/.dsh/.agent-presets/router-standard-subagent/{agent.cordis.yml,router-bootstrap-v1.mjs,.bak-ptcb7}`
- 超时改动：`~/.dsh/profiles/emc-test/cordis.patch.yml`、`~/.dsh/profiles/emc-test-headless/cordis.patch.yml`
- 人设验证会话：`C:/Users/Hi/.dsh/sessions/--D-Github-emotion_map--/session-048a67ce-0043-495a-b40e-f437a7c708db/session.jsonl.zstd`
- 人设验证截图：`docs/catch-ball/discuss/PT-CB7-dsh协助批2-人设验证.png`
- headless 复跑日志：`C:/Users/Hi/.dsh/profiles/emc-test-headless/run-ptcb7-b2-headless.log`
- headless 复跑会话：`C:/Users/Hi/.dsh/sessions/--D-Github-dsh--/session-835e7625-b66c-4457-b5b2-f2ab1aacede4/session.jsonl.zstd`
- Task C 截图：`docs/catch-ball/discuss/PT-CB7-dsh协助批2-进度卡三态.png` / `PT-CB7-dsh协助批2-自动开图无残留层.png`
- Task C 脚本输出：`C:/Users/Hi/AppData/Local/Temp/dsh_ptcb7_taskC_out.txt`
