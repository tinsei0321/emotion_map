# PT-CB4 · dsh 轻量批派发单（zcode·2026-08-19 晚）

> 派发方：zcode（主手）。执行：**dsh（通用副手）**。背景：高峰期 token 管制——zcode/Codex 暂停，dsh 承接机械前置活。
> 性质：**纯只读收集 + 新文档产出**。零判裁（不裁决"哪版对/该不该改"）、零生产代码修改、零数据文件移动。
> 分支：`EMC_harness_dsh`（main 冻结勿碰）。开工前 `git pull origin EMC_harness_dsh`。
> 任务卡格式：升级版（业务背景+数据源两栏·PT-CB3 学习线 A3 吸收项首次实用）。

---

## 任务 A：版本对账证据收集包（PT-CB4 T1 前置·判裁留主手）

**业务背景**：DATA/performance/ 与 DATA/analysis/ 存在同名（近名）数据文件新旧并存，后续需判裁哪版现行。判裁 = 主手活；**dsh 只负责把每对的四类证据材料摆齐**，主手恢复后凭包裁决。

**数据源**：两目录文件清单对比（自行找出全部同名/近名对，已知线索：占比表约 676 vs 690 行的一对；另有"安全/民生"各自的`分析整合`vs`分析汇总`双版本 md）。

**要做的**：对**每一对**候选文件收集四证据：
1. **提交时间**：`git log --diff-filter=A --format="%h %ad %s" -- <file>`（两版各查）；
2. **行数/要素数**：csv 用行数，geojson 数 features；
3. **引用链**：在 `DATA/analysis/_总账.md` 及 page3/page5 汇总类 md 里 grep 文件名，记录"现行文档引的是哪版"；
4. **生成线索**：文件头注释、生成脚本 grep（SCRIPT/ 下搜文件名），记录生成时间与出处。

**产出**：`docs/catch-ball/discuss/PT-CB4-T1证据收集_dsh-2026-08-19.md`——每对一节、四证据齐、**附所用命令**（主手可复现）。结尾给"发现的对数清单"，**不写任何结论**（哪版赢=主手裁决）。

**验收**：对数完整（含 md 双版本）；每对四证据齐；全只读。

## 任务 B：图层能力目录素材表（B3 口径注册表前置·机械提取）

**业务背景**：下一批要建"口径注册表 + 数据能力目录"，主手需要每个登记图层的**原始素材**（字段/类型/规模/样例）。机械提取，无判裁。

**数据源**：`DATA/boundaries/presets/manifest.json` 全部 57 项（含 2 个文件缺失项——标注即可）。

**要做的**：每层一行/一小节，字段：
`id | label | usage | group | 文件路径 | 几何类型 | 要素数 | 字段名列表(前15) | nameField 样例值×2 | 缺失标注`

**产出**：`docs/urban-renewal-plan/00-宜昌专项/_图层素材表.md`（下划线前缀=治理台账·不进 RAG·与将来注册表同目录）。

**限制（红线）**：
- 样例值只取 nameField 类中性字段；**禁止摘录电话/姓名/编号类字段值**（PII）；
- 只读源文件与 manifest，不修改任何文件；
- geojson 大文件只读头部/抽样统计，不整文件加载进上下文（用脚本统计）。

**验收**：57 层全覆盖；字段名与文件实际一致（主手抽检 5 层）；无 PII 摘录。

---

## 白名单（dsh 允许触碰）

- **读**：全仓只读。
- **写**：仅上述两个新产出文档。
- **git**：只 `add` 两个产出文档 + commit（前缀 `PT-CB4(dsh):`）+ push origin 分支。**禁碰其余一切文件**（含 `.dsh-meow/`·已 gitignore）。

## 完成动作

两件完成后：执行记录并入证据收集文档尾部（任务 B 可在素材表尾部附三行执行说明）；commit push；停下等主手回收。**不自行延伸任务范围**。
