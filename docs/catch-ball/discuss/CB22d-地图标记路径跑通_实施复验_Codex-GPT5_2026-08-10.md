# CB-22d · 地图标记路径跑通 — 实施复验回应（Codex-GPT5 · 2026-08-10）

> **复验方**：Codex 组（第三方评估·GPT-5）| **日期**：2026-08-10 | **性质**：CB 实施复验轮（实测失败根治后）
> **对象**：commit `ace4f8f`（6 文件 150 行·路径跑通）
> **方法**：定稿 P0-0/P0-1/P0-2 逐项对照 + 代码复算 + **amap_first 真实数据实测（8 名）** + 依赖核验 + 抽验测试（9 断言全过）。未做任何 git 操作。

---

## 〇、一句话结论

**「路径跑通 + 零命中不挂起」达成——判定「通过」（聚焦本轮范围）。P0-0 三项前提全真修（names split 实测成立·20s 冷加载·rapidfuzz/pypinyin 已装）；P0-1 amap_first 实测有效（葛洲坝片区→服务葛洲坝片区工作委员会·伍家岗工业园→湖北伍家岗工业园·防误伤成立）；P0-2 B1 零命中零 LLM 出口根治 6.5 分钟挂起（含结构性防回归断言）。附 1 项质量缺陷（「老城中心」核心实体残留 '中心'→误导命中·实测实证·准确度轮次修）+ 1 项定稿遗留（P0-2-3 finalStep 失败兜底未实施·影响「部分命中→finalStep」路径与「任何情况必收尾」口径）+ 验收口径需分路径修正。**

---

## 一、逐焦点复验

### 焦点 1：P0-0 前提 — **agree（三项全真修）**

| 项 | 代码证据 | 独立验证 |
|---|---|---|
| names split | `_raw.split(/[,，、;；\s]+/)`——数组 join 后再 split / 标量 String 后 split·中文逗号/顿号/分号/空白全兜·引号与 em-dash 保留 | ✅ 拼接串→逐名成立（Codex 根因 C 修复） |
| 冷加载 20s | `_TO = 20000`（旧 5000） | ✅ 实测冷加载 12.4~14.7s < 20s·首用必全 abort 消除（根因 D） |
| rapidfuzz/pypinyin | requirements 已有·本机补装 | ✅ **实测 `py` 下 jieba/rapidfuzz/pypinyin 均 OK**·difflib 回退退役 |

### 焦点 2：P0-1 数据层 — **agree 主体 + 1 项质量缺陷记录（准确度轮次）**

**amap_first 真实数据实测（8 名·`search_place(amap_first=True)`）**：

| 名称 | top-1 命中 | 评价 |
|---|---|---|
| 葛洲坝片区 | 宜昌市西陵区服务葛洲坝片区工作委员会（amap） | ✅ 比本地 fuzzy 的「船闸景区」更贴「片区」语义 |
| 红星路—二马路历史文化街区 | 二马路历史文化街区（amap） | ✅ |
| 伍家岗工业园 | 湖北伍家岗工业园 / 工业园区管委会（amap） | ✅ **防「伍家菜市场」类本地误伤成立**（定稿例子实证） |
| 夷陵广场 | 夷陵广场 / 音乐喷泉（amap） | ✅ 精确 |
| 土门片区 | 宜昌土门宾馆 / 土门馨苑一期（amap） | ✅ 大致区域点 |
| 达门船厂 | 宜昌达门船舶有限公司（amap） | ✅ 语义正确（score 恒 0·置信档缺失·见焦点 5） |
| 三峡青年城 | 三峡大学青年社区 / 三峡文化城（amap） | ⚠️ 近似命中（非目标项目）·准确度轮次 |
| **老城中心** | **中心人民医院西陵院区 / 中心幼儿园 / 老蔡甲鱼馆（local·amap 未命中）** | ❌ **误导命中——`_core_entities('老城中心')` 残留泛词 '中心' → substring 180+ 命中一切含「中心」POI** |

**A0 jieba 双路** ✓ 合理：`_core_entities` 后缀表（历史文化街区/示范区/片区/街道…）**不含实体后缀**（船厂/城/厂/街·防达门船厂/三峡青年城误伤·用户点破）·仅加分词前置·复用 `_match_score` 分层不重写·无 jieba 时保守剥单后缀回退。**聚合名放弃** ✓：`_isAggregate` 引号容忍·污水厂网一体示范区/其他项目不查 API 直接 unmatched（用户「无地点就放弃」）。

**质量缺陷（记录·不阻塞本轮）**：「老城中心」核心实体残留 '中心'（实测误导命中）·且 `_isAggregate` 是**硬编码正则 + 地名白名单**（与定稿「LLM 判意图」有偏差——确定性 Dumb 中间可接受·但词表需维护·见焦点 5）。

### 焦点 3：P0-2 决策层（命门）— **agree B1 根治 + 剩余场景 2 项**

- **B1 零命中零 LLM 出口** ✅：`runTemplatePath` 特判不调 `stages.finalStep`·直接确定性文字（「未能成功 + 未匹配列表 + 原因（片区/聚合类·无坐标）+ 如何继续（上传坐标/换具体地名）」）·`exit='answered'`·`onFinalDone` 必触发 → **计时必然收尾·根治 6.5 分钟挂起**。防回归断言 `test_no_hit_zero_llm_exit`（`'stages.finalStep' not in block`）是**结构性断言**（比纯字符串包含强）✅。
- **agentStep 30s + 流式 token** ✅：`stages.js` agentStep 传 `phase:'agent_step'` → `api.js` 30s 超时（`agent_step=30000`）·`onToken`/`onReason` 逐 token 流式（用户能看到在思考·非静默挂）——glm 指出的「单轮无超时」经核查实为**已有**（api.js 相位超时）·实施「确认」正确。
- **剩余「停半途」场景（非本轮阻塞·需记录）**：
  1. **部分命中（≥1 命中）→ 正常路径 finalStep LLM · 45s 超时仍无确定性降级兜底**——定稿 P0-2-3「finalStep 失败兜底（复用 `_composeDegradedConclusion`）」**未实施**（harness diff 仅 B1）→ 超时走 send() 错误分支（显示「请求失败」·非降级结论）·用户仍可能看到卡顿/错误。
  2. knowledge_qa / general 等其它 finalStep 路径同族（45s 超时错误）·非本轮范围。
  3. 非 generate_point_layer 工具零结果（clip/overlay 等）走既有 ask_user/gap·既有行为·非本轮。

### 焦点 4：路径跑通 + 验收口径 — **跑通 ✓ ·「0 挂起 30s 内」口径需分路径修正**

- **路径真跑通**：真实数据 8/9 有输出（我的 amap_first 实测复现 claude 组验证）——葛洲坝片区/红星路/达门船厂/夷陵广场/土门/伍家岗工业园命中合理·污水厂网示范区/其他项目**正确放弃**·老城中心误导但「有输出」·三峡青年城近似。**用户能看到标记（≥1 橙点）+ 未命中诚实文字** ✓。
- **验收口径修正（「任何情况 30s 内出口」严格未达成）**：
  - 零命中：即时确定性出口（<1s·B1）✅
  - agentStep 单轮：≤30s（已有）✅
  - **finalStep：≤45s 且超时必须出确定性降级结论——当前超时走错误分支（P0.5 补 `_composeDegradedConclusion` 兜底后才达成）**

### 焦点 5：补充与异议

1. **「老城中心」核心实体残留**（实测误导命中）——建议 `'中心'` 入 `_AGGREGATE_WORDS` / `_ZONE_SUFFIXES`；或核心实体须∈已知地名表（REGION_KW + 片区/POI 名集合）才启用 substring 双路·否则回退 unmatched（保守·宁缺勿错）。
2. **amap_first 命中 score 恒 0.0**（高德无 score）——`source='amap'` 无置信档·取 `_hits[0]` 仍可能错（三峡青年城→青年社区）。路径跑通可接受·准确度轮次建议标「高德解析·大致位置」。
3. **`_isAggregate` 硬编码词表 + 地名白名单**——与定稿「LLM 判意图」有偏差但确定性守卫合规（Dumb 中间）；建议注释标注「维护点·准确度轮次换 LLM/已知地名表」·新增聚合词/地名须同步补。
4. **names split 按空白切分**：含空格名称（「宜昌 达门船舶」类）会被拆成两条——低风险·后续可收紧为仅逗号/顿号/分号。
5. **测试**：9 断言全过（实测）·其中 B1 结构性断言值得肯定；仍缺**行为级**用例（stub 全未命中 → 断言 B1 出口文字 + `exit='answered'` + 无 LLM fetch；stub 部分命中 → 断言落图 + finalStep 正常）——P0.5 补。
6. **红线核查**：四态出口 ✓（B1=answered 诚实文字）·finalStep D019 未动 ✓ · diagnose 未动（豁免已登记）✓ · 追踪编号——`_core_entities` 私有函数无需 track·`forward` 已有 `MOD_PLACE.F_002`·`search_place` 改动在 `F_001` 内·**无新 ID·编号连续** ✓。

---

## 二、通过/需修正判定

**判定：通过（聚焦「路径跑通 + 零命中不挂起」本轮范围）**

- **通过理由**：P0-0 三项前提全真修（实测）·P0-1 amap_first 有效（防误伤成立）·A0 双路/聚合名放弃合理·**B1 根治 6.5 分钟挂起（结构性防回归）**·agentStep 30s+流式确认·真实数据 8/9 有输出·9 断言全过·红线未破。
- **附项（不阻塞本轮·建议 P0.5）**：
  1. 质量缺陷：「老城中心」核心实体残留 '中心' → 误导命中（实测实证·准确度轮次修）。
  2. 定稿遗留：P0-2-3 finalStep 失败兜底未实施 → 部分命中/知识问答 finalStep 45s 超时仍走错误分支（非降级结论）·「任何情况必收尾」口径待补。
  3. 验收口径修正：分路径（零命中即时 / agentStep 30s / finalStep 45s+降级）。

---

## 三、补充建议

1. **P0.5-老城中心**：`'中心'` 等泛词入聚合/挡词表；或核心实体双路加「已知地名表」门（∈ REGION_KW/片区名/POI 名集合才启用 substring·否则回退 unmatched）。
2. **P0.5-finalStep 失败兜底**（定稿 P0-2-3 补实施）：finalStep try/catch → `_composeDegradedConclusion`（零 LLM 确定性降级）·治部分命中/知识问答 45s 超时错误卡顿·兑现「任何情况必收尾」。
3. **P0.5-行为级测试**：e2e-seam——stub 全未命中 → 断言 B1 出口文字 + `exit='answered'` + 无 LLM 调用（fetch spy）；stub 部分命中 → 断言落图 + finalStep 正常 + 45s 内收尾。
4. **置信标注**：`source='amap'` 命中标「高德解析·大致位置」（score 恒 0 无置信档·防用户误读高精度）。
5. **词表维护注记**：`_isAggregate` / `_ZONE_SUFFIXES` / 地名白名单注释标注维护点（准确度轮次换 LLM 判意图或已知地名表）。
6. **验收口径落地**：三条件分路径（零命中即时 / agentStep ≤30s / finalStep ≤45s+降级）·用户真实数据复测（看标记 + 计时收尾）。

---

*Codex 组 · CB-22d 地图标记路径跑通实施复验 · 2026-08-10*
*评估方只读本地不 git · 复验落 discuss/ · claude组 收到后收敛定稿*
