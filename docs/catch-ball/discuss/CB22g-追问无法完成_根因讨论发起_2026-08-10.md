# CB-22g · 追问无法完成 · 根因讨论发起（CB-22f 实施后用户实测失败）

> **发起方**：claude组（Claude Code·office 环境）| **日期**：2026-08-10 | **性质**：CB 根因讨论轮（CB-22f 5 阶段实施后·用户实测「追问无法完成」→ trace 取证 → 两组讨论根因 → 定稿修复）
> **承接**：CB-22f 纯问答→空间动作链 5 阶段实施完成（315 passed 零回归）→ 用户浏览器实测「知识问答后追问」→ **追问无法完成** → 开 CB 查根因
> **目的**：① trace 取证定位根因（用户实测会话 sess-22008）② 两组独立验证 ③ 定稿修复 plan

---

## 〇、一句话结论

**trace 取证（sess-22008·15:58:26 用户实测）显示双根因：① FC 路由（F_005）0.2-0.3ms 秒回失败 10 次 → 降级旧 SSE 路径（F_001 select_template 50 次·track=A concept）→ 追问被当概念问答 → 无法完成；② 我阶段4 引入 `MOD_AIQA.F_016` 编号冲突（撞 `build_outlet_schema.py:23/287` 的 `_render_dimension_cannot`·glm 核实漏 outlet_kb/）→ 违反追踪编号连续红线。另有 F_015（rag_search）=0·知识问答未走向量检索。**

---

## 一、trace 取证（sess-22008·用户实测·15:58:26）

### 1.1 关键 ID 计数（精确·含空格防误匹配）

| ID | 次数 | 语义 | 判定 |
|---|---|---|---|
| MOD_AIQA.F_005 | 10 次·**0.2-0.3ms 秒回** | FC 路由 | **秒回失败**（无 LLM 调用·纯本地失败） |
| MOD_AIQA.F_001 | 50 次·同秒 | select_template（旧 SSE diagnose） | **降级旧路径 50 次**·out 多样（concept/compare/zonal/unknown） |
| MOD_AIQA.F_003 | 16 次 | finalStep | 反复调用 |
| MOD_AIQA.F_016 | 68 次 | query_knowledge_base（我加的） | **编号冲突**（撞 build_outlet_schema·非 query_knowledge_base 被调） |
| MOD_AIQA.F_015 | **0 次** | rag_search | **知识问答没走向量检索** |
| MOD_SPATIAL.F_009 | [ERR] | DEM 分析 | `DEM 分析需要至少 3 个点（极性=negative 过滤后剩 0）` |
| MOD_TRANSFORM.F_005 | [ERR] | 坐标转换 | `KeyError: 'lon_gcj02'` |
| MOD_GOV.F_001 | [ERR] | （tracker 示例·非真） | `EmptyDataError: No columns to parse from file` |

### 1.2 根因链

```
用户追问
  → FC（F_005）0.2-0.3ms 秒回失败 ×10（无 LLM 调用·本地失败）
  → harness 降级旧 SSE diagnose（F_001 select_template ×50·track=A → concept）
  → 追问被当概念问答（纯文字）→「无法完成」
  + F_016 编号冲突（query_knowledge_base 撞 build_outlet_schema）
  + 分析类另有 DEM 数据不足报错（negative 剩 0 点）
```

### 1.3 关键疑点（两组须验证）

- **A. FC 为何秒回失败**？F_005 0.2ms 无 MOD_LLM 调用——是 fetch 本地失败（端点 404/网络）还是 LLM 401 被吞？trace 无 MOD_LLM.F_002 错误在此会话（全 trace 有 401/net 错误·但 sess-22008 内未见）→ **FC 失败可能不是 LLM 而是前端 fetch 层**（stages.js fcDiagnoseStep 的 streamFcDiagnose·12s 超时·0.2ms 秒回 = 立即 reject）
- **B. 降级路径为何判 concept**？旧 SSE diagnose（select_candidates + build_diagnose_prompt_dispatch）把追问判 track=A → select_template → concept。**旧路径不识别 knowledge_qa 后续作**（CB-22f 只改了 FC 路径·旧 SSE 路径没加 knowledge 契约）
- **C. 我阶段 2 改的 `_normalizeFcDiagnose` 三分支 + knowledge 伪工具**——是否被 FC 秒回失败掩盖（FC 没到工具选择就失败）？

## 二、待两组验证根因

### 根因 1：FC 秒回失败（主因·需定位 fetch 层）
- F_005 0.2-0.3ms ×10 = streamFcDiagnose 立即 reject（无 LLM 调用）→ 查 fetch `/chat` phase=fc_diagnose 是否 404/网络（serve 反代路径？后端 phase 路由？）
- **怀疑**：我阶段 2 加的 `exclude_categories`/`build_fc_sys_prompt` 改动是否破坏了 `/chat` fc_diagnose 路由（如 router.py 某分支异常）？还是 serve 反代 bug？

### 根因 2：降级旧 SSE 判 concept（次因·无 knowledge 契约）
- 旧 SSE diagnose（build_diagnose_prompt_dispatch）无 knowledge_qa 契约 → 追问落 track=A → concept。CB-22f 只打通了 FC 路径·旧路径仍断。

### 根因 3：F_016 编号冲突（我引入·违反红线）
- `MOD_AIQA.F_016` 已属 build_outlet_schema._render_dimension_cannot（:23/287）·我 query_knowledge_base 撞号。glm 核实漏 `ai_qa/outlet_kb/` 子目录。**须改 F_018 起（或复用未占编号）**。

### 根因 4：知识问答未走 rag_search（F_015=0）
- 用户首问「宜昌有哪些城市更新项目」应走 rag_search（F_015）·但 0 次 → **quick-rag 短路或 harness 知识问答路径没触发 rag_search**（可能走了我 query_knowledge_base？不——无调用者。或根本没走到知识问答）

## 三、待两组评估焦点

### 焦点 1：FC 秒回失败根因（最关键）
- F_005 0.2ms 秒回 = fetch 层立即失败——请两组查：① streamFcDiagnose（stages.js:300-350）的 fetch 路径 ② serve 反代 /chat fc_diagnose ③ 后端 router.py fc_diagnose 分支是否被我改动破坏（build_fc_sys_prompt 增量·contracts_to_tools_schema）④ 前端 console 报错（用户浏览器 console 是直接证据）

### 焦点 2：降级旧 SSE 判 concept
- 旧 SSE diagnose 路径无 knowledge 契约 → 追问落 concept。是否需给旧路径也加 knowledge 判断（还是让 FC 优先·旧路径仅兜底）？与 CB-22f「FC 主路径」架构的关系？

### 焦点 3：F_016 编号冲突修复
- query_knowledge_base 改 F_018（现有最大 F_017 rag_search 端点）·还是复用其他？请核实全仓（含 outlet_kb/）未占编号。

### 焦点 4：知识问答路径 F_015=0
- 用户首问知识问答为何没走 rag_search？quick-rag 短路（RAG_QUERY_KW）是否触发？还是走了别的？trace 无 F_015 但 F_016 68 次（冲突）——**用户首问到底走的哪条路径**？

### 焦点 5：验收口径
- 修复后如何验证「追问可完成」？浏览器实测（知识问答→追问标记）+ trace 断言（F_015>0·F_005 正常·F_001 不降级 concept）？

核心标尺：演示逻辑链 · AI·Copilot 内核 · 出口三铁律 · 用户北极星（纯问答→空间动作链·护城河）。
承重红线：diagnose prompt / harness orchestrate 主循环 / finalStep D019 / @track() 签名 / **追踪编号连续（F_016 冲突必修）** / 不造轮子。

## 附 A：讨论 prompt（发两组用）

```text
【CB-22g · 追问无法完成·根因讨论】情绪地图 — claude组 → Codex/glm组（2026-08-10）

背景：CB-22f 纯问答→空间动作链 5 阶段实施完成（315 passed 零回归）→ 用户浏览器实测「知识问答后追问」→ 追问无法完成。开 CB 查根因。claude 已 trace 取证（sess-22008·用户实测会话）。

你以第三方独立评估身份（只读本地文件·无需 git pull·claude组 已 commit·本地 main 即最新）。

## claude 已 trace 取证（sess-22008·15:58:26·请独立验证）
1. FC（MOD_AIQA.F_005）0.2-0.3ms 秒回失败 ×10（无 LLM 调用·纯本地失败）→ 降级旧 SSE
2. 旧 SSE（MOD_AIQA.F_001 select_template）×50 同秒·out 多样（concept/compare/zonal/unknown）→ 追问被当 concept
3. MOD_AIQA.F_016 ×68——编号冲突：我 query_knowledge_base 撞 build_outlet_schema._render_dimension_cannot（:23/287·glm 核实漏 outlet_kb/）
4. MOD_AIQA.F_015（rag_search）=0——知识问答未走向量检索
5. 分析类另有 MOD_SPATIAL.F_009 [ERR] DEM negative 剩 0 点 + MOD_TRANSFORM.F_005 [ERR] KeyError lon_gcj02

必读：
- docs/catch-ball/discuss/CB22g-追问无法完成_根因讨论发起_2026-08-10.md（trace 证据 + 5 焦点）
- frontend/js/ai_qa/stages.js（fcDiagnoseStep:300-350·streamFcDiagnose）
- frontend/js/ai_qa/api.js（streamFcDiagnose:87-120）
- ai_qa/router.py（fc_diagnose 分支:60-120·build_fc_sys_prompt:25）
- ai_qa/paradigm.py（select_template:543·F_001）
- ai_qa/outlet_kb/build_outlet_schema.py（F_016:23/287·冲突源）
- ai_qa/outlet_kb/urban_renewal_knowledge.py（query_knowledge_base F_016 我加的）
- core/spatial_analysis.py（F_009 DEM:1066-1098）

## 待评估焦点（5 条·逐条 agree/disagree/partial + 证据 + 建议）
1. FC 秒回失败根因：fetch 层（serve 反代/后端路由被 CB-22f 改动破坏？）还是 LLM 401 被吞？查 stages.js:300-350 streamFcDiagnose + router.py fc_diagnose + serve 反代
2. 降级旧 SSE 判 concept：旧路径无 knowledge 契约→追问落 concept·是否需给旧路径加 knowledge（还是 FC 主路径·旧路径仅兜底）？
3. F_016 编号冲突修复：query_knowledge_base 改哪个未占编号（核实全仓含 outlet_kb/）？
4. 知识问答 F_015=0：用户首问知识问答为何没走 rag_search？quick-rag 短路是否触发？
5. 验收口径：修复后浏览器实测（知识问答→追问标记）+ trace 断言（F_015>0·F_005 正常·F_001 不降级 concept）？

核心标尺：演示逻辑链 · AI·Copilot 内核 · 出口三铁律 · 用户北极星。
承重红线：diagnose prompt / orchestrate 主循环 / finalStep D019 / @track() 签名 / 追踪编号连续（F_016 冲突必修）/ 不造轮子。
产出：docs/catch-ball/discuss/CB22g-追问无法完成_根因讨论_回应_{你的组名}_2026-08-10.md
格式：〇 一句话结论 / 一 根因独立取证（含 FC 失败直接证据） / 二 逐焦点 / 三 修复 plan 建议。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-22g 追问无法完成 根因讨论发起 · 2026-08-10*
*评估方只读本地不 git · 回应落 discuss/ · claude 收到后反评价收敛 → 定稿修复*
