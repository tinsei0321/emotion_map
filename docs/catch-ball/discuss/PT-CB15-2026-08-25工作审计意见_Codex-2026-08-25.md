> ⚠️ 性质标注：本文件为 **cdh 链路（EMC 外置大脑·codex_engine 运行时）产物**——非 CB 组员 Codex 的正式审计。正式审计已另存为 `PT-CB15-2026-08-25工作审计意见_Codex-2026-08-25_正式稿.md`，本文件保留作过程稿。

收到。本会话仍无法读文件/代码（exec 的 DPAPI 报错未恢复），审计将基于你浓缩的事实 + 本会话对 emc MCP 的实测证据 + 机制推理，逐项标注证据等级【已验证/机制一致/待核验】，不冒充读过原文。

**结论：有条件通过**
核心三块工作闭环、方向正确，但存在 3 个需处置的风险（仓外引导文件、API key 明文、关键证据未固化）和 2 个未闭环待办（未落盘 preset 清单、T1-T7 收尾指引），满足条件后可通过。

**1. 完整性（有无遗漏环节）**
- PT-CB14 四件：C1–C4 + 门禁 + render-contract §五已覆盖。【待核验】C1 的 11 层 CHECKUP 点层：本会话 `list_data` 可见段未出现 `checkup_qty_*`（中段被截断无法确认），且当前总数 84 与报告「60」口径不一致——建议报告补充计数口径（84 可能=全部 preset+点层+边界的汇总）。
- 建议补查 3 个缺口：①C2 过滤掉的「3 个未落盘 preset」是否登记了待上传清单（报错只给了单文件提示）；②C4 引擎徽标/[dsh引擎] 前缀是否在浏览器实测（报告只提到壳体验全通）；③门禁「597+2」是否写回基线文件。
- 数据治理：零改动 + 删错误产物 + 教训入 memory 闭环。【建议补查】删除产物若曾被注册为 preset/dataset，`available` 过滤会掩盖「幽灵引用」——跑一次 `list_data`/`resolve_boundary` 确认无残留。
- office/T8：三尝试链条、档位回滚、桌面版备份均已覆盖。【遗漏】T8 首测的可复跑脚本/步骤未落盘；8600 MCP 握手依赖未文档化（换机排障会重新踩坑）。

**2. 正确性（§四排查链）**
- 8600 MCP 依赖：机制上合理——本会话实测 emc MCP 是唯一工具面（我全程只能经它取数），harness 启动时对 MCP 握手有强耦合的推断成立。【待核验】`cdh app-server thread/start` 的具体代码事实需读码确认，建议补一句诊断命令（如 `emc_status`/端口探测）入文档。
- AGENTS.md 引导缺环：**与事实强一致【已验证】**。本会话系统提示词即注入自 `D:\Github\_codex_cwd\AGENTS.md` 的 EMC 人设（先 list_data、唯一工具面=emc MCP、禁 webSearch、必出图），我据此才调用了 emc 工具——「无引导不调工具、有引导全链通」的诊断成立。
- deepseek-chat = v4-flash 别名档位：【机制一致·官方表待核验】。本会话工具面确认 DeepSeek v4 家族存在 flash/pro 档位分层（deepseek-v4-flash / deepseek-v4-pro），「deepseek-chat 解析到 Flash 档」的机制可信；但「官方文档确认」属外部证据，建议把该 alias 表（vendor 元数据或文档摘录）落盘，避免换机后重新踩「全名/别名」分叉。
- 回滚 v4-flash 全名：**合理**。前提=桥默认 deepseek-chat 已解析到 flash 档 + 用户令「cdh 恒 flash」。且首试失败根因是引导缺环而非模型名，回滚不牺牲功能。建议在桥配置注释固化「恒 flash」决策与映射，防止后人改回全名引入双轨。

**3. 风险**
- `_codex_cwd/AGENTS.md` 仓外遗漏：**高风险·必须处置**。该文件位于仓外（工作区根目录），换机/克隆仓库不会携带；而它正是 T8 内容链能否调 emc 工具的开关（见 §2 引导缺环实证）。建议：①把 EMC 人设复制/移动到仓内（如 `emotion_map/AGENTS.md` 或 docs 下），`_codex_cwd` 只留全局壳；②加入换机复制清单首位。
- webSearch ok=false：【中低风险】。若为 EMC 人设「禁 webSearch」的设计结果，则不是故障，但报告需写明「设计 or 环境限制」，否则 T1-T7 收尾核对官方资料时会误判为故障。
- config.toml API key 明文：【中高风险】。明文 key 随配置扩散即泄露。建议：改用环境变量注入（如 provider 级 env），文件保持 755/仅当前用户 ACL，换机清单只带占位符不带真实 key；同时确认该文件不在任何 git 追踪/备份路径内。

**4. 纪律**
- render_client.js:113 白名单外单行：【有条件接受】。单行、纯增量（仅加 [测试] 徽标、未绕过白名单判定）、配合 render-contract §五清理纪律，可接受。条件：①行内注释标注「CB15 临时·待折入 token/白名单机制」；②不得以此为口子再叠加第二条白名单外改动；③在 C4 的 [dsh引擎] 前缀体系里一并收敛。
- 数据治理零改动：**符合纪律**。初始 dissolve 是按用户原令（括号标注 SSSQ）的合理解读，非擅自发挥；纠正后重新实证（1562/1562 唯一）→「无同名可合并」→零改动 + 删错误产物 + 教训入 memory，符合「无必要不改动、错误及时回滚」的治理原则。【改进】教训条目建议明确「中文缩写字段语义歧义→先确认字段再执行批量修改」，并确保 memory 已落盘可查。

**5. 建议**
- T1-T7 收尾指引：T1-T7 具体内容本会话无法核读任务书，按通用方法给——逐项对表任务书（每项=证据落盘+门禁更新+可复跑脚本），未落盘项一律标「待上传」，用户验收项单独列。建议优先闭环：未落盘 preset 清单 → T8 复跑脚本 → 8600 依赖文档。
- 换机复制清单（建议按此顺序）：①`_codex_cwd/AGENTS.md`（或先迁入仓）②config.toml（仅占位/env 方案）③codex-cli 0.149.1 npm vendor 安装方式 ④桌面版 models 备份+恢复 ⑤emotion_map 全量（DATA 大文件 + 重建 RAG 索引 `py tools/rag_index.py --build`）⑥MCP 8600 + 8080 启动与握手验证 ⑦门禁基线（597+2）⑧3 个未落盘 preset 待上传文件 ⑨memory/教训条目。
- 其他：①报告补一段「计数口径对照」（60 vs 当前 84）；②deepseek alias 表与「恒 flash」决策落盘到桥配置注释；③webSearch ok=false 定档为设计并标注；④T8 首测的 955 delta/68s 建议附复跑方法，避免成为一次性数据。

需要的话，你重启会话恢复文件读取后，我可以再对 §四 的代码级事实（render_client.js:113、8600 依赖、config.toml）做逐行核验并更新结论。
