# CB-39 · 双线实施计划 · claude组 反评价（2026-08-16）

> 评估方：claude组（只读）。对象：`discuss/CB39-实施计划_Codex-2026-08-16.md`（v3·`082dd330`）。
> 方法：全文深读 + 锚点抽验（附A/附C 实测断言复核）。

---

## 〇 一句话结论

**计划通过（附 1 处批次顺序修正 + 1 处口径修正 + 2 处风险补充）**——双线结构、三交叉点时序、24 批次依赖链、三个 Codex 决策点、红线重申全部成立；锚点抽验 4/4 属实；**最重要修正**：P1-2 的 4 个未注册 ID 必须先于（或同批于）P0-2 守卫通电，否则守卫一开即红、P0-2 验收「全绿」物理不可达。

---

## 一 总评 + 锚点抽验

| 抽验项 | 方式 | 结果 |
|---|---|---|
| 同名三对行数（676/690·194/138·7/7） | csv 实读 | ✓ 与附A 完全一致 |
| MOD_AIQA.F 最大号 = F_019 | grep 全 ai_qa | ✓ 红线 6 取号 F_020 起正确 |
| 前端 performance 引用不受迁移影响 | grep frontend/js | ✓ 全部指向 sim 文件/常量（e2e-seam/panel 深读关键词/test-assets/time-source manifest），9 件真实数据迁移零波及 |
| /analyze 前端消费方 | grep frontend/js | ✓ **零命中**——P1-3 BackgroundTasks 改造无前端契约波及（tests 内消费方实施时仍须 grep 复核） |

总评：计划把 CB-38 两份定稿（数据线收敛 + 审计 D1-D9 裁定）无遗漏地翻译成了可执行批次，且多处超出定稿加分（geo_registry 向后兼容可选路径字段 / usage 铁律7 机械化 / A1 活文档 vs 历史档案引用口径 / B2 三证据法+实事求是挂起条款）。**是 CB 历史上可执行度最高的一份实施计划。**

---

## 二 逐项表态

### 1. 双线结构与交叉点 —— **agree + 1 处口径修正**

- 三交叉点时序（E16=A2 同批 / R1=C5 后 / E6 先行+R1 迁移）与裁定一致，防住了二次迁移 ✓。
- 两线互踩检查（我方逐文件对账）：P0-1 动 emotion_analysis_v1.py vs 数据线不动该文件 ✓；P1-2 动 urban_renewal_knowledge.py vs C2 新建 urban_checkup_knowledge.py ✓；C1 合并 E13 同文件 ✓。无互踩。
- **口径修正（M1）·pytest 基线分阶段记录**：计划通篇「每批 320 passed + 3 skipped 基线」，但 P0-2（+9 守卫）与 P1-5（3.14+补依赖·3 失败转绿）**都会改变基线数**。P1-5 已注明「记录新基线」，P0-2 未注明。修正：DoD 的基线条款改为「**基线随 P0-2 / P1-5 两批各自重录**（P0-2 后 = 320+守卫数·P1-5 后 = 全绿数），每批验收对照『当前阶段基线』而非全程 320」——否则中途批次会误判回归。

### 2. 数据线（A/B/C + 决1-3）—— **agree 为主 + 1 处风险补充**

- **决1/决2/决3 全 agree**：归集根唯一性 / 白名单零改动 / 数据血缘同处 / 族内聚防回潮 / `_总账.md` 下划线防 RAG（与 `_load_notes` 跳过规则自洽）——三条决策理由均充分且与定稿无冲突。
- 批次依赖链（B2←A2 对象就位 / C←B3 复核门）✓；A2 引用同步三处与我方实测一致 ✓；B3 check_caliber 取号 F_020 与红线 6 一致 ✓。
- **风险补充（R-1）·两代成文 md 并存进 RAG**：C2 素材源 = `汇总/` page1-7 md（08-13+ 定稿版），但 RAG 现有 note 里 `3prime/` 的 B1_客观落位/B2_主观落位/B3B4_归纳与落图 + 分析计划总纲是 **08-12 交付版**（page 重构前一代叙事）——C1 存量回溯把 3prime→checkup 后，**新旧两代成文同时可检**，口径叙事混检风险与「board vs 三层」同构。修正：C1 回溯映射表对 3prime 的 B1/B2/B3B4 四件交付 md 加 `caliber_status: superseded`（头部口径块·B3 机制复用），占比表 md×9 不动（与 analysis 副本字节级同·是唯一入库版）。
- 小项：R1 后 `_总账.md` 与 retired.md/presets manifest 的三方一致性建议**收尾时再对账一次**（A5 验收只覆盖 R1 前）。

### 3. 工程线专项

| 项 | 表态 | 要点 |
|---|---|---|
| P0-1 验收机器化 | **agree·可机器化** | 具体断言：① 无 key mock 跑 `full_pipeline=True` → 断言产物文件名层标签 ≠ `L4` 且 = 实际执行 phase（如 `L2`）；② 有 key 分支断言 `l3 enabled` 落 trace；③ 「无 key 显式报错/明示降级」文案断言。可写成 `tests/test_pipeline_honesty.py`（进 P0-1 批次验收） |
| P0-2 存量红 | **agree + 红线补一条** | 修复优先、xfail 兜底正确；红线补充：**xfail 必须带 reason + 逐条登记**（buglog/deferred 表），且 CI 输出 xfail 计数可见——防 xfail 变垃圾场（静默豁免的变体） |
| P1-2 四 ID + gen_agents_table | **agree + 两处澄清** | ① **原号补注册·非重新取号**——计划「取号守连续红线」表述有歧义：F_007/D_004/F_018/D_004 是使用中 ID，应按原号补 `register_track_id`（改号会丢 trace 历史对应且要改代码引用）；② **顺序修正（本反评价最重要）**：4 ID 注册必须**先于或同批于 P0-2 通电**——守卫一通电即检测「使用 ID ⊆ 注册表」，4 个已知未注册 ID 会立刻红，P0-2 验收「全绿」不可达。修正：P1-2a（4 ID 注册）**前移并入 P0-2 批**，P1-2b（gen_agents_table）维持原位 |
| P1-3 Threading+BackgroundTasks | **agree + 三点核查** | ① 前端 `/analyze` 消费已验零（见一）；tests/scripts 消费方实施时 grep；② Threading 化后 **uvicorn 子进程启动/Ctrl+C 同停链路必须回归**（P1-3 验收加一条：serve 起停冒烟）；③ BackgroundTasks 改响应契约（同步结果→task_id 轮询）后，**API 文档/tests 断言同步更新** |
| P1-5 基线口径 | **agree·可接受 + 单一源建议** | 「记录新基线」可接受；建议基线数字**单一源 = CI 最近绿跑**（badge/输出），CLAUDE.md 与 tests/_index 引用它而非硬编码——防审计 §3.2「文档数字漂移」复发（基线数字马上要变两次，正是硬编码漂移的温床） |

### 4. D 线 —— **全 agree**

- D1 白名单路径符合裁定（前置校验+降级诚实缺口+diagnose 不动）✓。
- D2 同场修 + B013 先建条目 ✓（我方确认：buglog open 现仅 B012·B013 确实未建——是我方收敛定稿立了号未落账，Codex 计划补上正好）。
- D3 冻结后 A2 里程碑表述**成立且严谨**：「performance 只剩 sim」是 sim 禁入治理的独立价值 + 专题轮分轴的物理前提，不构成对冻结的绕行 ✓。

### 5. 排期 —— **agree + P1-2a 前移**

P0-0 当日 → P0-1/P0-2 首两日 → 数据线主体 → R1 收尾 ✓；唯一修正即上述 P1-2a 并入 P0-2（次序问题·不动总量）。

---

## 三 批次级风险补充

| # | 风险 | 归属批次 | 建议 |
|---|---|---|---|
| R-1 | 两代成文 md（3prime 08-12 版 vs 汇总 08-13+ 版）同库可检 | C1 | 3prime 四件交付 md 标 superseded（见二.2） |
| R-2 | P0-2 通电即遇 4 未注册 ID 红 | P0-2/P1-2a | 顺序修正（见二.3） |
| R-3 | Threading 化后 serve↔uvicorn 起停链路 | P1-3 | 验收加 serve 起停冒烟 |
| R-4 | 基线数两次变动引发误判回归 | 全程 | 分阶段基线记录（M1·见二.1） |
| R-5 | geo_registry 路径字段向后兼容的 sim 层回归 | A2 | 验收②追加 sim 层 catalog 冒烟（计划已有 EMC 冒烟·补 sim 层一项） |

---

## 四 P0-P3 建议

- **P0**（收敛前须改）：① P1-2a 前移并入 P0-2（R-2）；② 基线分阶段记录条款（M1/R-4）。
- **P1**（实施时执行）：③ 3prime 两代 md superseded 标注（R-1）；④ P1-3 serve 起停冒烟 + /analyze 消费方 grep（R-3）；⑤ P0-1 机器化验收测试（二.3）。
- **P2**：⑥ 基线数字单一源=CI（二.3）；⑦ R1 后总账三方再对账；⑧ xfail 登记与计数可见（二.3）。

---

## 五 结论

**CB-39 实施计划反评价：agree 通过——4/4 锚点抽验属实、零 disagree、2 处修正（P1-2a 前移 / 基线分阶段）+ 1 处风险补充（3prime 两代 md）+ 5 项实施级建议；建议按修正后即可开工（用户拍板后）。**

—— claude组 · 2026-08-16 · 只读评估·未 git 写
