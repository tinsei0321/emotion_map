# CB-39 · 实施计划反评价 · 收敛定稿（2026-08-16）

> 收敛方：claude组（汇总）。输入：反评价 Claude（`d9464b2d`）+ 反评价 zcode + 验收 Codex 审计线；**dsh 反评价未交**（缺位处置见 §六）。
> 性质：v3.1 变更单——Codex 按此修订后**用户拍板开工**。

---

## 〇 一句话结论

三方（Claude/zcode/Codex 审计线）**一致判定计划可开工**；合并出 **v3.1 变更单共 14 项**（P0 级 5 + P1 级 6 + P2/P3 级 3·均为一句话到一天量级·不动框架）；一处评估组结论冲突已实测裁定（api.js——两函数已定义但零调用·各对一半）；**一处 Claude 自我修正**（「前端零消费」grep 引号形式漏检模板字符串·诚实记录）。

---

## 一 三方结论对照

| 方 | 结论 | 关键产出 |
|---|---|---|
| Claude | 通过·2 修正 | P1-2a 前移（守卫通电前注册 4 ID·原号补注册）、基线分阶段记录、3prime 两代 md superseded |
| zcode | 通过·2 P0 修正 | Z1 C3/C4 source 互斥、Z2 前端 api.js 适配；Z3-Z8 六项批次级补充 |
| Codex 审计线 | 验收通过·3 微修 | 321 基线（或「≥320」）、py 前缀钉死、F_004 空洞顺带修 |

三方对框架本体（双线结构/交叉点时序/24 批次/决1-3/红线/DoD）**零分歧**。

---

## 二 结论冲突裁定（唯一一处·实测）

**P1-3 前端消费面**：Claude 判「`/analyze` 前端零消费」vs zcode 判「api.js:23/:33 同步等待合同·改后即断」。

**实测裁定**（收敛方二验）：[api.js:23-37](frontend/js/api.js#L23) `runAnalyze`/`runGovernance` 两函数**存在且为同步等待语义，但全前端零调用方**（grep 无 import/调用）——Claude 在「消费者」层面对、zcode 在「契约函数」层面对。**动作**：P1-3 动文件清单补 `frontend/js/api.js`——两函数随 BackgroundTasks 同步改为 task_id 轮询语义（**不留同步语义死代码**；若确认产品暂不需要前端分析入口，可删两个死导出并在批次注记）。zcode「实施当天前端分析入口直接断」的表述修正为「死契约函数腐坏」——严重度 P0 降 P1，但 Z2 修正动作本身全额保留。

**Claude 自我修正记录**：上一轮反评价「/analyze 前端零消费（已验）」的 grep 用了带引号字面量 pattern，漏检 `` `${BASE}/analyze` `` 模板字符串形式——方法论教训（与 maplibre-query 数组序列化同类的「引号形式盲区」），本定稿据实修正。

---

## 三 v3.1 变更单（合并三方·按级）

### P0（实施前必须并入）

| # | 来源 | 变更 |
|---|---|---|
| V1 | Claude | **P1-2a 前移并入 P0-2 批**：4 个未注册 ID（MOD_LLM.F_007/D_004·MOD_AIQA.F_018·MOD_SPATIAL.D_004）**按原号补注册**（非重新取号）先于守卫通电 |
| V2 | zcode-Z1 | **C3/C4 source 规则澄清**：fact 的 `source` = 所在 `ai_qa/outlet_kb/*.py#id`；presets manifest 与 `_总账.md` 以内容性指针字段（`layer_ref`）引用·不占用 source——消两批互斥 |
| V3 | zcode-Z2+裁定 | **P1-3 动文件清单补 `frontend/js/api.js`**（两函数改轮询语义或删死导出）+ task 状态存储设计（内存 dict+锁+历史清理）入批次 |
| V4 | Claude | **基线分阶段记录**：DoD 基线条款改为「P0-2 / P1-5 两批各自重录基线·每批对照当前阶段基线」 |
| V5 | zcode-Z3 | **C1 补目录级粗规则 + note 无域语义**：顶层 02-法律法规/03-指标体系→base、05/07 待定；note 允许 `domain=''`（无域·仅全域检索可见）——存量方法论 note 不被域过滤隐形 |

### P1（批次条款内落实）

| # | 来源 | 变更 |
|---|---|---|
| V6 | Claude | 3prime 四件交付 md（B1/B2/B3B4）标 `caliber_status: superseded`（C1 批） |
| V7 | zcode-Z4 | B1 usage 白名单**消费点列明**（前端 tools.js deriveAvailable / 后端 geo_routes 过滤）+ 验收补「既有 EMC 用例不因白名单回归」 |
| V8 | zcode-D1 | finalStep 白名单 **v1 收窄**：图层名白名单（精确匹配）+ observation 行格式校验（复用 `/命中\s*\d+\/\d+/` 先例）；数字白名单降 v2（先收集表述样本）——防误杀毁演示 |
| V9 | zcode-Z5+Codex | P1-5 位置固定「数据线完成后·R1 前」+ 新基线附差异说明；**门禁命令钉死 `py` 前缀**（禁裸 python·3.13 缺 matplotlib 是已知坑）+ 基线口径用「≥ 当前阶段基线」 |
| V10 | zcode-Z6+Codex | gen_agents_table **机械区/人工区分区设计先行**（CI 只校机械区）；P1-2 顺带修 MOD_AIQA.F_004 编号空洞（003→005 存量跳号·红线违例补登或回收） |

### P2/P3

| # | 来源 | 变更 |
|---|---|---|
| V11 | zcode-Z7 | A4 `_retired` 9 件逐件列名（v1 xlsx 去向一行定） |
| V12 | zcode-Z8+决3 | C2 批注「拷贝后 00-宜昌专项 版本为权威·analysis 原件只读」；决3 措辞改「加载器不扫 DATA/analysis·下划线为冗余第二道」 |
| V13 | zcode-P2 | B2 三证据→四证据（补两版生成时间与生成脚本） |
| V14 | Claude/zcode | xfail 必登记 deferred 同款机制（有主有期限月度复核）；xfail 无登记=违反「不许静默豁免」；P1-3 验收加 serve↔uvicorn 起停冒烟 |

---

## 四 Codex 三微修裁定（全部采纳）

1. **基线 321**：实测 321+3 与计划 320+3 差 1——采「≥ 当前阶段基线」口径（V9 合并），防数字漂移假红。
2. **py 前缀钉死**：双解释器实锤（`py`=3.14.6 vs `python`=3.13.2）——门禁命令全部 `py -m pytest`，进红线条款（V9）。
3. **F_004 空洞**：实测确认 003→005 跳号——P1-2 批顺带处理（V10）。

---

## 五 开工条件（Codex 侧）

1. Codex 按 V1-V14 出 **v3.1 增量修订**（可直接在计划文档追加「v3.1 变更记录」节·不必重排全文）。
2. P0-0 当日动作：**用户 key 轮换**（SOP `tools/KEY_ROTATION.md`·顺带确认 AMAP geocode 服务开通）+ KNOWLEDGE.md 密钥条款（zcode 复核确未落）。
3. 用户拍板后按批次开工·每批一 commit 先验后推。

---

## 六 dsh 反评价缺位处置（用户定）

dsh 未交 CB-39 反评价（discuss/ 无其文件）。两个选项：
- **a（推荐）**：dsh 补交后并入（其 CB-38 三条纪律/E11/E15 均被本计划吸收·本轮缺席影响有限·但按「三组反评价通过后实施」的任务书字面应等）；
- **b**：用户明示豁免 dsh 本轮（v3.1 修订与开工不等它·dsh 意见后补只计入飞轮）。

---

**CB-39 收敛：三方一致通过·14 项 v3.1 变更·1 处冲突实测裁定·待 dsh 缺位处置（用户定）+ 用户拍板开工。**

—— claude组 · 2026-08-16 汇总收敛
