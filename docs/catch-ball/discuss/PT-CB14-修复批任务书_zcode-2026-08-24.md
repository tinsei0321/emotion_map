# PT-CB14 · 修复批任务书（zcode 主手·2026-08-24）

> 依据：`PT-CB14-回收裁决_zcode-2026-08-24.md`（批一+批二+批三合并执行·用户令分配：**复杂类=Qoder·简单类=claude**）。
> 环境：分支 `EMC_harness_dsh`·基线 574+2·服务栈在跑（8600/3080/8080）。
> 纪律：白名单制；~/.dsh 改动先备份；测试纪律 R25（真实问题×3）；git 由主手代提交。

---

## Qoder 执行包（复杂类·D-3 形态治理·估 1 天）

### D-3/R2+R3：emc-test headless 思维管控 + 出图直出 + 超时重试

**目标**：把"单轮无管控"形态治成"有管控单轮"——发散概率压到 <10%、出图类问句直出不反问、偶发超时自动重试。

| # | 子件 | 规格 | 文件 |
|---|---|---|---|
| Q1 | **effort 档位** | emc-test profile `settings.yaml` 加 `reasoningEffort: low`（或 medium·二选一实测后定：low 快但复杂分析可能欠思考·medium 折中——**各跑 3 次真实分析题对比后定档**·记录数据） | ~/.dsh/profiles/emc-test/settings.yaml |
| Q2 | **步数预算** | 摸索 rc.2 的步数限制机制（rc.8 时代用 guard 插件·rc.2 官方是否有原生 maxSteps/预算配置——查 `dsh --help`/官方文档/`~/.dsh` 配置面）；有原生=配置之；无=评估轻量方案（超时本身即预算·只记录结论不强做） | 同上+调研记录 |
| Q3 | **persona 引导** | emc-test 挂 EMC persona（两句话版：①先 list_data 查清单再操作·口径纪律遵守 ②**出图类问句（含"显示/出图/铺到地图"字样）直接 render_spec/render_file 出图·不反问**）；挂载通道按 rc.2 实测（agent preset/patch/profile 均可·能生效即正解） | ~/.dsh（挂载方式实测定） |
| Q4 | **超时自动重试** | `api/aiqa_routes.py` `_run_dsh_sync` 或调用侧：失败（timeout/空 stdout）时**自动重试一次**（间隔 2s·重试在响应里标注 `retried: true`）；两次都失败才报错 | api/aiqa_routes.py |
| Q5 | **稳定性实测（R25 纪律）** | 修复后跑 3 个真实复杂分析题（停车位小区/12345 top10/西陵最差社区）**各 3 次**=9 轮：成功率 ≥8/9 达标；出图类问句 3 次全部直出图不反问 | 记录入执行记录 |

**验收**：Q5 数据达标+各子件配置在位有回执。白名单：`~/.dsh/`（先备份到独立目录）+ `api/aiqa_routes.py` + 执行记录 `PT-CB14-修复批Qoder执行记录_Qoder-2026-08-24.md`。

## claude 执行包（简单类·四件·估 0.5-0.8 天）

| # | 项 | 规格 | 文件 | 验收 |
|---|---|---|---|---|
| C1 | **D-4 qty 点层注册** | `_POINT_LAYERS` 加 11 个 qty 层（GeoJSON 格式·照 `subj_12345_*` 先例：第 4 元素子目录 'DATA/boundaries/presets'·level='CHECKUP'·get_layer_points GeoJSON 分支已有）；**注意文件在 presets 目录**（非 analysis）——路径先实测一层 | core/geo_registry.py | `list_data` 点层 12→23；`zonal_stats(layer='qty_民生_停车设施', boundary='checkup_cfg_community')` 可跑通 |
| C2 | **D-1 清单一致性** | list_data 的 presets 循环加 available 过滤（`os.path.isfile(预设目录/文件)` 判断·与点层段同纪律）；`resolve_boundary` 的 FileNotFoundError 文案改「文件未落盘·需上传：{file}（manifest 已登记）」 | tools/mcp_server_emc.py + core/geo_registry.py | admin_community 不再出现在清单；resolve 报错含"未落盘" |
| C3 | **D-6 测试件标记** | render_spec 的 data_nature 参数增 'test' 值支持（徽标显示 [测试]）；写一行清理纪律进 render-contract §五（测试 spec 用毕删） | tools/mcp_server_emc.py + docs/render-contract.md | data_nature='test' 投递→前端徽标 [测试] |
| C4 | **D-7 引擎徽标** | 8080 对话框常驻引擎徽标（light/dsh/mock·当前 engine 模式显示）；dsh_engine 端点报错文案加管线前缀 `[dsh引擎]` | frontend/js/ai_qa/panel.js + brain-adapter-dsh.js | 徽标可见·切 engine 徽标跟随 |

**验收**：四件各有测试或实测回执；全量门禁 574+2 零退化（上浮注明）。白名单：上述文件+测试文件+执行记录 `PT-CB14-修复批claude执行记录_claude-2026-08-24.md`。

## 执行顺序与回收

- 两包**并行**（文件不相交：Qoder 动 ~/.dsh+aiqa_routes·claude 动 geo_registry+mcp_server+前端）；
- 各自完成后停下等主手回收（抽检+门禁复核）；
- 全部回收后：T1-T7 重跑窗口（Qoder 重出用户指引）→ 体检医生专题开门。

## 测试纪律（R25·两执行方均适用）

修复涉及模型行为链路（Q1/Q3/Q4）→ 验收必含真实复杂问题×3 次稳定性数据；检查代码先验字段名。

---

> 用户令分配：复杂=Qoder·简单=claude。本任务书即定稿，两 prompt 随交付用户转发。
