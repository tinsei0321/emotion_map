# PT-CB16 · C2 交互三机制实施记录（Kimi·2026-08-25）

> **依据**：`PT-CB16-D1-交互三机制定稿_Kimi收敛-2026-08-25.md`（裁决点⑤按 A 定案）+ `docs/interaction-contract.md`（C2-0 冻结契约）。
> **门禁**：**613 passed / 1 skipped**（607+1 → 净 +6：scale×4 + followup×2；零失败零退化）。
> **git**：未 commit（建议前缀 `PT-CB16(C2)`·主手/用户代提交）。

## 白话摘要

三条「懂事的规矩」装好了。①**会接话**：你问「那第 8 个呢」，工具直接附好「重跑这件事需要的全部信息」，不用重查一遍数据；②**会请示**：遇到「两个口径都合法」这类岔路口，规矩已经写进 AI 的「员工手册」（身份卡）和工具说明书——130 vs 193 这种事以后它会先问你；③**会看事情大小**：系统自动数 AI 每次实际用了几件工具——小问题是「短平快」的跑法就安安静静，小问题用了大阵仗就悄悄提醒你一句（不弹窗、不打断）。

## 一、完成清单（C2-0 → C2-3 → C2-2 → C2-1）

| 件 | 内容 | 实证 |
|---|---|---|
| C2-0 规格冻结 | `docs/interaction-contract.md`：cue 两级 schema（resolved_params）/分支 ask 与缺槽 ask 划界表/告知式降级文案/scale 倒推判据表/接口锁版本化路径 | 契约落盘 |
| C2-3 scale 倒推 | `_TOOL_CALL_LOG` 埋点（13 个分析工具）+ 爆发段切分（>120s 切段防跨问题误判）+ `infer_scale` 确定性判级 + outlet_card 挂 `scale_check`（判对静默·判错一条注记） | 4 测试全过；判据表实测校准一处（brief 链 2 类工具属正常·契约同步改） |
| C2-2 选择机制双写 | 身份卡 EMC-IDENTITY-01 增【分支请示】【规模自觉】两纪律段（三触发/单轮≤1/告知式文案/划界）+ zonal_stats docstring 同步 + RAG 索引重建（四载体纪律·375 chunk） | 身份卡全文落盘；索引 19:45 重建 |
| C2-1 followup 两级 | `_action_cues` 派生（top_n 增量带全量参数·rank by 翻转·提示级跨工具）挂载 zonal/rank/grid 输出；rag_query 新增 `followup_actions` 键与旧 `followup_cues` 并存（接口锁版本化）；followup.js 对象兼容（防 [object Object]） | 2 测试全过；panel/mock-peer 兼容面核查无破坏（见 §三） |

## 二、关键设计落点（与 D1 定稿对齐）

- **resolved_params**：参数级 cue 的 params 为完整可执行参数集（从本轮调用复制+改一处），重放零历史依赖——与 render 契约 dataset_id 单号同思想（Qoder 调整①落实）；
- **确定性倒推**：scale 判级零 LLM——判据=当前爆发段调用数/工具种数/耗时（Qoder 调整③落实）；判对静默判错软提示（静音条件落实）；
- **白名单评估结论**：cdh 侧模型以纯文本提问即可（无 UI 选项按钮·A 案既定），无需动 ask 工具白名单；告知式文案已入身份卡；
- **接口锁**：rag_query 旧 `followup_cues`（文本数组）保留过渡，新结构走 `followup_actions` 新键——旧键退役须另走 CB 裁决+消费方清零。

## 三、兼容核查（Qoder 预警的三处）

| 点 | 核查结果 |
|---|---|
| followup.js normalize | 已改对象兼容（取 cue_text）·action 载荷不透传（UI 一键重放归 C2-4） |
| panel 渲染 | 经 normalize 单点进入·对象形态安全 |
| mock-peer（acp-mock-peer） | 仅生产文本 cue·不消费对象·无破坏 |

## 四、改动文件

- 新增：`docs/interaction-contract.md`、本记录、`PT-CB16-D1-交互三机制定稿_Kimi收敛-2026-08-25.md`（已定稿提交）、`PT-CB16-C2-4评估_cdh侧交互UI载体_Kimi-2026-08-25.md`
- 修改：`tools/mcp_server_emc.py`（scale 埋点+infer_scale+_action_cues+三工具挂载+outlet_card scale_check）、`ai_qa/outlet_kb/urban_renewal_knowledge.py`（身份卡两纪律段）、`frontend/js/ai_qa/followup.js`（对象兼容）、`tests/test_mcp_server_emc.py`（+6 测试）
- 派生：`DATA/RAG/rag_index/`（19:45 重建·含新身份卡）

## 五、验收口径（R7 三态：重启 8000+硬刷新 8080）

1. 续问「那第 8 个呢」→ rank/zonal 返回的 followup_actions[0].params.top_n=8 且参数完整——宿主按此重跑 ≤1 次；
2. 问「12345 最差 7 社区」但用 130 层表述 → AI 应先请示口径（身份卡纪律·实测观察）；
3. brief 问题连调 5+ 工具 → outlet_card 返回带 scale_check（判错注记）；正常 brief 链 → 无 scale_check（静默）；
4. 门禁 613+1 零失败。

## 六、遗留

- C2-4 评估件已落盘（cdh 侧胶囊/选项 UI 载体·bridge 透传范围与成本）——**待主手裁决是否立项实施**；
- 身份卡口径文本（两纪律段措辞）随 K6 同类草案一并向用户过目；
- commit+push（规则七）；home 机到岗重建索引。

---

> Kimi · 2026-08-25 · C2 全件完成（C2-0/2-3/2-2/2-1）+ C2-4 评估件。
