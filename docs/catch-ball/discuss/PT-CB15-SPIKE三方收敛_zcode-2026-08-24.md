# PT-CB15 SPIKE · 三方审计收敛（zcode·2026-08-24·报用户终裁）

> 输入：zcode 独立审计（10 发现）+ claude 审计（101 行）+ kimi 审计（102 行）。三方交叉对账。

## 一 白话摘要

三位审计员（我自己+claude+kimi）把「换脑桥」各自验了一遍——**结论一致：能用·但转正前有几笔工程债必须还**。最有意思的是三方的检查结果：最要命的一个坑（看门狗看不住门）只有我抓到·「一次测试都没写」只有 claude 抓到·「用户自测指引」只有 kimi 想到——**三人加起来才看全**。处置表已列·修完即转正。

## 二 三方交叉对账

### 互证发现（多组同抓=高置信）

| 发现 | zcode | claude | kimi | 判定 |
|---|---|---|---|---|
| `_SPIKE_CWD` 硬编码盘符 | Z-03 | B-3 | C-3 | **三组全抓**——必改（{REPO} 同级推导） |
| stderr 丢弃诊断面 | Z-05 | B-5 | — | 双组——P2 |

### 独有发现（互补面·交叉价值实证）

| 来源 | 发现 | 级 |
|---|---|---|
| **zcode 独有** | **Z-01 看门狗「有流量即续命」**（预算只在静默分支检查） | **P1** |
| zcode | Z-02 tool begin/end 配对断裂（item.id 未透传） | P2 |
| zcode | Z-06 seq 缺口检测未实现 | P2 |
| **claude 独有** | **5925118 零测试新增**（581+2 是 PT-CB14 基线·spike 无 test 文件） | P2 |
| claude | B-4 model 配置硬编码（-c model_provider 在代码内） | P2 |
| claude | R-2 SSE 帧分隔 `\n\n` 两端约定（换 SSE 库/代理即断） | P3 |
| claude | A-2 双轨并存未验证（降级卡+部分答案并存语义） | P3 |
| **kimi 独有** | C-2 「同机只跑一个后端」运维纪律入自测指引 | P2 |
| kimi | C-5 用户五步自测卡 | P3（产品件） |
| kimi | C-6 占位符 provenance 处置+首字占位符 | P3 |
| kimi | C-7 用量计量接入 | P3（正式化） |

## 三 收敛处置表（报终裁）

| 优先级 | 项 | 来源 | 修法要点 |
|---|---|---|---|
| **P1 必修** | Z-01 看门狗 | zcode | 循环内每行后检查预算·与静默分支同收口 |
| P2 | Z-02 配对断裂 | zcode | bridge 透传 item.id·前端 end 复用 |
| P2 | cwd 硬编码 | 三组 | {REPO} 同级推导 |
| P2 | Z-04 npm vendor 硬编码 | zcode | 多候选探测+fail-closed |
| P2 | B-4 model 配置 | claude | 入仓外 config·复刻清单 |
| P2 | Z-05/B-5 stderr | 双组 | 环形缓冲末 4KB·error 携带 |
| P2 | Z-06 seq 检测 | zcode | n 连续性·跳号按 error |
| P2 | 零测试补测 | claude | 转正任务书按 C9-C12 口径补（SSE 解析层可离线测） |
| P2 | C-1 map.js 样式竞态 | kimi | 既有 bug·等 styledata/failed 重试+告警 |
| P2 | C-4 inbox 竞争架构修 | kimi | 实例锁字段或消费扇出 |
| P2 | C-2 运维纪律 | kimi | 自测指引第一条「同机只跑一个后端」 |
| P3 | Z-07~Z-10 + B-6/R-2/A-2/C-5/C-6/C-7 | 三方 | 记账·随正式化 |

**总判**：**有条件通过**——P1 一件（看门狗）·转正前必修；P2 十一件·转正批合并修；P3 九件记账。**全部不阻塞「今天就能实测」**（单用户正常流无触发）。

## 四 双能力快评（本次审计）

- **claude**：「零测试新增」是三方最有分量的独有发现（spike 没写测试=转正最大风险）·SSE 分隔/R-2 细节敏感性好——维持 A-。
- **kimi**：运维纪律（C-2）和自测卡（C-5）是纯产品视角产出·补了三方唯一缺的用户面——审计转向产品面时价值显著。
- **三方交叉模式再实证**：P1 靠 zcode·测试缺口靠 claude·用户面靠 kimi——单方审计必然漏面。

## 五 蒸馏（已同步 debug-memory·R26/R27）

见 `docs/debug-memory.md` R26（看门狗语义）+R27（跨进程桥边界六坑）——本收敛同批入册。

> zcode 主手 · 2026-08-24 · 三方收敛·报终裁
