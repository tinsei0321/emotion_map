# PT-CB10 · 收口批任务书（zcode 主手·2026-08-21）

> 依据：`PT-CB10-全局收敛与控制线` §三。阶段门纪律：本任务书即定稿——批准后开工，批内完成后 claude 审计，销号后才进阶段 B（PT-CB11 RAG 重建）。
> 分工：zcode=判裁/清账/评估（C2规格+C3+C4）· Codex=实现（C1+C2实现）· claude=审计首用。

---

## 任务总览与时序

```
C1 入口v2插件重写（Codex） ──────────┐
C2 挂账顺修（zcode 规格 → Codex 实现）┤ → 审计（claude）→ 销号 → 阶段B
C3 Qoder评估尾段（zcode）────────────┤
C4 台账/todo/交接卡清账（zcode）──────┘
```

各任务独立可并行；C2 规格先行（zcode 当天出·Codex 后接）。

---

## C1 · 入口 v2 插件重写（模型当向导）——执行：Codex

- **任务书已签发**：`PT-CB8-入口v2设计任务书_zcode-2026-08-21.md`（流程规格/send 固定文本/复用与移除/验收五项）。
- **服务端已就绪**：`emc_status` 第 10 工具（F_032·三态实测过）。
- **白名单**：`vendor/dsh-emc-entry/`（**已入仓·唯一权威**·在仓内改）+ `~/.dsh/profiles/web/`（改前备份）；执行记录落 `PT-CB10-C1执行记录_Codex-2026-08-21.md`（唯一 EMC 仓新写入物）。
- **验收**：设计任务书 §五 五项（对话流进度→就绪自动弹默认浏览器→欢迎引身份卡→无乱码→token 零硬编码）截图留档。
- **双环境规则七落点**：插件在仓内改=进 git 自动同步；依赖指向仓内 file: 路径（两机同构）；`~/.dsh` 仅两处 profile 依赖行（复刻清单随执行记录）。

## C2 · 挂账顺修批（6+1 件 + 纪律固化）——规格：zcode · 实现：Codex

| # | 项 | 规格（zcode 已定） | 白名单 |
|---|---|---|---|
| C2-1 | A9 收窄：`_reject_analysis_output` 宽 except fail-open | manifest 不可用时收窄为「仅 preset 存在才判·其余显式返回 ok+拒绝语义」·禁静默 pass | tools/mcp_server_emc.py |
| C2-2 | inbox 自动清理 TTL（A6 补偿） | spec 消费后（applied/）自动归档 + 7 天 TTL 清理·保留当前 applied 机制 | api/render_routes.py |
| C2-3 | `_count_norm` 抽公共函数 | render_client.js 与 shared.js buildZonalFc 同公式合一·单源防漂移 | frontend/js/render_client.js + toolbox/shared.js |
| C2-4 | dataset 端点字段白名单 | 点层返回属性白名单（名称/坐标/极性/领域/指标类）·禁透传办件编号等准标识 | api/render_routes.py |
| C2-5 | K-C1 补 118 行 | 口径注册表 K-C1 社区枚举补 118（12345 西陵+伍家岗聚合面）·注派生关系 | docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md |
| C2-6 | demo 点层 preset 注册裁决+落地 | 裁决=注册进 geo_registry 点层表（dict send-in 改注册）·验证 demo_pioneer 改注册引用 | core/geo_registry.py + tools/demo_pioneer.py |
| C2-7 | 微调：render_file 入快分析档白名单 | 评估结论=入（用户「显示到地图」核心链路·T18 已验）·更新 emc-analysis guard 白名单 | ~/.dsh/profiles/emc-analysis（复刻清单同步） |
| C2-8 | D9 豁免纪律固化 | tracker 纪律条款：「高频循环扫描类（≥1s 周期）免 @track·注册表描述注明」 | docs/（tracker 纪律节） |

**验收**：各项有测试或实测；全量门禁 442+2 零退化（上浮注明）；C2-7 改后 emc-analysis 冒烟。

## C3 · Qoder 执行效果评估尾段——执行：zcode

- 范围：PT-CB7 后半（五连修 T18/T16/T19/T21/T15 + dsh 两批）+ 对照 dsh 前几批。
- 产出：`PT-CB10-C3_Qoder执行效果评估_zcode-2026-08-21.md`（评级+依据+下一批改进点）。
- 白名单：该文档（纯文档·半天）。

## C4 · 台账/todo/交接卡全局清账——执行：zcode

- 范围：台账 R56（收口批执行）+ todo 08-21 收口段 + 交接卡当前节点刷新（收口批进行中→销号后→阶段 B 续点）。
- 产出：随各任务 commit 增量更新（不单列文档）。

## 审计线（claude 组·首用）

- 时机：C1/C2 回收后统一发（一次审 C1+C2·C3/C4 文档随附）。
- 审计任务书由 zcode 届时签发（四件清单+基准 442+2+红线七项）。

## 纪律重申

先定稿再执行（本任务书=定稿）；未 push 不算交付（规则七）；白名单制；门禁 442+2；追踪 ID 连续（C2 若新增 F_033 起由主手注册）；审计通过才销号。

---

> 批准（用户确认）→ 主手立即开工 C2 规格 + C3；C1 派发 prompt 随本任务书交用户转发 Codex。
