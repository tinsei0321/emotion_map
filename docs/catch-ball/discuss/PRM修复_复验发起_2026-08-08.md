# 情绪地图 · PRM-03/04 修复复验 — 发起（claude组 · CB-19 补）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB 修复复验（两组·先验后推）
> **承接**：glm 发版回归 + claude 数据核对 → PRM-03/04 真根因确认（多工具绕过 G5）→ claude 修复（commit `083b78d`·**未 push**）→ 请两组复验
> **目的**：让两组对 PRM-03/04 修复独立复验（逐项核对 + 有无新问题）+ PRM-05/07 后置决策确认，通过后 push

---

## 〇、修复摘要（claude组 已完成·待两组复验）

### PRM-03/04 根治（commit `083b78d`·未 push）

**真根因**（08-08 浏览器复现确认）：LLM 的 FC 输出多工具（如 `[lookup_place, merge]`）时，orchestrate 直接走 `runAllToolCalls`（harness.js:1126 `_allToolCalls.length > 1`）→ **绕过 G5 路由修正**（:1529）→ 「周边 Nm 情绪」被错路由成 merge/lookup_place（实测「merge([L002,L003]) 合并 2 地块」）。

**修复**（harness.js:1529-1535）：
```
// G5 路由修正 + PRM-03/04：多工具错路由 → 重写 _allToolCalls 为 buffer 单 call
if (/(周边|附近|半径|缓冲|米内|公里内)/.test(q) && /情绪|点|分布/.test(q) && tool !== 'buffer' && !/(叠|裁|筛选)/.test(q)) {
  diagnose.template = 'buffer';
  diagnose.method = ['buffer()'];
  if (Array.isArray(diagnose._allToolCalls) && diagnose._allToolCalls.length > 1) {
    diagnose._allToolCalls = [{ name: 'buffer', params: {} }];   // 多工具错路由 → 强制 buffer 单 call
  }
}
```
- **改动点**：① G5 修正加 `_allToolCalls` 重写（对齐 PRM-07/10/08 多 call 重写模式·:1491/:1523/:1557）② 去掉 `/(合并)/` 排除（问句无「合并」词时不该被排除——是 LLM 错路由）。
- **验证**：浏览器复现「大南门·二马路滨江片区周边 300 米」3 次问**全部 buffer/ask_user·绝不 merge**（修复前 merge 错路由）+ `verify_prm03_fix.py` 守卫 + pytest 301 零回归。

**修复后行为**：问句命中「周边 Nm 情绪」→ 强制 buffer 单工具 → center 可 derive 则执行（radius 传对）+ center 缺则 ask_user（flywheel test-cases:330「center 缺 ask_user → PASS」）。**两条路都 PASS·merge 错路由被堵死**。

### PRM-05（zonal boundary derive）·后置决策

- **根因**：`deriveAvailable('西陵区范围内按面聚合情绪统计')` 偶发返回 null（词边界 `_validAfter` 判断·Flash 方差）→ boundary 未 derive → 计划1→未生成。
- **判定**：仅 Run 2 出现（Run 1 过·boundary derive 偶发）·**Flash 方差·非高频·非并发引入** → **后置**（glm 也判已知 backlog）。

### PRM-07（法定功能区白名单）·后置决策

- **根因**：LLM 直接调 zonal 传「小溪塔」GeoJSON → 前端 deriveAvailable 白名单（FIXED_ADMIN_DISTRICTS）只拦 derive 取值·**LLM 直传 GeoJSON 绕过** → 后端 resolve_boundary dict 直通执行（违反「EMC 不硬猜」CB-14）。
- **判定**：改前端所有工具边界校验 = 大改动（触「用户上传层不受限」设计）·实际演示不常触发（法定功能区问句少）·**后置**（glm 也判已知 backlog）。

---

## 一、复验焦点（两组独立复验）

### 焦点 1：PRM-03/04 修复正确性
- G5 修正加 `_allToolCalls` 重写——是否完备？（有没有其他「周边 Nm 情绪」绕过路径？）
- 去掉 `/(合并)/` 排除——有无副作用？（问句真含「合并」但也要 buffer 的场景？）

### 焦点 2：修复边界
- 修复是否覆盖所有「周边 Nm 情绪」问法？（含「附近/半径/缓冲/米内/公里内」变体）
- center 缺失走 ask_user 是否正确？（flywheel test-cases:330 判据）

### 焦点 3：PRM-05/07 后置决策
- PRM-05（boundary derive 方差·Flash 偶发）后置——认可？
- PRM-07（dict 直通·触「用户上传层不受限」）后置——认可？还是应本轮做（发版候选前）？

### 焦点 4：回归风险
- G5 修正改动对现有路由影响？（clip/extract/merge 等问句是否被误改 buffer？）
- `verify_prm03_fix.py` 守卫够不够？

### 焦点 5：两组补充与异议
- 对修复/后置决策有无异议？（用错标尺/事实错误/承重红线→指出）
- 各自环境有无已跑验证可复用为复验证据？

---

## 附 A：PRM-03/04 修复复验 prompt（发两组用）

```text
【CB-19 · PRM-03/04 修复复验】情绪地图 — 周边→buffer 路由修正 + 多工具重写

你以第三方独立验收身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·commit 083b78d 未 push·工作区已同步）。可跑只读测试（pytest / browser e2e-seam 直测）作为复验证据。

背景：glm 发版回归 B3 fail 集含 PRM-03/04（buffer radius）。claude 复现确认真根因 = LLM 多工具错路由（merge/lookup_place）绕过 G5 周边→buffer 修正 → 修复（G5 加 _allToolCalls 重写 + 去合并排除）。请对修复独立复验。
必读：
  docs/catch-ball/discuss/PRM修复_复验发起_2026-08-08.md（修复摘要 + 5 焦点）
  docs/catch-ball/discuss/PRM_backlog预检_2026-08-08.md（PRM-03/04 真根因 + PRM-07 方案）
  docs/catch-ball/discuss/发版回归_结果_glm组_2026-08-08.md（glm 回归·注意 claude 数据修正）
按需核实：frontend/js/ai_qa/harness.js（:1529-1535 G5 修正·:1126 runAllToolCalls·:1491/:1523/:1557 多 call 重写）·
  tests/browser/verify_prm03_fix.py（守卫）· frontend/js/ai_qa/tools.js（:8 FIXED_ADMIN_DISTRICTS·:617-640 deriveAvailable）·
  frontend/js/test-cases.js（:312-313 PRM-03/04 断言·:330 center 缺 ask_user 判据）

复验任务（5 焦点·逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. PRM-03/04 修复正确性（G5 加 _allToolCalls 重写完备？去合并排除副作用？）
2. 修复边界（覆盖所有周边 Nm 问法？center 缺 ask_user 正确？）
3. PRM-05/07 后置决策（boundary derive 方差 + dict 直通——认可后置？还是应本轮做？）
4. 回归风险（G5 改动对 clip/extract/merge 影响·verify 守卫够不够）
5. 补充与异议

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（编排器确定性·Smart 两端 + Dumb 中间）。
承重红线：四态出口 / finalStep D019 / diagnose 永不动 / 追踪编号连续 / 「EMC 不硬猜」（CB-14）。
产出：docs/catch-ball/discuss/PRM修复_复验回应_{你的组名}_2026-08-08.md
格式：〇 一句话结论 / 一 逐焦点 / 二 新问题风险清单（阻断/警告/建议）/ 三 可否 push + 进发版候选结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · PRM-03/04 修复复验发起 · 2026-08-08*
*commit 083b78d 未 push·两组复验通过后 push + 进发版候选·PRM-05/07 后置*
