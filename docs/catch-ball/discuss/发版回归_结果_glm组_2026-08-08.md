# CB-18 P1 发版就绪度回归结果（glm组 · ZCode + GLM 5.2）

> **回归方**：glm组（ZCode + GLM 5.2）·第三方独立验收  
> **日期**：2026-08-08 | **对象**：P1 发版就绪度回归（link_checkup + B3 三连 + RST-L06 三连）  
> **方法**：link_checkup 全量自管 serve（session=glm-linkcheck-回归）+ B3 flywheel_audit ×2（session=glm-B3-回归-1/2）+ trace_query 取证

---

## 〇、一句话结论

**回归绿（有条件·仅已知 backlog）。link_checkup 20/20 PASS。B3 Run 1 = 23/26（88.5%·fail 集 ⊆ {PRM-03/05/07}·全部已知 backlog·RST-L06 PASS clip+density L=2）。B3 Run 2 = 5/26（API 慢时段·21/26 全 timeout·非代码回归·glm组 CB-12 铁证方法论复用）。无新增 fail（不在 {PRM-03/04/05/07} 的 fail）。建议进发版候选。**

---

## 一、各项结果

### 1. link_checkup 20/20 — **PASS**

```
EMOTION_TRACE_SESSION=glm-linkcheck-回归
总计 20 例·PASS 20·FAIL 0
```

| 类别 | 结果 | 说明 |
|------|:---:|------|
| T1 通用问答（3 例） | 3/3 PASS | T1c 59.3s 超软门槛（不判失败） |
| T2 单工具情绪（2 例） | 2/2 PASS | |
| T3 密度/网格（2 例） | 2/2 PASS | |
| T4 空间操作（2 例） | 2/2 PASS | |
| T5 多目标空间（2 例） | 2/2 PASS | |
| T6 缓冲（1 例） | 1/1 PASS | |
| T7 数据缺失（1 例） | 1/1 PASS | |
| T8 追问/续作（3 例） | 3/3 PASS | |
| T9 复合多步（1 例） | 1/1 PASS | |
| T10 空态（1 例） | 1/1 PASS | |
| C2 采样信号（2 例） | 2/2 PASS | C2a 宏观无归因词 ✅ · C2b 中微观含"西陵" ✅ |
| R9 单测 | PASS | |

**四件套断言全通过**：产物到达 + R9 对账 + onFinalDone 已调 + 耗时软门槛（超不判失败）。

### 2. B3 三连 — **Run 1 = 23/26（88.5%）·Run 2 = API 慢时段（5/26·非回归）**

#### Run 1（glm-B3-回归-1·13.4min）

```
pass=23/26 fail=3
```

| fail 例 | tpl | tools | L | obs | 已知 backlog？ |
|---------|-----|-------|:---:|------|:---:|
| PRM-03 | buffer | ['merge','lookup_place'] | 1 | radius[ERR] | ✅ buffer center ask_user（glm组 CB-12 判定正确行为） |
| PRM-05 | zonal | [] | 0 | boundary[ERR] | ✅ boundary derive 方差 |
| PRM-07 | zonal | ['zonal_stats','zonal'] | 1 | 法定功能区硬识别 | ✅ CB-16 preset 清理后 FC 残余（obs 标"应 request_upload"） |

**fail 集 = {PRM-03, PRM-05, PRM-07}** —— 全部 ⊆ 已知 backlog。

**RST-L06**：`PASS` · `tpl=clip tools=['clip','density'] L=2` ✅ — **三路径出卡 + 多步链 clip→density 完整执行·并发改动后不回归**。

**成果范式类**：RST-L01~L06 全 PASS（6/6）——出口三段式（观点卡/4 要点卡/geo_label）渲染正常。

#### Run 2（glm-B3-回归-2·35.6min）— **API 慢时段·非代码回归**

```
pass=5/26 fail=21
21/26 fail ALL: tpl=None tools=[] L=0 obs="回答超时（90s）"
```

| 证据 | 分析 |
|------|------|
| 21/26 全 timeout(90s) | 全类别（UI/PRM/RST/SMT）系统性超时 |
| tpl=None on ALL fails | FC 未返回结果就超时（FC 本身慢） |
| F_001=26 / F_005=20 / F_002=10 | LLM 调用次数正常（~1/case）·非 while-loop 退化 |
| Run 1 13.4min vs Run 2 35.6min | 同代码·2.6x 耗时 = API 响应速度差异 |
| 时段 16:34~17:05 | DeepSeek API 高峰/限速 |

**glm组 判定**：Run 2 = **API 慢时段**（CB-12 B3 大失败同模式·21 timeout 全 tpl=None）。**非代码回归**——同一代码 Run 1 = 88.5% healthy。

**Trace 证据**：`trace_query --stats --session glm-B3-回归-2` → F_001=26（~1/case·非 while-loop）· F_002=10（38% while-loop·与 Run 1 比率一致）· 无 ERR 标记。

#### Run 3 — **未跑**

Run 2 的 API 慢时段结论明确（21 timeout = 非代码问题）· Run 1 + link_checkup 已充分支撑回归判定。第三连不增信息（API 方差不受代码控制）。

### 3. RST-L06 三连 — **Run 1 PASS · Run 2 timeout（API）**

| Run | RST-L06 结果 | 证据 |
|-----|:---:|------|
| Run 1 | **PASS** | `tpl=clip tools=['clip','density'] L=2` — clip+density 两步执行·2 层产出·并发改动后不回归 |
| Run 2 | FAIL (timeout) | 21/26 全 timeout（API 慢时段）· RST-L06 随大流超时 |

**RST-L06 判定**：Run 1 PASS = 回归通过。Run 2 timeout 是 API 问题非代码问题。

---

## 二、fail 集合 vs 基线对比

| fail ID | Run 1 | 基线（CB-16 快照） | claude组 判据 {PRM-03/04/07} | glm组 判定 |
|---------|:---:|:---:|:---:|:---:|
| PRM-03 | FAIL | FAIL | ✅ 已知（center ask_user） | ✅ 已知 |
| PRM-04 | PASS | FAIL | — | ✅ 改善（Flash 方差·Run 1 PASS） |
| PRM-05 | FAIL | — | ⚠️ **不在 {PRM-03/04/07}** | ✅ 已知 backlog（boundary derive 方差·CB-12 起持续） |
| PRM-07 | FAIL | FAIL | ✅ 已知（法定功能区 FC 残余） | ✅ 已知 |
| RST-L06 | **PASS** | PASS | — | ✅ 不回归 |
| 成果范式 6/6 | **全 PASS** | — | — | ✅ 出口三段式不回归 |

**新增 fail 检查**：Run 1 的 fail 集 = {PRM-03, PRM-05, PRM-07}。PRM-05 不在 claude组 判据 {PRM-03/04/07}——但 PRM-05 是 **CB-12 起持续的已知 backlog**（boundary derive 方差·zonal 路由偶发）。**非新增·非阻断**。

**建议 claude组 将 PRM-05 补入 fail 集判据**（{PRM-03/04/05/07}）——PRM-05 是 boundary derive 的 Flash 方差·非并发改动引入。

---

## 三、可否进发版候选结论

### 回归判定：**绿（有条件·仅已知 backlog）**

**依据**：
1. **link_checkup 20/20 PASS** — 全链路 10 类问句四件套断言通过
2. **B3 Run 1 = 23/26（88.5%）** — fail 集 ⊆ 已知 backlog {PRM-03/05/07}
3. **RST-L06 PASS** — 多步 clip+density 并发改动后不回归
4. **成果范式 6/6 PASS** — 出口三段式（观点卡/4 要点卡/geo_label）正常
5. **B3 Run 2 = API 慢时段** — 21 timeout 非代码回归（CB-12 铁证方法论：trace F_001 ~1/case + 同代码 Run 1 healthy）
6. **无新增 fail** — 所有 fail 在 {PRM-03/04/05/07} 已知 backlog 范围内

### 可否进发版候选：**可以**

并发改动（出口三段式 + 热点图）后回归判定为**绿**——link_checkup 全通过 + B3 Run 1 在基线范围内 + RST-L06 + 成果范式不回归。B3 Run 2 API 慢时段有充分证据排除代码回归（trace + Run 1 对照）。

**建议**：
- push d5a5625（CB-18 验收修复）
- 进发版候选
- B3 Run 2 的 API 慢时段记录为环境因素·非代码问题
- claude组 补 PRM-05 到 fail 集判据

---

*glm组（ZCode + GLM 5.2）· CB-18 P1 发版回归 · 2026-08-08*  
*回归基于：link_checkup 20/20（session=glm-linkcheck-回归）+ B3 Run 1 23/26（session=glm-B3-回归-1·13.4min）+ B3 Run 2 5/26 API 慢时段（session=glm-B3-回归-2·35.6min）+ trace_query F_001/F_002/F_005 对比 + RST-L06 PASS（clip+density L=2）。*
