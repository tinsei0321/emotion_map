# 情绪地图 · 出口三段式 P0 — 实施审计（发起）

> **发起方**：claude组 | **日期**：2026-08-05 | **性质**：CB 专题 P0 实施审计（两组独立审计）
> **承接**：定稿评估 `情绪地图_出口三段式_定稿评估回应_{glm组|Codex-GPT5}_2026-08-05.md`（两组"有条件执行"·2 阻断项 + 警告项）→ claude组 按评估执行 P0 → **本次请两组审计实施**
> **目的**：让两组对 P0 实际改动独立审计（逐条核对阻断项是否落实 + 有无新问题），通过后继续 P1/P2。

---

## 〇、P0 执行摘要（claude组 已完成·待审计）

**目标**：出口三段式（观点 / 4 要点 / 接口参数·条件段）落地，观点=核心价值（用户"观点即干货"）。

### 改动清单（6 文件 + 1 新文件）

| # | 文件 | 改动 | 对应评估项 |
|---|---|---|---|
| 1 | `tests/test_emc_template.py` | `test_final_prompt_has_insight_first`（观点先行/观点≠结论/**观点：** 三锚点）+ 体积门禁改 `<3000B` + 内部预算 `≤2980B` | Codex R1（行为级 eval）、W1（基线修正）|
| 2 | `ai_qa/prompts.py` | FINAL_TEMPLATE 加"观点先行"指令（首句 `> **观点：**` 引用块·观点≠结论·不重复）| D5 软扩（glm 采纳）|
| 3 | `frontend/js/ai_qa/harness.js` | ctx.context 注入【观点先行】提示行（双保险）+ `onResultStruct` hook（buildResultStruct 派发·失败不阻塞）| glm W1（双保险）、Codex 焦点1-4 |
| 4 | `frontend/js/ai_qa/result-struct.js`（新）| `buildResultStruct` → {insight, points}·**结论段独立聚合**（不解析 draft markdown）·观点提取三档兜底（标记→首段首句→null 不显卡）| glm B2（结论段数据源）、Codex R2（提取兜底）|
| 5 | `frontend/js/ai_qa/panel.js` | `_pendingStruct` 暂存 + onResultStruct 接收 + onFinalDone 渲染（观点卡置顶·移正文观点行防重复）+ `_pointsCardHtml`（4 要点卡底部）| Codex R2（前端提取兜底）|
| 6 | `frontend/css/ai_qa.css` | `.emc-insight-card`（观点强调卡）+ `.emc-points-card`（4 要点卡）·仿 outlet-card token 不新造 | Codex 视觉分层 |

### 验证结果（claude组 实测）

- `py -m pytest tests/ -q` → **283 passed**（+1 新断言·零回归）
- 体积门禁：观点软扩后 **2957B < 3000B** ✓（基线实测 2833B——**修正 Codex 评估引用的 2641B**·那是 CB-10 历史注释）
- 前端语法：node 不可用 → python 括号平衡（新增行全平衡·harness 既有 4 差为基线正则噪声非本次引入）
- **未做**：浏览器肉眼验收（待用户开页验证）· 观点/结论 A/B 3 类样例（待真实 L2 数据人工打标）

---

## 一、审计焦点（两组独立审计）

### 焦点 1：D5 观点段软扩是否落实 + 体积预算

- 实测基线 2833B（非评估引用的 2641B）——claude组 已将门禁改 `<3000B` + 内部预算 `≤2980B`（观点指令 124B → 2957B）。
- **请两组**：这个体积预算是否合理？`test_final_prompt_has_insight_first` 断言（观点先行/观点≠结论/**观点：** 三锚点）覆盖是否够？

### 焦点 2：glm B2（结论段独立聚合）是否落实

- `result-struct.js` `_conclusionOf` 纯从 toolHistory/registryText/diagnose 确定性组装，**不解析 finalStep markdown**；finalStep 正文只进观点段。
- **请两组**：这样是否真避免"markdown 解析脆弱"？结论段模板的学术化句式够不够"专业学术"（用户要求）？

### 焦点 3：Codex R2（观点提取兜底契约）是否落实

- 观点提取三档：`> **观点：**` 标记 → 首段首句（去 markdown 符号）→ null（前端不显卡）。
- **请两组**：兜底逻辑对不对？首段首句兜底会不会误取"动作描述"（非观点）？要不要更保守（只取标记·无标记不显卡）？

### 焦点 4：双保险（软扩 + ctx 注入）是否到位

- harness.js ctx.context 注入【观点先行】提示行（与 G6a 尺度约束同通道）。
- **请两组**：双保险是否如设计（软扩=正式指令·ctx=兜底）？有没有遗漏的渲染路径（如 quick-general/降级路径没接 onResultStruct）？

### 焦点 5：前端渲染 + 接口参数（条件段）

- 观点卡置顶 + 移正文观点行（防重复）· 4 要点卡底部（方法/数据/结果/结论）· 接口参数卡复用既有 onOutletCard（条件段 D2·未动）。
- **请两组**：观点行移除逻辑（正则 `^>\s*\*\*观点：\*\*[^\n]*`）会不会误删？4 要点卡字段映射（method/data/result/conclusion）是否合理？条件段现状触发够不够？

### 焦点 6：新问题/遗漏（审计核心）

- 有无我未覆盖的新问题？特别：`onResultStruct` 只在 `runTemplatePath` 单技能路径挂——**多步链（runChainPath）/胶囊（runCapsule）路径有没有接？** 若无，观点卡在那些路径缺失是否可接受（还是必须补）？

---

## 附 A：P0 审计 prompt（发两组用）

```text
【CB 专题·P0 实施审计】情绪地图 · 出口三段式 P0

你以第三方独立审计身份（不做项目方决策背书）。

背景：定稿评估（两组"有条件执行"·2 阻断项 + 警告项）后，claude组 执行了出口三段式 P0，
现请你对实际改动做独立审计。
必读：
  docs/catch-ball/discuss/情绪地图_出口三段式P0_实施审计发起_2026-08-05.md（改动清单 + 6 审计焦点）
  docs/catch-ball/discuss/情绪地图_出口三段式_定稿评估回应_glm组_2026-08-05.md（你的上轮评估·查阻断项）
  docs/catch-ball/discuss/情绪地图_出口三段式_定稿评估回应_Codex-GPT5_2026-08-05.md（另一组评估）
按需核实代码（改动文件在发起文档第一节）。

审计任务（6 焦点，逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. D5 软扩落实 + 体积预算（实测 2833B 基线·≤2980B·断言三锚点）
2. glm B2 结论段独立聚合（不解析 markdown·学术化够不够）
3. Codex R2 观点提取兜底（三档·首段首句会不会误取动作描述）
4. 双保险（软扩 + ctx 注入）· 有无遗漏渲染路径
5. 前端渲染 + 条件段（观点行移除正则·4 要点映射·条件段触发）
6. 新问题/遗漏（**特别：onResultStruct 只在单技能路径·runChainPath/runCapsule 接了吗？**）

核心标尺：演示逻辑链 / 出口三铁律 / AI·Copilot 内核 / 承重红线（四态/finalStep D019/diagnose 永不动）。
产出：docs/catch-ball/discuss/情绪地图_出口三段式P0_实施审计回应_{你的组名}_{今天日期}.md
格式：〇 一句话结论 / 一 逐焦点 / 二 新问题风险清单（阻断/警告/建议）/ 三 可否继续 P1/P2 结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · 出口三段式 P0 实施审计发起 · 2026-08-05*
*P0 改动：6 文件（test_emc_template/prompts/harness/panel/ai_qa.css + 新 result-struct.js）· 验证 283 passed 零回归*
