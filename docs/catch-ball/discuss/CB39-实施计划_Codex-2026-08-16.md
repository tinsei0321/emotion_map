# CB-39 · 紧急任务数据回收治理沉淀 · 实施计划（Codex·预检文档·零实施）

> 身份：Codex（主开发·唯一 git 写者）。日期：2026-08-16。
> **输入**：① `discuss/CB38-主线回归与数据沉淀_收敛定稿_2026-08-16.md`（含第八节补遗·**数据线**）；② `CB38-EMC全局系统审计_Codex-2026-08-16.md` + `CB38-EMC全局系统审计_评估_Claude-2026-08-16.md`（D1-D9 裁定·**工程线**）。本文档只做计划，零实施代码。
> 结构（v3 双线）：**数据线**（回收 A → 治理 B → 沉淀 C）+ **工程线**（P0 诚实度与安全 → P1 结构治理）+ **收尾归档批 R** + **并行 bug 线 D**。
> 每批完成跑 **`py -m pytest tests/ -q`**（门禁钉死 `py`=3.14·**禁裸 `python`**=3.13 缺 matplotlib 实测 315/3f/6s）对照**当前阶段基线**零回归——v3.1 起始基线实测 **321 passed + 3 skipped**（320 为 CB-38 底稿旧数）；P0-2 后（+守卫数）与 P1-5 后（全绿数）**各重录一次**，每批对照「当前阶段基线」而非全程硬编码。
> **修订 v2（2026-08-16）**：用户指示**时间轴暂不动工、需开专题讨论**——D3 批冻结移出 CB-39（批次 16→15·授权 gate 剩 E15 一处）。
> **修订 v3（2026-08-16）**：并入审计反评价裁定（commit `8c25e61f`）——升级为**双线实施计划**；三处交叉点时序按裁定锁定；P0-3 已由前会话经用户批准完成（见附C）。
> **修订 v3.1（2026-08-16）**：并入两组反评价 + 审计线验收（详见 `CB39-实施计划_回应_Codex-2026-08-16.md`·18 处修订）——P1-2a 前移并入 P0-2、基线分阶段（起始 321）、P1-3 补 api.js 适配（矛盾裁决：zcode 对）、C3/C4 source 规则消歧、D1 v1 范围收窄。

---

## 〇 一句话

**双线结构共 24 个批次**：数据线（A1-A5/B1-B4/C1-C5 + 收尾 R1）+ 工程线（P0-0/P0-1/P0-2 + P1-2…P1-6）+ bug 线（D1/D2/D4·全程并行）。一个用户授权 gate（E15 黄金问题圈选；时间轴 D3 冻结待专题轮）。三个 Codex 决策点已定（E16 主文件落位 / 散件数据族归位 / 总账落位）。P0-3（serve 加固/密钥验证器/轮换 SOP）已完成，P0-0 仅剩用户侧 key 轮换与规范条款两条尾巴。全部锚点实测（数据线见附A·工程线见附C）。

---

## 一 输入·边界·决策点

### 1.1 红线重申（承重·实施全程有效）

1. **manifest 生成须用户授权**（`time-source.js:21` 注释明示「再生成属数据红线（留用户）」）——D3 批设授权 gate，未授权只交付引用清单+设计稿。
2. **diagnose prompt 永不动**（KNOWLEDGE 承重红线）。
3. **蒸馏源纪律**：只认 `DATA/analysis` 最终产物与已成文 md，禁从 `discuss/` 讨论稿取数。
4. **术语纪律**：分类/标签源文档可溯；域枚举三域封顶（checkup/renewal/base），方法论走 concept 类型不设域。
5. **git 已删中间版不捞回；presets 26 件不迁**（图层体系正位）；根 12 底座物理不动（最小迁移原则，manifest 注册即可）。
6. **追踪编号连续**：新增公开函数 `register_track_id` 取号——现 MOD_AIQA.F 最大号 **19**（`ai_qa/router.py`·另有 F_017 注册于 `api/aiqa_routes.py:23`·zcode 定位修正），新号从 **F_020** 起，实施时全仓 grep 复核防撞号；一次性迁移脚本用 `_ops_` 前缀私有函数免号。**存量空洞核处（v3.1）**：MOD_AIQA.F_004 跳号（F_003→F_005·实测全仓零使用处）——登记历史空洞说明（AGENTS 表/注册表注记），**不造空号填充**；4 个使用中未注册 ID（F_007/D_004/F_018/D_004）按**原号补注册**（改号丢 trace 历史）。
7. **密钥纪律（D9·即日）**：CB/审计文档只输出 key 名不输出值（KNOWLEDGE.md 蒸馏一条·P0-0 落盘）；key 轮换由用户在控制台亲自操作（`tools/KEY_ROTATION.md` SOP 已备），Codex 不触碰 key 值。

### 1.2 Codex 决策点（定稿授权 Codex 定·三条）

| # | 决策 | 结论 | 理由 |
|---|---|---|---|
| 决1 | `checkup_12345_2024.csv` 落位（定稿 8.3 二选一） | **`DATA/analysis/12345主观/`**（不新建 DATA/checkup/） | ① 归集根唯一性——阶段0 定调 analysis=紧急任务数据唯一主根，新根削弱回收语义；② E14 白名单已含 DATA/analysis，不加根=白名单零改动；③ 与派生点层同处（`govern_12345_points.py` 的 OUT 本就指向该目录·数据血缘同处） |
| 决2 | 根目录散件归位（任务书「2 xlsx→元数据位」） | **数据族归位·不新建混合目录**：`体检点数据类型来源对应_2026-08-14.xlsx`→`77项量化/`（与 README/管线审计报告同族）；`图数表出图_PPT表格汇总.xlsx`→`汇总/`（与 page1-7 md 同族） | 避免新增「什么都放」的混合目录；族内聚比形式统一更防回潮；代价=两处写入脚本路径同步（A3 列明） |
| 决3 | 《紧急任务数据总账》落位 | **`DATA/analysis/_总账.md`**（下划线前缀） | 治理台账非检索素材；防扫描主依据=**加载器路径隔离**（`_load_notes` 只扫 `docs/urban-renewal-plan/`·DATA/analysis 本不在扫描范围·zcode 措辞修正），下划线为冗余第二道；与数据同处便于随批更新 |

---

## 二 总体结构

```
数据线： A1→A2(含P1-1)→A3→A4→A5 → B1→B2→B3→B4 → C1→C2→C3→C4→C5 → R1(仓库归档)
工程线： P0-0(密钥收尾·当日) → P0-1(诚实度) → P0-2(守卫通电) → P1-2…P1-6(插空)
bug 线： D1(B002/B004·P0) / D2(B012/B013) / D4(E2底图)    [D3 时间轴·冻结·待专题轮]
```

**三处交叉点时序（claude组裁定·锁定）**：
1. **E16 迁移 = 数据池归一前置（同批）**——A2 扩展吸收 D2 工程项（PROCESSED_DIR 单源收敛 + DATA 大小写 + DATA/README 入库规则）；
2. **deliverables/ 归档 = 数据线 C5 完成后**（R1 批）——先沉淀分流再归档残骸，防沉淀路径二次迁移；
3. **SCRIPT 一次性脚本迁移 = E6 工程线镜像**——_tmp 清理（9 件·实测清单）/README 重写/畸形目录清理**可先行**（P1-6·不依赖数据线）。

其余依赖：B2 依赖 A2（对账对象就位）；C 全部依赖 B3（复核 pass 是蒸馏前置门）；P1-5（3.14）**位置固定=数据线完成后·R1 前**（防基线门语义中途断裂·zcode Z5）；D1/D2/D4 完全独立。（D3 时间轴冻结——A2 的「performance 只剩 sim」里程碑保留，属 sim 禁入治理独立价值。）

---

## 三 阶段0 回收归集（A1-A5）

### A1 引用盘点（只读·前置）

- **动作**：对 A2/A3 全部待移文件做全仓活引用 grep（py/js/md/json/bat）；本轮已初扫（见附A），实施批内复核一遍防窗口期变化。
- **文档引用口径**（决策·防过度修改）：**活文档改**（OFFICE.md/HOME.md/session-handoff/_cb-index），**历史档案不改**（discuss/scan/revision-log 历史条目=快照）；xlsx 内嵌「数据源」字符串不改（以 md 为准·总账注明）。
- **产出**：《迁移引用清单》（附于 A2/A3 批次 commit 描述）。

### A2 E16 迁移：performance 9 件真实数据迁出（git mv 保历史）

| 文件 | 去向 | 说明 |
|---|---|---|
| `checkup_12345_2024.csv`（16MB） | `DATA/analysis/12345主观/` | 决1 |
| `77项_街办占比表.csv` / `77项_中类分布.csv` / `77项_质量分级.csv` | `DATA/analysis/77项量化/` | 独有件·数据族归位 |
| `12345_中类分布.csv` | `DATA/analysis/12345主观/` | 同上 |
| `双轨对照表.csv` | `DATA/analysis/汇总/` | 双轨对照=page 级汇总族 |
| `77项_社区占比表` / `77项_社区11类占比矩阵` / `12345_事件类型`（同名三对） | **暂留原位**，B2 裁决后赢家归位、输家进 `_retired` | 实测三对全 DIFF（676/690 行·194/138 行·7/7 行），不宜抢跑 |

- **引用同步（实测三处+一处注释）**：
  1. `core/geo_registry.py`：`_POINT_LAYERS` 增可选路径字段（无字段走 PERFORMANCE_DIR 旧逻辑·向后兼容·sim 层零影响），`checkup_12345_2024` 条目指 `DATA/analysis/12345主观/checkup_12345_2024.csv`；模块 docstring:7 同步。
  2. `SCRIPT/dual_track_density.py:78`：read_csv 路径改。
  3. `SCRIPT/govern_12345_points.py:13`：CHECKUP 路径改。
- **D2 工程项并入（交叉点1·数据池归一同批）**：`core/config.py:13` PROCESSED_DIR 单源收敛（实测仍指不存在的 `DATA/processed`）+ DATA 大小写统一 + `DATA/README.md` 入库规则（真实池=analysis·演示池=performance·红线：真实数据禁入 performance）+ FOLDER_OPTIONS 同步。
- **验收**：① grep 9 文件旧路径零活引用；② EMC 冒烟：`/api/v1/geo` catalog 含 checkup 层 + resolve `checkup_12345_2024` 行数不变（57,265）；③ `dual_track_density` 最小重跑成功；④ **sim 层 catalog 冒烟**（yichang/ermawu 层仍可解析·向后兼容实证·claude组 R-5）；⑤ pytest 阶段基线。
- **里程碑**：迁移后 `DATA/performance/` 仅剩 sim 演进数据 = **时间轴专题轮的分轴物理前提备好**（D3 已冻结·本里程碑不依赖其开工）。

### A3 根目录散件归子目录

- 3 个 12345 层（`12345_安全韧性_西陵伍家点` / `12345_民生基础_西陵伍家点` / `12345_西陵+伍家岗`）→ `12345主观/`；
- 2 xlsx 按决2 归位；
- 引用同步：`OFFICE.md:33-36`（活交接卡）+ `SCRIPT/gen_tidian_mapping_xlsx.py`（输出路径）+ `SCRIPT/update_page46_excel_westlian.py`（Excel 写入路径·注意该脚本把数据源 md 路径写进 xlsx 表头，md 路径不变则无需改）。
- 验收：根目录仅剩 12 底座大件 + `_retired` + `_总账.md`；pytest 基线。

### A4 page7 归档与退役（裁定执行：归档不删）

- 建 `DATA/analysis/page7小结/_retired/`，**9 件逐件归档**（zcode Z7 消歧·v1 xlsx 亦为中间版一并归档）：① `page7_分组汇总_2026-08-14.xlsx`（v1）② `_v2.xlsx` ③ `_v3横条.xlsx` ④ `page7_强度排名预览_2026-08-13.csv` ⑤ `page7_强度排名预览v2_2026-08-13.csv` ⑥ `preview_intensity_rank.py` ⑦ `preview_intensity_rank_v2.py` ⑧ `render_page7_bivariate.py`（E10）⑨ `page7_双高社区_双变量主图.png`（E10）。
- **E10 退役登记**：`render_page7_bivariate.py` + `page7_双高社区_双变量主图.png` → `docs/catch-ball/retired.md` 追加 2026-08-1x 节（旧口径·合并双高 26·CB-34/dsh 两轮报过·不转正）；脚本与 png 移入 `_retired/` + 台账登记（可从 git 历史恢复）。
- **E9 版本标识**：最终版 xlsx 已带「最终」后缀——补 `page7小结/README` 一行版本说明（四版口径链：v1→v2→v3横条→最终）+ 面版（presets page7_*·130 社区面剪裁）vs 点版（analysis 三组点层）两口径用途注明。
- 验收：retired.md 登记完整；`_retired/` 内 9 件清点；pytest 基线。

### A5 《紧急任务数据总账》

- 落位决3：`DATA/analysis/_总账.md`。
- **每文件一行**，字段：`路径 | 数据族 | 来源 commit（git log --diff-filter=A）| 去留（留/归档/退役）| 口径版本（caliber_status）| 沉淀去向（图层/fact/note/none）`。
- 覆盖：analysis 93 + A2 迁入 6（9-3 对账暂留）+ A3 归位 5 + A4 归档 9——**约 113 行**，与 retired.md / presets manifest 交叉一致（总账为准·两处引用它）。
- 验收：行数与 `git ls-files DATA/analysis` 差集为空；三处口径字段抽检一致。

---

## 四 阶段1 治理（B1-B4）

### B1 F1 落位 + presets 图层注册补全

- **manifest 新增 group「城市体检底座」**：根 12 底座注册（物理不动·`snapshot: true` 标注防双头权威）；**社区双口径显式标注**：`体检对象_社区_面`=193 含村(SQMC) vs `checkup_cfg_community`=174 去村(社区) vs `体检对象_西陵+伍家岗`=130。
- **12345 点层补注册**：安全/民生 × 区级/社区/有坐标 + 西陵伍家×2 + 西陵+伍家岗聚合面（A3 归位后的路径）。
- **page7 点版三组注册** + **18村面注册**。
- **usage 标注**（manifest 加字段）：`input`（底座/配置/qty 点层）vs `analysis_output`（西陵伍家合并范围/聚合范围174/18村面/page7 三组面+点版）——**铁律7 机械化**：EMC 空间操作输入白名单只认 `usage=input`。
- usage 白名单**消费点列明**（zcode Z4）：前端 `toolbox` 工具链 + `api/geo_routes` 输入校验——`usage=analysis_output` 层拒绝作空间操作输入。
- 验收：manifest JSON schema 自检（tools 内一次性校验脚本）；前端范围选择冒烟（新按钮出现且可加载）；**既有 EMC 用例回归**（zonal/双轨用例不受 usage 标注影响）；pytest 阶段基线。

### B2 版本对账裁决（performance vs analysis 同名三对 + 旧层双版本 md）

- **四证据法**（zcode 升级·逐表）：① git log 提交时间；② 汇总 md v6 引用链（`page3/5_体检结果` 引哪版）；③ 行数口径推断——预判：矩阵 perf 194 行≈193 含村口径 vs analysis 138 行≈130 西陵伍家口径 → **analysis 版大概率现行**（v6 去村链）；占比表 676 vs 690 行反向，需逐行 diff 定；④ **两版生成时间与生成脚本**（git log + 脚本头注释）——答辩可溯。
- 同法处理定稿点名的旧层双版本 md（安全/民生的`分析整合`vs`分析汇总`）→ superseded 标注。
- **产出**：《对账裁决表》并入 `_总账.md`；输家 git mv → `_retired/` + retired.md 登记。
- **实事求是条款**：三证据冲突时该表挂起待用户裁决，不强行拍板。

### B3 F4 口径注册表起步 + 复核 pass（蒸馏前置门）

- **注册表**落位：`docs/urban-renewal-plan/00-宜昌专项/_口径注册表.md`（下划线=不进 RAG·治理台账）。种子内容：① page2 §3.3 现行口径；② **双口径 test case**：board 全量（安全 8,046/民生 49,192）vs 三层筛选（安全 4,800/民生 42,871）——同名不同值并存须带口径标签；③ 社区四口径对照（193/174/130/1682 reference-only）；④ **作废数字清单**：5,615 / 87.9% / 港务 1,153 等集中登记（检索命中作废声明而非沉默）。
- **复核工具**：`tools/check_caliber.py`（新公开函数→MOD_AIQA.F_020 起取号）：读注册表旧值 → 扫描待蒸馏素材 → 命中即 fail 输出清单（人工裁决改写或加注「历史口径·已废弃」）。
- **门禁**：C2/C3 蒸馏素材必须先过 check_caliber 零命中才可入库。

### B4 F5 红线复核（治理侧）

- **PII 全量化复检**：字段扫描脚本跑全部 12345 派生层+分布表（本轮抽样已干净·批内全量）；字段白名单外告警。
- **办件编号**：图层层保留（zonal 可用）；**RAG 蒸馏层丢弃**（写入 C2 蒸馏规范）。
- **诉求原文禁入 RAG**（dsh）与**两板块=结论**（本项目对标分类非官方）均写入蒸馏规范条款（C2 执行）。
- **sim 禁入治理侧证据**：A2 后 performance 仅剩 sim 文件清单存档进总账（分轴前提的自证）。

---

## 五 阶段2 沉淀注入 RAG（C1-C5）

### C1 domain 三域标签 + 存量回溯（F2 定稿执行）

- `meta` 加 `domain` 字段，枚举 **checkup/renewal/base 封顶**；方法论=concept 类型（无域）。
- **存量 214 note 回溯映射表**（zcode 债·必列·v3.1 扩全）：目录级粗规则**覆盖 7 个顶层目录全部**——`00-宜昌专项/00-0*`→renewal；`00-宜昌专项/03-0*`→checkup；`3prime/`→checkup；`04-案例资料/`→renewal；`_笔记/`→按文件名关键词映射（体检类→checkup·更新类→renewal·其余→base）；根目录散件与未匹配者 `domain=''`（**未定语义**·zcode Z3：不冒充 base·build 单列输出人工裁决清单 + 量级预估）。
- **两代成文 superseded 标注**（claude组 R-1·v3.1）：`3prime/` 四件 08-12 一代交付叙事（`B1_客观落位` / `B2_主观落位` / `B3B4_归纳与落图` / `分析计划与内容_总纲`）头部加 `caliber_status: superseded`——现行口径以 08-13+ `汇总/` 版为准；占比表 md×9 不动（与 analysis 副本字节级同·唯一入库版）。
- 存量 67 fact 按 `CHK-`/`URP-` 前缀标注（15/52）。
- `search(query, k, domain=None)` 可选过滤（fact ×1.2 加权不动）。
- **E11**：素材显式 dim 标注、`_infer_dim` 降兜底；**E13**：`_DIM_KEYWORDS` 街道→街办 术语修（rag_index.py:49-50）。

### C2 体检域 fact 增量（CHK 续编）+ note 双形态

- 新文件 `ai_qa/outlet_kb/urban_checkup_knowledge.py`（不动存量 67 条）；编号续 `CHK-*` 现有序列。
- **素材源（白名单内）**：`汇总/` page1-7 md×7 + 盘点总表、17 项宏观率值 csv、分布表蒸馏、口径注册表结论。
- **蒸馏纪律（B4 条款落地）**：办件编号丢弃；诉求原文禁入；作废数字只进「作废清单」concept；两板块=结论注明；铁律7 结论层素材（page7 三组）标注结论性质+生成口径（阈值 28.57/146.67 + TOP-N 截断）。
- **note 双形态**（dsh）：汇总 md 整篇拷贝 `docs/urban-renewal-plan/00-宜昌专项/`（保口径演进脉络）+ 头部口径块（`caliber_status: current|superseded` + `caliber_date`）+ fact 抽高频数字——两形态都进。
- **权威侧条款**（zcode Z8·v3.1）：拷贝完成后 `docs/urban-renewal-plan/00-宜昌专项/` 为**权威版**，`DATA/analysis/汇总/` 原件降为只读快照——双份并存窗口内任何口径修订只改权威侧。

### C3 图层卡片 26+ 张（F3 桥接·dsh）

- fact 形态，schema：`图层名/几何类型/要素数/字段口径/数据族/口径版本/更新时点/usage`。
- **source 规则消歧**（zcode Z1·v3.1·P0 级）：图层卡片 fact 落 outlet_kb py 文件，`source = ai_qa/outlet_kb/urban_checkup_knowledge.py#id`；presets manifest 与 `_总账.md` 路径写入卡片 **detail / `layer_ref` 专字段**（**不入 source**）——C4 白名单三前缀 + `.geojson/.xlsx/.csv` 后缀禁令原样不变，两批规则不再互斥。
- 覆盖定稿 8.2 全部 26 件 + B1 新注册件。

### C4 E14 source_path 白名单硬实现 + F3 静态检查

- `rag_index.py --build` 增白名单校验：source 仅允许 `DATA/analysis` + `docs/urban-renewal-plan` + `ai_qa/outlet_kb` 三前缀，白名单外**拒收并报错**（E14/F5 硬实现）。
- meta source 后缀 `.geojson/.xlsx/.csv` 即违规（机器防回潮·zcode）；**先扫存量 295 条**（预期零命中：note=.md、fact/case/concept=.py）。

### C5 rebuild + E12 指纹 + E15 黄金问题验收

- `--build` 输出指纹（总条数 + domain 分布）写 `data/rag_index/_fingerprint.json`（目录不入 git·换机 rebuild 复现）。
- **E12 pytest 断言**：扩 `tests/validate_rag_material.py`（或新测试）断言指纹——索引漂移显式红灯；注意双环境一致性（embed_hash 机制已有）。
- **E15 黄金问题**（授权 gate：问题集用户圈选）：抽 5 个体检域问题跑通「检索→LLM 综合→引用来源」，落盘验收记录。候选见附B。

---

## 六 并行双线：bug 线（D）+ 工程线（P）+ 收尾归档（R）

### D1 【P0·前移】B002/B004 finalStep 假结论

- ① 复发用例锁定：把用户实测路径标准化进 B002/B004 buglog 条目（含复现步骤/数据前提/预期）→ 回归测试。
- ② **结论白名单代码裁定（v1 范围收窄·zcode·v3.1）**：v1 只做两层——**图层名白名单**（精确字符串·结论声明的图层必须存在于执行产物清单）+ **observation 行格式校验**（「已生成/产出 N」声明须与 observation 计数一致·复用 CB-22e `_composeDegradedConclusion` 的 `/命中\s*\d+\/\d+/` 先例）；**数字白名单 v2 渐进**（先收集 finalStep 实际数字表述样本——千分位/百分比/约数变体——再定匹配规则·防 v1 误杀毁演示可用性）。违者降级诚实缺口陈述（编排器确定性铁律·非 LLM 自觉）。diagnose prompt 不动。

### D2 B012 + B013 同场修（一次浏览器会话验收）

- **B012 方向 A**：`fillCommunity` 改指针 `e.lngLat` 实时归属（节流 + 空间索引/bbox 预筛防每 hover 遍历 174 面）；复现脚本 `_tmp_audit_b012_community_tip.py` 转正进验收链。
- **B013**：`map.js:1004 showRangeTooltip` 改 `queryRenderedFeatures` 取命中要素 nameField（manifest 已有 nameField 先例）；**先建 buglog 条目**（实测 open 目录现无 B013）。
- 同场顺收 UI-09/PRED-09 复现采集（一次收三条·zcode）。

### D3 时间轴工程——**冻结移出 CB-39（用户 2026-08-16 指示）**

- 用户定调：**时间轴暂不动工，需开专题讨论轮**（议题含 E3 manifest v2 再生成 / 真实-演示分轴 / 离散时间点 UI / E8 五处波及面全套）。
- 本批整体移出 CB-39：原「T1~T3 全仓引用清单先行」「manifest v2 生成」「离散 UI」均不在本轮实施；本节内容转为**专题轮输入**保留于此备查。
- A2 的里程碑「performance 只剩 sim」**保留**——它是 sim 禁入红线（数据治理）的独立价值，同时为将来专题轮分轴备好物理前提。

### D4 E2 office 底图

- 纯色底图 style.json 本地方案评估→落地（**演示场景默认兜底**·参赛断网刚需）；
- GeoQ/SuperMap 验证 spike：WGS84 叠印比对（已知地标 vs 天地图影像），GCJ-02 偏移一票否决；Esri 中国镜像不采；
- 天地图 key Referer 白名单 office 现场同查；瓦片缓存 P3 不做。
- 存量更新（commit `70f98b61`）：esri-imagery HD 底图已注册（CB-31 自适应兼容）——本批剩余 = 纯色兜底 + GeoQ spike + 白名单核查。

### P0-0 密钥与服务收尾（D9·**当日**·大半已完成）

- **已完成（前会话经用户批准·审计 §12）**：serve 路径白名单 + 默认 127.0.0.1 + `--host` 显式开放（`frontend/serve.py:132-136,574-587`·冒烟 `/.env`→403）；`tools/verify_keys.py`（不回显 key 值·当前全绿）；`tools/KEY_ROTATION.md` 四步零断流 SOP。
- **剩余两条**：① **用户今日亲自轮换 AMAP + DeepSeek 两把 key**（控制台操作→按 SOP 更新 `.env`→verify_keys 复验；顺带确认 AMAP `geocode/geo` 服务开通——存量 key 该接口 infocode=30001）；② **KNOWLEDGE.md 蒸馏一条「密钥只输出 key 名不输出值」**（审计规范条款·docs 改动）。

### P0-1 诚实度修复（D1 短期 c·按裁定修正机制）

- **phase 错标**：`SCRIPT/emotion_analysis_v1.py:1129` 全管道产物硬编码 `phase='L4'` → 改为真实计算层级（验收=任何导出文件层级标签与实际一致）。
- **L4 空壳标注**：`_call_api` raise NotImplementedError（:913-919）在 API/CLI/UI 显式标注「未接入」——中期转正见 P2-1。
- **key 断路修复（机制修正版）**：schema 实有 `api_key` 字段且路由有透传（claude组 核正）——修复=**全管道 key 空时显式报错/明示降级**（不再静默跳 L3）+ 前端设置面板透传，**不是补字段**。
- **L0 spider 骨架**移 `SCRAPER/experimental/`（现目录不存在·建之）。
- **机器化验收**（claude组·v3.1）：新增 `tests/test_pipeline_honesty.py`——① 无 key mock 跑 `full_pipeline=True` → 断言产物层标签 ≠ `L4` 且 = 实际执行 phase（如 `L2`）；② 有 key 分支断言 `l3 enabled` 落 trace；③ 无 key 显式报错/降级文案断言。

### P0-2 契约守卫通电（D4·裁定方案 b）

- `pytest.ini` `python_files = test_*.py validate_*.py`——9 个守卫（实测：field_dict_sync/generate_point_layer/knowledge_route/outlet_fields/outlet_trigger_sync/paradigm_map/rag_material/skill_params/track_ids）进收集；CI（已跑 `pytest tests/ -q`·收集面自动扩大）与 pre-commit 同受益。
- `SKILL_DEFS_DEFAULTS` 手抄 dict 改为**解析 `frontend/js/ai_qa/stages.js:45` 真身**（铁律 11 契约单一源落地）。
- **4 ID 原号补注册并入本批**（=P1-2a 前移·claude组 R-2·v3.1）：守卫通电即检测「使用 ID ⊆ 注册表」，4 个已知未注册 ID 必须先注册否则验收「全绿」不可达——`MOD_LLM.F_007`/`MOD_LLM.D_004`（ai_qa/llm.py）、`MOD_AIQA.F_018`（urban_renewal_knowledge.py）、`MOD_SPATIAL.D_004`（core/spatial_analysis.py）按原号补 `register_track_id`；顺带登记 F_004 历史空洞说明。
- **xfail 红线**（claude组+zcode 合并·v3.1）：存量红项修复优先；xfail 兜底必须带 reason + 逐条登记（buglog/deferred 表·复用月度复核机制）+ CI 输出 xfail 计数可见——防静默豁免变体。
- 验收：`pytest --collect-only` 含全部守卫且全绿（或显式登记 xfail）；**本批后重录阶段基线**（321+守卫数）。

### P1-2b 追踪治理工具（D8·4 ID 已前移 P0-2）

- `tools/gen_agents_table.py`：审计对账脚本产品化——从代码自动生成 AGENTS.md 模块表 + CI 零偏差校验（D8·治「9 个⬜中 6 个已埋点+2 幽灵条目」失真）。
- **分区设计先行**（zcode Z6·v3.1）：AGENTS 模块表分**机械区**（脚本生成·CI 强校验零偏差）与**人工区**（状态标注⬜✅🔧·人工维护·CI 不校验）——防全表重写抹人工状态或 CI 恒红两难。
- 「使用 ID ⊆ 注册表」交叉校验由 P0-2 通电后的 `validate_track_ids.py` 常驻承担。

### P1-3 双入口与并发（D6·短期 a + Threading）

- 删根 `serve.py`（实测仍存在·双入口 8080 陷阱）；
- `frontend/serve.py:450` `ReuseTCPServer(socketserver.TCPServer)` → **ThreadingHTTPServer**（保 SO_REUSEADDR·一行级）；
- `/analyze`、`/governance` async 内同步长任务 → **BackgroundTasks + task_id 轮询**（不上队列·单机演示场景裁定）。
- **前端适配必列**（zcode Z2·v3.1·矛盾裁决采 zcode）：`frontend/js/api.js:22 runAnalyze` / `:32 runGovernance` 为同步等待合同（`fetch→r.json()`）——改 task_id 轮询后**必须适配** + 调用方 UI 状态展示；task 状态存储（内存 dict+锁+完成清理）入批次设计；tests/scripts 内其余消费方实施时 grep 复核。
- 验收：① SSE 期间（25-60s）第二标签页 /chat、/geo 响应正常；② **serve↔uvicorn 起停冒烟**（Threading 化后 Ctrl+C 同停链路回归·claude组 R-3）；③ api.js 轮询路径浏览器冒烟。

### P1-4 图层可见性统一（D5）

- 全显默认（2026-08-13 用户要求）+ `focusOnlyResults` 改显式 **visibilityPolicy 单开关**；
- 删 `enforceMutualExclusion` 死代码（`frontend/js/state.js:1073`·首行 return []）。

### P1-5 环境对齐（D7）

- **排期固定**（zcode Z5·v3.1）：数据线全部完成后、R1 前执行——防基线门语义中途断裂；完成后重录基线并**附差异说明**（转绿清单）。
- 单一 venv 对齐 **3.14**（CI 已锁 3.14·本机 3.13 是漂移）+ 补装 matplotlib/pytest-timeout/pypinyin；
- CI 增 browser 冒烟 job（先只跑 `test_toolbox_pipeline.py`）；
- 验收：本机 `pytest tests/ -q` 全绿（「严格模式全测通过」物理可达·**基线数可能变动·变动即记录新基线**）。

### P1-6 仓库卫生先行（D3 可先行部分·不依赖数据线）

- `git rm` 9 个 `_tmp_*`（实测清单：b012/cb33×4/cb35/cb36×2/cb37——结论已被 buglog/审计吸收的先清·cb35 审计按 E6 定转正 tools/）；
- README 重写为项目说明（对话索引移 docs/）+ 畸形目录/怪名文件清理；
- **不动**：SCRIPT 一次性脚本迁移归 R1（交叉点3·防沉淀路径二次迁移）。

### P2 持续项（按审计 P2 清单·滚动）

1. **D1 中期 a'（L4 规则底转正）**：`core/spatial_analysis.py:622 _attach_4x5_attrs` 规则查表转正为 L4 正典（dumb 稳定层）+ 既有 `/aiqa/deep_attribution`（api/aiqa_routes.py:216）lazy LLM 深归因为 Smart 层——**不新建 LLM 通道**；
2. **先建网再拆楼**：e2e-seam 补 10-15 条 Playwright 冒烟（图层增删/七工具 ForAI/layers:changed 图例刷新）→ 再拆 `orchestrate()`（600 行 → 5 个 ≤120 行函数）与 panel/state 巨石；
3. 文档治理（architecture-pattern 乱码段/revision-log 归档压缩/tests/_index 校正）；表达层 token 化（706 处 hex 渐进）；铁律豁免边界（`SCRIPT/oneoff/` 或 pre-commit 三查）；空间工具双入口合并 / run_analysis 去 chdir。
4. **基线数字单一源 = CI 最近绿跑**（claude组·v3.1）：CLAUDE.md / tests/_index 引用 CI 输出而非硬编码——基线即将两次变动，正是硬编码漂移温床（审计 §3.2 防复发）。

### R1 仓库归档收尾（交叉点2·**C5 完成后**）

- 建 `deliverables/city-checkup/`：SCRIPT 一次性脚本 git mv 归档（33 个·保历史）；
- `DATA/analysis` 降级为归档区（沉淀分流已完成：图层→presets·知识→RAG·口径→注册表）；
- 验收：SCRIPT/ 仅管线核心 12 文件 + 根目录无化石入口 + **`_总账.md` / retired.md / presets manifest 三方一致性终对账**（claude组·v3.1·A5 验收只覆盖 R1 前）。

---

## 七 批次-文件-验收矩阵

| 批次 | 动文件（核心） | 验收门 | pytest |
|---|---|---|---|
| A1 | 无（只读） | 引用清单落批注 | - |
| A2 | performance 6 件迁出 + geo_registry + dual_track + govern_12345_points | grep 零活引用·EMC 层冒烟·行数 57,265 不变 | 必须 |
| A3 | 根 5 件归位 + OFFICE.md + gen_tidian | 根目录清单核对 | 必须 |
| A4 | page7 _retired 9 件 + retired.md | 台账登记完整 | 必须 |
| A5 | `_总账.md` 新建 | 行数与 git ls-files 对账 | 必须 |
| B1 | presets manifest + usage 字段 | schema 校验·前端冒烟 | 必须 |
| B2 | 同名三对裁决 + 旧版 md 标注 | 裁决表完整·输家入 _retired | 必须 |
| B3 | `_口径注册表.md` + tools/check_caliber.py | 注册表种子齐·复核 pass 跑通 | 必须 |
| B4 | PII 复检脚本 + 总账补 sim 清单 | 全量字段扫描零告警 | 必须 |
| C1 | rag_index.py + outlet_kb 存量标注 | --stats domain 分布输出·检索冒烟 | 必须 |
| C2 | urban_checkup_knowledge.py + 汇总 md 拷贝 | check_caliber 零命中·fact 抽检 | 必须 |
| C3 | 图层卡片 fact 26+ | 覆盖清单核对 | 必须 |
| C4 | rag_index 白名单+静态检查 | 存量 295 扫描零违规·违例试例拒收 | 必须 |
| C5 | _fingerprint.json + 测试扩展 | E12 断言过·E15 五问验收记录 | 必须 |
| D1 | harness/finalStep + buglog | 复发用例红→绿 | 必须 |
| D2 | tip-popup.js + map.js + buglog B013 | 浏览器验收·UI-09/PRED-09 采集 | 必须 |
| D3 | ~~引用清单 / manifest v2 / 离散 UI~~ **冻结移出**（待专题讨论轮） | — | — |
| D4 | map.js 底图 | 断网演示冒烟·GCJ-02 验证记录 | 必须 |
| P0-0 | 用户 key 轮换 + KNOWLEDGE 密钥条款（serve 加固已完成） | verify_keys 全绿·条款入库 | docs |
| P0-1 | emotion_analysis_v1.py（phase 错标+L4 标注+key 空报错）+ L0 骨架移 experimental | 导出层级标签=真实层级·无 key 明示降级 | 必须 |
| P0-2 | pytest.ini + SKILL_DEFS 真身解析 + **4 ID 原号补注册** | --collect-only 含 9 守卫·全绿（xfail 显式登记）·本批后重录基线 | 必须 |
| P1-1 | （并入 A2·数据池归一同批） | 同 A2 | 同 A2 |
| P1-2b | gen_agents_table.py（机械/人工分区）+ CI 校验 | 机械区零偏差·人工区不被覆盖 | 必须 |
| P1-3 | 删根 serve.py + ThreadingHTTPServer + BackgroundTasks + **api.js 适配** | SSE 期间第二标签页可用·serve 起停冒烟 | 必须 |
| P1-4 | visibilityPolicy 单开关 + 删死代码 | 一处配置·无死代码 | 必须 |
| P1-5 | 3.14 venv + 补装 + CI browser job（**位置固定 R1 前**） | 本机全测全绿（新基线+差异说明） | 必须 |
| P1-6 | _tmp×9 清理 + README 重写 + 畸形目录 | 仓库卫生清单过 | 必须 |
| R1 | deliverables/ 隔离 + SCRIPT 一次性 git mv | C5 后执行·SCRIPT 仅管线核心 | 必须 |

**推送粒度**：每批一 commit，先本地验收后 push（先验后推）。

---

## 八 风险与回滚

| 风险 | 缓解 | 回滚 |
|---|---|---|
| geo_registry 路径改动波及 sim 层 | 可选路径字段向后兼容（无字段走旧逻辑） | git revert 单批 |
| 数据迁移丢文件 | 全部 git mv 保历史 | git 历史恢复 |
| RAG 索引漂移 | E12 指纹断言红灯 | `--rebuild` 重建（目录不入 git） |
| 口径对账三证据冲突 | 该表挂起待用户裁决（实事求是条款） | — |
| manifest 未获授权 | D3 只交清单+设计稿 | — |
| 存量 note 分域误标 | 未分类清单人工裁决后固化 | rebuild 重跑 |
| key 轮换断流 | KEY_ROTATION.md 先验证后切换（旧 key 最后一步前有效） | 旧 key 可即时回填 |
| 守卫通电暴露存量红 | 先 --collect-only 摸底·红项修复或 xfail+登记 | 逐项 revert |
| 3.14 依赖不兼容 | 新 venv 全装 requirements 冒烟后再切 | 旧 venv 保留 |
| phase 修复波及导出消费方 | grep 层级标签消费点逐个核 | git revert 单批 |

---

## 九 DoD（用户验收点）

- [ ] `_总账.md` 全量过目（113 行级）
- [ ] 对账裁决表过目（含挂起项）
- [ ] E15 五条黄金问题验收（问题集用户圈选）
- [ ] B012/B013 浏览器同场验收
- [ ] 时间轴：已冻结移出 CB-39，另开专题讨论轮（本 DoD 不含）
- [ ] 每批 `py -m pytest tests/ -q` 对照**阶段基线**零回归（v3.1 起始 321+3·P0-2/P1-5 后各重录·禁裸 python）
- [ ] P0-0：用户 key 轮换完成 + verify_keys 全绿 + 「只输出 key 名」条款入库
- [ ] P0-1：抽验导出文件层级标签 = 真实计算层级
- [ ] P0-2：pytest --collect-only 含 9 个 validate_* 守卫
- [ ] P1-3：SSE 期间第二标签页可用（浏览器验收）
- [ ] R1：SCRIPT/ 仅管线核心·README=项目说明

---

## 附A 引用盘点初扫结果（2026-08-16 只读实测）

**performance 9 件活引用**：`core/geo_registry.py:43`（+PERFORMANCE_DIR 解析·docstring:7）、`SCRIPT/dual_track_density.py:78`、`SCRIPT/govern_12345_points.py:13`。前端 performance 引用（e2e-seam/test-assets/panel/time-source）均指 sim 文件或常量，**不受迁移影响**。

**DATA/analysis 活引用**：`SCRIPT/gen_page7_density_rank.py:12`、`gen_tidian_mapping_xlsx.py`、`update_page46_excel_westlian.py:79,133`（md 路径字符串）、`_tmp_audit_cb35_page7.py:7`、`_tmp_cb37_verify.py:9-12`、`govern_12345_points.py:15-16`；活文档 OFFICE.md:33-36。历史 discuss/scan 不改。

**同名对账实测**：社区占比表 perf 75,521B/676 行 vs analysis 69,310B/690 行；11 类矩阵 perf 18,816B/194 行 vs analysis 13,713B/138 行；事件类型 99B vs 102B（均 7 行）。三对全 DIFF。

**其他实测**：presets 新增 26 件（git --diff-filter=A --since=08-10 实数）；buglog open = B002/B004/B008/B012（**B013 未建**·D2 建）；MOD_AIQA.F 最大号 19；测试基线 320 passed + 3 skipped。

---

## 附B E15 黄金问题候选（5 条·供用户/评估组圈选）

1. 「2024 年 12345 安全类诉求多少件？用什么口径算的？」（验双口径标签 test case）
2. 「双高社区是哪几个？怎么生成的？」（验铁律7 结论层标注：阈值+TOP-N 截断）
3. 「社区口径有几套？174 和 193 什么关系？」（验 base 域口径卡）
4. 「体检安全韧性发现的重点问题是什么？」（验汇总 md note 蒸馏）
5. 「体检问题怎么衔接到更新项目？」（验 checkup↔renewal 跨域检索）

---

## 附C 工程线锚点实测清单（2026-08-16 只读复核·v3 新增）

**P0-3 已完成证据（审计 §12·前会话经用户批准）**：
- `frontend/serve.py:132-136`（白名单注释）+ `:574-587`（默认 127.0.0.1 + `--host=` 显式放开 + LAN 警告）——冒烟记录：`/.env`→403、`/README.md`→403、`/frontend/index.html`→200、`/DATA/performance/*`→200、`/`→302；
- `tools/verify_keys.py`（DeepSeek 0.6s OK·AMAP place/text 235 命中 + regeo OK·不回显 key 值）；
- `tools/KEY_ROTATION.md`（四步先验证后切换·零断流）；
- 顺带发现：存量 AMAP key 的 `geocode/geo` 接口 infocode=30001（主力 place/text 正常）——用户轮换新 key 时注意勾选该服务。

**4 个未注册 ID 实测确认（P1-2 对象）**：`ai_qa/llm.py` MOD_LLM.F_007（用 1 处）/ MOD_LLM.D_004（用 2 处）、`ai_qa/outlet_kb/urban_renewal_knowledge.py` MOD_AIQA.F_018（用 2 处）、`core/spatial_analysis.py` MOD_SPATIAL.D_004（用 1 处）——四处文件内均无对应 `register_track_id`。

**其余锚点**：
- 双入口：根 `serve.py` 实存（P1-3 删）；`frontend/serve.py:450` `ReuseTCPServer(socketserver.TCPServer)` = 单线程实锤；
- 守卫现状：`pytest.ini` `python_files = test_*.py`（9 个 `tests/validate_*.py` 不被收集）；`.github/workflows/ci.yml` = python 3.14 + 仅 `pytest tests/ -q`（无 browser job）；`.githooks/pre-commit` = 仅 pytest 快速子集；
- SKILL_DEFS 真身：`frontend/js/ai_qa/stages.js:45`（P0-2 解析目标）；
- L4 规则底（P2-1 转正对象）：`core/spatial_analysis.py:622 _attach_4x5_attrs` + `/aiqa/deep_attribution`（`api/aiqa_routes.py:216`·lazy·低置信回退规则底）；
- phase 错标：`SCRIPT/emotion_analysis_v1.py:1129`（P0-1）；L4 空壳 `:913-919`；
- E6 镜像清单：`SCRIPT/_tmp_*` 9 件实测（b012/cb33×4/cb35/cb36×2/cb37）；
- `core/config.py:13` PROCESSED_DIR 仍指不存在的 `DATA/processed`（P1-1·并入 A2）；
- 存量更新：esri-imagery HD 底图已注册（commit `70f98b61`·D4 剩余项相应缩减）。
