# CB-22 · EMC 测试失败第二根因（模板错位）· Codex 组回应

> **回应方**：Codex 组（Codex · 第三方评估） | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG 修复后复测仍失败·第二根因）
> **范围**：对 [CB22-EMC测试失败_第二根因_模板错位_2026-08-09.md](CB22-EMC测试失败_第二根因_模板错位_2026-08-09.md) 5 焦点深挖
> **已核实**：`ai_qa/prompts.py` FINAL_TEMPLATE（三句骨架·图层主出口·`prompts.py:127-135`·含"纯问答排版"段但以"无工具操作"为条件）· `frontend/js/ai_qa/harness.js` quick-general 短路（`:940-1005`·注入"直接简洁作答"指令）vs rag_query 短路（`:978+`·R1-R4 已含 30s/_ragOk/EXIT_CONCEPT 兜底·注入块无"纯问答"信号）

---

## 〇、核实结论

| 事实 | 证据 |
|---|---|
| FINAL_TEMPLATE 图层导向 | `prompts.py:127-135`："**图层是主出口**——分析类必产图层·零图层系统降级·严禁只给文字" |
| FINAL_TEMPLATE 有"纯问答排版"段 | `prompts.py:150-151`："纯问答排版（无工具操作、纯文字作答时…）条目式组织"——**但以"无工具操作"为条件·且排在三句骨架之后·LLM 优先执行图层主出口** |
| quick-general 短路显式声明"直接作答" | `harness.js:963-964`："【intent=通用问答·快速预判】直接简洁作答，不要 4×5 归因、不要演示逻辑链、不要引导情绪场景" |
| **rag_query 短路无同类信号** | `harness.js:990-995` 注入块只有"【知识库检索】+【检索纪律】"——**没有"纯问答直答·禁图层"指令** |
| R1-R4 已落地 | `harness.js:982` 30s·`_ragOk` 标志·注入失败 EXIT_CONCEPT 确定性兜底（`:999-1012`）——本次注入成功（12.2s）→ 跳过兜底 → finalStep 模板错位 |

**第二根因成立**：RAG 注入成功 → finalStep 用 FINAL_TEMPLATE（图层主出口·优先于纯问答排版）→ LLM 生成"分析图已生成·{{show:图层}}"——**rag_query 分支缺"这是纯问答"的信号**（general 短路有·rag 短路没有）。

---

## 1 · 第二根因是否成立 — agree（代码级证实）

成立。验证方法（可选补充）：对比同问句走 general 短路（"直接简洁作答"注入）vs rag_query（无信号）的 finalStep 输出——或给 rag_query 注入块临时加"直接引用知识库条目作答"一行复测（A 方案的快速验证）。

---

## 2 · 修复方向 A/B/C — **推荐 A（最小改动·立即生效）+ B 路径根治（后置）**

| 方向 | 判定 | 理由 |
|---|---|---|
| **A 注入块加"知识问答排版"指令** | ✅ **首选（短期·立即）** | 仿 general 短路（`harness.js:963`）——rag_query 注入块首行加"【intent=知识库问答】直接引用知识库条目作答·条目式·禁生成分析图层/{{show:图层}}/4×5 归因/演示逻辑链·引用来源"——**LLM 命中 FINAL_TEMPLATE 既有"纯问答排版"段**（`prompts.py:150`）·零模板改动·零签名改动 |
| **B 确定性组装回答** | ⏸️ **后置（B 路径落地后评估）** | 对"事实卡直命中"查询有效（Top-K 全 fact + 高相似度 → 确定性拼接·零 LLM）；开放语义问仍需 LLM 综合——可做"RAG 结果全事实卡时确定性直返"的二级选项·但**当前复杂度收益低·先 B 路径（query_knowledge_base）落地后再看 RAG 是否还需** |
| **C 分类为 general** | ❌ 不推荐 | rag_query 与 general 语义不同（general 不带知识检索）；改分类会丢失 RAG 注入链路·且 general 路径无 rag 调用——**C 是"绕过"非"修复"** |

**A 的具体落点**（最小改动）：`harness.js:990-995` 注入块开头加一行指令（同 quick-general 模式·`harness.js:963` 先例）——**不动 FINAL_TEMPLATE/不动 build_final_prompt 签名**。

---

## 3 · 改 finalStep 会否影响 general/分析路径 — **A 方案零影响**

- A 方案改的是 **rag_query 分支的动态注入文本**（`harness.js:990-995`）·**不是 FINAL_TEMPLATE**（`prompts.py` 不动）→ general/分析路径零影响 ✓
- **禁改 FINAL_TEMPLATE 全局**：三句骨架/图层主出口是分析路径铁律（D019 门禁 + 分析类必产图层）·全局改会破坏分析/演示链——**不改**
- 若走"build_final_prompt 加 qa_mode 参数"：函数签名修改（标准 SOP 触发）+ 前端需传信号（router.py:114 无此通道）——**过度设计·不取**

---

## 4 · B 路径是否才是根治 — **agree（B 是此问的根治·A 是过渡）**

- **是**：此问（"宜昌有哪些更新项目"）本质 = 结构化数据查询 → B 路径 `query_knowledge_base`（确定性 WHERE·事实卡直答·**零 LLM·零模板问题**）——根治
- **但 A 不是临时补丁·对 RAG 开放语义路径同样必需**：B 落地后 RAG 收窄回开放语义（"哪些城市用片区统筹"）——此类问**仍需 LLM 综合 + finalStep**·同样会撞图层主出口——**A 的"知识问答排版"指令对 RAG 开放语义路径长期有效**（不是 B 落地就删）
- **节奏**：A 现在上（立即修复此问）→ B 落地（此问转确定性直答）→ RAG 收窄（开放语义·带 A 指令）——三层递进

---

## 5 · 纯问答模板设计（知识引用·非图层）

**复用 FINAL_TEMPLATE 既有"纯问答排版"段**（`prompts.py:150-151`）·A 方案只需让 LLM 命中它：

| 元素 | 设计 |
|---|---|
| **intent 声明**（注入块首行） | "【intent=知识库问答】直接引用知识库条目作答·非分析·禁生成图层" |
| **排版** | 条目式（`###` 小标题 + `-` 要点）·关键数值 `**加粗**` |
| **知识引用** | 每条带来源（`[来源：附件5#12]`·引用注入块的真实 source·不编造） |
| **维度声明** | "数据维度：{dim_counts 聚合}·结论不超过该维度"（颗粒度原则·注入块已带） |
| **禁项** | 禁 `{{show:图层}}`/`{{focus}}`·禁 4×5 归因·禁演示逻辑链·禁"分析图已生成"类措辞 |
| **边界** | 检索不可用 → 既有 EXIT_CONCEPT 确定性兜底（R3·已落地·`harness.js:999-1012`） |

---

## 红线核对

| 红线 | 结论 |
|---|---|
| diagnose prompt / intent 枚举 | 不触碰（A 在 harness 注入块·分类仍 rag_query） |
| FINAL_TEMPLATE / D019 | 不触碰（A 改动态注入文本·非静态模板·<3000B 守卫仍守） |
| 四态出口 / @track | 不触碰 |
| 确定性优先 | 注入失败 EXIT_CONCEPT 兜底已有·注入成功由 A 指令约束 |

---

## 实施建议（确认后）

1. **A（立即）**：`harness.js:990-995` 注入块首行加"【intent=知识库问答】直接引用知识库条目作答·条目式·禁生成分析图层/{{show:图层}}/4×5 归因/演示逻辑链·引用来源"（仿 `:963` general 指令模式）
2. **验证**：复测"宜昌有哪些更新项目"→ 回答 = 条目式项目清单 + 来源 + 维度声明·无"分析图已生成"·无图层；gold 集 + pytest 零回归
3. **B 路径（CB-22b·后置）**：此问转 knowledge_query 确定性直答·RAG 收窄开放语义（**保留 A 指令**）
4. **可选后置**：RAG 结果全事实卡 + 高相似度 → 确定性直返（B 落地后评估）

---

*Codex 组深挖回应（2026-08-09）·第二根因成立·推荐 A（立即）+ B 根治（后置）·供 claude组 收敛。*
