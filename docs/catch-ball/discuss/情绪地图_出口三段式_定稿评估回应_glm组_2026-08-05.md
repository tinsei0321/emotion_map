# 情绪地图 · 出口三段式定稿计划评估回应（glm组）

> **评估方**：glm组（ZCode + GLM 5.2 · 第三方独立评估·不做项目方决策背书）
> **日期**：2026-08-05 | **性质**：CB 专题第三轮（定稿计划评估）
> **回应对象**：`情绪地图_出口三段式_定稿评估发起_2026-08-05.md`（定稿 D1-D5 + 执行范围 P0-P3 + 验证）
> **上轮承接**：glm组 上轮建议"finalStep 软扩"被 D5 采纳——本轮以**特别审慎**态度复核（不因被采纳而 rubber-stamp）
> **核心标尺**：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（Smart 两端 + Dumb 中间 + 编排器确定性）

---

## 〇、一句话结论

**定稿计划方向正确、可执行——D1-D4 agree，D5（观点段软扩）agree 但须"双保险加固"（软扩 + ctx 注入并行·非二选一），且 Codex 上轮指出的"风格张力"（manifesto 禁学术八股 vs 用户要结论学术）是定稿未充分处理的阻断级风险·须在 P0 前先定调。P0 改动量估计偏乐观（结果结构化纯函数是隐性强项·非"纯函数可测"那么轻松）。可执行·但执行前须解 2 个阻断项。**

---

## 一、逐焦点回应

### 焦点 1：D5 观点段 = glm 软扩（claude组 采纳 glm·否 Codex）

**glm组 判定：agree（软扩方向对）+ 修正（不应二选一·应"软扩 + ctx 注入"双保险）**

**审慎复核（glm组 上轮被采纳·本轮独立重判）**：

我上轮建议"finalStep 软扩"·定稿采纳。但 Codex 上轮提出"ctx 注入是安全通道（test_emc_template.py:82 门禁只测 `build_final_prompt('','')` 模板本体·ctx.context 注入不受门禁·harness.js:678 已用此通道注入 execSummary/G6a）"——**这个技术事实成立**。即 Codex 的"ctx 注入"不是"零改动"·而是"改 harness.js 的 ctx.context 拼接·不改 prompt 模板"·同样能让 LLM 看到指令。

**两条路径的真实差异**：

| 维度 | glm 软扩（D5 采纳） | Codex ctx 注入（被否） |
|---|---|---|
| LLM 可见性 | FINAL_TEMPLATE 正文（强·必读） | ctx.context 头部（中·与 execSummary/formatRegistry 混排） |
| 遵守度 | 强（正式指令位） | 中（附加提示·可能被其他 ctx 内容稀释） |
| D019 门禁 | ⚠️ 触碰（+50 字·0.9KB→~1.0KB·<3KB 有余量·但须扩 eval） | ✅ 不触碰（门禁不测 ctx） |
| 可回退性 | 中（改 prompt·须走 SOP） | 高（改 ctx 拼接·纯前端·随时调） |
| 用户反馈"观点=核心价值" | ✅ 强遵守对齐 | ⚠️ 中遵守·用户担心"价值打折" |

**glm组 修正建议：双保险·非二选一**：
- **软扩（D5）作主路径**——FINAL_TEMPLATE 加"观点先行"正式指令（~50 字）·强遵守·对齐"观点=核心价值"。**这是用户拍板的理由·尊重**。
- **ctx 注入作兜底**——harness.js ctx.context 同时加一行"观点先答提问"（复用 G6a 同通道）·双保险·防 LLM 在长 tool_history 中淡忘 prompt 指令。
- **理由**：用户说"观点=干货·核心价值"——对核心价值值得"强指令（prompt）+ 兜底提示（ctx）"双保险·而非省一个。两条路径成本都低（各 ~50 字 / 1 行）·**并行落地**比二选一更稳。

**对 Codex 的回应**：Codex"零改动+ctx 注入"被否的核心理由是"遵守度弱"——这个判断成立（ctx 与 execSummary/formatRegistry 混排·LLM 注意力分散）。但 Codex 揭示的"ctx 是安全通道"技术事实**有价值**——它让"兜底注入"成为低成本加固手段。**不是二选一·是主+兜底**。

**D5 SOP 充分性复核**：
- 定稿称"先扩 eval 门禁（test_emc_template <3KB·现 0.9KB 有余量）再动·一次一处"——**SOP 充分**。
- glm组 实测门禁（test_emc_template.py:82-84）：`build_final_prompt('','').encode() < 3000`·传空参测模板本体·+50 字软扩后 ~1.0KB·**远低于 3KB**·安全。
- "一次一处"——P0 观点段软扩**单独一个 commit**（不动其他）·扩 eval 后动 prompt·可回退。✅ 充分。

### 焦点 2：D2 第三段 = 条件段（意图判断放哪层）

**glm组 判定：agree（条件段对）+ 建议意图判断复用 resolve_outlet_id（非新函数）**

**意图判断放哪层**：
- **diagnose 卡 outlet 字段**——7 值枚举（生成图层/地图定位/指标排序/报告结论/建议清单/预警/执行操作·paradigm.py:462）·已有"是否需出卡"的粗判。
- **resolve_outlet_id（build_outlet_schema.py:27-75）**——已有评分制（domain+scale+TRIGGER_WORDS+outlet hint→score·score>0 才出卡）·**这是现成的条件段判断器**。

**glm组 建议**：**不新建判断函数·复用 resolve_outlet_id**。
- 现状 resolve_outlet_id 评分制已是"意图判断"——问句含 TRIGGER_WORDS（更新/体检/需求/满意度/排序/识别/时序/改造）+ domain/scale 匹配 → score>0 出卡。
- D2 条件段语义 = "score>0 出第三段·score=0 不出"——**完全复用现状**·零新判断逻辑。
- 唯一增强：若要让 diagnose 卡的 outlet 字段更细（如 outlet="报告结论"→ 强制出第三段·即使 TRIGGER_WORDS 未命中）·这是 resolve_outlet_id 的 OUTLET_HINT（:41-46）已有机制——**无需新层**。

**TRIGGER_WORDS 触发灵活性**：
- 现状 8 词（更新/体检/需求/满意度/排序/识别/时序/改造）——**机械但够用**。
- "灵活"不靠扩词表·靠 **outlet hint + domain_lens 双保险**（即使问句不含 TRIGGER_WORDS·诊断卡判 outlet="建议清单"+domain=urban_renewal → score=2 仍出卡）。
- glm组 建议：**不扩 TRIGGER_WORDS**（扩则误触发风险增）·靠 outlet hint 兜底·现状机制够灵活。

### 焦点 3：D3 结论段 = 确定性为主 + LLM 润色可选

**glm组 判定：agree（确定性为主对）+ 润色实现须"确定性护栏内可选 LLM"**

**润色实现方式**：
- **禁用"后处理 LLM 润色"**——额外 LLM 调用撞 D019 极瘦精神（即使技术上不碰 prompt 模板·增 LLM 轮 = 延迟+成本+不可控）。
- **推荐"前端模板 + 静态润色"**——确定性聚合产 4 要点 JSON（方法/数据/结果/结论）·前端模板渲染·"润色"=模板变体（学术/通俗/简洁 3 档·CSS + 句式模板）·零 LLM。

**确定性保护**：
- 结论段 = 后端纯函数聚合（diagnose 方法 + tool_history 流程 + 图层结果 + finalStep 结论正文）——**确定性组装·不调 LLM**。
- finalStep 结论正文（LLM 产）**原样进结论段**·不做二次 LLM 润色——保 LLM 只产一次（finalStep）·结论段是"重组呈现"非"再生成"。
- **润色=前端模板变体**（非 LLM）·确定性不被破坏。

**Codex 上轮的"风格张力"风险（阻断级·glm组 同意并强化）**：
- manifesto:75 "不写学术八股（问题定性→数据证据→结论建议 那套）"+ FINAL_TEMPLATE "简短 3-5 句·通俗"——**与用户"第二段结论更专业学术"冲突**。
- **同一 finalStep 输出无法同时满足"通俗观点" + "学术结论"**——这是定稿 D3 未充分处理的**阻断级风险**。
- **glm组 建议**：P0 前必须先定调——
  - 选项 A：观点通俗（finalStep 现状风格）+ 结论学术（前端模板重组·加学术化句式模板·"研究表明/数据显示/综合研判"）
  - 选项 B：按出口类型切风格（概念问通俗·情绪分析学术）
  - **glm组 倾向 A**——finalStep 保持通俗简短（不改 manifesto 风格）·"学术"由前端结论段模板实现（确定性·不撞风格约束）。

### 焦点 4：D4 排期 = A+C 先行（P0）、B 后置

**glm组 判定：agree（排期对）+ P0 子项拆分基本合理·"结果结构化纯函数"是隐性强项**

**P0 四子项复核**：

| 子项 | 拆分 | glm组 评估 |
|---|---|---|
| 观点段软扩 | FINAL_TEMPLATE ~50 字 | ✅ 合理·独立 commit·SOP 充分 |
| 结果结构化纯函数 | 新增（聚合 diagnose+tool_history+result+finalStep→三段 JSON）| ⚠️ **隐性强项**——非"纯函数可测"那么轻松：①需定义三段 JSON schema（观点/4 要点/接口参数）②需从 4 个数据源（diagnose 卡/tool_history/图层 fc/finalStep 正文）抽字段 ③需处理数据源缺失降级。**改动量被低估**·建议 2-3 人天（非"纯函数"一词暗示的 0.5 天） |
| 前端三段渲染 | panel.js 复用 outlet-card 渲染器 | ✅ 合理·纯增量 |
| 地点联动结构化 | 按尺度选字段·复用 G6a/R10/R11 | ✅ 合理·零新 LLM 阶段 |

**遗漏**：P0 未列"风格定调"（焦点 3 阻断项）——glm组 建议加为 **P0.0 前置任务**（观点通俗/结论学术分层定调·非代码·是设计决策）。

### 焦点 5：执行范围复核（P0/P1/P2/P3）

**glm组 判定：partial（范围对·改动量/风险标注须修正）**

**改动量复核**：

| 优先级 | 定稿估计 | glm组 复核 | 差异 |
|---|---|---|---|
| P0 | （未明确·隐含 2-3 人天）| **3-4 人天** | 结果结构化纯函数被低估（schema 定义+4 源抽取+降级） |
| P1 | 1-2 人天 | 1-2 人天 | ✅ 准（outlet_kb 数据细化·确定性） |
| P2 | 1-2 人天 | 1-2 人天 | ✅ 准（地点联动按 scale 参数化） |
| P3 | 2-3 人天 | 2-3 人天 | ✅ 准（并行编排·红线区） |

**风险标注复核**：
- P0 观点段软扩触 D019——✅ 已标注（SOP 充分）
- P0 结果结构化纯函数——⚠️ **未标注风险**：从 finalStep 正文（LLM 产 markdown）抽"结论段"字段·需解析 markdown 结构（标题/列表/引用块）·**markdown 解析脆弱**（LLM 输出格式可能漂移）。建议：结论段**不解析 finalStep 正文**·而是**独立聚合**（tool_history+图层+diagnose → 描述性结论模板）·finalStep 正文只进"观点段"。
- P3 触编排器红线——✅ 已标注

**依赖顺序**：
- 定稿 P0→P1→P2→P3 —— ✅ 合理（P0 三段式骨架 → P1 接口细化 → P2 地点补齐 → P3 管线）
- glm组 建议补：**P0.0 风格定调**（前置·非代码）→ P0 观点段软扩（独立）|| P0 结果结构化（独立）→ P0 前端渲染（依赖前两者）

### 焦点 6：验证方案复核

**glm组 判定：agree（验证方案对）+ 补 3 项**

**定稿验证方案**（4 项）：观点/结论 A/B·D019 门禁·全量 pytest·R11 冲突·条件段——✅ 覆盖主路径。

**glm组 补强**：
1. **风格张力验证**（阻断项）：3 类样例的 finalStep 输出·人工评估"观点通俗 + 结论学术"是否可分层实现（非同一输出两风格）。
2. **markdown 解析脆弱性验证**：若结果结构化纯函数从 finalStep 正文抽结论段·测 LLM 输出格式漂移（如缺标题/列表乱）时降级是否正常。
3. **双保险验证**（焦点 1 修正）：观点段软扩 + ctx 注入并行后·测 LLM 是否在长 tool_history 下仍守"观点先行"（双保险 vs 单路径对比）。

---

## 二、定稿计划风险清单

### 阻断级（须 P0 前解决）

| # | 风险 | 来源 | glm组 建议 |
|---|---|---|---|
| **B1** | **风格张力未处理** | Codex 上轮指出·定稿 D3 未定调 | P0 前定调：观点通俗（finalStep 现状）+ 结论学术（前端模板）·非同一输出两风格 |
| **B2** | **结果结构化"结论段"数据源歧义** | 定稿 P0"聚合 finalStep 结论"——但 finalStep 是 LLM markdown·解析脆弱 | 结论段**独立聚合**（tool_history+图层+diagnose → 模板）·finalStep 正文只进观点段 |

### 警告级（P0 中处理）

| # | 风险 | glm组 建议 |
|---|---|---|
| **W1** | D5 软扩"单路径"·无兜底 | 软扩（主）+ ctx 注入（兜底）双保险·非二选一 |
| **W2** | P0 结果结构化改动量被低估（"纯函数可测"暗示轻松） | 实际 2-3 人天（schema+4 源抽取+降级）·排期须写实 |
| **W3** | TRIGGER_WORDS 扩词风险（若试图"更灵活"） | 不扩·靠 outlet hint + domain_lens 双保险兜底 |

### 建议级（P1+ 优化）

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | 验证补"风格张力 + markdown 解析 + 双保险" 3 项 | P0 验证 |
| **S2** | P0.0 前置任务：风格定调（设计决策·非代码） | P0 前置 |
| **S3** | 结论段模板变体（学术/通俗/简洁 3 档·CSS+句式·零 LLM） | P1 |

---

## 三、最终可否执行结论

### ✅ 可执行·但执行前须解 2 个阻断项

**定稿计划 D1-D5 方向正确**·对齐核心标尺（演示逻辑链 / 出口三铁律 / AI·Copilot 内核）·承重红线 SOP 充分（D019 先扩 eval 再动·一次一处）。

**执行前阻断项（P0.0 前置）**：
1. **B1 风格定调**——观点通俗 + 结论学术分层方式（前端模板 vs finalStep 双风格）·须先定再写代码。
2. **B2 结论段数据源**——结论段独立聚合（不解析 finalStep markdown）·须先定 schema 再写纯函数。

**解阻后即可执行 P0**·P0 内建议：
- 焦点 1 修正：D5 软扩 + ctx 注入双保险（非二选一）
- 焦点 4 修正：P0 改动量写实（结果结构化 2-3 人天·非"纯函数"暗示的轻松）
- 焦点 6 补强：验证加"风格张力 + markdown 解析 + 双保险"3 项

**glm组 立场**：本轮以特别审慎态度复核了上轮自己被采纳的建议（D5 软扩）——软扩方向对·但应加固 ctx 注入兜底（双保险）·并诚实指出定稿未处理的风格张力阻断项。**不 rubber-stamp·不固执己见·以代码事实和用户价值为据**。

### 承重红线复核

- **D019 极瘦**：D5 软扩触红线·SOP（先扩 eval·test_emc_template <3KB·现 0.9KB 有余量·一次一处）**充分**。✅
- **四态出口契约**：P0 三段式是 EXIT_RESULT 内部组织·非新态。✅
- **diagnose 永不动**：A/C/B 均不涉及 diagnose。✅

---

## 附：现状核实证据（glm组 独立）

| 发现 | 证据 |
|---|---|
| D019 门禁只测模板本体 | test_emc_template.py:82-84 `build_final_prompt('','').encode() < 3000`·传空参 |
| ctx.context 是安全通道（不受 D019 门禁）| harness.js:678/788 execSummary+formatRegistry+G6a 走 ctx.context·非 prompt |
| 风格张力（manifesto 禁学术 vs 用户要学术）| manifesto:75"不写学术八股"+ FINAL_TEMPLATE"简短 3-5 句·通俗" vs 用户"结论更专业学术" |
| resolve_outlet_id 已是条件段判断器 | build_outlet_schema.py:27-75 评分制（score>0 出卡）|
| outlet hint + domain_lens 双保险 | build_outlet_schema.py:41-46 OUTLET_HINT + :53 domain 匹配 |
| finalStep 正文是 LLM markdown（解析脆弱）| prompts.py:127-152 FINAL_TEMPLATE 产 markdown（标题/列表/引用）|

### 声明

本回应由 glm组（ZCode + GLM 5.2）独立产出·2026-08-05·基于定稿计划全文 + Codex 上轮回应 + 代码级核实（D019 门禁/ctx 通道/风格张力/resolve_outlet_id）。第三方独立评估·不做项目方决策背书。本轮对上轮自己被采纳的 D5 建议做特别审慎复核——不 rubber-stamp。

---

*登记：docs/context-map.md。*
