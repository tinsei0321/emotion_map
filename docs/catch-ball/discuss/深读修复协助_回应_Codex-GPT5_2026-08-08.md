# CB-19 深读修复协助回应（Codex · GPT-5）

> **验证方**：Codex（GPT-5）·第三方独立评估
> **日期**：2026-08-08 | **对象**：CB-19 未决修复项详读 + 已修项复验（PRM-03/04/05/07 + P3-4 + Codex 补验 + glm 回归复核）
> **方法**：逐行代码核验（harness.js / tools.js / stages.js / shared.js / hotspot-tool.js / zonal-tool.js / geo_registry.py / geo_routes.py / tool_contracts.py / test-cases.js / verify_prm03_fix.py / test_result_struct.py / test_insight_cards_3path.py）+ 原始 audit json 核实（tests/reports/report-2026-08-08-01/02/03）+ 上轮 P3-4 复验报告引用 + **环境实测：定向 pytest 57 passed（test_geo_routes 14 + test_outlet_schema 41 + validate_outlet_fields 2）·全量（除 sandbox）272 passed + 6 skipped**

---

## 〇、一句话结论

**PRM-03/04 修复正确（通过·1 个建议级缝隙）；PRM-05 真因需修正（词边界非根因·实为 FC 方差 + derive 层依赖 + 测量端判据）→ 修复方案改道；PRM-07 残余属实·推荐方案 B 轻量版（前端 handler 校验 + 后端兜底）且建议发版候选前实施；P3-4 有条件通过（上轮报告）；Codex 补验 W-2 全链闭合、S-3 充分、三路径合理；glm 回归数据修正与原始 audit 完全一致·PRM-05 补入判据合理。综合：083b78d + 9680dcc 可 push，PRM-05/07 修复建议在 push 后、进发版候选前完成。**

---

## 一、PRM-03/04 复验（083b78d·已修）— **通过（1 个建议级缝隙）**

### 现状确认

**调用顺序实证（G5 重写生效的前提成立）**：`harness.js:1082` `deriveMissingParams(...)` 先于 `:1127` `runAllToolCalls` 入口执行——G5（`:1529-1535`）对 `_allToolCalls` 的重写必然在批量执行前生效。✅

**重写后行为链完整推演**（问句「大南门·二马路滨江片区周边 300 米范围内的情绪点分布」）：

| 步骤 | 代码 | 结果 |
|------|------|------|
| G5 命中 | `harness.js:1529-1530` `/(周边\|附近\|半径\|缓冲\|米内\|公里内)/` + `/情绪\|点\|分布/` + `tool!=='buffer'` + `!/（叠\|裁\|筛选）/` | template='buffer'·method=['buffer()'] |
| 多工具重写 | `harness.js:1534-1535` `_allToolCalls.length>1 → [{name:'buffer',params:{}}]` | 单元素数组 |
| 批量入口拦截 | `harness.js:1127` `_allToolCalls.length>1` 条件不满足 | 不进 runAllToolCalls·merge 错路由堵死 ✅ |
| radius derive | `harness.js:1598-1601` 门控 `diagnose.template==='buffer'` → `(?:周边\|附近\|半径\|缓冲\|以内)\s*(\d+)\s*(m\|米\|km\|公里)` | radius_m=300 ✅（重写后 template 已是 buffer·旧 tool 局部变量不影响） |
| center 缺失 | `harness.js:1477-1645` 通读确认 **无 center derive**（仅 boundary/cell_size/radius/polarity） | center 保持空 |
| 必填槽拦截 | `harness.js:533-537` runTemplatePath `validateParams` → `stages.js:51` buffer `required_slots:['center']` → `_missingSlotAsk`（`:494`「还缺「center」」） | ask_user ✅ |
| 执行 | 用户补 center 或点地图 → resume 续作执行 buffer | 合法 |

**verify_prm03_fix.py 守卫评估**：3 次问 + 断言「tpl 含 buffer 或 ask」「绝不 merge」——与上面推演的行为空间完全对齐（buffer 执行成功 或 合法 ask_user 二选一）✅。判据合理。

### 根因/缝隙

1. **chain 路径残余（建议级·低概率）**：`:1135` `if (diagnose.chain) return await runChainPath(...)` 在 G5 之后仍优先。若 FC 产 chain（顺序词触发·stages.js `n` trigger）+ 问句含周边 Nm 情绪 → G5 只改 `template/method/_allToolCalls`，**不删 `diagnose.chain`** → runChainPath 按原链执行（可能含非 buffer 工具）。当前链 trigger 均需「先/再/然后」顺序词（周边单步问句不触发），实际风险低；但作为修复一致性，建议 G5 命中时若 `diagnose.chain` 存在且含非 buffer 工具 → 重写链为 buffer 单步（约 2 行）。
2. **去 `/(合并)/` 排除的副作用（建议级）**：原条件排除「合并」是为防合并类问句被误改 buffer；去掉后，**真含「合并」+「周边」的复合问句**（如「把 A 与 B 合并后看周边 300 米情绪」）会被 G5 强制 buffer 单 call → 合并意图丢失。当前 B3/flywheel 无此类用例，但属行为回归面；建议 G5 条件加 `!/(合并|叠置)/` 保护复合问句（与「去合并排除」不冲突——PRM-03/04 场景问句本身无「合并」词，加回排除只影响真含合并词的问句）。
3. **center 缺失 ask_user 正确性**：✅ 正确。Smart/Dumb 内核（失败时交流不放弃）+ P2 原则——center 是空间锚点（无默认·尺度表只推 radius），代码不可知时问用户是最优解，非硬猜（符合 CB-14「EMC 不硬猜」）。
4. **无其他绕过路径**：recover（`:1049` 区域）先于 derive（`:1082`），G5 仍会对 recover 结果生效；autoExpand（族 A）基于 template=buffer 扩展，不会长出 merge。✅

### 修复建议（非阻断·push 后）

- G5 加 chain 重写保护（缝隙 1）——低优先。
- G5 条件加 `!/(合并|叠置)/` 防复合问句截断（缝隙 2）——低优先。

### 风险

低。核心路径（周边单步 → buffer/ask_user）已闭环，两个缝隙均为低频复合场景，不阻断 push。

---

## 二、PRM-05 深挖（未修）— **词边界非根因·真因 = FC 方差 + derive 层依赖 + 测量端判据**

### 现状确认（完整链路推演）

**report-01 vs report-02 的同一用例差异（原始 audit json 逐例核实）**：

| Run | template | method | params | tools | 结果 |
|-----|----------|--------|--------|-------|------|
| Run 1（16:06） | zonal | `['zonal_stats()']` | `boundary: GeoJSON{1}#西陵区` | `['zonal_stats','zonal']` | PASS（FC 直接给了 boundary） |
| Run 2（16:25） | zonal | `null` | `{}` | `[]` | FAIL（boundary[ERR]·0 层） |

**Run 2 运行时推演**：
1. FC 输出 `method=null`、`params={}`（**Flash 方差：不给 method 也不给 boundary**）。
2. `harness.js:1071` `deriveDiagnoseMethod('zonal', {})`（`:879-885`）→ `['zonal_stats()']`（template=zonal 时派生单工具）。
3. `harness.js:1082` deriveMissingParams → 聚合分支（`:1520-1537`）：`/(聚合|归因|统计).{0,4}(情绪|极性)|按面聚合/` 对「西陵区范围内按面聚合情绪统计及 4×5 归因」**命中** → `_zonalD = deriveAvailable(q, layers)`。
4. **deriveAvailable 返回 null → 聚合分支不强制**（`:1536-1537` 注释：boundary 不能 derive → 不强制改 template·防强制 zonal + 无 boundary → gap）→ template 保持 zonal、boundary 空。
5. `harness.js:1131` template 非空 → 单工具路径 runTemplatePath → `stages.js:55` zonal `required_slots:['boundary']` → validateParams 缺 boundary → **ask_user「还缺 boundary」**。
6. 测量端（test-cases.js:314 `expectBoundary:'西陵'`）→ params.boundary 空 → 判 `boundary[ERR]`（**未识别"合法 ask_user"·与 PRM-04 判据不一致**）。

### 根因（修正发起文件的归因）

**发起文件把根因归为「词边界 `_validAfter` 偶发失败」——静态推演不成立**：

`tools.js:610-618`，`_validAfter('西陵区', q='西陵区范围内按面聚合情绪统计及 4×5 归因')`：
- `_idx=0`·`_after=q.slice(3,5)='范围'`·`_first='范'`
- `_sepRe.test('范')=false`（范∉`[空格，。；、的区内范围边界]`）
- `_blockRe.test('范围')=false`（范围∉路/山/街/公园/广场/大道/车站/码头/桥）
- `!false=true` → **匹配** ✅

stripped 兜底（第二循环）同样匹配（`_validAfter('西陵', q)` → `_after='区范'`·`_first='区'`·`_sepRe.test('区')=true` → 匹配）。**词边界正则对该问句不失败。**

**真因 = 两层方差叠加**：
1. **FC 输出方差**（源头）：Run 2 的 FC 不给 method/boundary（Run 1 给了 boundary 所以 PASS）。
2. **deriveAvailable 层依赖**（兜底失败）：`tools.js:590` 只扫「boundary首选」层——`_polyRole`（`:533-540`）要求层名命中 `_BOUNDARY_NAME_RE=/（行政区|行政区划|片区|范围|边界|核心|主城|中心城区|建成区|社区|街办|乡镇）/`（`:531`）。B3 会话注入「行政区.geojson」应命中，但**层加载时序/角色判定/要素截断任一失败 → deriveAvailable=null**。
3. **测量端判据**（表象固化）：zonal boundary 是必填槽，运行时已走 ask_user（合法），但测量端把「预期 boundary 未填」一律判 `[ERR]`——**未区分"合法 ask_user（诚实追问）"与"真 ERR"**，而 PRM-04 的同类判据（`report-02` 判 PASS：合法 ask_user）已存在。判据不一致放大了 PRM-05 的表象。

### 修复方案（三层）

| 层 | 方案 | 改动 | 优先级 |
|----|------|------|:---:|
| a 测量端 | test-cases.js PRM-05 判据对齐 PRM-04：boundary 缺失 → 合法 ask_user 判 PASS（obs 标「合法 ask_user（boundary 缺·诚实追问）」） | 1 行 | P1（快·消误报） |
| b 执行侧可观测 | deriveAvailable 失败 → `_recordSkip('derive_boundary_fail')` + observation 带「区名未在已加载面层找到」（现仅 console.info `harness.js:1590-1595`·B3 无 console）→ 下次 B3 带 session 可溯源 | ~5 行 | P2 |
| c 契约强化 | tool_contracts zonal 的 `when/examples` 强化「boundary 必带（区名 → 已加载要素 GeoJSON）」·减少 FC 空参输出（根治方差源·对齐 CB-09 契约 `when`=FC 工具说明的承重教训） | 契约文案 | P1 |

**推荐**：a + c 主修（消表象 + 减源头），b 辅助（可观测）。均不碰 diagnose/finalStep/红线。

### 风险

低。a 是判据修正（对齐既有 PRM-04 模式），c 是契约文案（非 prompt 重写·不撞「diagnose 永不动」），b 纯增强。

---

## 三、PRM-07 深挖（未修·方案收敛）— **残余属实·推荐方案 B 轻量版·建议进发版候选前实施**

### 现状确认（完整链路）

**LLM 直传 GeoJSON 如何绕过白名单（逐环核实）**：
1. LLM FC 输出 `zonal_stats({boundary: {type:'FeatureCollection', features:[小溪塔…]}})`（工具契约允许 dict boundary）。
2. `tools.js` zonal handler → `ref(params.boundary)` 直传（无来源校验）。
3. `harness.js:1573-1576` `_boundarySuspect`：`!Array.isArray(f) || f.length !== 1` —— **单要素小溪塔 GeoJSON 的 features.length===1 → 不判定可疑** → 不触发重新 derive。
4. 前端 `deriveAvailable` 白名单（`tools.js:623-640` FIXED_ADMIN_DISTRICTS）**只作用于 derive 取值路径**——boundary 已有值时完全不参与。
5. 后端 `core/geo_registry.py:174-185` dict 分支：`from_features` 直通执行，**无词表校验**（str 分支 `:158-167` 已堵：preset 查 manifest → 法定功能区不在 manifest → 诚实报错）。
6. 结果：zonal 成功执行（report-01/02 tools=['zonal_stats','zonal']·newLayers=1）→ **违反 CB-14「EMC 不硬猜不可信范围」**（EMC 硬识别了法定功能区·应 request_upload）。

### 方案 A/B/C 评估

| 方案 | 机制 | CB-14 符合度 | 「用户上传层不受限」 | 改动面 | 评估 |
|------|------|:---:|:---:|------|------|
| A 后端 dict 词表校验 | resolve_boundary dict 分支校验 `properties.name` 命中法定功能区词表（小溪塔/龙泉/东部产业新区/生物产业园）→ 拒绝 | 部分 | **有风险**：dict 无来源标记·无法区分用户上传 vs LLM 编造·用户真上传小溪塔标准资料会被误拦 | geo_registry.py ~8 行 | 若作为**兜底**（只拦 name 明确命中词表）可接受·单靠它不充分（name 缺失/异名绕过） |
| B 前端来源标记 | dict 带 `_source:'upload'\|'llm'\|'tool'`·用户上传/绘制标 upload 放行·LLM 直传标 llm 校验 | 高（语义最准） | 不破（upload 放行） | boundary-resolve.js + 各 geo handler + 后端 ~15 行 | **最符合 CB-14**·改动面略大 |
| B' 前端 handler 校验（轻量版·推荐） | EMC 工具执行入口校验 FC 直传 boundary 的 name：命中法定功能区词表且非用户上传标记 → 拒绝 + request_upload 文案；后端 dict 分支同步兜底（name 明确命中词表 → 拒绝） | 高 | 不破（只拦 FC 直传·不拦 upload/draw/import） | tools.js zonal/compare/rank handler 各 ~5 行 + geo_registry.py ~8 行 | **改动面最小 + 语义准确**·推荐 |
| C 不动 | 维持现状 | 低（持续违反） | 无改动 | 0 | 不建议长期：B3 每轮 fail（PRM-07 持续 backlog）+ 演示一旦触发即违红线 |

**关键设计点（防误伤）**：法定功能区词表 = 固定枚举 `['小溪塔','龙泉','东部产业新区','生物产业园']`（KNOWLEDGE §2 已列）·**只拦 name 明确命中该枚举的 dict**——用户主动上传的相同名称标准资料会被拦（诚实提示「此名称属法定功能区·请上传标准边界资料」·用户可改名/换来源），属可接受的诚实边界；若要完全零误伤则上方案 B 全量来源标记。

### 修复建议

1. **前端**：tools.js zonal/compare/rank 的 boundary/boundaries 参数处理处（`ref()` 前）加校验函数 `_rejectNonAdminBoundary(b)`：`typeof b==='object' && b.features[0]?.properties?.name 命中法定功能区词表 → 返回拒绝 observation`（诚实 request_upload 文案·对齐 test-cases.js:316 期望）。
2. **后端兜底**：geo_registry.py dict 分支加同词表校验（name 经 `find_boundary_name_column` 推断后命中 → 抛 ValueError「法定功能区·请上传标准边界资料」）。
3. **测试**：test-cases PRM-07 已期望 `expectRequestUpload`——实施后 B3 应转 PASS；补单测（后端 dict 拒绝 + 前端校验函数）。

### 风险

中（承重相关·CB-14）。**建议先扩 eval/browser 测试再动**（红线区 SOP·一次一处）：实施 PRM-07 修复后跑 pytest 全量 + 浏览器复现「小溪塔」3 次问均 request_upload 再进发版候选。改动不触 diagnose/finalStep/四态出口。

---

## 四、P3-4 复验（9680dcc·已实施）— **有条件通过（引用上轮独立报告）**

详见 [P3-4地点联动_复验回应_Codex-GPT5_2026-08-08.md](P3-4地点联动_复验回应_Codex-GPT5_2026-08-08.md)。核心结论复用：

- prop_cols（`geo_routes.py:378-380/:518-520`）纯 additive（`_props_df` 按存在列裁剪 `:156-158`）✅
- buildZonalFc 透传（`shared.js:65-69`）null 安全 ✅
- limitations 四态动态化（`build_outlet_schema.py:388-397`）✅
- micro POI 升级（`:329-340`）方向正确·**W-A 文案矛盾**（source 兜底读升级后值·建议级）+ **W-B poi_names 首个无排序依据**（`spatial_analysis.py:685-694`·建议级）
- `_fmtRow`（`tools.js:256`）place_name 优先 ✅（flywheel/test-cases 无文本硬断言）
- 实测：test_geo_routes 14 + test_outlet_schema 41 + validate_outlet_fields 2 = **57 passed 独立复跑通过**

---

## 五、Codex 补验（W-2 / S-3 / 三路径）— **W-2 全链闭合·S-3 充分·三路径合理**

### W-2 threshold 转发全链 — **闭合 ✅**

逐环核实（契约 vs 运行时）：

| 环 | 代码 | 状态 |
|----|------|:---:|
| 契约承诺 | `tool_contracts.py:263-275` threshold(1.96)/soft_threshold(1.0)·`panel_source:'EMC-only（无 Toolbox dialog·AI 执行）'` | 参数化承诺 |
| EMC 工具 | `tools.js:1293-1294` hotspot handler `threshold: params.threshold, soft_threshold: params.soft_threshold` | 转发 ✅ |
| Toolbox 执行核 | `hotspot-tool.js:15-16` `_execute` `if (params.threshold != null) body.threshold=...`（+soft） | 转发 ✅ |
| 后端端点 | `geo_routes.py:660-662` 请求模型默认 1.96/1.0 + `:673-675` 透传 | 接收 ✅ |

hotspot-tool.js 是「纯内嵌无 UI·仅 generateHotspotForAI 程序化入口」（头部注释）——`_execute` 是唯一执行路径，**无第二入口可遗漏**。W-2 缺口已闭合（CB-18b glm 结论复核一致）。

### S-3 result-struct 纯函数直测 — **充分 ✅**

`tests/browser/test_result_struct.py` 覆盖：P0-1 观点提取（有标记→值）·P0-4 无观点不显卡（null）·P0-2 4 要点四段（method/data/result/conclusion）·P2-1 结论带地点（place_name+极性+样本+归因）·P1-5/P2 scale 三档（宏观/中观/微观）·边界（无地名→「关注区域」降级）。纯函数主分支全覆盖，且为**真实 JS 逻辑直测**（e2e-seam `buildResultStruct`·非 Python 复刻）。接线由 harness.js 三处 `_dispatchResultStruct`（:705/:819/:1962）保证 + 三路径浏览器抽验。

**边界评估**：缺「多行 rows 取 Top-1」（`_topRow` 选含极性首行）与「toolHistory 空 + rows 空」分支——但后者已有 `test_r7_truncation` 同类覆盖，前者属低风险。建议级补 1 例即可，非必需。

### 三路径 DOM 断言 — **合理·无需前置 ✅**

`tests/browser/test_insight_cards_3path.py`：单技能/多步链/multi 三路径 + 断言 `.emc-insight-card`/`.emc-points-card` 存在 + 注入数据后 PASS。判据设计合理（观点卡"保守不显卡"时 PARTIAL 不判 fail·符合 W3 保守化）。**前置单元级 DOM 断言评估**：需模拟整个 harness 渲染链（onFinalDone → 卡片插入）·脱离真实 LLM 的模拟价值有限——当前「纯函数直测（S-3）+ 回归期三路径抽验」组合已覆盖确定性 + 接线两层，**不前置**。可选增强：e2e-seam 增加确定性渲染直测（注入假 draft → buildResultStruct → 断言 DOM 插入·不依赖 LLM）——列入后置。

---

## 六、glm 回归数据复核 — **claude 修正准确·PRM-05 补入判据合理**

**原始 audit json 逐例核实**（上轮完成·结论复用）：

| Run | audit 时间/commit | 实际结果 | fail 集（json 核实） | claude 修正 vs 原始 |
|-----|------|------|------|------|
| Run 1 | 16:06 / 9fcd811 | **23/26（88%）** | **{PRM-03, PRM-04, PRM-07}**（PRM-05 PASS·boundary[OK]） | ✅ 一致 |
| Run 2 | 16:25 / 9ae6e12 | **23/26（88%）** | **{PRM-03, PRM-05, PRM-07}**（PRM-04 PASS·合法 ask_user） | ✅ 一致（glm 原报告漏报此 Run·且 Run 1 fail 集误写含 PRM-05） |
| Run 3 | 17:04 / 9ae6e12 | 5/26（21 timeout） | 21 例全 `tpl=None obs="回答超时（90s）"`·仅短耗时用例（CPD-L01/02/03 + UI-L01/02）PASS | ✅ 一致（glm 原报告错标「Run 2」） |

**判定**：
1. **claude 修正准确**——两个 88% Run + Run 3 API 慢与 audit json/md 完全吻合；glm 原报告存在 Run 序号错标 + Run 1 fail 集误写 + 漏报一个 88% Run（claude 注记已全部纠正）。
2. **PRM-05 补入 fail 集判据 {PRM-03/04/05/07} 合理**——PRM-05（boundary derive 方差）Run 1 PASS / Run 2 FAIL = Flash 偶发·CB-12 起持续·非并发改动引入。✅
3. **补充事实（文档均未明说）**：report-01 commit=9fcd811、report-02/03=9ae6e12——**B3 三连全部跑在 P3-4（9680dcc·17:12）与 PRM-03/04 修复（083b78d）之前**。回归绿未覆盖这两个 commit 本身；其验证依赖 pytest 301 + verify_prm03_fix（083b78d）+ e2e-seam（9680dcc）——**建议 push 后补一轮 B3 快照**（判据 {PRM-03/04/05/07}·不新增 fail 即过）。

---

## 七、综合修复建议清单（按优先级）

### P0 阻断（无）

### P1 建议（push 后·进发版候选前）

| # | 项 | 对应 | 改动 | 验证 |
|---|------|------|------|------|
| 1 | **PRM-07 方案 B 轻量版**：前端 zonal/compare/rank handler 校验 FC 直传 boundary（name 命中法定功能区词表 → 拒绝 request_upload）+ 后端 geo_registry.py dict 兜底 | 三 | tools.js 3 handler ~15 行 + geo_registry.py ~8 行 | pytest 全量 + 浏览器「小溪塔」3 次问均 request_upload（先扩测试再动·红线区 SOP） |
| 2 | **PRM-05 测量端判据对齐**：test-cases.js PRM-05 缺 boundary → 合法 ask_user 判 PASS（对齐 PRM-04 模式） | 二 | test-cases.js 1 行 | B3 复跑 PRM-05 不再误报 |
| 3 | **PRM-05 契约强化**：tool_contracts zonal `when/examples` 强调「boundary 必带（区名→已加载要素 GeoJSON）」·减少 FC 空参 | 二 | 契约文案（非 prompt 重写） | FC 参数 eval |

### P2 建议（发版后）

| # | 项 | 对应 | 改动 |
|---|------|------|------|
| 4 | **P3-4 W-A**：micro 升级 source「面域代表名兜底」缓存原值（`build_outlet_schema.py:334-339` 约 1 行） | 四 | 文案修复 |
| 5 | **P3-4 W-B**：poi_names「首个」选择改质心最近/poi_count 最高·或 source 措辞降级（`spatial_analysis.py:685-694`） | 四 | 排序逻辑/文案 |
| 6 | **PRM-05 derive 可观测**：deriveAvailable 失败进 execSkips/observation（现仅 console.info·`harness.js:1590-1595`） | 二 | ~5 行 |
| 7 | **G5 chain 缝隙**：G5 命中且 chain 含非 buffer 工具 → 重写链为 buffer 单步 | 一 | ~2 行 |
| 8 | **G5 复合问句保护**：条件加 `!/(合并\|叠置)/` | 一 | 1 行 |
| 9 | **P3-4 W-C**：补「无 POI 缺键」路径单测 | 四 | 测试 |

### P3 后置

| # | 项 | 对应 |
|---|------|------|
| 10 | S-3 补 Top-1 多行/空 rows 分支 + e2e-seam 确定性 DOM 渲染直测（不依赖 LLM） | 五 |
| 11 | B3 回归基线补跑（push 后·判据 {PRM-03/04/05/07}）作为 9680dcc/083b78d 之后的实证 | 六 |

### 承重红线确认

全部建议**未触碰**：四态出口（success/gap/partial/answered 未简化）·finalStep D019（FINAL_TEMPLATE 未动）·diagnose prompt（契约文案 ≠ diagnose prompt·非重写）·追踪编号（无新 ID）·「EMC 不硬猜」（PRM-07 修复正是强化该准则·P3-4 全部锚定聚合产物字段）。

---

*Codex（GPT-5）· CB-19 深读修复协助 · 2026-08-08*
*验证基于：harness.js（:494/:533-537/:879-885/:1082/:1127/:1477-1645/:1529-1535/:1590-1601/:1573-1576）· tools.js（:531-540/:590-640/:1203-1220/:1293-1294）· stages.js（:51/:55）· shared.js（:82-107/:272-297）· hotspot-tool.js（:10-16）· zonal-tool.js（:7-8）· geo_registry.py（:151-186）· geo_routes.py（:660-675）· tool_contracts.py（:263-275）· test-cases.js（:314-316）· verify_prm03_fix.py · test_result_struct.py · test_insight_cards_3path.py · tests/reports/report-2026-08-08-01/02/03（原始审计）· 实测 pytest 定向 57 passed / 全量（除 sandbox）272 passed + 6 skipped*
