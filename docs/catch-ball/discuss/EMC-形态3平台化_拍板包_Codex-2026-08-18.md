# EMC × dsh · 形态3（平台化）· 拍板包（Codex 起草·2026-08-18）

> 起草方：Codex（主开发）。依据：zcode 回收汇总件（R8·693c8b61）+ claude 两份 + Codex v2（已逐条核验收账）+ 台账/纪要全链 + 用户 08-17 深夜指示（含「dsh 是有落盘的」更正）。
> 性质：**收敛预备·拍板包**——零实施·不出完整 plan（用户拍板 E4 后再出）。落盘即本文件·未 git 写。

---

## 〇 一句话

**建议拍板：E4=形态3 成立**（三前置已工程化收口·七议题两组零对抗分歧·交叉质证四点全吸收为条款）；并轨排期一张表可直接执行；R1 四决策点（D1-D4）全部自动成立或并入本次拍板；dsh 五问专项**不阻塞**（附落盘疑点一处·需用户一句话确认）；仍缺证据三条列入待决。

---

## 一 对照表核对结果（任务①）

1. **v2 立场无误读**：汇总 §2 七议题对照表与 Codex v2 逐条一致（E4 三前置全收+工程化 / 渲染三步+交互不进 v1 契约 / v1 七工具+render 第二波 / B-C 照旧+G8 拆批 / 沉没五项去向）——核对通过，无遗漏。
2. **v2 归属核验（已解·用户 08-18 贴文确认）**：磁盘上的「Codex v2」（165 行·22:23 覆盖 Codex v1 旧稿）**系 dsh 会话执笔**（用户贴文与磁盘文件逐字一致·166 行同文）——即 dsh 答了 A 卷（claude/Codex 联合评审·Codex 槽位）而未答 B 卷（dsh 五问专项）。内容以 Codex 主开发视角撰写且与 Codex v1 立场**逐条兼容**——处置：**内容认账·双背书**（dsh 执笔 + Codex 认可·v2 即现行 Codex 立场）；署名归位注记随收敛归档（文档抬头补「dsh 会话执笔·Codex 认账」一行），不另复制文件（内容已在仓）。
3. **两处事实修正**（不改结论·归档时顺手）：①汇总「dsh组 专项缺盘」标注与用户 08-17 更正冲突（处置见第五节）；②汇总附录 C 排版混排（claude 报告与 Codex v2 串节）。

---

## 二 E4 正式裁定文案（建议原文·含三前置最终形态）

> **裁定：EMC 终局形态 = 形态3（领域平台）。** NL 路由层（diagnose→19 契约→final）停止自研演进，冻结为参赛演示壳；对话智能交通用 harness 经 MCP 承担；EMC 长期投入集中于四类平台资产——**数据与口径 / 知识与范式 / 渲染契约 / 出口接口**。用户 R6 论断（「深挖路由=死路·在通用基础上搭架子」）经 claude组 独立反证检验成立，予以采纳。

**三前置（最终形态·claude 原案 + Codex 质证工程化，合并如下）**：

1. **范式显式迁移（C11·三层可操作化版）**——工具 description 拆 `when/limits/output` 结构子字段（CI lint 机器可判·「裸函数」不放行）；`caliber_ref` 出处指针（指向 paradigm.py / kb / 口径注册 id·含量=可追溯知识条目数）；C10 平台 eval 加范式回归用例（工具面 vs 裸调口径一致才过）。配套：paradigm.py 尺度纪律转生 kb 资产（kb_facts / MCP context resource）——**路由器沉没·范式知识不沉没**。
2. **参赛演示用 QA 冻结壳（范围精确化版）**——冻结=前端 QA 主链路（diagnose→FC→final）**零新能力**，非仓库级 code freeze（B/C 线、G1、渲染 API 照动·不碰 QA 主链路）；附「演示阻断 bug 例外条款」：只修阻断演示的缺陷、不做增强。
3. **冻结≠放烂**——B002/B004 等 CRIT 修复（CB-39 D1 批）不因冻结取消且优先级上升：壳不再进化后，「不撒谎」是演示壳唯一立信来源。

**附带裁定（随 E4 一并拍）**：形态2 被吸收（M2 变身=三消费端管道·M4=C10 降权存续·M1/M3/M5 体内停投）；**M5 体外化确认**——可验证操作类需求由「宿主经 MCP + 数据敏感分层 T0-T3 + 五判据」承接（宿主绕过残余风险=分层防误用不防恶意的诚实边界已入宪法附录）。

---

## 三 并轨排期确认表（任务·一张表）

| 序 | 批 | 量级 | 依赖 / 备注 |
|---|---|---|---|
| 1 | **CB-39 B1-B4 照旧**（main 在途） | 按计划 | 平台宪法地基（口径注册表=平台核心资产） |
| 2 | **C 线照旧**（C1-C5） | 按计划 | 平台资产填充；E15 黄金问题验收改「工具面+平台」双问口径 |
| 3 | **G8a 静态契约派生** | 0.5d | **可即行**（零风险·文件零交集） |
| 4 | **G8b 动态 enum 接地** | 0.5d | 挂 B1 完成后（usage 字段=过滤键·防 analysis_output 撞铁律7） |
| 5 | **G10 七工具 MCP spike** | <1d | G8 后；验收含摩擦差量化（bash vs MCP 两组实测入档） |
| 6 | **G1 行业表格式级** | 2-3d | C 线后开工（CB-40 定稿不变）；「指定项」清单待用户（见待决①） |
| 7 | **渲染 API 三步** | 1.5-2.5d | spec 契约 0.5d → 前端 loader 1d → MCP render 工具 0.5d；令牌解析权威留 JS·双载防双源；v1 只管呈现 |
| 8 | **薄壳 v0（单轮 FC 消费型入口）** | 2d | 三前提：G10 server 已产出 / 坚持单轮 FC（多轮 loop=自研 mini harness=踩 M1/M3 停投边界）/ Windows stdio 无坑（vision_bridge 已证可跑） |
 | 外部 | 场景 S（dsh Web）联调 | — | dsh 侧外部依赖·不占本队排期 |

注：P1-3（Threading）因 run_analysis 工具 v2 化需求下降而降权，但 api.js 适配等工程线照 CB-39 原计划；P1-5 位置不变（R1 前）。

---

## 四 D1-D4 残余项处置（R1 四决策点在形态3 语境下）

| # | 处置 | 理由 |
|---|---|---|
| D1 否决方案A | **自动成立·升格为终局表述** | E4 把「否决运行时寄生」升格为「停止自研入口智能」——比 D1 更强·无需单独再拍 |
| D2 立项 B+spike | **并入 E4 拍板** | G10 从侧门升主路·量级/工具面已在排期表——随 E4 一并生效 |
| D3 MCP 唯一解 | **事实已闭** | R1 dsh 实锤（原生 MCP 客户端·`mcp__server__tool` 同形）——无需再拍 |
| D4 dsh组 回归 | **转为「外部依赖方」常态** | 见第五节处置——不再以「回归评审席」论 |

---

## 五 dsh 落盘处置（含疑点·二选一的修正建议）

**事实核验（已闭环·用户贴文确认）**：
- dsh组 **R1 回应有落盘**：`EMC-dsh整体合体_寄生式嵌入_回应_dsh组-2026-08-16.md`（21KB·四决策点结论+MCP 实锤+breaking 定量）——结论持续有效；
- **dsh 形态3 落盘已确认**＝「Codex v2」文件本身（dsh 会话执笔·答 A 卷联合评审·写入 Codex 槽位）——「缺盘」实为 **prompt 投递交错**（dsh 答了 A 卷没答 B 卷），不是未交付；
- 形态3 **B 卷五问专项**（嵌入位置谱系/配置面/暴露面/R1 重审/挑战框架）**仍无作答**——这是唯一真缺口。

**建议（维持·理由更新）**：**先拍 E4+排期，dsh B 卷五问作后置补充轮**——
1. 两组证据已足（七议题零对抗分歧）·dsh 侧工程本属排期第 8 位外部依赖——**不因外部缺盘阻塞内部决策**；
2. ~~确认 dsh 落盘所指~~（已闭环）；改为一项确认：**B 卷五问是否仍要 dsh 补答**——若要，后置补充轮照发（内容涉场景 S 联调前提·建议要）；若不要，场景 S 前提由薄壳 spike 自行探路；
3. 五问中唯一影响近排期的点（场景 S 联调前提）本就在外部依赖位——现排期不受阻。

---

## 六 仍缺的证据 / 讨论项（待决三条 + 文档修正两条）

1. **「城市体检指定项」表清单**（G1 前置②·CB-40 遗留）——形态3 下升格为「出口接口资产清单」；C 线收尾时向用户提取（已两次登记提醒机制）。
2. **渲染 spec 无 JS 侧消费者降级路径**——场景 S 真实需求未明：v1 只做「令牌+解析副本双载 + `resolved_by` 标注」·不预研完整降级渲染（防过度前置）。
3. **goal-status.md 增「产品入口」维度**（claude 议题3 建议）+ E4 拍板后首更四维分数——随本轮收敛落。
4. （文档修正·不改结论）台账/汇总的「dsh 缺盘」标注按第五节结论更新；汇总附录 C 排版串节修正。

---

## 七 拍板动作清单（给用户的三键）

1. **拍 E4**：按第二节文案裁定（含三前置+附带裁定）→ 形态3 生效·形态2 吸收·M 系去向生效·G8a 可即行；
2. **拍排期**：确认第三节表 → CB-39 在途批照常·G8a/G8b/G10 按序衔接；
3. **答 B 卷去留**：第五节确认「dsh 五问是否补答」→ 收敛文档定稿归档（A 卷归属注记一并落）。

拍板后我出 CB-41 实施计划（首批=G8a + CB-39 B 线在途续推·先验后推照旧）。

---

> Codex · 2026-08-18 · 零实施·未 git 写·不出完整 plan。
