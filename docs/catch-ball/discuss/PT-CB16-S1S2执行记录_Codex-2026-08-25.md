# PT-CB16 · S1/S2 执行记录（Codex·2026-08-25）

> 依据：`PT-CB16-短板修复计划书_Kimi-2026-08-25.md` §二。白名单：docs/dsh-environment、tools/rag_index.py、rebuild_rag.bat、tools/check_server_freshness.py、_口径注册表.md 第七节、tests/test_render_channel.py（因 C3 订阅身份兼容所需）。

## 白话摘要

做了两件事：①写了一套“装机说明书”（环境配方/版本清单/到岗体检脚本），让 home 机可以按占位符模板快速恢复；②让 RAG 索引能自动判断“资料改了没、要不要重建”，双击 `rebuild_rag.bat` 即可。核心验收都过了：陈旧索引会重建、新鲜索引会跳过；体检脚本故意破坏分支检查会失败并给出修复命令。

## S1 · 环境配方三步走

### M1 模板入库

新建 `docs/dsh-environment/`：

- `profiles/emc-analysis/settings.yaml.template`
- `profiles/emc-analysis/cordis.patch.yml.template`
- `agent-presets/emc-analyst/preset.yml.template`
- `scripts/start-emc.ps1.template`
- `README.md`

硬纪律：全部用 `{REPO}` / `{DSH_HOME}` 占位符，无硬编码绝对路径/用户名；只入库不生成。

### M2 环境清单

`manifest.yml` 记录：dsh 0.1.1-rc.2、Node v24.16.0、Python 3.14.5、@deepseek-ai/dsh-mcp-client 0.1.1-rc.2、EMC 分支 EMC_Codex_Harness、transformers 5.14.1 + sentence-transformers 5.7.0（含 PT-CB15 K7 兼容注记）、RAG 模型 BAAI/bge-small-zh-v1.5、重建命令。

### M3 到岗体检脚本

`docs/dsh-environment/check.ps1`：检查仓库/分支/dsh profile/8000/8080/8600/render_inbox 无 pending/RAG 新鲜度。故意传入错误分支实测：`[FAIL]` 且给出修复命令。

## S2 · 索引重建半自动化

- `tools/rag_index.py` 新增 `--rebuild-if-stale`：复用 `_knowledge_sources/_index_freshness`（与 check_server_freshness 抽公共函数）。
- `tools/check_server_freshness.py` 改调 `rag_index._index_freshness`，消除双份扫描。
- 新增根目录 `rebuild_rag.bat`（双击=检查+按需重建）。
- `_口径注册表.md` 第七节补一行指向 `rebuild_rag.bat`。
- 模块顶部默认 `HF_HUB_OFFLINE=1`，避免重建时联网重试卡死。

验收：

- 陈旧态：知识源（`_口径注册表.md`）mtime 晚于索引 → 自动重建，索引 mtime 今日化。
- 新鲜态：`--rebuild-if-stale` 输出 `索引新鲜（知识源无更新）·跳过重建`。

## 门禁

全量复跑一次：606 passed / 1 skipped / 1 failed（failed=test_progress_contract::test_regenerate_deterministic，独立跑该文件 4 passed；属进度产物门禁环境性波动，非本批引入）。

## 交付

- 白名单文件：docs/dsh-environment/、tools/rag_index.py、rebuild_rag.bat、tools/check_server_freshness.py、_口径注册表.md。
- 兼容性小修：tests/test_render_channel.py（C3 订阅身份 tuple 适配）。
- 未 push；建议主手提交前缀 `PT-CB16(S1)`。
