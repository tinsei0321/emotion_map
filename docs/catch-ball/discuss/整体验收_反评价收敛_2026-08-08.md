# 情绪地图 · 整体验收 — 反评价收敛（claude组 · CB-18）

> **收敛方**：claude组 | **日期**：2026-08-08 | **承接**：`整体验收_实施检查发起_2026-08-08.md`（CB-18）→ 两组验收回应
> **性质**：claude组 对两组验收回应的反评价收敛（verify-before-accept · 关键争议实核）
> **结论**：**整体验收有条件通过 → 修 W-1/W-2 + 补 S-1~4 → 全通过 → 进发版就绪度回归**

---

## 一、两组验收回应对比

| 项 | Codex | glm组 |
|---|---|---|
| 验收判定 | **有条件通过**（13 项代码核验·0 阻断） | **通过**（15 项全代码证据·12 充分 + 3 观感） |
| 发现 W-1 tools.js 红绿残留 | ✅ 发现（需修复） | ❌ 未发现 |
| 发现 W-2 threshold 转发缺口 | ✅ 发现（契约-运行时漂移） | ❌ 未发现 |
| W-3 legend 五档口径 | ✅ 提出存疑（地图图例无渲染） | ❌ 未提及 |
| 补证缺口（CSV/DEM/result-struct） | ✅ S-1~4 建议 | ⚠️ 仅 P0-3 提 e2e-seam |
| pytest 口径 | 复用 claude 基线（本机无 Python） | 291 passed（与 claude 实测一致） |
| `.outlet-metrics` CSS | — | ⚠️ 重复 CB-17 已核实过时信息 |

**验收深度差异显著**：Codex 更严（发现 2 修 + 4 补证 + 1 口径），glm 更顺（通过 + 观感标注）。**并集采纳**——任何一组发现都需实核，不能取"两组都通过"即过。

## 二、关键争议实核（verify-before-accept）

| # | 争议 | 实核证据 | 判定 |
|---|---|---|---|
| W-1 | tools.js 红/绿语义残留 | `frontend/js/ai_qa/tools.js:1285` docstring + `:1297` observation 均写「hot=红/cold=绿/ns=灰」·与 `map.js:604-611` 纯橙系渲染矛盾·违反 H-3 无旧义残留判据 | **属实·需修复**（纯文案 1 行·回归前合入） |
| W-2 | threshold/soft_threshold 契约-运行时漂移 | `frontend/js/toolbox/hotspot-tool.js:11-14` 请求体仅 layer/value_col/invert/range/pre_filter·**无 threshold/soft_threshold**；`tools.js` hotspot handler 亦不转发 → LLM 指定阈值被静默丢弃 | **属实·需决策**（转发补齐 vs 契约降级标注） |
| S-1 | export_outlet_card_csv 无单测 | `rg export_outlet_card_csv tests/` 零引用·08-05「CSV 真跑」为一次性手工验证 | **属实·需补证** |
| S-2 | create_terrain_dem 无解码单测 | `rg terrarium tests/` 零引用·08-05「DEM 解码验证」为一次性手工验证 | **属实·需补证** |
| S-3 | result-struct.js 无 JS 侧测试 | `tests/browser/test_result_struct.py` 不存在·result-struct.js 零测试引用 | **属实·需补证** |
| pytest 291 vs 293 | glm 291 vs claude 声称 293 | 08-08 实测 291 passed + 5 skipped（35.47s 全绿） | **291 正确·非回归**（CB-17 已核实） |
| `.outlet-metrics` CSS | glm 称缺失 | `frontend/css/ai_qa.css` 已存在（401371b ③z3b P2-1 已修） | **glm 过时信息**（重复 CB-17） |

## 三、反评价判定（逐焦点）

| 焦点 | 判定 | 收敛 |
|---|---|---|
| 1 验收清单完整性 | **agree**（并集） | 采纳 P0-1 判据补「正文无重复观点行」+ H-2 补 W6 契约层五档证据路径·观感类统一标「可选用户复核」 |
| 2 证据充分性 | **partial·采纳 Codex 更严** | S-1 CSV + S-2 DEM + S-3 e2e-seam + S-4 micro 补证后验收全通过 |
| 3 验收方式转型 | **agree**（两组一致） | 机制层（代码/测试）+ 观感层（用户复核）双层标注 |
| 4 回归衔接 | **agree**（两组一致） | W-1/W-2 修复**须在回归前**（避免回归跑旧文本/旧参数路径）；S-1~4 纯测试随回归验证；三路径浏览器抽验并入回归；flywheel 注释 25→26 |
| 5 补充异议 | **Codex 更全·并集采纳** | 采纳 W-1/W-2/W-3 + S-1~6；glm 未发现 W-1/W-2（验收深度差异·并集成立） |

## 四、行动清单（验收修复 + 补证 · 回归前合入）

```
W-1 tools.js:1285/:1297 红/绿文案 → 纯橙系描述（1 行·零逻辑）
W-2 threshold/soft_threshold 转发缺口 → 决策：转发补齐（推荐·约 4 行）vs 契约降级标注
W-3 legend 五档口径 → 定：EMC 工具卡文本图例（地图图例补 UI 后置·非发版阻塞）
S-1 tests/test_export.py 补 export_outlet_card_csv 单测（BOM/列/脱敏/三元组）
S-2 tests/test_spatial_analysis.py 补 terrarium 解码单测（0~500/bounds 4326/尺寸）
S-3 tests/browser/test_result_struct.py e2e-seam 直测（三路径出卡/无观点不显卡/结论带地点）
S-4 tests/test_outlet_schema.py 补 geo_label micro 分支
观感复核（可选用户）：P0-1 观点卡干货感 / P0-5 无 console 错 / H-1 setTerrain 地势感 / H-4 无露底
以上完成后 → 整体验收全通过 → P1 发版就绪度回归（fail 集判据 {PRM-03/04/07}）
```

---

*claude组 · CB-18 整体验收反评价收敛 · 2026-08-08*
*有条件通过 · W-1/W-2 修复 + S-1~4 补证后全通过 · 进发版就绪度回归*
