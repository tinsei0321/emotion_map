# PT-CB1 · T2（G8a 契约派生）完工审计 · Codex（2026-08-19）

> 审计方：Codex（审计协助组·按 T4 章程五动作执行）。对象：提交 `35eb32dd`（分支 EMC_harness_dsh·main 冻结纪律遵守）。
> 结论先行：**通过——零打回项**。五动作全绿 + 两处行为变更均判定为有意修复；附 2 条非阻塞观察。

---

## 审计五动作结果

| # | 动作 | 结果 |
|---|---|---|
| 1 | 全量 diff 对照白名单 | **✅ 零越界**——六文件与派发清单逐一吻合（2 新增 + 4 修改·271+/70-），无范围外文件 |
| 2 | 守卫专项 `py -m pytest tests/validate_skill_params.py -q` | **✅ 7 passed**——含 `mirror_freshness`（派生 diff=0）与 `tool_alias_derived`（别名派生等价）两道新闸 |
| 3 | 全量基线 `py -m pytest tests/ -q` | **✅ 375 passed + 3 skipped**——与验收预期完全一致（372+3 重录基线 + T2 新增 3 守卫），零退化 |
| 4 | 追踪注册核查 | **✅ 零新增 ID**——全 diff 无 `register_track_id`/`@track`（纯派生改造·符合 G8a 性质） |
| 5 | 两处行为变更复核 | **✅ 均为有意修复**（详见下节） |

## 行为变更复核（重点两项）

### ① 别名表「通用+专属双层」→ 全按工具派生：**修复确认·非回归**

- **lookup_place `name→q`**：contracts L257 `q` 参数 alias=`['query','name','place']` → 派生正确。旧通用层 `name→'as'` 对 lookup_place 是**误映射**（它没有 `as` 参数）——G8a 修复；
- **hotspot `field_name→value_col`**：contracts L275 `value_col` alias 含 `field_name` → 归位正确。旧通用层 `field_name→'field'` 是**无主映射**（hotspot 无 `field` 参数）——根治；
- **覆盖无损核验**（审计自加项）：旧通用层全部 26 条别名逐条比对新派生表——每条都能在其参数真实存在的工具上找到归宿（`inverse→invert` 唯一所有者 hotspot 已带；`target_layer/target_poi→target` 在 nearest；`mode→how` 在 overlay；`radius_meters/buffer_radius/distance→radius_m` 在 buffer…）。旧表中「映射到不存在的参数」的条目（如 rank 的 `output→as`·density 的 `value→value_col`）被正确清除而非保留——**语义严格更安全，零覆盖回归**；
- 守卫 `tool_alias_derived` 锁死「生成文件==contracts 派生」——禁手改/漏项双向设防。

### ② stages.js SKILL_DEFS 改 re-export：**消费面零影响**

- re-export 行在位（stages.js L13-15 `import … export { SKILL_DEFS }`）；
- panel.js L8 从 `'./stages.js'` 导入 ✓；harness.js 五处消费点（L573/697/935/1072/1406）全走 `stages.SKILL_DEFS` 命名空间 ✓；
- 旧 `_PARAM_ALIAS/_TOOL_ALIAS` 本为模块私有（未导出）——无外部消费者受损；TOOL_ALIAS 仅 stages.js 内 normalizeParams 使用，作用域收窄正确。

## 非阻塞观察（2 条·供主手裁量）

1. **guard 第 6 项（paradigm guard 字段差异报告）当前输出为报告不 fail**——设计正确（diagnose prompt 红线保护区），但建议后续轮看一眼实际打印的差异清单量级：若差异条目多且长期不动，报告会沦为噪音（可加「差异冻结清单」一次性登记）；
2. **生成器幂等性**：`render()` 无时间戳/字典序稳定——✅ 已达标；仅提示未来若 contracts 参数顺序重排（非增删），diff 会非语义变化地报 freshness 红——属可接受代价，无需处理。

## 验收判定

**T2（G8a）审计通过**：白名单零越界 / freshness 绿 / 基线 375+3 零退化 / 零新增 ID / 两处行为变更为修复。**无打回项**——主手可进入收敛。

> Codex · 2026-08-19 · 审计协助组 · 只读+落盘·零实施·零 git 写（本文件由主手归档）。
