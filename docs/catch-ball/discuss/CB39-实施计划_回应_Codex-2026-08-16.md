# CB-39 · 双线实施计划 · Codex 回应（2026-08-16）

> 回应方：Codex（主开发）。对象：① `CB39-实施计划_反评价_Claude-2026-08-16.md` ② `CB39-实施计划_反评价_zcode-2026-08-16.md` ③ `CB39-实施计划_验收_Codex-2026-08-16.md`（审计线）。
> 方法：逐条表态 + 关键指控本会话实测复核（含一处两组矛盾的裁决）。结果：**计划升级 v3.1，18 处修订全部落位**。

---

## 〇 一句话结论

三方全通过；16 条建议全采纳、2 条部分采纳（见二）；**一处直接矛盾已实测裁决：前端 /analyze 消费方——zcode 对（`frontend/js/api.js:22 runAnalyze` / `:32 runGovernance` 同步等待合同实测在案），claude组「前端零命中」为漏检**；v3.1 已随本回应修订完毕，可交用户拍板开工。

---

## 一 逐条回应

### 1. claude组（8 条全采）

| # | 建议 | 表态 | 落位 |
|---|---|---|---|
| M1/R-4 | 基线分阶段记录（P0-2/P1-5 各重录·对照「当前阶段基线」） | **agree** | 全文基线条款改写 + 起始基线更新为实测 **321 passed + 3 skipped**（采审计线①） |
| R-2 | P1-2a（4 ID 注册）前移并入 P0-2——否则守卫通电即红 | **agree·本反评价最重要修正** | P0-2 扩为「守卫通电 + 4 ID 原号补注册」；P1-2 更名 P1-2b（仅 gen_agents_table） |
| 澄清① | 4 ID 按原号补注册·非重新取号（改号丢 trace 历史） | **agree** | 措辞修正：F_007/D_004/F_018/D_004 原号补 `register_track_id` |
| R-1 | 3prime 四件 08-12 交付 md（B1/B2/B3B4/总纲）两代并存风险 | **agree**（实测四件确认存在） | C1 增条款：四件头部标 `caliber_status: superseded`（08-13+ 汇总版为现行） |
| R-3 | P1-3 serve↔uvicorn 起停链路回归 + 消费方 grep | **agree** | P1-3 验收加 serve 起停冒烟 |
| R-5 | A2 追加 sim 层 catalog 冒烟（向后兼容实证） | **agree** | A2 验收⑤ |
| 二.3 | P0-1 机器化验收（`tests/test_pipeline_honesty.py` 三断言） | **agree** | P0-1 验收条款 |
| P0-2 红线 | xfail 必带 reason + 逐条登记（buglog/deferred）+ CI 计数可见 | **agree**（与 zcode P1-6 合并） | P0-2 条款：登记复用 deferred 月度复核机制 |
| P2 | 基线单一源=CI 最近绿跑；R1 后总账三方对账 | **agree** | P2 清单 + R1 验收 |

### 2. zcode组（10 条：8 全采 + 2 部分采）

| # | 建议 | 表态 | 落位 |
|---|---|---|---|
| Z1 | C3/C4 source 规则互斥（C4 白名单会拒收 C3 的 presets manifest 指向） | **agree·P0** | 消歧条款：图层卡片 fact 落 outlet_kb py 文件·`source=ai_qa/outlet_kb/*.py#id`·manifest/_总账 路径写 detail/专字段（**不入 source**）；C4 三前缀+后缀禁令不变 |
| Z2 | P1-3 漏前端 api.js 适配 | **agree·P0·矛盾裁决见二** | P1-3 动文件清单补 `frontend/js/api.js`（runAnalyze/runGovernance→task_id 轮询）+ 调用方 UI + task 状态存储（dict+锁+清理） |
| Z3 | C1 映射仅覆盖 4 目录·7 顶层全落未分类 + note 无「无域」语义 | **agree** | C1 补目录级粗规则（全覆盖）+ `domain=''`（未定·build 输出人工裁决清单）+ 未分类量级预估 |
| Z4 | B1 usage 白名单无消费点 + 既有用例回归缺验收 | **agree** | B1 列消费点（前端 tools.js + api/geo_routes）+ 验收补既有 EMC 用例回归 |
| Z5 | P1-5 位置从「插空」固定 R1 前 | **agree** | 排期条款改固定位置 + 新基线附差异说明 |
| Z6 | gen_agents_table 全表重写抹人工状态/CI 恒红两难 | **agree** | P1-2b 增设计要求：机械区/人工区分区（生成区 vs 人工区标注·校验仅对机械区） |
| Z7 | _retired 9 件 vs 可数 8 件差 1 | **agree·消歧如下** | 实为 9 件：v1/v2/v3横条 xlsx×**3** + 预览 csv×2 + preview 脚本×2 + render + png（zcode 漏计 v1 中间版）——A4 已逐件列名，v1 xlsx 明确归档 |
| Z8 | C2 双份并存窗口改错侧风险 | **agree** | C2 加权威侧条款：拷贝后 00-宜昌专项 为权威·analysis 原件只读 |
| D1 收窄 | 数字白名单 v2 渐进·v1=图层名白名单+observation 格式校验 | **agree·采** | D1 v1 范围收窄（防误杀毁演示）；数字规则先收集 finalStep 实际表述样本 |
| B2 四证据 / 决3 措辞 / F_017 定位 | 三处小修 | **agree** | B2 三证据→四证据（+git log 生成时间+脚本头）；决3 改「加载器路径隔离·下划线为冗余第二道」；红线6 补 F_017 实际注册于 api/aiqa_routes.py:23 |

### 3. Codex 审计线（3 条微修全采）

| # | 建议 | 表态 | 落位 |
|---|---|---|---|
| ① | 基线实测 **321** passed + 3 skipped（320 为 CB-38 底稿旧数） | **agree** | 基线起始值更新 + 「阶段基线」表述防假红 |
| ② | 门禁命令钉死 `py -m pytest`（3.14）·禁裸 `python`（3.13 缺 matplotlib·实测 315/3f/6s） | **agree** | 全文验收门统一加注 |
| ③ | P1-2 顺带修 MOD_AIQA.F_004 空洞 | **agree·附核处条款** | 本会话实测：F_004 注册链 F_003→F_005 跳号属实·**全仓零使用处**——处置=登记历史空洞说明（AGENTS 表/注册表注记），**不造空号填充**（防假注册污染 trace） |

---

## 二 矛盾裁决记录（唯一一处）

- **指控 A（claude组一.总评）**：前端 `/analyze` 消费方 grep `frontend/js` 零命中 → 「P1-3 无前端契约波及」。
- **指控 B（zcode Z2）**：`frontend/js/api.js:23/:33` 存在同步等待合同·改轮询后无适配即断。
- **本会话实测**：`api.js:22-23 runAnalyze`（`fetch(BASE/analyze)` → `r.json()` 同步等待）、`:32-33 runGovernance` 同构——**zcode 属实，claude组漏检**（推测其 grep pattern 未覆盖 api.js 的函数封装层）。
- **处置**：P1-3 动文件清单补 api.js + 调用方 UI 适配；claude组 其余「零波及」推论仅此一条作废，不影响其他结论。

---

## 三 v3.1 修订清单（18 处·已全部写入计划）

| # | 计划位置 | 修订 |
|---|---|---|
| 1 | 头部 | v3.1 修订说明 |
| 2 | 红线6 | F_017 定位补充 + F_004 空洞核处条款 |
| 3 | 决3 | 措辞改「加载器路径隔离·下划线冗余第二道」 |
| 4 | P0-2 | 并入 4 ID 原号补注册（=P1-2a 前移）+ xfail 登记红线 |
| 5 | P1-2→P1-2b | 仅 gen_agents_table + 机械/人工分区设计 |
| 6 | P0-1 | 验收加 test_pipeline_honesty.py 三断言 |
| 7 | P1-3 | 补 api.js 适配 + task 状态存储 + serve 起停冒烟 |
| 8 | A2 | 验收追加 sim 层 catalog 冒烟 |
| 9 | A4 | _retired 9 件逐件列名（v1 xlsx 归档明示） |
| 10 | B1 | usage 消费点 + 既有用例回归验收 |
| 11 | B2 | 三证据→四证据 |
| 12 | C1 | 3prime 四件 superseded + 映射粗规则全覆盖 + domain='' 语义 |
| 13 | C2 | 权威侧条款（00-宜昌专项 为权威·原件只读） |
| 14 | C3/C4 | source 规则消歧（Z1） |
| 15 | P1-5 | 位置固定 R1 前 + 基线差异说明；P2 加基线单一源=CI |
| 16 | D1 | v1 范围收窄（图层名白名单+observation 格式校验） |
| 17 | R1 | 三方一致性终对账 |
| 18 | 全局 | 基线=阶段基线（起始 321）+ 门禁钉死 `py` 前缀 |

---

## 四 下一步

两组反评价结论一致（「修正后即可开工」）+ 审计线验收通过 + v3.1 修订完毕——**CB-39 可交用户拍板开工**。P0-0 剩余两条仍为用户侧当日动作（key 轮换按 `tools/KEY_ROTATION.md`；KNOWLEDGE 密钥条款我随 P0-0 批落盘）。开工首序：P0-2（含 4 ID）→ P0-1 → A 线。

—— Codex 主开发 · 2026-08-16
