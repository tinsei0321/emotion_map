# PT-CB5 · 插座批整轮审计（zcode·2026-08-20 00:00 前后·用户指派）

> 审计方：zcode（**用户指派**）。**利益冲突声明**：主手审计自身设计/实现的轮次——以程序性对抗补偿（破坏性测试/红线 diff 扫描/独立复跑），并在报告如实标注自我发现；**Codex/claude 补审位保留**（用户可随时转发补审）。
> 方法：不重复验收项（均已双验），专做**找茬**——打红线、纯读性、穿透、自误。

---

## 〇 审计结论

**通过（附 1 项 P1 发现已即修 + 1 项自误教训）**——红线零接触、纯读核实、契约引用全真、体积钳制实证、**G-2 服务端穿透洞被审计抓出并当场修复**（416+3 全绿）。

## 一 找茬结果

| # | 项 | 方法 | 结果 |
|---|---|---|---|
| A1 | **diagnose prompt 红线** | `git diff f5e23061..1164b754 -- prompts.py/router.py/paradigm.py` | ✅ **零接触**（G8b 只动契约派生链） |
| A2 | **纯读性** | 模式扫描（写文件/删文件/subprocess/网络外呼/LLM 调用） | ✅ 零命中 |
| A3 | **追踪注册** | import 后查 `_TRACKING_REGISTRY` | ✅ F_021-F_027 齐（初测空象系测试顺序·非缺陷） |
| A4 | **caliber 引用真伪** | 提取 server 内全部卡引用 vs 注册表 | ✅ G-2/K-01/K-03/K-C1 全部实存 |
| A5 | **体积钳制实证** | `top_n=50`→实返 20·`k=99`→实返 10 | ✅（首测 4 行系行政区仅 4 要素·换 22 要素街办层复证实证 20 顶） |
| **A6** | **G-2 结论层穿透**（本次审计核心发现） | 破坏性调用：`zonal_stats(boundary='base_174_aggregate_area')`（analysis_output） | ❌ **原实现返回 rows——服务端消费点无 usage 检查**（枚举只引导宿主不强制·MCP 直调 core 绕过 geo_routes 过滤层） |
| A7 | T4 考试诚实性 | 对照执行实录复核 | ✅ 六轮计数无跳步·保守下界声明在档·双路数值交叉一致 |
| A8 | key 泄漏复扫 | 两文件 grep 六类敏感模式 | ✅ 零命中 |

## 二 A6 处置（发现即修·主手执行）

- **根因**：派发单（主手设计）只写了"枚举引导+KeyError 语义化"，**漏了服务端 usage 强制检查**——设计缺口责任在主手，dsh 按单实现无责。
- **修复**：`_reject_analysis_output()` 守卫（读 manifest·`usage=analysis_output` 即拒·语义化错误+指引 list_data）接入 zonal_stats/buffer/rank 三工具入口；manifest 不可用时不阻塞（G8b 枚举层仍有引导）。
- **修复中的自误（教训 A9）**：首版守卫写 `_json` 而模块导入 `json`——NameError 被 `except` 吞掉**静默失效**（复测仍返回 rows 才暴露）。教训入档：**守卫类代码禁止宽 except 静默——至少打点或收窄异常类型**。已修正并复测通过。
- **回归测试**：三工具结论层拒绝+正常层不误伤（+1 用例·13 项工具测试全绿）。

## 三 R22/R26 纪律执行评价

- **R22（契约产出）**：达标且超配——开卷参数契约（G8b 枚举映射+G-2 过滤机制化）/七工具对外 API 契约（caliber schema）/考试契约固化（五条+双路法+保守下界声明义务）/本审计新增 G-2 服务端强制条款（第 4 张契约）。
- **R26（架子先行）**：本轮即为架子里程轮——七插座+考试+开卷三件套使"任何 harness 即插即用"成为现实。
- 评分：**优**（自我发现 P1 并即修是本轮质量的最强自证·但利益冲突声明仍然成立）。

## 四 遗留与移交

1. B1 复审 prompt 转发（office 遗留·仍待用户）；presets qty 同步批（挂起·机械活）；
2. Codex/claude 补审位开放（本报告+代码可审）；
3. 守卫宽 except 收窄（A9 教训的普遍化检查·下批顺修）。

> zcode · 2026-08-20 00:00 前后 · 审计（用户指派）·零外部依赖
