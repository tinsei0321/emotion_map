# CB-22 · 三支柱对齐实施复验 · 反评价收敛定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（对齐轮实施复验收尾）
> **前置**：两组深度复验已回收（Codex 判定"实施到位·可交付·2 建议级补强" + glm 判定"实施到位可交付·e2e 失败须定性 + B5 用户实测"）
> **状态**：**定稿·待 push + 用户手动检查**

---

## 〇、两组复验问题评估（先评估·再定稿修复）

| # | 问题 | 来源 | 评估 | 处置 |
|---|---|---|---|---|
| **P0** | e2e `test_quickintent_rag_trigger` 1 failed（4/5） | glm 挑战 1 | **环境问题·非回归**——claude 复跑该用例 **PASSED**（11.28s）·整组 e2e **5/5 全绿**（58.6s）·本次改动未触碰 `_quickIntent` 短路逻辑（仅注入块内容）·glm 侧失败为 Playwright/端口环境（三组并发已知项·OFFICE.md 端口隔离） | ✅ 定性完成·无修复 |
| **P1** | finalStep 断言弱化（`"finalStep" in seg` 字符串包含·删调用留注释可误过） | Codex 挑战 2 | **采纳·属实**——精确调用点断言更强防回归（低成本） | ✅ 已修 |
| **P2** | 注入体积 5.5KB 超 <3000B 设计预算·无守卫 | Codex 挑战 1 | **采纳·方案 ②**——素材质量优先（不降 500B/条）·**明确动态注入预算 ≤8KB + 显式守卫测试**（防未来膨胀·与总守卫一致） | ✅ 已修 |
| **W** | B5 指令优先级（ctx.context 强标记在后 vs FINAL_TEMPLATE 图层导向在前·LLM 可能混读） | 两组一致（Codex B5 + glm W1 延伸） | **不预改**——FINAL_TEMPLATE 是承重红线（D019）·强标记已是必要最小动作·两组均标"用户实测核心看点" | 👉 用户手动检查核心 |

## 一、修复实施（P1 + P2 · 已落地）

### P1 · finalStep 断言精确化

`tests/validate_paradigm_map.py::test_rag_query_branch_uses_finalstep`：
- `"finalStep" in seg`（弱·字符串包含）→ **`"await stages.finalStep(ctx, hooks, '')" in seg`**（精确调用点·删调用留注释/变量名不再误过）

### P2 · 注入体积守卫（新测试 + 预算文档化）

`tests/validate_paradigm_map.py::test_rag_injection_volume_budget`（新·体积预算守卫）：
- ① snippet 上限锁 `slice(0, 1000)`/条（5 条 × 1000B + 指令 ≈ 5.5KB ≤ 8KB）
- ② Top-K 请求锁 `k: 5`
- ③ 知识问答指令常量行数有界（1~6 行·防注入块无限膨胀）

`frontend/js/ai_qa/harness.js` 注入块注释补预算标注（★ 动态注入预算 ≤8KB·守卫在 validate_paradigm_map）。

## 二、验证结果

| 项 | 结果 |
|---|---|
| validate_paradigm_map（P1 改 + P2 新） | ✅ **6 passed**（0.04s） |
| e2e 整组（P0 定性） | ✅ **5/5 全绿**（58.6s·含黄金集/端点 text 断言/短路） |
| test_quickintent_rag_trigger（glm 失败用例） | ✅ **PASSED**（11.28s·claude 环境） |
| **pytest 全量** | ✅ **305 passed + 3 skipped 零回归**（validate_* 轨不进全量收集·pytest.ini `python_files = test_*.py` 项目惯例·验证门禁单独跑） |

## 三、B5 指令优先级（用户手动检查核心看点 · 不预改理由）

- **两组一致结论**：强标记（`【本次为知识问答·严禁图层/分析模板】`·ctx.context 头部）是必要最小动作·但 FINAL_TEMPLATE 图层导向（prompts.py:134 三句骨架·正文在前）vs 强标记（注入在后）——LLM 先读谁无实证·**两组均标"须用户实测确认"**（glm 自我挑战也承认是预防性标注·非已证实问题）。
- **不预改理由**：FINAL_TEMPLATE 是承重红线（D019 守卫·<3000B·prompt 内容断言）·任何"条件判据"改动须先过 CB 预检；强标记已是最优非侵入方案·若用户实测仍出图层导向 → 再走 CB 讨论升级机制（如注入头部协议）。
- **glm 提示的升级路径**（若需要）：FINAL_TEMPLATE 加条件判据（"若 ctx.context 含知识问答标记·跳过图层主出口"）或 ctx.context 注入 prompt 头部——均须 CB 预检后动。

## 四、定稿结论

**实施到位·修复完成·可 push**——P0 定性（环境非回归）+ P1/P2 已修 + B5 留用户实测。红线零违反（FINAL_TEMPLATE/diagnose/四态未动）。**下一步：push + 用户手动检查**（检查清单见下）。

---

## 附：用户手动检查清单（复验发起时用户要求"两组复验+修复完成后我会手动检查"）

### ① 知识问答主路径（B5 核心看点）

1. `py frontend/serve.py 8080` 打开 → 问 **「宜昌有哪些更新项目」**
2. 期望：回答为 **LLM 综合的条目式段落**（非文件名列表·非拼贴）+ **关键内容带来源标注** + **无「分析图已生成」/无图层产物**
3. **若仍出图层导向**（"分析图已生成"）→ B5 风险证实·回 CB 升级指令优先级机制

### ② 回答准确性（glm 看点 2）

4. 关键数值（如项目数/投资额）**来自素材**（可对 `docs/urban-renewal-plan/` 原文核对）·素材外信息标注"知识库未收录"

### ③ 回答全面性（glm 看点 3）

5. 回答**综合 ≥3 个检索条目**（非只 Top-1）·归纳共性与差异

### ④ 稳定性（Codex V3·可选）

6. 同问 3 次 → 核心要点一致（非逐字·LLM 表述方差允许）

### ⑤ 边界（可选）

7. 问「宜昌西陵区情绪分布」（分析问）→ 应走正常分析链路（非知识问答短路）·负例不误触

---

*claude组（2026-08-09）·两组复验反评价收敛·P0 定性 + P1/P2 修复 + B5 留用户实测·定稿可推。*
