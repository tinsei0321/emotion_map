# PT-CB11 · P1 批主手回收审计（zcode·2026-08-21 深夜）

> 范围：本批两个 commit——`0fb5d604`（zcode·B3-1/B3-2 注入灰框治本后端）+ `3f9e55a4`（Codex·C1 三件工具 F_033-F_035）。
> 方法：派发单逐条对账 + 真身独立核实 + **真实链路复测**（R8：不与执行方 mock 测试同构）+ 门禁复跑。

## 一 裁决：**通过**（Codex C1 三件·含 2 处记账级小注）

### 逐项对账（派发单 §〇 八条铁则）

| # | 项 | 结论 |
|---|---|---|
| 1 | 纯只读/不改既有函数体 | ✅ diff 纯增量（3 注册行+3 函数+build_server 3 行）·禁改区零触碰 |
| 2 | 惰性导入 | ✅ geopandas/pandas 均函数体内 |
| 3 | _safe_print/禁 emoji | ✅ 零 print 零 emoji |
| 4 | caliber 四键 | ✅ 三件齐·refs=K-C1 |
| 5 | 体积纪律 | ✅ top_n cap 20·layer_output 复用 200KB 硬顶 |
| 6 | 服务端守卫 | ✅ G-2 实测触发（page7_12345_top20 作 boundary 被拒·hint 带 input 层指引） |
| 7 | 五判据答辩 | ✅ 执行记录 §三 逐工具一行 |
| 8 | 测试 | ✅ 29 passed（18→29） |

### 真身独立核实（与执行记录 §一 表互证·全部相符）

- `create_square_grid` 输出 `point_count`/`{col}_sum`/`{col}_mean` ✅
- `hot_spot_analysis` 输出 `Gi_Z`/`Gi_P`/`hotspot`/`hotspot_tier` ✅
- `aggregate_by_polygons(polygon_name_col='name')` 输出可读名 ✅

### 真实链路复测（主手实测·非 mock）

- **grid_aggregate**：L2 T1·800m 格 → 1062 格·max 315 点/格 ✅；value_col 缺列拒绝附可用列 ✅；G-2 拒结论层 ✅；value_col=score 走 _mean 排序 ✅
- **compare_regions**：admin_district vs admin_county → 12954 vs 16932 点·diff 可算 ✅；<2 区拒绝 ✅；str 分隔规整 ✅
- **hotspot_analysis**：L2 T1 → counts {hot:0, tend_hot:66, ns:16711, tend_cold:156, cold:0} ✅（ns 占多=P1 修正口径的预期形态·U 形离散分布）；缺列拒绝附 density 防呆 ✅

### 记账级小注（不阻塞·留档）

1. 执行记录写「新 12 用例/原 17」，实为 **+11/原 18**（末态 29 正确·纯笔误）。
2. 环境缺依赖 7 失败（matplotlib×3+sentence_transformers×4）= 存量环境项，主手 stash 对比与 Codex 分析互证一致——**列 Ops 挂账**（是否补装待用户定，涉 pip install）。

## 二 观察项（本批外·留档不立项）

- `admin_community` preset 报「文件未上传」（84MB 社区.geojson 在盘但 available 检查不过）——**存量问题**，zonal_stats 同样不可用，与本批无关；后续 M1 环境体检批顺手查。
- compare_regions 对不可用 preset 的报错坍缩为通用 `_UNKNOWN_HINT`（沿 zonal_stats 既有约定）——真身的 FileNotFoundError 信息其实更具指导性，留 P2 顺手改进项（不阻塞）。

## 三 后续

1. 派 claude 独立审计（覆盖本批两 commit·含 zcode B3 自审段）——prompt 落盘 `PT-CB11-P1审计派发prompt_claude_zcode-2026-08-21.md`。
2. Kimi 件（B3-3~B3-6+area_stats）回收时并入同一审计轮。
3. 用户复测点：3080 问「12345 诉求最密集的 5 个社区显示在地图上」四口径（填色/图例/tip/面板）——**须 Kimi 前端四件合入后执行**。

> zcode 主手 · 2026-08-21 · P1 半批回收通过（Codex 段）·待 claude 独立审计
