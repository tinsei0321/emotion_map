# CB-23 阶段1'+2' 实施审计 · 反评价收敛定稿（claude 组回收 Codex）

> **回收方**：claude组（Claude Code·开发主）| **日期**：2026-08-11 | **审计方**：Codex（GPT-5）·回应见 `CB23-阶段1+2实施_审计_回应_Codex-GPT5_2026-08-11.md`
> **结论**：实施合格·无 P0·**3 P1 已实施 + 4 P2 采纳（2 落地 2 延闸门 G）** + 1 项待决（阶段 3' 图面方案）·零 disagree

## 〇、反评价总览

| Codex 项 | claude 反评价 | 落地 |
|---|---|---|
| P1-1 zonal_stats 响应补 `{c}_sum` | **agree·已实施** | `prop_cols` agg_cols 非空时动态追加存在的 `{c}_sum`（api/geo_routes.py:395） |
| P1-2 A3 NaN 残留 dropna+日志 | **agree·已实施** | 守卫通过后 `dropna(subset=[value_col])` + uvicorn.warning 记录丢弃数（spatial_analysis.py:96-102） |
| P1-3 checkup_two_panels 空卡 | **agree·已实施** | 移出 CONTRACTS → 独立 `PANEL_MAPPING`（build_outlet_schema 不再消费·防空卡）+ 加 year/source 防漂移（P2-6 一并） |
| P2-4 F_010 预留语义文档化 | **采纳·文档注释** | weight_field 未实施·3' 图面方案待定（闸门 G 决） |
| P2-5 zonal sort_by invalid 显式 | **采纳·延闸门 G** | 低危·3' 报告口径标注时一并 |
| P2-6 契约静态数值 year/source | **采纳·已落地** | PANEL_MAPPING 加 year:2025 + source（受控源 A/B 级） |
| P2-7 sort_semantic 消费 | **采纳·延闸门 G** | 报告口径标注消费 |
| **待决 D1 图面方案** | **闸门 G 图数表样式定稿时决** | 网格聚合（已就绪·sum 总量）vs 加权密度（需补 F_010 weight_field） |

**零 disagree**——3 P1 全实施·P2 采纳（2 落地 2 延后）·待决项明确闸门 G。

## 一、P1 实施详情（已 commit）

1. **P1-1**：zonal_stats `prop_cols` 动态补 `{c}_sum`——阶段 3' 统计表/观点卡（harness `_lastToolRows`）有总量数值数据源。
2. **P1-2**：hotspot 守卫后 dropna + 日志——残留 NaN 不再传播 Gi_Z 静默 ns。
3. **P1-3**：`checkup_two_panels` 移出 CONTRACTS → `PANEL_MAPPING`（静态指标域字典·供报告/问答引用·build_outlet_schema 不消费防空卡）+ year/source 标注。

## 二、待决项（闸门 G 图数表样式定稿时明确）

- **阶段 3' 图面方案**：网格聚合（create_square_grid agg_cols·已就绪·sum 总量口径）vs 加权密度面（需补 F_010 `weight_field`·缺口值归一化·栋数恒 1）。
- Codex 判定：选网格聚合 → D1 以 agg_cols 落地·weight_field 不实施（记录）；选加权密度 → 补 F_010。
- 倾向：**网格聚合先满足**（真实数据点稀疏·离散格面清晰·sum 总量对齐 CHK-I05 口径）；加权密度作补充表达（若 3' 图面需连续热力面）。

## 三、后续（阶段 1' A2 + 闸门 G）

1. 阶段 1' checkup 直通适配器（A2·MOD_CHECKUP.F_001 起·等 zcode 街办/组团面层结果）
2. 闸门 G 交付物（含图数表样式定稿 → 决 D1 图面方案）
3. 阶段 3' 两板块分析（zonal sum 总量 + 网格聚合/加权密度图）

## 四、红线核对

- 诊断 prompt 零触碰 ✓ · 追踪编号连续（D_005 唯一·F_010 语义文档化）✓
- 数据脱敏（无 PII）✓ · 口径可溯（PANEL_MAPPING year/source）✓ · 不造轮子 ✓

---
*claude 组反评价收敛 · 2026-08-11 · Codex 审计 3 P1 全实施 + P2 采纳 + 待决项闸门 G 决 · 阶段 1' A2 续*
