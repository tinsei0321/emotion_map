# PT-CB7 · 主执行记录（Qoder · 2026-08-21）

> 执行人：Qoder（用户令主执行·白名单内 git 写权）。依据：任务书（T1-T6+增补 T7-T9/F1-F4）+ 主执行前审计（M1-M4 修正随执行生效）+ 任务拆解与执行计划（复杂自执/简单派 dsh 三批）。
> 纪律遵守：每任务一 commit（`PT-CB7(T{n}):` 前缀·显式路径·禁 add -A）；EMC 仓外改动只落记录；dsh 上游零 merge；编码铁律全生效。
> 提交链：`5b91f19e`(规划) → `97fb95bd`(T1) → `0d752150`(T6) → `9e7ca1dc`(T8) → `c6013cf8`(T5+T9) → 本记录。

---

## 一 任务销号总表

| T | 状态 | 证据 |
|---|---|---|
| T1 图层残留自清理 | ✅ 完成 | commit 97fb95bd；全量 435 passed |
| T2 better-sidebar terminal chunk | ⏳ 派 dsh 批 1（决策树照做·简单任务） | 见拆解文档派发单 |
| T3 EMC 入口内嵌+就绪 gate | ✅ 代码半完成（构建+服务供给已验证）·运行时验证派 dsh 批 2 | 见 §三 |
| T4 计时取证 | ⏳ 派 dsh 批 1（headless 跑题填表·简单任务） | 见拆解文档派发单 |
| T5 身份卡+人设+RAG | ✅ EMC 半完成（c6013cf8）·dsh 半（人设 prompt 落 profile）派 dsh 批 2 | 见 §五 |
| T6 MCP 描述紧凑化 | ✅ 完成 | commit 0d752150；全量 435 passed |
| T7 双模预设 | ✅ 设计定稿（本文 §六）·配置落地+验收跑派 dsh 批 3 | 见 §六 |
| T8 800m 脚本参数化 | ✅ 完成 | commit 9e7ca1dc；复现数字全对 |
| T9 口径对照段固化 | ✅ 并入 T5 身份卡 + T8 summary.caliber_compare | 见 §五 |
| F1-F4 调研 | ✅ F1/F4 我已实证（见 §六/§七）；F2/F3 结案 | 见 §六 §七 |

## 二 T1 图层残留自清理（EMC 仓）

- **改动**：`frontend/js/render_client.js` +22 行——新增 `_clearDshLayers()`（遍历 `getLayers()` 移除所有 `[dsh] ` 前缀层·`removeLayerFromMap`+`removeLayer` 成对调用·单项 try/catch + console.warn 不阻塞=A9 口径），在 `_apply` 内首个 `addToolboxLayer` 之前调用；import 扩 `state.js`/`map.js` 真身（M1 修正：shared.js 未转出这三函数）。
- **测试**（M2 修正口径）：`tests/test_render_channel.py` +1 用例 `test_render_client_clears_dsh_layers_before_apply`——静态契约断言（import 在位/清理函数存在/顺序：_apply 体内清理先于首个 addToolboxLayer）。运行时实测移交浏览器实测清单。
- **验收**：单文件 11 passed（10+1）；全量 435 passed / 2 skipped。
- **设计风险备注 D-R1**（记录在案）：「只留最新 [dsh] 层」为任务书语义裁定；未来若需多图并存回到 spec 级开关。

## 三 T3 EMC 入口内嵌 + 就绪 gate（dsh 环境·dsh-emc-entry）

**改动文件**（仓外·按纪律只落记录）：`D:/Github/dsh-emc-entry/src/client/index.ts` + `components.tsx`；bundle 已重建 `lib/client.js` 13,631B（tsdown 2026-08-20 23:42）。

- **registerTab 内嵌**（取代外部 Edge 开图·任务书用户偏好裁定）：`apply()` 内经 `ctx.betterSidebar.registerTab({ id:'dsh-emc-entry:map', title:'EMC 情绪地图', single:true, hidden:true, component:EmcMapTabDescriptor })` 注册（蓝本实证：better-sidebar service.d.ts L117-200 TabDescriptor 契约·component 必填·single=单实例聚焦）；`inject` 增 `'betterSidebar'`。服务不存在时跳过注册、launch 自动降级 `window.open`。
- **launch 链改造**：startSession（void·不 await）→ 欢迎卡 → `hostOpen(start.bat)`（R14 直连 `/api/host.openPath` 保留）→ **就绪 gate** `waitForEmc()`（40s·轮询条件升级为 `emcFullyReady()` = `/api/v1/health` 与 `/frontend/index.html` **双端点 no-cors 送达**，不读 status·探测纪律不变）→ 通过则 `betterSidebar.openTab({type:EMC_TAB_ID, url:EMC_MAP_URL})` 内嵌；失败 console.warn 不导航。
- **半载补救**（M3 修正·跨源 iframe 不可读 contentDocument）：`EmcMapTab` 组件 = iframe + 顶部「新窗口打开」备用按钮（外部 Edge 兜底=任务书④）；挂载起 8s 宽限期未 onload 视为半载 → remount 重加载一次（reloaded 哨兵防循环）。
- **服务端验证**：`GET /plugins/dsh-emc-entry/client.js` 200 / 13,631B / EMC_TAB_ID·emcFullyReady·registerTab·FIRST_LOAD_GRACE 四特征全在位（junction 直供·无需重启 web·浏览器硬刷新即载新 bundle）。
- **遗留（运行时验证·dsh 批 2 + 主手实测）**：点击入口 → 内嵌 Tab 完整加载无乱码（含 8080 冷启动场景）；terminal 标签状态（与 T2 联调）；「新窗口打开」按钮行为。
- **执行过程记录**（诚实披露）：① 首版 JSX 写在 .ts 文件致 tsdown PARSE_ERROR——已将 JSX 移入 components.tsx（EmcMapTabDescriptor 转接）修复；② 仓外文件编辑工具受限，经仓内幂等补丁脚本（精确段匹配·缺段即中止不部分写入）落盘，补丁脚本用后已清理。

## 四 T6 MCP 八工具描述紧凑化（EMC 仓）

- **改动**：`tools/mcp_server_emc.py` 八工具 docstring 压为 3 行结构（一句用途 + 参数要点（必填与关键枚举）+ 一句限制）；实测字符数（去换行）：list_data 143 / rag_query 163 / kb_facts 152 / outlet_card 135 / zonal_stats 221 / buffer 141 / rank 194 / render_spec 245。
- **例外说明**：render_spec 245 字符超 ~200 目标——11 参数的工具，保留必要枚举（kind 两值/dataset_id 与 geojson 二选一/value_field 默认/K-C1）后的最小集，判为合理例外。
- **红线自查**：函数签名 / CALIBERS / 返回结构零变化；`ai_qa/tool_contracts.py`（EMC 内部 FC 权威）零触碰；tests/test_mcp_server_emc.py 无描述断言（实测 grep 零命中）无需同步。
- **五判据答辩**（逐工具）：数据面（list_data/rag_query/kb_facts/zonal_stats/buffer/rank）用途句均点明输入输出域；确定性（kb_facts/outlet_card 标注确定性/零 LLM）；口径（list_data 限制句含 caliber 口径标·render_spec 含 K-C1）；限制（冷启动/体积/索引前置一句合并）；边界（render_spec 注明 T1 覆盖行为）。
- **验收**：13 passed（单文件）；全量 435 passed / 2 skipped。

## 五 T5+T9 身份卡扩写 + 口径对照纪律（EMC 半）

- **M4 修正执行**：FACTS 已存在简化身份卡 `EMC-IDENTITY-01`（08-20 加·仅欢迎卡文案）——**原地扩写 detail**（不新增重复卡）：保留原欢迎卡文案 +【能做什么】七件工具+render_spec 出图+口径引用义务 +【不能做什么】不编造/结论层禁作输入/宏观≠微观/不碰 QA 管线 +【口径纪律】K-01/K-02/K-C1 +【交付纪律（T9）】数据类交付必带口径对照段（卡 ID+子集声明+覆盖说明）。补 version_note 注明扩写沿革。
- **T9 固化双落点**：① 身份卡纪律条款（认知层）；② `SCRIPT/gen_12345_grid_800m.py` 的 summary.caliber_compare（执行层·见 §六 T8）。
- **验证**：`kb_facts(keyword='EMC')` 命中 EMC-IDENTITY-01（实测 hits=1）；`py tools/rag_index.py --build` 重建 364 条 / 512 维 / 33.9s；全量 435 passed。
- **dsh 半（派 dsh 批 2）**：web + emc-test 两 profile 落 EMC 人设 system prompt。**定稿文本**（dsh 照录）：
  > 你是 EmotionMap Copilot（EMC 情绪地图助手），宜昌城市情绪空间分析平台的分析助手。能力：经七件标准工具（list_data/rag_query/kb_facts/outlet_card/zonal_stats/buffer/rank）与 render_spec 出图做区域情绪与空间分析。纪律：先 list_data 查清单再操作；引用数据带 caliber 与口径注册表卡 ID；社区口径按 K-C1 枚举不混用；不编造数字，无数据则明说；usage=analysis_output 的结论层仅展示、禁作分析输入；宏观倾向不作微观诊断；数据类交付必带口径对照段（卡 ID+子集声明+覆盖说明）。
- **待补**：新会话欢迎后自述身份验证（需 RAG+人设就位后·dsh 批 2 取证）。

## 六 T8 800m 脚本参数化 + 口径对照段（EMC 仓）

- **改动**：`SCRIPT/gen_12345_grid_800m.py`——四参 CLI（`--grid-size` 默认 800 / `--source` 默认原路径 / `--region-caliber xw_174|none` / `--out-dir` 默认 `DATA/exports/12345_<N>m方格`）；summary.json 增 `caliber_compare` 口径对照段（本结果声明/源总数/落点率 61.8%/K-01=42,871/K-02=49,192 只读引用/子集偏差声明/注册表路径）；render_inbox 重跑前**自清本脚本旧 spec**（按 origin.source_tool 匹配·本次实跑清 5 份——顺带治了 CB 评估 R2 的多 spec 残留）；`SCRIPT/gen_task_report.py` 会话路径参数化（--log/--out-md/--out-json·缺省=原路径）。
- **审计自我纠错（诚实披露）**：我此前评估报告 R1 判「poi_names/place_name 为脚本遗留污染」——**经 core 源码核实为误判**：这些字段是 `core.spatial_analysis._attach_poi_attrs`（spatial_analysis.py:705-768·CB-16 Wave 2）在 grid 模式有意自附的下钻链字段。T8 处置：输出前裁剪（12345 交付不需要·不动 core）+ 口径对照段声明出处（dropped_core_fields/dropped_fields_note）。
- **验收（复现同数字）**：默认参数实跑——ok 点 11,207 / 网格 434 / 最大格 845 / in_xw 点 9,004 / in_174 点 7,656，与原交付全对；源含坐标点总数 18,130（落点率 61.8%）。

## 七 T7 双模预设设计定稿 + F1-F4 结案

### F1 实证（effort 覆写支持）

- dsh 支持 **profile/部署级** effort：`@deepseek-ai/dsh-llm-deepseek` 插件 config `reasoningEffort: 'off'|'low'|'high'|'max'`（缺省 high；`thinking: 'disabled'` 可全禁）——出处 `D:/Github/dsh/docs/config-catalog.md` L916-926（dsh-llm-deepseek Config 节）。请求级显式值优先于部署默认。
- **无 per-step 覆写**；词表无 medium——任务书「low/medium」映射裁定：**分析档 = low**。

### T7 设计定稿（dsh 批 3 照此落地·禁自行发挥）

**emc-analysis（默认档）**——复制 emc-test 为基底：
1. bundles：`@deepseek-ai/dsh-base` + `@deepseek-ai/dsh-headless`（命令行形态·工具面天然只剩 mcp__emc__* 与核心件·满足「工具子集」）；
2. cordis.patch.yml：保留 mcp-emc insert（照 emc-test）+ 追加 llm-deepseek 配置：
   ```yaml
   - override:
       - id: llm-deepseek
         config:
           reasoningEffort: low
   ```
   （override 语法以 `dsh --profile emc-analysis --dump-config` 实测校准；若 id 不符按 dump 输出修正）
3. settings：模型 deepseek-v4-flash；
4. step 预算 ≤15：dsh 无原生 step 预算配置（F1 附带查证）——**软约束**：profile 目录放 `README-emc-analysis.md` 写明预算与「硬阻塞即停下问一次」纪律；执行时超限由会话人裁定。
5. 升级三信号（机械规则·禁 AI 分类器）：显式词（研究/调研/探索/对比方案/系统评估/为什么）→ 改用 emc-research；分析档硬阻塞 → 停下问一次；任务模板化后分析=选模板直跑（T8 模板为首个）。

**emc-research（显式档）**——复制 web 或 emc-test-headless 基底：`reasoningEffort: max` + 全工具（pwsh/editor 保留）+ 无预算。

**验收锚**：`dsh --profile emc-analysis` 跑「12345 热线诉求最密集的 10 个社区」类任务（=rank 一次调用形态）wall-time ≤2 分钟。口径提示：RAG/工具冷启动 10-30s 为硬底，目标只在「模板/工具直跑」形态成立（审计已预告）。

### F2/F3/F4 结案

- F2（模板先行 vs G10 插座）：已裁=模板先行——T8 已落地；`SCRIPT/gen_12345_grid_800m.py` git log 无他人并行改动（commit 链 1c2ce974 入库 → 9e7ca1dc 参数化）。
- F3（EMC 内部 effort 分档）：观察项——本轮零动作（QA 壳冻结期·本次慢在 dsh 侧非 EMC 内部）。
- F4（context 压缩机制）：**存在**——`@deepseek-ai/dsh-compaction-basic` 插件（config-catalog.md L470-503）：thresholdRatio 0.8 触发 / retainRatio 0.16 / 可配 summarization 模型 / auto 步边界压力监听默认开。长任务（30+ step）cacheRead 膨胀有原生解；是否调参列入观察项（分析档低 effort+短链路下暂不需要）。

## 八 待主手裁决 / 备注

| # | 项 | 说明 |
|---|---|---|
| Q1 | 前序 3 份文档入库 | 已随 5b91f19e 提交（CB42/架构评议/PT-CB6 审计）——如主手不同意可 revert 该三件 |
| Q2 | T3 运行时验证窗口 | 需浏览器点击链路实测（冷启动场景含 8080 停启）——建议主手在场时做 |
| Q3 | render_spec 描述 245 字符例外 | 见 §四——是否接受或再压 |
| N1 | pre-push 插件同步提醒 | dsh-emc-entry / dsh-subprocess-win32 等 link/file 本地包未入 sync-config 清单（仓库钩子提醒·非阻塞·归 dsh-profile 仓维护） |
| N2 | 评估报告 R1 误判 | 已在 §六 纠正（poi 字段=core 有意设计） |

## 九 浏览器实测清单（用户/主手参与·dsh 批 2 后）

1. dsh web 页面硬刷新（Ctrl+Shift+R）→ 左下角「EMC 情绪地图」按钮在位；
2. 8080 停时点击 → start.bat cmd 窗口弹出 + 新会话 + 欢迎卡 → 就绪后**内嵌 Tab 打开地图页无乱码**；
3. Tab 上「新窗口打开」按钮 → 外部浏览器开图；
4. 同会话两次 render_spec 出图 → 地图仅剩最新 [dsh] 层（T1）；
5. 新会话问「你是谁」→ 自述 EmotionMap Copilot 身份与边界（T5·人设就位于 dsh 批 2 后）。

---

> Qoder · 2026-08-21 · EMC 仓四 commit 全绿（435 passed/2 skipped）；dsh 环境 T3 代码半就绪；dsh 批 1/2/3 派发单在 `PT-CB7-任务拆解与执行计划_Qoder-2026-08-21.md`。等 zcode 回收验收。
