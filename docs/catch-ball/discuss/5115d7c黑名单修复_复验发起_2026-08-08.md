# 情绪地图 · 5115d7c 黑名单误伤修复复验 — 发起（claude组 · CB-19 补）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB 修复复验（两组·评估修复正确性）
> **承接**：Codex T7 阻断（发版回归全面测试·黑名单误伤「龙泉」）→ claude 修复（`5115d7c`·只拦单要素）→ 两组复验
> **目的**：让两组对黑名单误伤修复独立评估——是否彻底（T7 PASS）+ 是否过头（小溪塔仍拒识）+ 语义权衡（只拦单要素的漏拦风险）+ 回归风险

---

## 〇、修复摘要（claude组 已完成·待两组评估）

### 背景（Codex T7 阻断·发版回归全面测试）

`_rejectNonAdminBoundary`（boundary-resolve.js·469bf32 引入）检查**所有** GeoJSON dict 的 `feats[0]`——演示行政区层 `行政区.geojson` 首要素恰为「龙泉」（黑名单词）→ 多步链「把西陵区裁出来」的 extract_feature 解析层首 → 被误拦 → **新增 fail·阻断发版候选**（Codex 逐行核实 + 浏览器复现 3 次稳定失败）。

### 修复（`5115d7c`·前后端同步）

```js
// 前端 _rejectNonAdminBoundary（boundary-resolve.js）
function _rejectNonAdminBoundary(b) {
  if (b == null || typeof b !== 'object') return null;
  const feats = b.features;
  if (!Array.isArray(feats) || feats.length !== 1) return null;   // 仅单要素（LLM 直传特征）·多要素（图层）放行
  ...
}
```
```python
# 后端 resolve_boundary（geo_registry.py）
if _name_col is not None and len(polys) == 1:   # 仅单要素
    for _nm in polys[_name_col].astype(str): ...
```

**逻辑**：只拦**单要素** GeoJSON（LLM 直传特征 `features.length===1`·如 `{features:[小溪塔]}`）·多要素（从已加载面层/索引解析的整层·行政区层 9 要素）放行。

### 自测（claude 已完成）

- **T7 三路径 PASS**（多步链/multi 出 4 要点卡·PARTIAL 无观点保守不显卡·W3 正确）
- **verify_prm57 PASS**（PRM-05 西陵区聚合执行）
- **pytest 303 passed + 5 skipped** 零回归（含 PRM-07 单测·单要素拒识仍过）

---

## 一、复验焦点（两组独立评估）

### 焦点 1：修复是否彻底
- T7 多步链「把西陵区裁出来，再和商业用地叠置取交集」现在 PASS？（可跑 `test_insight_cards_3path.py`）
- 有无其他被黑名单误伤的路径（clip/overlay 链·multi·extract）？

### 焦点 2：修复是否过头
- 小溪塔单要素仍拒识？（可跑 `verify_prm57_fix.py` 或代码核验）
- 前后端「只拦单要素」逻辑是否一致？

### 焦点 3：语义权衡（核心）
- 只拦「单要素」是否合理？**漏拦风险**：LLM 直传**多要素**法定功能区 GeoJSON（如 `{features:[小溪塔, 龙泉]}` 2 要素）现在会漏过——是否需补（多要素但全部命中黑名单也拦）？还是接受（LLM 直传多要素法定功能区极罕见·用户上传层放行更重要）？
- 与 CB-14「EMC 不硬猜」+「用户上传层不受限」的平衡？

### 焦点 4：回归风险
- pytest 303 零回归·有无遗漏场景（如 compare 多区·buffer 多要素）？

---

## 附 A：复验 prompt（发两组用）

```text
【CB-19 · 5115d7c 黑名单误伤修复复验】情绪地图 — T7 阻断修复正确性评估

你以第三方独立评估身份。**读本地文件即可·无需 git pull/push**（评估方只读·工作区已同步·HEAD=417c997）。

背景：发版回归全面测试中，Codex 发现 T7 多步链 FAIL【阻断】——PRM-07 黑名单 `_rejectNonAdminBoundary`（boundary-resolve.js）误伤演示行政区层（首要素「龙泉」命中黑名单·合法 extract/链被拦）。claude 修复（5115d7c）：前后端黑名单只拦单要素 GeoJSON（features.length===1）·多要素图层放行。

必读：docs/catch-ball/discuss/发版回归全面测试_结果_Codex-GPT5_2026-08-08.md（T7 阻断根因）
按需核实：frontend/js/ai_qa/boundary-resolve.js（_rejectNonAdminBoundary·只拦单要素）· core/geo_registry.py（resolve_boundary·len(polys)==1 才拦）· verify_prm57_fix.py · test_insight_cards_3path.py

评估焦点（逐条 agree/disagree/partial + 证据）：
1. 修复是否彻底（T7 多步链 PASS？有无其他误伤路径？）
2. 修复是否过头（小溪塔单要素仍拒识？前后端逻辑一致？）
3. 语义权衡（只拦单要素合理？多要素法定功能区漏拦风险——需补还是接受？CB-14 平衡？）
4. 回归风险（pytest 303·有无遗漏场景？）

核心标尺：CB-14「EMC 不硬猜」+「用户上传层不受限」平衡。
产出：docs/catch-ball/discuss/5115d7c黑名单修复_复验回应_{你的组名}_2026-08-08.md（〇 结论 / 一 逐焦点 / 二 可否接受结论）
报告中文为主，代码/路径保留英文。
```

---

*claude组 · 5115d7c 黑名单修复复验发起 · 2026-08-08*
*评估只读不 git·回应落 discuss/·claude 收到后反评价收敛*
