# PT-CB4 · T3 执行记录（dsh·2026-08-19）

> 执行：dsh（副手）· 2026-08-19 · 分支：`EMC_harness_dsh`（main 冻结勿碰）。
> 任务：新建 `tools/check_caliber.py` + `tests/test_check_caliber.py`（口径复核工具）。
> 白名单：`tools/check_caliber.py`、`tests/test_check_caliber.py`、本执行记录。

---

## 〇 F_020 注册确认

- 模块内 `register_track_id('MOD_AIQA.F_020', '口径复核：读注册表作废值→扫素材→命中即fail清单')` 已写入 `tools/check_caliber.py`；
- 主公开函数 `main()` 已挂 `@track('MOD_AIQA.F_020', track_args=False)`；
- 未改 `core/tracker.py`，未跳号/改号。

---

## 一 交付物

| 文件 | 说明 |
|---|---|
| `tools/check_caliber.py` | 新工具：读 `_口径注册表.md` X-01 作废值表 → 生成书写变体 → 扫描默认白名单 → 命中输出并 exit 1 |
| `tests/test_check_caliber.py` | 7 个用例：千分位/百分比两种形态、干净通过、命中清单含卡 ID、_retired/注册表排除、文字前缀+斜杠复合、数值边界防误报 |

---

## 二 实现要点

- 解析 `### X-01 作废数字清单` 表格第一列，去掉 `**` 加粗标记。
- 变体生成覆盖：
  - 带千分位 `5,615` ↔ 不带 `5615`；
  - `87.9%` ↔ `87.9`；
  - 文字前缀紧凑/空格差异（`港务 1,153` ↔ `港务1153`）；
  - 斜杠复合作废值（`双高 16/32/3/26（各代）` → `双高 16` / `双高3` / `双高 32` 等，不生成裸 `3` 防全域误报）。
- 数字匹配加前后边界：`5615` 不命中 `15615`/`56150`；裸 `3` 不因双高复合值误报。
- 默认扫描 `DATA/analysis` 与 `docs/urban-renewal-plan` 两个前缀；排除 `_retired/` 目录、注册表自身、二进制扩展名（xlsx/png/pdf 等）。
- `print` 全部走 `_safe_print`，ASCII 标记 `[OK]/[ERR]/[WARN]`。

---

## 三 真命中清单（默认白名单实测）

执行 `py tools/check_caliber.py` → **exit 1，6 处命中**（有价值发现，非失败）：

| # | 文件:行 | 命中值 | 所属卡 |
|---|---|---|---|
| 1 | `DATA/analysis/汇总/page2_两个方面_分析汇总_2026-08-13.md:4` | 87.9% | X-01 |
| 2 | `DATA/analysis/汇总/page2_两个方面_分析汇总_2026-08-13.md:103` | 87.9% | X-01 |
| 3 | `DATA/analysis/汇总/page2_两个方面_分析汇总_2026-08-13.md:108` | 87.9% | X-01 |
| 4 | `DATA/analysis/汇总/page2_两个方面_分析汇总_2026-08-13.md:144` | 87.9% | X-01 |
| 5 | `DATA/analysis/汇总/体检2026补充_全域数据包_2026-08-17.md:178` | 双高3 | X-01 |
| 6 | `docs/urban-renewal-plan/3prime/分析计划与内容_总纲_2026-08-12.md:111` | 双高 16 | X-01 |

说明：以上均为**历史口径/作废演变记录中的真实旧值**，需主手/蒸馏批按纪律决定改写或加注「历史口径·已废弃（见口径注册表 X-01）」；本工具只报数不代改。

---

## 四 验收

- `py tools/check_caliber.py` 默认白名单实跑：exit 1，6 处命中（见上）。
- `py -m pytest tests/test_check_caliber.py -q`：7 passed。
- `py -m pytest tests/ -q`：**396 passed + 3 skipped**（基线 389+3，新增 7 passed，上浮已注明）。

---

## 五 提交

- `PT-CB4(T3): 新增 check_caliber 口径复核工具 + 测试（F_020 注册）`
- push origin `EMC_harness_dsh`
