# PT-CB7 · 「稳定与灵魂」批 · 任务书（zcode 设计·dsh 执行·2026-08-21）

> 依据：`PT-CB7-综合评估与下一步优化_zcode-2026-08-21.md` + 用户四 bug 报告。设计=zcode·执行=dsh。
> 分支纪律：EMC 仓 = `EMC_harness_dsh`（main 冻结）；dsh 环境 = `D:/Github/dsh/`（**冻结：不 merge 上游**）+ `D:/Github/dsh-emc-entry/` + `~/.dsh/profiles/`。commit 前缀 `PT-CB7(T{n}):`。EMC 仓基线 **425+2**（上浮注明）。

## 任务六件（各自独立 commit+push）

### T1（bug1）图层残留自清理 · EMC 仓

- 白名单：`frontend/js/render_client.js` + `tests/test_render_channel.py`（+1 用例）。
- 规格：`_apply(spec)` 在铺新层**之前**，调 `addToolboxLayer` 同源的图层管理接口，**移除所有名称以 `[dsh]` 开头的既有图层**（先 grep 图层管理真身：shared.js 的 addToolboxLayer 内部去重/移除逻辑照用；移除后同 spec 重铺不再叠层）；移除动作失败时 log 一行继续铺新层（不阻塞）。
- 验收：同会话两次 render_spec 后，地图上 `[dsh]` 前缀图层**仅剩最新一张**；测试用例断言（构造两层→apply 第二层→查询层列表只剩其一）；425+2+新增。

### T2（bug2）better-sidebar terminal chunk · dsh 环境

- 现象：`[dsh-better-sidebar] chunk "terminal": client module system unavailable`。
- 规格（决策树·按序执行）：①查 `~/.dsh/profiles/web/` 中 better-sidebar 版本（已知 0.12.2）与其 client chunk 构建时间戳 vs 宿主 `apps/web/dist` 构建时间——**怀疑与黑屏根因 B 同族（旧 chunk×新宿主运行时）**；②修法 A：`cd ~/.dsh/profiles/web && pnpm install` 重装对齐 + 若插件自带构建则重建 → 重启 web 验证 terminal 标签；③修法 B（A 失败）：查 better-sidebar 配置项禁用 terminal 标签（宿主内置终端够用·EMC 入口用 `ctx.workspaces.openPath` 已走内置）→ 验证其余标签正常；④落盘根因+修法（含 file:line/时间戳证据）。
- 注意：Qoder 已记 better-sidebar 0.12.2 monkey-patch 宿主 `workspaces.openPath`（interceptOpenPath 默认开）——若 A/B 仍不稳，评估升级/降级版本并**与 T3 联调确认**（openPath 被拦截会影响 T3 链路）。
- 验收：web 重启后 terminal 标签可用（或已显式禁用且无报错刷屏）；执行记录留证据。

### T3（bug3）EMC 入口改内嵌+就绪 gate · dsh 环境（dsh-emc-entry）

- 用户偏好裁定：**打开 EMC 页面以 dsh 内嵌（registerTab/iframe）为主**，不再外部 Edge 开图（取代当前 `openPath(http://localhost:8080/frontend/index.html)` 外部打开）。
- 规格：①保留左下角按钮+8080 探测+新会话+欢迎卡+start.bat 内置终端链（已验证段不动）；②改「开图」段：在 dsh-emc-entry 注册一个 Tab（iframe 指向 `http://localhost:8080/frontend/index.html`·以 better-sidebar 或其他既有 registerTab 机制的真身 API 为准——**先 grep 蓝本**）；③**就绪 gate**（治首开乱码·内嵌与外开通吃）：轮询条件从「8080 可达」升级为「`GET /api/v1/health` 200 **且** `GET /frontend/index.html` 200」后再打开 tab；若 iframe 内仍偶发半载，加一次 reload（onload 后延时 1.5s 检测 DOM 根节点·缺失则 reload 一次）；④外部 Edge 打开保留为 tab 上的「在新窗口打开」小按钮（兜底非默认）。
- 验收：首次点击入口（含 8080 冷启动场景）→ 内嵌 tab 完整加载无乱码、无手动刷新；8080 关着时按钮置灰不误开。

### T4（bug4-①）思维过长计时取证 · dsh 环境

- 规格：emc-test 与 web 两 profile 各跑一次同一简单题「12345 热线诉求最密集的 5 个社区是哪些？」（不开 render），用 session log/控制台计时分解：每 step 模型思考耗时/工具调用与耗时/总轮次/总时长；同时记录两 profile 各自生效的模型与 preset（settings/会话内可见）。
- 产出：计时分解表+两 profile 模型配置对照+根因初判（思考 token vs 工具 vs 轮次哪端占大头）——**只取证不改**（改法随 T6 与主手后续裁决）。
- 注意：web profile 里跑会污染日常会话记录——用新会话并在报告标注；emc-test 用其 flash/minimal 预设对照。

### T5（阶段二·灵魂）身份卡+人设+RAG 重建 · EMC 仓+dsh 环境

- EMC 仓白名单：`ai_qa/outlet_kb/urban_renewal_knowledge.py`（追加一条 FACTS 身份卡·不动存量）+ 文档记录。
- 身份卡内容（设计稿·dsh 按 FACTS 真身 schema 落）：**EMC 是谁**（情绪地图·城市情绪空间分析平台的分析助手）；**能做什么**（七件标准工具：知识检索/事实卡/数据清单/出口卡/单元聚合/缓冲/排序 + 出图渲染·口径引用义务）；**不能做什么**（不编造数字·结论层(usage=analysis_output)禁作分析输入·宏观倾向≠微观诊断·不碰 EMC 前端 QA 管线）；**口径纪律**（引用带 caliber/口径注册表卡 ID·社区口径枚举按 K-C1）。city='宜昌'·topic 用既有体系（grep 真身后定）。
- dsh 环境：web profile 会话预设加 EMC 人设 system prompt（一段·中文·与身份卡同源：你是 EmotionMap Copilot·能力边界/口径义务/工具偏好——先 list_data 再操作、结果带口径、不编造）；emc-test 同加。
- 验收：`py tools/rag_index.py --build` 重建后 `kb_facts(keyword='EMC')`（或同义查询）能命中身份卡；新会话欢迎后自述身份与边界。

### T6（bug4-②）MCP 工具描述紧凑化 · EMC 仓

- 白名单：`tools/mcp_server_emc.py` + `tests/test_mcp_server_emc.py`（若描述断言存在需同步）。
- 规格：八工具的**描述/docstring 正文**压缩为 ≤3 行结构：一句用途+参数要点（必填与关键枚举·不逐参列举）+限制一句（超时/体积/冷启动合并成一句）；**caliber 与返回结构零变化**；description 里删除大段 `when` 式长文（MCP 侧专用·与 EMC 内部 FC 的 contracts 长描述分道·零 diagnose prompt 接触）。
- 验收：`build_server().list_tools()` 各描述 ≤~200 字符；五判据逐工具答辩仍成立（写入执行记录）；425+2 全绿。

## 验收总锚

T1/T3/T5/T6 各自 commit+push（EMC 仓）；T2/T4 落盘报告。浏览器实测清单（用户参与）：入口首开完整内嵌无乱码 / 同会话两次出图仅剩最新 [dsh] 层 / terminal 不报错 / 新会话自述 EMC 身份。完成后告知用户，主手回收。

## 纪律

EMC 仓外（dsh 环境）改动只落执行记录；grep 真身后再动·签名不符停手；A9 禁宽 except 静默；报时查钟；**dsh 上游冻结期内禁 merge**（Qoder D2·已裁定）。


---

## 增补（2026-08-21·合并讨论纪要后·总纲=「让不需要思维的任务不进思维循环」）

### T7 双模预设落地（dsh 环境）

- `emc-analysis`（默认档）：flash + effort **low/medium** + 工具子集（G10 八件·pwsh/editor 只读受限或移除）+ step 预算 ≤15；
- `emc-research`（显式档）：effort max 保留 + 全工具 + 无 step 预算；
- 升级三信号（机械规则·禁 AI 分类器）：显式词（研究/调研/探索/对比方案/系统评估/为什么）→研究档；分析档硬阻塞→停下问一次；任务模板化后分析=选模板直跑。
- 落盘：双 profile 配置+使用说明（哪档默认、怎么切）。

### T8 800m 脚本参数化模板 + 入库（EMC 仓）

- 白名单：`SCRIPT/gen_12345_grid_800m.py`（改造·grid_size/source/region_caliber 三参数 CLI）+ `SCRIPT/gen_task_report.py`（会话路径参数化）+ git 入库提交。
- 输出文件带「口径对照」段（本结果 vs 全量 49,192 vs 注册表 K-01/K-02 + 落点率）。
- 验收：三参数跑通（复用原 800m 参数复现同数字）；pytest 零影响（脚本不在测试面）。

### T9 交付模板「口径对照」段固化（并入 T5 同批）

- 身份卡纪律同批加一条：**所有数据类交付必带口径对照段**（口径卡 ID + 子集声明 + 覆盖说明）。

### F1-F4 调研并入（dsh 环境·随 T2 同批报告）

- F1 dsh per-session/per-step effort 覆写支持与否；F2 已裁=模板先行；F3=观察项；F4 dsh context 压缩/摘要机制可用性——四问结论并入执行记录。

**验收锚（增补）**：`dsh --profile emc-analysis` 一次简单聚合任务 wall-time 目标 ≤2 分钟（同"最密集 10 社区"类任务实测）。
