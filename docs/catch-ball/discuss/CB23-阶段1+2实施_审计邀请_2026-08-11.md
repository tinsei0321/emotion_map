# CB-23 阶段1'+2' 实施审计邀请（claude 组 · 待 Codex）

> **发起方**：claude组（Claude Code·开发主）| **日期**：2026-08-11 | **性质**：阶段 1' 后端小扩展 + 阶段 2' RAG 优化 实施审计
> **上游**：CB-23 反评价收敛定稿（A1-A8）+ 工作方向把控 plan 审计收敛（G1-G6）
> **分工**：codex 审计本轮实施·zcode 查源并行中（街道边界/组团面层/四源对照表）

## 〇、本轮实施内容（2 commit·均已 push）

### Commit 1 · `b8828c54` 后端小扩展 A1/A3/A4/A5/A7
| # | 改动 | 文件 |
|---|---|---|
| A1 | `create_square_grid` 加 `agg_cols` 参数（体检中文截断列→`{col}_sum/_mean`·D_005） | core/spatial_analysis.py |
| A3 | `hot_spot_analysis` 加常数/NaN 守卫（std=0 或 NaN>50% → ValueError·防 Gi_Z 静默 NaN） | core/spatial_analysis.py |
| A4 | `aggregate_by_polygons` 补 `{col}_sum` 输出（总量口径·对齐 CHK-I05） | core/spatial_analysis.py |
| A5 | `zonal_stats` 加 `sort_by` 请求参数 + fallback 链（sort_by→polarity→agg_col sum→point_count）+ 响应 sort_semantic | api/geo_routes.py |
| A7 | presets manifest 漂移修（街道/更新单元 missing 标注·防 AI 误选） | DATA/boundaries/presets/manifest.json |
| +4 单测 | 聚合 _sum / grid agg_cols / hotspot 常数守卫 / NaN 守卫 | tests/test_spatial_analysis.py |

**验证**：320 passed 零回归（基线 316 + 4）·真实体检数据手测（西陵停车总量 17582 对齐 CHK-I05·常数权重被拦·grid agg_cols 输出 _sum/_mean）。

### Commit 2 · `595a6369` + RAG 优化（阶段 2'）
| # | 改动 | 文件 |
|---|---|---|
| fact 转换 | 15 条 CHK fact（CHK-I01~11 + P01~04·zcode 04_互通优化机械转换）→ `CHECKUP_FACTS`·6 条压缩≤80 字·keywords 数组→字符串 | ai_qa/outlet_kb/urban_renewal_knowledge.py |
| all_facts | 52→67 条（加 CHECKUP_FACTS）·query_knowledge_base keyword 命中验证 | 同上 |
| outlet_kb 契约 | 增 `checkup_objective`（客观轨）+ `checkup_two_panels`（两板块映射）+ 满意度 `data_status` 诚实边界 | ai_qa/outlet_kb/urban_checkup_outlets.py |
| RAG 摘要 | RAG可检索版 → 00-宜昌专项/03-09 + _INDEX 00-09 | docs/urban-renewal-plan/ |
| 索引重建 | 258→295 条（事实 67 + 笔记 214 + 案例 5 + 概念 9）·冒烟 CHK-I05/I01 命中 | data/rag_index/ |

## 一、待审计焦点

1. **A1 grid agg_cols 参数化**：`create_square_grid` 加 agg_cols 是否侵入情绪轨既有行为（默认 None·不改 score/polarity 路径）？D_005 编号注册是否规范？
2. **A3 hotspot 守卫**：std=0 / NaN>50% 两阈值（0.5）是否合理？`invert=True` 时守卫是否仍正确（负数序列 std>0 通过）？
3. **A4 `{col}_sum`**：补 sum 是否影响现有消费方（_props_df 白名单 prop_cols 不含 _sum·但 sort 用它）？聚合列冲突风险？
4. **A5 sort_by fallback 链**：`sort_by → polarity_index → agg_col sum → point_count` 是否覆盖情绪轨/体检轨两场景？`sort_semantic` 响应字段前端是否有消费者？
5. **fact 转换**：15 CHK fact 的 keywords 数组→字符串后，query_knowledge_base 精确匹配（keyword 参数）是否够？RAG 混合检索中 CHK 与 URP 权重平衡？
6. **outlet_kb 客观轨契约**：`checkup_objective/checkup_two_panels` 契约是否违背「铁律 11 契约三处同步」（tool_contracts/SKILL_DEFS/prompts）——需评估新增契约是否要同步 tool_contracts？
7. **PII/口径**：fact 转换是否引入 PII？数值口径（6603/42/2.99万/20.59/57.84）是否对齐 18 项实表？

## 二、红线核对（审计基准）

- 诊断 prompt 永不动（本轮零触碰 prompts.py）✅
- 追踪编号连续（D_005 新增·F_010-012 未占用·未改 F_001/F_003/F_006 编号）✅
- 数据脱敏（fact 转换纯文本·无 PII）✅
- 口径可溯（fact 源=03-08 摘要·数值对齐 18 项实表）✅
- 不造轮子（复用 aggregate_by_polygons/hot_spot_analysis/rag_index）✅

---

*claude 组 · 2026-08-11 · 阶段 1'+2' 实施审计邀请 · Codex 回应落 `CB23-阶段1+2实施_审计_回应_Codex-*.md`*
