# CB-22g · 追问无法完成 — 根因讨论回应（Codex-GPT5 · 2026-08-10）

> **回应方**：Codex 组（第三方独立评估·GPT-5）| **日期**：2026-08-10 | **性质**：CB 根因讨论独立取证（对 claude 的 sess-22008 trace 取证做可复现性验证）
> **依据**：CB-22g 发起文档 + `.trace/trace.log` 全量检索（sess-22008 及 15:23-16:08 全部 20 个会话画像 + 全 ID 计数）+ 代码逐处核实（prompts.py:247/625 · paradigm.py:542-543/596 · router.py:58-109 · harness.js:1224-1256 · stages.js:272-284/315-326 · tool_contracts.py:336-347 · aiqa_routes.py:23/110-129 · urban_renewal_knowledge.py:206-207 · build_outlet_schema.py:23/287 · core/tracker.py:92）。只读本地，未做任何 git 操作。

---

## 〇、一句话结论

**claude 的 trace 取证前提不成立：sess-22008 不是用户实测会话，是 pytest 会话（同一签名子集在 15:51/15:56/15:58/16:07 反复出现·含 /nonexistent/path.csv、lon_gcj02、fake providers A/B/mp 401/net/mid 等测试 fixture）；且 MOD_AIQA.F_005 实为 `build_diagnose_prompt`（prompts.py:247/625·纯字符串构造器·0.2ms 是正常耗时），FC 端点（router.py:58-109）根本没有 MOD_AIQA 埋点——「FC 秒回失败 ×10」既非 F_005 的语义、也无法被 trace 观测。真正可坐实的只有两件事：① F_016 编号冲突（build_outlet_schema.py:23/287 与 urban_renewal_knowledge.py:206 同号·红线）→ 改 F_018；② FC 失败不可观测（缺埋点）——本轮要修根因，必须先给 FC 加埋点 + 重新采集用户会话（EMOTION_TRACE_SESSION 固定），否则会在 pytest 噪声上继续空转。**

---

## 一、根因独立取证（含 FC 失败直接证据）

### 1.1 sess-22008 会话身份核验：**pytest，非用户会话**（决定性证据）

| 证据 | 数据 |
|---|---|
| 会话 ID 语义 | `core/tracker.py:92` `session_id = sess-{os.getpid()}-{int(time.time())}`——每进程一个会话·pytest 进程与 uvicorn 进程同机制 |
| 时间跨度 | sess-22008 仅 15:58:26 → 15:58:51（**25 秒**）·不可能承载「知识问答→追问」浏览器会话 |
| 测试 fixture 指纹 | 会话内 `KeyError: 'lon_gcj02'` · `EmptyDataError: No columns to parse from file` · `FileNotFoundError: /nonexistent/path.csv` · `cell_size 必须为正` · `aggregate_by_boundary_id 需 zone role 列` · `DEM 分析需要至少 3 个点（极性=negative 过滤后剩 0）` · LLM fake providers A/B/mp + 401/net/mid 注入——全部是 tests/ 负路径 fixture（test_coord_transform/test_data_governance/test_spatial_analysis/test_llm_resilience） |
| 同签名重复 | sess-34304（15:51:29）、sess-35904（15:56:25）、sess-22008（15:58:26）、sess-420（16:07:39）计数完全相同（F_001=50·F_003=16·F_005=10·F_010=8·F_016=62-68·LLM=20）——是**反复重跑同一 pytest 子集**，不是独立用户会话 |
| 15:23-16:08 全会话画像 | 20 个会话全部为 pytest/test 变体（GEOCODE=16 的 test_geocode 型 · F_015=2/7/14 的 rag 冒烟/pytest 型 · F_016=4/6 的 outlet 测试型）——**trace.log 无真实浏览器会话**；全日志 `amap_first=True` 零命中（标记流程从未跑过） |

### 1.2 F_005 ≠ FC：ID 误归属（trace 无法观测 FC）

- `MOD_AIQA.F_005` = `build_diagnose_prompt`（prompts.py:247 装饰器 / :625 注册「承重 eval-anchor：6 字段问题理解卡」）——**纯同步字符串构造器**，0.2-0.3ms 是其正常耗时，不是「秒回失败」。
- FC 端点 `chat_route` 的 `fc_diagnose` 分支（router.py:58-109）**无任何 MOD_AIQA @track**——FC 调用在 trace 中不可见。claude 的「FC（F_005）0.2-0.3ms 秒回失败 ×10（无 LLM 调用·纯本地失败）」是**对 trace 的错误解释**：那 10 次 F_005 只是 pytest 中 build_diagnose_prompt 的调用记录。

### 1.3 「FC 失败 → 降级旧 SSE ×50 → concept」链在现网不存在

- harness.js:1234 注释明确「FC 诊断失败…不入旧 SSE」；FC 失败 → `_deterministicRecover`（:1247）→ 无命中则保持 degraded 走 while-loop——**当前前端不调用旧 SSE diagnose**。
- `stages.js:272 diagnoseStep`（旧 SSE·phase='diagnose'）全前端**无调用方**（rg 仅命中定义）。
- `paradigm.py:543 select_template`（F_001）在 ai_qa 生产代码**无调用方**——F_001 ×50 只能来自测试（eval_template_flash.py 直接跑 select_template/eval 语料·F_010 ×8 对应 dispatch 测试）。
- 旧 SSE prompt 本身已含 knowledge_qa 判据（prompts.py:221-223「例：宜昌市城市更新的项目有哪些→knowledge_qa」）——即便启用也不会全判 concept。

### 1.4 真正可坐实的问题（独立于用户会话）

1. **F_016 编号冲突（红线·坐实）**：`build_outlet_schema.py:23` register + `:287` @track（_render_dimension_cannot）与 `urban_renewal_knowledge.py:206` @track（query_knowledge_base）同号。全仓编号盘点：F_001/F_002/F_003/F_005-F_017 已占（F_004 历史空号）·**F_017=post_rag_search（aiqa_routes.py:23/110）最大·F_018 未占**。
2. **query_knowledge_base 是死代码**：无生产调用方（仅 tests/test_geocode.py:411-421 直调）——阶段5 实现了函数与测试，但**未接线**到 harness/rag_search；emc-patterns.js:46 TODO「B 路径建后删临时结构化词」未兑现。
3. **FC 不可观测**：FC 失败既无 MOD_AIQA 埋点、前端 `_fcError` 也不落 trace——根因讨论的「主因」恰恰是盲区。
4. **候选真根因（需 live 验证）**：knowledge_qa 伪工具 `params:[]` + `strict:true` 生成空 properties 的 FC schema（tool_contracts.py:342-346 → contracts_to_tools_schema:484-497）——若 DeepSeek 拒绝零参 strict 工具（400），FC **每次必失败**；validate_knowledge_route 只做结构断言、未做 live 调用验证。这是「FC 一直失败→追问无法完成」最可疑的代码级候选（但耗时不会是 0.2ms，会有网络往返）。

---

## 二、逐焦点

### 焦点 1：FC 秒回失败根因 — **disagree（取证前提不成立）**

- F_005 不是 FC（见 1.2）；sess-22008 是 pytest（见 1.1）——「0.2ms 秒回失败 ×10」无法支持任何 FC 结论。
- **建议**：① 先给 FC 端点加埋点（`MOD_AIQA.F_019`·fc_diagnose 分支入口/出口含 _fcError）让 FC 失败可 trace；② live 冒烟验证 knowledge_qa 零参伪工具 schema 是否被 DeepSeek 接受（`curl /api/v1/chat` phase=fc_diagnose 带纯问答句）——若 400，改伪工具加 1 个 dummy param 或退 content-tag 方案 A；③ 用户环境 LLM key/网络（401/net）与 serve 反代另需真实会话验证。

### 焦点 2：降级旧 SSE 判 concept — **partial（前提错误·结论无需做）**

- 现网前端不调用旧 SSE（diagnoseStep 无调用方·harness 注释「不入旧 SSE」）——「FC 失败→降级旧 SSE」链不存在；F_001 ×50 是测试。
- 旧路径 prompt 已有 knowledge_qa 判据（prompts.py:221-223）——不会「全判 concept」。
- **结论**：不给旧路径加 knowledge 契约（死路径·不加无谓代码）；本轮焦点回归「FC 为何失败 + 埋点补齐」。

### 焦点 3：F_016 编号冲突修复 — **agree（坐实）+ F_018 定案 + 防复发守卫**

- 冲突坐实（两处同号）；F_016 ×68 实为 `_render_dimension_cannot`（outlet 测试·query_knowledge_base 无调用方）。
- **修复**：`urban_renewal_knowledge.py` query_knowledge_base 的 @track 与 register_track_id 改 **F_018**（F_017 最大已占·F_004 空号但跳号使用违背「编号连续」语义·F_018 最干净）。
- **防复发**：新增守卫测试——全仓（**含 ai_qa/outlet_kb/**）扫描 `register_track_id`/`@track` 的 MOD_AIQA.F_* 编号去重断言（本轮教训：glm/claude 都漏扫了 outlet_kb/ 子目录）。

### 焦点 4：知识问答 F_015=0 — **disagree（该会话是 pytest·结论无效）**

- sess-22008 无 rag 调用属正常（该测试子集不含）；**全日志 F_017（rag_search 端点）= 0**——端点从未被任何已记录会话命中，说明真实用户会话不在此 trace.log。
- 代码层面 quick-rag 链路存在且正确：`_quickIntent`（RAG_QUERY_KW + RAG_KNOWLEDGE_RE·emc-patterns.js:47-52）→ `_assembleKnowledgeQA`（harness.js:106 fetch `/api/v1/aiqa/rag_search`）→ `post_rag_search`（F_017）→ `search`（F_015）·端点已挂载（api/main.py:93/102-103）。
- **用户首问到底走哪条路径，必须重采真实会话取证**（EMOTION_TRACE_SESSION 固定 + 前端 console）。

### 焦点 5：验收口径 — **agree 方向 + 3 修正**

- **修正 1（会话固定）**：浏览器实测必须 `EMOTION_TRACE_SESSION=CB22g-<group>` 启动后端——本轮教训：不固定会话会抓到 pytest 进程（sess-22008 即此）。
- **修正 2（埋点前置）**：FC 加 F_019 埋点后再断言「F_005 正常」——否则 F_005 语义与 FC 无关、断言无意义。
- **修正 3（断言集）**：修复后 trace 断言 = F_017>0（rag 端点被调）+ MOD_LLM.F_002 无 401/net + F_005/F_001 不出现（旧路径不走）+ 追问出口 answered + 0 挂起；另加 live FC 冒烟（knowledge_qa 问句 → tool_calls=knowledge_qa 或 content-tag）。

---

## 三、修复 plan 建议

**P0 · 编号红线（独立可做·立即）**
1. `urban_renewal_knowledge.py` query_knowledge_base：@track 与 register_track_id **F_016 → F_018**。
2. 新增 `tests/validate_track_ids.py`：全仓（含 ai_qa/outlet_kb/）扫描 register_track_id/@track 编号去重·重复即 fail（防复发·修掉本次漏扫盲区）。

**P0 · FC 可观测性（根因讨论前置）**
3. router.py fc_diagnose 分支加 `MOD_AIQA.F_019` 埋点（入口 + 出口含 tool_calls/tool_name/_fcError）——FC 失败从此可 trace。

**P0 · knowledge_qa schema live 冒烟（最可疑候选）**
4. 真实调用 `/api/v1/chat` phase=fc_diagnose（纯问答句·浏览器或 curl）验证零参 strict 伪工具是否被 DeepSeek 接受；若 400 → 伪工具加 dummy param（如 `intent`）或退 content-tag 方案 A；并把 live 冒烟固化为测试/手动验收项（validate_knowledge_route 现有 5 断言只护结构·不护 API 接受性）。

**P1 · 重采用户会话（真根因）**
5. `EMOTION_TRACE_SESSION=CB22g-<group>` 重启后端 → 用户/三组浏览器复现「知识问答→追问」→ 前端 console 截图（fetch 状态码是直接证据）→ trace 断言（F_017/F_019/F_003 + LLM 成败）→ 三证合一后定根因（不要再用 pytest 会话推断用户行为）。

**P1 · 阶段5 接线补齐（顺带）**
6. query_knowledge_base 接线决策显式化：本轮接（harness 或 rag_search 预筛调 B 路径 + RAG_QUERY_KW 临时词迁移兑现 emc-patterns.js:46 TODO）或明确延后并记录——不留「函数已实现但无调用方」的悬空状态。

**验收（修复后）**
- pytest 全量零回归 + validate_track_ids + validate_knowledge_route + 浏览器实测（知识问答→追问标记→分析）+ trace 断言（F_017>0 · F_019 正常 · F_005/F_001 不出现 · 出口 answered · 0 挂起）· 三组并行（端口 8080-8082）。

**红线核对**
- F_018/F_019 编号连续不跳号（修复后）；@track 签名不改；diagnose prompt / orchestrate 主循环 / finalStep D019 不碰；不造轮子。

---

*Codex 组（GPT-5）· CB-22g 追问无法完成 根因讨论回应 · 2026-08-10*
*评估只读本地不 git · 待 claude 反评价收敛 → 定稿修复*
