# PT-CB2 · T3 执行记录（zcode·2026-08-19）

> 执行：zcode（主手）。日期：2026-08-19。分支：`EMC_harness_dsh`。
> 任务：B1 验收包三件——manifest schema 校验脚本 + 前端范围选择冒烟 + 全量 pytest 阶段基线复核。
> 白名单：`tools/check_manifest.py`（新）+ 本执行记录。

---

## T3a · manifest schema 一次性校验脚本 — ✅

- `tools/check_manifest.py`：JSON 可解析 / 顶层结构 / id 唯一 / 必填字段（ERR 级）+ usage 值域覆盖 / 文件路径存在性 / nameField（WARN 级）。exit 1 on ERR（可挂 CI）。
- 实测：**0 err / 2 warn**（`admin_community`、`renewal_unit` 文件缺失——T1 已知存量项，B2 版本对账批次处理）。
- **设计修正记录**：首版含「snapshot=true 应为 analysis_output」一致性校验，实测 4 误报后移除——snapshot（物理快照视图·防双头权威）与 usage（原料/结论语义）为**正交维度**，input 底座同时是快照是 T1 合法设计。修正已注明在脚本头。

## T3b · 前端范围选择冒烟 — ✅（三链路验证）

1. **DOM 渲染**：浏览器实开页面（build 807a789f），Range 面板容器渲染 T1 三个新组全部在场——「城市体检底座」「12345 主观轨（点层）」「page7 结论层（点版）」，关键按钮（174 快照/双高点/安全韧性/18村 等 7 关键词）全部命中。
2. **数据链路**：preset 加载接口直验——page7-双高点 714 要素、174 聚合快照 174 要素（口径正确）。
3. **守卫实战**（真实服务端到端）：
   - 拒绝：`zonal_stats(boundary=base_174_aggregate_area)` → 400 三段式文案完整返回（层名/铁律7/同组替代）；
   - 放行：`zonal_stats(boundary=admin_district)` → 200 正常聚合。
4. **遗留（轻）**：UI 视觉点击链路未走完——左栏为 chip 唤出式抽屉（CSS display:none），自动化 force click 超时；DOM+API 双链路已证功能完整，视觉点击留用户日常使用确认。

### ⚠ R7 事件记录（入档·验证纪律）

首次实测时**真实服务未拦截**（返回 200）。排查：后端 8000 端口被今晨 10:14 启动的旧进程占用（PID 1628·载旧代码），新 serve.py 后端绑定失败（[Errno 10048]）而前端 8080 静态文件可响应——形成「前端读磁盘新文件（build 号新）+ 后端跑内存旧代码（无守卫）」的假象。处置：杀三个相关进程（旧后端 1628 / 旧前端 18340 / 半启动新服务 11556）→ 重启 → 拦截恢复正常。

**教训（补充 R7）**：验证后端行为前必须确认服务进程身份（netstat 查占用 + 进程启动时间 vs 代码提交时间）；起验证服务前先清端口；「页面 build 号是新的」不构成「后端是新代码」的证据（静态文件与内存代码不同源）。

## T3c · 全量 pytest 阶段基线复核 — ✅

`py -m pytest tests/ -q`：**387 passed + 3 skipped**（T2 后基线复核一致，无波动）。

---

## 验收结论

B1 验收三件全过：schema 校验 0 错误（存量 2 警告有主）；新按钮渲染+数据链路+守卫实战三验通过；基线 387+3 稳定。**PT-CB2（T1+T2+T3）完工，送审包就绪**：

| 送审件 | 文档 |
|---|---|
| T1 执行+四裁决 | `PT-CB2-T1执行记录_dsh-2026-08-19.md` |
| T2 执行+两开放裁决 | `PT-CB2-T2执行记录_zcode-2026-08-19.md` |
| T3 验收包（本件） | `PT-CB2-T3执行记录_zcode-2026-08-19.md` |
| 设计依据 | `CB42-dsh精髓学习_zcode回应与调优_2026-08-19.md`（守卫四态→T2 设计出处） |

审计要点建议（供 Codex）：①两开放裁决点（全参数位拦截 / 多步链放行）语义合理性；②通道 B（已加载结论层转匿名 fc）拦截完备性——presetId 标记覆盖 4 处 preset 加载点；③指路精度边界（同组替代 vs 跨组配置头的映射留 B3）；④R7 补充纪律是否入 debug-memory。
