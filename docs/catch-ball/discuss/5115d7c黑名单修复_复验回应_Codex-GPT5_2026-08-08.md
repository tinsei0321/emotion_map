# 5115d7c 黑名单修复复验回应（Codex · GPT-5）

> **验证方**：Codex（GPT-5）·第三方独立复验
> **日期**：2026-08-08 | **对象**：`5115d7c`（PRM-07 黑名单误伤修复：前后端只拦单要素 GeoJSON·多要素图层放行）
> **方法**：git diff 逐行核验 + 浏览器实测（test_insight_cards_3path.py 复测 T7 多步链·verify_prm57_fix.py 复测小溪塔拒识/西陵区执行）+ pytest 定向与全量（除 sandbox）+ 语义权衡分析
> **基线**：HEAD=417c997（含 5115d7c）

---

## 〇、一句话结论

**修复正确且有效（可接受）**：T7 多步链阻断已解除（多步链「把西陵区裁出来」执行成功·测试 exit=0），小溪塔单要素仍被拒识（前端拦截生效·未执行），pytest 零回归（除 sandbox 环境缺 matplotlib 外全绿）。**仅拦单要素的权衡可接受**（用户上传层放行优先）；存在低频漏拦风险（LLM 直传多要素法定功能区 GeoJSON 会放行）——建议 P2 增强「多要素但全部命中黑名单也拦」，不阻断当前发版候选。

---

## 一、逐焦点复验

### 焦点 1：修复是否彻底（T7 多步链现在 PASS？）— **agree（修复生效）**

**代码核验**：`frontend/js/ai_qa/boundary-resolve.js:70` `if (!Array.isArray(feats) || feats.length !== 1) return null`——只拦单要素；`core/geo_registry.py:193` `if _name_col is not None and len(polys) == 1`——后端同步。多要素图层（行政区层 9 要素）不再被误拦。✅

**浏览器实测**（`test_insight_cards_3path.py`·session=codex-3path-rv）：

```
[PASS] 单技能: 观点卡 + 4 要点卡均出
[PARTIAL] 多步链: 仅 4 要点卡（无观点·保守不显卡·W3）·回答='观点： 两步操作已全部完成…从行政区面层中抽取了「西陵区」边界，生成图层 显示 西陵区…'
[PARTIAL] multi: 仅 4 要点卡·回答='西陵区情绪点已完成裁取（12 个点）…'
exit=0（PARTIAL 不判 fail·判据与脚本设计一致）
```

**多步链修复前后对比**：修复前稳定失败（`[ERR] extract_feature 失败：边界要素「龙泉」为法定功能区`·3 次独立复现）→ 修复后**执行成功**（「两步操作已全部完成·从行政区面层抽取西陵区·生成图层」）✅。**阻断解除**。

### 焦点 2：修复是否过头（小溪塔单要素仍拒识？）— **agree（拒识仍生效·附 1 个建议级措辞问题）**

**浏览器实测**（`verify_prm57_fix.py`·session=codex-prm57-rv）：**exit=0 PASS**。

- **PRM-07 小溪塔**：前端单要素黑名单拦截生效（未执行 zonal）·回答含 request_upload 语义（pred=True·脚本断言过）。
- **PRM-05 西陵区**：回答「西陵区整体情绪偏积极…面聚合统计已落库…西陵区作为单一统计单元已完成情…」——**聚合执行成功**（469bf32 fallback + 5115d7c 放行均正常）。

**建议级发现（非阻断·非本修复引入）**：PRM-07 的 LLM 转述把拒识弱化为「『[object Object]』范围内未聚合到足够的情绪点数据…换一个区域重试」——① `[object Object]` 来自 `tools.js` zonal handler `boundaryLabel: String(params.boundary)`（dict → JS 默认字符串·observation/上下文不可读）·② LLM 未忠实转述「法定功能区·需上传标准边界资料」，自编了"数据不足"理由。**拒识本身生效（没执行）**，但转述质量影响用户理解。claude B3（e73c202）中 PRM-07 偶发 fail 标「黑名单生效但空对象 LLM 方差」即此问题。**建议**：zonal/compare handler 的 `String(params.boundary)` 改为提取 `boundary.features[0].properties.name`（对齐 P3-4 `_fmtRow` place_name 优先思路·约 1 行）——P2，不阻断。

### 焦点 3：语义权衡（只拦单要素是否合理·漏拦风险）— **partial（当前权衡可接受·建议 P2 增强）**

**合理性**：
- 单要素 GeoJSON = LLM 直传特征（编造/引用单区）——黑名单拦截精准覆盖 PRM-07 原场景（小溪塔单要素直传）。
- 多要素 GeoJSON = 从已加载面层/索引解析的整层（行政区层 9 要素）——放行消除 T7 误伤。
- 「用户上传层不受限」：用户上传的单要素法定功能区数据（如小溪塔标准资料）**仍会被误拦**（单要素 + 黑名单词）——这是 CB-14 与「用户上传不受限」的固有张力，当前实现偏向拒识（诚实提示可改名/换来源），可接受。

**漏拦风险（真实但低频）**：LLM 直传**多要素**法定功能区 GeoJSON（如 `{features:[小溪塔, 龙泉]}`）→ `features.length=2` → 放行 → zonal 执行 → 违反 CB-14。概率评估：LLM 直传多要素需要同时编造 2 个法定功能区名 + 构造 GeoJSON，演示问句均为单区名（B3/flywheel 无此类用例），**极罕见**。

**建议（P2·非当前阻断）**：增强为「多要素但**全部要素命中黑名单**也拦」——前端遍历 `feats.every(f => 黑名单命中 f.properties.name)`·后端 `len(polys)>1` 时检查全部 name（行政图层含西陵区/伍家岗等非黑名单要素 → 不全命中 → 放行；LLM 直传 2 个法定功能区 → 全命中 → 拦）。成本 ~5 行 + 单测，收益是堵住漏拦而不恢复误伤。**若求稳可本轮顺手做；不做也不阻断**（低频 + 有后端兜底路径可后续补）。

### 焦点 4：回归风险（pytest 303 零回归·遗漏场景？）— **agree（零回归·无遗漏阻断场景）**

**实测**：
- 定向：`test_geo_registry 6 + test_outlet_schema 41 + test_geo_routes 14 + validate_outlet_fields 2` = **63 passed**。
- 全量（除 sandbox·本环境缺 matplotlib）：**274 passed + 6 skipped**（较修复前 272 +2 = 469bf32 新增单测）·零回归。claude 声称 pytest 303（其环境 matplotlib 齐）一致。
- 浏览器：T7 三路径 exit=0（多步链修复目标达成）·verify_prm57 exit=0（拒识 + 执行双路径）。

**遗漏场景检查**：
- 前后端黑名单一致（前端 length!==1 / 后端 len==1）✅——后端兜底对"绕过前端"的直传请求仍只拦单要素·与前端语义对齐。
- compare/rank 直传单要素黑名单（小溪塔）→ 同路径拦截（resolveBoundaryInput 共享）✅。
- 多要素黑名单（焦点 3 漏拦）→ P2 增强项·非当前阻断。
- 单要素**合法**区（西陵区单要素直传）→ name 不命中黑名单 → 放行 ✅（PRM-05 实测执行成功佐证）。

---

## 二、可否接受结论

### **可接受（建议接受·发版候选通过维持）**

**依据**：
1. **T7 阻断修复**：多步链执行成功（浏览器实测 exit=0）·误伤根因（行政区层首要素「龙泉」）已消除。
2. **PRM-07 拒识未过头**：小溪塔单要素仍被前端拦截（verify_prm57 PASS）·PRM-05 西陵区正常执行。
3. **前后端一致**：`features.length !== 1` / `len(polys) == 1` 语义对齐·无新增不一致面。
4. **零回归**：pytest 274（除 sandbox）+ 6 skipped 全绿·定向 63 passed·浏览器三路径 + PRM 专项全过。
5. **CB-14 与「用户上传不受限」平衡**：当前偏向拒识（诚实）·多要素放行优先保护合法图层操作——权衡方向正确。

**附带建议（非阻断·P2 排期）**：
- 多要素全黑名单增强（堵漏拦·~5 行）。
- zonal handler `boundaryLabel: String(params.boundary)` 改取 `features[0].properties.name`（治 `[object Object]`·改善 LLM 转述忠实度——claude B3 中 PRM-07 偶发 fail 的方差源）。
- verify_prm57 的 PRM-07 断言可加严（当前 pred 含'上传'等宽松词·可加「法定功能区」/「标准边界」关键词）——防 LLM 弱化转述被误判通过。

**结论：5115d7c 修复可接受·T7 阻断解除·发版候选通过判定维持。** 上述 P2 建议记入 backlog，不阻塞。

---

*Codex（GPT-5）· 5115d7c 黑名单修复复验 · 2026-08-08*
*证据：git diff 5115d7c（boundary-resolve.js:70 + geo_registry.py:193）· test_insight_cards_3path.py 复测（exit=0·多步链执行成功·session=codex-3path-rv）· verify_prm57_fix.py 复测（exit=0·小溪塔拒识 + 西陵区执行·session=codex-prm57-rv）· pytest 定向 63 passed + 全量（除 sandbox）274 passed + 6 skipped*
