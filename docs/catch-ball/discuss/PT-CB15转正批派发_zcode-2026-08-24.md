# PT-CB15 转正批 · 修复派发单（Qoder 执行·zcode 收敛）

> 依据：spike 三方审计收敛（`PT-CB15-SPIKE三方收敛_zcode-2026-08-24.md`）处置表。执行=Qoder·commit 前缀 `PT-CB15(PROMOTE):`·分支 **`EMC_Codex_Harness`**·零 pull 零 push。
> 门禁基线 **579+4**·rag_eval 96.7% 零退化红线。产出含白话摘要段。

## P1 必修（1 件）

### P1-1 看门狗「有流量即续命」（Z-01·zcode 独立审计）

- 位置：`core/codex_bridge.py` ask() 主循环。
- 修法：预算检查移到**每行处理后**（主循环内）——`while not completed` 循环体尾部加 `if time.time() - t0 > budget: yield error CODEX_TURN_TIMEOUT; return`·与静默超时分支同收口（保留 15s 心跳 ping 逻辑不变）。
- 测试：mock 高频行流（每 10s 吐无关行）→ 断言 300s 预算仍触发（可缩短 budget 注入测试值）。

## P2 限期修（11 件）

| # | 项 | 修法 |
|---|---|---|
| P2-1 | tool begin/end 配对断裂（Z-02） | bridge item/started 与 item/completed 透传 `item.id` → 前端 end 复用同 id 生成 toolcall_id |
| P2-2 | `_SPIKE_CWD` 硬编码（三组全抓） | `{REPO}` 同级推导（如 `Path(__file__).parents[1]/'_codex_cwd'`·仓外仍隔离）·复刻清单登记 |
| P2-3 | npm vendor 路径硬编码（Z-04） | 多候选探测：which→APPDATA npm→npm root -g 命令·全败 fail-closed（已有） |
| P2-4 | model 配置硬编码（B-4） | `-c model_provider/model` 改环境变量读取（`CODEX_MODEL_PROVIDER`/`CODEX_MODEL`·默认 deepseek+deepseek-chat·用户令 08-24）·复刻清单 |
| P2-5 | stderr DEVNULL（Z-05/B-5） | 环形缓冲（末 4KB·简单 deque）·error 事件携带 tail |
| P2-6 | seq 缺口检测（Z-06） | 前端 n 连续性检查·跳号按 error（wire 补 message） |
| P2-7 | 零测试补测（claude） | 按 C9-C12 口径最小集：桥解析单测（JSONL 半行/坏行/超长行·monkeypatch subprocess）+ SSE 帧分隔兼容 + 端点错误路径——**SSE 解析层离线可测·优先** |
| P2-8 | map.js 样式竞态（C-1·既有 bug） | styledata 未就绪时挂 retry/failed 告警（前端小件·白名单内） |
| P2-9 | inbox 竞争架构修（C-4） | 实例锁字段（spec 带 server 实例 id·watcher 只消费自家）或消费后扇出——**选最轻量方案**·评审注记 |
| P2-10 | 运维纪律（C-2） | 自测指引第一条「同机只跑一个后端」——文档件 |
| P2-11 | SSE 帧分隔注释（R-2） | 两端约定 `\n\n` 注释明示·顺带兼容 `\r\n\r\n`（解析前 strip） |

## P3（随本批顺手·可选）

Z-07 握手超时（wait_for 30s·一行）·Z-08 _reason_sent 每 turn 重置（一行）——顺手做。

## DoD

- [ ] P1-1 修+测试（高频行流用例）
- [ ] P2 十一件全修（P2-8/9 为既有 bug·白名单内）
- [ ] 全量 pytest **585+ 绿**（579+4 基线+新增约 6-10 测试）
- [ ] rag_eval 96.7% 零退化·light/dsh 引擎零退化
- [ ] 执行记录落盘 `PT-CB15-PROMOTE执行记录_Qoder-2026-08-24.md`（白话摘要段+逐件验证）
- [ ] 显式路径 commit·零 pull 零 push

> zcode 主手 · 2026-08-24 · 转正批派发
