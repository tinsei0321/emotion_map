# PT-CB9-R · RAG 增量运营批任务书（zcode·2026-08-23·三线并行）

> 性质：PT-CB9 收官后的运营批——把「重建」变成「可日常运营」。**学习输入显式盘点（用户令·learning-direction-loop 首批应用）**：
> ←OPUS-5 收敛 B2（入库校准三原则）/ B3（不对称标注+suppressed_count）/ A1（语气校准·落点修正版）
> ←泳道③审计记账 F-01（截断区注记）/ F-05（护栏 3 消费者）/ F-06（前注全量巡检）
> ←v1.2-pre §5.1（语料增长性三原则落地：五步流程/语料地图/dim 扩展）
> 分支 `EMC_harness_dsh`·各线 commit 前缀 `PT-CB9R(C/K/Z):`。本地仓即最新·零 pull 零 push。基线 **517 绿（Kimi 环境）/515+4（zcode/Qoder 环境）·rag_eval 96.7% 零退化红线**。

## 件① 契约面：B3 治理标注返回结构透出（执行=Codex·~0.5d）

- `rag_query`/`kb_facts` 返回**不对称标注**（dsh 消费端硬要求·OPUS-5 收敛 §二.2）：
  - 每条结果条目：现行条目**零标签**（默认无成本）；status='superseded' 不会出现（检索层已滤）——**历史叙述类**（lineage 非空且关联 X-01 者）带 `caliber_note: '历史口径·现行见 <替代指针>'`（从 L1 登记的 lineage/X-01 关联派生·确定性零 LLM）；
  - 返回顶层增 `suppressed_count`（本次检索被 superseded 过滤的条数——Kimi 提议·可观测）。
- **不动检索排序/过滤**（96.7% 红线）；D4 断言扩展：双入口（rag_query vs search）条目标注一致性。
- +2 测试：历史类条目标注存在性/现行条目零标签断言；eval 零退化复跑。

## 件② 内容面：SOP+巡检+订正（执行=Kimi·~0.5d·第三次独立担纲）

- `docs/rag-corpus-map.md` 五步流程第 2 步扩**入库校准三原则**（←OPUS-5 B2·Kimi 已验落点现成）：①一次证据只支撑「提过/出现过」级表述·泛化须多源；②转述与原文分层（R24 引句纪律镜像）；③耐久措辞优先·精确数值标注时点。
- F-01 记账落地：L3 执行记录登记表补注「值在 #0 截断区外·无检索面」；F-02 序号订正（#4→#3/#6）。
- F-06 全量前注巡检：`tools/rag_ctx_prefix.py --stats` 扩 `--audit`——59 条前注出处要素统计报表（文档名/小节/口径状态三要素各自命中率·非断言巡检）。
- +1 测试：corpus-map 与索引面一致性断言（地图登记的来源数=loader 实际来源数）。

## 件③ 主手件：佐证追加+checklist+回路台账（zcode 自做·随回收 commit）

- B1 佐证追加：CB42 思维链成本诊断讨论文档追加一节「业界佐证：OPUS-5 thinking_behavior（L2047-2049·模型内置分档非提示词劝说）」。
- A1 语气校准：render-contract §七 结果呈现契约补一行 checklist——「负面聚集类结果文案保持平实机制化·禁戏剧化措辞（落点=LLM 综合层与 report_assemble 类文案面·←dsh 纠错修正版）」。
- 学习→方向回路台账：`docs/catch-ball/discuss/_INDEX.md` 学习线登记 OPUS-5 链（报告+四回应+收敛）。

## DoD

- [ ] 三线齐·全量门禁不降·rag_eval 96.7% 零退化·D4 双入口断言绿
- [ ] 各线执行记录落盘·显式路径 commit·零 pull 零 push
- [ ] 收口时主手出「运营批验收单」（五步流程可日常走通=验收标准）

> zcode 主手 · 2026-08-23 · 增量运营批开批（学习输入 6 项显式盘点）
