# CB-22 · RAG 接入 EMC 验证 — 反评价收敛 + 定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG 接入验证）
> **回应**：[Codex](CB22-RAG验证_Codex-GPT5_2026-08-09.md) + [glm组](CB22-RAG验证_glm组_2026-08-09.md)
> **判定**：两组一致「**可交付**」→ 采纳 + 修正 Codex 发现的缺陷 → RAG 接入 EMC 交付完成 → 进 B 路径

---

## 〇、两组一致结论

- **RAG 接入 EMC 可交付**——9 项成果全属实（glm 独立核实+实跑·Codex 核验 8 项 + 1 缺陷）
- 黄金集 100% 非 cherry-pick（含难例·glm 确认）
- 承重红线全守（diagnose 零触碰·@track F_014-017 连续·D019 <3000B）
- glm 历轮 11 项关键补强·10 项落实（B 后补 TODO 1 项）

## 一、采纳两组

### Codex 发现并已修（1 项·交付前）
- **e2e 跨环境编码缺陷**：`test_rag_emc_e2e.py:82` subprocess `text=True` 未指定 encoding·GBK locale 读 UTF-8 输出 → UnicodeDecodeError → 断言失败
- **已修**：`encoding='utf-8', errors='replace'` + `out = r.stdout or ''`·**e2e 5/5 通过**（`bde8025b`）

### 后续补（两组建议·不阻塞交付·B 路径时做）
| # | 建议 | 来源 | 时机 |
|---|---|---|---|
| S1 | 黄金集补难例（跨文档/案例方法论正向 3-5 条） | Codex + glm | CB-22b 时补 |
| S2 | 黄金集补维度覆盖（社区/街区/城区/城中村·3-5 条） | glm | CB-22b 时补 |
| S3 | 真实 LLM 抽验越维回答 1-2 例（回答层断言·非仅检索层） | Codex | B 落地后补 |
| S4 | **B 路径收窄 RAG 触发词**（删临时结构化词） | 两组 | **CB-22b 必做** |

## 二、RAG 接入 EMC 交付完成

```
✅ case point 方法论（5 案例·无数据·test 守卫）
✅ L1.5 事实卡 35 条（7 类·含 dimension）
✅ rag_index 225 条（模型缓存 11-13ms·原子写·维度标注）
✅ 黄金集 3 类 100%（召回/越维/案例·含难例）
✅ cannot 维度化（F_016）
✅ 端点 /aiqa/rag_search（F_017·dim_counts·降级）
✅ harness 'rag_query' 短路（双条件保守·不改 diagnose）
✅ finalStep 注入（<3000B·防越维）
✅ e2e 5/5（含编码修复·跨环境可移植）
✅ 305 passed 零回归
```

## 三、下一步：B 路径（CB-22b）

验证收敛 → **进 B 路径**（query_knowledge_base·确定性·结构化查询）：
1. `query_knowledge_base` 纯函数（三层分级：city+topic 精确 → 关键词 → 兜底）
2. `/aiqa/knowledge_query` 端点
3. `_quickIntent` 加 `'knowledge_query'` 分支（KNOWLEDGE_QUERY_KW）
4. **收窄 RAG 触发词**（删「有哪些项目/体检问题」等临时结构化词·结构化转 B·emc-patterns TODO 已标）
5. 同步补黄金集难例 + 维度覆盖（S1/S2）+ 真实 LLM 抽验（S3）

## 四、待用户

- **RAG 接入 EMC 交付完成**·可浏览器人工验证（"宜昌有哪些更新项目"问答效果）
- **确认进 B 路径**（CB-22b·query_knowledge_base）→ 我发起 CB-22b 实施计划

---

*claude组（2026-08-09）·RAG 验证反评价收敛定稿·交付完成·待进 B 路径。*
