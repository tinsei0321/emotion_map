# CB-22b · B 路径实施计划确认（glm组）

> **评估方**：glm组（ZCode + GLM 5.2 · 第三方独立评估·不做项目方决策背书）
> **日期**：2026-08-09 | **CB 轮次**：CB-22b（B 路径·确定性结构化查询·承接 RAG 收敛）
> **回应对象**：`CB22b-B路径_实施计划_2026-08-09.md`
> **上轮承接**：glm CB-22 RAG 接入确认提"B 后补收窄 RAG 触发词是必做项"——本轮 B 路径落地 + 收窄执行
> **核心标尺**：演示逻辑链 / 出口三铁律 / AI·Copilot 内核（Dumb Tool + 编排器确定性）/ 承重红线

---

## 〇、一句话结论

**B 路径计划全 agree——三层分级查询（glm CB-22b 原建议被采纳）/ F_018 端点 / knowledge_query 短路 / RAG 收窄删除清单 / 黄金集补充·都对齐 glm 历轮立场。事实卡 7 表数据源充分（PROJECTS 8 + INDICATORS 8 + dimension 全 7 维度覆盖）·B 查询有料。RAG 收窄 TODO 已标（emc-patterns:48）·删除清单与 glm CB-22 接入确认建议一致。承重红线：diagnose 零触碰（_quickIntent 前置·同 RAG 模式）。glm 补 2 实现细节（B→RAG 降级触发条件 + RAG_KNOWLEDGE_RE 收窄后自洽性）+ 1 测试补强（B/RAG 路径不混断言）。无阻断·可执行。**

---

## 现状核实：B 路径数据源与接入点（glm 独立）

**事实卡 7 表（B 查询对象·glm 核实）**：

| 表 | 条数 | topic/dimension 覆盖 |
|---|---|---|
| PROJECTS | 8 | topic=project·dimension={片区3/住房1/城中村1/街区1/城区1/社区1}——**全 6 维度覆盖** ✅ |
| INDICATORS | 8 | dimension={住房2/小区2/街区1/社区1/城区1/片区1}——**全 6 维度** ✅ |
| CHECKUP_ISSUES | 有（体检问题） | — |
| PANELS | 有（八大片区） | — |
| CASES | 有（案例） | — |
| POLICIES | 有（政策） | — |
| METRICS_SYSTEM | 有（指标体系） | — |

**glm 判定**：事实卡 7 表**数据源充分**——B 三层分级查询有料（L1 精确 city+topic·L2 keyword contains·L3 兜底）。

**rag_search 端点 F_017（B 仿照·aiqa_routes:111）**：`RagSearchIn → {ok, results, count, dim_counts}`——B 端点 F_018 仿此·连续编号 ✅。

**RAG_QUERY_KW 现状（emc-patterns:46-54）**：
- 开放语义（本职）：`'哪些城市','哪些案例','哪些项目适合','如何参考','做法','机制','路径'`
- **临时结构化（TODO 删）**：`'有哪些项目','体检问题','体检指标','更新项目','项目库','问题清单'`
- TODO 注释（:48）明确标"CB-22b 建后删"——**glm CB-22 接入确认的必做项·本轮执行**。

---

## 一、5 焦点确认（glm组）

### 焦点 1：query_knowledge_base 三层分级 — **agree（glm 原建议被采纳）**

**计划**：L1 精确（city+topic+region）/ L2 关键词（name/detail contains keyword）/ L3 兜底（topic-only + 诚实标注）。

**glm组 确认**：**agree——三层分级是 glm CB-22b 讨论回应原建议（焦点 2）·全采纳。**

**glm 历轮建议对照**：
- glm CB-22b 讨论回应焦点 2："city+topic 确定性对·但需补分级查询 L1 精确/L2 关键词 contains/L3 兜底"——**计划 :26-33 全采纳**。
- L3 兜底"诚实标注'未找到 {city} 专属·以下是其他城市参考'"——**glm CB-22b 原话**（:30）。

**glm组 实现细节建议（2 点·非阻断）**：

**① L1 精确查询的 topic 枚举映射**：
- 事实卡 7 表（PROJECTS/INDICATORS/CHECKUP_ISSUES/PANELS/CASES/POLICIES/METRICS_SYSTEM）·用户问句 topic 需映射到表名：
  ```python
  TOPIC_TO_TABLE = {
      'project': PROJECTS, 'projects': PROJECTS,
      'indicator': INDICATORS, 'metric': INDICATORS, '指标': INDICATORS,
      'issue': CHECKUP_ISSUES, '体检问题': CHECKUP_ISSUES,
      'panel': PANELS, '片区': PANELS,
      'case': CASES, '案例': CASES,
      'policy': POLICIES, '政策': POLICIES,
  }
  ```
- 问句"有哪些项目" → topic='project' → PROJECTS 表·"体检指标" → topic='indicator' → INDICATORS 表。
- **glm 建议**：topic 枚举含中英文（'项目'/'project' 都映射）·防用户问句中文 topic 不命中。

**② region 参数语义（vs city）**：
- 计划 :26 `city + topic + region`——city='宜昌'·region='葛洲坝'/'伍家岗'？
- 事实卡 region 字段（urban_renewal_knowledge）—— glm 核实 PROJECTS 有 region（如'葛洲坝'/'伍家岗'）。
- **glm 建议**：region 是 city 内子区域（片区/行政区）·L1 精确可选过滤·缺省=全市。

### 焦点 2：/aiqa/knowledge_query 端点 F_018 — **agree（仿 rag_search 正确）**

**计划**：`KnowledgeQueryIn{city,topic,region,keyword}` → F_018 → `{ok,results,count,fallback,note}`。

**glm组 确认**：**agree——仿 rag_search（F_017）·F_018 连续编号·返回含 fallback/note（L3 兜底标注）正确。**

**glm组 建议（端点实现细节）**：
- 返回 results 每条含 `dimension`（颗粒度原则·与 rag_search data_dim 同档）。
- **失败降级**：查询返空（L1/L2/L3 都无）→ 返 200 + `{ok:True, results:[], count:0, note:'知识库无相关条目'}`（非 500·非 False·"查了但没找到"≠ "查询失败"）。
- **vs rag_search 降级**：rag_search 索引未构建返 ok:False（系统故障）·B 端点查询返空返 ok:True+results:[]（查到了空集·正常）——**两者降级语义不同·禁混**。

### 热点 3：harness 'knowledge_query' 短路（B 先 RAG 后·降级）— **agree + 降级触发条件建议**

**计划**：_quickIntent 加 'knowledge_query' 分支（KNOWLEDGE_QUERY_KW + RAG_KNOWLEDGE_RE）→ B 先 → 未命中降级 rag_search。

**glm组 确认**：**agree——B 先 RAG 后是 glm CB-22 一贯立场（结构化走确定性·开放走向量）。**

**glm组 降级触发条件建议（关键实现细节）**：

计划 :61 "B 未命中降级 /aiqa/rag_search"——**但"未命中"定义需明确**：

| B 查询结果 | 是否降级 RAG？ | 理由 |
|---|---|---|
| L1/L2 命中（results 非空） | ❌ 不降级 | B 确定性查到·够答 |
| L3 兜底（fallback=True·其他城市参考） | ⚠️ **可选降级** | 兜底非用户想要的本地数据·RAG 或许召回本地段落 |
| 全空（results=[]·L3 也无） | ✅ **降级 RAG** | B 完全无果·RAG 兜底召回 |

**glm 建议（降级条件）**：
```javascript
// harness 主循环 knowledge_query 分支
const bRes = await fetch('/aiqa/knowledge_query', {...});
const bData = await bRes.json();
if (bData.results.length > 0 && !bData.fallback) {
  // B 命中·注入 finalStep
} else if (bData.fallback) {
  // L3 兜底·标注"非本地"+ 可选 RAG 补
} else {
  // 全空·降级 RAG
  const rRes = await fetch('/aiqa/rag_search', {query, k:5});
}
```
- **禁 B 兜底就降级 RAG**——兜底有结果（虽非本地）·RAG 可能更好·但也可直接答兜底（诚实标注）。
- **降级仅限 B 全空**——B 是确定性·全空说明事实卡无此条目·RAG 向量或召回段落。

### 焦点 4：收窄 RAG 触发词（删除清单）— **agree（删除清单正确 + KNOWLEDGE_QUERY_KW 对应）**

**计划**：RAG_QUERY_KW 删临时结构化词（有哪些项目/体检问题/体检指标/更新项目/项目库/问题清单）·转 KNOWLEDGE_QUERY_KW。

**glm组 确认**：**agree——删除清单与 glm CB-22 接入确认"必做项"一致。KNOWLEDGE_QUERY_KW 接管正确。**

**glm 核实（RAG_KNOWLEDGE_RE 自洽性）**：
- RAG_QUERY_KW 删"有哪些项目"等后·RAG_QUERY_KW 只剩开放语义词（哪些城市/案例/如何参考/做法/机制/路径）。
- 但 `RAG_KNOWLEDGE_RE = /项目|指标|体检|案例|政策|片区|问题|做法|机制/`（:55）**仍含"项目/指标/体检/问题"**。
- **自洽性验证**：
  - "宜昌有哪些项目" → RAG_QUERY_KW 删"有哪些项目" → **不命中 rag_query**（即使 KNOWLEDGE_RE 含"项目"·RAG_QUERY_KW 不命中则短路不触发）→ ✅ 正确（转 knowledge_query）
  - "如何参考项目做法" → RAG_QUERY_KW 含"如何参考" → 命中 + KNOWLEDGE_RE 含"项目/做法" → **触发 rag_query** → ✅ 正确（开放语义）
- **glm 判定**：RAG_QUERY_KW（列表）+ RAG_KNOWLEDGE_RE（正则）**双条件**——删列表词不影响正则·自洽 ✅。

**glm组 建议（KNOWLEDGE_QUERY_KW 复用 KNOWLEDGE_RE）**：
- 计划 :60 `KNOWLEDGE_QUERY_KW.some(w) && RAG_KNOWLEDGE_RE.test(s)`——**复用 RAG_KNOWLEDGE_RE 正则**（非新建 KNOWLEDGE_QUERY_RE）。
- 理由：知识词判断（项目/指标/体检/案例）是通用的·B 和 RAG 共用·DRY。
- **但须确认**：RAG_KNOWLEDGE_RE 名字含"RAG"——B 复用语义不清晰。**glm 建议改名** `KNOWLEDGE_RE`（去 RAG 前缀·通用知识词正则）·B/RAG 共用。

### 焦点 5：黄金集补充（难例 + 维度覆盖）— **agree（glm 上轮 P2 建议被采纳）**

**计划**：补难例（跨文档/案例方法论正向）+ 维度覆盖（社区/街区/城区/城中村）。

**glm组 确认**：**agree——glm CB-22 RAG 验证 P2 建议（"补黄金集维度覆盖·偏更新/项目·缺社区/街区/城区/城中村"）被采纳。**

**glm 组补充建议（黄金集设计细节）**：

**① B 路径专属黄金集（新增·与 RAG 黄金集区分）**：
- RAG 黄金集（已有·14 条）：开放语义·召回/越维/案例。
- **B 黄金集（新增）**：结构化查询·三层分级验证。
  ```python
  B_GOLD = [
      # L1 精确
      {'query':'宜昌有哪些更新项目','expect_topic':'project','expect_count_min':3},
      {'query':'葛洲坝体检问题','expect_topic':'issue','expect_region':'葛洲坝'},
      # L2 关键词
      {'query':'伍家岗停车','expect_keyword':'停车','expect_dim':'小区'},
      # L3 兜底
      {'query':'北京更新项目','expect_fallback':True,'expect_note_contains':'未找到'},
  ]
  ```
- **断言**：L1 命中正确 topic + count ≥N / L2 命中 keyword + dim / L3 fallback + note。

**② 维度覆盖（glm P2 建议）补到 RAG 黄金集**：
- 计划 :75-76 维度用例（葛洲坝 33 社区/二马路街区/汉宜村城中村/西陵区城区）——补到 RAG 黄金集 recall 类。
- **glm 建议**：4 条维度用例·断言命中对应 dimension（社区/街区/城中村/城区）——验证颗粒度全维度覆盖。

**③ 路径不混断言（glm 关键补强）**：
- B/RAG 路径分离后·须断言"不混"：
  ```python
  # "宜昌有哪些更新项目" → knowledge_query（B）·非 rag_query
  assert _quickIntent('宜昌有哪些更新项目') === 'knowledge_query'
  assert _quickIntent('宜昌有哪些更新项目') !== 'rag_query'  # 收窄后不命中
  # "如何参考上海做法" → rag_query（开放）·非 knowledge_query
  assert _quickIntent('如何参考上海做法') === 'rag_query'
  ```
- 防 B 收窄后 RAG 仍误触发结构化查询（glm CB-22 接入确认 W2 风险）。

---

## 二、风险清单

### 阻断级
无。

### 警告级

| # | 风险 | glm组 建议 |
|---|---|---|
| **W1** | B→RAG 降级条件不清（兜底算不算未命中） | 降级仅限 B 全空（results=[]）·兜底（fallback）不降级（有结果·诚实标注） |
| **W2** | RAG_KNOWLEDGE_RE 名字含 RAG·B 复用语义不清 | 改名 KNOWLEDGE_RE（通用·B/RAG 共用） |

### 建议级

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | topic 枚举中英文映射（'项目'/'project' 都映射表） | P0 |
| **S2** | B 专属黄金集（三层分级验证·与 RAG 黄金集区分） | P0 |
| **S3** | B/RAG 路径不混断言（收窄后 RAG 不误触发结构化） | P0 |
| **S4** | RAG_KNOWLEDGE_RE 改名 KNOWLEDGE_RE（DRY·B/RAG 共用） | P1 |
| **S5** | region 是 city 内子区域（片区/行政区）·L1 可选过滤 | P1 |

---

## 三、可否执行结论

### ✅ 可执行——5 焦点全 agree·glm 原建议全采纳·附 2 警告 + 5 建议

**glm组 核心立场**：

B 路径计划**全 agree**——glm CB-22b 讨论回应（三层分级）+ CB-22 接入确认（B 后补收窄）+ CB-22 RAG 验证（P2 维度覆盖）**三历轮建议全采纳**：
- 三层分级查询（L1/L2/L3）：glm CB-22b 焦点 2 原建议
- RAG 收窄（删临时结构化词）：glm CB-22 接入确认"必做项"
- 黄金集维度覆盖：glm CB-22 RAG 验证 P2 建议

**5 焦点判定**：
- 焦点 1 三层分级：**agree**（glm 原建议·补 topic 枚举映射 + region 语义）
- 焦点 2 F_018 端点：**agree**（仿 rag_search·补降级语义区分 B 空集 vs RAG 故障）
- 焦点 3 knowledge_query 短路：**agree**（B 先 RAG 后·补降级触发条件 = B 全空）
- 焦点 4 RAG 收窄：**agree**（删除清单正确·RAG_KNOWLEDGE_RE 自洽性验证通过·建议改名通用）
- 焦点 5 黄金集：**agree**（glm P2 采纳·补 B 专属黄金集 + 路径不混断言）

**承重红线核验**：
- **diagnose prompt**：零触碰（_quickIntent 前置·同 RAG 模式）✅
- **四态出口**：B 走 EXIT_CONCEPT（general 直答）·非新态 ✅
- **D019 final 极瘦**：B 注入 intent-gated + Top-N ≤5·体积可控（事实卡条目 ≤80 字·5 条 ≤400B）✅
- **@track**：F_018 连续（F_014-017 RAG 系列 + F_018 B）✅

**glm 关键提醒（2 实现 + 3 测试）**：

**实现（2）**：
1. **B→RAG 降级条件**：仅 B 全空（results=[]）降级·兜底（fallback）不降级（有结果·诚实标注）。
2. **RAG_KNOWLEDGE_RE 改名**：B/RAG 共用·去 RAG 前缀 → `KNOWLEDGE_RE`（语义清晰 + DRY）。

**测试（3）**：
1. **B 专属黄金集**：三层分级验证（L1 精确/L2 keyword/L3 兜底）·与 RAG 黄金集区分。
2. **维度覆盖**：社区/街区/城区/城中村用例（glm P2 采纳）。
3. **路径不混断言**：收窄后"有哪些项目" → knowledge_query（B）·"如何参考" → rag_query（RAG）——防 B 收窄后 RAG 误触发。

**glm 历轮追踪（CB-22b 从讨论到实施）**：
- CB-22b 讨论回应：三层分级 + A/B 分工 + INVENTORY 数据源区分
- CB-22 接入确认：B 后补收窄 RAG 必做
- CB-22 RAG 验证：P2 维度覆盖建议
- **本轮（B 实施）**：三历轮建议全采纳·计划可执行

---

## 附：现状核实证据（glm组 独立）

| 发现 | 证据 |
|---|---|
| 事实卡 7 表 | urban_renewal_knowledge: PROJECTS:15/INDICATORS:51/CHECKUP_ISSUES:87/PANELS:107/CASES:123/POLICIES:147/METRICS_SYSTEM:167 |
| PROJECTS 8 条 dimension 全 6 维度 | Counter({片区3/住房1/城中村1/街区1/城区1/社区1}) |
| INDICATORS 8 条 dimension 全 6 维度 | Counter({住房2/小区2/街区1/社区1/城区1/片区1}) |
| rag_search 端点 F_017 | aiqa_routes:111 `post_rag_search` |
| RAG_QUERY_KW 临时结构化词 TODO | emc-patterns:48 `TODO(CB-22b): B 路径建后删...` |
| RAG_KNOWLEDGE_RE（B 可复用） | emc-patterns:55 `/项目\|指标\|体检\|案例\|政策\|片区\|问题\|做法\|机制/` |
| RAG_QUERY_KW + RAG_KNOWLEDGE_RE 双条件 | harness:71（glm CB-22 RAG 验证已确认自洽）|

### 声明

本回应由 glm组（ZCode + GLM 5.2）独立产出·2026-08-09·基于核实事实卡 7 表（topic/dimension 分布）+ rag_search 端点 + RAG_QUERY_KW/KNOWLEDGE_RE 自洽性。第三方独立评估·不做项目方决策背书。glm CB-22b 讨论回应 + CB-22 接入确认 + CB-22 RAG 验证三历轮建议被本轮 B 计划全采纳。

---

*登记：docs/context-map.md · CB-22b B 路径 glm组 确认。*
