# CB-40 · EMC 现状与目标差距 · 收敛定稿（2026-08-16）

> 收敛方：zcode组（**经用户授权出稿**·claude组/Codex 评估已齐 + zcode 回应与交叉挑战已落盘·dsh组 已退出不参与）。
> 输入：`CB40-..._讨论发起_zcode` + `..._评估_Claude` + `..._评估_Codex` + `..._回应与交叉挑战_zcode`（同目录·2026-08-16）。
> 性质：**定稿 = CB-39 之后排期的决策依据**。本轮除「顺手项」（CLAUDE.md 修正 / goal-status.md 建立 / index+journal 登记·两组共同约定）外零实施；CB-39 B/C 线并行不受影响。

---

## 〇 一句话结论

发起稿框架全采纳；**Codex 两处事实修正采入**（出向实为三层结构·单卡 CSV 已落地；JS 计数口径勘误）；四维分数定稿 **80 / 30 / ≈30（区间 22-35·双口径注）/ 65**；排序定稿 **G1（行业表格式级·开工点=CB-39 C 线完成后·2-3 天）> G5（时间轴专题·可插空·用户 gate）> G2（前端测试·分层按需）**；F3 vitest 时序冲突裁定采 zcode 折衷案；F4 演示期零工程投入+表述限定；F5 双采（CLAUDE.md 出向节三态修正 + `docs/goal-status.md` 每轮必更 DoD 化）——两项顺手项**本轮已执行**。G6-G9（dsh 借鉴候选）入全景表、归 dsh 专题会话裁定后并回。

---

## 一 焦点裁定总表（F1-F5）

| 焦点 | 三方立场 | **定稿裁定** |
|---|---|---|
| F1 量化标尺 | claude 独立给分 78/30/22/65（三项吻合·出向微调）；Codex 认同框架但 M1/M2 两处事实修正（zcode 实测复核认错） | **框架全采 + 分数定稿见第二节**；发起稿勘误表（zcode 回应文档§五）为本定稿组成部分；zcode 方法教训入档（中文关键词 grep 漏英文命名体系·与「fact 67」同类） |
| F2 CB-39 后第一优先 | 三组实质一致 G1>G5>G2；claude 加约束（G1 等 C5 完成·终归用户拍板）；Codex 加时序（G1 开工点=C 线完成·G5 插空·G2 绑拆楼） | **`G1 > G5 > G2` 定稿**。G1=「行业表格式级」（Codex 重定义采）：开工点=CB-39 C 线完成后（B3 口径注册表+C2 素材是输入·先做会空心+固化漂移口径）；量级 2-3 天（四件基建复用：契约库/导出/脱敏/下载通道·openpyxl 零新依赖）；**前置输入两件**（中转站 5 份真件补齐 `DATA/exchange/copy/` 或 G 盘直引 + 用户确认「城市体检指定项」表清单——非工程问题·用户拍板）。G5 零依赖可插空（C 线与 G1 间等待窗口可用·唯一 gate=manifest 生成用户授权）。G2 不作独立竞争者（见 F3）。价值序与执行序分离（claude 约束②·演示时间表可改执行序） |
| F3 G2 补法 | **两组直接冲突**：claude=现在补纯函数模块（state 词表/色板·result-struct·paradigm 镜像·field_dictionary）；Codex=推迟到拆楼前夜（package.json gitignore 约束 + 为巨石写单测=负资产） | **裁定采 zcode 折衷案**：① e2e 网优先（CB-39 P1-5/P2 既定·最便宜先护演示链路）；② vitest 基建**不等拆楼**——独立工程文件（frontend/package.json 或 tests/frontend/·.gitignore:54 例外·Codex 约束成立）+ 首批仅测 3-4 个纯函数模块（claude 清单收窄·P0-2 跨语言守卫抓 compare 漂移=纯函数层测试价值实证）·0.5-1 天；③ 巨石（harness/panel/map DOM 重件）等拆楼后为拆出物补测（Codex 主张对·「为巨石写单测=负资产」）。本轮只留一行 vitest 选型记录（Vite 生态原生/可升组件测）·不建工程 |
| F4 持续数据流 | 两组一致（零工程投入）+ Codex 补执行件 | **演示期零工程投入**（L0 购买不投·12345 年度等数据·管线已就绪 2025 数据到手当天可接）；「连续主观体检」**命名不改**（北极星定位·自毁纲领）但**表述限定**（演示物料/manifesto/panel 统一加「2024-2025 横截面序列·持续接入规划中」——与 P0-1 诚实度纪律同源）；**G5 时间轴专题 = 「连续」的演示期诚实载体**（真实时间点序列呈现时间维度·不需假装数据流） |
| F5 漂移治理 | 两组全采 + 落位一致（独立文件·DoD 化） | **双采定稿**：① CLAUDE.md 出向节三态修正（措辞采 Codex 版）——**本轮已执行**；② `docs/goal-status.md` 建立（四维分数+三画像距离+G 缺口状态·首轮内容=本定稿第二节/第三节·**每轮收敛必更·收敛方负责**·与 E5 登记债同款 DoD）——**本轮已建立** |

---

## 二 四维分数定稿（goal-status.md 首轮基线）

| 维度 | 定稿分 | 口径注 |
|---|---|---|
| 入向链路 | **80%** | claude 独立 78% 吻合（±5 内）；扣分主因=B002/B004 假结论 open + 参数填充瓶颈 |
| 知识库/RAG | **30%** | 按三支柱①完备度口径（claude/Codex 双确认）；按「演示可用」口径已够（更新域 52 fact+214 note）——**C 线后 70%+ 预估双组背书** |
| 出向链路 | **≈30%（区间 22-35）** | **双口径注**：主口径=纲领验收（行业表=0·立项风险本体）→claude 22%；副口径=基建完成度（四件可复用）→Codex 35%。**承重的是三层结构事实**：答案级 Wave 0 ✅ / 单卡 CSV ✅（export.py:200 F_005·CB-18）/ 行业表格式 = 0（G1 对象） |
| 质量基建 | **65%** | 后端 366+3+守卫通电 vs 前端零单测（JS 计数口径待统一·45-52 间 excl vendor）+ e2e 单源 + 环境漂移（P1-3/P1-5 已排未做） |

**三画像距离**（排期按此定·非按维度分数）：业内同行（当前演示对象）= 基本达标；事企业单位（立项根本客户）= 最远（CB-39 完成才「能对话」·行业表对接是下一程）；城市运营单位 = 中（界面在·持续数据流不在）。

---

## 三 G 缺口全景排序表（G1-G9）

| # | 内容 | 状态/排期锚点 | 定稿要点 |
|---|---|---|---|
| G1 | **出向·行业表格式级** | **CB-39 后第一个专题轮**（开工点=C 线完成）·2-3 天 | 重定义（单卡 CSV 已在）；前置输入两件待用户；风险=口径漂移固化（故排在 C 线后）+「指定项」样例未确认 |
| G5 | 时间轴专题 | 插空（C 线与 G1 间窗口可用）·v3.1 D3 议题清单直接复用·1.5-2 天 | 唯一 gate=manifest 生成用户授权；=「连续」诚实载体（F4） |
| G2 | 前端测试 | 分层：e2e（CB-39 内）→ vitest 基建+首批纯函数 3-4 模块（0.5-1d·不等拆楼）→ 巨石拆楼后补 | F3 折衷裁定；package.json gitignore 例外约束 |
| G3 | 持续数据流 | 策略轮（拿到 2025 数据/购买预算时再定·不提前设计） | 演示期零工程投入；执行件=F4 限定词+G5 载体 |
| G4 | 知识库更新机制 | **并入**：策略维度入 G3 轮·工程维度入 CB-39 C5 收尾 SOP（一页纸） | 两组建议合并表述（claude 并 G3 / Codex 并 C5·兼容） |
| G6-G9 | dsh 借鉴四候选（session log 可重放/守卫管线化/契约全自动派生/CPD delegated turn） | **归 dsh 专题会话裁定后并回本表** | 时间线信息差说明：两组评估落盘（18:32）早于 dsh 底稿（18:48），Codex「无新增 G6」在其时点为真但未覆盖 dsh 维度；底稿=`EMC-harness架构借鉴dsh_专题底稿_zcode-2026-08-16.md`（已入 todo·五议题议程） |

---

## 四 定稿行动项

**P0（收敛顺手项·本轮已执行 ✅）**
1. ✅ CLAUDE.md「出向链路」节三态修正（=0 表述 → 答案级✅/单卡 CSV✅/行业表未开工）。
2. ✅ `docs/goal-status.md` 建立（首轮基线=本定稿第二/三节）+ 每轮收敛必更 DoD 生效。
3. ✅ `_cb-index.md` + `cb-journal.md` CB-40 登记（E5 DoD）。

**P1（CB-39 收尾前后启动）**
4. G1 前置输入：① 中转站 5 份真件补齐 `DATA/exchange/copy/`（或登记 G 盘直引·manifest copy 段已列名）；② **用户拍板「城市体检指定项」表清单**。
5. `requirements.txt` 补 openpyxl（实装未登记·依赖漂移·Codex 发现）。
6. vitest 选型一行决策记录 + 首批纯函数模块清单（state 词表/field_dictionary 镜像/harness 纯函数/result-struct）——不建工程。

**P2**
7. JS 计数口径统一（excl vendor·一次性清点·goal-status 基数修正）。
8. G4 入库 SOP 一页纸随 C5 收尾交付。
9. Codex 在 A 线批次记录补一句 367+2→366+3 第三次 skip 归因（pii_guard·A2 迁移所致·claude 提·防下轮考古）。

**P3**
10. F4 限定词落展示层（manifesto/panel 副标题「2024-2025 横截面序列·持续接入规划中」）。

---

## 五 DoD 与移交

- [x] 本定稿落盘 + 顺手项三项执行（P0）
- [ ] 用户过目：排序 G1>G5>G2 与 F3 折衷裁定（可推翻·终归用户）
- [ ] G1 前置输入两件（P1·其中表清单须用户拍板）
- [ ] G6-G9 → dsh 专题会话（用户已排期另开会话）→ 裁定后并回 G 表
- [ ] goal-status.md 每轮收敛必更（下轮收敛方执笔时首查）
- [x] CB-39 B/C 线并行不受本轮影响（确认）

---

> CB-40 状态：**已收敛**（zcode 出稿·用户授权；claude组/Codex 评估 + zcode 回应全链齐备·事实分歧已实测消除·口径分歧已双注裁定·唯一冲突 F3 已裁）。
