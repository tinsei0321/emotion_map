# PT-CB15 · 2026-08-25 工作审计收敛（Codex 主收敛）

> 参与方：Claude（被审计对象）· Codex（初审计）· Qoder（独立复审计）。
> 收敛人：Codex。本文件为最终收敛口径，取代此前两份审计中的错漏与分歧。

## 白话摘要

三方像三位验房师分别看同一套房，结论一致：**房子能住，但有条件**。工具箱补件、数据整理、新机器开机这三块活儿，核心事实都对；问题集中在几件“交付后手续”上：文件还没提交推送、一张关键说明书放在仓库外（换电脑会丢）、AI 用的模型名是官方已经宣布停用的旧名字、还有几处数字对不上账。Qoder 复核时还帮我把我的初审纠了三处小错、补了四处我看漏的，最重要的一个是：新旧模型名不是纯等价，切换会连带改变“思考档”，所以不能简单建议换名字。最终通过条件是 6 件必办事项，办完即通过。

## 收敛结论：有条件通过

三方结论一致为 **有条件通过**。核心事实链（PT-CB14 四件、数据治理零改动、office 环境重建 + T8 首测）均成立，但存在高优先交付缺口和模型名遗留风险，须满足文末 6 项条件后方可通过。

## 各方结论对照

| 方 | 结论 | 备注 |
|---|---|---|
| Claude | 待审计 | 报告自述三块工作完成 |
| Codex 初审计 | 有条件通过 | 5 项风险/遗漏 + 模型别名风险 |
| Qoder 独立复审计 | 有条件通过 | 复核 Codex 结论，纠 3 错、补 4 漏、1 处立场分歧 |
| 本收敛 | 有条件通过 | 全部采纳 Qoder 纠错与补漏 |

## 对 Qoder 复核意见的采纳

以下全部采纳，并已修正我方初审计口径：

1. **备份计数纠错**：现行 `~/.codex/config.toml` + **6 个** `.bak-*` 共 7 个文件（非“7 个 .bak”）；合计密钥行约 29 处，且各备份是不同时期快照，非同一密钥复制 7 份。风险等级不变（高），但量化与定性须按此表述。
2. **撤销冗余建议**：`netstat -ano | findstr ":8600.*LISTENING"` 已在 `docs/codex-harness-ops.md` §一.2 原文存在，Codex 此前通读不完整，撤回“补一条诊断命令”的建议。
3. **presets 计数漂移归因纠错**：漂移项为 `tmp_render_1787569867`（停车设施缺口_140区，登记时间 2026-08-24 19:11:07），来自 PT-CB14 回收批“停车位题出图副作用实证”，早于 T8；非 Codex 初判“后续 render_file 又登记”的模糊对象。
4. **补漏：附录 B 与主报告尝试表不一致**：主报告三行（含 chat+无引导对照），附录 B 两行且表头与表体矛盾；关键对照实验未入附录 B，需补行对齐。
5. **补漏：门禁口径漂移**：`OFFICE.md` 预告“应 595+4”，office 实测 597+2；同日 home 文档自相矛盾，需对账并解释 skip 机差来源。
6. **补漏（最重要）：`deepseek-v4-flash` 默认思考档**：官方模型明细载明 v4-flash 默认思考模式，而 `deepseek-chat` 过渡期映射为非思考模式。两者非纯别名，切换会翻转默认思考档。因此 Codex 原建议“优先改为规范名”缺少关键前提，完整版必须是“规范名 + 显式非思考配置 + 有引导 A/B 实测”。
7. **补漏：任务书 C3 自相矛盾**：验收要求前端徽标，但白名单未列 `render_client.js`，这是白名单外单行的直接成因，影响纪律裁定。
8. **立场分歧收敛**：`render_client.js:113` 由 Codex“有条件接受（3 条件）”收敛为 **接受**——主手已在提交 6d089391 明文“白名单外单行追认”；追加行内注释本身会构成又一次包外改动，故注释并入下一次授权批次，不单独开工。

## 最终关键发现与通过条件

### 已无条件通过

- PT-CB14 四件：代码、测试、验收口径均核验通过，全量门禁 **597 passed / 2 skipped**。
- 数据治理零改动：字段事实（XQMC unique=1562、SSSQ=187/188、SSJD=22、组内最多 40）与报告一致，错误产物已删、数据零改动、教训已入 memory。
- office 环境与 T8 首测环境事实：codex-cli 0.149.1、`_resolve_codex_exe()` 命中 npm vendor、`required=true`、`_codex_cwd/AGENTS.md` 在位、三服务监听、MCP 18 工具，均实读/实测一致。

### 通过条件（6 项，满足后方可通过）

1. **commit + push** 全部未提交内容：`core/codex_bridge.py`、`docs/progress.md`、5 份 discuss 文档（含 Claude 报告、附录 B、Codex 初审计、Qoder 复审计、本收敛稿）。
2. **人设模板入仓 + 三处复刻面回写**：权威模板入 `docs/emc-codex-persona.md`；`docs/codex-harness-ops.md`、`_handoff/OFFICE.md`、spike §八复刻表补“到岗生成/复制 `{REPO}/../_codex_cwd/AGENTS.md`”步骤。
3. **deepseek 模型名定稿闭环**：有引导 A/B 实测 `deepseek-v4-flash`（**显式非思考配置**）；通过则切规范名，不通过则书面记录退役别名依据/风险/回退预案进 ops 文档；提交前对齐 `core/codex_bridge.py` 注释“待用户终裁”与报告“已定稿”的口径。
4. **T1-T7 逐项回执 + T8 复跑步骤落盘**；附录 B 尝试表补行与主报告三行对齐。
5. **密钥面处置**：`~/.codex/config.toml` 改环境变量注入/占位符；清理或脱敏 6 个备份；必要时轮换已入备份的历史密钥；换机清单只带占位符。
6. **两处计数口径 + 文档一致性修正**：presets 60/61、门禁 595+4/597+2；统一 8600 失败点表述（“工具调用快速失败” vs “thread/start 失败”）；顺带定档 4 个在盘 tmp_render 项与 `render_file` 登记清理纪律。

## 保留的未核验项

- 8600 缺失的精确失败点（thread/start vs initialize）；app-server 自动加载 cwd AGENTS.md 行为；T8 三次尝试会话细节；webSearch ok=false 根因与普遍性；home 机 595+4/597+2 skip 机差根因；PT-CB14 时点 581+2 的 home 历史值跨机引用。
