# CB-22d · 地图标记路径跑通 — 实施复验发起

> **发起方**：claude组（Claude Code·家环境）| **日期**：2026-08-10 | **性质**：CB 实施复验轮（实测失败根治后·两组复验）
> **承接**：地图标记实测失败（三证合一）→ 用户两想法（LLM 参与模糊搜索 / 放弃不可识别·像人思维）→ 两组根因讨论（修正 claude 根因 + 补 3 层）→ 反评价收敛定稿 → **claude 实施（commit `ace4f8f`·路径跑通）**
> **目的**：① 两组复验实施是否符合定稿（P0-0 前提 + P0-1 数据层 + P0-2 决策层）② 确认「路径跑通 + 不挂起」③ 通过后定稿

---

## 〇、一句话结论

**generate_point_layer 实测失败根治已实施（commit `ace4f8f`·6 文件 150 行）·307 passed 零回归·路径跑通（8/9 名有输出）·请两组复验「路径跑通 + 不挂起」是否达成。**

---

## 一、实施内容（commit `ace4f8f`·按定稿 P0-0/P0-1/P0-2）

### P0-0 前提修正（Codex 根因 C/D + 依赖）
- **names split**：generate_point_layer 标量/拼接串 split（中文逗号/顿号/半角逗号/空格）→ 逐名数组（修 Codex 根因 C「拼接串被当单名」）
- **冷加载 20s**：单请求超时 5s→20s（修 Codex 根因 D「冷加载 12.4s > 5s abort」）
- **装 rapidfuzz + pypinyin**：requirements 已有·本机补装·difflib 退役（修 Codex 依赖层）

### P0-1 数据层（用户想法 1：LLM 参与 + 成熟 API + 本地兜底）
- **高德 API 优先**：`search_place(amap_first=True)` → `/place/search?amap_first=true`——先高德 place/text 解析片区名（成熟 API·不造轮子·防伍家菜市场类本地误伤）
- **A0 jieba 分词双路**：`place_layer._core_entities`（jieba 分词 → 剔除规划后缀 → 核心地名）+ `forward` 双路匹配（完整串 + 核心实体·取高分·复用 `_match_score` 分层）
- **聚合名放弃**：`_isAggregate`（引号容忍）——污水厂网一体示范区/其他项目无具体地名 → 不查 API 归 unmatched（用户想法 2·无地点就放弃）

### P0-2 决策层（用户想法 2：像人思维·放弃 + 有思考上限）
- **B1 零命中零 LLM 确定性出口**：generate_point_layer 全未命中 → 直接确定性文字（observation 命中 n/N + 未命中列表 + 建议上传坐标）·exit='answered'·onFinalDone 必触发·**计时必然收尾**（根治 6.5 分钟挂起·不再调 finalStep LLM）
- **agentStep 已有 30s 超时 + 流式 token 输出**：确认 api.js:34 `agent_step=30000` 超时 + api.js:72-73 逐 token onToken/onReason（用户能看到在思考·不静默挂）

### 测试
- `validate_generate_point_layer` 9 断言（含 B1 零 LLM 防回归 + names split + 高德优先 + 聚合名）
- **pytest 307 passed + 3 skipped 零回归**
- **路径跑通验证**：模拟 9 名 → 8/9 有输出（葛洲坝片区→工作委员会 ✓ / 红星路-二马路→历史文化街区 ✓ / 夷陵广场 ✓ / 达门船厂 ✓ / 其他项目→放弃 ✓ / 污水厂网示范区→正确放弃 ✓）

## 二、待两组复验焦点

### 焦点 1：P0-0 前提
- names split 是否正确（拼接串→逐名·Codex 根因 C）？
- 冷加载 20s 是否解决首用必全 abort（Codex 根因 D）？
- rapidfuzz/pypinyin 装后匹配质量提升确认？

### 焦点 2：P0-1 数据层
- 高德优先（amap_first）是否正确（成熟 API·防误伤）？
- A0 jieba 分词双路（_core_entities + forward 双路·复用 _match_score）是否合理？
- 聚合名放弃（_isAggregate·引号容忍）是否符合用户「无地点就放弃」？

### 焦点 3：P0-2 决策层（命门）
- **B1 零命中零 LLM 出口是否根治挂起**？（不再调 finalStep·计时必收尾）
- agentStep 30s 超时 + 流式 token 输出确认？
- 是否还有「停半途」场景未覆盖？

### 焦点 4：路径跑通 + 验收口径
- 路径是否真跑通（真实数据·用户能看到标记 + 未命中诚实文字）？
- 验收口径：「葛洲坝片区→实体点」「污水厂网示范区→unmatched 诚实」可接受？
- **0 挂起**（任何情况 30s 内出口）达成？

### 焦点 5：补充与异议
- 有无遗漏/更好做法？（准确度后续完善·本轮聚焦路径跑通）

## 附 A：复验 prompt（发两组用）

```text
【CB-22d · 地图标记路径跑通复验】情绪地图 — claude组 → Codex/glm组（2026-08-10）

背景：generate_point_layer 实测失败根治已实施（commit ace4f8f·6 文件 150 行）。用户确认：聚合名无地点就放弃·agentStep 30s 超时但须流式 token·准确度后续完善今天先跑通路径。请两组复验「路径跑通 + 不挂起」是否达成。

你以第三方独立评估身份（只读本地文件·无需 git pull·claude组 已 commit·本地工作区即最新）。**先独立取证再判断·保持客观立场·可挑战 claude组。**

## 必读
- docs/catch-ball/discuss/CB22d-地图标记失败_反评价收敛定稿_2026-08-10.md（定稿 plan）
- docs/catch-ball/discuss/CB22d-地图标记路径跑通_实施复验发起_2026-08-10.md（实施内容）
- 代码（git show ace4f8f 或读工作区）：
  - frontend/js/ai_qa/tools.js（names split·聚合名·amap_first·B1）
  - frontend/js/ai_qa/harness.js（B1 零 LLM 出口）
  - core/place_layer.py（_core_entities·forward 双路）
  - core/geocode.py（search_place amap_first）
  - api/routes.py（amap_first 参数）
  - tests/validate_generate_point_layer.py（9 断言）

## 待复验焦点（5 条·逐条 agree/disagree/partial + 证据 + 建议）
1. P0-0 前提：names split 正确？冷加载 20s 解决首用全 abort？rapidfuzz/pypinyin 装后质量提升？
2. P0-1 数据层：高德优先（amap_first）正确？A0 jieba 分词双路合理？聚合名放弃符合「无地点就放弃」？
3. P0-2 决策层（命门）：B1 零命中零 LLM 出口根治挂起？agentStep 30s 超时 + 流式 token 确认？还有停半途场景未覆盖？
4. 路径跑通 + 验收口径：真实数据用户能看到标记 + 未命中诚实文字？0 挂起（30s 内出口）达成？
5. 补充与异议：遗漏/更好做法？（准确度后续完善·本轮聚焦路径跑通）

核心标尺：演示逻辑链 · AI·Copilot 内核（Smart 两端+Dumb 中间+编排器确定性）· 出口三铁律 · 不造轮子。
承重红线：四态出口 / finalStep D019 / diagnose 永不动（增量豁免已登记）/ 追踪编号连续。
产出：docs/catch-ball/discuss/CB22d-地图标记路径跑通_实施复验_{你的组名}_2026-08-10.md
格式：〇 一句话结论 / 一 逐焦点复验 / 二 通过/需修正判定 / 三 补充建议。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-22d 地图标记路径跑通实施复验发起 · 2026-08-10*
*评估方只读本地不 git · 复验落 discuss/ · claude组 收到后收敛定稿*
