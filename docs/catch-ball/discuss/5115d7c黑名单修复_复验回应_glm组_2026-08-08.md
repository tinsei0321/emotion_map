# CB-19 5115d7c 黑名单误伤修复复验（glm组 · ZCode + GLM 5.2）

> **验证方**：glm组（ZCode + GLM 5.2）·第三方独立评估  
> **日期**：2026-08-08 | **对象**：commit 5115d7c（黑名单只拦单要素·多要素放行·治 T7 阻断）  
> **方法**：boundary-resolve.js 逐行审查 + geo_registry.py 后端黑名单审查 + Python 6 场景模拟（前后端各 6 例）+ pytest 303 passed

---

## 〇、一句话结论

**修复通过。前后端黑名单只拦单要素 GeoJSON（features.length===1 / len(polys)==1）——T7 多步链多要素行政区层放行 ✅·小溪塔单要素仍拒识 ✅·漏拦风险低（LLM 构造多要素法定功能区 GeoJSON 极罕见·且有 deriveAvailable 白名单 + preset 4 要素双重保险）。语义权衡合理（CB-14「EMC 不硬猜」+「用户上传层不受限」平衡）。pytest 303 零回归。建议接受。**

---

## 一、逐焦点

### 焦点 1：T7 多步链是否修复 — **agree 修复**

**代码核验**：

前端 `boundary-resolve.js:73`：
```javascript
if (!Array.isArray(feats) || feats.length !== 1) return null;   // 仅单要素·多要素放行
```

后端 `geo_registry.py:195`：
```python
if _name_col is not None and len(polys) == 1:   # 仅单要素·多要素放行
```

**T7 场景推演**：行政区层（4 要素：西陵/伍家岗/猇亭/点军）→ `features.length=4` → 前端 `:73` `!==1` → return null（放行）→ 后端 `:195` `len(polys)=4` → 不进黑名单检查（放行）。**T7 阻断消除**。

**Python 模拟确认**：多要素行政区层（含龙泉）→ 前端 blocked=False ✅ · 后端 blocked=False ✅

### 焦点 2：小溪塔单要素仍拒识 — **agree 拒识**

**代码核验**：

前端 `:73-78`：`feats.length === 1` → 进入检查 → `nm='小溪塔'` → `_ADMIN_BLOCKLIST.find(d => nm.includes(d) || d.includes(nm))` → 命中'小溪塔' → throw Error。

后端 `:195-199`：`len(polys) == 1` → 进入检查 → `nm='小溪塔'` → `next(d for d in _ADMIN_BLOCKLIST if d in nm or nm in d)` → 命中 → raise ValueError。

**Python 模拟确认**：单要素小溪塔 → 前端 blocked=True ✅ · 后端 blocked=True ✅

### 焦点 3：语义权衡（只拦单要素是否合理） — **agree 合理·漏拦风险低可接受**

**漏拦场景**：LLM 直传 2 要素法定功能区 GeoJSON（如 `{features:[{MC:'小溪塔'},{MC:'龙泉'}]}`）→ 前端 `length=2` → 放行 → 后端 `len(polys)=2` → 放行。

**风险评估**：

| 维度 | 分析 |
|------|------|
| **LLM 构造多要素 GeoJSON 的能力** | FC（function calling）的工具 schema 中 boundary 是 single value（preset_id \| GeoJSON）·LLM 不太可能主动构造多要素 FeatureCollection——FC 倾向传 preset_id 字符串或单要素 dict |
| **deriveAvailable 白名单** | 即使 LLM 直传多要素法定功能区·deriveAvailable（tools.js:621-622）的 FIXED_ADMIN_DISTRICTS 白名单仍过滤——deriveAvailable 返回 null → zonal 强制路由不触发 → 不走黑名单路径 |
| **preset fixture 4 要素** | CB-16 preset 清理后行政区层只含 4 真实区划（西陵/伍家岗/猇亭/点军）——LLM 从 preset 获取的 GeoJSON 不含法定功能区 |
| **三重保险** | ① preset fixture 清理（CB-16）+ ② deriveAvailable 白名单 + ③ 单要素黑名单——三道防线覆盖了绝大多数场景 |

**结论**：漏拦风险**低**。LLM 主动构造多要素法定功能区 GeoJSON 的概率极低·且有 deriveAvailable 白名单 + preset fixture 作为上游防线。**接受**——不过度防御（防「用户上传层不受限」被破坏更重要）。

**如果未来需要补充**（P3·非本次）：加一个"多要素全命中黑名单也拦"的检查——但当前 ROI 不够（场景极罕见 + 三重保险已覆盖）。

### 焦点 4：回归风险 — **agree 零回归**

pytest 303 passed + 5 skipped · 零回归 ✅

**遗漏场景检查**：
- preset_id 字符串路径（`:87` typeof !== 'string' → return b 原样）→ 不受影响 ✅
- 用户上传层（source=upload · 无 _source 标记）→ 前端 `:71` typeof === 'object' 但多要素 → 放行 ✅
- 单要素合法边界（如用户上传单个"深圳南山区"）→ `:77` nm='深圳南山区' → 不在黑名单 → 放行 ✅
- 后端 preset str 路径（`:159` isinstance str）→ 不进 dict 黑名单分支 ✅

---

## 二、可否接受结论

### 可否接受：**可以**

**依据**：
1. **T7 阻断消除**——多要素行政区层放行（前后端 `length !== 1` / `len != 1` 条件正确）
2. **小溪塔单要素仍拒识**——CB-14「EMC 不硬猜」对齐
3. **漏拦风险低**——LLM 构造多要素法定功能区极罕见 + deriveAvailable 白名单 + preset fixture 三重保险
4. **用户上传层不受限**——多要素 = 图层操作（可信）·单要素 = LLM 直传（需校验）
5. **pytest 303 零回归**

**语义总结**：

```
单要素 GeoJSON + 命中黑名单 → 拒识（LLM 直传法定功能区·CB-14）
单要素 GeoJSON + 不在黑名单 → 放行（LLM 直传合法区·或用户上传单要素）
多要素 GeoJSON → 放行（图层操作·无论是否含法定功能区）
preset_id str → 放行（走 load_preset·preset fixture 已清 4 要素）
```

**合理平衡**：CB-14 拦的是"LLM 编造不可信的单一法定功能区范围"·不拦"图层级操作（多要素整层）"和"用户上传"。

---

*glm组（ZCode + GLM 5.2）· CB-19 5115d7c 黑名单修复复验 · 2026-08-08*  
*验证基于：boundary-resolve.js :67-82 _rejectNonAdminBoundary + geo_registry.py :185-200 dict 黑名单 + Python 6 场景模拟（前后端各 6 例全 OK）+ pytest 303 passed。*
