# CB-22 · EMC 人工测试失败修复确认（先验后推）

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG 冷加载超时修复）
> **背景**：根因已定案（两组 trace 一致）·修复计划已批（用户：先修 RAG 再进 B·先走 CB 确认）·本文档发两组确认修复方案。
> **先验后推**：修复方案 R1-R5 → 两组确认 → 实施。

---

## 〇、根因定案（两组 trace 取证一致·已入库）

**冷加载 18.6s > AbortController 15s → fetch abort → RAG 结果从未注入 → finalStep 弱 context 情绪分析式幻觉**

```
问句 → _quickIntent 'rag_query'（分类正确·H4 证伪）
→ fetch /rag_search（F_015·冷加载 18.6s）
→ 15s abort（harness.js:982）→ _data=null（:987）→ 注入 if 不成立 → 从未注入
→ finalStep 用现有 ctx.context → LLM 弱 context 情绪分析式短答
```

**纠正诊断文档**：H1 旧代码不成立（F_015 触发=短路生效）·MOD_RANGE=grounding 预处理·H2 是「从未注入」非「覆盖」·分类正确走 rag_query。

## 一、修复方案（R1-R5·待两组确认）

### P0 · 治本（消除冷加载 + 诚实降级）
| # | 修复 | 位置 | 说明 |
|---|---|---|---|
| R1 | **serve 启动预热 RAG 模型** | `serve.py` + `rag_index.warmup()` | 启动后台加载·首检 <1s·消除 18.6s |
| R2 | **catch 兜底诚实标注** | `harness.js:1003` | 超时/失败 → ctx.context 注入"【知识库检索超时·本次未引用 RAG·可能不完整】"·非静默 |
| R3 | **防幻觉约束行** | finalStep 注入块 | RAG 不可用时约束："不要编造项目/数据/分析结论·如实说明"·防情绪分析式幻觉 |

### P1 · 容错 + 治本
| # | 修复 | 位置 | 说明 |
|---|---|---|---|
| R4 | **AbortController 15s→30s** | `harness.js:982` | 容冷加载（预热失效兜底）·禁无限延长 |
| R5 | **B 路径（CB-22b·后置）** | query_knowledge_base | "有哪些项目"走 B 确定性（无模型加载）→ 无超时·治本 |

### 其他
- R6：修正诊断文档事实 2（MOD_RANGE = grounding·非分类走错）
- R7：serve 静态缓存核对（硬刷新复测 module graph）

## 二、待两组确认焦点

1. **R1 serve 预热**：`serve.py` 启动后台预热 `rag_index.warmup()`（异步·不阻塞 serve 启动）——实现认可？预热失败降级？
2. **R2 catch 兜底**：超时/失败 → ctx.context 注入诚实声明（非静默）——措辞认可？是否会误导（用户以为失败）？
3. **R3 防幻觉约束**：RAG 不可用时的约束行——能否防情绪分析式幻觉？还是有更好机制（如直接 EXIT_CONCEPT 直答而非走 finalStep）？
4. **R4 超时 30s**：15s→30s——预热后是否仍必要？容错窗口合理？
5. **R5 B 路径**：确认先修 RAG 再进 B（分阶段）·B 是治本（无模型加载）——节奏认可？
6. **验证**：修复后复测「宜昌有哪些更新项目」→ F_015 <1s + 引用知识库 + 黄金集回归——验收标准认可？

## 附：发给两组的确认 prompt（A 组通用）

```
【CB-22 EMC 测试失败修复确认 · R1-R5】

背景：根因已定案（冷加载 18.6s > 15s 超时 → RAG 丢失）·修复计划已批（先修 RAG 再进 B）。
请你确认修复方案 R1-R5（先验后推·确认后实施）。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-EMC测试失败_修复确认_2026-08-09.md（本方案）
- 读 docs/catch-ball/discuss/CB22-EMC测试失败_{你的组}_2026-08-09.md（你之前的根因诊断）
- 核实：frontend/serve.py + tools/rag_index.py（_get_model）+ harness.js:976-1005（rag_query 分支）

第二步 · 确认焦点（agree/调整 + 证据）
1. R1 serve 启动预热（实现/失败降级）
2. R2 catch 兜底诚实标注（措辞/是否误导）
3. R3 防幻觉约束行（能否防情绪分析式幻觉·还是直接 EXIT_CONCEPT）
4. R4 AbortController 30s（预热后是否仍必要）
5. R5 B 路径节奏（先修 RAG 再进 B）
6. 验证验收标准

第三步 · 产出回应
- 落盘 docs/catch-ball/discuss/CB22-EMC修复_{组名}_{YYYY-MM-DD}.md
- 中文为主·引用 path:line·不 git
```

---

*claude组（2026-08-09）·EMC 测试失败修复确认·先验后推·待两组确认。*
