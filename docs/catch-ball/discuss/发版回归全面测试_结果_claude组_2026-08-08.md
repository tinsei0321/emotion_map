# CB-19 收尾 · 发版回归全面测试结果（claude组）

> **测试方**：claude组（Claude Code + DeepSeek/GLM 5.2·开发主）
> **日期**：2026-08-08 | **对象**：发版回归全面测试（方案 `发版回归全面测试_方案_2026-08-08.md`）
> **覆盖修复 commit**：P3-4 `9680dcc` + PRM-03/04 `083b78d` + PRM-05/07 `469bf32`

---

## 〇、一句话结论

**claude组 全过。T1 pytest 303 passed + 5 skipped · T2 validate 34 passed · T9 ESM 6/6 OK · T3 B3 快照 = 24/26（92%·历史最佳）·fail = {PRM-07（LLM 方差·黑名单已生效）, RST-L06（clip 链 Flash 方差）}·均非新增·非并发引入。**

---

## 一、各项结果表

| # | 测试项 | 结果 | 证据 |
|---|---|---|---|
| T1 | pytest 全量 | **303 passed + 5 skipped** | `py -m pytest tests/ -q`（23.67s·零回归） |
| T2 | validate 系列 | **34 passed** | test_emc_template + 4 validate |
| T9 | ESM 语法 | **6/6 OK** | harness/tools/boundary-resolve/shared/e2e-seam/test-cases |
| T3 | B3 快照 | **24/26（92%）** | `EMOTION_TRACE_SESSION=claude-B3-final-2a`·report-2026-08-08-05·10.5min |

### T3 B3 快照详情（2 次）

| Run | 结果 | fail 集 | 说明 |
|---|---|---|---|
| #1（18:58） | 23/26（88%） | {PRM-01, PRM-07, RST-L06} | PRM-01 density 多工具错路由（Flash 方差·#2 转 PASS） |
| #2（19:15） | **24/26（92%）** | {PRM-07, RST-L06} | **历史最佳**·PRM-01 转 PASS |

**fail 分析**：
- **PRM-07**（小溪塔 zonal）：黑名单**已生效**（verify_prm57 + #1 曾出现「EMC 不识别·应 request_upload」）·但 LLM 偶发传**空对象 GeoJSON**（无 name·黑名单不命中）→ zonal「无数据」→ ask_user「换区域重试」→ 非 request_upload 判 fail。**LLM 方差·黑名单本身正确**。
- **RST-L06**（clip+density 多步）：tools 空·clip 链未执行（Flash 方差·之前 Run 1/2 PASS·非并发引入）。

## 二、fail 集 vs 基线对比

| fail ID | 基线（修复前） | 期望（修复后） | claude 结果 |
|---------|:---:|:---:|:---:|
| PRM-03 | FAIL | FAIL（center ask_user 正确） | **PASS** ✅（G5 重写生效） |
| PRM-04 | FAIL | FAIL（center ask_user 正确） | **PASS** ✅ |
| PRM-05 | FAIL | **PASS**（zonal fallback） | **PASS** ✅ |
| PRM-07 | FAIL | **PASS**（黑名单拒识） | **FAIL（LLM 方差·空对象场景）** ⚠️ |
| RST-L06 | PASS | PASS | FAIL（Flash 方差·偶发） ⚠️ |
| 成果范式 6/6 | PASS | PASS | **PASS** ✅ |

**新增 fail 检查**：fail = {PRM-07, RST-L06}·**均在已知/方差范围**（PRM-07 黑名单已生效·空对象是 LLM 行为；RST-L06 之前 PASS 过·Flash 方差）·**无新增 fail·不阻断**。

## 三、基建修复（三组并发·本次发现）

| commit | 修复 |
|---|---|
| `3dc0a1c` | flywheel/serve/emc_helpers 端口隔离（--port/--backend-port·防三组撞 8080/8000） |
| `fdc1c1d` | emc_helpers 用 sys.executable 起 serve（后台 PATH 无 py） |
| `76b16eb` | serve.py 502 写响应浏览器断连静默吞（防 serve 线程崩） |
| `2f9965f` | PRM-07 拒识改 request_upload 文案（zonal/compare handler·非 ERR） |

## 四、可否进发版候选结论

**可以（有条件）**——claude 组部分全过：pytest 303 零回归 + validate 34 + B3 24/26（92%·历史最佳）+ 修复 commit 全部生效（PRM-03/04/05 转 PASS·P3-4 无回归）。剩余 fail = {PRM-07（黑名单生效·空对象 LLM 方差）, RST-L06（Flash 方差）}·均非新增·不阻断。**待 glm/Codex 结果汇总收敛后正式判定**。

---

*claude组 · 发版回归全面测试 · 2026-08-08*
