# 壳工程深度审计 · claude（正确性/测试/集成视角）

> zcode 派发（2026-08-23 深夜）· 双组交叉（Qoder=架构/安全/性能·本组=正确性/测试/集成）。审计对象 = 今日壳工程五文件 + 测试面。
> **总裁决（先行）**：**有条件通过**——无 P0；P1 一条（C3 question 超长 argv 越限 OSError 未捕获→500）；P2 集中两类（测试覆盖缺口 + provenance/caliber/hint 三层「生产者有·消费者无」断链）；修 P1 + 补 S5/S4 测试后可转通过。零实施零 git 写（本报告除外）。

## 一 白话摘要段

把新"门诊大厅"（壳）的核心零件挨个做了体检，重点看三件事：**零件之间对得上吗**（事件格式逐字段核对）、**出错时病人看得懂吗**（报错文案）、**出厂自检够不够**（测试有没有漏网）。结论：**骨架扎实、接线正确**——四族事件的"格式单"逐字段对上、三引擎切换隔离干净、追问按钮数据流无丢失无重复；但发现**三处"说了不做"的断链**（格式上写了"必带"的字段没人发、诚实性标记发了没人读、报错原因传了三层最后被丢弃），外加**一处真漏洞**（问题问得超长会直接把后端打崩成 500），以及出厂自检**只测了两个引擎、没测第三个**。总体：能收工，但建议把这几处补上再正式交付。

## 二 逐文件发现清单

### C1 acp-channel.js（事件契约合规）

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| C1-1 | **P2** | **caliber 摘要零发射**——契约 §5-2「tool.end 必带 caliber 摘要（scale/refs）」·schema 列为可选·S4 发射器与 BA 均不发 | [acp-channel.js:121-129](frontend/js/ai_qa/acp-channel.js#L121-L129) wire 构造无 caliber 键；[acp-contract-v1.md §5-2](docs/acp-contract-v1.md) 明写「必带」 | 二选一：发射层从工具结果补 caliber（沿 harness caliber 已有时·工程量大），或契约降级为「有则带」（S2 增补时定稿）——**勿让契约与实现长期相悖** |
| C1-2 | P3 | error wire 丢 hint——真实原因只在 bus 信封·wire 消费者（S6/远端）拿到的是无差别硬编码 message | [acp-channel.js:138-139](frontend/js/ai_qa/acp-channel.js#L138-L139) message 恒 '模型输出未能解析为可执行动作'·hint 不入 wire；error.schema hint 可选 | wire 补 `hint: String(text).slice(0,200)`（一行） |
| C1-3 | P3 | 多 observation 工具第二条起无 wire/toolcall_id——`_lastTc` 一次配对后置 null·后续 observation 不造型 | [acp-channel.js:123-129](frontend/js/ai_qa/acp-channel.js#L123-L129) | 契约粒度=每次调用·可接受；建议注释明示「首条摘要为准」防误读 |
| C1-4 | P3 | thought 子事件（tool.begin 族）无 wire——若未来把 S4 发射器纳入 wire dump·test_brain_adapter_wire 的「tool.begin 族必带 wire」断言会误伤 | [test_brain_adapter_wire.py:63-65](tests/acp_schema/test_brain_adapter_wire.py#L63-L65) 断言无 sub 判别 | 测试侧加 `e.sub==='thought'` 豁免或按子事件映射 |

**合规结论**：四族 wire 与 8 份 schema **逐字段吻合**（msg_delta kind/delta/session_id/turn_id/seq·tool_begin 五键·tool_end 六键·error 三必填——空值守卫正确：空 token 不造型、verb 缺失 wire=null、params_summary/result_summary 有长度钳制）。legacy 向后兼容成立（[harness.js:1097](frontend/js/ai_qa/harness.js#L1097) 一行判别·hooks 对象入参原样直用·e2e-seam 直测口不受影响）。

### C2 brain-adapter-dsh.js（BA 适配器）

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| C2-1 | **P2** | **dsh fetch 无总超时护栏**——/chat 有 CB-22i 的 Promise.race 强制超时（曾致用户干等 5 分半的根因类）·dsh_engine 无·后端/代理挂死时 ping 桩无限循环「已 Ns」永不收敛 | [brain-adapter-dsh.js:66-69](frontend/js/ai_qa/brain-adapter-dsh.js#L66-L69) 无 race·仅依赖后端 subprocess timeout 240s（代理层挂死时后端根本不接请求） | 补 `Promise.race(fetch, 后端超时+60s)` 类护栏（复用 api.js 范式） |
| C2-2 | P3 | ping 桩与全文批量的时间竞争——fetch 40ms 级返回时（node 单测路径）ping 可能一枪未发·「已 Ns」进度承诺在快返场景为空头 | [brain-adapter-dsh.js:56-60](frontend/js/ai_qa/brain-adapter-dsh.js#L56-L60) setInterval 首击在 pingMs 后 | 可接受（快返即直答·桩是慢场景保险）；无需改 |
| C2-3 | ✓ | 诚实性红线全守——msg.delta/tool.*/error 恒 synthesized·经 pytest 逐事件断言 | [test_brain_adapter_wire.py:71-77](tests/acp_schema/test_brain_adapter_wire.py#L71-L77) | 无 |

### C3 api/aiqa_routes.py（post_dsh_engine 段）

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| C3-1 | **P1** | **question 无长度上限**——Windows 命令行限 32767 字符·超长问句 subprocess 抛 OSError(WinError 206)·仅捕获 TimeoutExpired·→ 未处理异常 500·无 ok:False 语义化响应 | [aiqa_routes.py:137-148](api/aiqa_routes.py#L137-L148) 两个 subprocess.run 均只 except TimeoutExpired；`question: str` 无 max_length | ① DshEngineIn.question 加 `max_length=4000`（pydantic 422 语义化）② 顺手补 `except OSError` → ok:False |
| C3-2 | **P2** | 超时只杀直接子进程——Windows TerminateProcess 不杀孙进程·node bin.js 再 spawn 的 dsh 子进程超时后孤儿存活（僵尸风险） | [aiqa_routes.py:137-147](api/aiqa_routes.py#L137-L147) subprocess.run 标准行为 | 超时后补 `taskkill /T` 或 start_new_session+job object（随 Qoder C2 僵尸进程发现合并处置） |
| C3-3 | **P2** | 无并发闸——每请求 spawn 一个 node 进程（约 100MB 级）·并发 5 请求 = 5 进程 5 份内存 | [aiqa_routes.py:92](api/aiqa_routes.py#L92) 无锁无队列 | 轻量 semaphore（并发 2）或排队——壳阶段低频可挂账 |
| C3-4 | P3 | 全 200 + ok 字段——与 episode 惯例一致·但与同文件 post_search 的 502 抛错双风格并存（该处因前端 fallback 需要） | [aiqa_routes.py:69-71](api/aiqa_routes.py#L69-L71) vs [aiqa_routes.py:107](api/aiqa_routes.py#L107) | 沿惯例可接受·注记「远端 REST 客户端看 status code 会误判」 |
| C3-5 | ✓ | 错误响应语义化达标——empty question / dsh not found / timeout / returncode≠0 四路均 ok:False + error·stderr 400 字截断合理·npm shim 零注入面（argv 传参·shell=False） | [aiqa_routes.py:105-157](api/aiqa_routes.py#L105-L157) | 无 |

### C4 panel.js（今日 diff 段）

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| C4-1 | **P2** | **dsh 轮静态兜底追问不搭调**——BA diagnose card intent='dsh'·_followUps 无 dsh 分支→落入默认 emotion_analysis 分支→知识问答后弹出「深读归因/区域对比/热点分析」 | [panel.js:714-733](frontend/js/ai_qa/panel.js#L714-L733) 默认分支；[brain-adapter-dsh.js:45-47](frontend/js/ai_qa/brain-adapter-dsh.js#L45-L47) intent='dsh' | _followUps 补 `intent==='dsh'` 分支（知识类追问：如「换 kb_facts 精确查口径」）或 BA 复用 knowledge_qa intent |
| C4-2 | **P2** | 降级卡丢弃 hint——ERROR 处理器「固定降级卡·忽略传入文本」·dsh 不可用真实原因（如「本机未装 dsh」）对用户不可见·只剩通用换问法文案 | [panel.js:1609-1619](frontend/js/ai_qa/panel.js#L1609-L1619) | 降级卡正文前追加一行 hint（白名单枚举原因·非裸文本——既有「永不裸输」红线下的折中） |
| C4-3 | ✓ | chips 数据流无丢失/重复——多 TOOL_END 仅非空覆盖（保留上轮 cues）·截断 3·trim+过滤·trace 每轮重建防串轮·localStorage 持久化无害 | [panel.js:1551-1556](frontend/js/ai_qa/panel.js#L1551-L1556) | 无 |
| C4-4 | ✓ | 三引擎分发隔离正确——每 send 新建 channel+shell·旧引擎事件落旧 bus（各自闭包）·_streaming 互斥·abort 经 signal 传导 dsh fetch | [panel.js:1749-1763](frontend/js/ai_qa/panel.js#L1749-L1763) | 无 |
| C4-5 | P3 | 引擎模式在 send 内单次读·window.__EMC_ENGINE_MODE__ 旗标任意代码可改（仅测试位·生产走 URL） | [brain-adapter-dsh.js:14-24](frontend/js/ai_qa/brain-adapter-dsh.js#L14-L24) | 观察即可 |

### C5 tests/acp_schema/ + test_shell_smoke.py

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| C5-1 | **P2** | **S4 引擎发射器 wire 未过 S6 校验器**——契约验收条 1「任一形态实现的事件流过 S6 schema 校验器」仅 BA 形态履行·轻循环（主路）形态零 jsonschema 覆盖 | [test_brain_adapter_wire.py](tests/acp_schema/test_brain_adapter_wire.py) 只 dump BA | 仿 ba_wire_dump 建 s4_wire_dump（fake hooks 驱动 createEngineEmitter 全 14 方法）·同三断言 |
| C5-2 | **P2** | **S5 chips 三态零独立测试**——仅 S7 场景 2 happy path（2 cues 渲染+回填+不直发）·空 cues/ask 互斥/超 3 截断/非字符串过滤/优先级（cues>胶囊>静态）全无 | [test_shell_smoke.py:85-108](tests/browser/test_shell_smoke.py#L85-L108) | 仿 G0 R9 单测范式·e2e-seam 直调 harness 同级断言（零 LLM·确定性） |
| C5-3 | **P2** | **dsh 引擎 E2E 零覆盖**——三引擎只验 light/mock·dsh 全链（panel→runDshEngine→POST→后端 spawn→渲染）无自动化·本机 dsh 真实链路一次没跑过 | 全测试面检索无 dsh E2E | S7 冒烟加场景 4（?engine=dsh·fake fetch 或真后端·assert 桩事件+全文+seal） |
| C5-4 | P3 | schema 边界缺口：seq 负数/非整数未测·caliber refs 元素类型未测·delta 超长（>1MB）未测·params_summary 超 120 钳制未验 | [test_acp_schema.py:50-85](tests/acp_schema/test_acp_schema.py#L50-L85) 22 坏例清单 | 补 3-4 例（seq:-1·refs 含非串·delta 空串已测√） |
| C5-5 | P3 | 错误路径 DOM 渲染无测试——BA 单测覆盖事件层·但降级卡/（已停止）卡的 DOM 呈现未验 | — | 并入 C5-3 场景 4 的错误分支 |

**测试账目**：schema 校验 8 合法+22 坏例+1 齐全性 = 31 用例（派发称 30·以实为准）·BA wire 3 测试·冒烟 3 场景。坏例字段级定位断言（防误过）设计优秀·format_checker 必开（防 opened_at 假绿）·node 缺席 skip 不红——工程纪律好评。

## 三 横切发现

| # | 严重度 | 发现 | 链路 |
|---|---|---|---|
| X-1 | **P2** | **provenance 孤儿字段**——S4/BA 全事件发射带 provenance·panel 渲染零消费（唯一读者=测试断言）·契约 §5-1「前端对 synthesized 渲染为步进进度样式」未落地·当前仅靠文本自述（「（dsh 引擎思考中·已 Ns）」）+诊断卡诚实 | C1/C2 发射 → C4 渲染断链 |
| X-2 | **P2** | **error hint 三层全断**——bus 有 hint（真实原因）→ wire 无 hint → panel 降级卡忽略 hint·用户最终只看到通用文案·「语义化错误码+hint」的 hint 侧全链路空转 | C2/C1 → C4 |
| X-3 | P3 | session_id 跨引擎不一致——S4/BA='emc-shell'·mock='mock-session'·语义同（无真实 session 占位）但值不同·未来接真实 session 需 grep 三处 | C1/C2/mock |
| X-4 | P3 | 错误码无注册表——'DEGRADED_PARSE'/'DSH_ENGINE_FAIL' 硬编码散落两文件·契约「语义化错误码」无枚举权威源（对照 MCP 工具契约单一源纪律） | C1/C2 |
| X-5 | P3 | turn 族无 wire 无 schema——turn 事件（diagnose/seal/round）字段漂移无人看守·S2 增补前属已知盲面 | C1/C2/C4 |

## 四 测试覆盖缺口清单（汇总）

1. **P2** S4 发射器 wire 未过 S6 校验器（主路形态零契约校验）；
2. **P2** S5 chips 三态（空/ask 互斥/截断/过滤/优先级）零独立测试；
3. **P2** dsh 引擎 E2E 零覆盖（三引擎只验二）；
4. P3 schema 边界（seq 负数/refs 类型/超长载荷）；
5. P3 错误路径 DOM 渲染（降级卡/已停止卡）；
6. P3 并发事件面（ping 与 fetch 竞争已由 dump 隐式覆盖·三引擎切换竞态无显式测试）。

## 五 盲区自查

- **未实跑 dsh 真链路**：本机 dsh 安装/验证未做（spawn 路径仅代码审查）——C3-1 修复后建议真跑一次；
- **真引擎路径未验**：REAL_ENGINE_READY=true 需外网 DeepSeek（本机离线）——S4 真引擎 E2E 由联网机补；
- **harness 71 处 hooks 调用点**未逐一核对第三参（followup_cues）透传面——信任 S4「循环零改动」审计结论·未独立复验；
- **schema v1.1 增量未定稿**（provenance 不在 wire schema）——本审计按 v1 骨架比对·S2 增补后需复检；
- 内存/长会话压力面不在本视角（Qoder 覆盖）。

## 六 总裁决

**有条件通过**（四档：通过 / 有条件通过 / 退回 / 搁置）。

- **阻断转通过条件**：① C3-1（P1·question max_length + OSError 捕获）；② C5-1+C5-2 补测（S4 wire dump + S5 三态单测）。
- **并入排期**：X-1/X-2 断链修（provenance 渲染分支 + hint 展示）随 BA 二轮；C3-2/C3-3 随 Qoder 架构发现合并处置。
- **搁置观察**：C5-3 dsh E2E（待本机 dsh 环境就绪）；X-5 turn 族 schema（S2 增补一并）。
- 其余 P3 注记性项：不改不阻塞。

—— claude · 2026-08-23 · 家机 · 零实施
