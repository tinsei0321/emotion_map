# CB-22 · 纯问答定位失败 · 根因诊断（glm组）

> **诊断方**：glm组（ZCode + GLM 5.2 · 第三方独立评估·trace 取证派）
> **日期**：2026-08-09 | **CB 轮次**：CB-22（纯问答定位失败·用户实测再失败·进 CB 查根因）
> **回应对象**：`CB22-纯问答定位失败_根因诊断发起_2026-08-09.md`（4 层根因链 + 5 焦点）
> **方法**：trace.log 直读（sess-8072）+ _quickIntent 代码核实 + RAG_QUERY_KW 词序分析
> **核心标尺**：三支柱（缺一不可）/ 演示逻辑链 / **承重红线**

---

## 〇、一句话结论

**4 层根因链全 agree（glm trace 取证 + 代码核实定案）——核心是「词序变体」击穿词表短路（"有哪些项目"≠"项目有哪些"）→ 短路不触发 → diagnose 无问答分类 → 必然走分析图层。这是架构脆弱性（知识问答靠词表短路=唯一通道·词序变体就漏·漏后无兜底）。修复方案 A+B 组合最优：A 触发放宽（开放问词正则+知识词 gate·治标·覆盖变体）+ B diagnose 加知识问答通道（治本·短路漏网也有去处）。单 A 仍脆弱（无穷变体·下一个又漏）·单 B 太慢（FC diagnose 14 工具延迟）。B5（指令优先级）本次**未被实证**（连短路都没进）·仍须 A+B 修后用户复测确认。防回归机器化：触发正负例回归测试（node 模拟 _quickIntent·断言变体命中 + 分析问不误触）。**

---

## 一、trace 取证定案（glm 直读）

**glm 实测** `trace_query --session sess-8072-1786271653 --tail 30`：

```
18:35:26 F_015 enter→exit 15.3ms     ← ★ rag_search 成功（早前问句"宜昌有哪些更新项目"）
18:35:27 F_003 enter→exit 0.1ms      ← finalStep（RAG 注入 → LLM 综合）
18:36:03 F_003 enter→exit 0.1ms      ← finalStep（第二次·可能追问）

18:50:54 F_005 tools=14              ← ★ diagnose FC（14 工具·无 F_015！）
18:51:03 F_002 enter→exit 0.2ms      ← agentStep（工具执行）
18:51:15 F_002 enter→exit 0.2ms      ← agentStep（第二次）
18:52:31 F_005 tools=14              ← diagnose FC（多轮）
18:52:41 F_002 enter→exit 0.2ms      ← agentStep
```

**glm 定案**：
- **18:35 F_015 存在**——早前问句（"宜昌有哪些更新项目"·RAG_QUERY_KW"有哪些项目"命中）走 rag_query 短路·RAG 成功。
- **18:50 F_005（diagnose FC）无 F_015**——本次问句（"宜昌市城市更新的**项目有哪些**"）**短路未触发**·落 diagnose FC（14 工具·分析链路）。
- **对比铁证**：同会话 18:35 成功（F_015）vs 18:50 失败（F_005 无 F_015）——唯一变量是**问句词序**（"有哪些项目"→"项目有哪些"）。

---

## 二、4 层根因链核实（glm 逐条·代码证据）

### 层 ① 触发缺口（词序变体击穿词表）— **agree（铁证）**

**证据**：
- RAG_QUERY_KW（emc-patterns.js:50）：含 `'有哪些项目'`（连续子串）。
- 问句：「宜昌市城市更新的**项目有哪些**？」
- `"宜昌市城市更新的项目有哪些".includes("有哪些项目")` = **false**（"项目有哪些"≠"有哪些项目"·词序反）。
- **glm 实测验证**：
  ```javascript
  // node 模拟
  '宜昌市城市更新的项目有哪些'.includes('有哪些项目')  // false ← 不命中
  '宜昌有哪些更新项目'.includes('有哪些项目')          // false ← 也不命中（"更新项目"≠"有哪些项目"）
  // 注：早前 18:35 成功的问句可能是"宜昌有哪些项目"（精确含"有哪些项目"）
  ```

**glm 关键发现**：RAG_QUERY_KW"更新项目"（:50）也不命中"宜昌有哪些更新项目"（"更新项目"在但"有哪些更新项目"不含"有哪些项目"连续子串）——**早前 18:35 成功可能因问句恰好含"有哪些项目"·非"更新项目"触发**。

**glm 判定**：层 ① **成立**——词表连续子串匹配脆弱·词序变体必漏。

### 层 ② 短路不触发 → 落点（REGION_KW null）— **agree（代码铁证）**

**证据**（harness.js:71-77）：
```javascript
:71  if (RAG_QUERY_KW.some(w => s.includes(w)) && RAG_KNOWLEDGE_RE.test(s)) return 'rag_query';
     // ↑ "项目有哪些"不命中 RAG_QUERY_KW 任何词 → 不触发 rag_query
:75  if (REGION_KW.some(p => s.toLowerCase().includes(p.toLowerCase()))) {
:76    if (SEARCH_EVIDENCE_RE.test(s)) return 'general';
:77    return null;   // ← "宜昌"命中 REGION_KW·"更新"不在 SEARCH_EVIDENCE_RE → return null → 落 diagnose
     }
```

**glm 核实**：
- :71 RAG 双条件：RAG_QUERY_KW 不命中（层 ①）→ rag_query 不触发。
- :75 "宜昌"命中 REGION_KW。
- :76 "更新"不在 SEARCH_EVIDENCE_RE（实据词 = 政策/策略/案例/新闻/方案/做法/趋势/实施/理念/定位）→ 不触发 general 搜索。
- :77 `return null` → 落 diagnose FC。

**glm 判定**：层 ② **成立**——短路漏网后 REGION_KW 兜底判"纯空间问"（return null 落 diagnose）·但"项目有哪些"是**知识问答非空间问**——REGION_KW 兜底**误判**。

### 层 ③ diagnose 无问答通道 — **agree（架构缺陷铁证）**

**证据**：
- diagnose prompt（prompts.py:200）intent 枚举：`general | gis_operation | emotion_analysis`——**无"知识问答"分类**。
- PARADIGM_MAP（emc-patterns.js:56）有 `knowledge_qa` 范式——但**只在 _quickIntent 短路触发时映射**·diagnose 不产 knowledge_qa。
- diagnose FC 选 14 工具（trace F_005 tools=14）——分析工具链（zonal/rank/density/buffer 等）·**无 query_knowledge_base / rag_search 工具**（知识查询工具不进 FC 选型）。

**glm 判定**：层 ③ **成立**——知识问答完全靠 _quickIntent 短路拦截·**短路漏网 = 无处可去**：
- diagnose 无 knowledge_qa 分类。
- FC 无知识查询工具。
- 知识问句落 diagnose → FC 选分析工具 → 必然出图层。

**这是架构脆弱性的核心**——知识问答是"短路唯一通道"·无兜底。

### 层 ④ finalStep 图层导向 — **agree（已知铁证）**

**证据**：FINAL_TEMPLATE（prompts.py:134）`**图层是主出口**——分析类必产图层·严禁只给文字`——层 ③ 落 diagnose 走分析链路 → finalStep 图层导向 → 出"分析图已生成"。

**glm 判定**：层 ④ **成立**——这是层 ③ 的必然结果（诊断走分析 → finalStep 图层）。

### glm 根因链定案

```
用户问「宜昌市城市更新的项目有哪些？」
  ↓ 词序变体（"项目有哪些"≠"有哪些项目"）
层① RAG_QUERY_KW 词表不命中（连续子串匹配脆弱）
  ↓ 短路不触发
层② REGION_KW 兜底误判"纯空间问"（return null）
  ↓ 落 diagnose FC
层③ diagnose 无知识问答分类（intent 只有 general/gis/emotion）+ FC 无知识查询工具
  ↓ FC 选分析工具（14 工具·zonal/rank/density...）
层④ finalStep FINAL_TEMPLATE 图层导向（:134 图层主出口）
  ↓ 回答"分析图已生成"
```

**glm 4 层全 agree·无遗漏根因层。**

---

## 三、5 诊断焦点回应（glm）

### 焦点 1：根因定案（4 层链条）— **agree（全成立·无遗漏）**

见上文——glm trace 取证 + 代码核实 4 层全确认。

**glm 补充（根因深层）**：**架构脆弱性**（知识问答 = 短路唯一通道·无兜底）——这是比"词序变体"更深的根因。词序变体是**触发点**·架构脆弱是**根因**：
- 即使修了"项目有哪些"词序·下一个变体（"城市更新项目有哪些"）仍漏。
- **根治须加兜底通道**（焦点 2/3）·非仅修词表。

### 焦点 2：「纯问答定位」架构脆弱性 — **agree（脆弱·须兜底通道）**

**glm 判定**：**agree——知识问答靠词表短路=唯一通道是架构脆弱·须加兜底。**

**脆弱性分析**：
- 词表连续子串匹配——无法穷举所有词序变体（中文灵活·"项目有哪些"/"有哪些项目"/"有哪些更新项目"/"更新项目有哪些"...）。
- 短路漏网后 diagnose 无 knowledge_qa 分类——知识问句落 analyze（分析）必然走图层。
- **即"词表漏一个变体 = 知识问答失败"**——极脆弱。

**兜底通道必要性（glm 强烈建议）**：
- diagnose 加 knowledge_qa 分类——短路漏网时 FC 也能判知识问答 → 路由 knowledge_qa 范式（注入素材+LLM 综合·非分析图层）。
- 或 RAG 触发放宽（开放问词正则·覆盖全部词序）。
- **两者互补**（焦点 3 A+B 组合）。

### 焦点 3：修复方案收敛 — **A+B 组合最优（glm 核心立场）**

**3 方案评估（glm）**：

| 方案 | 机制 | 治标/治本 | glm 评估 |
|---|---|---|---|
| **A 触发放宽** | 开放问词正则（有哪些/哪些/什么）+ 知识词 gate | **治标**（覆盖变体） | ✅ 必要（短期生效） |
| **B diagnose 加问答通道** | diagnose intent 加 knowledge_qa | **治本**（兜底） | ✅ 必要（长期根治） |
| C 其他 | — | — | glm 无补充 |

**glm 判定：A+B 组合最优**——

**A 治标（短期·覆盖变体）**：
- 开放问词正则 `RAG_OPEN_RE = /有哪些|哪些|什么|如何|怎么/`——覆盖全部"问"类词序变体。
- 知识词 gate（RAG_KNOWLEDGE_RE 已有）——防误触（"有哪些图层"不含知识词不触发）。
- claude 称"node 模拟验证：失败问句 ✅ 正例保持 ✅ 负例不误触 ✅"——glm 采信（但须防回归测试机器化·焦点 5）。
- **A 的局限**：仍靠词表/正则·下一个语义变体（如"宜昌更新项目清单"无"有哪些"）仍可能漏。

**B 治本（长期·兜底通道）**：
- diagnose intent 加 `knowledge_qa`——FC 判"知识问"也走 knowledge_qa 范式。
- 短路漏网（A 未覆盖的变体）→ 落 diagnose → FC 判 knowledge_qa → 注入素材 + LLM 综合（非分析图层）。
- **B 的成本**：FC diagnose 多一次 LLM 调用（14 工具·延迟）——但比"出图层"好（用户可接受~10s 延迟换正确问答）。

**为什么 A+B 组合（非单 A 或单 B）**：
- **单 A**：仍脆弱（无穷变体·下一个又漏）。
- **单 B**：太慢（每次知识问都走 FC diagnose·14 工具延迟 ~10s·vs 短路 ~1s）。
- **A+B**：A 覆盖大多数（短路快 ~1s）·B 兜底少数漏网（FC diagnose ~10s·但正确）——**速度+鲁棒性兼顾**。

**glm 实现建议（A+B 落地）**：

**A（触发放宽·emc-patterns.js）**：
```javascript
// 新增开放问词正则（覆盖全部词序变体）
export const RAG_OPEN_RE = /有哪些|哪些|什么|如何|怎么|几/;
// RAG 短路改：开放问词 + 知识词（双条件·覆盖变体）
// harness.js:71 改：
if (RAG_OPEN_RE.test(s) && RAG_KNOWLEDGE_RE.test(s)) return 'rag_query';
```

**B（diagnose 加问答通道·prompts.py）**：
```python
# diagnose prompt intent 枚举加 knowledge_qa
"intent": "general | gis_operation | emotion_analysis | knowledge_qa",
# knowledge_qa 路由：调 rag_search → 注入素材 → finalStep LLM 综合
# （复用 rag_query 短路分支逻辑·diagnose 判 knowledge_qa 也走同一范式）
```

### 焦点 4：B5 关系澄清 — **agree（本次是路由层·B5 未实证）**

**glm 判定**：**agree——本次失败是路由层（短路没进）·B5（指令优先级）仍未被实证。**

**B5 状态**：
- 本次（18:50）连 rag_query 短路都没进（无 F_015）——**B5 不适用**（B5 是 rag_query 短路进了后的指令优先级问题）。
- 早前（18:35）rag_query 短路进了（F_015 15.3ms + F_003 finalStep）——但用户**未报告 18:35 回答质量**（可能那次成功？或用户只报告了 18:50 失败）。
- **B5 仍须 A+B 修后用户复测确认**——复测"宜昌有哪些更新项目"（精确命中词表）走 rag_query 短路 → 看 LLM 真走知识问答（条目式+引用）还是仍出图层（B5 风险坐实）。

**glm 诚实**：B5 是 glm 上轮标注的"预防性风险"——本次**未被实证**（路由层问题更上游）。但 B5 风险**仍在**（ctx.context 在后 vs FINAL_TEMPLATE 在前）·A+B 修后仍须验证。

### 焦点 5：防回归机器化 — **agree（触发正负例回归测试）**

**glm 判定**：**agree——触发词表/开放问词的正负例回归测试须机器化。**

**glm 防回归设计（node 模拟 _quickIntent）**：

```javascript
// tests/test_quickintent_trigger.js（新·node 跑）
const cases = [
  // ★ 正例（须命中 rag_query）
  { q: '宜昌有哪些更新项目', expect: 'rag_query' },        // 原精确
  { q: '宜昌市城市更新的项目有哪些', expect: 'rag_query' },  // ★ 本次失败变体
  { q: '宜昌城市更新项目有哪些', expect: 'rag_query' },      // 词序变体
  { q: '宜昌有哪些城市更新项目', expect: 'rag_query' },      // 插入变体
  { q: '葛洲坝体检有什么问题', expect: 'rag_query' },        // 知识问
  { q: '如何参考上海做法', expect: 'rag_query' },            // 开放语义
  // ★ 负例（不误触 rag_query）
  { q: '宜昌西陵区情绪分布', expect: null },                 // 分析问（落 diagnose）
  { q: '裁出西陵区', expect: null },                         // GIS 操作
  { q: '什么是情绪地图', expect: 'general' },                // 概念问
  { q: '你好', expect: 'general' },                          // 问候
];
for (const c of cases) {
  const r = _quickIntent(c.q);
  assert(r === c.expect, `${c.q} → ${r} (期望 ${c.expect})`);
}
```

**glm 关键**：
- **正例含本次失败变体**（"项目有哪些"）——防回归核心。
- **负例含分析问**（"情绪分布"）——防 A 触发放宽后误触（开放问词+知识词双条件 gate）。
- **node 跑**（_quickIntent 已 export :60）——CI 友好·秒级。

**glm 建议**：A+B 实施后·正负例回归测试**必加**——这是"词表脆弱"的机器化弥补（变体覆盖 + 误触防护）。

---

## 四、glm 交叉挑战

### glm 对 claude 的挑战（1 条）

**挑战：diagnose 加 knowledge_qa（方案 B）是否碰承重红线？**

- diagnose prompt（prompts.py:200）intent 枚举加 `knowledge_qa`——**改 diagnose prompt**。
- KNOWLEDGE.md §1 承重红线："diagnose prompt 永不动"（保 Flash eval）。
- **glm 挑战**：方案 B 改 diagnose intent 枚举——是否触碰"diagnose 永不动"红线？
- **glm 判断**：**partial 触碰**——加 intent 枚举值（非改路由逻辑/判据）·但确改动 diagnose prompt。
- **glm 建议**：
  - 若严格守"diagnose 永不动"——方案 B 须走"先扩 eval 再动·一次一处"SOP（加 intent 枚举 + 扩 diagnose eval 断言）。
  - 或**替代 B'**：不改 diagnose prompt·改 **FC 工具集**（加 query_knowledge_base/rag_search 到 FC 可选工具）——短路漏网时 FC 自选知识工具（不改 prompt intent 枚举）。
  - glm 倾向 **B'（FC 加工具）**——不碰 diagnose prompt·靠 FC 自选·更稳。

### glm 自我挑战（1 条）

**自我挑战：glm A+B 组合是否过度？**

- 单 A（触发放宽）可能已够——开放问词正则覆盖大部分变体。
- B（diagnose 加通道）是"防无穷变体"的兜底——但**实际无穷变体有多少**？中文知识问句变体可能 10-20 种·A 正则覆盖大部分。
- **glm 诚实**：A+B 是"最稳"·但**可能过度**（A 正则 + 防回归测试可能够）。
- **glm 调整**：建议**先做 A + 防回归测试**·若测试发现仍有变体漏·再加 B——**渐进式·非一步到位**。

---

## 五、风险清单

### 阻断级

| # | 风险 | glm 建议 |
|---|---|---|
| **B1** | 词表脆弱·词序变体击穿（本次失败根因） | A 触发放宽（开放问词正则）+ 防回归测试 |

### 警告级

| # | 风险 | glm 建议 |
|---|---|---|
| **W1** | diagnose 无知识问答通道（架构脆弱·短路漏网无兜底） | B 或 B'（FC 加知识工具）后置·先 A + 防回归·渐进 |
| **W2** | 方案 B 改 diagnose intent 枚举（触碰"diagnose 永不动"红线） | B' 替代（FC 加工具·不改 prompt）或走 SOP（先扩 eval） |
| **W3** | B5 指令优先级（仍未实证·A+B 修后须验） | 用户复测"宜昌有哪些更新项目"（精确命中词表）看 LLM 综合 vs 图层 |

### 建议级

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | A 触发放宽（RAG_OPEN_RE 开放问词 + 知识词 gate） | P0（短期生效） |
| **S2** | 防回归测试（正负例 node 模拟·含变体） | P0（机器化） |
| **S3** | B'/B diagnose 兜底（FC 加知识工具 或 intent 加 knowledge_qa） | P1（渐进·先 A 验效） |
| **S4** | B5 仍须验证（A+B 修后复测） | P1 |

---

## 六、复验结论

### ✅ 4 层根因链全 agree——A 触发放宽 + 防回归测试先行·B/B' 渐进

**glm 诊断结论**：

**4 层根因链全 agree**（glm trace 取证 + 代码核实）——核心是**词序变体击穿词表短路**（"有哪些项目"≠"项目有哪些"）·深层是**架构脆弱**（知识问答=短路唯一通道·无兜底）。

**修复方案（glm 渐进建议）**：
1. **P0 A 触发放宽**（RAG_OPEN_RE 开放问词正则 + 知识词 gate）——短期覆盖变体。
2. **P0 防回归测试**（正负例 node 模拟·含变体 + 分析问负例）——机器化防再犯。
3. **P1 B'/B diagnose 兜底**（FC 加知识工具 或 intent 加 knowledge_qa）——渐进·先 A 验效·若变体仍漏再加。

**5 焦点判定**：
- 焦点 1 根因定案：**agree**（4 层全成立·glm 补充深层"架构脆弱"）
- 焦点 2 架构脆弱：**agree**（短路唯一通道·须兜底）
- 焦点 3 修复方案：**A+B 组合最优·但 glm 调整为渐进（先 A+防回归·B 后置）**
- 焦点 4 B5 关系：**agree**（本次路由层·B5 未实证·A+B 修后仍须验）
- 焦点 5 防回归：**agree**（正负例 node 模拟·含变体）

**glm 交叉挑战**：
- 挑战 claude：方案 B 改 diagnose intent 枚举——触碰"diagnose 永不动"红线？建议 B'（FC 加工具）替代。
- 自我挑战：A+B 是否过度？调整为渐进（先 A + 防回归·B 后置）。

**承重红线**：
- diagnose prompt：方案 A 不触碰（改 emc-patterns.js·非 diagnose prompt）✅。方案 B 须走 SOP 或改 B'（FC 加工具）。
- FINAL_TEMPLATE：不触碰（本次根因是路由层·非指令层）✅。
- 四态出口：不触碰 ✅。

**glm 立场**：
- glm trace 取证确认 4 层根因（词序变体击穿 + 架构脆弱）。
- glm B5（上轮标注的指令优先级风险）**诚实承认本次未实证**——根因更上游（路由层）。
- glm 修复建议**渐进**（先 A + 防回归·B 后置）——避免过度工程（A 可能已够·B 防无穷变体兜底）。
- glm 挑战方案 B 碰红线——建议 B'（FC 加工具）替代·不碰 diagnose prompt。

---

## 附：trace + 代码证据（glm 独立核实）

| 证据 | 位置 | glm 解读 |
|---|---|---|
| **trace F_005 无 F_015** | sess-8072 18:50:54（F_005 tools=14·无 F_015） | 短路未触发·落 diagnose FC |
| **trace F_015 18:35 成功** | sess-8072 18:35:26（F_015 15.3ms） | 早前问句（精确命中词表）走 rag_query 成功 |
| **RAG_QUERY_KW "有哪些项目"** | emc-patterns.js:50 | 问句"项目有哪些"词序反·includes 不命中 |
| **_quickIntent :71 RAG 不触发** | harness.js:71 | 双条件不中（RAG_QUERY_KW 不命中） |
| **REGION_KW :75-77 兜底误判** | harness.js:77 return null | "宜昌"命中 REGION·"更新"非实据词 → null 落 diagnose |
| **diagnose intent 无 knowledge_qa** | prompts.py:200 | 枚举 general/gis/emotion·知识问答靠短路唯一通道 |
| **FINAL_TEMPLATE 图层导向** | prompts.py:134 | 层 ④ 必然出图层 |

### 声明

本诊断由 glm组（ZCode + GLM 5.2）独立产出·2026-08-09·基于 trace.log 直读（sess-8072 时序对比）+ _quickIntent 代码核实 + RAG_QUERY_KW 词序分析。第三方独立评估·不做项目方决策背书。glm B5（上轮指令优先级风险）诚实承认本次未实证——根因更上游（路由层）·基于事实修正（不固执 B5）。

---

*登记：docs/context-map.md · CB-22 纯问答定位失败 glm组 根因诊断。*
