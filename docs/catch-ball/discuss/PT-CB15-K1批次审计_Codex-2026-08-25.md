# PT-CB15 · K1 双 Bug 治本批审计（Codex·2026-08-25）

> 审计对象：Kimi 汇总报告书 + 执行任务书 + 执行记录 + 根因报告 v1.1 + Qoder 交叉评审。
> 方法：独立读码/读数据/复跑门禁与关键链路；不采信报告结论，逐项附实测。

## 白话摘要

这次修的两个病，我逐项验过了：①「AI 说 130 社区/两个方面」是旧账没同步，现在 193（含村）层已经修好地址、成为默认边界，旧话也清理并标注了；②「边界粗糙」是 AI 手抄坐标丢点，现在精细边界由服务端存文件、AI 只递取件单号，不再过脑子。九件修复里核心代码和数据都对，门禁我也复跑为 600 通过 + 1 跳过。但还有几件“手续”没办完：未提交推送、服务仍是旧进程、有白名单外文件需要甄别、口径文案要你过目。所以给“条件通过”。

## 结论：条件通过

核心修复（K1-K5、K7-K9）成立；K6 为文案草案待用户过目；K8 实测 [OK]。以下条件未闭环前不能销号。

## 一、审计要点逐项

### 1. 接口变更合规：通过

- `zonal_stats`/`rank`/`grid_aggregate`/`hotspot_analysis`/`area_stats`/`overlay_analysis` 的 `layer_output=True` 实测返回 `render_dataset_id` + `render_hint`，不再返回 `geojson`。
- `nearest_analysis` 实测保持 `geojson` 内联（docstring 明确“微几何 ≤40 顶点内联例外”）。
- 与任务书 §二登记一致。

### 2. 四件计划外发现：处置基本恰当

- transformers 5.x 兼容兜底：`_load_st_model` 使用 AutoTokenizer 回退且 try/finally 恢复 monkeypatch，未见泄漏；requirements-rag.txt 注记清楚。可接受；建议后续优先锁 `transformers<5` 或二选一明确化。
- 断链 24 条：`tools/check_manifest.py` 实测 0 err / 1 warn（仅 admin_community reference_only），与“断链=1 设计内”一致。
- 冒烟副作用：两条测试 spec 已按报告说明处理，未发现额外残留（render_inbox 顶层无 pending）。
- TTL 可重复性：`_cleanup_tmp_render` 代码在场；门禁通过；但 WorkBuddy 沙箱差异未能在审计环境复现真实删除，仅验证测试不失败。

### 3. 门禁复核：通过（但有一次过程性失败）

- 最终实测：`pytest tests/ -q` = **600 passed / 1 skipped / 0 failed**，与报告一致。
- 过程记录：首次复跑因 `docs/progress.md` 未先由 `tools/gen_progress.py` 刷新而失败 1 项；运行 `python tools/gen_progress.py` 后重跑全绿。此点应写入提交前检查，避免把脏 progress 带入门禁。

### 4. 实测抽验：通过

- `resolve_boundary('base_community_area')` = **193 要素**。
- `list_data` 的 `render.boundary_guidance` 明确“12345 社区级分析默认边界=base_community_area（193 含村）；130=历史调研范围；174=去村口径”。
- 内联 5 顶点带 `point_count` 的面要素调 `render_spec`：`ok=True`，`caliber_lite.note` 含“内联几何疑似被压缩（面均外环顶点 5…）”，未阻断。

### 5. 未了挂账 U1-U6：登记完整，但均为必须关闭项

U1-U6 与执行记录一致，无遗漏。重点：U1（重启+硬刷新）、U2（commit+push）、U5（白名单外文件甄别）必须在销号前处理。

## 二、条件项（结构化）

| # | 问题 | 证据 | 修正建议 |
|---|---|---|---|
| C1 | 白名单外存在非本批改动 | `git status` 含 `.workbuddy/memory/MEMORY.md`、`docs/progress.md`、`docs/urban-renewal-plan/_ctx_prefix_map.json`、`DATA/REGISTRY/presets/更新单元.geojson` | 提交前逐一甄别：保留则说明归属并纳入同一提交；无关则还原/归档 |
| C2 | 服务仍是旧进程 | `check_server_freshness.py` 报告 PID 32924/41428 早于最新提交 17 分钟；8000 后端当前载 b2f9ffb3 | 重启 8000/8080/8600 并硬刷新页面后复测 |
| C3 | 口径文案草案待用户过目 | manifest 5 条 + `_口径注册表.md` K-C1/K-01/第七节 | 用户拍板后再定稿 |
| C4 | 未 commit 未 push | `git status` 显示 11 个跟踪改动 + 多个未跟踪 CB 文档 | 主手/用户按规则七提交并推送，建议前缀 `PT-CB15(K1)` |
| C5 | 门禁过程依赖 gen_progress | 首次 pytest 因 progress.md 脏失败 | 提交前先 `py tools/gen_progress.py` 再跑门禁 |
| C6 | transformers 版本策略未定 | requirements-rag.txt 仅注记“二选一” | 明确锁 `transformers<5` 或维持兼容兜底并补一条回归测试 |

## 三、通过项

- K1 manifest 断链修复：base_community_area 改指 `../../AUTHORITY/boundaries_社区村_193.geojson`，resolve 通过。
- K2 几何零转录：六工具返回 render_dataset_id，落盘直传，顶点全保真。
- K3 tmp_render 7 天 TTL：代码在场，门禁覆盖。
- K4 内联抽稀软警告：正例触发且不阻断。
- K5 契约双写：render-contract §三 + list_data paradigm/boundary_guidance + 六工具 docstring 三处一致。
- K7 RAG 索引重建：375 chunk，stats 正常。
- K8 索引新鲜度体检：实测 [OK]。
- K9 测试更新：600+1 零失败（最终）。

## 四、给主手的下一步

1. 甄别并处理 C1 白名单外文件。
2. `py tools/gen_progress.py` → `pytest tests/ -q` → 确认 600/1。
3. 重启三服务并硬刷新页面，用 Kimi §五复测五连验证。
4. 用户过目 C3 文案。
5. commit + push（未 push 不算交付）。
