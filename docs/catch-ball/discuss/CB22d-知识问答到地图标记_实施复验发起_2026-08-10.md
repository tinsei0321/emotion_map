# CB-22d · 知识问答 → 地图标记 — 实施复验发起（generate_point_layer）

> **发起方**：claude组（Claude Code·家环境）| **日期**：2026-08-10 | **性质**：CB 实施复验轮（代码已实施·两组复验后定稿）
> **承接**：反评价收敛定稿（修正版 plan·7 项缺陷全核实）→ 用户批准实施（主路径 POI+逆地理·GIS 仅甄别增强·绝不强行匹配）→ **claude组 实施完成（commit `fde2595`）**
> **目的**：① 两组复验实施是否符合修正版 plan（P0-0 接线 + P0-1 工具 + P0-2/3 契约镜像 + P0-4 测试）② 两组确认修复有效（工具能触发·不是"实现了却从不被触发"）③ 通过后定稿

---

## 〇、一句话结论

**generate_point_layer 已按修正版 plan 实施（POI+逆地理主路径·GIS 甄别增强·7 项缺陷全修）·305 passed 零回归·请两组复验确认修复有效。**

---

## 一、实施内容（commit `fde2595`·17 文件 1001 行）

### P0-1 工具层（tools.js）
- `generate_point_layer({names, as})`：批量地名 → 点位图层
  - 主路径 = search_place（本地 4490 POI + 高德逆地理兜底）·绝不强行匹配
  - 命中 → 橙色 circle 点位图层（name/source/match_type 属性·溯源铁律）
  - 未命中 → 诚实文字列出（不编造坐标·不 request_upload）
  - 并发：上限 50/并发 ≤10/单请求 5s 超时（防 55 名挂死）

### P0-2/3 契约 + 镜像 + 豁免
- `tool_contracts.py` 新增契约（`when` 含「标记/地图/点位」诱导·FC 走 when 不走铁律）
- `paradigm.py` GEO_TOOL_CATALOG + TEMPLATE_REGISTRY
- `stages.js` SKILL_DEFS（names 必填）
- KNOWLEDGE §1 登记 diagnose 第二次增量豁免（3 条件）

### P0-0 接线（7 项缺陷全修）
| 缺陷 | 修复 |
|---|---|
| glm B.1 resume 恒 false | `_markupCue` FC 前置路由注入（不依赖 resume·上轮 knowledge_qa+标记词→FC 提示选新工具 + 注入上轮 final 片段） |
| glm B.2 FC 走契约 when | `when` 含诱导词（标记/地图/点位）·prompts 铁律保留不作主依赖 |
| glm F.1 names 无来源 | `final_excerpt`（上轮回答片段供 FC 提取项目名）+ `rag` 识别（quick-rag 轮 priorTurn 修复） |
| Codex A-1 ask_user 陷阱 | runTemplatePath 零图层诚实出口（`generate-point-layer-no-hit`·exit='answered'） |
| Codex A-2 _dataGate 误拦 | `_markupCue` 时跳过 _dataGate |
| Codex A-3 面化白名单 | 本实现 POI 主路径·不依赖 FIXED_ADMIN_DISTRICTS |
| Codex A-4 SLOT_HINT | names 必填契约·缺则提示从上一轮提取 |

### P0-4 测试
- `validate_generate_point_layer.py` 8 断言（契约/镜像/接线/诱导·全过）
- `test_emc_template.py` 注册 generate_point_layer + names/as slot
- **pytest 305 passed + 5 skipped 零回归** · JS node --check 四文件全过

## 二、待两组复验焦点

### 焦点 1：工具层（P0-1）
- `generate_point_layer` 实现是否正确（search_place 主路径·未命中诚实·并发限流·match_type 属性）？
- 点图层渲染（addToolboxLayer kind:point + 显式 circle）是否正确？

### 焦点 2：路由接线（P0-0）
- `_markupCue` 注入能否实际触发 FC 选 generate_point_layer？（上轮 knowledge_qa + 标记词）
- `runTemplatePath` 零图层诚实出口是否正确（全未命中 → finalStep 文字·非 ask_user）？
- `_deterministicRecover` 兜底是否生效？

### 焦点 3：契约镜像（P0-2/3）
- 四镜像（tool_contracts/paradigm/stages/validate）是否一致？
- 豁免登记（KNOWLEDGE §1 第二次）是否符合 3 条件？

### 焦点 4：修复有效性（命门）
- **是否真能触发**（不是"实现了却从不被触发"）？——用户核心关切
- 验收口径（tier-2 行政区级面+标注为合格）是否可接受？

### 焦点 5：补充与异议
- 有无遗漏/更好做法？（保持各自客观立场）

## 附 A：复验 prompt（发两组用）

```text
【CB-22d · 实施复验】情绪地图 — claude组 → Codex/glm组（2026-08-10）

背景：知识问答→地图标记（generate_point_layer）已按修正版 plan 实施（commit fde2595·17 文件 1001 行）。用户核心关切 = "工具实现了却从不被触发"（修复层面脱节）须避免。请两组复验实施是否有效。

你以第三方独立评估身份（只读本地文件·无需 git pull·claude组 已 commit·本地工作区即最新）。**先独立取证再判断·保持客观立场·可挑战 claude组 实施。**

## 必读
- docs/catch-ball/discuss/CB22d-知识问答到地图标记_反评价收敛定稿_2026-08-10.md（修正版 plan·7 项缺陷）
- docs/catch-ball/discuss/CB22d-知识问答到地图标记_实施复验发起_2026-08-10.md（实施内容）
- 代码（git show fde2595 或读工作区）：
  - frontend/js/ai_qa/tools.js（generate_point_layer）
  - frontend/js/ai_qa/harness.js（_markupCue / runTemplatePath 零图层出口 / _deterministicRecover）
  - frontend/js/ai_qa/panel.js（final_excerpt / rag 识别）
  - frontend/js/ai_qa/stages.js（SKILL_DEFS）
  - ai_qa/tool_contracts.py + ai_qa/paradigm.py（契约/镜像）
  - tests/validate_generate_point_layer.py（8 断言）

## 待复验焦点（5 条·逐条 agree/disagree/partial + 证据 + 建议）
1. 工具层：generate_point_layer 实现正确？（search_place 主路径·未命中诚实·并发限流·match_type 属性·点图层渲染）
2. 路由接线：_markupCue 能触发 FC 选新工具？runTemplatePath 零图层诚实出口正确？_deterministicRecover 兜底生效？
3. 契约镜像：四镜像一致？豁免登记符合 3 条件？
4. 修复有效性（命门）：是否真能触发（非"实现了却从不被触发"）？验收口径（tier-2 行政区级面+标注）可接受？
5. 补充与异议：遗漏/更好做法？

核心标尺：演示逻辑链 · AI·Copilot 内核（Smart 两端+Dumb 中间+编排器确定性）· 出口三铁律。
承重红线：四态出口 / finalStep D019 / diagnose 永不动（增量豁免已登记）/ 追踪编号连续。
产出：docs/catch-ball/discuss/CB22d-知识问答到地图标记_实施复验_{你的组名}_2026-08-10.md
格式：〇 一句话结论 / 一 逐焦点复验 / 二 通过/需修正判定 / 三 补充建议。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-22d 实施复验发起 · 2026-08-10*
*评估方只读本地不 git · 复验落 discuss/ · claude组 收到后收敛定稿*
