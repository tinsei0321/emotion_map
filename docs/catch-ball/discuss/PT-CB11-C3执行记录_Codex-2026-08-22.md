# PT-CB11 · C3 样式面板完善执行记录（Codex · 2026-08-22）

> 派发单：`PT-CB11-样式面板完善派发单_Codex_zcode-2026-08-22.md`；设计方案：`PT-CB11-数据驱动图层样式面板_设计方案_zcode-2026-08-22.md`（用户拍板：方案 A + 图例双入口·不做样式记忆/MCP 回写）。
> 分支 `EMC_harness_dsh`·commit 前缀 `PT-CB11(C3):`·零 pull 零 push。**零 Python 生产改动**（仅测试断言）；零新追踪 ID；与 Phase 2（Python 侧）零文件冲突。

## 一 五件交付

### ① settings.js · polygon+gridField 分支 → 本地色带编辑器（核心件）

- B3-5 止血提示（「数据驱动着色·换色请重投递 spec」）替换为 `sectionGridRampEditor(p)`：
  - **色带选择器**：复用 grid dialog 的 `hm-ramp-item` 渐变条按钮 + `hm-ramp-group-label` 分组（dialog.css 全局类·零新 CSS 文件·零新 token；容器用内联 grid 平铺）；
  - **语义分组排序**：`semantic='count'` → 单色系→多色系→发散系·默认 `oranges`；`'polarity'` → 发散系置顶·默认 `terrain-9`；无语义 → 原序平铺·默认 `grid-warm`；**已有受管 `paint.rampKey` 则如实回显当前所用**；
  - **反向 toggle**（buf-cap 同款）：就地翻转当前 gridStops 高低端；
  - **零值显隐 toggle**：就地翻转 `zeroIsNoData`（状态机=文案表当前态·is-sel 高亮「显示中」·三态实测全对）；
  - 保留既有：面开关/线宽/线型/填充透明度。
- **应用路径**（纯本地·ms 级）：选中 k → `normStops(k)`（grid-tool 导出）→ count 语义基向反转（与投递链 `countStops` 默认同向·见观察项）→ 反向再翻 → `applyPaint({gridStops, rampKey})` → setLayerPaint + renderLayer + commitPaint（图例/Overview 跟随）。

### ② render_client.js · semantic/rampKey 透传

- isPolarity 分支：`semantic:'polarity'`；count 分支：`semantic:'count'`；
- `ramp_hint` 命中 HEATMAP_RAMPS → 落 `paint.rampKey` 且**色带同步消费**（polarity 分支 `polarityStops('overall', rampKey)`·count 分支既有 `countStops(palette)`）——stops 与 rampKey 永不一致漂移；未注册回落不设。

### ③ grid-tool.js · normStops 导出

- `function normStops` → `export function normStops`（面板复用·归一化单源）。

### ④ sidebar.js · 图例双入口

- `refreshLegend()` 的 legend-grid 色带条：cursor:pointer + title「调整色带」+ `onclick → openSettingsPopover(grid)`（**同一 settings popover 组件实例**·非另做一套；onclick 覆盖式绑定防 refresh 叠绑）。

### ⑤ render-contract.md §七-7 契约升级

- 「换色必须重投递 spec」→「**色带/反向/零值显隐/透明度/线样式=面板本地可调**（归一化值随层内嵌）；**数据/字段/归一化变更才需重投递**」；本地调整限受管词表内选色（图层同源原则不破）。

## 二 浏览器实测（8080 现网·投递 count choropleth 内联 spec·ramp_hint=oranges·含零值格）

| # | 项 | 结果 |
|---|---|---|
| 1 | 投递后面板打开 | gridField 层渲染色带编辑器（单色系 12 + 多色系 8 + 发散系 5 渐变条 + 反向/零值按钮）✓ |
| 2 | ramp_hint 回显 | 面板默认选中 `oranges`（「当前所用」如实显示）✓ |
| 3 | 换色带 | 点 blues → 选中切换 + 图例渐变即时变蓝；再点 reds → 变红（截图 2 核验：地图格/图例/面板选中三处同步红系）✓ |
| 4 | 反向 | 点击 → 图例首尾色互换（rgb(8,81,156)↔rgb(239,243,255)）·data-reverse='1' ✓ |
| 5 | 零值显隐 | 三态机：隐藏(无高亮)→显示(高亮+文案)→隐藏；applyPaint(zeroIsNoData) 生效 ✓ |
| 6 | 图例双入口 | 真关闭面板后点图例色带条 → 同一 popover 打开且为该层（oranges 选中）✓ |
| 7 | 截图 | `PT-CB11-C3实测-面板色带编辑器.png` / `PT-CB11-C3实测-换色带reds图例跟随.png`（本目录·视觉模型复核双确认）✓ |

实测投递件（demo·4 格含零值格）已随 watcher 归档 `DATA/exports/render_inbox/applied/c3-test-codex.json`（gitignore 运行痕迹·重复投递件已清理）。

## 三 测试与门禁

- 新增静态契约断言 4 例（test_render_channel.py：settings 编辑器结构/semantic 分支/normStops 导出/图例双入口+契约文档）；
- 存量适配 1 行：test_d_batch.py D10 断言 `polarityStops('overall')` → 前缀判据 `polarityStops('overall'`（C3② 增 ramp_hint 第二参·语义不变）；
- `python -m pytest tests/ -q`：**collected 504：492 passed / 7 failed / 5 skipped**——7 失败与基线完全一致（matplotlib×3 + sentence_transformers×4 存量环境缺依赖）·**零新增失败** ✓。

## 四 观察项（待主手裁决·非本批引入）

- **count 单色系方向语义**：投递链 `countStops` 的默认反转（grid-tool.js:103-109）是 B013 为 grid-warm（原序深→浅）设计的基向；ColorBrewer 单色系（oranges/blues 等·原序浅→深·state.js:544 注释「低→高=浅→深」）经同一反转后呈「低深高浅」，与 B013 注释「点数越多越深」相悖。**本批面板已与投递链同向对齐（一致性优先）**；方向语义本身是否修（countStops 按色系排列方向区分基向）属 B3 遗留，建议主手下批裁决。

## 五 改动文件（6 + 本记录 + 2 截图）

- frontend/js/settings.js（色带编辑器 + wire 三组绑定）
- frontend/js/render_client.js（semantic/rampKey 透传）
- frontend/js/grid-tool.js（normStops 导出·1 行）
- frontend/js/sidebar.js（图例双入口·4 行）
- docs/render-contract.md（§七-7）
- tests/test_render_channel.py（+4 断言）/ tests/test_d_batch.py（D10 前缀适配 1 行）
- 工作区他人在途件未纳入提交：`DATA/boundaries/presets/manifest.json`、`tools/gen_yichang_l2_800m_full.py`。

> Codex · 2026-08-22 · PT-CB11 C3 五件齐 + 浏览器实测通过，待主手回收。无新坑需蒸馏（count 方向属 B3 遗留已列观察项上报）。
