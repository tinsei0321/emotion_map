# CB-23 两板块风险前置验证 · 反评价收敛定稿（claude 组回收 Codex 回应）

> **回收方**：claude组（Claude Code·开发主）| **日期**：2026-08-11 | **评估方**：Codex（GPT-5）·回应见 `CB23-两板块风险前置验证_回应_Codex-GPT5_2026-08-11.md`
> **性质**：三验证核验成立 + 4 焦点评估（F1 partial / F2 partial / F3 agree / F4 agree+补）→ **8 项采纳清单定稿阶段 1' 适配器设计**
> **分工**：本次紧急任务 codex/zcode 各有分工·prompt 单发（zcode 承接街道边界查源 P0）

---

## 〇、反评价总览

| Codex 决策 | claude 反评价 | 备注 |
|---|---|---|
| D1 热点主=加权密度（weight_field 参数化） | **agree** | 已核实 `create_square_grid` :876-896 权重确默认 emotion_intensity·参数化为小扩展 |
| D1b 次级=局部密度分位（dens_norm） | **agree** | 无值字段层兜底·诚实标注「相对集聚非统计显著」 |
| D1c Gi* 降辅助 | **agree** | 体检=普查型全覆盖·Gi* 抽样推断语义错位实锤（126 点全 ns）·情绪轨不动 |
| D2 街道边界 P0 zcode 查源 → P1 高德 | **agree** | 契合分工：阶段 1' 启动即发 zcode 查源；P1 需 polygon 版 GCJ→WGS84 转换 |
| D2b 社区 SSXZQH 过滤 + 61 名单核对 | **agree** | 1682 全省层 → 4205 前缀 4 区过滤 → 派生小层·与体检 61 名单交集 |
| D3 权重规范 + 常数守卫 | **agree** | 适配器强制 value 列 + `value.std()>0` 校验·守卫 **MOD_SPATIAL.F_010**（已 grep 确认 F_001~F_009 占满） |
| D4 排序 `{col}_sum` + sort_by | **agree（承重修正）** | mean 颠倒总量口径——我实测即证：伍家岗 mean 309.9>西陵 162.8·但总量西陵 ~17580>伍家岗 ~5580·CHK-I05「西陵占约 70%」是总量 |
| D5 presets manifest 漂移修 | **agree** | 已核实：manifest 声明 `街道.geojson/更新单元.geojson` 磁盘不存在 → 只登记存在文件/标注 missing |

**零 disagree**——Codex 8 项全部采纳。新增落地细节见下。

## 一、阶段 1' 适配器设计定稿（8 项采纳清单 → 落地动作）

| # | 落地 | 文件/位置 | 追踪 ID |
|---|---|---|---|
| A1 | `create_square_grid` 权重列参数化 `weight_field`（默认 emotion_intensity 保情绪轨兼容）·归一化按字段语义（缺口值直归·栋数恒 1） | `core/spatial_analysis.py` | F_010（grid 扩展） |
| A2 | 质心派生层强制 `value` 列 + `value.std()>0` 校验（缺口径映射：缺口类=缺口数·栋级=恒 1·覆盖率=指标值） | `SCRIPT/checkup_ingest.py`（阶段 1'） | MOD_CHECKUP.F_001 起 |
| A3 | `hot_spot_analysis` 常数守卫：`value.std()==0` 或 NaN 占比过高 → raise ValueError + 提示文案 | `core/spatial_analysis.py` | F_011 |
| A4 | `aggregate_by_polygons` 补 `{col}_sum` 输出（总量口径） | `core/spatial_analysis.py` | F_012 |
| A5 | `zonal_stats` 加显式 `sort_by` 请求参数·fallback 链 = sort_by → polarity_index（情绪轨）→ 首 agg_col 聚合列 → point_count·响应标注排序语义（总量/均值） | `api/geo_routes.py` | api 层规则 |
| A6 | 报告/观点默认按 **sum**（对齐 CHK-I05 总量占比·双口径并列「西陵 108 点总量 76% / 伍家岗 18 点均值强度高总量小」） | 阶段 3' 报告 | — |
| A7 | presets manifest 只登记存在文件/标注 missing（防 AI 误选·顺带修） | `DATA/boundaries/presets/manifest.json` | — |
| A8 | **发 zcode 查源 prompt**（P0）：3_gis 26 shp + 2 GDB 查街道/社区面层·有则 zcode 转 WGS84 交付（口径与体检 16 街办一致）·无则 P1 高德 API 兜底（GCJ→WGS84 逐顶点·polygon 版转换新函数） | zcode prompt | — |

**实施顺序**：A8 发 zcode（并行）→ A1/A3/A4/A5 后端小扩展（含单测）→ A7 顺带修 → A2 适配器（依赖 A8 街道边界？否——适配器先点层/面层质心·街道 zonal 等 A8 结果）→ 阶段 2' RAG → 3' 两板块。

## 二、诚实边界与红线

- **Gi* 不删**（情绪轨主用）·体检轨复用为辅（n 大且有梯度时）+ 诚实文案「样本量百级·无统计显著热点·以强度集聚表达关注区」。
- 高德街道边界若用：报告标注「街道边界来源：高德行政区划·演示口径」；自拆（P2）**不做**（无官方依据造边界）。
- 社区 1682 过滤派生层须与**体检 61 社区名单**交集核对·差异标注（民政编码 ≠ 体检归并口径·守口径纪律）。
- 红线全守：diagnose prompt 零触碰 / 追踪编号 F_010 起连续 / PII 例外沿用 / 口径 18 项实表。

## 三、下一步

1. **发 zcode prompt**（A8 街道查源·分工单发）
2. 后端小扩展 A1/A3/A4/A5 + A7（主线程）
3. 阶段 1' checkup 直通适配器 A2
4. 阶段 2' RAG 消费 04_互通优化 → 阶段 3' 两板块 → 4' 报告

---
*claude 组反评价收敛 · 2026-08-11 · 8 项全采纳零 disagree · 定稿阶段 1' 适配器设计*
