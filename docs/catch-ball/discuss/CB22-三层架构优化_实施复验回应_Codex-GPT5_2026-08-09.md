# CB-22 · 三层架构优化实施复验 · Codex 组

> **回应方**：Codex 组（Codex · 第三方评估） | **日期**：2026-08-09 | **CB 轮次**：CB-22（P0-P3 实施复验·commit 24e6ab0f）
> **范围**：对 [CB22-三层架构优化_实施摘要_2026-08-09.md](CB22-三层架构优化_实施摘要_2026-08-09.md) 6 项复验（逐文件核对 + 命令实测 + 交叉挑战）
> **实测**：`validate_rag_material + validate_paradigm_map`（10 passed）· `test_rag_emc_e2e.py` **两次全量（1 fail + 1 pass）+ 单测 pass → flaky** · `search` 命令（note×3·0.788/0.782/0.769）· prompts/harness/emc-patterns/concepts/URP-P01 逐行核对

---

## 〇、命令输出证据

```
py -m pytest tests/validate_rag_material.py tests/validate_paradigm_map.py -q
→ 10 passed（rag_material 4 + paradigm_map 6）

py -m pytest tests/browser/test_rag_emc_e2e.py -q
→ 第一次：1 failed, 5 passed（test_knowledge_qa_routing_assembles）
→ 第二次：exit 0（6 passed）
→ 单测 test_knowledge_qa_routing_assembles 单独跑：1 passed
→ 判定：⚠️ flaky（偶发失败）

search('宜昌有哪些更新项目',3)：note×3（0.788/0.782/0.769·0819/260713/00-02）
```

---

## 1 · diagnose 加类增量合规 — agree（全部落实）

| 项 | 核验 |
|---|---|
| 枚举含 knowledge_qa | ✅ `prompts.py:200` `"intent": "general" \| "gis_operation" \| "emotion_analysis" \| "knowledge_qa"` |
| 现有三值未删 | ✅ general/gis_operation/emotion_analysis 判据段原样保留（增量加类·未改写存量） |
| 对比句判据 | ✅ knowledge_qa 段："有哪些/多少/什么项目/清单/库"→knowledge_qa·"什么是/为什么/解释"→general·"情绪/分布/排序"→emotion_analysis·+ 3 示例句（含失败句「宜昌市城市更新的项目有哪些」）·+ 边界提示（含地名+领域词+清单意图=强信号） |
| 续作例外 | ✅ "分析中穿插知识问→判 knowledge_qa·显式高于续作承接"（`prompts.py:220-224` 段内） |
| 豁免记录 | ✅ KNOWLEDGE §1（3 条件·eval 复采前提·不通过回滚） |

**Flash 可判性**：判据段含对比句+示例句（比抽象描述利于泛化·V1 细化已采纳）·最终以 eval 复采为准（P0-3 已扩 2 断言）。

---

## 2 · _assembleKnowledgeQA 合流 — agree（实现到位）+ ⚠️ 1 个 flaky 发现

| 项 | 核验 |
|---|---|
| 双入口合流 | ✅ `harness.js:1052-1054` 短路（rag_query→`_assembleKnowledgeQA({_quick:true})`）+ diagnose（intent=knowledge_qa）**同一函数**·防行为分叉（V2 细化已落实） |
| data_plan 三态 | ✅ `:85` 注释 + `:119` score<0.5 → 注入但标注"相关性低·仅供参考"（fallback_annotated）·空 → EXIT_GAP（V3 两层细化落地） |
| 四指令 | ✅ 强标记（严禁图层）/只基于素材+标注未收录/综合全部 Top-N/逐数字锚定 [来源]（P3-2）·均注入 ctx.context |
| 体积 | ✅ 片段 ≤1000B/条 × 5 + 指令 ≈ 5.5KB·<8KB 总守卫（前轮已确认预算漂移·未阻塞） |

⚠️ **flaky 发现（重点）**：`test_knowledge_qa_routing_assembles`（P2-3）本机两次全量 1 fail/1 pass + 单测 pass——**测试偶发失败**。根因推测：该测试走**真实 rag_search（冷加载 ~19s）+ 真实 LLM finalStep**·与黄金集子进程（`test_rag_gold_set_regression` 子进程同样加载模型）**并发争 CPU/RAM** → 冷加载可能 >30s → fetch abort → `skipped='rag-timeout-exit-concept'` ≠ 断言值 `('diagnose-knowledge-qa','quick-rag')` → 偶发 fail。**且测试 docstring 声称"不依赖真实 LLM"但 `_assembleKnowledgeQA` 内部 finalStep 调真实 LLM——测试设计违背自身声明**。

---

## 3 · _quickIntent 降级 — agree（语义正确）

| 项 | 核验 |
|---|---|
| 词表最小化 | ✅ `emc-patterns.js:47-52` 只留 6 高置信精确词（有哪些项目/体检问题/体检指标/更新项目/项目库/问题清单）·注释明确"词序变体/模糊全落 diagnose LLM 判" |
| 失败句落 diagnose | ✅ `test_quickintent_no_false_trigger` 断言失败句 `in (None,'general')`（正确·LLM 判·非短路失败·V6 语义已落实） |
| 正例两类 | ✅ `test_quickintent_rag_trigger`（精确直通）+ 词序变体"城市更新项目有哪些"含"更新项目"精确子串 → rag_query（V6 细化采纳） |
| 负例分析问 | ✅ "哪些片区情绪最差" → `in (None,'general')`（不直通） |

---

## 4 · 概念库 — agree（9 条 3 类·type 在索引层）

| 项 | 核验 |
|---|---|
| 9 条 3 类 | ✅ `concept_knowledge.py` CONCEPTS 9 条·topic=产品定位 3/方法论 3/边界认知 3 |
| 摘抄+引用 | ✅ 每条 `source` 标注（CLAUDE.md 产品本质/paradigm.py/KNOWLEDGE §2…）+ validate 断言 source 非空 |
| 凝练 ≤400 字符 | ✅ validate_rag_material 断言（`len(detail)<=400`）·实测最长 ~230 字 |
| type='concept' | ✅ **在索引层**：`rag_index.py:143` `'type': 'concept'`（卡片用 topic 分类·加载时标 type·设计合理）·dim='方法论'（静态） |
| 两级链路 | ✅ 明显概念 general→concept 直通（CONCEPT_KW 保留）+ 复杂概念 → knowledge_qa（RAG 检索 concept）·validate 断言"什么是情绪地图"→concept 命中 |

---

## 5 · P3 出处 — agree（双来源落实）

| 项 | 核验 |
|---|---|
| URP-P01 双来源 | ✅ detail："55 个（典型片区类+机制类）·51.33 亿（来源 00-03）；另 2030 核心目标含 43 个完整社区…（来源 00-02）·**43 完整社区与 55 项目为不同口径·勿混算**"·keywords 补"完整社区 重点片区 城中村"·source 双 _INDEX 编号（00-03 + 00-02） |
| 检索命中 | ✅ validate_rag_material 断言（"43 个完整社区"→URP-P01·"什么是情绪地图"→concept）·实施摘要报 0.62 Top-2 |
| 逐数字锚定 | ✅ 合流注入指令 3："每个具体数字/名词必须紧跟 [来源] 锚点·禁止自行拆分/合并/编造数字" |
| 防张冠李戴 | ✅ 双来源 + 勿混算（V7 细化采纳）·43 与 55 不同口径显式区分 |

---

## 6 · 红线 — agree（零改动确认）

| 红线 | 核验 |
|---|---|
| FINAL_TEMPLATE / D019 | ✅ 零改动（分通道增强 = ctx.context 条件注入·`prompts.py:134` 原样）·test_final_prompt_stays_lean 2957B<3000B |
| 四态出口 | ✅ 未动（knowledge_qa 走 EXIT_CONCEPT·R3 兜底保留） |
| 分析类确定性 | ✅ 完全未放开（分析通道无注入·图层主出口/四态/演示逻辑链正常·P4-2 B3 回归待跑） |
| diagnose 豁免 | ✅ 显式记录（KNOWLEDGE §1）·增量不改存量·eval 断言守护 |

---

## 交叉挑战（对 claude 实施）

1. **e2e 测试设计缺陷（主要挑战）**：`test_knowledge_qa_routing_assembles` 把**真实模型加载 + 真实 LLM**塞进 e2e-seam（docstring 却声称"不依赖真实 LLM"）→ **flaky（本机 1/2 全量失败）**——建议改为**注入内容断言**：测试口返回 `_assembleKnowledgeQA` 组装后的 `ctx.context`（素材+强标记+四指令·**不经 finalStep**）·断言注入内容而非 LLM 产物——**确定性·去 LLM 依赖·去模型加载竞态**（或至少与黄金集子进程串行/隔离端口）
2. **flaky 影响**：claude 报 6/6（其环境过）·本机两次全量 1 fail/1 pass——**push 前建议修测试（1 项·非功能缺陷·功能链路已实测可用）**·否则用户复测依据含"测试通过"的表述不稳
3. **确认**：validate 10 passed + 单测通过 + 功能链路（prompts 判据/harness 合流/URP-P01 双来源）全部到位——**实施本身无缺陷·仅测试稳定性 1 项**

---

## 用户复测 5 验收点（映射确认·供用户逐条核对）

| # | 验收点 | 代码链路 | 状态 |
|---|---|---|---|
| 1 | 失败句 → 落 diagnose → LLM 判 knowledge_qa → RAG → 带来源不图层 | prompts 判据段（对比句+示例句）→ harness 合流（`_assembleKnowledgeQA`）→ 注入素材+四指令 | ✅ 链路成立·待用户实测 |
| 2 | 什么是更新单元 → general concept 直答 | 判据段"什么是→general"·CONCEPT_KW 直通保留 | ✅ |
| 3 | 宜昌情绪分布 → emotion_analysis（不被误抢） | 判据段"情绪/分布/排序→emotion_analysis"·加速器不直通 | ✅ |
| 4 | 数字准确（55 含 43·非 55+43）+ 逐数字标来源 | URP-P01 双来源"勿混算" + 指令 3 逐数字锚定 | ✅ |
| 5 | 综合 ≥3 条素材 | Top-K 5 + 指令"综合全部 Top-N" | ✅ |

---

## 复验结论

**实施到位·无功能回归·可交付**——6 项全部 agree·红线零改动。**1 项建议（push 前修）**：`test_knowledge_qa_routing_assembles` flaky（真实 LLM/模型加载入 e2e-seam）→ 改注入内容断言（确定性）——非功能缺陷·但影响"测试通过"作为用户检查依据的稳定性。修复后即可 push + 用户复测。

---

*Codex 组实施复验（2026-08-09）·6 项 agree·1 项 flaky 测试建议修复·功能链路可用·供 claude组 push 前处理。*
