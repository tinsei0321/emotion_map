# 情绪地图 · 整体验收（出口三段式 P0-P2 + 热点图 P0/P1/P1.5）— 验收回应（Codex-GPT5 · CB-18）

> **验收方**：Codex（GPT-5）| **日期**：2026-08-08 | **性质**：CB-18 整体验收实施检查（第三方独立验收·只读不 git）
> **验收对象**：`docs/catch-ball/discuss/整体验收_实施检查发起_2026-08-08.md`（P0-1~5 / P1-1~5 / P2-1 / H-1~4 + 审计修正 B1/W1/W2/W3/W6）
> **证据方式**：代码核验（grep path:line）+ 文档交叉比对；**本环境无可用 Python**（WindowsApps stub 不可执行·`py` 无解释器），未能实跑 pytest/e2e-seam——测试侧证据复用 claude组 08-08 实测基线（`5d5e67b`：pytest 291 passed + 5 skipped·validate 34 passed），本组以静态核验补 path:line 证据；浏览器实测未跑（可选）

---

## 〇、一句话结论

**有条件通过（conditional pass）**：两专题 14 项验收中 13 项代码核验通过、0 阻断、无承重红线触碰（四态出口 / finalStep D019 / diagnose 均未动）；但发现 **2 处需修复（W-1 tools.js 红绿语义残留·低危；W-2 threshold/soft_threshold 契约-运行时漂移·警告级）** 与 **3 处需补证（P1-3 CSV 无单测、H-1 DEM 无解码单测、P0-2/3/4+P2-1 无 JS 直测）**——建议先修 W-1/W-2 + 补 S-1/S-2 单测，再正式判定"整体验收通过"并进发版就绪度回归；纯观感项（观点卡干货 / setTerrain 地势感 / 无露底 / 无 console 错）标注"可选用户复核"。

---

## 一、逐项验收（通过 / 需补证 / 需修复·附证据）

### P0 出口三段式（观点先行 + 骨架）

| # | 判定 | 证据（path:line） | 说明 |
|---|---|---|---|
| P0-1 观点卡置顶 | **通过** | `ai_qa/prompts.py:132` FINAL_TEMPLATE 观点先行软扩（观点≠结论·三句骨架前）；`tests/test_emc_template.py:89-100`（含"观点先行须在'三句骨架'之前"位置守门 + 2957B<3000B 门禁）；`frontend/js/ai_qa/result-struct.js:33-46` `_extractInsight` 整块捕获 `> **观点：**`；`frontend/js/ai_qa/panel.js:1552-1556` 观点卡置顶渲染 + 移除引用块防正文重复（todo 原文"正文无重复观点行"落点） | 观感"是否干货"需用户复核 |
| P0-2 4 要点卡底部 | **通过（需补证）** | `result-struct.js:48-53` 四段聚合（方法/数据/结果/结论）；`:95-119` `_conclusionOf` 学术句式（"数据显示 X 极性指数 N（样本）·归因集中于…"）；`panel.js:280-290` `_pointsCardHtml` 四栏（分析方法/使用数据/分析结果/分析结论）+ 头部"分析支撑（4 要点）"；`panel.js:1561` 底部渲染 | 无 JS 单测 → 建议 e2e-seam 直测（S-3） |
| P0-3 三路径都出卡 | **通过（需补证）** | `frontend/js/ai_qa/harness.js:705`（single/template 路径）、`:819`（chain 路径·B1 补丁）、`:1962`（multi 路径·B1 补丁）三处 `_dispatchResultStruct`；`:713-726` 统一走 `buildResultStruct` + `hooks.onResultStruct`（失败不阻塞主链路）；`panel.js:25` `_pendingStruct` 暂存 + onFinalDone 统一渲染 | B1 补丁无测试覆盖（Codex 08-08 提醒成立）→ 建议 e2e-seam 直测或并入回归期三路径浏览器抽验 |
| P0-4 无观点不显卡 | **通过（需补证）** | `result-struct.js:39-46` 无 `> **观点：**` 标记 → `return null`（W3 保守化·删首段首句兜底·不取动作描述当观点）；`panel.js:1552` `if (_s && _s.insight)` 才渲染观点卡 | 无单测 → 建议 e2e-seam 直测负例（draft 无标记 → insight null → 不显卡） |
| P0-5 无 console 错 | **无法代码验收** | 浏览器实测项（可选） | 建议标注"回归期浏览器检查/用户复核" |

### P1 指标细化 + CSV 入库

| # | 判定 | 证据（path:line） | 说明 |
|---|---|---|---|
| P1-1 需求强度等级 | **通过** | `ai_qa/outlet_kb/build_outlet_schema.py:33-55` `grade_demand_intensity`（L2 归一 pi/2·四档 <−0.5 高 / ≤−0.15 中 / ≤0.15 低 / >0.15 无显著需求）；`:328-332` 卡内"需求强度等级"字段；`tests/test_outlet_schema.py:435-447`（L2/L1 边界 + None 缺省）+ `:456-461` 卡内接入断言 | ✓ |
| P1-2 复合优先级 | **通过** | `build_outlet_schema.py:60-112` `priority_score`（PRIORITY_WEIGHTS 0.5/0.3/0.2·coverage 用 rows_max_pc（p95）归一·缺省项不参与加权权重归一·topic 0/0.5/1·topic_intent=False 转移权重）；`:350` 卡内"优先级排序"Top1 字段；`tests/test_outlet_schema.py:449-467`（p95/缺省/主题三档/无主题）+ `:468-477` 卡内 Top1 断言 | ✓ |
| P1-3 CSV 导出按钮 | **通过（需补证）** | `core/export.py:200-231` `export_outlet_card_csv`（outlet_id/name/scale/interface/flat fields/data_base/task_link/limitations/geo_label/source/export_time·SENSITIVE_FIELDS 显式脱敏·`'\ufeff'` BOM·返回 (bytes, filename, media_type)）；`panel.js:341` 导出按钮 + `:357-380` `_exportOutletCardCsv`（前端 BOM + Blob 下载·防功能空转） | **无 export_outlet_card_csv 单测**（BOM/列/脱敏无守卫）→ 建议补测试（S-1） |
| P1-4 案例口径标注 | **通过** | `build_outlet_schema.py:373` limitations 卡级声明"行业案例为对标参照·非评分基准·数值口径以当地官方发布为准"；`tests/test_outlet_schema.py:490-494`（卡内声明）+ `:496-502`（case_library 四案例均含 measure_note/source） | ✓ |
| P1-5 地点 scale 标注 | **通过（需补证）** | `build_outlet_schema.py:376` `geo_label`（宏观·面域 / 中观·单元 / 微观·落点·scale 分派）；`tests/test_outlet_schema.py:479-488`（macro/meso 前缀断言） | **micro 分支无单测**（else 兜底经代码核验存在）→ 建议补 micro 用例（S-4·低危） |

### P2 地点联动

| # | 判定 | 证据（path:line） | 说明 |
|---|---|---|---|
| P2-1 结论段带地点 | **通过（需补证）** | `result-struct.js:100-113` `_conclusionOf` 从 rows 取 place_name/name/district/unit 组学术句式；`harness.js:723-725` 传 `_lastToolRows`（W1 审计·macro 权威产物）；数据侧 place_name 双源（`core/spatial_analysis.py` 聚合产物 + CB-15 P0） | 无 JS 单测 → 建议 e2e-seam 直测（rows 带 place_name → 结论含地点·按尺度） |

### H 热点图重做

| # | 判定 | 证据（path:line） | 说明 |
|---|---|---|---|
| H-1 setTerrain 连续曲面 | **通过（需补证）** | `core/spatial_analysis.py:1067-1166` `create_terrain_dem`（KDE→terrarium RGB `(R*256+G+B/256)-32768`·bounds 4326·F_009 + D_004 埋点）；`api/routes.py:351-395` `/spatial/dem`（polarity 校验·X-DEM-Bounds/Size/HeightScale）；`frontend/js/api.js:106-108` runDem；`frontend/js/map.js:333-367` `setTerrainDEM`（raster-dem terrarium·setTerrain·draping 隔离）；`frontend/js/heatmap-tool.js:929-961` `generateTerrain3DForAI`/`closeTerrain3D`；`frontend/js/ai_qa/tools.js:1314-1316` EMC 委托已切（B1） | **无 DEM 解码单测**（发起文档证据路径 `tests/` 为空·todo 08-05"DEM 解码验证"为一次性手工验证）→ 建议补 terrarium round-trip/0~500/bounds 单测（S-2） |
| H-2 显著聚集五档橙系 | **通过（需修复）** | `frontend/js/map.js:604-611` colorMode='hotspot' 纯橙系五档（hot #e8590c / tend_hot #ff922b / tend_cold #ffd8a8 / cold #fd7e14 / ns #adb5bd·半径/描边分档·弃红蓝极性色）；`frontend/js/toolbox/hotspot-tool.js:17` 去极性映射保留 hotspot_tier；`core/spatial_analysis.py:110-127` `_classify_hotspot` + `api/geo_routes.py:655-676` threshold/soft_threshold 透传 + legend 五档响应；`tests/test_spatial_analysis.py:310-333`（五档/阈值参数/hotspot_tier 字段） | **需修复 W-1**：`frontend/js/ai_qa/tools.js:1285` docstring + `:1297` observation 仍写"hot=红/cold=绿/ns=灰"旧极性色描述——与纯橙系渲染矛盾·误导 LLM/用户观感（纯文案 1 处） |
| H-3 命名定标 | **通过（需修复）** | `frontend/js/toolbox/hotspot-tool.js:32` 图层名「显著聚集点(Gi*)」；`frontend/js/toolbox/grid-tool.js:27,32` 「显著聚集(Gi*)」；`frontend/js/panel.js:1514` spatial_hotspot→「代表地点」；`docs/spec.md:30` 「代表地点（POI 名·生成器注入）」；`frontend/js/test-cases.js:263` hotspot 语料「显著聚集分布」；`frontend/index.html:663` 「显著聚集 Gi*」 | "无旧义残留"判据与 W-1 冲突：`tools.js:1297` observation 前缀"热点分析：" + "hot=红/cold=绿"为旧义残留 → 归入需修复 |
| H-4 网格柱互斥 + 无露底 | **通过** | `frontend/js/map.js:346-352` 开启 terrain 时隐藏 3D 网格柱（kind=polygon + tool=grid/mode=3d·防漂移穿模）；`:354-360` sky 层防地坪线露底；`:361-367` 关闭时移除 terrain+sky + 恢复图层可见；`:356` easeTo pitch 60 | 视觉"无露底"需用户复核（可选） |

### 审计修正核验（08-05 B1/W1/W2/W3/W6）

| 修正 | 判定 | 证据 |
|---|---|---|
| B1 EMC 委托切 generateTerrain3DForAI | **通过** | `tools.js:1314-1316` density mode=terrain 委托 + `heatmap-tool.js:929` 实现 + `closeTerrain3D` 成对 |
| W1 纯橙系符号层 | **部分通过** | 渲染层通过（`map.js:604-611`）；**文本层残留红/绿描述（W-1·需修复）** |
| W2 /geo/hotspot 透传 threshold/soft_threshold | **部分通过** | 后端通过（`geo_routes.py:655-657/668-670` + `spatial_analysis.py:36-37/97`）；**前端两处未转发（W-2·需修复/需决策）：`tools.js:1286-1292` 与 `hotspot-tool.js:15-18` 的请求体均无 threshold/soft_threshold**——LLM/调用方指定阈值被静默丢弃·契约（`tool_contracts.py:274-275` "EMC-only·AI 执行"）与运行时漂移 |
| W3 legend 五档 | **部分通过** | 响应字段层通过（`geo_routes.py:673-676` legend 五档 95%/84% 对称）+ EMC 观察文本 `tools.js:1294` `_CLS_CN`；**地图图例层无渲染**（heatmap-legend.js 仅热力图图例·main.js 无 hotspot 图例分支）→ 验收口径需确认（W-3） |
| W6 契约层五档同步 | **通过** | `ai_qa/tool_contracts.py:260-275`（hotspot 契约 + threshold/soft_threshold 参数）；`ai_qa/paradigm.py:427-431`（SKILL_DEFS 五档）；`frontend/js/ai_qa/stages.js:61`（optional_defaults 五档）；`tests/validate_skill_params.py:39`（_KNOWN_SLOTS 五档） |

### 5 焦点判定（逐条 agree/disagree/partial + 证据 + 建议）

**焦点 1：验收清单完整性 — agree（+2 补充 +4 观感标注）**
- 对照 `docs/todo.md`「整体验收清单」原文：P0-1~5/P1-1~5/P2-1 全覆盖；热点图原文合并项（setTerrain 连续曲面·Gi\* 网格化·命名定标）拆分为 H-1~4 合理——注：原文"Gi\* 网格化"已被 P1 修正弃置（A/B 全 ns 实锤后改软分级·`docs/todo.md` 08-05 段），非清单遗漏。
- **补充 1**：P0-1 判据应含"正文无重复观点行"（todo 原文·落点 `panel.js:1556` 移除引用块）——现清单未列。
- **补充 2**：H-2 判据应显式含 W6 契约层五档同步（tool_contracts/paradigm/stages/validate）——现并入 H-2 但未单列证据路径。
- **必须用户观感复核**：观点卡是否"干货"（P0-1）· setTerrain 是否"地势感"（H-1）· 无露底（H-4）· 无 console 错（P0-5）——建议在验收通过结论中显式标注"可选用户复核"。

**焦点 2：证据充分性 — partial（3 项证据路径为空/不足·给出 e2e-seam 方案）**
- **P1-3 CSV**：发起文档证据路径含 `tests/`，但 `rg export_outlet_card_csv tests/` 为空——todo 08-05"CSV 真跑（BOM/列✓）"是一次性手工验证，无守卫。建议补单测（S-1）：BOM 前缀、outlet_id/name/scale 列、SENSITIVE_FIELDS 脱敏、Excel 兼容。
- **H-1 DEM**：发起文档"DEM 解码单测"证据路径为空（全仓 `rg terrarium tests/` 无结果）。建议补单测（S-2）：构造小点集 → create_terrain_dem → 解码 `(R*256+G+B/256)-32768` 回高度（0~500）·bounds 为 WGS84。
- **P0-2/3/4 + P2-1**：均无 JS 侧测试（result-struct.js 无任何测试引用）。建议 **e2e-seam 直测（S-3·先例 `tests/browser/test_r7_truncation.py`）**：
  - ① `buildResultStruct` 纯函数直测：draft 含/不含 `> **观点：**` → insight 提取/`null`（P0-1/P0-4）；rows 带 place_name → 结论含地点（P2-1）；scale 三档 → 宏观/中观/微观（P1-5/P2-1）。
  - ② 三路径派发断言（P0-3）：经 e2e-seam 暴露 harness 三路径（runTemplatePath/runChainPath/runAllToolCalls）或 stub `onResultStruct` 收集，断言三路径均产生 `{insight, points}` 且非 null 观点时出卡。
  - ③ 负例：draft 无观点标记 → insight null → panel 不渲染 `.emc-insight-card`。

**焦点 3：验收方式转型 — agree（风险可控·给出值得跑的浏览器项）**
- 风险属实但可控：代码核验证明"机制存在"，观感类（干货/地势感/露底）只能用户感知——建议验收结论按"机制层 + 观感层"双层标注，观感层留用户复核，避免两组"证据驱动"替代用户验收。
- **浏览器实测值得做**（回归期一次即可）：P0-3 三路径出卡（B1 补丁最高价值项）· P0-5 无 console 错 · H-4 3D 互斥切换无露底。
- **代码核验已充分**（不必浏览器）：P1-1/2/4/5（后端确定性组装 + 单测）· P0-4 兜底逻辑 · H-2 渲染层色值。

**焦点 4：回归范围衔接 — agree（+3 条边界建议）**
- 顺序：本验收（有条件通过）→ 修 W-1/W-2 + 补 S-1/S-2 → 发版就绪度回归——**W-1/W-2 修复须在回归前**（避免回归跑旧文本/旧参数路径），S-1/S-2 纯测试不动行为、可随回归一并验证。
- 边界建议：① 回归 fail 集判据 = {PRM-03/04/07}（CB-17 已定·维持）；② 三路径观点卡浏览器抽验并入回归（P0-3 与回归共用一次）；③ 顺手对齐 `tests/browser/flywheel_audit.py:7` B3 注释 25→26 例。

**焦点 5：两组补充与异议 — partial（2 处证据异议 + 1 环境说明）**
- 环境：本机无可用 Python（`python.exe` 为 WindowsApps stub·`py` 无解释器），未能实跑 pytest/e2e-seam；测试侧证据**复用 claude组 08-08 实测基线**（291 passed + 5 skipped·validate 34 passed）+ 本组静态核验；建议 glm组 若可跑则补跑 `tests/test_emc_template.py` + `tests/test_outlet_schema.py` + `tests/test_spatial_analysis.py`（hotspot 三测）。
- **证据异议 1**：发起文档 H-1"DEM 解码单测"证据路径实际为空（见焦点 2）。
- **证据异议 2**：H-2/H-3"legend 五档 + 无旧义残留"与 `tools.js:1285/1297` 红绿描述残留冲突——验收判据与实际代码不一致，需先修再判（W-1）。
- 补充发现：W2 前端转发缺口（W-2）在发起文档未列——契约承诺参数化而运行时只用默认值，属 CB-04"契约-运行时同步"同类风险。

---

## 二、新问题风险清单（阻断 / 警告 / 建议）

### 阻断（0 项）

无。两专题核心实现均代码可证；承重红线未触碰（四态出口契约未改·FINAL_TEMPLATE 软扩守 3000B 门禁·diagnose prompt 未动·验收全程只读）。

### 警告（3 项·建议验收通过前处理）

| # | 风险 | 证据 | 建议 |
|---|---|---|---|
| W-1 | **tools.js hotspot 红/绿语义残留**：`tools.js:1285` docstring + `:1297` observation 仍写"hot=红/cold=绿/ns=灰"，与 W1 纯橙系渲染（`map.js:604-611`）矛盾，且违反 H-3"无旧义残留"判据 | `frontend/js/ai_qa/tools.js:1285/:1297` | 1 处纯文案修复（"hot=深橙/tend=浅橙/ns=灰/冷区同橙系"），零逻辑影响·回归前合入 |
| W-2 | **threshold/soft_threshold 契约-运行时漂移**：tool_contracts 承诺 AI 可传阈值（`tool_contracts.py:274-275` "EMC-only·AI 执行"），但 EMC handler（`tools.js:1286-1292`）与 Toolbox（`hotspot-tool.js:15-18`）均未转发 → LLM 指定阈值被静默丢弃·运行时恒用 1.96/1.0 | `tools.js` / `hotspot-tool.js` vs `geo_routes.py:655-657` | 二选一：① 前端转发 threshold/soft_threshold（补齐 W2 闭环·约 4 行）；② 若默认值即定稿，契约降级标注"仅默认值·参数保留待 EMC 规模化"（防 LLM 填了白填） |
| W-3 | **legend 五档口径存疑**：五档图例仅在 `/geo/hotspot` 响应（`geo_routes.py:673-676`）与 EMC 观察文本（`tools.js:1294`），地图图例层无渲染（heatmap-legend.js 仅热力图） | `frontend/js/heatmap-legend.js` / `main.js` | 确认验收口径：若要求"地图可见五档图例"→ 补 UI；若接受"EMC 工具卡文本图例"→ 在验收记录中显式定口径（本次按后者判定·需 claude组 确认） |

### 建议（6 项·不阻塞验收但建议排入回归批次）

| # | 建议 | 验证方法 |
|---|---|---|
| S-1 | 补 `export_outlet_card_csv` 单测（BOM 前缀 / outlet_id/name/scale 列 / SENSITIVE_FIELDS 脱敏 / 返回值三元组） | `tests/test_export.py` 新增（纯函数·无依赖） |
| S-2 | 补 DEM 解码单测（构造小点集 → create_terrain_dem → terrarium 解码回高度 0~500·bounds 4326·尺寸 >0） | `tests/test_spatial_analysis.py` 新增（依赖 numpy/PIL·对齐现有依赖） |
| S-3 | e2e-seam 直测 result-struct（P0-2/3/4 + P2-1·三路径派发断言·负例无观点不显卡） | `tests/browser/test_result_struct.py`（先例 test_r7_truncation.py） |
| S-4 | 补 geo_label micro 分支单测（scale=micro → "微观·落点"） | `tests/test_outlet_schema.py` +1 |
| S-5 | 回归期三路径观点卡浏览器抽验（P0-3 与发版回归共用） | 浏览器实测（serve.py 8080） |
| S-6 | 观感复核项标注进验收结论：观点卡干货 / setTerrain 地势感 / H-4 无露底 / P0-5 无 console 错 | 用户浏览器复核（可选） |

---

## 三、可否整体验收通过结论

**判定：有条件通过（conditional pass）**——不设阻断，但验收通过结论需满足以下三个前置：

1. **W-1（tools.js 红/绿文本残留）合入**（1 行文案·回归前）；
2. **W-2 决策落地**：转发 threshold/soft_threshold（推荐·补 W2 闭环）或契约降级标注（默认值定稿声明）；
3. **W-3 口径确认**：legend 五档按"EMC 工具卡文本图例"口径记录（若要求地图图例则补 UI）。

满足以上三项 + S-1/S-2 单测补入后，**14 项验收全部判定"通过"**（P0-5 与观感类标注"可选用户复核"），两专题 P0 闭环 → 进发版就绪度回归（fail 集判据 {PRM-03/04/07} + RST-L06 + eval 复采带 session + link_checkup + 三路径观点卡浏览器抽验）。

---

*Codex（GPT-5）· CB-18 整体验收实施检查回应 · 2026-08-08*
*验收只读不 git · 证据基于本地代码核验（path:line）+ claude组 08-08 实测基线复用；本环境无 Python 未实跑测试*
