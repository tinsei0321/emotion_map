# PT-CB7 · 任务拆解与执行计划（Qoder 主执行 · 2026-08-21）

> 依据：任务书 + 主执行前审计（同日两件）。拆解原则（用户令）：**复杂任务（长程·复杂思考·架构判断）Qoder 自执；简单任务（机械执行·有明确决策树/模板·与长短无关）成批派 dsh**。
> 总纲遵守：「让不需要思维的任务不进思维循环」——派 dsh 的批次本身也按此原则写成「照做清单」，不给 dsh 发散空间。

---

## 一 拆解总表

| 任务 | 复杂度判定 | 归属 | 理由 |
|---|---|---|---|
| T1 图层残留自清理 | **复杂**（JS 运行时语义 + 去重真身衔接 + 测试形态裁定 M2） | Qoder 自执 | 需对 state.js/map.js/shared.js 三处真身的语义判断 |
| T6 MCP 描述紧凑化 | **复杂**（八工具语义保真压缩 + 五判据逐件答辩 + 零红线） | Qoder 自执 | 压缩是语义判断，不是机械截断 |
| T8 脚本参数化+口径对照段 | **复杂**（三参数改造 + schema 清理 + 口径注册表引用 + 复现同数字验收） | Qoder 自执 | 长程改造，口径风险高 |
| T5+T9 身份卡扩写+口径对照纪律（EMC 半） | **复杂**（内容设计：能力/边界/口径纪律措辞） | Qoder 自执 | 灵魂内容需要产品理解 |
| T3 EMC 入口内嵌+就绪 gate | **复杂**（registerTab 蓝本侦察 + 跨源 iframe 方案 M3 + R14 衔接） | Qoder 自执 | 架构判断密集（但构建/重启验证步可派 dsh） |
| T7 双模预设（设计） | **复杂**（预设形态设计 + F1 结论转化） | Qoder 自执 | 依赖 F1 调研结论的裁定 |
| T2 better-sidebar terminal chunk | **简单**（决策树四步·照做） | **dsh 批 1** | 任务书已给完整决策树 |
| T4 计时取证 | **简单**（headless 跑题 + 填表） | **dsh 批 1** | 只取证不改·模板化 |
| F1-F4 调研 | **简单**（grep 源码/文档 + 回答四问） | **dsh 批 1** | 纯检索取证 |
| T5 dsh 半（人设 prompt 落 profile + RAG 重建触发） | **简单**（照 Qoder 定稿文本配置） | **dsh 批 2** | 内容 Qoder 写·dsh 落 |
| T7 配置文件落地 + ≤2min 验收跑 | **简单**（照 Qoder 定稿配置建 profile + 计时） | **dsh 批 3** | 配置与计时都是机械动作 |
| T3 构建/重启验证步 | **简单**（tsdown 重建 + web 重启 + 截图取证） | **dsh 批 2** | Qoder 写码·dsh 验证 |

## 二 执行顺序与批次

**Qoder 自执（EMC 仓批·先行）**：T1 → T6 → T8 → T5+T9（各自独立 commit+push，规范 `PT-CB7(T{n}):`，显式路径）。
**dsh 批 1（可与 Qoder EMC 仓批并行）**：T2 + T4 + F1-F4（全部只取证/按决策树修，落执行记录素材）。
**Qoder 自执（dsh 环境批·依赖批 1 结论）**：T3 写码 → T7 设计。
**dsh 批 2**：T3 构建验证 + T5 dsh 半。
**dsh 批 3**：T7 配置落地 + emc-analysis ≤2min 验收跑。
**收尾**：Qoder 汇总主执行记录 → 告知用户 → 主手回收。

## 三 派 dsh 批次规格（派发单正文见各节·用户转发即可）

### dsh 批 1 派发单（T2 + T4 + F1-F4）

```text
【PT-CB7 · dsh 协助批 1】（Qoder 主执行派发 · 2026-08-21）

纪律：dsh 环境内操作；EMC 仓零触碰；dsh 上游冻结禁 merge；一切改动与证据落盘到
docs/catch-ball/discuss/PT-CB7-dsh协助批1记录_dsh-2026-08-21.md（file:line/时间戳证据）。

任务 A（T2 · 决策树照做）：
① 查 ~/.dsh/profiles/web/node_modules/dsh-better-sidebar/ 版本与 lib/ 各 chunk 构建时间戳
   vs D:/Github/dsh/apps/web/dist 构建时间（2026-08-20 21:03）——记录对照表；
② 修法 A：cd ~/.dsh/profiles/web && pnpm install（顺带观察 pnpm-lock.yaml 的 synapse
   残留条目是否被修剪——记录前后 diff）→ 重启 web → 验证 terminal 标签与报错刷屏；
③ A 失败走 B：查 better-sidebar 配置项禁用 terminal 标签（SidebarPrefs/interceptOpenPath
   同族配置·grep lib 内 prefs）→ 验证其余标签；
④ 无论成败：记录 interceptOpenPath 当前值（T3 依赖此结论）。

任务 B（T4 · 计时取证·只取证不改）：
① dsh --profile emc-test-headless 跑题「12345 热线诉求最密集的 5 个社区是哪些？」
   （不开 render）——记录 wall-time、step 数、每 step 思考/工具耗时分解（从 session log 提）；
② 同题在 web profile 新会话跑一次（标注会话 id·注明污染声明）；
③ 记录两 profile 各自生效的模型/preset/effort；
④ 产出计时分解表 + 根因初判（思考 token vs 工具 vs 轮次哪端占大头）。

任务 C（F1-F4 · 检索取证）：
F1：dsh 是否支持 per-session/per-step reasoning effort 覆写？（grep D:/Github/dsh 的
    settings/config schema：reasoning/effort 键·profile settings.yaml 可配项）
F2：已裁=模板先行——仅需确认 SCRIPT/gen_12345_grid_800m.py 无他人占用（git log 即可）；
F3：观察项——无需动作，记录一行；
F4：dsh 是否有 context 压缩/摘要机制？（grep compact/summary/truncate 于
    packages/context 或 docs）——给出有/无 + 出处。
```

### dsh 批 2 派发单（T3 构建验证 + T5 dsh 半）

```text
【PT-CB7 · dsh 协助批 2】（待 Qoder T3 代码与 T5 人设文本落盘后启动）
A：D:/Github/dsh-emc-entry 重建 bundle（tsdown）→ 重启 web → 按 Qoder 执行记录中的
   验证清单逐项取证（含 8080 冷启动场景）。
B：把 Qoder 定稿的 EMC 人设 system prompt 写入 web + emc-test 两 profile 的预设配置；
   跑 py tools/rag_index.py --build；验证 kb_facts(keyword='EMC') 命中身份卡。
```

### dsh 批 3 派发单（T7 落地 + 验收跑）

```text
【PT-CB7 · dsh 协助批 3】（待 Qoder T7 设计定稿后启动）
A：照 Qoder 定稿建 emc-analysis / emc-research 两 profile（配置逐键照抄·禁自行发挥）；
B：dsh --profile emc-analysis 跑「12345 热线诉求最密集的 10 个社区」类任务·记录 wall-time
   （目标 ≤2 分钟）；失败则记 step 分解回报 Qoder。
```

## 四 风险与停止条件

1. dsh 批 1 任务 A 若 A/B 均失败 → 停，报 Qoder（涉及 better-sidebar 版本升降级=超范围，待主手裁决）；
2. T3 侦察若发现 registerTab 真身与 service.d.ts 不符 → 停手写码，先落侦察报告；
3. T8 复现验收若数字与原版不一致 → 停，先对账（口径错误零容忍）。

---

> Qoder · 2026-08-21 · 拆解完成，进入自执批次。
