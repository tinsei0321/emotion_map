# CB-22 · EMC 测试失败修复 — 反评价收敛 + 定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（RAG 冷加载超时修复）
> **回应**：[Codex](CB22-EMC修复_Codex-GPT5_2026-08-09.md) + [glm组](CB22-EMC修复_glm组_2026-08-09.md)
> **判定**：R1/R2/R4/R5 agree·**R3 升级为确定性兜底（禁 LLM 编）**→ 定稿实施

---

## 〇、两组一致（含 R3 共识升级）

- **R1/R2/R4/R5 全 agree**（预热/catch 兜底/超时 30s/B 路径节奏）
- **R3 关键共识升级**：两组独立都认为「约束 LLM 不编」不够 → 应**禁走 LLM**（Codex 确定性直返 / glm EXIT_CONCEPT 确定性兜底）——**合并为：注入失败 → EXIT_CONCEPT 确定性兜底·零 LLM·零幻觉**

## 一、定稿修复清单（采纳两组）

### P0 · 治本
| # | 修复 | 位置 | 采纳 |
|---|---|---|---|
| R1 | **serve 启动异步预热 RAG 模型** | `rag_index.warmup()`（复用 `_get_model` 单例）+ `api/main.py` startup 后台线程（daemon·不阻塞·失败降级 WARN） | Codex+glm |
| R2 | **catch 兜底诚实标注（措辞中性）** | `harness.js:1003` catch → 注入"【知识库检索未就绪·本次回答基于通用知识·未引用项目库·建议稍后重试】" + **覆盖 `_data.ok===false`（索引未构建）静默路径** | Codex 措辞 + glm 中性化 |
| R3 | **注入失败 → EXIT_CONCEPT 确定性兜底（禁 LLM 编）** | `harness.js` rag_query 分支 catch → 直接返回确定性文案"知识库检索未就绪·模型加载中·稍后重试/换问"·**不走 finalStep** | Codex+glm 共识 |

### P1 · 容错
| # | 修复 | 位置 |
|---|---|---|
| R4 | **AbortController 15s→30s**（预热窗口容错·预热稳定后回 15s） | `harness.js:982` |
| R5 | **B 路径（后置）** | query_knowledge_base（此问句最终治本·无模型加载） |

### 测试补强
| # | 修复 |
|---|---|
| V6 | R2 失败路径断言（索引缺失/超时 → 回答带"未就绪"声明·不静默） |
| V7 | e2e 编码修复（`:82` text=True 加 encoding='utf-8'·Codex 已提） |

## 二、验证验收（两组补强）

| # | 标准 |
|---|---|
| V1 | serve 重启 >20s 后首检 F_015 <1s（预热生效） |
| V2 | 回答引用知识库（含"来源：urban_renewal_knowledge.py#..."） |
| V3 | **预热窗口兜底**（启动 <18.6s 首检 → EXIT_CONCEPT"知识库检索未就绪"·非情绪分析式幻觉）——R3 验证 |
| V4 | 黄金集回归 100% |
| V5 | pytest 零回归 |
| V6 | R2 失败路径（索引缺失 → 诚实声明） |
| V7 | e2e 5/5（含编码修复） |

## 三、承重红线
- diagnose prompt / 四态出口 / @track 不碰（R3 EXIT_CONCEPT 是 harness 分支·非新态）
- warmup 复用 F_014/F_015 路径·不新增跳号 ID（Codex 建议）
- D019 final 极瘦：R3 不走 finalStep·不涉 D019

## 四、实施顺序
1. `rag_index.warmup()` + `api/main.py` 后台预热
2. `harness.js` rag_query 分支：R2 catch 兜底 + R3 EXIT_CONCEPT 确定性直返 + R4 超时 30s
3. e2e 编码修复 + V6/V7 测试补强
4. 复测 V1-V5（F_015 时长 + 回答断言 + 黄金集 + pytest）
5. 收敛 → B 路径（CB-22b）

---

*claude组（2026-08-09）·EMC 修复反评价收敛定稿·开始实施。*
