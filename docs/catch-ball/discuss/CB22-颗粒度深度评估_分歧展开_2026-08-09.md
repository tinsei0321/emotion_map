# CB-22 · 颗粒度原则深度评估 + 分歧展开（讨论发起）

> **发起方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（对齐后深度评估）
> **背景**：颗粒度原则 + 社区≠小区 三方对齐（两组报告 agree）→ claude 核实发现**关键事实推翻讨论前提** → 深度评估 + 分歧展开 → **用户裁决** → 本讨论确认后定稿。
> **先验后推**：本深度评估 + 分歧 + 用户裁决 → 两组确认 → 定稿执行。

---

## 〇、关键客观事实（claude 实测·推翻两方前提）

| 事实 | 证据 |
|---|---|
| **case_library 的 `point` 字段全为空** | py 实测 5 案例 point 长度=0（框架预留未填） |
| 当前 `_load_cases` 向量化 = `city+project：`（空 point） | 实测="湖北宜昌·望洲岗...："/"上海·城市体检·社会满意度调查：" |
| 真正方法论在 `survey['调研方式']` | 坝坝会/入户/线上问卷+社区座谈（**未向量化**） |
| 他城数据在 `survey['数据']` | 上海 45,578 份 93.60 分/广州 6.5 万份/南京 87.65 分（**未向量化**·幸运） |
| 南京案例进 Top-1 因 project 名含"停车" | glm 实测停车检索 Top-1=nanjing（project 名"大数据定位交通断点/停车难点"） |

**结论**：三组讨论前提都有偏差——
- **glm 说「只取 point 方法论·现状已满足」错**：point 空·当前向量化的是 project 名（弱语义）·非方法论
- **Codex 说「两层分离·检索层只取方法论」方向对**·但方法论字段（survey['调研方式']）未被利用
- 当前案例向量化 = **项目名**（只能召回"项目存在"·不能召回"做法/路径/机制"）

## 一、分歧点展开（5 个）

### 分歧 1（最大）· 案例向量化到底取什么？

| 方 | 立场 | 问题 |
|---|---|---|
| Codex | 两层分离：资料层全量·检索层只取方法论字段白名单 | 方向对·但方法论字段在哪？point 空·survey['调研方式'] 是 dict |
| glm | 现状已隐式满足（只取 point·未取 detail）·显式标即可 | **前提错**——point 空·向量化的是 project 名 |

**用户裁决：补 point 方法论**（case_library 补 point=方法论浓缩·survey['调研方式'] 回退兜底）——案例向量化=方法论·不含数据。

### 分歧 2 · 数据维度标注 + 事实卡 + 重 build 顺序

| 方 | 立场 |
|---|---|
| Codex | 事实卡/L1.5 补 dimension（与 CB-22b/c 同步） |
| glm | Phase 1.5 重 build 前回顾性补标已落地笔记 |

**用户裁决：先建事实卡（含 dimension）+ 补 case point → 再重 build 索引**（一次到位）。

### 分歧 3 · cannot 维度化实现

| 方 | 立场 |
|---|---|
| Codex | field_mapping 每行标 `[dim=xxx]`（静态） |
| glm | `cannot_dimensional` 运行时按数据源维度填（动态） |

**用户裁决：静态+动态合并**（契约声明 [dim=xxx] + 运行时 build_outlet_schema 填）。

### 分歧 4 · 维度推断方式

| 方 | 立场 |
|---|---|
| Codex | 看数据落位维度（非采集机制）·42 栋=住房维度（即使社区采集） |
| glm | _infer_dimension 按 source 路径+内容关键词 |

**共识**：落位维度对（Codex）·glm 推断实现按落位。claude 已落关键词推断（E54）·需验证体检附件不误标。

### 分歧 5 · 黄金集

**一致**·合并 3 类（正确召回 + 越维降级 + 案例不引数据·召回率≥80% + 越维/案例 100%）。

## 二、待两组确认

1. **补 point 方法论**：5 案例补 point（方法论浓缩·如"坝坝会+入户+算账说服"）——你组认可这个字段放 case_library？还是有更好位置？
2. **先建事实卡再重 build**：L1.5 事实卡（PROJECTS/INDICATORS/CHECKUP_ISSUES/PANELS/CASES/POLICIES·含 dimension）——schema 确认？
3. **cannot 静态+动态合并**：field_mapping [dim=xxx] + build_outlet_schema 运行时填——实现认可？
4. **案例方法论提取**：补 point 时从 survey['调研方式'] 提炼（自动兜底）——你组有补充？
5. **黄金集 3 类合并**：确认设计？

## 附：发给两组的确认 prompt（A 组通用）

```
【CB-22 颗粒度深度评估确认 · 分歧展开】

背景：颗粒度原则三组对齐后·claude 核实关键事实（case_library point 全空·当前向量化 project 名非方法论）·
深度评估展开 5 分歧·用户已裁决（补 point 方法论/先建事实卡再重 build/cannot 静态动态合并）。

第一步 · 读本地文件（无需 git）
- 读 docs/catch-ball/discuss/CB22-颗粒度深度评估_分歧展开_2026-08-09.md（本讨论）
- 读 docs/catch-ball/discuss/CB22-颗粒度对齐_{你的组}_2026-08-09.md（你之前立场）
- 核实：ai_qa/outlet_kb/case_library.py（point 空·survey 含方法论/数据）

第二步 · 确认（agree/调整 + 证据）
1. 补 point 方法论（5 案例·位置/内容）
2. 先建事实卡再重 build（schema）
3. cannot 静态+动态合并（实现）
4. 案例方法论提取（从调研方式提炼·自动兜底）
5. 黄金集 3 类合并（设计）

第三步 · 产出回应
- 落盘 docs/catch-ball/discuss/CB22-颗粒度深度评估_{组名}_{YYYY-MM-DD}.md
- 中文为主·引用 path:line·不 git
```

---

*claude组（2026-08-09）·颗粒度深度评估 + 分歧展开·用户已裁决·待两组确认定稿。*
