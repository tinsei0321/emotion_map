# PT-CB9 · 泳道③ Qoder 审计报告（2026-08-22）

> 审计方：Qoder（用户指派·PT-CB7 审计先行模式回归首审）。对象：Kimi 泳道③工程收尾交付（commit `892d22b7`）。
> 纪律：零实施零 git 写（本报告除外）·本地仓即最新零 pull·独立复测三跑全自跑·引句 grep ≥4 条（实做 8 处+全语料对抗 grep）。
> 审计材料：派发单/执行记录/892d22b7 全 diff/rag-loader-contract.md/X-01 全段/R24——全部实读。

---

## 一 逐件四档裁决

| 件 | 裁决 | 一句话 |
|---|---|---|
| ① 重建命令一体化（护栏 4） | **通过** | `--build` 内串 generate() 增量·五项摘要自跑逐字复现·失败双档留痕不阻塞（A9）·tmp+os.replace 原子写 |
| ② loader 护栏（护栏 3+5） | **通过** | content_hash 覆盖正文+前注已落实（meta.jsonl 实测两 hash 并存）；护栏 5 测试在且取样法经自我修正（黄金集关联·R8.1 正向） |
| ③ X-01 逐条登记（R24 应用） | **有条件通过** | 2 条 superseded 判定与引句全部 verbatim 属实；**条件 = F-03 总纲#5 补入待裁/登记**（同族漏项·2 行改动即销） |

**整批裁决：有条件通过**（条件仅 F-03；其余发现均 P3 记账级）。红线三连零退化属实。

## 二 发现清单

| # | 严重度 | 发现 | 证据 | 建议 |
|---|---|---|---|---|
| F-03 | **P2** | **总纲#5 漏登记**：`分析计划与内容_总纲_2026-08-12.md#5` 含作废值「双高 16 格/聚拢 57-81%」（计划进度表·性质与已登记待裁 1 的 #3/#4 完全同族），执行记录表 8 只覆盖 #3/#4 | 全语料 grep「16 格」命中总纲#3/**#5**（`_tmp` 取证脚本输出）；#5 原文「2② 落图 \| ✅ 双高 16 格/聚拢 57-81% \| ⬜ 片区边界…」status=active | 并入待裁 1（范围改「总纲#3/#4/#5」）或主手裁定后补 `_SUPERSEDED_SOURCES`/active 登记——执行记录 §四已预留 2 行生效路径 |
| F-01 | P3 | **素材表登记值在 chunk 截断区**：表 7/12 引句（checkup_dual_high 16格/qty公服 111点·1068）在磁盘原文节#0 属实，但该 chunk text 2000 字截断**不含**这些行——登记防护对检索面无实际作用（检索永远命不中截断区） | 磁盘 grep 全 True vs chunk text grep 全 False（`_tmp` 双向取证）；`_图层素材表.md#0` len=2000 满额截断 | 登记表补注「值在 #0 截断区外·无检索面」；长期看截断策略（CB-22 全文纪律 vs 2000 上界）与表格型文档的适配另议 |
| F-02 | P3 | **注册表 chunk 定位序号误差**：表 1-3 标「`_口径注册表.md#4`」，实测 87.9%/5,615/1,153 在 **#3 与 #6**（#4 不含任何作废值） | 语料 grep 值命中：#3（4 值全）/#6（2 值）·#4 空 | 执行记录引句表订正为「#3/#6」；不影响 active 判定正确性（治理卡全域 active 正确） |
| F-04 | P3 | **pytest 数字差异未注明**：执行记录声明 517 passed+2 skipped，我环境实测 **515+4**——差异 2 例=本机缺 `pypinyin`（test_geocode 两例条件 skip），非造假；但执行记录未注明 skip 构成与环境前提 | 自跑 `pytest -rs`：skip 明细 pypinyin×2 + pii_guard 参数空 + range_selector preset | 收敛记录补一行环境注记；双方 passed 均 ≥ 基线 514 不降成立 |
| F-05 | P3 | **护栏 3 现为「写而不读」**：content_hash 覆盖前注的语义已写入 meta，但全仓 grep 仅 rag_index.py 写入点（:352/:359）无任何读取消费——快照/门禁报警要等下游消费者出现才真正生效（「全仓无下游消费零破坏」声明本身**属实**） | `git grep content_hash -- *.py *.js` 仅 2 处写入 | 记账：护栏 3 的防护闭环依赖后续批建快照/门禁消费者；本批派发单只要求改覆盖语义——合规 |
| F-06 | P3 | **护栏 5 门禁面有限**：黄金集关联取样仅覆盖 expect 命中面的带前注 chunk（59 条前注抽 6）——非黄金集覆盖的弱前注不在门禁面；文档名/小节要素用 4 字连续片段匹配（弱匹配·存在无关片段误放的理论空间） | test_rag_loader.py 抽样逻辑审读（by_id 每 id 取 sorted 首条·等距 6·任二断言） | 补测建议：①非断言性全量前注巡检（59 条出处要素统计报表）；②「4 字片段」匹配改词级或加最小覆盖阈值（后批） |

**未成立的怀疑（自查排除）**：旧机制死代码——`_SUPERSEDED_FILE_PREFIX/_EXEMPT_PREFIX` 代码内零残留（仅 L1 历史文档提及，非死代码）；「32 格/3/26」无语料独立命中（无漏登记）；00-02#5「人口和建设度双高」=通用词组判定正确（表 9 结论复核成立）；search 与黄金集期望零触碰（diff --stat 4 文件验证）。

## 三 独立复测记录（三跑+对抗 grep·全自跑）

| 命令 | 我的数字 | 执行记录数字 | 判定 |
|---|---|---|---|
| `py tools/rag_index.py --build` | 构建摘要：chunk 374·前注覆盖 59·新生成 0·跳过(未变更) 59·**superseded 2** | 同（374/59/0/59/2） | **逐字一致** |
| `python -m pytest tests/ -q` | **515 passed + 4 skipped**（67.9s） | 517+2 | 总数同为 519；差异=pypinyin 环境 2 例（F-04）·双方 ≥ 基线 514 不降 |
| `python tools/rag_eval.py` | **总 Recall@5 96.7%**·caliber 93.8(n=16)·checkup 93.8(n=16)·narrative 100(n=14)·noun 100(n=14)·刚性两例过·MISS=C12/U06（各 1） | 96.7% 逐类全同 | **逐类一致·零退化属实** |

**引句抽验（8 处 verbatim grep·C 轴）**：B3B4#2「双高区（G10·重头·16 格」✓「最急难愁盼」✓；占比表#0「总 1068 点」✓「覆盖 136 社区」✓；总纲#3「16 格」✓「结论层」✓；03-10 摘要#1「旧口径」✓/#5「取消双高」✓——**全部命中**。语义判定复核：1068=误计值作现行总量结论（全表以其为分母）→superseded 正确；双高 16 格=作废概念作现行分析结论→superseded 正确；治理卡/清册/计划记述→active 正确。

**全语料对抗 grep（F 轴）**：87.9%→仅注册表#3；5,615/1,153→注册表#3/#6；1068→仅占比表#0；「16 格/16格」→B3B4#0/#2/#3+总纲#3/**#5（漏）**；「双高」词组族→00-02#5（词组·非命中）——**除 F-03 外无漏登记**。

## 四 盲区自查（诚实声明）

1. **未做第四路边界构造实验**（改前注验 hash 报警）：因 F-05 已证 content_hash 无下游消费者——构造实验无观测面，改用代码审读+meta.jsonl 字段实测替代（ctx_prefix_hash/content_hash 并存确认）。任务书标「鼓励」非强制，声明不做及原因。
2. **active 登记引句未逐字验全 8 条**：抽验覆盖表 4/5/8/10/11/12 + 值命中佐证表 1-3；表 2/3 的引句行本身未逐字比对（值已 grep 命中同一 chunk，佐证充分度可接受）。
3. **rag_eval 60 用例未逐条复核**（只验总分/分类分/MISS 明细）——与执行记录对比粒度一致。
4. **前注语义质量不在本批审计面**（护栏 5 是门禁不是质检）——前注内容好坏未评。

## 五 对执行方能力的旁证意见

R24 redemption 成立：两条 superseded 的引句全部 verbatim、判定语义准确、待裁三项全部克制不硬拍；执行记录中护栏 5 取样自我修正（全量等距→黄金集关联）是 R8.1 的正向应用。缺口集中在**登记覆盖完备性**（总纲#5 漏项）与 **chunk 定位精度**（#4 序号/截断区）——属作业细致度而非纪律违规。

---

> Qoder · 2026-08-22 · 六轴审毕：**有条件通过**（F-03 补登记即销）·零 git 写·全部结论可经 `_tmp` 取证脚本与自跑命令复现。
