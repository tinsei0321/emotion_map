# CB-22 · EMC 三层架构优化（glm组）

> **评估方**：glm组（ZCode + GLM 5.2 · 第三方独立评估·不做项目方决策背书）
> **日期**：2026-08-09 | **CB 轮次**：CB-22（三层架构优化·用户主导·意图判断归位）
> **回应对象**：`CB22-EMC三层架构优化_讨论发起_2026-08-09.md`（三层架构 + 三通道路由 + 6 焦点）
> **上轮承接**：glm 根因诊断建议"A 触发放宽 + 防回归·B' 后置"——用户拍板"NL 意图判断必须通过 LLM"**修正了 glm A 方案**（规则正则治标·意图判断归 Smart 才治本）
> **核心标尺**：三支柱（缺一不可）/ AI·Copilot 内核（Smart 两端 + Dumb 中间）/ 承重红线

---

## 〇、一句话结论

**glm 完全对齐用户三层架构 + 三通道路由 + 意图判断归 Smart——用户拍板"NL 意图判断必须通过 LLM"是对 glm 上轮"A 触发放宽（规则正则）"的根本修正：规则词表不得做判断主体（仅加速器）·意图判断归 diagnose LLM。6 焦点：焦点 0/1/4 agree（三通道+三层+意图归位）/ 焦点 2 partial（概念库落地范围须收窄）/ 焦点 3 agree 倾向 claude（diagnose 加 knowledge_qa·LLM 判意图·非 glm B' FC 加工具）/ 焦点 5 agree（防回归·含 diagnose 枚举断言）。承重红线关键：diagnose intent 枚举加 knowledge_qa = 改 diagnose prompt·须走"先扩 eval 再动·一次一处"SOP（非"永不动"绝对禁止·而是"扩 eval 守护下增量"）。glm 历轮 A 方案（触发放宽）诚实承认是治标·接受用户归位修正。**

---

## 一、用户顶层原则对齐（glm 完全认可）

| 用户原则 | glm 对齐 |
|---|---|
| ① NL 意图判断必须通过 LLM（Smart 端） | ✅ **完全认可**——意图判断是 NL 理解·归 Smart·规则词表仅加速器 |
| ② 意图判断位置在计划执行前（诊断阶段） | ✅ **完全认可**——diagnoseStep（prompts.py:187）正是此位 |
| ③ 语义判断 = NL→intent（非数据字段语义） | ✅ **完全认可**——intent 判"用户意图"非"字段含义" |

**glm 诚实承认上轮修正**：
- glm 上轮（纯问答定位失败根因诊断）建议"A 触发放宽（RAG_OPEN_RE 开放问词正则+知识词 gate）"——这是**规则正则做判断主体**·与用户原则①**矛盾**。
- glm A 方案是**治标**（覆盖词序变体）·非**治本**（意图判断归 Smart）。
- **用户拍板修正了 glm**——规则词表不得承担判断主体（仅可做明显命中直通省调用）·意图判断必须 LLM。
- **glm 接受修正**——glm"基于事实·不固执"原则包括接受用户对架构的判断（用户是产品意图最终裁定者）。

---

## 二、6 焦点回应（glm 逐条）

### 焦点 0：三通道路由（意图判断=总调度·三通道·统一三段式）— **agree（架构正确·可实现）**

**用户期望**：意图判断 agent = 总调度·读 NL 意图 → 路由三通道（知识问答→RAG→LLM 综合 / GIS→14 工具→图层 / 情绪→分析→报告）·统一表达层三段式。

**glm 判定**：**agree——三通道路由架构正确·EMC 已具备大部分骨架·唯一断的是知识问答通道。**

**三通道现状核实（glm）**：

| 通道 | 现状 | 缺口 |
|---|---|---|
| **GIS 工具调用** | ✅ diagnose→FC 选 14 工具→runTemplatePath→finalStep 图层 | 无（已通） |
| **情绪分析** | ✅ diagnose intent=emotion_analysis→zonal/rank→finalStep 图层/报告 | 无（已通） |
| **知识问答** | ❌ diagnose intent 无 knowledge_qa → 知识问落分析通道 → 出图层 | **断（本次失败根因）** |
| **表达层三段式** | ✅ 观点先行+4 要点+出口卡（CB-22 P0 已实施） | 无（已通·但知识问答通道未接入三段式） |

**glm 确认**：
- 三通道路由**可实现**——GIS + 情绪已通·**补知识问答通道（diagnose 加 knowledge_qa）= 三通道完整**。
- 意图判断 agent = diagnoseStep（已有·位置正确 :187）——只须 **intent 枚举加 knowledge_qa**（让 LLM 判知识问答意图）。
- 统一表达层三段式——知识问答也走三段式（观点+知识要点+接口对标）·非特殊出口。

**用户期望"完全可能"——glm agree**：EMC 迷你版 AI 平台（资料库+RAG+意图判断+回答）·聚焦反而更准。

### 焦点 1：三层架构对齐（描述 + 现状对照）— **agree（差距判断准确）**

**glm 判定**：**agree——三层描述清晰·现状对照准确。差距 2 项（概念库待建 + Q&A 归位）。**

**三层对照核实（glm 确认 claude 取证）**：

| 层 | 用户设想 | 现状（glm 核实） | 差距 |
|---|---|---|---|
| **数据层·概念库** | 产品定义/宏观背景 | ⬜ 无独立库——散在 MANIFESTO（:14）+ CLAUDE.md | **待建** |
| **数据层·情绪库** | L0-L4 管线 | ✅ L2 情绪点+L3L4 富归因 | 无 |
| **数据层·基础库** | industry_kb+事实卡+RAG | ✅ 事实卡 35+RAG 225+boundaries | 无 |
| **实施层·流水线** | Smart Agent+Dumb Tool | ✅ diagnoseStep→harness→finalStep | 无 |
| **实施层·工具集** | 14 GIS + 1 Q&A | ⚠️ 14 GIS ✅·Q&A 非工具（短路旁路） | **Q&A 归位** |
| **表达层·三段式** | 观点+要点+接口 | ✅ CB-22 P0 已实施 | 无 |

**glm 补充（概念库定位）**：
- 概念库 = RAG 知识库一部分（非独立物理库）——产品定义/宏观背景/边界认知**高度凝练**后入 RAG 索引。
- vs 事实卡（note 类）：概念库是"定义/背景"（静态）·事实卡是"项目/指标"（动态）——**概念类素材 type='concept'**（与 note/fact 区分）。

### 焦点 2：概念库落地（RAG 补概念类素材）— **partial（范围须收窄 + 粒度须明确）**

**计划**：RAG 补概念类素材（产品定义/宏观背景/边界认知·高度凝练+引用）·概念问答从模板直答→RAG 检索+LLM 综合。

**glm 判定**：**partial——方向对（概念库入 RAG）·但范围须收窄 + 粒度须明确。**

**glm 范围建议（收窄 3 类·非全量产品定义）**：

| 概念类素材 | 内容 | 来源 | 入 RAG？ |
|---|---|---|---|
| **产品定位** | "EMC = 城市连续主观体检" | MANIFESTO:14 | ✅（高度凝练 ~200 字） |
| **方法论** | 尺度范式/4×5 归因/三段式 | paradigm.py/CLAUDE.md | ✅（核心方法论条目） |
| **边界认知** | "EMC 不替代客观指标/不做精确识别" | MANIFESTO 边界段 | ✅（诚实标注） |
| ~~全部 MANIFESTO~~ | ~~完整文档~~ | ~~prompts.py:14~~ | ❌（太大·prompt 注入更合适·不进 RAG） |

**glm 粒度建议**：
- 概念类素材**高度凝练**（每条 ~200 字·非全文）——与事实卡（≤80 字 detail）区分。
- 引用摘抄（标 MANIFESTO/CLAUDE.md 行号）——可溯。
- type='concept'（RAG meta 标注）——与 note/fact/case 区分。

**概念问答链路改造（glm 确认可行性）**：
- 现状：concept 模板直答（素材来自 prompt 预训练·非检索）。
- 改造：concept 问 → RAG 检索概念类素材 → LLM 综合（引用 MANIFESTO/CLAUDE.md）。
- **可行性**：✅——概念类素材入 RAG 后·concept 问走 knowledge_qa 范式（检索+LLM 综合）。
- **但**：concept 问 vs knowledge_qa 问的边界须明确（焦点 4）。

### 焦点 3：Q&A 归位（意图判断层 vs FC 工具）— **agree 倾向 claude（diagnose 加 knowledge_qa·LLM 判意图）**

**glm 判定**：**agree 倾向 claude 方案（diagnose 加 knowledge_qa）——glm 上轮 B'（FC 加工具）修正。**

**glm 立场修正（诚实）**：
- glm 上轮建议 B'（FC 加 query_knowledge_base/rag_search 到 FC 可选工具）——这是"工具选型"路径。
- 用户拍板"意图判断归 LLM"——**支持 claude 倾向**（diagnose intent 加 knowledge_qa·LLM 判意图 → 路由知识问答通道）。
- **glm B' vs claude diagnose**：

| 方案 | 机制 | 对齐用户原则 | glm 评估 |
|---|---|---|---|
| **glm B'**（FC 加工具） | 短路漏网 → FC 自选知识工具 | ⚠️ FC 是"工具选型"非"意图判断"（工具选型方差风险） | 治标（兜底·非意图归位） |
| **claude diagnose**（intent 加 knowledge_qa） | diagnose LLM 判 knowledge_qa → 路由通道 | ✅ 意图判断归 Smart（用户原则①） | **治本**（意图归位） |

**glm 修正**：**agree claude diagnose 方案**——用户拍板后·意图判断归 LLM 是架构归位·glm B'（FC 工具）是兜底（非归位）。

**但 diagnose 红线**：
- diagnose intent 枚举加 knowledge_qa = **改 diagnose prompt**（prompts.py:200）。
- KNOWLEDGE §1 "diagnose prompt 永不动"——但"永不动"是**防 Flash eval 路由退化**（CB-01 教训）·非绝对禁止。
- **glm 建议**：加 intent 枚举值（增量·非改路由逻辑/判据）须走 **"先扩 eval 再动·一次一处" SOP**：
  1. 先扩 diagnose eval（test_diagnose_prompt 含 knowledge_qa 断言·Flash 能判知识问答）。
  2. 再改 prompt intent 枚举（加 knowledge_qa + 判定要点说明）。
  3. eval 复采核验（Flash 路由准确率不退化）。

### 焦点 4：意图判断强化（diagnose 加类 + _quickIntent 降级 + 概念/知识边界）— **agree（全对齐）**

**glm 判定**：**agree——4 项全对齐用户原则。**

**逐项核实**：

**① diagnose 加 knowledge_qa（类增量·不改不删现有三值）**：
- 现状：`general | gis_operation | emotion_analysis`（:200）。
- 加：`knowledge_qa`（知识问答意图）。
- **不改不删现有三值**——增量·非替换。
- **红线豁免**：走 SOP（先扩 eval·焦点 3 已述）。

**② _quickIntent 降级加速器（明显命中直通·非判断主体）**：
- 现状：_quickIntent（:60-82）抢判断（在 diagnose 之前）。
- 降级：**仅明显命中直通**（CONCEPT_KW/INVENTORY_KW/GREETING_KW 等高置信）·**知识问答不靠短路**（走 diagnose LLM 判）。
- **glm 关键修正**：glm 上轮建议"A 触发放宽（RAG_OPEN_RE 正则）"——**降级为加速器**（明显知识问直通省 FC 调用）·**非判断主体**（变体/模糊走 diagnose LLM）。

**③ 概念问 vs 知识问边界（general vs knowledge_qa·LLM 判时区分）**：
- 概念问（general）："什么是情绪地图"（定义解释·无知识检索）。
- 知识问（knowledge_qa）："宜昌有哪些更新项目"（知识检索+LLM 综合）。
- **边界 LLM 判**——diagnose prompt 加 knowledge_qa 判定要点（"问句要求检索知识库信息 → knowledge_qa；问句要求定义解释 → general"）。
- **glm 建议**：边界判据写入 diagnose prompt knowledge_qa 判定段（类似现有 general/gis/emotion 各有判定要点 :202-216）。

**④ 多轮续作覆盖"分析中穿插问"**：
- 现状：多轮续作取上轮 intent（:220-224·防续作误判 general）。
- 补：分析中穿插知识问（如 zonal 后问"葛洲坝有哪些项目"）→ 判 knowledge_qa（非续作 emotion_analysis）。
- **glm 建议**：多轮续作规则加例外（"分析中穿插知识问 → 判 knowledge_qa·非续作"）。

### 焦点 5：防回归机器化 — **agree（触发正负例 + diagnose 枚举断言）**

**glm 判定**：**agree——2 层防回归。**

**① 触发正负例（_quickIntent 加速器·含本次失败句）**：
- 正例：明显知识问（"宜昌有哪些更新项目"精确命中 RAG_QUERY_KW）→ 知识问答直通。
- 负例：分析问（"宜昌情绪分布"）→ 不触发知识直通（走 diagnose）。
- **本次失败句**（"项目有哪些"）→ **不靠 _quickIntent 命中**（走 diagnose LLM 判）—— _quickIntent 降级后此句**正确落 diagnose**（非短路失败）。

**② diagnose 枚举断言（intent 含 knowledge_qa）**：
```python
# tests/test_diagnose_prompt.py（扩）
def test_diagnose_intent_has_knowledge_qa():
    """CB-22 三层架构：diagnose intent 枚举含 knowledge_qa（知识问答通道）。"""
    from ai_qa.prompts import build_diagnose_prompt
    p = build_diagnose_prompt('')
    assert 'knowledge_qa' in p, 'diagnose intent 枚举缺 knowledge_qa（知识问答通道断）'
```

**③ PARADIGM_MAP 断言（已有·validate_paradigm_map.py）**：
- knowledge_qa 范式已在 PARADIGM_MAP（emc-patterns.js:56）——validate 断言 rag_query→knowledge_qa 已有。
- 补：diagnose 出 knowledge_qa → 路由 knowledge_qa 范式（harness 分支·与 rag_query 短路合流）。

---

## 三、glm 交叉挑战

### glm 对 claude 的挑战（1 条）

**挑战：diagnose 加 knowledge_qa 后·harness 如何路由？**

- diagnose LLM 判 intent=knowledge_qa → diagnose 卡含 knowledge_qa。
- harness 主循环现据 diagnose.intent 路由（runTemplatePath / runChainPath / general 短路）。
- **knowledge_qa 路由**：harness 须加分支（diagnose.intent==='knowledge_qa' → 调 rag_search → 注入素材 → finalStep LLM 综合）。
- **vs rag_query 短路**：rag_query 短路（_quickIntent）是"加速器"（明显知识问直通）·knowledge_qa diagnose 路由是"正式通道"（LLM 判后走）。
- **glm 挑战**：两者**合流**（都走"rag_search → 注入 → finalStep LLM 综合"）还是**分离**（短路快路径 vs diagnose 慢路径）？
- **glm 建议**：**合流**——无论短路还是 diagnose 判·knowledge_qa 范式统一（rag_search → 注入 → finalStep）·_assembleKnowledgeQA(ragResults) 复用。

### glm 自我挑战（1 条）

**自我挑战：glm 上轮 A 触发放宽（RAG_OPEN_RE 正则）是否完全废弃？**

- 用户拍板"意图判断归 LLM"——A 正则做判断主体**违背原则**。
- 但 A 正则降级为"加速器"（明显知识问直通省 FC 调用）**仍有用**——非完全废弃。
- **glm 调整**：A 正则**降级**（加速器·非判断主体）——明显命中直通（RAG_QUERY_KW 精确匹配）·变体/模糊走 diagnose LLM。
- **即 A 仍有价值·但定位变了**——从"判断主体"降为"加速器"。

---

## 四、风险清单

### 阻断级

| # | 风险 | glm 建议 |
|---|---|---|
| **B1** | diagnose intent 无 knowledge_qa（知识问答无通道·本次失败根因） | 加 knowledge_qa 枚举（走 SOP·先扩 eval） |

### 警告级

| # | 风险 | glm 建议 |
|---|---|---|
| **W1** | diagnose 加 intent 枚举 = 改 diagnose prompt（红线 §1"永不动"） | 走 SOP（先扩 eval·增量·非改路由逻辑）——"永不动"防 eval 退化·非禁增量 |
| **W2** | _quickIntent 降级后·明显知识问也走 diagnose（延迟 +FC 调用） | 保留加速器（精确命中直通·变体走 LLM）·速度+鲁棒性兼顾 |
| **W3** | 概念问 vs 知识问边界（general vs knowledge_qa）LLM 判方差 | diagnose prompt 加判定要点（类似现有 general/gis/emotion 各有段） |

### 建议级

| # | 建议 | 优先级 |
|---|---|---|
| **S1** | diagnose intent 加 knowledge_qa（走 SOP·先扩 eval） | P0（治本·意图归位） |
| **S2** | _quickIntent 降级加速器（精确命中直通·变体走 LLM） | P0（对齐用户原则） |
| **S3** | harness 加 knowledge_qa 路由分支（rag_search→注入→finalStep） | P0（知识问答通道） |
| **S4** | 概念库入 RAG（3 类凝练·type='concept'） | P1 |
| **S5** | 防回归（触发正负例 + diagnose 枚举断言） | P0 |
| **S6** | 多轮续作加"穿插知识问"例外 | P1 |

---

## 五、可否执行结论

### ✅ 三层架构 + 三通道路由全 agree——意图判断归 Smart·补知识问答通道

**glm 核心立场**：

用户三层架构 + 三通道路由 + 意图判断归 Smart——**glm 完全对齐**。用户拍板"NL 意图判断必须通过 LLM"修正了 glm 上轮 A 方案（规则正则治标）·glm 诚实接受（意图归 Smart 是架构归位·glm A 降级为加速器）。

**6 焦点判定**：
- 焦点 0 三通道路由：**agree**（架构正确·补知识问答通道=完整）
- 焦点 1 三层对齐：**agree**（差距 2 项·概念库+Q&A 归位）
- 焦点 2 概念库落地：**partial**（范围收窄 3 类 + 粒度明确 type='concept'）
- 焦点 3 Q&A 归位：**agree 倾向 claude**（diagnose 加 knowledge_qa·LLM 判意图·非 glm B' FC 工具）
- 焦点 4 意图判断强化：**agree**（4 项全对齐·diagnose 加类+_quickIntent 降级+边界判定+多轮例外）
- 焦点 5 防回归：**agree**（触发正负例 + diagnose 枚举断言）

**承重红线关键（glm 立场）**：
- **diagnose intent 加 knowledge_qa = 改 diagnose prompt**——但"永不动"（§1）是**防 Flash eval 路由退化**·非绝对禁止增量。
- **glm 建议**：走 **"先扩 eval 再动·一次一处" SOP**——① 先扩 diagnose eval（knowledge_qa 断言）② 再改 intent 枚举（增量）③ eval 复采核验（Flash 路由不退化）。
- **glm 不认为"永不动"绝对禁止加 intent 枚举**——加枚举值是增量扩展（非改路由逻辑/判据）·SOP 守护下可行。
- FINAL_TEMPLATE：不触碰（知识问答走 ctx.context 注入·非改模板）✅。
- 四态出口：knowledge_qa 走 EXIT_CONCEPT（LLM 综合·非新态）✅。

**glm 历轮修正脉络（诚实）**：
- CB-22 R3：RAG 不可用 → EXIT_CONCEPT 兜底（正确·保留）。
- CB-22 第二根因：RAG 可用 → 零 LLM 确定性组装（**错误·用户否定·砍支柱③**）。
- CB-22 纯问答失败：A 触发放宽（**治标·用户修正为加速器非判断主体**）。
- **CB-22 三层架构**：**意图判断归 Smart（diagnose LLM）+ 知识问答通道（knowledge_qa）** —— 架构归位。

**glm 立场**：
- glm 从 R3 → 第二根因 → 纯问答失败 → 三层架构——**持续基于事实修正立场**。
- 本次用户拍板"意图判断归 LLM"是**架构归位**——glm A 降级为加速器·B'（FC 工具）让位 diagnose（意图归位）。
- **glm 不固执**——包括接受用户+claude 对架构的判断（diagnose 归位 > FC 工具兜底）。
- diagnose 红线 SOP 是关键——"先扩 eval 再动"是 glm 一贯承重周边处理纪律。

---

## 附：现状核实证据（glm 独立）

| 发现 | 证据 |
|---|---|
| diagnose intent 仅 3 值（无 knowledge_qa） | prompts.py:200 `general\|gis_operation\|emotion_analysis` |
| _quickIntent 抢判断（diagnose 之前） | harness.js:60-82（RAG 短路:71 在 REGION:75 之前） |
| diagnose 8 字段卡骨架完整 | prompts.py:198-213（intent/domain_lens/scale/...） |
| 多轮续作取上轮 intent | prompts.py:220-224 |
| 模板命中 gate（Flash 降级保护） | harness.js:92-105（0.6 gate） |
| PARADIGM_MAP 有 knowledge_qa 范式 | emc-patterns.js:56（validate 断言已守） |
| 事实卡 35 + RAG 225 | urban_renewal_knowledge.py + rag_index.py |
| KNOWLEDGE §1 diagnose 永不动 | KNOWLEDGE.md 红线清单（防 Flash eval 退化·非禁增量） |

### 声明

本回应由 glm组（ZCode + GLM 5.2）独立产出·2026-08-09·基于核实 diagnose intent 枚举 + _quickIntent + 模板 gate + MANIFESTO + KNOWLEDGE 红线。第三方独立评估·不做项目方决策背书。glm 上轮 A 触发放宽（规则正则）被用户修正为加速器·B'（FC 工具）让位 diagnose（意图归位）——glm 诚实接受架构归位修正。

---

*登记：docs/context-map.md · CB-22 三层架构优化 glm组 回应。*
