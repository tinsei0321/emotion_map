# CB-22d · 实施复验 — 回应（Codex-GPT5 · 2026-08-10）

> **复验方**：Codex 组（第三方评估·GPT-5）| **日期**：2026-08-10 | **性质**：CB 实施复验轮
> **对象**：commit `fde2595`（generate_point_layer·17 文件 1001 行）
> **方法**：反评价收敛定稿 7 项缺陷逐条对照 + 代码逐行复算 + search_place 真实数据实测 + 抽验测试（validate_generate_point_layer 8 断言 / test_emc_template 26 项全过）。未做任何 git 操作。

---

## 〇、一句话结论

**实施质量高：7 项缺陷 6 项真修 + 1 项半修（A-4 _SLOT_HINT.names 未落地）·触发链路逻辑闭环（_markupCue 双子句 + FC 契约 when 诱导 + _deterministicRecover 兜底三层保底·非「实现了却从不被触发」）·判定「通过（附 2 项实质 quality 项 + 3 项低危建议 + 1 项命门测试缺失）」。实质 quality 项：① tier-2 面化未实现（工具注释声称三级回退·代码只有 point+unmatched·与修正版 plan P0-1 不一致）② tier-1 无甄别——实测「污水厂网一体示范区」误导命中到「丁家坝污水处理厂」（违背用户批准的「绝不强行匹配」）。**

---

## 一、逐焦点复验

### 焦点 1：工具层（P0-1）— **partial（主体正确 + 2 项实质发现）**

**正确项（代码复算 ✓）**：

| 项 | 证据 | 结论 |
|---|---|---|
| search_place 主路径 | `tools.js` `generate_point_layer` 逐名 `fetch /api/v1/place/search?q=...&limit=3`（复用 `core/geocode.py:188`） | ✅ |
| 未命中诚实 | `unmatched[]` 收集 + observation「未匹配到坐标（诚实列出·不编造）」·不 request_upload | ✅ |
| 并发限流 | `_MAX=50 / _CONC=10 / _TO=5000ms` + worker 分桶（`_idx` 单线程原子安全） | ✅ |
| match_type 属性 | point fc properties 含 `name/source/match_type/address/category`·source=local/amap | ✅ |
| 点图层渲染 | `addToolboxLayer({kind:'point', paint: 显式 circle})`（circle-radius 7·#ff9000·白描边）·`map.js:516/635` point circle 渲染 | ✅ |

**实质发现 ①：tier-2 面化未实现**。工具注释声称「tier-2 面：未命中但为已知片区/行政区名（面 preset）→ 面要素（source=preset·名称标注）」——**代码无面化回退**（无 preset containment 匹配、无面要素合成、match_type 无 'area' 档）。与修正版 plan P0-1「三级面化回退（自建 name→面 containment·不复用 FIXED_ADMIN_DISTRICTS 白名单）」不一致；实施文档「主路径 POI+逆地理·GIS 仅甄别增强·绝不强行匹配」的「甄别」在实现里也不存在（见发现②）。

**实质发现 ②：tier-1 无甄别 → 误导命中**。实现无条件取 `_hits[0]`。**search_place 真实数据实测**：

| 名称 | top-1 命中 | 评价 |
|---|---|---|
| 葛洲坝片区 | 葛洲坝船闸景区（local·66.7） | ✅ 大致区域点可接受（片区级） |
| 红星路-二马路街区 | 二马路社区（local·57.1）/二马路历史文化街区 | ✅ 大致区域点可接受 |
| 二马路 | 二马路历史文化街区（local·250） | ✅ 好命中 |
| **污水厂网一体示范区** | **丁家坝污水处理厂（amap·score=0）** | ❌ **误导命中**——聚合类名称（16 个项目的示范区）被错标到单个污水厂坐标·违背「绝不强行匹配」 |

「污水厂网一体示范区」正是知识问答答案里的聚合口径——把 16 个项目标成 1 个污水厂点位是**数据诚实问题**（比未命中更糟·用户会质疑）。建议甄别：score 阈值/来源（amap score=0 降级）/聚合词检测（示范区/个/类/一批）→ 标「大致区域·非精确点位」（match_type 加 'approx' 档）或归 unmatched。

**小缺陷**：`names>50` 截断无提示（observation 计数用 `_list.length`·未注明「仅标记前 50 名」）。

### 焦点 2：路由接线（P0-0）— **agree 主体 + 2 处小修**

| 接线 | 代码证据 | 结论 |
|---|---|---|
| `_markupCue` 触发 | 双子句：① `priorTurn.intent==='knowledge_qa' && /标记\|标到地图\|在地图上\|点位\|把.*标/` ② **无条件** `/标记到地图\|标到地图\|在地图上标/`。主句「能在地图上标记出这些项目的位置吗」命中第二子句（「在地图上标」）→ **不依赖 resume·不依赖 priorTurn** → 主场景必然注入 | ✅ 强 |
| `_dataGate` 豁免 | `_markupCue ? null : _dataGate(...)` → 「标记…点位/地块」变体不再误拦 | ✅ |
| runTemplatePath 零图层出口 | `def.tool==='generate_point_layer' && newLayerCount===0 && unmatched.length` → finalStep + `exit:'answered'`（四态合法·非 ask_user/gap） | ✅ 正确 |
| `_deterministicRecover` 兜底 | 绕开 `_polys.length` 守卫·命中即 synth diagnose `{template:'generate_point_layer', names:undefined}` → FC 失败/unknown 时兜到新工具 | ✅ 生效 |
| **A-4 `_SLOT_HINT.names`** | **未补**——`harness.js:573` `_SLOT_HINT` 无 names 条目（commit diff 无此变更）→ recover→ask 路径走泛问「要做 generate_point_layer 分析，还缺 names」·无「从上一轮提取」引导 | ⚠️ **半修** |
| **`_distillTurn` rag 条件** | `(dg.rag && !dg.intent) ? 'knowledge_qa' : (dg.intent || '')`——`_assembleKnowledgeQA` 返回 diagnose 恒 `intent:'general'`（truthy）→ `!dg.intent` 恒 false → **quick-rag 轮 priorTurn.intent 仍 'general'·「归 knowledge_qa」未达成**（注释与实现不符）。功能上被 `_markupCue` 第二子句兜住（主场景不受影响） | ⚠️ 建议改 `dg.rag ? 'knowledge_qa' : ...` |

### 焦点 3：契约镜像（P0-2/3）— **agree**

- **四镜像一致**：`tool_contracts.py`（skill/tool/names required+array+aliases/when 含「标记/地图/点位」诱导）↔ `paradigm.py` GEO_TOOL_CATALOG + TEMPLATE_REGISTRY ↔ `stages.js` SKILL_DEFS（names 必填）——`validate_generate_point_layer.py` 8 断言**实测全 PASS** + `test_emc_template.py` 26 passed ✓。
- **豁免登记符合 3 条件**：KNOWLEDGE §1 第二次豁免（增量加技能条目 / eval 复采 / 静态断言守现有技能 id·`test_emc_template` 全在 + 新增 validate）✓ 记录完整。
- **注意**：8 断言全部是**静态文本断言**（正则匹配源码字符串）——修正版 plan P0-4 要求的「FC 选型正例（`tool_calls[0].function.name==='generate_point_layer'`）+ FC 选错兜底行为断言」**未落地**·现无任何行为级断言证明真能触发（见焦点 4）。

### 焦点 4：修复有效性（命门）— **逻辑闭环 ✓ · 证据等级待补**

- **真能触发（非「实现了却从不被触发」）**：代码路径确定性成立——主复现句命中 `_markupCue` 第二子句（无条件）→ ① FC 前置注入「选 generate_point_layer + 从上一轮回答提取 names」② FC 契约 `when` 含「标记/地图/点位」诱导（FC 走 when·不走铁律·glm B.2 已修）③ FC 失败/unknown → `_deterministicRecover` 兜底 → 三层保底，任一路径都到新工具。✓
- **证据等级**：目前只有静态断言 + 代码推演·**无行为级全链测试**（无 stub FC 的 e2e-seam 用例、无真实 LLM 选型样本）。建议补（见三-4）——这是「命门」的机器化证据缺口，B3 浏览器用例只覆盖真实 LLM 冒烟。
- **验收口径（tier-2 行政区级面+标注）**：与实现不符——**无 tier-2 面化**。实测「葛洲坝片区/红星路-二马路街区」经 POI 有大区域点命中（tier-1 片区级点+名称属性可标注）→ 建议口径改为「**tier-1 片区级大致点 + 名称标注 + tier-3 文字**」为当前合格；若用户坚持「行政区级面」须补 tier-2 面化（P0.5）。另须向用户确认「污水厂网一体示范区→单个污水厂点」是否接受（建议不接受·归甄别降级）。

### 焦点 5：补充与异议

1. **tier-2 面化缺失**（与修正版 plan 不一致·建议 P0.5 补或向用户明示口径变更）。
2. **tier-1 甄别缺失**（聚合类误导命中·建议补 score 阈值/聚合词检测/approx 档）。
3. `_SLOT_HINT.names` 未补（A-4 半修）。
4. `_distillTurn` rag 条件恒假（注释与实现不符·2 行可修）。
5. names>50 截断无提示。
6. **红线核查**：四态出口（answered 合法）✓·finalStep D019 未动 ✓·diagnose 增量豁免已登记（3 条件）✓·追踪编号——纯前端工具无新 F_NNN（复用 runTemplatePath provenance + `tool:executed` 派发·无后端 @track 需求）✓·knowledge_qa 禁图层契约未动 ✓。

---

## 二、通过/需修正判定

**判定：通过（附 2 项实质 quality 项 + 3 项低危建议 + 1 项测试缺口）**

- **通过理由**：7 项缺陷 6 项真修 + 1 项半修（A-4）·触发链路三层保底逻辑闭环（主场景必然触发）·305 passed 零回归（抽验 8+26 绿）·承重红线未破。
- **实质 quality 项（建议 P0.5 补·不阻塞主场景验收）**：① tier-2 面化未实现（与 plan 不一致·验收口径需向用户确认）② tier-1 甄别缺失（聚合类误导命中·「污水厂网一体示范区」→单个污水厂点位·违背「绝不强行匹配」）。
- **低危建议**：`_SLOT_HINT.names` / `_distillTurn` rag 条件 / names 截断提示。
- **测试缺口**：命门行为级断言未落地（FC 选型正例 + 选错兜底 + 全链 e2e-seam）。

---

## 三、补充建议

1. **P0.5-甄别（建议优先）**：tier-1 命中加甄别——`_hits[0].score` 阈值（如 local≥50 / amap 需 score 或名称包含完整词）+ 聚合词检测（示范区/个/类/一批/全部）→ 命中但低置信标 `match_type:'approx'` + observation「大致区域·非精确点位」；误导命中（污水厂网示范园区）归 unmatched 或 approx 标注。
2. **P0.5-面化口径确认**：向用户确认「葛洲坝片区→葛洲坝船闸景区点（片区级大致点）」是否可接受；若要求面 → 补 tier-2 面化（扫 preset 要素 name containment·不复用 FIXED_ADMIN_DISTRICTS 白名单）。
3. **低危 2 行修复**：`_SLOT_HINT.names`（「请提供要标到地图的项目/地点名，或让我从上一轮回答提取」）+ `_distillTurn` 改 `(dg.rag ? 'knowledge_qa' : (dg.intent || ''))`。
4. **命门行为测试（建议进 P0-4 补测）**：e2e-seam（`emc_session` + `__emcTest` 暴露）全链——stub `/api/v1/place/search`（全命中/部分/全未命中）+ stub FC 响应 → 断言：`_markupCue` 注入生效、FC 选 `generate_point_layer`、落图（newLayerCount≥1）、零命中 → finalStep 诚实文字（exit='answered' 非 ask_user）、`_dataGate` 变体负例（「标记…点位」不 request_upload）、recover 兜底（stub FC 返 lookup_place/unknown）。B3 浏览器用例做真实 LLM 冒烟。
5. **observation 完善**：names>50 注明「仅标记前 50 名」；截断/未完成者归 tier-3 文字并列出。

---

*Codex 组 · CB-22d 实施复验 · 2026-08-10*
*评估方只读本地不 git · 复验落 discuss/ · claude组 收到后收敛定稿*
