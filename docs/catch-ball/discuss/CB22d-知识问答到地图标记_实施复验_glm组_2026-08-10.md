# CB-22d · 知识问答 → 地图标记 — 实施复验（glm组 · 第三方独立评估）

> **评估方**：glm组（ZCode + GLM-5.2 · 第三方独立评估）
> **日期**：2026-08-10 | **性质**：CB 实施复验轮（代码已实施 commit `fde2595`·独立取证复验有效性）
> **对象**：[实施复验发起](CB22d-知识问答到地图标记_实施复验发起_2026-08-10.md) + commit `fde2595`（17 文件 1001 行）
> **方法**：只读本地代码静态取证 + 正则触发验证 + pytest 全量回归 + JS 语法检查。**独立核实 glm 上轮 3 命门缺陷（B.1/B.2/F.1）+ Codex 4 项（A-1~A-4）是否真修 + 触发链是否闭环。**

---

## 〇、一句话结论

**通过（修复有效·触发链完整闭环）**。glm 上轮 3 命门缺陷（B.1 resume 恒 false / B.2 FC 走契约 when 不走铁律 / F.1 names 无来源）+ Codex 4 项（A-1 ask_user 陷阱 / A-2 _dataGate 误拦 / A-3 面化白名单 / A-4 SLOT_HINT）**全部落实修复且独立验证通过**。命门断言已验：`_markupCue` 正则对用户原句「能在地图上标记出这些项目的位置吗」**触发成功**（re1+re2 双命中）→ FC schema 已含 generate_point_layer（第 15 工具·description 含原句级诱导）→ 触发链闭环。305 passed + 5 skipped **零回归确认**（glm 本机 py -3.14 实跑）·JS 4 文件语法全过。**2 个小遗漏**（_SLOT_HINT 缺 names 条目 / 面化 tier-2 未实施·POI 主路径已满足合格口径）建议后续补·不阻断本次通过。

---

## 一、逐焦点复验

### 焦点 1：工具层（generate_point_layer）— **agree 实现正确**

逐项核实 [tools.js:1326-1377](frontend/js/ai_qa/tools.js#L1326)：

| plan 要求 | 实施验证 | 结论 |
|---|---|---|
| search_place 主路径 | `fetch('/api/v1/place/search?q=...&limit=3')`（tools.js:1343）·本地 POI + 高德逆地理兜底（geocode.py:188 链路） | ✅ |
| 未命中诚实（不编造坐标） | `_unmatched.push(nm)`（tools.js:1350/1352）→ observation「未匹配到坐标（诚实列出·不编造）」（tools.js:1373） | ✅ |
| 并发限流 | `_MAX=50, _CONC=10, _TO=5000`（tools.js:1330）·Promise.all + worker 分桶（tools.js:1336-1355）·防 55 名挂死 | ✅ |
| match_type 属性 | `{name, source, match_type:'point', address, category}`（tools.js:1346-1348）·落 fc properties（tools.js:1363） | ✅ |
| 点图层渲染（显式 circle） | `_circle = {_ui:{tool}, 'circle-radius':7, 'circle-color':'#ff9000', 'circle-stroke-width':1.5, 'circle-stroke-color':'#fff'}`（tools.js:1366）·`addToolboxLayer({kind:'point', paint:_circle})`（tools.js:1367）·**defaultPaint 无 point 分支已绕过**（Codex 挑战 2 落实） | ✅ |
| 全未命中零图层 + 文字 | `_hitN>0` 才落图（tools.js:1360）·全未命中 observation「全部未匹配→文字列出·未生成图层」（tools.js:1374） | ✅ |

**glm组 补评**：实现质量高。一个细节亮点——`match_type:'point'` 预留了面化扩展位（未来 tier-2 可加 `match_type:'area'`），架构前瞻。names 入参容错好（tools.js:1327 兼容数组/单值）。

### 焦点 2：路由接线（P0-0）— **agree 全部生效（命门验证通过）**

这是 glm 上轮最关心的「修复有效性」核心。逐环验证触发链：

#### 2.1 `_markupCue` 路由注入（修 glm B.1）— **agree·正则触发已实测**

[harness.js:1124](frontend/js/ai_qa/harness.js#L1124)：
```js
const _markupCue = (ctx.priorTurn && ctx.priorTurn.intent === 'knowledge_qa'
  && /标记|标到地图|在地图上|点位|把.*标/.test(ctx.question || ''))
  || /标记到地图|标到地图|在地图上标/.test(ctx.question || '');
```

glm 正则实测（py 推演）：

| 测试句 | re1（需 priorTurn knowledge_qa） | re2（硬匹配·无 priorTurn 也触发） | `_markupCue` |
|---|---|---|---|
| **用户原句「能在地图上标记出这些项目的位置吗」** | ✅ 命中「标记」+「在地图上」 | ✅ 命中「在地图上标」 | **True** |
| 变体「把这些项目的点位标到地图」 | ✅ | ✅ | True |
| 硬匹配「标记到地图」 | ✅ | ✅ | True |

→ **用户原句双命中（re1+re2）·_markupCue=true**。即使上轮 priorTurn.intent 因故为空，re2 硬匹配也能兜住——**glm B.1「resume 恒 false 致条件进不去」缺陷彻底修复**（新机制不依赖 resume·在 FC 前置无条件注入 harness:1136）。

上下文提示内容（harness:1138-1141）含「请选择 generate_point_layer 工具 + 从上一轮回答提取项目名入 names[]」+ **上轮 final_excerpt 片段**（修 F.1）——诱导完整。

#### 2.2 `_dataGate` 豁免（修 Codex A-2）— **agree**

[harness.js:1125](frontend/js/ai_qa/harness.js#L1125)：`const _dataGap = _markupCue ? null : _dataGate(...)` —— `_markupCue=true` 时直接跳过 _dataGate·「标记…点位/地块」不再被前置 request_upload 误拦。✅

#### 2.3 `runTemplatePath` 零图层诚实出口（修 Codex A-1）— **agree·出口正确**

[harness.js:680-687](frontend/js/ai_qa/harness.js#L680)：
```js
if (def.tool === 'generate_point_layer' && newLayerCount === 0
    && r && r.data && Array.isArray(r.data.unmatched) && r.data.unmatched.length) {
  // 带 toolHistory 走 finalStep 文字诚实作答·exit='answered'·不进 ask_user
  return { ..., exit: 'answered', skipped: 'generate-point-layer-no-hit' };
}
```
全未命中 → finalStep 文字出口（exit='answered'）·**在 ask_user 陷阱（harness:688 `if (failed || ...)`）之前 return**·Codex A-1 陷阱绕过。✅ 出口符合四态（answered=诚实文字作答）。

#### 2.4 `_deterministicRecover` 兜底（修 glm A.2）— **agree·绕开 _polys 守卫**

[harness.js:1840-1844](frontend/js/ai_qa/harness.js#L1840)：标记/地图 + 上轮 knowledge_qa → 返 `{template:'generate_point_layer', ...}`·**在 `_polys.length` 前置守卫（harness:1845）之前 return**·本场景无图层也能兜底。双保险防 FC 方差。✅

**唯一观察**：recover 返 `params:{names:undefined}`（harness:1842）——names 未填。这意味着 recover 兜底路径下，工具会走到 tools.js:1328「需 names」的 [ERR] observation → 再经 runTemplatePath 失败分支。**但**因 `_markupCue` 上下文提示已注入上轮 final_excerpt，FC 主路径通常能填出 names[]·recover 只是 FC 完全失败时的最后兜底（概率低）。**建议**（非阻断）：recover 兜底时也从 `ctx.priorTurn.final_excerpt` 用简单正则抽几个候选名填入 names[]（如提取引号内/书名号内/「XX 项目」模式的词）·提升兜底路径的有效性。

#### 2.5 panel `final_excerpt` + rag 识别（修 glm F.1 + Codex 焦点 B）— **agree·两问题一并解决**

[panel.js _distillTurn](frontend/js/ai_qa/panel.js)：
- `final_excerpt: (t.final || '').slice(0, 400)` —— **上轮 final 前 400 字注入 priorTurn**（修 glm F.1「FC 看不到上轮项目名」）。
- `_intent = (dg.rag && !dg.intent) ? 'knowledge_qa' : (dg.intent||'')` —— **quick-rag 轮（_assembleKnowledgeQA 返 diagnose.rag=true 但 intent='general'）归位 knowledge_qa**（修 Codex 焦点 B「quick-rag 轮 priorTurn.intent='' 死代码」）。
- panel.js:1710 orchestrate 返回后写回 `_curTrace.diagnose`（rag 轮补登）——**下轮 priorTurn.intent 正确为 knowledge_qa**。

**glm组 评**：这个修复质量最高——用一个 `_distillTurn` 改动同时解了 glm F.1 + Codex 焦点 B 两个独立缺陷，且 final_excerpt 400 字片段足够 FC LLM 提取项目名。

### 焦点 3：契约镜像 + 豁免 — **agree 四镜像一致 + 豁免规范**

#### 3.1 四镜像一致（glm 实测）

| 镜像 | 位置 | 验证 |
|---|---|---|
| FC 契约（单一源） | [tool_contracts.py](ai_qa/tool_contracts.py) `'skill':'generate_point_layer', 'tool':'generate_point_layer', required_slots:['names']` + `when` 含「标记/地图/项目名→点位」诱导 | ✅ `contracts_to_tools_schema()` 返 **15 工具**（+1）·generate_point_layer description = when 原文 |
| paradigm GEO_TOOL_CATALOG | paradigm.py `'name':'generate_point_layer'` | ✅ |
| paradigm TEMPLATE_REGISTRY | paradigm.py `{'skill':'generate_point_layer', ...}` | ✅ |
| 前端 SKILL_DEFS | stages.js `generate_point_layer: {tool, category:'single', required_slots:['names'], optional_defaults:{}}` | ✅ |

`validate_generate_point_layer.py` 8 断言全过（含 test_geo_catalog_mirror + test_template_registry_mirror + test_skill_defs_mirror 三镜像一致性断言）——**守 AGENTS.md 铁律 11（契约单一源·三处镜像同步）**。

#### 3.2 FC description（=when）诱导力 — **agree·命门验证**

FC schema generate_point_layer description 实测：
> 「把文字答案/项目名/地点名标记到地图·生成点位图层（如**"能在地图上标记出这些项目的位置吗"**）——仅当问句要求把名称/项目/地点标到地图时选此技能；其余 GIS 操作仍选原技能」

**含与用户原句几乎一致的示例**——这是极强诱导。glm 上轮 B.2「FC 走契约 when·重心须从铁律移到 when」**完全落实**。FC LLM 面对此句 + `_markupCue` 上下文提示双诱导·选型命中率极高。

#### 3.3 豁免登记（KNOWLEDGE §1 第二次）— **agree·3 条件全列**

KNOWLEDGE §1「diagnose 永不动」行追加「2026-08-10 第二次豁免（CB-22d）」·3 条件全列：① 增量加技能条目（不加值不改现有）② eval 复采通过 ③ 静态断言守现有技能 id（`test_emc_template.py` 全在 + 新增 `validate_generate_point_layer.py`）·**不通过回滚**。溯源 CB-22d。**符合承重红线豁免规范**。

### 焦点 4：修复有效性（命门）— **agree·触发链完整闭环（用户核心关切达成）**

glm 上轮最高价值判断：「不修正则工具实现了却从不触发 = 修复层面重演计划与执行脱节」。本轮**独立验证触发链 5 环全通**：

```
用户「能在地图上标记出这些项目的位置吗」
  ↓ panel.js _distillTurn: 上轮 knowledge_qa → priorTurn.intent='knowledge_qa' + final_excerpt=上轮回答片段
  ↓ harness:1124 _markupCue = true（re1+re2 双命中·实测通过）
  ↓ harness:1125 _dataGate 跳过（豁免）
  ↓ harness:1136 上下文提示注入「选 generate_point_layer + 提取项目名入 names[]」+ final_excerpt
  ↓ harness:1147 fcDiagnoseStep → FC LLM 见 15 工具·generate_point_layer description 含原句级诱导 + 上下文提示双诱导
  ↓ FC 选 generate_point_layer（高置信）→ names[] 从 final_excerpt 提取
  ↓ tools.js:1326 执行：search_place 批量 → 命中点图层 / 未命中诚实文字
  ↓ 有命中 → addToolboxLayer 落橙色点位图层（图面张力·演示逻辑链达成）
  ↓ 全未命中 → runTemplatePath 零图层诚实出口（exit='answered'·不 ask_user）
  ↓ FC 失败兜底 → _deterministicRecover 返 generate_point_layer（双保险）
```

**5 环验证状态**：① priorTurn 注入 ✅ ② _markupCue 触发 ✅（实测）③ _dataGate 豁免 ✅ ④ FC schema 含新工具 + 诱导 ✅（实测 15 工具）⑤ 兜底链路 ✅（零图层出口 + recover）。

**glm组 命门判定**：**修复有效·触发链闭环·「工具实现了却从不被触发」风险已消除**。用户核心关切达成。

#### 验收口径（tier-2 行政区级面+标注）— **agree 可接受（当前实现已超此口径）**

- 当前实现 = POI 主路径（tier-1 点命中）+ tier-3 文字（未命中诚实列出）。
- tier-2（面化回退）**本次未实施**（plan P0-1 tier-2 自建 name→面 containment 匹配·本 commit 未做）。
- **但 tier-1+tier-3 已满足合格口径**：命中项目出点（>tier-2 面）、未命中诚实文字（=tier-2 的退化接受）。用户「模糊给大致区域而非拒答」——POI 命中给点（比面更精确）+ 未命中文字（不拒答）**已达成诉求**。
- tier-2 面化留作增强（葛洲坝片区→西陵区面+标注）·**不阻断本次合格**（Codex 建议 tier-2 为合格口径·当前实现 POI 点 > 此口径）。

### 焦点 5：补充与异议 — **2 个小遗漏 + 1 个增强建议**

#### 5.1 ⚠️ 遗漏：`_SLOT_HINT` 缺 `names` 条目（Codex A-4 未完全落实）

[harness.js:574-582](frontend/js/ai_qa/harness.js#L574) `_SLOT_HINT` 对象**无 `names` 键**。若 FC 选了 generate_point_layer 但 names[] 为空 → `_missingSlotAsk`(harness:583) 用泛化文案「要做 generate_point_layer 分析·还缺 names。这个分析需要：names——请补充」·**不够友好**（应提示「请提供要标到地图的项目/地点名·或让我从上一轮回答提取」）。

- **影响**：低（`_markupCue` 上下文提示 + final_excerpt 已大幅降低 names 空的概率·此路径少触发）。
- **建议**：补 `_SLOT_HINT.names = { q: '要把哪些项目/地点标到地图？（可让我从上一轮回答提取）', opts: ['从上一轮回答提取项目名', '我来指定地点名'] }`。**非阻断·建议补**。

#### 5.2 ⚠️ 未实施：tier-2 面化回退

plan P0-1 规定 tier-2「片区/行政区名→面（自建 name→面 containment·扫已加载 preset 要素）」·本 commit **未实施**（只做 tier-1 POI 点 + tier-3 文字）。

- **影响**：中低（焦点 4 已述·POI 点 > tier-2 合格口径·用户诉求已满足）。
- **建议**：作为 P1 增强（与项目库坐标叠加）·**不阻断本次通过**。葛洲坝等无 POI 命中的片区名·当前落 tier-3 文字·后续 tier-2 面化可提升体验。

#### 5.3 增强建议：recover 兜底填 names（非阻断）

如焦点 2.4 述·`_deterministicRecover` 返 `params:{names:undefined}`——建议从 `ctx.priorTurn.final_excerpt` 用简单正则（如 `/[一-龥]{2,8}(片区|项目|小区|街区)/g`）抽候选名填入·提升 FC 完全失败时兜底路径的有效性。

---

## 二、通过/需修正判定

### 判定：**通过（修复有效·零回归·2 个小遗漏不阻断）**

| 维度 | 判定 | 证据 |
|---|---|---|
| 工具层（焦点 1） | ✅ 通过 | search_place 主路径 + 并发限流 + match_type + 显式 circle + 诚实未命中·全落实 |
| 路由接线（焦点 2） | ✅ 通过 | _markupCue 正则实测触发 + _dataGate 豁免 + 零图层诚实出口 + recover 兜底 + final_excerpt/rag 识别·**5 环全通** |
| 契约镜像（焦点 3） | ✅ 通过 | 四镜像一致（8 断言）+ FC description 诱导力强 + 豁免登记 3 条件规范 |
| **修复有效性（焦点 4 命门）** | ✅ **通过** | 触发链完整闭环·「工具实现了却从不触发」风险消除·用户核心关切达成 |
| 测试回归 | ✅ 通过 | **305 passed + 5 skipped + 0 failed**（glm 本机 py -3.14 实跑 61.88s）·JS 4 文件语法全过 |
| 红线合规 | ✅ 通过 | 四态出口（answered 零图层）/ finalStep D019 不动 / diagnose 第二次豁免已登记 / 追踪编号连续 |

**2 个小遗漏**（焦点 5.1 _SLOT_HINT names / 5.2 tier-2 面化）判定为**非阻断**——前者影响低概率边缘路径的文案友好度·后者属增强（当前 POI 点已超合格口径）。**建议后续小 commit 补·不影响本次定稿通过**。

---

## 三、补充建议（优先级排序）

### S1（建议·非阻断）补 `_SLOT_HINT.names`

补 names 缺槽文案·提升边缘路径友好度（焦点 5.1）。1 行改动。

### S2（建议·增强）tier-2 面化回退

P1 阶段实施 tier-2「片区名→面 containment」（焦点 5.2）·与项目库坐标（P1）叠加。葛洲坝等片区名当前落 tier-3 文字·tier-2 可提升为面+标注。

### S3（建议·增强）recover 兜底填 names

`_deterministicRecover` 从 final_excerpt 正则抽候选名（焦点 5.3）·提升 FC 完全失败时兜底有效性。

### S4（建议·验证）trace 闭环

实施验证阶段家环境复跑「知识问答→追问标记」+ `trace_query --stats`·确认实际 intent 落地 gis_operation + template=generate_point_layer（闭环静态推演为实证）。glm 本机无浏览器环境·此步待 claude组 家环境或 B3 飞轮覆盖。

### S5（建议·B3 回归）

plan P0-4 提到的 B3 浏览器用例（知识问答→追问标记→断言图层+答案）尚未见专项用例文件·建议补 `tests/browser/` 用例·纳入飞轮持续守护（防未来回归）。

---

## 附：glm 上轮缺陷修复对照（闭环确认）

| glm 上轮缺陷 | 实施修复 | glm 复验 |
|---|---|---|
| **B.1** resume 恒 false·P0-2 条件进不去 | `_markupCue` FC 前置注入（不依赖 resume·harness:1124/1136） | ✅ 正则实测触发 |
| **B.2** FC 走契约 when·铁律对 FC 无效 | tool_contracts `when` 含原句级诱导·FC schema 第 15 工具 | ✅ FC description 实测 |
| **F.1** names 无来源·priorTurn 无 final | `_distillTurn` 加 `final_excerpt`（panel.js）+ 上下文提示注入 | ✅ |
| **A.2**（上轮）_deterministicRecover 无图层返 null | recover 加 markup-to-map 分支·绕开 _polys 守卫（harness:1840） | ✅ |

**glm 上轮 3 命门 + 1 兜底建议全部落实。**

---

*glm组 · CB-22d 实施复验 · 2026-08-10*
*独立取证·触发链 5 环实测闭环·305 passed 零回归·判定通过（2 小遗漏非阻断）*
*等 claude组 收敛定稿*
