# PT-CB12 · T1 guard 统一接线执行记录（Codex · 2026-08-22）

> 任务书：`PT-CB12-guard接线与ACP契约_任务书_zcode-2026-08-22.md` 件①。依据：claude 全批终审 N1（挂表≠有守卫·下批第一顺位）。
> 分支 `EMC_harness_dsh`·commit 前缀 `PT-CB12(T1):`·基线 **500 passed + 4 skipped**（6b7dab64 首次全绿）。零 pull 零 push。

## 一 改动（行为零变化·接线重构）

**`tools/mcp_server_emc.py`**：

1. **9 件带面输入工具入口统一改调 `_guard_check`**（原 8 处体内 `_reject_analysis_output` 直调全部移除·zonal/buffer/rank/grid_aggregate/compare_regions/area_stats/nearest/overlay + trend 原有接线保持）——守卫语义零变化：同参同序（overlay 双参合并为一次调用·检查顺序仍 a→b）；
2. **`_GUARD_SPECS` 补齐 9 件**：新增 `compare_regions.usage_params=('boundaries',)` 与 `area_stats.usage_params=('boundary',)`（其余 7 件原声明保留·参数名与真实签名对齐，`_audit_input_surfaces` 启动核对兜底）；
3. **单一通路**：`_reject_analysis_output` 现仅剩定义(:106) + `_guard_check` 内部调用(:1006) 一处消费——工具体内零直调，防双份漂移（任务书规格④推荐方案）；
4. **表头注释更新**：N1 注记（"挂表不等于自动守卫·仅 trend 接线"）替换为「全量接线完成」+ 新工具登记纪律说明。

## 二 细节说明（一处语义微调·非行为破坏）

`compare_regions` 逐项守卫的拒绝 hint 参数名由原 `boundary=xxx` 变为 `boundaries=xxx`（_guard_check 以 spec 参数名命名）——与函数真实入参名一致（原直调硬写 'boundary' 反而不准确）；既有测试断言 `'analysis_output' in hint`（不含参数名），全绿不受影响。

## 三 测试与门禁

- 新增 1 例文档化断言：`test_guard_check_undeclared_tool_passes_documented_behavior`——未声明工具调 `_guard_check` → 放行（None·fail-open 不崩），防线=B4 启动核对 WARN + 全量接线约定（新带面输入工具必须登记+接线）；
- 既有 G-2 拒绝用例（散在 test_mcp_server_emc：zonal/rank/buffer 三工具 + grid/compare/overlay 分析层拒绝 + trend guard 拒绝 + `test_analysis_output_rejected_three_tools`）**原样全绿**；
- `python -m pytest tests/test_mcp_server_emc.py -q` → **68 passed**（67+1）；
- 全量 `python -m pytest tests/ -q` → **collected 505：501 passed / 4 skipped / 0 failed**——较基线 500+4 净增 1（本批新断言）·零回归 ✓。

## 四 改动文件

- `tools/mcp_server_emc.py`（_GUARD_SPECS 表 + 9 处接线 + 注释）
- `tests/test_mcp_server_emc.py`（+1 断言）
- 本记录

> Codex · 2026-08-22 · PT-CB12 T1 完成（守卫单一通路·行为零变化·门禁 501+4），待主手回收。
