# 壳工程深度审计 · 收敛记录与双能力评估（zcode·2026-08-23 深夜）

> 输入：Qoder 审计（113 行·20 发现·架构/安全/性能视角）+ claude 审计（99 行·20 发现·正确性/测试/集成视角）。收敛方=zcode。

## 一 交叉对账

### 两组同判（互证发现）

| 发现 | Qoder 编号 | claude 编号 | 收敛判定 |
|---|---|---|---|
| 并发/线程池风险 | QA-01+QA-02（P1·深入到 dsh profile 竞争） | C3-3（P2·并发闸） | **P1**·取 Qoder 更深判定（线程池饥饿+profile 双重风险） |
| 前端无超时护栏 | QA-03（P2） | C2-1（P2·引 CB-22i 先例佐证） | **P2**·互证成立 |
| hint 链断裂 | QA-12（P3·降级卡丢 hint） | X-2+C4-2（P2·**三层全断**：bus→wire→panel） | **P2 升档**·取 claude 更完整链路分析 |

### 独有发现（互补面）

| 来源 | 发现 | 收敛判定 |
|---|---|---|
| **Qoder 独有** | QA-04（P2·Windows fallback 死路径+未捕获异常→500） | **P2 确认**——claude 未入此层 |
| Qoder | QA-05（P2·输出无上限+markdown 注入面） | P2·与 claude C5-5 合并处置 |
| Qoder | QA-06（P2·_summary 全量序列化性能） | P2 |
| Qoder | QA-07（P2·8600 MCP 依赖未入环境清单） | **P2 确认**——运维盲区 |
| Qoder | QA-08（P2·C3 端点零自动测试） | 与 claude C5-3 合并 |
| Qoder | QA-15（P2·引擎分发矩阵无单测） | P2 |
| **claude 独有** | **C3-1（P1·question 超长→OSError→500）** | **P1 确认**——真 500·Qoder 未入此层 |
| claude | C1-1（P2·caliber 摘要零发射·契约说必带实际不发） | **P2 确认**——契约与实现相悖 |
| claude | C4-1（P2·dsh 轮兜底追问不搭调） | P2·UX 面 |
| claude | X-1（P2·provenance 孤儿字段） | P2 |
| claude | C5-1（P2·S4 主路 wire 未过 S6 校验器） | **P2 确认**——契约验收条未全履行 |
| claude | C5-2（P2·S5 chips 三态零独立测试） | P2——**主手此前评估也标注过此项** |

### 正面确认（两组同✓）

事件四族与 schema 逐字段吻合（claude 核过）·向后兼容真兼容（Qoder 核过）·三引擎隔离无竞态（两组均✓）·chips 数据流闭合（两组均✓）·降级不伪造（Qoder✓）·依赖 DAG 干净（Qoder QA-18✓）。

## 二 收敛总裁决：**有条件通过**

**合并 P1 三件（必修·下批 SHELL2(FIX)）**：
1. **QA-01+02**：dsh 端点并发安全（async 化+有界信号量+单飞闸）
2. **C3-1**：question 长度上限+OSError 捕获
3. 以上修复配套 QA-08 端点单测

**P2 限期修（下批或随批）**：QA-03/C2-1（前端超时）·QA-04（fallback 封死）·QA-05（输出上限）·C1-1（caliber 契约对齐）·X-1/X-2（provenance 渲染+hint 展示）·C4-1（dsh 兜底追问）·C5-1/C5-2（补测）

**P3 记账**：其余 15 项

**不阻塞明日验收**：单用户场景 T1-T7 可正常测——P1 均为并发场景（多用户/超长输入）。

## 三 双能力评估（审计能力研判）

### Qoder 审计能力：**A（架构审计标杆）**

| 维度 | 评 | 依据 |
|---|---|---|
| 深度 | **A+** | 线程池饥饿链（sync→anyio 40 槽→拖垮全部端点）·dsh profile 并发竞争·subprocess 注入面分析——到系统架构层 |
| 独有发现质量 | **A** | QA-04（Windows fallback 死路径）·QA-07（8600 隐性依赖）——两个 claude 未触达的运维级盲区 |
| 正面确认 | **A** | 6 项 ✅ 主动标注（不是只找茬·还确认了什么没问题）——审计成熟度标志 |
| 白话摘要 | **A** | 「发电机保险丝」比喻精准 |
| 盲区声明 | **A** | 7 项诚实声明（含「dsh 并发耐受未实测」的推断属性标注） |
| 弱点 | — | 契约逐字段比对深度略逊 claude（QA 未做 schema 逐字段核） |

### claude 审计能力：**A-（正确性审计强·接近 A）**

| 维度 | 评 | 依据 |
|---|---|---|
| 契约合规 | **A+** | 四族事件与 8 份 schema **逐字段吻合**验证——两组中唯一做到字段级 |
| 独有发现质量 | **A** | **C3-1（P1·超长→500）是本审计最关键的单件发现**（Qoder 漏）·X-1/X-2 跨层断链追踪（发射→wire→渲染三层全断） |
| 测试缺口 | **A** | C5-1（S4 主路 wire 零校验）·C5-2（chips 零独立测试）——精准且可操作 |
| 白话摘要 | **A-** | 「门诊大厅/零件对不上/说了不做」比喻合格·略逊 Qoder 的凝练度 |
| 盲区声明 | **A** | 5 项诚实（含「71 处 hooks 未逐一核对·信任 S4 结论」的依赖声明） |
| 弱点 | — | 运维/基础设施层覆盖较薄（线程池/进程管理/性能面基本未入） |

### 审计能力对比总结

| | Qoder | claude |
|---|---|---|
| **最强维度** | 系统架构/运维风险 | 契约合规/测试缺口/跨层追踪 |
| **覆盖面** | 基础设施层厚·字段层薄 | 字段层厚·基础设施层薄 |
| **P1 抓获** | 2 件（并发双重） | 1 件（超长 500） |
| **互补性** | **极佳**——两组独有发现零重叠·合并后覆盖面完整 | 同左 |

**结论**：Qoder A + claude A- = **双组审计互补性完美的实证**——20+20=40 发现中仅 3 项重叠（互证），其余全为独有（互补）。这种分视角交叉审计模式值得固化为标准流程。

**claude 升档判定**：本次审计表现 A-（此前执行能力 B+）——**如果下次复杂执行件也能 A- 级·综合可升 A-**。C3-1 的 P1 发现+X-2 三层追踪证明了 claude 的技术深度底子在·只是执行侧还没给足舞台。

> zcode 主手 · 2026-08-23 · 收敛+双评完毕
