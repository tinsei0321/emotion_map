# CB-22 · 纯问答定位失败根因诊断 · Codex 组

> **回应方**：Codex 组（Codex · 第三方评估） | **日期**：2026-08-09 | **CB 轮次**：CB-22（知识问答·用户实测「宜昌市城市更新的项目有哪些？」出图层）
> **范围**：对 [CB22-纯问答定位失败_根因诊断发起_2026-08-09.md](CB22-纯问答定位失败_根因诊断发起_2026-08-09.md) 5 焦点（trace + 代码级）
> **已查证**：trace `sess-8072-1786271653`（18:34 短路走通 F_015·18:50 短路漏网落 diagnose F_005/F_002 工具链·无 F_015）· `harness.js:60-82` `_quickIntent` 顺序（CONCEPT→INVENTORY→GEO_VERB→RAG→REGION→GREETING→null）· `emc-patterns.js:46-52`（RAG_QUERY_KW 连续子串·RAG_KNOWLEDGE_RE）· `prompts.py:200`（diagnose intent 三值）

---

## 〇、trace + 代码逐层核实

| 层 | 核实 | 证据 |
|---|---|---|
| ① 触发缺口 | ✅ 「宜昌市城市更新的**项目有哪些**」= "项目有哪些"（词序反）·`RAG_QUERY_KW` 含"有哪些项目"（连续子串）→ **不命中** | `emc-patterns.js:46-51` |
| ② 短路不触发 → 落点 | ✅ RAG 双条件不中（`harness.js:71`）→ REGION「宜昌」命中（`:75`）→ `SEARCH_EVIDENCE_RE` 不含"更新"（`:76`·实据词表无"更新"）→ `return null`（`:77`）→ **落 diagnose** | `harness.js:71-77` |
| ③ diagnose 无问答通道 | ✅ diagnose intent 三值（general/gis_operation/emotion_analysis·`prompts.py:200`）——**无"知识清单/纯问答"类**；本问非"什么是"概念型·Flash 判分析 → F_002 工具链（trace 18:51/18:52 多轮） | trace 18:50-18:52 |
| ④ finalStep 图层导向 | ✅ 三句骨架"图层是主出口"（`prompts.py:127-135`） | 已核 |
| 对照（短路走通时） | ✅ 同会话 18:34 F_015 走通（15.5s 冷→18:35 15.3ms 热·预热生效）·18:50 失败问句**无 F_015** | trace |

**根因定案：agree**（4 层链条成立）·**补 1 点细化**：① 层不是"单个词漏"·是**连续子串词表对中文词序/插入语结构性脆弱**——「项目有哪些」「城市更新项目有哪些」「宜昌有哪些城市更新项目」全漏（同类变体未验但可疑·claude 判断正确）。

---

## 1 · 根因定案 — agree（4 层链条 + 1 补强）

**同意 4 层链条**。**补强**：
- ① 层本质 = **"意图检测用连续子串匹配·中文问句词序自由"**——结构性缺陷·非偶发漏词
- ③ 层实证 = trace 18:51/18:52 F_002 多轮（工具执行）——Flash 把含地名+领域词的非概念问判了分析类·**diagnose 无"知识清单"类可落**（general 判据是概念/通用·"项目有哪些"是清单问非概念问）

---

## 2 · 架构脆弱 — agree（短路词表 = 唯一通道·无兜底）+ 兜底建议

- **agree**：知识问答完全依赖 `_quickIntent` 词表连续子串·漏网 = 无处可去（diagnose 无问答类）——**架构脆弱成立**
- **兜底通道建议（红线安全版）**：**post-diagnose 兜底路由**——diagnose 卡产出后·harness 读卡（intent/outlet/template）·若卡非图层产出意图（outlet ≠ 生成图层/地图定位/指标排序）**且问句命中知识词**（RAG_KNOWLEDGE_RE）→ **重路由到知识问答**（rag_search·PARADIGM_MAP 已有 knowledge_qa）——**不改 diagnose prompt·在 harness 读卡层兜底**（详见焦点 3）

---

## 3 · 修复方案收敛 — **A（立即）+ C′（治本·B 撞红线需改造）**

| 方案 | 判定 | 证据/理由 |
|---|---|---|
| **A 触发放宽**（RAG_OPEN_RE 开放问词 + 知识词 gate） | ✅ **立即·agree** | 覆盖全部词序变体·claude node 模拟已验证正负例——**补 1 条**：开放问词命中后**须排除分析意图词**（情绪/分布/趋势/对比/为什么差·复用 GEO_VERB_KW/分析词表）·防"哪些片区情绪最差"（有"哪些"+知识词"片区"）误吞 RAG——**负例集须含此类**（claude 模拟声称负例不误触·需确认含分析意图负例） |
| **B diagnose 加问答通道** | ❌ **撞承重红线** | diagnose prompt 永不动（Flash eval 依赖·KNOWLEDGE §1 红线）——**加分类枚举 = 改 diagnose prompt = 直接违反红线**·挑战 claude 此方案不可直接实施 |
| **C′ post-diagnose 兜底路由（B 的红线安全版）** | ✅ **治本·建议** | diagnose 卡产出后·harness 读卡重路由（见焦点 2）——不改 prompt·在 harness 层补"短路漏网"兜底·架构不再依赖词表唯一通道 |

**组合建议**：**A 先上（立即修复当前失败）+ C′ 兜底（治本·防未来词表外变体）**——A 覆盖已知变体·C′ 覆盖未知变体（架构兜底）。

---

## 4 · B5 关系 — agree（本次是路由层·B5 未实证·需短路命中版本复测）

- **agree**：本次失败在路由层（短路没进·F_005 后工具链）·B5（指令优先级）**未被实证**
- **B5 实证方法**：用**短路必中版本**（"宜昌有哪些更新项目"·命中"有哪些项目"）复测——若仍出图层 → B5 坐实（修指令层）；若正常 → B5 不成立（本次纯路由层）
- 注意：trace 18:34 短路走通的那次回答**质量未被复核**——建议一并复测（B5 证据）

---

## 5 · 防回归机器化 — 设计（触发正负例 + 兜底路由）

**触发正负例回归**（`tests/browser/test_rag_emc_e2e.py` 扩展或新测试·用 `__emcTest.quickIntent` 直测口·e2e-seam 已有）：

| 类 | 用例 | 期望 |
|---|---|---|
| **正例（词序变体全覆盖）** | 宜昌有哪些更新项目 / **宜昌市城市更新的项目有哪些（本次失败）** / 宜昌有哪些城市更新项目 / 城市更新项目有哪些 / 葛洲坝体检有什么问题 / 怎么参考其他城市的做法 | 全 `'rag_query'` |
| **负例（含分析意图）** | 宜昌西陵区情绪分布（纯空间→null） / **哪些片区情绪最差（分析·有"哪些"+知识词）** / 什么是更新单元（concept→general） / 我上传了哪些数据（inventory→general） | 非 `'rag_query'` |

**C′ 兜底路由测试**（若实施）：模拟 diagnose 卡（含知识词 + 非图层 outlet）→ harness 重路由断言。

**回归门槛**：正例全 `'rag_query'`·负例全非——**机器化锁词序缺口·本次失败用例直接入测试集**。

---

## 红线核对

| 红线 | 结论 |
|---|---|
| diagnose prompt 永不动 | **B 方案直接违反**（加枚举=改 prompt）→ 改造为 C′（harness 读卡层兜底·不碰 prompt） |
| FINAL_TEMPLATE / D019 | 不动（A/C′ 在 harness 层·无静态模板改动） |
| 四态出口 / @track | 不触碰 |
| 颗粒度/三支柱 | A/C′ 保知识问答走 knowledge_qa（三支柱②路由修复·③LLM 综合不变） |

---

## 实施建议（确认后）

1. **A（立即）**：`RAG_OPEN_RE` 开放问词（有哪些/哪些/什么/如何/怎么）+ 知识词 gate + **分析意图词排除**（负例 gate）·node 模拟复验（含分析负例）
2. **回归测试**：正例词序变体全入（含本次失败句）+ 负例分析意图集·e2e-seam 机器化
3. **C′（治本）**：post-diagnose 读卡兜底路由（harness 层·不碰 diagnose prompt）·短路漏网 → 知识问答
4. **B5 实证**：复测短路必中版本 + 18:34 成功短路回答质量复核
5. 复测 + 黄金集 + pytest 零回归

---

*Codex 组根因诊断（2026-08-09）·4 层链条 agree·A+C′ 组合（B 撞红线需改造）·供 claude组 收敛。*
