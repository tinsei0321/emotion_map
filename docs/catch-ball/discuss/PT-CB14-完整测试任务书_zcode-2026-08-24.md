# PT-CB14 · EMC×dsh 完整测试任务书（zcode 主手签发·Qoder 执行引导·2026-08-24）

> 执行：**Qoder（测试引导+记录）**·用户（真人操作员）。zcode 回收裁决。
> 性质：壳工程全链闭环后的**首次完整结构化测试**——对标功能体系与展示效果。
> 环境（已就绪·主手 08-24 预检三绿）：8600 MCP（18 工具）/ 3080 dsh web / 8080 EMC（三引擎）/ 基线 574+2。
> 纪律：测试只读不改码；发现缺陷**记录不现场修**（记入缺陷清单·主手裁决）；全程留痕（截图+回执）。

---

## 一、测试体系结构（四层·对标功能体系）

```
L1 服务栈层（自动化·Qoder 直跑）
L2 工具面层（18 MCP 工具·Qoder 直调）
L3 交互体验层（3080/8080 双窗口·引导用户操作）
L4 展示效果层（地图渲染/图例/面板·随 L3 顺带评）
```

## 二、L1 服务栈层（Qoder·10 分钟）

| # | 项 | 命令/方法 | 通过标准 |
|---|---|---|---|
| 1.1 | MCP 8600 握手 | curl POST /mcp（Accept: application/json, text/event-stream 双头）+ initialize | protocolVersion 返回 |
| 1.2 | 工具清单 | tools/list | **18 件全列**（list_data…emc_status） |
| 1.3 | 8080 health | GET /api/v1/health | status ok + engines 三项 |
| 1.4 | 3080 web | GET http://127.0.0.1:3080/ | HTTP 200 |
| 1.5 | dsh 引擎端点 | POST /api/v1/aiqa/dsh_engine {"question":"hi"} | ok:true·elapsed<30s·非空回答 |

## 三、L2 工具面层（Qoder 直调·30 分钟·每工具一用例）

**数据类**：list_data（清单含 12345 层）/ kb_facts（keyword=EMC 命中身份卡）/ rag_query（"体检口径" hits>0 带 caliber）
**空间分析类**：zonal_stats（12345×174 社区·top5）/ rank（worst top5）/ buffer（500m）/ grid_aggregate（800m）/ compare_regions（西陵 vs 伍家岗）/ hotspot_analysis / nearest_analysis / overlay_analysis / area_stats / trend_analysis
**输出类**：outlet_card（组装）/ report_assemble / **render_spec**（投递后 8080 出图层）/ **render_file**（直显）
**守卫类**：**负测试**——zonal_stats(boundary=base_174_aggregate_area) 应拒（铁律 7 三段式报错）/ rag_query synthesize 参数行为
**状态类**：emc_status（ready）

每项记录：调用参数/返回摘要/耗时/口径字段在否。**守卫负测试必须做**（红线验证）。

## 四、L3 交互体验层（引导用户·30 分钟）= T1-T7 原清单

| # | 窗口 | 操作 | 预期 |
|---|---|---|---|
| T1 | 3080 | 问「12345 诉求最密集的 5 个社区，显示在地图上」 | 正常填色/图例/悬停社区名+数值/面板无误导拾色器 |
| T2 | 3080 | 三连问：800m 方格统计/两区对比/聚集性 | 各 1-2 次工具调用出结论·口径标签在 |
| T3 | 3080 | 问「各行政区面积占比」 | 面积表·不冒充情绪结论 |
| T4 | 3080 | （顺带）观察错配自纠 | 一次拒绝→自动改对 |
| T5 | 3080 | 方格层参数面板换色带/反向/零值显隐 | 即时生效·图例跟随 |
| T6 | 8080 ?engine=light | 2-3 个知识问题 | 流式+追问建议条 |
| T7 | 8080 ?engine=dsh | 「西陵区最差社区」 | 思考等待→完整答案+地图联动 |

## 五、L4 展示效果层（随 L3 顺带·评分制 1-5）

对 T1/T5/T7 的地图效果评：填色语义正确性/图例可读性/悬停信息完整度/图层切换干净度/时间响应体感。

## 六、产出要求

落盘 `docs/catch-ball/discuss/PT-CB14-完整测试记录_Qoder-2026-08-24.md`：
1. 四层逐项 pass/fail/评分表（L1-L2 附命令回执·L3-L4 附用户截图或确认语）；
2. **缺陷清单**（编号 D-起·严重级 P0-P3·复现步骤）；
3. 总评（功能体系完整度 %+展示效果评价+建议）；
4. EMC 仓唯一新增写入物·git 主手代提交。

## 七、流程

Qoder 先跑 L1-L2（独立完成）→ 整理 L3-L7 用户操作指引交用户（一次测完不打断）→ 用户测完 Qoder 汇总 L3-L4 → 全记录落盘 → 停下等 zcode 回收 → 缺陷进 CB 裁决修复批。

## 八、参照文档

- `docs/catch-ball/用户复测滚动清单.md`（T1-T7 原文）
- `docs/render-contract.md`（渲染契约·展示效果对标）
- `docs/brain-adapter.md`（dsh 引擎契约）
- 同步报告（office 环境·H 盘 reports/2026-08-24）

---

> 用户已令测试移交 Qoder 引导。本任务书即定稿·Qoder 可直接开工。
