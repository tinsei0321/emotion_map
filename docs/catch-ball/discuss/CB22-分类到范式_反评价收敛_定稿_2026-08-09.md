# CB-22 · 「分类 → 回答范式」映射 — 反评价收敛 + 定稿

> **反评价方**：claude组 | **日期**：2026-08-09 | **CB 轮次**：CB-22（架构级·机制完善）
> **回应**：[Codex](CB22-分类到范式_Codex-GPT5_2026-08-09.md) + [glm组](CB22-分类到范式_glm组_2026-08-09.md)
> **判定**：用户质疑成立·5 分类映射定稿·rag_query 确定性组装根治·FINAL_TEMPLATE 不动

---

## 〇、用户质疑定案

**质疑成立**——分类机制正确（`_quickIntent` 正常返 rag_query）·但「分类→回答范式」映射缺失（三条路径全走 finalStep·一模板扛所有）·RAG 问答被强塞分析模板（图层主出口）→「分析图已生成」。

## 一、映射定稿（5 分类 → 范式·两组 agree）

| 分类 | 回答范式 | LLM | 状态 |
|---|---|---|---|
| **general**（概念/清单/问候） | finalStep 纯问答（quick-general·注入「直接简洁作答」） | ✅ | 保留·**标注已知风险**（W1·未来若出图层式→改确定性） |
| **rag_query**（知识检索） | **确定性组装**（条目式+来源+维度·`_assembleRagResults`·**禁 finalStep**） | ❌ | **改（根治第二根因）** |
| **knowledge_query**（B 路径·CB-22b） | **确定性查询直答**（query_knowledge_base） | ❌ | 新建（B 路径） |
| **gis_operation** | finalStep 图层（分析模板） | ✅ | 不动（正确） |
| **emotion_analysis** | finalStep 图层（分析模板） | ✅ | 不动（正确） |

**核心原则**：**分析类走 finalStep（图层·Smart LLM）·知识类走确定性组装（Dumb Tool·零 LLM）·FINAL_TEMPLATE 保持不动**（分析专用·D019 守卫）。

## 二、采纳两组

### rag_query 范式（分歧收敛·采纳 glm 纯确定性 + Codex 两级结构）
- **主路径（P0）**：确定性组装（glm B·零 LLM·`_assembleRagResults`·条目式+来源+维度）——根治模板错位
- **兜底（Codex L3）**：开放语义问（Top-K 非全 fact）→ 注入"知识问答排版"指令 + finalStep（LLM 综合·禁图层）——**后置随 B 路径评估**
- **顺序**：先确定性组装（根治本次失败）→ 开放语义兜底随 B 落地

### 落地方式（两组 agree·harness 分支独立）
- `rag_query` 分支（harness.js:978）改：注入成功 → `_assembleRagResults` 确定性组装 → onFinalDone → return（禁 finalStep）
- `_assembleRagResults` 纯函数（条目式 + 来源 + 维度声明）
- **PARADIGM_MAP 显式化**（Codex：emc-patterns.js·`{general:'text_qa', rag_query:'knowledge_qa', knowledge_query:'knowledge_qa', gis_operation:'layer', emotion_analysis:'layer'}`）+ 完整性单测
- 不新建编排层（glm：分支独立·最小改动）

### 承重红线（两组 agree）
- **FINAL_TEMPLATE 不动**（分析专用·D019 <3KB 守卫不触碰）
- diagnose prompt / intent 枚举：零触碰（分类不动·映射在其后）
- 四态出口：rag_query 走 EXIT_CONCEPT（确定性·非新态）·复用 EXIT_CONCEPT 卡机制
- @track：新增函数埋点注册（确定性组装器）

### 回归影响（两组 agree）
- rag_query 改独立分支（零回归）·general/gis/emotion 不动
- **general 标注已知风险**（W1·glm 建议·未来失败同步改确定性）

## 三、实施清单

1. **`_assembleRagResults` 纯函数**（harness.js·条目式+来源+维度·零 LLM）
2. **rag_query 分支改**：注入成功 → 确定性组装 → return（禁 finalStep）·失败 → EXIT_CONCEPT 兜底（已有 R3）
3. **PARADIGM_MAP**（emc-patterns.js·显式映射）+ 完整性单测
4. **e2e 断言更新**：知识问答 → 条目式+来源（负例"无分析图已生成"）
5. **general 风险标注**（KNOWLEDGE.md/注释）
6. 复测「宜昌有哪些更新项目」→ 条目式项目清单 + 来源·无"分析图已生成"
7. pytest 零回归 + 黄金集 3 类

## 四、待 B 路径（CB-22b）
- knowledge_query（确定性 WHERE·"有哪些项目"根治·零 LLM/零模型）
- rag_query 收窄开放语义（保留确定性组装）

---

*claude组（2026-08-09）·分类→范式映射反评价收敛定稿·开始实施。*
