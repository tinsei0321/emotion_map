# PT-CB4 · 全轮完工审计 · Codex（2026-08-19 深夜）

> 审计方：Codex（审计协助组·按章程执行）。对象：T1 `987876ea` + T2 `651966d7` + T3 `b1d3ef27` + 主手回收 `1d6c9836`。分支 EMC_harness_dsh。
> **结论：通过（含 1 条环境观测 + 2 条格式注记 + 1 条中优建议）——无打回项。**

---

## 一 五动作结果

| # | 动作 | 结果 |
|---|---|---|
| T1 | mv/superseded 与裁决表一致性 | **✅ 一致**——三件 R100 rename 至 `_retired/`（零字节改动）与裁决表逐条对应；生成器模板同步更新；活引用扫描 zero（`performance/` 三旧名全仓已无活代码引用） |
| T2 | 卡片七字段 + 锚点抽查 | **✅ 通过（附格式注记 2 条）**——详下 |
| T3 | F_020 注册 + 7 测试 + 6 命中裁决 | **✅ 全部合规**——详下 |
| 全套 | pytest 基线 | **✅ 396+3**（首次跑出 1 failed 属 Windows 环境编码·非代码退化·PYTHONIOENCODING=utf-8 后 396+3 精确达标） |
| 工具 | check_caliber 实跑 | **✅ 6 命中与主手基线逐条吻合**（详下） |

## 二 T1 对账裁决审计

- **裁决表六条**：每条附四证据（git 时间/引用链/行数口径/生成线索）·主手声明已独立复核——审计抽验 #1（137⊇126+伪社区行）、#6（qty 双头/presets 957 点误计）**依据属实**；
- **挂起不硬拍**：presets 同步批 + 其余孤儿登记 = 超白名单项正确挂起——**不越权裁定·纪律好**；
- **生成器同步**：`_ops_gen_ledger_cb39.py` 挂起区三行更新与裁决一致——模板可复跑不漂移。

## 三 T2 契约卡审计（R22 重心）

### 3.1 七字段齐检（机器+人工双查）

K-01~K-04 **七字段全齐**。**格式注记**：K-C1 与 X-01 采用**表格式变体**（列名替代部分字段名）——**语义等价不构成缺陷**（枚举值/作废值的定义=行结构本身·适用=典型用途列·值锚点=来源列），但建议下批补齐「值锚点」显式字段以严格自洽（非阻塞）。

### 3.2 锚点可达性抽查（3 张全通）

| 卡 | 锚点 | 实测 |
|---|---|---|
| K-01 | page2 汇总 §一/§3.2 | ✅ L4/L14/L16/L17 逐字命中「83.3%/47,693/安全+民生」·v12 版本号在头注在位 |
| K-03 | 体检2026补充 §九 | ✅ L185-189 逐字命中「≥15 个/48 社区/≥50 件/47 社区/重叠 10」·L141 用户定稿声明在位 |
| K-04 | 体检2026补充 头部 | ✅ 口径铁律+全覆盖口径注记节在位 |

### 3.3 K-C1 与 08-17 用户三调 / T1 裁决一致性

六枚举值 193/174/137/130/154/1682——与 T1 裁决（#1/#2=137 现行·194 归档）、08-17 全覆盖扩围（154）、空间底座原口径（193/174/130/1682）**全部自洽**。137 被正确新收为 T1 裁决产物（旧表不存在此值——主手吸收及时）。X-01 双高作废（16/32/3/26 各代）与 08-17 用户「取消双高」裁定一致。

### 3.4 漏登检查

对照全链已收敛口径清单——**未发现遗漏的现行口径**。挂起区已如实登记（presets 同步/usage_alt/语义详注 4 条）。

## 四 T3 工具审计

### 4.1 F_020 注册合规

- **连续号**：F_019 为前最大号（paradigm/router 系列）→ F_020 连续 ✅；
- **双埋点**：`register_track_id` + `@track`（`check_caliber.py:32/191`）✅；
- **描述准确**：「读注册表作废值→扫素材→命中即fail清单」与实现一致 ✅。

### 4.2 七测试覆盖

`tests/test_check_caliber.py` 125 行——正常解析/无注册表/无作废值/命中报告/多作废值/边界（BOM/大小写）等场景覆盖 ✅（7 passed）。

### 4.3 6 命中「合规基线」裁决——**成立**（逐条抽查）

| 命中 | 文件:行 | 上下文 |
|---|---|---|
| 87.9% ×4 | page2 L4/L103/L108/L144 | L4=头部「修订 v12」演变叙述行（自带旧值上下文）·L103/L108/L144=§3.3 口径演变表（正是记录这些数字的地方）——**历史演变记录内合规提及** |
| 双高3 | 全域包 L178 | §9 变更叙述「口径已变更（用户定·两表口径·取消双高）」——**变更声明本身引用旧词** |
| 双高 16 | 3prime 总纲 L111 | 该文件已标 superseded（外挂大脑轮收敛已注）——**已作废文档内提及** |

**判定：6 条全部是「记录作废数字本身的文档」命中作废数字——与 X-01 纪律「检索命中→引本卡声明」不矛盾（这些文档正是声明载体）。「门禁真对象=C2/C3 蒸馏素材」的裁决合理——届时命中按纪律改写。**

### 4.4 变体匹配误报/漏报

当前实现=精确子串匹配+BOM/大小写容忍——对本批 6 值已够用。**潜在漏报**：数字千分位变体（如「5615」vs「5,615」）与半角/全角混写——v2 可选增强（非本轮缺陷·X-01 已登记 v2 方向）。

## 五 R22 纪律执行评价

**T2 是 R22（每批必产出契约条目）的首个达标批**——不是数据搬运而是**契约提炼**（四现行口径卡从叙事中抽出算法规格+适用边界+禁用条件+值锚点），X-01 作废清单和 G1-G6 治理规则将 T1 案例经验**升格为可机核条款**。执行质量高：七字段纪律（含两处合理格式变体）、锚点全指成文出处不内联数据（查证可行）、CB39 B3 四种子全覆盖 + 08-17 增量吸收。

**评分：优**——「契约=资产·数据内容=耗材」的纪律转译到位。

## 六 非阻塞观测

| # | 级 | 内容 |
|---|---|---|
| 1 | **环境** | `tests/test_manifest_gate.py` 在 Windows 默认编码（GBK）下 1 failed——**非代码退化**（设 `PYTHONIOENCODING=utf-8` 后精确 396+3）；该测试不在 T1-T3 白名单内（CB2 复审产物·环境敏感）。建议后续加编码自适配或在 CI 层钉死 UTF-8（可派 dsh·0.1h） |
| 2 | 格式 | K-C1/X-01 表格式变体（语义等价·建议下批补显式字段） |
| 3 | 中优 | check_caliber v2 千分位/全角变体匹配（X-01 已登记方向·非本轮缺陷） |

## 七 验收判定

**PT-CB4 全轮（T1+T2+T3）审计通过——零打回项。** 主手可进入收敛定稿。观测 #1 建议随下批顺修（不阻塞本轮）。

> Codex · 2026-08-19 深夜 · 审计协助组 · 只读+落盘·零实施·零 git 写（本文件由主手归档）。
