# 情绪地图 · 整体验收（出口三段式 + 热点图）— 实施检查发起（CB-18）

> **发起方**：claude组 | **日期**：2026-08-08 | **性质**：CB 专题整体验收（两组独立验收）
> **承接**：08-05 两专题 CB 全闭环（出口三段式 P0-P2 + 热点图 P0/P1/P1.5·实施完 + 审计修正完）→ **用户新工作方式：验收交两组走 CB**（2026-08-08 明确）→ claude组 将 todo「整体验收清单」转两组成证据驱动验收
> **目的**：让两组对两专题**整体验收清单**逐项独立验收（代码核验 + 可跑测试 + e2e-seam 直测 + 可选浏览器实测），通过后 P0 闭环 → 进发版就绪度回归
> **验收方式转型**：原清单为「用户浏览器肉眼验证」→ 改为**两组证据驱动**（评估方只读不 git·可跑 pytest/browser 直测·浏览器实测可选不做硬要求）。纯观感类（观点卡是否"干货"/setTerrain 是否"地势感"）两组只能代码核验 + 标注「可选用户观感复核」

---

## 〇、验收对象（两专题实施摘要·claude组 已完成）

**分支** `fix/emc-buglog` @ `5d5e67b` · pytest **291 passed + 5 skipped**（08-08 实测基线）· validate（test_emc_template + validate_skill_params + validate_outlet_fields + validate_trigger_sync + validate_field_dict_sync）**34 passed**

### 出口三段式 P0-P2

| 项 | 落点 |
|---|---|
| P0 观点先行 | `ai_qa/prompts.py` FINAL_TEMPLATE 软扩 `> **观点：**`（观点≠结论·<3000B 门禁）+ `frontend/js/ai_qa/result-struct.js`（结论段独立聚合·不解析 markdown·观点三档兜底）+ `frontend/js/ai_qa/panel.js` 观点卡/4 要点卡 + `frontend/js/ai_qa/harness.js` `_dispatchResultStruct` 三路径（默认 :705 / chain :819 / multi :1962） |
| P1 指标细化 | `ai_qa/outlet_kb/build_outlet_schema.py` `grade_demand_intensity`（四档 L2 归一）+ `priority_score`（p95 复合）+ `core/export.py` `export_outlet_card_csv`（脱敏）+ `frontend/js/ai_qa/panel.js` 导出按钮 + case_library measure_note + limitations 声明 |
| P2 地点 scale | `build_outlet_schema.py` `geo_label`（宏观面域/中观单元/微观落点） |

### 热点图 P0/P1/P1.5

| 项 | 落点 |
|---|---|
| P0 命名定标 | 8 处（Gi\*→显著聚集点/spatial_hotspot→代表地点/热力图统一） |
| P1 软分级 | `core/spatial_analysis.py:110-127` `_classify_hotspot`（五档 hot/tend_hot/ns/tend_cold/cold·threshold 1.96/soft 1.0 参数化）+ `hotspot_tier` + `frontend/js/map.js` `colorMode='hotspot'` 纯橙系显著性符号层 |
| P1.5 setTerrain | `core/spatial_analysis.py` `create_terrain_dem`（F_009·KDE→terrarium RGB）+ `api/routes.py` `/spatial/dem` + `frontend/js/map.js` `setTerrainDEM`（draping 隔离）+ `frontend/js/heatmap-tool.js` `generateTerrain3DForAI` + `frontend/js/ai_qa/tools.js` EMC 委托已切 |

### 审计修正（08-05 已全采纳）
- B1（tools.js EMC 委托切 generateTerrain3DForAI）· W1（纯橙系）· W2/W3（/geo/hotspot 透传 threshold + legend 五档）· W6（契约层五档同步）

---

## 一、整体验收清单（转两组验收·逐项证据驱动）

> 原 todo「整体验收清单」逐项转两组。**验收方式**：代码核验（grep path:line）+ 可跑测试（pytest / e2e-seam 直测）+ 浏览器实测（可选·不硬要求）。每项给出验收判据 + 证据路径。

### P0 验收（观点先行 + 三段式骨架）

| # | 验收项 | 验收判据（两组） | 证据路径 |
|---|---|---|---|
| P0-1 | **观点卡置顶** | 代码核验：result-struct.js 观点提取 + panel 观点卡渲染逻辑存在；prompts.py FINAL_TEMPLATE 含 `> **观点：**` 指令（LLM 必读） | `prompts.py`（观点段）· `result-struct.js` · `panel.js`（观点卡） |
| P0-2 | **4 要点卡底部** | 代码核验：result-struct.js 四段（方法/数据/结果/结论）聚合·结论含学术句式（"数据显示 X 区极性指数…"）· panel 4 要点卡渲染 | `result-struct.js`（四段聚合）· `panel.js`（4 要点卡） |
| P0-3 | **三路径都出卡** | 代码核验：`_dispatchResultStruct` 三处接线（默认 :705 / chain :819 / multi :1962）· 各路径均接 onResultStruct；**e2e-seam 直测**（tests/browser/ 先例 test_r7_truncation.py）可补断言三路径出卡 | `harness.js` 三处 · `tests/browser/` |
| P0-4 | **无观点不显卡** | 代码核验：无 `> **观点：**` 标记 → 观点卡兜底逻辑（W3·不取动作描述当观点）·有单测覆盖？ | `result-struct.js` 兜底分支 · 相关测试 |
| P0-5 | **无 console 错** | 浏览器实测（可选）：F12 无 JS 报错 | — |

### P1 验收（第三段指标细化 + CSV 入库）

| # | 验收项 | 验收判据（两组） | 证据路径 |
|---|---|---|---|
| P1-1 | **需求强度等级** | 代码核验：`grade_demand_intensity` 四档（高/中/低/无显著需求·L2 归一）+ 出口卡渲染字段；单测覆盖 | `build_outlet_schema.py` · `tests/` |
| P1-2 | **复合优先级** | 代码核验：`priority_score`（p95 归一 + 缺省不参与 + 主题 0.5/1）+ 出口卡"优先级排序"渲染 | `build_outlet_schema.py` · `tests/` |
| P1-3 | **CSV 导出按钮** | 代码核验：`export_outlet_card_csv`（脱敏 + outlet_id/name/scale 列 + BOM）+ panel 导出按钮；可跑 `export_outlet_card_csv` 单测 | `core/export.py` · `frontend/js/ai_qa/panel.js` · `tests/` |
| P1-4 | **案例口径标注** | 代码核验：case_library measure_note + limitations "行业案例为对标参照·非评分基准" | `outlet_kb/` |
| P1-5 | **地点 scale 标注** | 代码核验：`geo_label`（宏观面域/中观单元/微观落点） | `build_outlet_schema.py` |

### P2 验收（地点联动）

| # | 验收项 | 验收判据（两组） | 证据路径 |
|---|---|---|---|
| P2-1 | **结论段带地点** | 代码核验：结论段聚合含地点（宏观面域名/中观单元名/微观 POI·按尺度）+ place_name 双源 | `result-struct.js` · `spatial_analysis.py` place_name |

### 热点图重做验收（另一专题）

| # | 验收项 | 验收判据（两组） | 证据路径 |
|---|---|---|---|
| H-1 | **setTerrain 连续曲面** | 代码核验：`create_terrain_dem` terrarium 编码 + `/spatial/dem` + map.js setTerrainDEM（draping 隔离）；DEM 解码单测 | `spatial_analysis.py` · `routes.py` · `map.js` · `tests/` |
| H-2 | **显著聚集五档橙系** | 代码核验：`_classify_hotspot` 五档 + `colorMode='hotspot'` 纯橙系符号层（弃极性色）+ legend 五档 | `spatial_analysis.py` · `map.js` |
| H-3 | **命名定标** | 代码核验：8 处（hotspot-tool「显著聚集点(Gi*)」/grid-tool「显著聚集(Gi*)」/panel「代表地点」/test-cases 语料/index/spec）无"热点"旧义残留 | 8 文件 grep |
| H-4 | **网格柱互斥 + 无露底** | 代码核验：setTerrain 与 3D 网格柱互斥 + sky 层（draping 隔离） | `map.js` |

---

## 二、验收焦点（两组独立验收）

### 焦点 1：验收清单完整性
- 上述 P0/P1/P2/H 清单有没有遗漏？（对照 todo「整体验收清单」原文 + 08-05 审计修正 B1/W1/W2/W3/W6）
- 有没有该验但两组证据驱动**验不了**、必须用户观感复核的项？（观点卡是否"干货"/setTerrain 是否"地势感"——建议标注"可选用户复核"）

### 焦点 2：证据充分性
- 各项证据路径够不够支撑"通过"判定？哪些项需要补 e2e-seam 直测（先例 test_r7_truncation.py）？
- 特别关注 P0-3 三路径出卡——B1 补丁只被 pytest 覆盖未被 B3 覆盖（Codex 08-08 提醒），两组是否有 e2e-seam 直测建议？

### 焦点 3：验收方式转型
- 从"用户浏览器肉眼"→"两组证据驱动"转型，有没有风险？（如纯渲染类/交互类无法完全代码覆盖）
- 浏览器实测（评估方可选跑）哪些项值得做？哪些项代码核验已充分？

### 焦点 4：回归范围衔接
- 验收通过后进**发版就绪度回归**（B3 三连 fail 集判据 {PRM-03/04/07} + RST-L06 + eval 复采 + link_checkup + 三路径观点卡浏览器抽验）——验收与回归的衔接顺序/边界有无建议？

### 焦点 5：两组补充与异议
- 对验收清单/证据路径有无异议？（用错标尺/事实错误/承重红线→指出）
- 各自环境（Codex/glm组）有无已跑测试可复用为验收证据？

---

## 附 A：整体验收 prompt（发两组用）

```text
【CB-18 · 整体验收实施检查】情绪地图 — 出口三段式 P0-P2 + 热点图 P0/P1/P1.5

你以第三方独立验收身份（不做项目方决策背书）。**读本地文件即可·无需 git pull/push**（评估方只读·claude组 负责 git·工作区已同步）。可跑只读测试（pytest / browser e2e-seam 直测）作为验收证据·浏览器实测可选不硬要求。

背景：08-05 两专题 CB 全闭环（实施完 + 审计修正全采纳）。用户新工作方式 = 验收交两组走 CB（2026-08-08）。现请对「整体验收清单」逐项独立验收。
必读：
  docs/catch-ball/discuss/整体验收_实施检查发起_2026-08-08.md（验收对象 + 清单 + 5 焦点）
  docs/todo.md「📋 整体验收清单」（原文）
  08-05 审计修正记录（B1/W1/W2/W3/W6·见 docs/todo.md 08-05 段 / revision-log §5）
按需核实：ai_qa/prompts.py（观点段）· frontend/js/ai_qa/{result-struct,panel,harness}.js ·
  ai_qa/outlet_kb/build_outlet_schema.py · core/export.py · core/spatial_analysis.py（_classify_hotspot/create_terrain_dem）·
  api/routes.py（/spatial/dem）· frontend/js/map.js（setTerrainDEM/colorMode='hotspot'）· tests/browser/（e2e-seam 先例）

验收任务（5 焦点·逐条 agree/disagree/partial + 证据 path:line + 建议）：
1. 验收清单完整性（P0-1~5/P1-1~5/P2-1/H-1~4·有无遗漏？哪些必须用户观感复核？）
2. 证据充分性（各项证据路径够不够？哪些需补 e2e-seam 直测？三路径出卡 P0-3 怎么测？）
3. 验收方式转型（"用户肉眼→两组证据驱动"有无风险？浏览器实测哪些值得做？）
4. 回归范围衔接（验收通过→发版回归·顺序/边界建议）
5. 两组补充与异议（清单/证据异议·各自环境已跑测试可复用？）

核心标尺：演示逻辑链（张力图面→引导点击→交互分析→宏观诊断信号）/ 出口三铁律 / AI·Copilot 内核。
承重红线：四态出口 / finalStep D019 / diagnose 永不动——验收只读不触实施·确认无违反。
产出：docs/catch-ball/discuss/整体验收_验收回应_{你的组名}_2026-08-08.md
格式：〇 一句话结论 / 一 逐项验收（通过/需补证/需修复·附证据）/ 二 新问题风险清单（阻断/警告/建议）/ 三 可否整体验收通过结论。
报告中文为主，代码/路径保留英文。
```

---

*claude组 · CB-18 整体验收实施检查发起 · 2026-08-08*
*验收方式转型：用户肉眼 → 两组证据驱动（代码核验 + 可跑测试 + e2e-seam 直测 + 浏览器可选）· 通过后进发版就绪度回归*
