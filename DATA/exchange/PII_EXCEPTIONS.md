# PII 例外清单（阶段 0' 交付）

> **红线**：数据脱敏——PII 剥离·照片 URL 不入库·门牌级降社区/小区。验收扫描见 README 四、校验命令 #2。
> 实读日期：2026-08-11（zcode 中转站 v1.1.0）。

## 一、PII 实读结果

| 层 | 要素数 | PII 字段 | 实读结果 | 处置 |
|---|---|---|---|---|
| `building_50year_1.geojson` | 380 | `yslxr`(验收联系人)/`yslxrdh`(验收电话)/`ysbm`/`yspd`/`ysyj` | 字段 380 要素全存在·**值均为空格（无真实人名/电话内容）** | **只引用不复制**·派生层剥离 5 字段 |
| 住房维度_面层 8 个 | 42~454 | `存在问1`(现场照片 URL·鉴权外链) | 全部要素非空（454/240/6/186/42/25/39/31） | 不入库展示·副本/派生剥离 |
| 小区维度_点层 11 个 CSV | 2~273 | `存在问1`(照片 URL) | 全部含该列 | L1 导出剥离该列 |
| `中心城区250栋危旧房.geojson` | 250 | — | 无个人字段（fwbh/fwlb/mc/jzmc/dscs/dcmj/gd/build_time） | ✅ 干净 |
| `中心城区建筑汇总霞`/`建筑2001至2005年` | 46845/17941 | — | 同 250 危旧房字段集·无个人字段 | ✅ 只引用 |
| 01_文本 CSV 6 个 | — | — | 无个人身份字段（责任单位/部门非个人） | ✅ 副本安全 |
| 点层/面层 `标准地`(标准地址·门牌) | — | 门牌地址 | 公开信息·非个人身份 | 报告落位用社区/小区名替代门牌级 |

## 二、结论与纪律

1. **`building_50year_1.geojson` 原始层禁止同步进 git**（Codex M3 修正版·字段存在即风险·不因值空而放松）。
2. **派生层（50 年建筑脱敏精简层）必须剥离** `yslxr/yslxrdh/ysbm/yspd/ysyj`，仅保留空间几何 + 关键属性（fwbh/dscs/dcmj/build_time 等）。
3. **`存在问1`（照片 URL）不入库展示**：L1 导出去列·前端不渲染该字段。
4. **门牌级地址**：公开非敏感，但报告落位以社区/小区名为主，门牌级仅作定位辅助。
5. **验收扫描**（阶段 1 后必跑）：
   ```bash
   grep -rE "yslxr|yslxrdh|存在问1" DATA/performance/ DATA/boundaries/ docs/urban-renewal-plan/00-宜昌专项/ || echo "[OK] PII zero residue"
   ```
