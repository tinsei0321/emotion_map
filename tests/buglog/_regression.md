# 回归关注清单（resolved · 自动生成）

> 自动生成（`py tests/buglog/_gen_index.py`）·勿手改。已修复 bug 的标准化用例 + 根因指针。
> 供发版前**手动复验**。非飞轮自动执行——数据前提逐案不同（如 B001 需带字段 MC 的面层），
> 无法从语义描述自动装配；关联飞轮用例（case_ref）在常规跑中已覆盖执行。

## 用例速查

| ID | 标题 | 关联用例 | 模块 |
|:-:|------|:-:|:-:|
| B001 | 多要素裁剪失败螺旋（extract_feature 字段重命名断裂 + 单工具限制 + FC 推理死循环） | TC-06 | 数据识别 |
| B003 | LLM 推理螺旋 — 简单查询耗时异常（复现） | TC-22 | FC诊断 |
| B005 | 多步操作链 — 只做一半停下（部分新） | TC-24 | 工具调用 |
| B006 | 意图理解偏差 + 图层样式不匹配（部分新） | TC-25 | FC诊断 |
| B007 | 图层类型混乱 — 声称面层实际产出点层（部分新） | TC-26 | 工具调用 |
| B009 | 回到底部按钮位置+样式不当（太显眼·右下→右上） | — | UI |
| B010 | 飞轮测试数据层命名混乱 — L2·e2e 不知所云 | — | UI |
| B011 | 飞轮测试每例重复加载行政区 — 图层堆叠 | — | 工具调用 |

## 标准化用例（问句 + 预期，供手动复验）

### B001
- **问句**：「帮我从中心城区范围中裁剪出西陵 + 伍家岗的范围」
- **预期**：`extract_feature` 被选中且 `where` 正确引用字段名（`MC` 或重命名后的 `name`） / 西陵区 + 伍家岗区面图层已生成 / 结论诚实描述（不编造未生成的图层）
- **根因**：[2026-07-28-multi-extract-reasoning-spiral.md](../../docs/catch-ball/rootcause/2026-07-28-multi-extract-reasoning-spiral.md)

### B003
- **问句**：「我上传了哪些数据？」
- **预期**：_quickIntent 识别为 general 类问句（非地理分析，是数据清单查询） / 直接短路回答（不走 FC / diagnose），从接地上下文中提取已加载数据清单 / 回答按类型分组（情绪评价点 / 行政边界 / 用地类型 / 水系），附数量统计 / 响应时间 < 5s（简单数据查询，无需推理）
- **根因**：[2026-07-28-multi-extract-reasoning-spiral.md](../../docs/catch-ball/rootcause/2026-07-28-multi-extract-reasoning-spiral.md)

### B005
- **问句**：「将西陵区+伍家岗区范围内商业用地筛选出来。」
- **预期**：Step 1：获取/确认西陵区+伍家岗区合并范围（merge 或 extract_feature 双区） / Step 2：用合并范围 clip 商业用地面层 / Step 3：生成「西陵伍家岗商业用地」面图层（含 2 个面） / 所有步骤执行完毕后再出结论
- **根因**：[2026-07-28-multi-extract-reasoning-spiral.md](../../docs/catch-ball/rootcause/2026-07-28-multi-extract-reasoning-spiral.md)

### B006
- **问句**：「能帮我筛选出西陵区的情绪点吗？」
- **预期**：LLM 正确理解"情绪点"= 三个极性的情绪点（非仅积极） / 方案 A：先 merge 三个极性点层 → 再 clip 西陵区 → 生成「西陵区全量情绪点」 / 方案 B：分别 clip 三个极性 → 生成三个独立图层 / 生成的图层**严格匹配标准图例样式**（颜色/大小/透明度按图例规范）
- **根因**：—

### B007
- **问句**：「筛选出西陵区中的商业用地。」
- **预期**：clip 工具被选中，target=用地_商业（面层），range=西陵区边界 / 生成的图层为**面层**（polygon），而非点层 / 图层类型与源图层一致（面 → 面） / 结论中描述的图层类型与实际产出一致
- **根因**：—

### B009
- **问句**：「（任意多轮对话后上滑查看历史消息）」
- **预期**：上滑离开底部时，右上角出现轻量「↓」按钮（透明磨砂背景·非深色填充） / 点击按钮 → 滚回底部 + 按钮消失 + 恢复自动跟随 / 手动拖拽至底部 → 按钮消失 + 恢复自动跟随 / 新话轮自动复位跟随
- **根因**：—

### B010
- **问句**：「（飞轮测试运行时查看 Layers 面板）」
- **预期**：Layers 面板中测试注入的图层名清晰可辨（如「测试数据 · 情绪点」） / 不使用 `e2e` 等内部代号命名面向用户的图层
- **根因**：—

### B011
- **问句**：「（飞轮运行 25 例 llm 测试·每例调 loadRange('行政区')）」
- **预期**：同一 GeoJSON 文件（行政区.geojson）只加载一次 / Layers 面板不出现多个同名「行政区」面层
- **根因**：—
