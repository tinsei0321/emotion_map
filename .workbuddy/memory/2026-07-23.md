# 2026-07-23 工作日志

> 分支 `main`（cpd 已合并清理）· 测试飞轮 v3→v4 + 分支收敛 + todo 机制反思

## 分支合并清理（cpd → main）

- cpd 分支（~60 commit：CPD 引擎 + EMC v1.4-1.6 + 测试飞轮 v1-3）fast-forward 合并进 main，历史线性
- 删除 cpd 本地 + `origin/cpd` 远程；删 `backup/pre-forcepush-9be02c3`（含未合并提交 9be02c3，用户确认弃）
- 仓库收敛为单 `main` 分支

## 测试飞轮 v3（行内摘要 + 工具标注 + 固定位置报告 + 一键启动）

- 行内摘要：工具类显 tool 名（从 fetch 拦截 /geo /spatial 端点抓，非猜）
- 重跑 R 修复：批量中点 R 先停批量再重跑（不再静默）
- 报告落盘 `tests/reports/report-<日期>-<编号>-<类型>.md`（serve.py `/_test/report`）
- `start.bat` 一键开主页+测试页（serve.py `--open=both`，webbrowser 后台线程）
- commit b63acca

## 测试飞轮 v4（方向纠偏 + 意图/工具各 100）

- **核心纠偏**：意图识别 = NL→可执行工作流转译（范围→筛选→分支→工具→步骤），非回答文本；断言转向 template+工具转译链（chatPhases + geoCalls）
- DATA 资产系统 [test-assets.js](frontend/js/test-assets.js)：语义清单（'行政区'/'二马路片区'/'L2-T1'→文件），llmRun 自动加载，不再让用户补范围
- 意图 100 + 工具 100 生成器（270 总·≤2 工具≤4 步·针对性）；slider 默认 25；存报告覆盖确认；按钮状态机（停止↔重新开始）
- 用户重组 boundaries（presets/ 上移顶层）一并修
- commit 89c6a31

## _fill 中文占位符修复（用户实测发现意图 prompt 全显 {区}）

- 根因：`_fill` 正则 `\w`=[A-Za-z0-9_] **不含中文** → {区}/{要素}/{用地} 全未替换（200 例 prompt 失效；语法绿·计数对·仅输出扫描能查出）
- 全局审查 4 文件正则：仅此一处同类（其他处理 ASCII URL/markup 或用中文 literal）
- 修 `\w+`→`[^}]+`；运行时扫 270 例 0 残留
- memory 防复发：`js-regex-word-chinese-trap`（~/.claude）
- commit 524305d

## 测试报告入库 + 交接

- `tests/reports/`（3 份报告）纳入 git 同步（换环境要用，勿 gitignore）
- commit 81288aa

## todo 机制反思（本日最大教训）

- 用户多次报"todo 不更新"，我反复调 TodoWrite 工具（5 次）无效
- **根因**：看错面板——用户看的是 `.workbuddy` 面板（读 `.workbuddy/memory/` 最新日期文件），不是 Claude Code 的 TodoWrite 工具；07-23 文件缺失 → 面板停在 07-22
- 教训：诊断工具不生效时，应早问"你看的 todo 显示什么内容"定位，而非反复换工具输入
- 用户级 CLAUDE.md 立「Todo 生命周期」规则（定期归档·列表勿空）

## 承重不变
diagnose prompt / 四态出口 / harness 主循环 / tracker 不动 ｜ 改动全在 ?test=1 测试层 + serve dev + 启动器 ｜ 不派 subagent
