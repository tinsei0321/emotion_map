# 2026-08-25

## PT-CB15 渲染链路治本复核（CB 组员身份）
- 复核 Codex 根因报告（渲染残留/幽灵图层），落盘 `docs/catch-ball/discuss/PT-CB15-渲染链路治本复核意见_Kimi-2026-08-25.md`。
- 关键事实：backlog 重放已按用户令关闭（render_routes.py:243）；watcher 有 pidfile 竞争锁（P2-9 双后端实例问题）；inbox 零积压、applied/ 31 件；render_spec 对 dataset_id 只校验注册合法性（TOP7→TOP10 根因=模型选错数据源，守卫无立场拦）；_time_manifest.json 缺失是刻意留白（数据红线留用户，time-source.js:21 注释）。
- 我的机制主张（与 Codex 两处分歧）：①幽灵图层根治不做 turn_id 绑定（地图页无 turn 权威源·需反向通道），改 watcher TTL 300s + 前端连接水位线（spec_id 前缀即毫秒时间戳，connectTs−60s 宽限）；②出图数据源不做硬白名单（误伤演示流），改软校验回显要素数+契约双写+tmp_render_ TTL。
- 注意：代码已演进——/version 徽章端点已实现（PT-CB11 A-4，即我 08-21 设计书 A+-5 建议的落地）；工具面已扩到 F_034（grid_aggregate/compare_regions 等新增）。
- 新条款注意：08-23 用户令——面向用户的报告必须「白话摘要」开篇（零术语+比喻+数字），本次报告已遵守。

## 评价 Qoder 渲染链路复核报告（同日第二轮）
- 落盘 `docs/catch-ball/discuss/PT-CB15-Qoder复核报告评价_Kimi-2026-08-25.md`，总评 A 级。
- 关键时间线：Qoder 的 P0-A（codex_bridge 缺 turn/interrupt=幽灵图层源头）当日被主手采纳，commit f234bab3（08-25 14:10）实施 interrupt+hello+软警示+时间轴降级。我曾差点误判其声明失实（当前代码已有 interrupt），git log -S 才还原「报告在先、修复在后」。
- 结论：四层合并方案 interrupt+hello+turn_ref+TTL，我的 watcher TTL（F1）是唯一未落地的洞，建议补入。Qoder 弱点：turn_ref 依赖模型透传（软机制）+ 行号取证未注基线 commit。

## 新 Harness 出图双 bug 根因分析（同日第三轮·零实施）
- 落盘 `docs/catch-ball/discuss/PT-CB15-新Harness出图双bug根因分析与修复计划_Kimi-2026-08-25.md`。
- Bug2（边界粗糙）铁证：今日 14:17 TOP7 spec 仅 5.4KB、7 社区顶点 4-19 个，但 100% 是权威源（130 层 REGISTRY/presets/体检对象_西陵+伍家岗.geojson·字段名=社区）顶点子集——**非幻觉编造，是模型转录抽稀**（layer_output ~90-100KB 经 LLM 上下文必然有损；服务端 simplify 未触发因 <200KB 阈值）。根治=layer_output 改服务端落盘+返回 dataset_id（几何不过脑子）。
- Bug1（口径旧）：四载体分裂——RAG 索引停在 08-20（早于 08-24/25 DATA 重组四提交）、manifest label/note 仍写「130 社区/两方面合计」、两类点层在册、口径注册表已更 193。模型如实汇报了它实际用的旧层。
- 排查教训：比对脚本字段名不匹配（源用「社区」非 SQMC/name）曾导致「0% 吻合=编造」误判，第三次全字段扫描才翻案——验证脚本的键名假设必须先核实。

## 双 bug 报告 v1.1（14:53 用户四项拍板落进计划书）
- 拍板：①重建 RAG 索引·权威社区=193 含村；②「两个方面」叙事退役（上一任务结论·非数据来源·点层物理保留）；③R1-1 layer_output 接口变更并入当前治本批；④manifest 清洗措辞主手拟稿用户过目。
- 报告已更新为 v1.1：R2-1~R2-4/R1-1 标【已拍板】，待裁决清单清零，可转主手定稿派发。仍零实施。

## PT-CB15 双 Bug 治本执行（16:08-17:35·K1-K9 全完成）
- 定稿 `PT-CB15-双Bug治本执行版任务书_Kimi-2026-08-25.md` + 执行记录同名落盘；门禁 600+1 三轮零失败（前基线 595+4·净增为新增/改写测试）。
- 关键执行事实：①manifest 断链 24 条（非 3 条）——22 修复+1 删死链+1 设计内保留；193 层改指 DATA/AUTHORITY/boundaries_社区村_193.geojson 后 resolve/可见性实测通过。②layer_output 六工具改服务端落盘（tmp_render/）+render_dataset_id；simplify 阶梯退役；nearest 内联例外。③P1-B 软警示对直传产物误报→加豁免（_dataset_meta 补 note 字段）。④RAG 重建首败（transformers 5.14.1×bge 旧模型卡 AutoProcessor 不兼容）→ rag_index._load_st_model 兜底 + HF_HUB_OFFLINE=1，22s 跑完 375 chunk。
- 环境教训：本机 Bash 的 WorkBuddy shim 拦截 os.remove（fail-closed）——跑 pytest/删除类验证须 `PYTHONPATH=` 前缀旁路；真实服务进程无 shim 不受影响。
- 遗留：未 commit（用户/主手代提交·建议前缀 PT-CB15(K1)）；8000 后端载旧码须重启后复测；口径文本草案待用户过目；home 机到岗需重建索引。

## 审计汇总报告书 + 用户汇报（17:50）
- 落盘 `PT-CB15-双Bug治本汇总报告书_Kimi-2026-08-25.md`（供 Codex 审计·含接口变更登记/计划外发现/U1-U6 未了事项/审计验证口径）。
- 服务实况快照：8000 载 b2f9ffb3（本批修复未生效·须重启）；8080 正常（302→地图页）；分支已更名 EMC_Codex_Harness。

## 审计收尾 + 短板计划（18:20-18:35）
- Codex 审计=条件通过（C1-C6）。收尾完成：renewal_unit 甄别入库（nameField=编号·load_preset 注入机制·勿改 Id）、watcher 迟到 spec TTL 300s 落地+测试、transformers 兼容回归测试、gen_progress 刷新、门禁 602+1、commit PT-CB15(K1)（20 文件·未 push·用户执行）。
- 教训：renewal_unit 的 nameField='编号' 是 load_preset 的自动注入触发器——改注册表 nameField 前必须 grep 消费方（tests/test_range_selector_presets 有断言）。
- 落盘 `PT-CB16-短板修复计划书_Kimi-2026-08-25.md`：S1 环境配方 M1-M3（Codex·2天）+S2 索引重建半自动（Codex·0.5天）；C1 E2E 测试骨架（Kimi·2-3天）+C2 交互三机制（Kimi·D1 定稿后 3-4天）+C3 SSE 多页面串台（Kimi·1-2天）。watcher TTL 已消化不再列入。
- 派发 prompt 义务规则已入 MEMORY.md（2026-08-25 用户令：凡给 xx 看/审计/讨论的动作必须附一键复制 prompt）。

## PT-CB16 C1/C3 执行（18:50-19:10·复杂类两件完成）
- C1 E2E 骨架：tests/e2e/test_render_chain_e2e.py（A 级 HTTP 链路 TestClient+B 级真实数据黄金问题顶点 100% 保真+面均≥30 双防线）+ tests/browser/ptcb16_render_e2e.py（栈上验证未完成·已登记）。踩坑：mock 泄漏罩住端点步（端点步须还原真实 resolve·R8）；真实 manifest 复制到 tmp 后相对路径断（file 改绝对路径）。
- C3 SSE 串台：render_client sessionStorage 订阅 id + ?sub= 登记；render_spec 增可选 target 参数；_publish 定向过滤；无 target 广播不变（宽松规则）。render_stream docstring 漂移顺手修。
- C2：D1 讨论稿落盘（PT-CB16-D1-交互三机制讨论稿·追问/选择/规模三机制+三裁决点）·未定稿不实施。
- 门禁 607+1（净 +5）；commit PT-CB16(C1C3) 已提（未 push）。Codex 侧 S1/S2 并行在途。

## D1 交互三机制收敛（19:25）
- Qoder 回应四点调整全部吸收：①裁决点⑤引擎断层（零件在 light 引擎·Codex 侧未接·brain-adapter-codex L93-98 不透传工具返回值）②param_delta→resolved_params 全量自包含③scale 模型自填→确定性倒推④增 C2-0+顺序 0→3→2→1。
- 我保留一处：rag_query cue 变更走接口锁版本化（新键并存过渡·不直接替换）。
- 落盘 `PT-CB16-D1-交互三机制定稿_Kimi收敛-2026-08-25.md` 并已 commit；唯一待用户拍板=裁决点⑤（推荐 A：本批模型侧引导+bridge 透传单列 C2-4 评估件）。
- 教训（与 Qoder 点名共鸣）：机读结构的真正消费方是前端胶囊一键重放（跳过 LLM），不是模型——「为机读而机读」要警惕。

## C2 交互三机制实施（19:32-19:55·全件完成）
- 裁决⑤按 A 定案+战略定向（cdh 聚焦·dsh 大概率退役·已入 MEMORY.md）。实施：C2-0 交互契约冻结（docs/interaction-contract.md）；C2-3 scale 确定性倒推（13 工具埋点+爆发段 120s 切分+infer_scale+outlet_card scale_check 判对静默）；C2-2 身份卡增【分支请示】【规模自觉】+zonal docstring+RAG 重建（四载体纪律）；C2-1 _action_cues 两级（resolved_params 全量自包含）挂 zonal/rank/grid+rag_query followup_actions 新键（接口锁版本化）+followup.js 对象兼容。
- 判据校准：brief 链 2 类工具属正常（list_data+一件分析）——契约与实现同步改（先写 ≤1 被测试打脸）。
- 门禁 613+1（净 +6）；commit PT-CB16(C2) 已提（未 push）。C2-4 评估件落盘（cdh 侧胶囊/选项 UI：bridge 透传 1 天为主·建议下一批首件）。
- 今日累计提交：PT-CB15(K1)/PT-CB16(C1C3)/D1 收敛/PT-CB16(C2) 四批·全部待用户 push。

## PT-CB17 cdh 产品层四问题治本（20:22-20:50·B1-B4 全做）
- 统一根因：**引擎更替产品层未随迁**（dsh→cdh 后身份/范式/数据产物通道/RAG 语料四剖面断裂）。四问题一根四治：
- B1 身份串味：_codex_cwd/AGENTS.md 产品层太薄→补【身份纪律+回答范式+只读沙箱纪律】；权威副本入仓 docs/cdh/AGENTS.md + start.bat 强制同步（治仓外资产无配方）。
- B2 出图沙箱拦截：codex_bridge thread/start 定参 sandbox=read-only+approvalPolicy=never（脚本路径设计上必失败）→ ③档工具化新工具 aggregate_export（F_044·永久分组注册·非 tmp TTL）。
- B3 排版：范式入 AGENTS.md 三段式 + ai_qa.css 表格规范（网格/表头底/斑马纹）。
- B4 RAG 分层：语料从未含 DATA 目录→loader 增 _load_data_readmes（DATA/README+THEME READMEs·单一权威保持）→385 chunk·检索 Top-1 命中·rag_eval 96.7% 零退化。
- 门禁 616+1；commit PT-CB17(B1B4)。今日共五批待 push。
- 复测要点：身份问答无「读代码改文件」/分层问答命中四层/全量出图走 aggregate_export/三段式+表格网格。

## 旧病复发案（21:08·手绘图复发根因=8600 插座进程陈旧）
- 取证：8000 已是新码（3bfb07c7·20:53 重启），但两条最新 spec（20:57/21:06）仍内联抽稀 geojson——**工具是 8600 MCP 进程供的，它从中午起没重启过**（旧 layer_output 返回内联 geojson）。curl 实测 8600 zonal_stats 旧行为（只有 geojson 键）定案。
- 处置：重启 8600（注意：我的 shell 里 `py` 解析到无 numpy 的解释器——用户终端的 py=Python314 才正常；用绝对路径 Python314 拉起）→ 实测 render_dataset_id/followup_actions 在位。
- 治本两件已提交（bd7834fa）：start.bat 8600 段「在听即跳过」改「先杀旧再起新」（与 8000 同款）；check_server_freshness PORTS 补 8600。
- **教训（R7 变体·应入 debug-memory）**：多进程系统的「重启生效」必须按进程逐个核对——8000/8080/8600 三个进程独立载码，「重启了后端」≠「工具进程也新了」。R7 三态提醒应扩展为「重启三服务」。

## 防旧码体系件（21:22-21:32·R7 扩展落地）
- 四层防旧码体系：①start.bat=全量重置唯一入口（三进程先杀旧再起新+预检打印最新提交）②devcheck.bat=一键体检（三进程载码 vs 最新提交+RAG 新鲜度+判读指引）③emc_status.version=8600 进程自证戳（commit/uptime/stale·60s 缓存·实测可用）④R7 铁律扩展三服务版入 AGENTS.md。
- 门禁 616+1；commit 已提（dfa98173）。注意：当前 8600 是 21:15 重启的（无自证戳代码）——下次 start.bat 后自证戳生效，属正常滚动。

## bat 乱码修复+自检折进启动（21:33 用户两连问）
- 乱码根因：Write/Edit 工具写 bat 均为 UTF-8，cmd 按 GBK 解析→中文全花；且我的 Edit 把原本 GBK 的 start.bat 也转成了 UTF-8（波及）。两 bat 已转回 GBK 实测正常。**教训：Windows .bat 落盘必须 GBK 编码（或 chcp 65001+BOM 但易踩解析坑）——写完必跑 cmd 实测。**
- 自检折进启动：serve.py main 在前端就绪后自动跑 check_server_freshness（子进程·带 [自检] 前缀输出）——直跑 serve.py 绕过 start.bat 的路径也兜住；devcheck.bat 保留给「测试中随时犯嘀咕」场景。
- 门禁 616+1；commit 4db2ba8d。
