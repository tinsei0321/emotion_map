# 2026-07-22 工作日志

## CB-CPD-01 第三方首评（k3 版）已完成

- 被评：docs/cpd-core-plan.md v0.2（CPD 核心引导 plan）
- 产出：docs/catch-ball/SCAN_CPDPlan_01-k3.md（六维评价 + H/M/L 分级建议 + U 系列立场）
- 评价定位：CB 第三方中立评测方（资深前端架构师 + 产品设计师 + 信息可视化专家）
- 权限：只读全项目 + 仅写 docs/catch-ball/ 内文件，无 commit/push

### 核心发现（P0 三个 spec 错误）
1. `.aiq-conclusion` 死信号——cpd-state.js:29 查询该类名，但全前端 JS 无生产者 → curState 永远到不了 S4。改法：换 `.aiq-exit-badge`（panel.js:378 已创建）
2. exit 词表不匹配——plan 用大写 RESULT/GAP/PARTIAL/CONCEPT，harness 实际返回小写 'result/gap/partial/ask/drift'，且 general 短路（harness.js:372/412）不返回 exit 字段 → "exit=CONCEPT" 行永不触发，drift 缺席
3. §4.2 映射表 key 结构性矛盾——以 curState 为 key，但 deriveState 不分层型（任何可见层→S2），"S0有import"/"S1有range" 两行永不可达。改法：key 改为特征信号向量真值表

### 其他要点
- U6 实质答案：引导只服务演示逻辑链"前链路"（装载自动化），交互环未闭合；建议 G3 加"在地图上定位该区域"CTA
- theme var 承重被既有光环实现违反（ai_qa.css:431 硬编码 hex 渐变）
- 尺度诚实（§配套 A）机制成立，但需灰度验证防 revise 率飙升；U7 建议三态分级（无声明=fail/有声明无替代=warn/齐全=pass）
- 承重边界清白：diagnose/四态出口/harness 均未暗触，eval 只测 diagnose 已实证

## CB-CPD-02 第二轮验证评审（k3 版）已完成

- 被评：docs/cpd-core-plan.md v0.3（CB-CPD-01 反评价后修订版）
- 产出：docs/catch-ball/SCAN_CPDPlan_02-k3.md
- 首轮建议核验：15/15 执行或合理处置，CB-CPD-01→v0.3 闭环成立
- 综合评级 B+（与 DeepSeek 独立收敛），agree 可进 P0 测试铺底

### v0.3 新引入问题（修订动作自身产生）
- H1 循环 import 风险：init 恢复"只读 getter"方案 → cpd-guide.js import panel.js + panel.js import cpd-guide.js。解法：依赖注入（panel.js→cpd-guide.js 单向）
- H2 S4 文案动态变量"主题倾向 X×Y，排序第 N"客户端无源 → 降级区域名（_followUps:455 复用）或静态（与 DS 收敛）
- M1 文案"深红/深绿"与线上新色带脱节：tokens.css:25-29 已是 #0F6E56 深青绿/#D85A30 深珊瑚橙，"深红"不存在 → 改"深绿/深橙"
- M2 真值表 hasRange=false+result 组合未标注（深读/导出 CTA 永缺席）
- M3 优先级文字"hasImport 优先一切→streaming"与表格矛盾（应 streaming 第一）
- L1-L5：turnId 断链重置/cpd:reset 后引导错位/纯 AI 层文案/U8 魔数改 is-open 信号/引导态不持久化明文

## CB-CPD-03 第三轮验证评审（k3 版）已完成

- 被评：docs/cpd-core-plan.md v0.4（CB-CPD-02 反评价后修订版）
- 产出：docs/catch-ball/SCAN_CPDPlan_03-k3.md
- 上轮闭环核验：9/9 修订属实（DI 注入/{区域名}降级/深绿深橙/streaming 第一/兼带 CTA/R3/R4/U8/L1/L5）
- 综合评级 A-（上轮 B+），agree P0 测试可立即启动

### v0.4 新引入问题（事件机制组合缺陷）
- H1（高优·G1 必修）general 短路断链：general 轮无 exit 字段（harness.js:372/412）→ finally 守卫不 dispatch，但 _history 照常 +2（panel.js:1126/1182）→ 严格 expectedTurnId+1 去重断链 → 引导永久静默冻结。改法：dispatch 改 `if (settled)` 完成守卫（exit ?? null）+ 去重改单调递增 `turnId > last`；intent 载荷在原方案下是死重
- M1 hasAnalysis 死信号：特征向量定义但真值表零引用；建议加 interpret 行（dock 产图无对话 → "问 EMC 解读这张图"，闭合 dock→EMC 编排环）
- M2 hasVisibleEmotionLayer 谓词不判情绪性：任意可见层都算 → 导入无情绪字段 GeoJSON 会直推"点击深绿/深橙"，演示链第一环断点。改法：谓词加 paint/字段判据
- L1 U8 事实修正：#dock 元素不存在（三工具统一 openParamPanel）；cpd-state.js:58-63 observer 是定位用途不暴露 busy 态 → deriveGuidance 同步查 #param-panel.is-open 即可，无需 observer
- L2 §6.4 #ff9000 硬编码：G3 实现时确认/补 sync-highlight token

### 预期
- v0.5 修 H1/M2 + 吸收 M1/L1 → k3 预期可出末轮 A 级收敛意见

## 端到端体验指导建议（k3 版）已交付

- 背景：CB-CPD 专轨三轮收尾，plan v1.0 定稿（H1 general 断链已修：settled 守卫 + 单调去重）
- 产出：docs/catch-ball/GUIDANCE_E2E-k3.md（指导建议，非评测报告）
- 总纲：端到端 = 四链闭环（装载链/交互环/诊断链/产出链），v1.0 已覆盖前两条
- 架构：三层编排（信号谓词注册表+谓词级测试 / cpd-guide 编排 / 既有执行层）+ Context Fabric（LLM 与引导引擎共用会话快照）+ 事件命名空间规范 + 策略表外置（远期）
- 机制：意图三层+L2 意图链 / readiness 数据就绪度（尺度护栏底座）/ 语义双轨（episodes 挖掘→_quickIntent 规则库）/ 四态出口呈现范式标准化 / 反馈闭环三件套（回放评估+引导漏斗遥测+👍👎）
- Feature（8 个）：F1 演示模式（旗舰，脚本化 live 演示）/ F2 情绪气象播报 / F3 张力雷达 / F4 三端同步 2.0（散文句子级联动）/ F5 时光叙事 / F6 尺度护栏（事前 nudge）/ F7 遥测面板 / F8 What-if 沙盘（远期）
- 推进计划：Phase 0-5（继承 v1.0 P0/P1/P2 骨架），M1-M4 里程碑 + 验收硬标准
- 已存在的底座：L1=MANIFESTO/L2=wisdom.py/L3=episodes.jsonl 三层自成长闭环（api/main.py:81-82）
