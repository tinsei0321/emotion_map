# 2026-08-23 工作日志

## OPUS-5 CB 讨论回应（kimi 组·零实施零 git）
- 落盘 docs/catch-ball/discuss/OPUS5提示词工程吸收_回应_kimi-2026-08-22.md（按发起档案命名纪律用 08-22 日期）。
- 总判断：同意报告「方法论验证为主·增量仅 B2/B3/B5·不新开专项」；两处修正：
  1. F3/B3 时间差——superseded 检索层硬排除已由泳道②落地（rag_index.py:458/494·注释「loader 契约：检索层职责」），门禁 96.7% 在该行为下验证；真正增量收窄为返回结构层加 suppressed_count 统计（rows 当前不带 status·因返回全 active）。
  2. F2/B2 成本低于预估——原则②已被 R24+lineage 覆盖为超集（≈0 成本）；落点=rag-corpus-map.md 五步流程第 2 步（L1 所建承载点现成）。
- 表述校准：「判定三分法」建议引用 L3 行为式原始定义（以作废数字为现行结论=superseded/历史叙述提及=active/不确定=待裁），标签式概括易在增量期误标（L1 的 03-10 误读即此模式）。
- 裁决：A2/A3/B1 吸收·A1/B2/B3(修正形态)并入排期·B4/B5 暂缓·C1-C6 反对吸收；D1/D2/D3 全同意。
- 可复用经验：F1 立场依据=护栏 5 教训「门禁价值在判定面代表性不在样本数量」——反例对须 miss 案例驱动逆推。

## RAG 方向 CB 讨论回应（Kimi 组·零实施零 git·今日收敛）
- 落盘 docs/catch-ball/discuss/RAG方向讨论回应_Kimi-2026-08-23.md。
- 总判断：同意主手「A 立即+C 规划+B 缓议」，但 B 缓议换理由——非裁定边界（工具内自治可辩护·三判据：循环封闭/输出契约不变/预算硬顶），而是需求证据+循环级埋点双缺（Neo4j：agentic 故障面多环节需 loop depth/stop reasons 埋点）。
- 真读四篇：Neo4j Agentic RAG（五模式·Corrective=换源非重查）+ Karpathy LLM wiki（raw→wiki→schema 三层·EMC 已同构）+ 补盲 RAPTOR（arXiv 2401.18059·摘要树·EMC 事实卡=手工 RAPTOR 顶层·暂缓）与 A-MEM（arXiv 2502.12110·NeurIPS2025·memory evolution=口径漂移温床·必须提案-审批两段）。
- D3 审批位设计（可复用）：三级分流——L1 派生资产自动通过（可重建即正确）/ L2 事实层人审必过（非抽检·L1 误读实证抽检放不过）/ L3 待裁队列；agent 写 inbox 草拟层不直写语料；superseded 标注权永不自动化；口径注册表 agent 只读禁区。
- 裁决：A 吸收（confidence=top1 分+margin 三档·retry_cue 复用 followup_cues 派生）·C 并入排期（草拟层吸收/evolution 自动化反对/RAPTOR 暂缓）·B 暂缓·A 子项多路召回暂缓。

## PT-CB9R-A2 信号字段族（commit f09a5620·收敛后第一优先件）
- rag_query/kb_facts 增 confidence 三档/coverage_hint/thin_result/retry_cue——零 LLM 零二次检索。
- 两个关键工程判断（执行记录 §二）：①置信读 dense_score 不读 score——RRF 秩分不承载语义（离题 0.0206 vs 相关 0.021 不可分）·rag_index.search 两路透传加权前 raw_cos（原始余弦离题 0.44 vs 相关 0.64 分离干净）；②margin 只调文案不调档——离题/相关 margin 区间重叠·降档会冤杀一强多弱正常命中。
- 阈值 _RAG_SIG 可观测参数（dense_high 0.62/low 0.50·实测校准）；coverage_hint 加「>=3 条」防 1/1=100% 噪声；A2 三态分流闭环（rag_query 低置信→kb_facts / kb_facts 零命中→rag_query）。
- 红线三连：build 摘要逐字一致→520 绿（517+3）→rag_eval 96.7% 逐类全同。待裁 1 项：离题查询的相邻维度提示语义牵强（列观察项未自改）。
- 注意：tests/test_rag_gate.py、docs/progress.md 有他组并发改动（未入我 commit·显式路径纪律）。

## S3 壳对话框架事件化改造（壳阶段联合任务书 v1.0·Kimi 串行首件）
- 前置侦察落盘 docs/catch-ball/discuss/S3-壳对话框架耦合面侦察与事件化设计_Kimi-2026-08-23.md：四层直连（panel→harness→stages→api）·14 hooks 归位 ACP 五族（10 process/4 content）·四隐藏坑（闭包状态 9 件共享/onRound no-op/quick-rag 无 diagnose/onAskUser 副作用序）。
- 实施：新模块 frontend/js/ai_qa/acp-channel.js（createAcpChannel·bus+emitter·turn_id 贯穿·provenance=real·分层 lane）；panel.js buildHooks 机械重构——14 hooks 体改 bus.on 订阅（闭包内·状态共享）·返回 emitter（legacy 签名·harness 零改动）·shell._acp 注入点预留。onOptimize 独立流不动。
- 验证：node 单测 14 断言全绿（_tmp/s3_acp_channel_test.mjs）；E2E（_tmp/s3_e2e_accept.py·/chat 按 phase mock·rag_search 走真后端）最终答案/reason 块渲染通过·截图证链路通；console error 三类=shader/瓦片超时/图层 0=环境性（对照实验因 git 事件中断未跑成·推断非本件）；C3 面板未验成（图层 0）。
- ⚠ 未 commit——见下方 git 事件。

## ⚠ git 对象库损坏事件（15:55·本日最大坑）
- 诱因：一次 git gc 中断（gc.pid=38676/BF-202608101011 死进程）→ refs/heads 清空+274MB tmp_pack 截断+08-22 晚后 commit 对象整链缺失。
- 抢救：unpack-objects -r 救回 11,744 松散对象；stale gc.pid 已清；pack-064a3518 缺 idx 隔离 _tmp/pack_quarantine；工作树零损失；reflog 完整到 5c0b593e。
- 恢复=用户终端两条：git fetch origin + git update-ref refs/heads/EMC_harness_dsh origin/EMC_harness_dsh（沙箱无凭据做不了）。若 S6/S129 未 push 则照 reflog message 重 commit。
- 仓外备份 D:/Github/_emc_rescue_backup_20260823/（S3 全件+讨论文档+记忆+Qoder 在途件）。
- 事件记录落盘 docs/catch-ball/discuss/事件记录-git对象库损坏与恢复指引_2026-08-23.md（含预防三条：并发仓禁窗口期 gc/commit 尽快 push/到岗 fsck 体检）。
- 教训：stash 对照实验做到一半 git 崩——重大操作前应先确认仓健康（fsck）；多组并发写同一仓时 gc 风险被低估。
