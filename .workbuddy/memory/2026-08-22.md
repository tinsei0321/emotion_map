# 2026-08-22

## PT-CB11 K3 · A-4 版本徽章三件套交付
- commit 6cdf443a（代码+测试）/ 4b2e001d（执行记录+2 验收截图入 docs/catch-ball/discuss/）；push 仍待用户本机（沙箱无 gitee 凭据）。
- 实现要点：/version 端点启动时 subprocess 缓存一次（模块级 _VERSION_INFO）；角标锚 #map 左下 bottom:96px（左下控件上方·attribution 实际在右下）；A-4c 横幅首访只记录不打扰、点击关闭、更新记录后不重现。
- 验收手法（可复用）：serve.py `--backend-port` 起隔离栈（8090/8009）零干扰用户常驻 8080/8000；headless playwright 验角标/横幅/截图；「commit→重启栈→角标变」闭环证明启动缓存语义。
- 门禁：483 passed+2 skipped（派发基线 457+7 环境失败·本机未复现·零新增）。
- 发现：K1/K2 已被回收合并（zcode 终裁考核优）；本地 HEAD 前进到 bf8c2ab3 时含 Codex F_036 nearest/F_038 overlay（C2 批）。

## Harness 选型讨论回应（Kimi·产品视角）
- 落盘 `docs/catch-ball/discuss/EMC-Harness选型回应_Kimi-2026-08-22.md`（未 commit·随主手回收）。
- 核心立场：有条件支持主手（dsh 主舱+Codex spike）；议题2=3080 价值拆两半（过程叙事难替代/结果核验在 8080 与 harness 无关·K1 验收全程未开 3080 实证）；议题4=双脑≠双倍用户复杂度（前提=第二脑不进演示路径）·真风险是 M 配方债+人设漂移；挑战主手两点=同批立「EMC 自持对话框」纯评估件 + 试点两周时限纪律。

## PT-CB9 RAG 重构讨论回应（Kimi·产品视角）
- 落盘 `docs/catch-ball/discuss/PT-CB9-RAG重构讨论回应_Kimi-2026-08-22.md`（未 commit·随主手回收）。
- 核心立场：议题2=A2 双跳可设计消除（主防线工具描述调用前分流·附注改结构化字段 caliber_ref 变改道）；议题4=cue 价值序维度相邻>口径关联（文案翻译禁黑话）>小节邻接（降级兜底）·四红线（有据/是问题非答案/≤3条/固定序）·无 cue 省略字段；议题1=前注按权威资产进 git·索引才按派生重建（防前注重生成致漂移）；最虚件=A3 重排（小语料边际收益·排末位+Recall@5<5% 判据写死）；提醒 H1 与前端 _followupCue（harness.js:67）层级边界。

## PT-CB9 泳道① 内容治理交付（commit 475cc3df）
- 语料地图 docs/rag-corpus-map.md（实跑 364 chunk）+ status（3prime 15 数据文件 38 chunk superseded·03-10 §一为据·总纲待裁豁免）+ lineage（67/68 事实卡·逐卡 token 验证·_tmp/l1_lineage_match|resolve 两脚本可复用）+ load_chunks() 契约签名 + tests/test_rag_loader.py 6 用例。
- 关键边界：search/build_index 零触碰（泳道② Codex 分区）；chunk text 零改动→索引免重建→门禁零翻转；build_index metas 未透传 status/lineage=泳道②接 load_chunks() 的已知缺口（记录 §五）。
- 并发情况：泳道② L2 三提交（BM25/RRF/A2+H1+H2）在途合入零冲突；rank_bm25 已在 requirements.txt:71 但本机未装→门禁 ModuleNotFoundError（非我方改动）·pip 装后全绿；office 机到岗需同装。
- 全量 516 passed+2 skipped（基线 508+4·+8）；docs/progress.md 他组在途改动未触碰。
- 待裁 6 项入执行记录 §四（总纲 status/三体检笔记互标/case 谱系/概念卡/IDENTITY 豁免/corpus-map 同步纪律）。
- ⚠️ 后续修正（当日回滚）：L1 的 3prime 批量压旧系语义误读（03-10 原文=「新区增量·空间互斥·直接相加」=增量并存非取代）——主手回滚为全 active·规则化进 debug-memory R24（治理标注必引原文句子）；lineage 67 卡维持有效。

## PT-CB9 泳道③ 工程收尾交付（commit 892d22b7·双审计件·R24 redemption）
- 三件：①--build 内串 rag_ctx_prefix.generate()（增量·实测零 LLM 调用·摘要行 chunk374/前注59/superseded2）②content_hash 覆盖正文+前注（全仓 grep 无下游消费·零破坏）+前注质量门禁测试（护栏5·黄金集关联取样——初版等距取样命中弱前注·改取样面=R8.1 正向应用）③X-01 逐条登记 _SUPERSEDED_SOURCES 2 条（占比表公服 1068 误计值结论+B3B4 双高 16 格结论·各附原文引句）+active 登记 8 项+待裁 3 项（总纲#3#4 定性/素材表图层退役/U06 期望收紧）。
- 关键认知：注册表/素材表已在索引白名单内——X-01 值大量命中=治理卡自身（登记 active）；只有「以作废数字为现行结论」才 superseded。
- 红线三连：build→517 passed+2 skipped（基线 514+4 不降）→rag_eval 96.7% 逐类全同零退化；data/rag_index 未跟踪（派生资产·一条命令重建）；docs/progress.md 他组在途未触碰。

