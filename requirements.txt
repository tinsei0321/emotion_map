# emotion_map 核心依赖
# 安装: pip install -r requirements.txt
#
# 生成日期: 2026-06-12
# 环境管家 @ops 维护，版本号与实际安装保持一致


# ── 数据处理 ──
pandas==3.0.3
numpy==2.4.6
altair==6.2.1
tqdm==4.68.2

# ── 地图与空间分析 ──
folium==0.20.0
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2

# ── 情绪分析 ──
snownlp==0.12.3
jieba==0.42.1

# ── 数据采集（2026-06-12 新增）──
scrapy==2.16.0
requests==2.34.2

# ── API 服务（2026-06-16 新增 — 溯佰科接入前置）──
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# ── 测试框架（2026-06-16 新增）──
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0          # FastAPI TestClient（端点测试，如 /spatial/* 空间聚合）

# ── 空间统计分析（2026-06-16 新增）──
libpysal>=4.12.0
esda>=2.7.0
scipy>=1.11.0          # 核密度 KDE（gaussian_kde）；亦为 libpysal/esda 依赖，显式声明
h3>=4.0.0
matplotlib>=3.8.0      # 情绪地形 KDE 等值面提取（contourpy 随附；core/spatial_analysis.create_terrain_mesh）

# ── 地点搜索 / 地理编码（2026-06-25 新增 — Phase 2）──
# rapidfuzz：本地 POI 模糊匹配（core/place_layer.py 用）。py3.14 无 wheel 时
# 自动回退 difflib（place_layer 已实现回退），故为可选；有则更快更准。
rapidfuzz>=3.0.0
# pypinyin：POI 名拼音匹配（输 wd/wanda → 万达；Search v2）。纯 Python 无 C 扩展，
# py3.14 友好；不可用时 forward 退化为仅汉字/类别匹配（place_layer 已守 _HAVE_PYPINYIN）。
pypinyin>=0.50.0

# ── 数据库（2026-06-16 新增 — SQLite 替代 CSV）──
# sqlite3 为 Python 内置模块，无需额外安装
# 如需 SpatiaLite 空间扩展，安装: pip install spatialite

# ── 多模态分析（2026-06-16 新增）──
# requests 已在数据采集中声明，多模态 API 调用共用
# Pillow>=10.0.0     # 图片预处理（OCR 需要时启用）
# websocket-client   # 讯飞 OCR/ASR WebSocket 连接（接入 OCR/Audio 时启用）
ijson>=3.2        # 流式读大三调用地 GeoJSON（ingest_landuse_preset.py；不装则回退 json.load）

# ── Excel 读写（CB-40 补登记·2026-08-16）──
# openpyxl：实装未登记的依赖漂移补正——3 个先例脚本已用（gen_tidian_mapping_xlsx.py /
# update_page46_excel_westlian.py / CB-35 审计），G1 行业表格式导出（xlsx 路线）复用。
openpyxl==3.1.5

# ── MCP server SDK（PT-CB5 T3·EMC 标准插座 FastMCP stdio）──
mcp>=1.0.0,<2.0.0    # FastMCP 位于 mcp.server.fastmcp；2.0 移除该模块故锁 1.x
sentence-transformers>=2.0.0      # RAG 检索验证测试依赖（validate_rag_material·08-22 环境补装销项 N3）
rank_bm25==0.2.2      # RAG 混合检索 BM25 侧（PT-CB9 泳道②·纯 Python 零重依赖·Py3.14 兼容性待 office 机核）

# ── ACP schema 校验（壳阶段 S6·2026-08-23）──
jsonschema>=4.0      # tests/acp_schema JSON Schema 校验桩（draft 2020-12·双机重建声明·本机已装 4.26）
