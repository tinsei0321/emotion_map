# emotion_map 核心依赖
# 安装: pip install -r requirements.txt
#
# 生成日期: 2026-06-12
# 环境管家 @ops 维护，版本号与实际安装保持一致

# ── Web 应用 ──
streamlit==1.58.0

# ── 数据处理 ──
pandas==3.0.3
numpy==2.4.6
altair==6.2.1
tqdm==4.68.2

# ── 地图与空间分析 ──
folium==0.20.0
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2
pydeck

# ── 情绪分析 ──
snownlp==0.12.3
jieba==0.42.1

# ── 数据采集（2026-06-12 新增）──
scrapy==2.16.0
requests==2.34.2

# ── 多模态分析（2026-06-16 新增）──
# requests 已在数据采集中声明，多模态 API 调用共用
# Pillow>=10.0.0     # 图片预处理（OCR 需要时启用）
# websocket-client   # 讯飞 OCR/ASR WebSocket 连接（接入 OCR/AudioAnalyzer 时启用）
